# Бенчмарки, фаззинг и продвинутые методы тестирования

В этом файле собраны уникальные упражнения по теме **"Бенчмарки, фаззинг и продвинутые методы тестирования"**, отсортированные по возрастанию сложности.

---

1. Напишите простейший бенчмарк `BenchmarkAdd(b *testing.B)` с циклом `for i := 0; i < b.N; i++`. Запустите его через `go test -bench=.`.

2. Используйте `b.ResetTimer()`, чтобы исключить время setup-операций из измерений.

3. Запустите бенчмарк с флагом `-benchmem`, чтобы увидеть количество аллокаций памяти и выделенных байт.

4. Используйте `b.ReportAllocs()` внутри бенчмарка для явного указания, что нужно отчитываться по аллокациям.

5. Напишите два бенчмарка для одной функции с разными входными данными и сравните производительность.

6. Оптимизируйте функцию (например, замените `append` на предаллокацию слайса) и докажите улучшения через бенчмарки.

7. Используйте `benchstat` (`golang.org/x/perf/cmd/benchstat`) для статистического сравнения результатов двух запусков бенчмарков (старая vs новая версия кода).

8. Создайте бенчмарк с `b.RunParallel(func(pb *testing.PB) { ... })` для измерения производительности в многопоточном режиме.

9. Изучите `b.Loop()` (появился в Go 1.24) — более элегантную замену цикла `for i := 0; i < b.N`.

10. **Написание бенчмарка**: Напишите две функции конкатенации слайса строк в одну большую строку: первая использует обычное сложение `+=` в цикле, а вторая использует `strings.Builder`. Напишите для обеих функций бенчмарки (`BenchmarkConcat1` и `BenchmarkConcat2` на основе `testing.B` и цикла `b.N`) [341]. Запустите замер производительности и аллокаций памяти с помощью команды: `go test -bench=. -benchmem` [341]. Проанализируйте результаты.

11. Используйте `testing.AllocsPerRun` для проверки, что функция делает ровно ожидаемое количество аллокаций.

12. Напишите fuzz-тест `FuzzParseJSON(f *testing.F)` для функции-парсера. Используйте `f.Fuzz(func(t *testing.T, input []byte) { ... })`.

13. Добавьте начальные значения (seed corpus) с помощью `f.Add([]byte("..."))` — это известные валидные/невалидные примеры.

14. Напиши тест для `sync.Mutex`-защищённого счётчика. Запусти 1000 горутин, каждая инкрементирует 1000 раз. Проверь, что итог = 1,000,000. Запусти с `-race`.

15. **Сброс таймера в бенчмарках**: Напишите бенчмарк для функции, которая перед началом работы требует тяжелой подготовки данных (например, генерации массива из 1 000 000 случайных чисел). Используйте метод `b.ResetTimer()`, чтобы время этой подготовки не учитывалось в итоговом результате работы бенчмарка [342].

16. Напиши тест, который **намеренно ловит data race**: удали `sync.Mutex` из счётчика. Запусти `go test -race`. Покажи, что race detector выдаёт подробный отчёт с stack trace'ами.

17. Изучите директорию `testdata/fuzz/FuzzParseJSON/`, куда Go сохраняет найденные "краш-кейсы" (inputs, вызывающие падение).

18. Напиши тест для канала: запусти producer и consumer в разных горутинах. Producer отправляет 100 значений, consumer суммирует. Используй `sync.WaitGroup` для синхронизации завершения.

19. Напишите fuzz-тест для функции валидации email-адреса. Попробуйте найти входные данные, которые вызовут панику или бесконечный цикл.

20. Напиши функцию `SumSlice(nums []int) int`. Напиши бенчмарк `BenchmarkSumSlice(b *testing.B)`: `for i := 0; i < b.N; i++ { SumSlice(data) }`. Запусти `go test -bench=. -benchmem`.

21. Напиши тест для **"happens-before"**: `var x, y int`. Горутина 1: `x = 1; y = 2`. Горутина 2: читает `y`, затем `x`. Покажи, что без синхронизации порядок не гарантирован. Запусти много раз — иногда `y` прочитается раньше `x`.

22. **Аллокации памяти (`-benchmem`)**: Запусти бенчмарки из упр. 417 с флагом `go test -bench=. -benchmem`. Изучи колонки `B/op` (байты за операцию) и `allocs/op` (выделения памяти). Осознай, почему `strings.Builder` лучше.

23. Создайте fuzz-тест, который проверяет инвариант: `Decode(Encode(x)) == x` (round-trip property).

24. **Параметризованные бенчмарки**: Напишите бенчмарк, который измеряет скорость сортировки слайсов разного размера (10, 100, 1000 элементов). Реализуйте это с помощью суббенчмарков `b.Run` в цикле, передавая размер слайса в качестве параметра.

25. Напиши тест для **"channel deadlock"**: две горутины пытаются записать друг в друга. Используй `select` с `default` или таймаутом, чтобы избежать deadlock в тесте. Проверь, что deadlock не произошёл.

26. Используйте `f.Add` с разными типами данных (int, string, bool) в одном fuzz-тесте для тестирования функций, принимающих множество типов.

27. Напиши тест для **context cancellation**: запусти горутину с `ctx.Done()`. Отмени контекст. Проверь, что горутина завершилась в течение 100ms (через `select` + `case <-done:`).

28. Напишите fuzz-тест для вашей JSON-сериализации и найдите edge cases, которые ломают парсер.

29. **Сброс таймера (`b.ResetTimer`)**: В бенчмарке сгенерируй гигантский массив случайных данных (это занимает время), а затем вызови `b.ResetTimer()` ПЕРЕД циклом `for i := 0; i < b.N...`. Это исключит время подготовки из результатов теста.

30. Напиши тест для **"goroutine leak"**: функция запускает горутину, которая пишет в канал, но никто не читает. Используй `runtime.NumGoroutine()` до и после. Проверь, что количество горутин не выросло (или выросло ожидаемо).

---

31. Изучите, как fuzzing помогает находить уязвимости безопасности (например, ReDoS в регулярных выражениях).

32. **Фаззинг-тестирование (Fuzz Testing)**: Напишите функцию `ReverseString(s string) string`, которая разворачивает строку задом наперед. Напишите для неё фаззинг-тест `FuzzReverse(f *testing.F)` [344]. Запустите фаззинг с помощью команды `go test -fuzz=Fuzz`. Посмотрите, как Go генерирует случайные входные данные, и найдите баг, связанный с некорректной работой функции с многобайтными символами UTF-8 (кириллица, эмодзи). Исправьте функцию, работая с рунами вместо байт.

33. Напиши тест для `sync/atomic`: `atomic.AddInt64(&counter, 1)` из 1000 горутин. Проверь результат. Сравни производительность с `sync.Mutex` через `testing.B` (бенчмарки).

34. Проанализируй вывод бенчмарка: `BenchmarkSumSlice-8    10000000    105 ns/op    0 B/op    0 allocs/op`. Объясни каждое число. Покажи, что `-8` — GOMAXPROCS.

35. Напиши бенчмарк для **аллокаций**: функция, которая `append`ит в слайс в цикле. Запусти с `-benchmem`. Покажи `allocs/op`. Оптимизируй: предвыдели слайс через `make([]int, 0, len)`. Сравни `allocs/op` до и после.

36. Напиши бенчмарк для **строковой конкатенации**: `+` vs `strings.Builder` vs `bytes.Buffer` vs `fmt.Sprintf`. Сравни производительность для 1000 конкатенаций. Покажи, что `strings.Builder` — оптимальный выбор.

37. Напиши бенчмарк для **map vs switch**: поиск значения по строковому ключу. Сравни `map[string]int` vs `switch key { case "a": ... }`. Покажи, что для малого числа кейсов `switch` быстрее.

38. **Тестирование на примерах (Examples)**: Напишите функцию `FormatMoney(amount int) string`. В файле тестов напишите функцию-документацию `ExampleFormatMoney()` [345]. Внутри тела функции вызовите `fmt.Println(FormatMoney(1000))` и добавьте в самом конце специальный комментарий `// Output: 1 000 руб.` [345]. Убедитесь, что `go test` автоматически валидирует соответствие вывода программы вашему комментарию.

39. **Параллельные бенчмарки (`b.RunParallel`)**: Протестируй производительность конкурентной мапы `sync.Map`. Используй метод `b.RunParallel(func(pb *testing.PB) { for pb.Next() { ... } })`, чтобы запустить бенчмарк одновременно на всех ядрах процессора.

40. Напиши бенчмарк для **разных размеров входных данных**: используй `b.Run(fmt.Sprintf("size=%d", size), func(b *testing.B) { ... })`. Покажи, как алгоритм ведёт себя при росте N (O(1), O(n), O(n²)).

41. **Поиск бага с помощью Фаззинга**: Если ты написал `Reverse` перебирая строку байт за байтом (через индексы), фаззер быстро упадет, когда сгенерирует не-ASCII символ (например, иероглиф). Посмотри, как фаззер нашел баг, сохранив падающий кейс в папку `testdata/fuzz`.

42. Напиши функцию `Reverse(s string) string`, переворачивающую строку (руны!). Напиши фазз-тест: `func FuzzReverse(f *testing.F) { f.Add("hello"); f.Fuzz(func(t *testing.T, orig string) { rev := Reverse(orig); rev2 := Reverse(rev); if orig != rev2 { t.Fatalf("Reverse(Reverse(%q)) = %q", orig, rev2) } }) }`. Запусти `go test -fuzz=FuzzReverse`.

43. Напиши бенчмарк с **`b.ResetTimer()`**: подготовь данные перед циклом (генерация слайса), затем `b.ResetTimer()` — измеряй только сам алгоритм, без подготовки.

44. Напиши бенчмарк с **`b.StopTimer()` / `b.StartTimer()`**: останови таймер во время настройки внутри цикла (например, сброс состояния). Возобнови перед измеряемой операцией.

45. **Разделение тестов по тегам (Build Tags)**: Напишите медленный интеграционный тест, который не должен запускаться при обычном вызове `go test` (чтобы не замедлять CI). Добавьте в самое начало файла теста тег сборки (например, `//go:build integration` [346]). Проверьте, что обычный вызов `go test` игнорирует этот файл. Запустите только интеграционные тесты с помощью `go test -tags=integration` [346].

46. **Исправление кода после Фаззинга**: Перепиши `Reverse`, работая с рунами (тип `[]rune`), чтобы она корректно поддерживала любые Unicode-символы. Запусти фаззинг снова (оставь на пару минут) — он должен работать без ошибок.

47. Покажи, что фаззер находит баг: если `Reverse` работает с байтами, а не рунами, фаззер найдёт строку с Unicode (например, `"Hello, 世界"`), где `Reverse(Reverse(s)) != s`.

48. Напиши фазз-тест для `ParseInt(s string) (int, error)`: фаззер генерирует строки, функция парсит. Проверь, что при `err == nil` — результат корректен (можно `strconv.Itoa` обратно и сравнить). При `err != nil` — ожидаемо.

49. Протестируйте `SafeCounter` с конкурентными горутинами: запустите N горутин, каждая инкрементирует счётчик K раз, проверьте финальное значение.

50. **CPU Профайлинг**: Запусти бенчмарк (например, сортировку большого массива) с флагом `go test -bench=. -cpuprofile=cpu.prof`.

51. **Детектор гонок в тестах**: Напишите тест, который запускает несколько горутин, параллельно пишущих данные в общую мапу без синхронизации. Запустите тесты с флагом детектора гонок `go test -race` [347] и проанализируйте подробный отчет о возникновении состояния гонки в выводе теста.

---

52. Напиши фазз-тест для **криптографической функции**: `Hash(data []byte) []byte`. Проверь свойства: длина хеша фиксирована, малое изменение входа → большое изменение выхода (hamming distance).

53. Напиши фазз-тест с **кастомным mutator'ом** (упрощённо): используй `f.Add(seed...)` с разными типами данных. Покажи, что seed corpus помогает фаззеру быстрее найти интересные входы.

---

54. Напиши функцию `ReverseString(s string) string`. Создай бенчмарк `func BenchmarkReverseString(b *testing.B)`. Внутри используй цикл `for i := 0; i < b.N; i++ { ReverseString("hello") }`.

55. **Анализ профиля (pprof)**: Вызови встроенную утилиту: `go tool pprof cpu.prof`. Внутри консоли pprof введи `top10` (чтобы увидеть самые тяжелые функции) и `web` (чтобы сгенерировать граф вызовов в браузере, потребуется установленный Graphviz).

---

56. Протестируйте, что функция с `sync.Mutex` не приводит к гонкам: запустите тест с флагом `-race`.

57. **Сравнение алгоритмов**: Напиши две функции конкатенации строк. Одна через `+=`, другая через `strings.Builder`. Напиши два бенчмарка для них. Сравни скорость.

58. Запусти бенчмарк с помощью `go test -bench=.`. Посмотри на количество наносекунд на операцию (`ns/op`).

59. Запустите fuzz-тест с ограничением по времени: `go test -fuzz=FuzzParseJSON -fuzztime=30s`.

60. Протестируйте Worker Pool: убедитесь, что все задания обработаны и результаты собраны в правильном количестве.

61. Используй `b.ReportAllocs()` в бенчмарке. Посмотри на количество выделений памяти на операцию (`allocs/op`).

62. Проверьте корректность `context.Context` в конкурентной среде: отмените контекст и убедитесь, что все горутины завершились за разумное время.

63. **[Каверзный кейс]**: Сброс таймера: если подготовка данных для бенчмарка занимает много времени, используй `b.ResetTimer()` перед циклом, чтобы время подготовки не учитывалось.

64. Запусти тесты с флагом покрытия: `go test -cover`. Посмотри процент покрытия (например, `coverage: 85.2% of statements`).

65. Сравните производительность сериализации/десериализации JSON стандартной библиотекой и `jsoniter`.

66. Property-based тест для `Sort`: после сортировки слайс отсортирован (`sort.Ints` + проверка `sort.IntsAreSorted`). И длина не изменилась. И все элементы на месте (через мапу частот).

67. Сгенерируй HTML-отчет о покрытии: `go test -coverprofile=coverage.out && go tool cover -html=coverage.out`. Открой его в браузере и найди непокрытые строки (красным цветом).

68. Property-based тест для `Add(a, b) == Add(b, a)` (коммутативность). И `Add(a, Add(b, c)) == Add(Add(a, b), c)` (ассоциативность). И `Add(a, 0) == a` (нейтральный элемент).

69. Запустите тесты с флагом `-race`, чтобы обнаружить data races. Создайте намеренный race и изучите подробный отчет от race detector.

70. Напиши **"table-driven benchmark"**: слайс структур `{name string, data []int}`. Для каждого — `b.Run(tc.name, func(b *testing.B) { ... })`. Сравни производительность на разных размерах данных.

71. Бенчмарк для алгоритма сортировки (ваш vs `sort.Ints`). Интерпретируйте результаты.

72. Протестируйте функцию, которая запускает горутины, используя `sync.WaitGroup` для ожидания их завершения в тесте.

73. Напиши **"sub-benchmark"** с разными алгоритмами: `b.Run("linear", ...)`, `b.Run("binary", ...)`. Сравни `LinearSearch` vs `BinarySearch` на отсортированных данных.

74. **[Суб-бенчмарки]**: Напиши бенчмарк, который использует `b.Run("small", func(b *testing.B){...})` и `b.Run("large", ...)` для тестирования функции на слайсах разной длины.

75. Напиши **"benchmark comparison"**: сохрани результат `go test -bench=. > old.txt`, оптимизируй код, запусти снова `> new.txt`. Сравни через `benchstat old.txt new.txt`. Покажи статистически значимое улучшение.

76. Тестируйте каналы: пишите в них и читайте в тесте, проверяя корректность данных.

77. Используй `github.com/leanovate/gopter` или `testing/quick` для property-based тестинга. Свойство: `Reverse(Reverse(s)) == s` для любой строки `s`. Генератор: `gopter.GenString()`.

78. Напиши тест с **`t.Parallel()` + `t.Setenv()`**: проверь, что `t.Setenv` безопасен для параллельных тестов (Go 1.17+ — каждый тест получает копию окружения). До 1.17 — `t.Setenv` запрещал `t.Parallel`.

79. Используйте `testing.T.Deadline()` для проверки, что ваш код уважает контекстные дедлайны.

80. Используйте `pprof` для профилирования CPU и памяти вашего HTTP-сервера под нагрузкой, найденной через бенчмарки.

81. Напиши функцию `ParseDuration(s string) (int, error)`, которая парсит строки вида "10s", "5m". Создай Fuzz-тест: `func FuzzParseDuration(f *testing.F)`.

82. Напиши **"example test"**: `func ExampleAdd() { fmt.Println(Add(2, 3)) // Output: 5 }`. Запусти `go test`. Покажи, что Example-тесты выполняются и проверяют `// Output:` комментарий.

83. Протестируйте Graceful Shutdown: запустите сервер в горутине, отправьте сигнал `SIGINT` через `os.FindProcess` и проверьте, что сервер корректно завершился.

84. Напиши **"example test with Unordered output"**: `// Unordered output:` для вывода, где порядок строк не важен. Используй для тестирования map-итераций.

85. Используйте `goleak` (`go.uber.org/goleak`) для обнаружения утечек горутин в тестах.

86. Напиши **"TestMain"**: `func TestMain(m *testing.M) { setup(); code := m.Run(); teardown(); os.Exit(code) }`. Покажи глобальную настройку перед всеми тестами пакета (например, подключение к test DB).

---

87. Добавь начальные данные (seeds) для фаззера: `f.Add("10s")`, `f.Add("5m")`, `f.Add("invalid")`.

88. Протестируйте Worker Pool, отправив туда N задач и проверив, что все они были обработаны ровно один раз.

89. Напишите fuzz-тест для функции `ParseDate(s string) (time.Time, error)`, которая принимает строку в формате `YYYY-MM-DD`. Запустите `go test -fuzz=FuzzParseDate`.

90. Напиши функцию `ProcessWithTimeout(ctx context.Context) error`, которая спит 5 секунд или завершается по `ctx.Done()`. Напиши тест с `context.WithTimeout(100ms)` — проверь `context.DeadlineExceeded`.

91. Используйте `eventually`-паттерн (цикл с retry и таймаутом) для тестирования асинхронных операций без `time.Sleep`.

92. Напиши функцию, зависящую от текущего времени: `IsBusinessHours() bool`. Напиши тест: подмени `time.Now()` через **dependency injection** (передай `func() time.Time` в конструктор). Проверь граничные случаи (9:00, 17:59, 18:00).

93. Внутри `f.Fuzz(func(t *testing.T, s string) { ... })` вызови свою функцию. Если она не вернула ошибку, убедись, что результат имеет смысл. Если вернула панику — тест упадет.

94. Используй `github.com/benbjohnson/clock` (или свой mock clock): `mockClock := clock.NewMock()`, `mockClock.Add(2 * time.Hour)`. Напиши тест для функции с `time.Sleep` или `time.Ticker`, не дожидаясь реального времени.

95. Сформулируйте инвариант для функции сортировки: "после сортировки каждый элемент <= следующего". Проверьте это на 1000 случайных слайсов.

96. Fuzz-тест для `Divide(a, b int) int`: проверьте, что при любых входных данных (кроме деления на ноль) функция не паникует.

97. Напиши тест для **"retry with exponential backoff"**: функция пытается 3 раза с задержками. Используй mock clock, чтобы "промотать" время без реального `Sleep`. Проверь количество попыток.

98. Протестируйте сериализацию/десериализацию через property: `Deserialize(Serialize(x)) == x` для случайных значений.

99. Напиши тест для **"cache with TTL"**: используй mock clock. Установи время T, положи значение в кеш с TTL=5 мин. "Промотай" время на 4 минуты — значение должно быть валидно. На 6 минутах — протухло.

---

100. Запусти фаззинг с помощью `go test -fuzz=FuzzParseDuration`. Позволь ему поработать несколько секунд (он будет генерировать мутации строк). Останови (Ctrl+C).

101. Используйте shrinking — механизм, который автоматически находит минимальный контрпример при нарушении свойства.

102. Используйте `vegeta` или `hey` для нагрузочного тестирования вашего HTTP API.

103. Если фаззер нашел падение (crash), он сохранит данные в папку `testdata/fuzz/`. Запусти обычный `go test` — он автоматически подхватит этот кейс как обычный юнит-тест и упадет. Исправь баг в коде.

104. Напишите скрипт на Go, который использует `golang.org/x/sync/errgroup` для генерации 10000 параллельных HTTP-запросов и собирает статистику (latency, error rate).

105. Настройте GitHub Actions или GitLab CI для автоматического запуска `go test -race -cover ./...` на каждый PR.

106. **[Параллельные тесты]**: Напиши 3 теста. В начале каждого вызови `t.Parallel()`. Запусти `go test -v`. Убедись, что тесты выполняются одновременно (в выводе будет `PAUSE`, затем `CONT`).

107. Добавьте в CI проверку минимального покрытия кода (например, 80%) через `go tool cover`.

108. Настройте линтеры (`golangci-lint`) в CI и блокируйте PRы, которые не проходят проверки.

109. Используйте `go test -count=1` для отключения кэширования тестов (полезно в CI, чтобы тесты всегда запускались).

110. **[Каверзный кейс с `t.Parallel()`]**: Напиши табличный тест с `t.Parallel()` внутри `t.Run`. Переменная `tc` (testCase) будет захвачена по ссылке! Исправь это, создав копию переменной внутри цикла: `tc := tc`.

111. Примените TDD (Test-Driven Development): напишите проваливающийся тест для функции, затем минимальную реализацию, затем рефакторинг. Повторите цикл 3 раза.

112. Используйте паттерн "Test Helpers" — вынесите общую setup-логику в функцию с `t.Helper()`, чтобы ошибки указывали на вызывающий тест, а не на сам хелпер.

113. **Тестирование паник**: Напиши функцию, которая паникует при отрицательном аргументе. Напиши тест, который использует `defer func() { if r := recover(); r != nil { ... } }()` для проверки, что паника действительно произошла, и тест при этом не упал.

114. Напишите тест для `main()` функции, переопределив `os.Args` и перехватив `os.Stdout` через `os.Pipe`.

115. Создайте "golden files" тесты: сохраните эталонный вывод функции в файл `testdata/golden.txt` и сравнивайте реальный вывод с ним в тестах.

116. **[Высокая сложность — Golden Files]**: Напиши функцию, которая генерирует сложный JSON-отчет. В тесте сгенерируй результат. Если файла `testdata/expected.json` не существует — создай его. Если существует — сравни результат с содержимым файла (`os.ReadFile`). Если тесты падают из-за изменения логики, удали файл, чтобы он перегенерировался.

117. Реализуйте flag `-update` в тестах, который при запуске обновляет golden files (полезно при легитимных изменениях вывода).

118. Напишите тест, который проверяет, что ваша программа потребляет не больше X МБ памяти под нагрузкой (используйте `runtime.ReadMemStats`).

119. **[Финальное испытание по тестированию]**: Напиши CLI-утилиту (работает с флагами, читает файл, делает HTTP-запрос). Покрой её полностью:
    - Логику работы с флагами (передавай `os.Args` в функцию `Run(args []string) error`).
    - Чтение файла (через интерфейс или `os.ReadFile`).
    - HTTP-запрос (через `httptest.NewServer`).
    - Напиши бенчмарк для парсинга.
    - Добейся покрытия кода 90%+.

---

Твой список теперь состоит из **470 упражнений**.
Если ты пройдешь весь этот путь: от синтаксиса до тестирования, конкурентности, сетей и баз данных — ты будешь уверенным Middle+/Senior Go-разработчиком, способным проектировать системы, писать надежный код и покрывать его тестами. 

Удачи в этой интенсивной тренировке! Если упрешься в непонятную концепцию (особенно в горутинах, мапах или тестах с моками) — не стесняйся просить теоретических разъяснений!

120. **Финальный босс (Полный цикл TDD микросервиса):**
    Возьмите задачу: "Сервис коротких URL (URL shortener)".
    * Начните с написания **интеграционных тестов** (через testcontainers с Postgres и Redis) для API: создание ссылки, переход по ссылке, статистика.
    * Напишите **unit-тесты** для сервисного слоя с моками репозитория.
    * Добавьте **benchmarks** для функции генерации коротких кодов.
    * Напишите **fuzz-тесты** для декодера base62 и валидации URL.
    * Используйте **httptest** для тестирования middleware (auth, rate-limit).
    * Проверьте **race conditions** при конкурентном создании ссылок с одинаковым slug.
    * Настройте **CI** с покрытием 90%+, проверкой race и линтерами.
    * Сделайте **нагрузочный тест** с 10k RPS через vegeta и убедитесь, что P99 latency < 50ms.

---
