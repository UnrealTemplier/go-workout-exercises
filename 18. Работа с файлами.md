# Работа с файлами (текстовые и бинарные, I/O)

В этом файле собраны уникальные упражнения по теме **"Работа с файлами (текстовые и бинарные, I/O)"**, отсортированные по возрастанию сложности.

---

1. Прочитайте файл целиком в строку через `os.ReadFile` (1.16+).

2. Откройте файл на дозапись с помощью `os.OpenFile` и флагов `O_APPEND|O_CREATE|O_WRONLY`, допишите несколько строк.

3. Откройте файл с помощью `os.Open`, прочитайте его построчно, используя `bufio.Scanner`, и обязательно закройте файл через `defer`.

4. Сформируйте бинарный файл из последовательности `int32` и прочитайте их обратно в слайс.

5. Рекурсивно обойдите директорию (`filepath.Walk`), выведите только файлы с расширением `.go`.

6. Запишите в файл структуру фиксированного размера через `encoding/binary.Write` и прочитайте обратно `binary.Read`.

7. Прочитайте всё содержимое созданного файла в память с помощью `os.ReadFile` и выведите на экран.

8. Измените права доступа к файлу с помощью `os.Chmod` (например, сделать только чтение).

9. Обязательно добавь `defer file.Close()` сразу после создания.

10. Создайте временный файл (`os.CreateTemp`), запишите данные, выведите путь, а затем удалите (или используйте `defer os.Remove`).

11. Открой существующий файл с помощью `os.Open`. Прочитай его содержимое целиком с помощью `os.ReadFile`.

12. Скопируйте содержимое одного файла в другой с использованием `io.Copy`.

13. Прочитай файл построчно с помощью `bufio.Scanner`. Выведи каждую строку в консоль с её номером.

14. Допишите (append) текст в конец существующего файла, используя флаги `os.O_APPEND` и `os.O_WRONLY` при открытии через `os.OpenFile`.

15. **Построчное чтение (Scanner)**: Создайте большой текстовый файл с несколькими строками текста. Напишите программу, которая с помощью `bufio.NewScanner` считывает этот файл построчно и выводит каждую строку с префиксом `[Line X]: `.

16. Создайте программу, которая копирует содержимое одного текстового файла в другой, читая и записывая данные небольшими блоками (буферами).

17. Создай файл `hello.txt` программно (`os.Create`). Запиши в него строку "Hello, File!". Закрой файл (`defer f.Close()`). Прочитай файл обратно (`os.ReadFile` или `ioutil.ReadFile`) и выведи содержимое.

18. **Быстрая запись всего файла**: Используя функцию `os.WriteFile`, запишите массив байт в новый файл `fast.txt`, указав права доступа `0644`.

19. Используй `os.WriteFile` для быстрой записи массива байт в файл (перезапись).

20. Слейте два текстовых файла в третий: откройте оба на чтение, третий на запись, копируйте последовательно.

21. Создайте CSV-файл (через запятую), запишите туда 5 строк данных, а затем прочитайте его с помощью пакета `encoding/csv`.

22. Откройте файл для чтения и запишите его содержимое в `os.Stdout` с помощью `io.Copy`.

23. Проверь существование файла с помощью `os.Stat`. Обработай ошибку `os.IsNotExist(err)`.

24. Реализуйте копирование директории со всеми поддиректориями и файлами (рекурсивное создание папок и копирование содержимого).

25. **Запись строки**: Откройте файл на запись и запишите в него строку `"Привет, Go!"` с помощью метода `file.WriteString`.

26. Напиши функцию `ReadLines(filename string) ([]string, error)`, которая читает файл построчно (`bufio.Scanner` с `ScanLines`). Обработай пустые строки и файл без последнего `\n`.

27. Удали файл с помощью `os.Remove`.

28. Найдите и замените определенное слово в текстовом файле, перезаписав его (прочитайте -> измените -> перезапишите).

29. **Буферизированная запись**: Для быстрой записи большого количества строк используй `bufio.NewWriter`. Напиши 1000 строк в файл через него и обязательно вызови `Flush()` в конце, чтобы сбросить буфер на диск.

30. Читайте большой файл блоками по 1 КБ, вычисляя SHA256-хэш всего файла.

31. **Произвольное позиционирование (Seek)**: Откройте файл. Используйте метод `file.Seek`, чтобы переместить указатель чтения/записи на 5 байт вперед от начала файла. Прочитайте следующие 3 байта и выведите их.

32. Создай новую директорию с помощью `os.Mkdir`.

33. Обработайте ошибки открытия файла: проверьте `os.IsNotExist`, корректно закройте файл через `defer` и обработайте возможную ошибку закрытия.

34. Напиши программу, которая подсчитывает количество строк, слов и символов в текстовом файле (аналог `wc`). Используй `bufio.Scanner` с разными `SplitFunc`.

35. Напиши функцию `AppendToFile(filename string, text string) error`, которая дописывает текст в конец файла. Используй флаг `os.O_APPEND`. Проверь, что старое содержимое сохраняется.

36. Сканируйте файл построчно, выведите строки, содержащие заданное слово (простой grep).

37. Создай вложенные директории `a/b/c` с помощью `os.MkdirAll`.

38. Создай файл с правами доступа `0644` (`os.Create` + `os.Chmod` или `os.OpenFile` с правами). Проверь права через `os.Stat` и `FileMode.Perm()`.

39. Используйте `fmt.Fprintf` для форматированной записи в файл (создайте простой отчёт).

40. Получите информацию о файле (размер, права доступа, время модификации) через `os.Stat` и выведите её в человекочитаемом формате.

41. Прочитай содержимое директории с помощью `os.ReadDir`. Выведи имена всех файлов.

42. Покажите разницу режимов открытия: `os.O_RDONLY`, `os.O_RDWR`, `os.O_TRUNC` и т.д., проверьте ошибки при записи в файл, открытый только на чтение.

43. **Простое чтение**: Открой `test.txt` через `os.Open`. Считай все данные с помощью `io.ReadAll` (или `os.ReadFile`) и выведи в консоль.

44. **[Высокая сложность]**: Напиши рекурсивную функцию, которая обходит все файлы и папки в указанной директории (дерево) и выводит их пути.

45. **Создание файла**: Напишите программу, которая создает новый пустой файл `test.txt` с помощью `os.Create`. Обязательно обеспечьте закрытие файла через `defer file.Close()`.

46. Используйте функции `path/filepath.Join`, `filepath.Dir`, `filepath.Base`, `filepath.Ext` для разбора и построения путей.

---

47. Прочитайте созданный `gob`-файл обратно в память и выведите данные на экран.

48. **Defer для файлов**: Перепиши упр. 96 и 97 так, чтобы функция `Close()` вызывалась через `defer` сразу после успешной проверки на ошибку открытия (`if err != nil`).

49. Прочитай бинарные данные из файла с помощью `file.Read`. Обработай ошибку `io.EOF`.

50. Напиши программу, которая заменяет все вхождения подстроки в файле на другую подстроку (аналог `sed`). Создай резервную копию оригинала.

51. Откройте бинарный файл с помощью `os.Open` и прочитайте первые 16 байт, чтобы определить "магическое число" (сигнатуру) файла.

52. **Чтение по строкам**: Создай большой текстовый файл (несколько абзацев). Используй `bufio.Scanner`, чтобы считывать и выводить файл строго по одной строке (чтобы экономить оперативную память).

53. Напиши программу, которая копирует бинарный файл (изображение или архив). Используй `io.CopyBuffer` с буфером 4KB для эффективности. Сравни хеши оригинала и копии (`crypto/sha256`).

54. **Открытие для чтения**: Напишите код, который открывает существующий файл `test.txt` в режиме "только для чтения" с помощью `os.Open`. Обработайте возможную ошибку (например, если файла нет).

55. Напиши программу, которая читает CSV-файл (или файл с разделителем табуляции) и выводит данные в виде отформатированной таблицы. Используй `strings.Split` или `encoding/csv`.

---

56. Используйте `io.Seek` для перемещения курсора чтения в середину бинарного файла и прочитайте оттуда 4 байта.

57. **Проверка существования**: Напиши функцию, которая с помощью `os.Stat` и `os.IsNotExist(err)` проверяет, существует ли файл перед тем, как попытаться его прочесть.

58. Запиши структуру в файл в формате JSON с помощью `encoding/json` и `json.NewEncoder`.

59. Создайте программу, которая шифрует текстовый файл простым XOR-шифром, сохраняя результат в бинарный файл, а затем расшифровывает его обратно.

60. **Чтение по кускам (Chunks)**: Открой файл и читай его порциями ровно по 16 байт в цикле, используя метод `file.Read(buf)` до достижения `io.EOF`.

61. Прочитай структуру из JSON файла с помощью `json.NewDecoder`.

62. **Запись среза байт**: Запишите в файл массив байт (например, `[]byte{65, 66, 67, 10}`) с помощью метода `file.Write`. Проверьте, какие символы появились в файле.

63. Запиши массив структур в текстовый файл формата CSV (используй `encoding/csv`).

64. Напиши программу, которая сериализует структуру в JSON (`encoding/json`) и записывает в файл. Затем читает из файла и десериализует. Используй теги структур `json:"field_name"`.

65. Используйте пакет `path/filepath` для обхода директории и вывода путей всех `.txt` файлов (walk).

66. **Быстрое чтение всего файла**: Используя функцию `os.ReadFile`, прочитайте весь файл `test.txt` за один вызов. Преобразуйте результат в строку и выведите его в консоль.

67. Напиши программу, которая читает заголовок PNG-файла (первые 8 байт — сигнатура `\x89PNG\r\n\x1a\n`). Проверь, что файл действительно PNG.

68. Создайте вложенную структуру папок с помощью `os.MkdirAll` и удалите её рекурсивно с помощью `os.RemoveAll`.

69. **Права доступа (Chmod)**: Создай скрипт (например, файл `script.sh` с текстом `echo "hello"`). Используй `os.Chmod`, чтобы выдать ему права на исполнение (`0755`).

70. Напиши программу, которая записывает слайс `[]int32` в бинарный файл. Используй `encoding/binary.Write` с порядком байт `binary.LittleEndian`. Прочитай обратно и проверь.

71. Напиши программу, которая разбивает большой бинарный файл на части (chunk'и) заданного размера (например, 1MB каждый). Затем напиши программу-сборщик, которая объединяет chunk'и обратно.

---

72. **Дозапись в файл (Append)**: Откройте существующий файл с помощью `os.OpenFile` с флагами `os.O_APPEND|os.O_WRONLY`. Допишите новую строку в конец файла, не стирая старое содержимое.

73. Напиши программу, которая реализует простую базу данных в файле: запись структур `Record { ID int; Name string; Score float64 }` с фиксированным размером записи. Реализуй CRUD: Create, Read, Update, Delete (soft delete).

74. Используй `bufio.NewReader` для чтения файла блоками по 1024 байта (через метод `Read`).

75. Напиши программу, которая читает файл с произвольной позиции (`os.File.Seek`). Реализуй "tail" — чтение последних N байт файла без загрузки всего файла в память.

76. **Копирование файлов**: Напиши программу, которая открывает `source.txt`, создает `dest.txt` и использует `io.Copy` для перекачки данных напрямую между файловыми дескрипторами.

77. **Посимвольное или пословное чтение**: Используя `bufio.Scanner` и метод `scanner.Split(bufio.ScanWords)`, настройте сканер так, чтобы он читал файл не построчно, а слово за словом. Выведите каждое слово на новой строке.

78. Напиши программу, которая мониторит файл на изменения (размер, время модификации). Используй `os.Stat` в цикле с `time.Sleep`. При изменении — выведи сообщение.

79. Напишите утилиту, которая принимает путь к папке и выводит суммарный размер всех файлов внутри неё, рекурсивно обходя подпапки.

80. **Работа с папками**: Используй `os.ReadDir`, чтобы получить список всех файлов и папок в текущей директории. Выведи их имена и укажи, кто из них является директорией (метод `IsDir()`).

81. Создайте структуру `Record` и запишите слайс таких структур в бинарный файл с помощью пакета `encoding/gob`.

82. Узнай размер файла с помощью `file.Stat().Size()` до его чтения.

83. **Буферизованная запись (Writer)**: Напишите программу, которая записывает 10 000 строк в файл. Используйте `bufio.NewWriter`. Не забудьте вызвать метод `Flush()` в конце (объясните в комментариях, почему без `Flush` часть данных может не записаться).

84. Напиши программу, которая реализует лог-файл с ротацией: при достижении размера 1MB — переименовывает текущий файл в `log.1`, `log.2` и т.д., и создаёт новый.

85. **[Каверзный кейс]**: Открой файл для чтения и записи одновременно `os.O_RDWR`. Запиши что-то в середину файла, не затирая его полностью (используй `Seek`).

86. **Создание вложенных директорий**: Используй `os.MkdirAll`, чтобы создать сложную структуру папок: `data/logs/2026/07/`.

87. Временно создай файл в системной папке для временных файлов с помощью `os.CreateTemp`.

88. Напиши программу, которая рекурсивно обходит директорию (`filepath.Walk` или `filepath.WalkDir`). Выведи дерево файлов и папок с отступами.

89. **Временные файлы**: Используй `os.CreateTemp`, чтобы создать временный файл в системной `Temp` директории. Прочитай/запиши в него и добавь `defer os.Remove(tempFile.Name())`, чтобы он удалился после работы.

90. Напиши программу, которая находит все файлы с определённым расширением в директории и поддиректориях. Подсчитай их общий размер.

91. **Управление курсором (Seek)**: Открой файл. Используй `file.Seek`, чтобы пропустить первые 10 байт, а затем считай остальные.

92. **Бинарная запись**: Создайте структуру `PlayerData` с полями `ID uint32`, `Level uint16`, `Score float32`. Используя пакет `encoding/binary`, запишите эту структуру в файл `player.dat` в бинарном формате (в кодировке Little Endian).

93. Напиши программу, которая создаёт временный файл (`os.CreateTemp`), пишет в него, а затем автоматически удаляет (`defer os.Remove`). Объясни, зачем нужны временные файлы.

94. **Запись бинарных данных**: Используй пакет `encoding/binary`. Создай слайс чисел типа `int32` и запиши их в файл как сырые байты в формате `LittleEndian` (`binary.Write`). Обрати внимание, что файл после этого нельзя нормально прочитать в обычном блокноте.

95. **Бинарное чтение**: Откройте файл `player.dat`. Считайте из него бинарные данные напрямую в новую структуру `PlayerData` с помощью `binary.Read`. Выведите поля структуры, чтобы убедиться в правильности восстановления.

96. Напиши функцию, которая читает лог-файл (текстовый) и возвращает количество строк, содержащих слово "ERROR".

97. Напиши программу, которая использует `io.MultiWriter` для одновременной записи в файл и в консоль. Напиши логгер, который пишет в несколько destination'ов.

98. **[Финальное испытание]**: Напиши CLI-утилиту, которая принимает флаг `-input` (путь к txt файлу) и `-output` (путь к новому файлу). Программа должна прочитать input, преобразовать весь текст в ВЕРХНИЙ РЕГИСТР, посчитать количество слов, и записать в output файл результат в формате: "WORDS: <count>\nUPPERCASE TEXT: ...". Обработать все ошибки (нет файла, нет прав и т.д.) через `log.Fatalf`.

---
**Совет по прохождению:** Не подсматривай в готовые решения до тех пор, пока не попробуешь решить задачу хотя бы 15-30 минут. Ошибки компилятора в Go — твои лучшие друзья. Удачи в кодинге!

99. **Временные файлы**: Напишите программу, которая создает временный файл с помощью `os.CreateTemp`, записывает туда конфиг, читает его, а перед самым выходом из программы удаляет этот файл с диска с помощью `os.Remove`.

---

100. Напиши программу, которая реализует `io.Reader` и `io.Writer` для кастомного типа (например, `type MemoryBuffer struct { data []byte }`). Реализуй методы `Read` и `Write` по интерфейсам.

---
