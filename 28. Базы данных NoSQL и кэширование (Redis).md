# Базы данных NoSQL и кэширование (Redis)

В этом файле собраны уникальные упражнения по теме **"Базы данных NoSQL и кэширование (Redis)"**, отсортированные по возрастанию сложности.

---

1. Подключитесь к Redis с помощью `go-redis`. Установите ключ `greeting` со значением и TTL 30 секунд.

2. Подключитесь к Redis с помощью `github.com/redis/go-redis/v9`. Используйте `Ping` для проверки связи.

3. Проверьте существование ключа, получите его значение и обработайте `redis.Nil`.

4. **Подключение к Redis**: Установите клиент `go-redis` версии 9 (`github.com/redis/go-redis/v9`). Напишите код подключения к локальному серверу Redis через `redis.NewClient`. Выполните команду `Ping` с передачей контекста и выведите ответ в консоль.

5. Напиши функцию `CacheGet(key string) (string, error)`, использующую `rdb.Get(ctx, key).Result()`. Обработай `redis.Nil` — это **не ошибка**, а отсутствие ключа. Верни `("", false)` или кастомную ошибку.

6. Напиши функцию `CacheDelete(key string) error`. Используй `rdb.Del(ctx, key)`. Проверь, что удаление несуществующего ключа возвращает 0 (не ошибка).

7. Реализуйте базовые операции: `Set` (с указанием TTL), `Get`, `Del`. Всегда проверяйте ошибку `redis.Nil`, которая означает, что ключ не найден (это не ошибка сети!).

8. Напиши функцию `CacheSetNX(key string, value any, ttl time.Duration) (bool, error)`, использующую `rdb.SetNX(ctx, key, value, ttl)`. Это **distributed lock**: если ключ не существует — устанавливает и возвращает `true`. Иначе — `false`.

9. Используйте `Incr` для создания атомарного счетчика (например, для генерации ID или подсчета просмотров).

10. Увеличьте счётчик (`INCR`) и атомарно получите новое значение.

11. Подключись к Redis через `github.com/redis/go-redis/v9`. Создай клиент: `rdb := redis.NewClient(&redis.Options{Addr: "localhost:6379"})`. Проверь `rdb.Ping(ctx).Result()`.

12. Напиши функцию `CacheIncr(key string) (int64, error)`, использующую `rdb.Incr(ctx, key)`. Покажи атомарность: запусти 1000 горутин, каждая `Incr` — результат будет ровно 1000.

13. Используйте `SetNX` (Set if Not eXists) для реализации простейшего распределенного лока (mutex).

14. **Запись и чтение строк (SET/GET)**: Напишите функцию кэширования текстовых данных. Запишите в Redis ключ `"session_token"` со значением `"user_123_active"`. Установите время жизни ключа (TTL) равным 10 секундам. Прочитайте значение ключа и выведите его на экран. Подождите 11 секунд и попробуйте прочитать ключ снова.

15. Напиши функцию `CacheDecr(key string) (int64, error)`. Покажи, что `Decr` на 0 даст -1. Используй `IncrBy`/`DecrBy` для произвольных значений.

16. Изучите разницу между `Keys("*")` и `Scan()`. Напишите скрипт, который удаляет ключи по паттерну, используя **только** `Scan`, чтобы не заблокировать продакшен-Redis (команда `KEYS` блокирует поток!).

17. Сохраните JSON-объект в виде строки и прочитайте обратно, распарсив в структуру.

18. Настройте пул соединений в `go-redis` (`PoolSize`, `MinIdleConns`) и изучите, как он влияет на пропускную способность.

19. Реализуй **distributed lock** через `SetNX` + `Expire`: `SET key value NX EX 10`. Если lock получен — выполни работу, затем `Del(key)`. Обработай "владение" lock'ом: значение = UUID процесса, проверяй перед `Del`.

20. Хэши: сохраните поля пользователя (name, email) в хэш `user:1`, прочитайте все поля (`HGETALL`).

21. Запиши значение по ключу: `client.Set(ctx, "user:1:name", "Alice", 0)`.

22. **Hashes**: Сохраните профиль пользователя (`HSET user:1 name "John" age 30`). Прочитайте одно поле (`HGET`) и весь объект (`HGETALL` с маппингом в структуру через `ScanAsTuple` или `MapStringString`).

23. Реализуй **distributed lock с автопродлением**: goroutine раз в 5 секунд делает `Expire` на lock, пока основная работа не завершится. При завершении — `Del`. Используй `context` для отмены продления.

24. **Базовый Set/Get и redis.Nil**: Сохрани строковое значение по ключу: `client.Set(ctx, "mykey", "hello", 0)`. Прочитай его. Затем попробуй прочитать несуществующий ключ и обработай ошибку `errors.Is(err, redis.Nil)`.

25. **Обработка отсутствия ключа**: При попытке получить несуществующий ключ клиент `go-redis` возвращает специальную ошибку. Напишите проверку: если возвращаемая ошибка равна `redis.Nil`, выведите сообщение `"Ключ не найден в кэше"`, иначе обработайте сетевую ошибку.

26. **TTL (Время жизни)**: Сохрани ключ с таймаутом (expiration) в 3 секунды. Прочитай его (убедись, что он есть), сделай `time.Sleep(4 * time.Second)`, попробуй прочитать снова и убедись, что он удален.

27. **Lists**: Реализуйте простую FIFO-очередь задач, используя `LPUSH` (для добавления) и `BRPOP` (блокирующее чтение для воркера).

28. Напиши функцию `CacheMGet(keys ...string) ([]string, error)`, использующую `rdb.MGet(ctx, keys...)`. Покажи, что `MGet` возвращает `[]interface{}`, где отсутствующие ключи — `nil`.

29. **Sets**: Создайте систему "друзей" или "тегов". Используйте `SADD`, `SINTER` (для поиска общих друзей) и `SUNION`.

30. **Sorted Sets (ZSET)**: Реализуйте лидерборд игроков. Используйте `ZADD` с оценкой (score), `ZINCRBY` для изменения счета и `ZREVRANGE` для получения топ-10 игроков.

31. **Кэширование структур**: Redis хранит только строки/байты. Сериализуй структуру `User` в JSON (`json.Marshal`) и сохрани в Redis. Прочитай и десериализуй обратно.

32. **Сериализация структур для Redis**: Redis хранит строки или байты. Напишите функции `SaveUserToCache(ctx context.Context, u User)` и `GetUserFromCache(ctx context.Context, id string) (*User, error)`. Внутри функций сериализуйте структуру в JSON-строку при записи и десериализуйте обратно при чтении.

33. Напиши функцию `QueuePush(key string, value string) (int64, error)` и `QueuePop(key string) (string, error)`, использующие `RPush` + `BLPop` (blocking left pop). Реализуй **FIFO очередь** с блокирующим ожиданием.

34. Установи время жизни (TTL) для ключа при записи (`10 * time.Second`). Выведи значение, подожди 11 секунд, попробуй вывести снова (убедись, что ключ исчез).

35. Множества: добавьте теги к статье (`SADD`), получите все теги (`SMEMBERS`), найдите пересечение множеств (`SINTER`).

36. **Bitmaps**: Подсчитайте количество уникальных пользователей, заходивших на сайт за год, используя всего несколько килобайт памяти (`SETBIT`, `BITCOUNT`).

37. **Счетчики (Incr / Decr)**: Реализуй счетчик просмотров статьи. Используй `client.Incr(ctx, "article:123:views")`. (В отличие от реляционной БД с транзакциями, в Redis `Incr` атомарен и работает за наносекунды).

38. **HyperLogLog**: Оцените количество уникальных IP-адресов за день с погрешностью ~0.81% с помощью `PFADD` и `PFCOUNT`.

39. Напиши функцию `SortedSetAdd(key string, members ...redis.Z) error`, использующую `rdb.ZAdd(ctx, key, members...)`. Реализуй **leaderboard**: добавь игроков с очками, получи топ-10 через `ZRevRangeWithScores(ctx, key, 0, 9)`.

40. Напиши функцию `SetIsMember(key, member string) (bool, error)` через `SIsMember`. Используй для проверки "в чёрном списке".

41. Используй `client.Incr(ctx, "page_views")` для атомарного инкремента счетчика просмотров. Запусти 100 горутин, инкрементирующих счетчик, и убедись, что в итоге будет ровно 100.

42. Сортированные множества: сохраните рейтинг игроков (`ZADD`), получите топ-10 (`ZREVRANGE WITHSCORES`).

43. **Streams**: Реализуйте современную очередь сообщений (аналог Kafka) с использованием `XADD`, `XREADGROUP` и Consumer Groups для распределенной обработки сообщений несколькими воркерами.

44. **Работа со структурами через Hashes**: Redis поддерживает тип данных Hash, позволяющий работать с отдельными полями объекта без полной его перезаписи. Используйте команды `HSet`, `HGet` и `HGetAll` для хранения и чтения профиля пользователя.

45. **Pub/Sub**: Напишите чат-сервер, где сообщения публикуются в канал, а подключенные клиенты (в отдельных горутинах) подписываются на него.

46. Напиши функцию `SortedSetRank(key, member string) (int64, error)` через `ZRevRank` (ранг от высшего к низшему). Обработай `redis.Nil` (игрок не в таблице лидеров).

47. Реализуйте простой кэш с TTL: перед запросом к PostgreSQL проверяйте Redis, если промах — загружайте из БД и сохраняйте в Redis.

48. **Pipelines**: Сгруппируйте 1000 команд `GET` в один Pipeline (`pipe := rdb.Pipeline()`), отправьте их одним сетевым запросом и измерьте разницу во времени (latency) по сравнению с последовательными вызовами.

49. Напиши функцию `HashSet(key string, fields map[string]string) error` через `rdb.HSet(ctx, key, fields)`. Реализуй хранение объекта `User` в Redis hash: `HSET user:123 name Alice email alice@example.com`.

50. **Паттерн Кэширования "Cache-Aside"**: Напишите комплексную функцию `GetUserData(ctx context.Context, id int) (*User, error)`:
    * Функция проверяет наличие данных о пользователе в Redis (ключ `user:id`).
    * Если данные есть в кэше, она десериализует их и мгновенно возвращает результат (cache hit).
    * Если данных в кэше нет (cache miss), она делает SELECT-запрос к PostgreSQL, сохраняет полученные данные в Redis с TTL 5 минут и возвращает результат пользователю.

51. **Списки (Lists - Очередь)**: Используй Redis как очередь задач. Сделай `LPush` (добавление в начало списка) трех строк. Из другой горутины читай их через `RPop` (взятие с конца), пока список не опустеет.

52. **Транзакции (WATCH/MULTI/EXEC)**: Реализуйте оптимистичную блокировку. "Следите" (`WATCH`) за ключом баланса, начинайте транзакцию и выполняйте списание. Если ключ изменился извне до `EXEC`, транзакция прервется, и вы повторите её.

53. Удали набор ключей по маске (например, `user:*`). Внимание: использование `KEYS user:*` в production блокирует Redis. Используй безопасную команду `SCAN` для итерации и удаления.

54. Создайте канал Pub/Sub: подписчик в одной горутине получает сообщения, издатель в другой отправляет.

55. **Хэши (Hashes)**: Сохрани профиль пользователя не как JSON, а как Redis Hash. Используй `client.HSet(ctx, "user:1", "name", "Ivan", "age", 25)`. Прочитай только возраст через `HGet`, а затем все поля сразу через `HGetAll`.

56. Напиши функцию `BitmapSet(key string, offset int64, value int) error` через `rdb.SetBit(ctx, key, offset, value)`. Используй для **daily active users**: каждый пользователь = бит по своему ID. Посчитай `BitCount` для анализа.

57. **Множества (Sets - Уникальность)**: Тебе нужно хранить уникальные IP посетителей. Сделай 10 вызовов `SAdd` с одним и тем же IP и 5 с другим. Сделай `SCard` (количество). Убедись, что размер сета = 2.

58. **Redis Cluster**: Настройте локальный кластер Redis (через Docker) и подключитесь к нему через `go-redis/cluster`. Поймите, как происходит шардирование ключей (CRC16 hash slots).

59. Напиши функцию `StreamAdd(key string, values map[string]string) (string, error)` через `rdb.XAdd(ctx, &redis.XAddArgs{Stream: key, Values: values})`. Реализуй **event sourcing**: каждое событие = запись в stream.

60. **List**: Используй `client.LPush` и `client.RPop`. Реализуй простую очередь задач (клади строки в список слева, забирай справа).

61. Транзакции Redis: атомарно обновите два ключа с помощью `MULTI/EXEC` (или `TxPipelined`).

62. **Cache-Aside (Lazy Loading)**: Напишите функцию `GetProduct(id)`. Сначала ищите в Redis. Если нет (`redis.Nil`) — делайте запрос в Postgres, сохраняйте в Redis с TTL и возвращайте.

63. Напиши функцию `HyperLogLogAdd(key string, elements ...string) error` через `rdb.PFAdd(ctx, key, elements...)`. Посчитай уникальные элементы через `PFCount`. Покажи, что HyperLogLog приблизительный, но очень экономичный по памяти.

64. **Lua-скрипты**: Напишите Lua-скрипт, который атомарно проверяет наличие ключа и, если его нет, создает его с TTL (идеально для rate-limiting или идемпотентности). Выполните его через `Eval`.

65. **Конвейер (Pipeline)**: Тебе нужно сделать 1000 операций `Set`. Вызови `pipe := client.Pipeline()`, в цикле сделай 1000 `pipe.Set(...)`, а в конце один раз `pipe.Exec(ctx)`. Это отправит все команды в одном TCP-пакете.

66. **Очередь задач на списках Redis (LPUSH/RPOP)**: Реализуйте простейшую очередь на структурах данных Redis. Напишите одну горутину (Producer), которая раз в секунду добавляет ID задач в конец списка Redis с помощью `LPush`. Напишите вторую горутину (Consumer), которая забирает эти задачи с помощью `RPop` или блокирующего чтения `BRPop`.

67. **Cache Stampede (Thundering Herd)**: Воспроизведите ситуацию, когда TTL популярного ключа истекает, и 1000 горутин одновременно бьют в БД. Решите проблему с помощью пакета `golang.org/x/sync/singleflight`, чтобы в БД пошел только один запрос, а остальные ждали его результата.

68. Pipeline: выполните пачку команд без ожидания ответа после каждой, затем считайте все ответы.

69. **Write-Through Cache**: При обновлении данных в Postgres (`UPDATE`) синхронно обновляйте или инвалидируйте соответствующий ключ в Redis в рамках той же бизнес-логики.

70. Напиши функцию `StreamReadGroup(group, consumer, stream string) ([]redis.XMessage, error)` через `rdb.XReadGroup`. Реализуй **consumer group**: несколько воркеров читают из одного stream, каждое сообщение обрабатывается одним воркером.

71. **Транзакции в Redis**: Redis поддерживает транзакции (они не такие строгие, как в SQL). Используй `client.TxPipeline()`, чтобы выполнить несколько команд. Если хоть одна упадет, остальные не откатятся (особенность Redis), но они гарантированно выполнятся друг за другом, не прерываясь командами других клиентов.

72. **Set**: Используй `client.SAdd` для хранения тегов. Добавь теги к двум разным постам. Используй `client.SInter` для нахождения общих тегов у этих постов (пересечение множеств).

73. Напиши функцию `StreamAcknowledge(stream, group string, ids ...string) error` через `rdb.XAck`. Покажи, что сообщение удаляется из pending list только после acknowledge.

---

74. **Распределенный Lock (Redlock)**: Реализуйте надежный мьютекс, который работает across нескольких независимых инстансов Redis, чтобы гарантировать, что фоновая задача (cron job) выполняется только в одном экземпляре во всей кластерной инфраструктуре.

75. Реализуй **Cache-Aside pattern**: `GetUserFromCacheOrDB(ctx, id int) (*User, error)`. Сначала `CacheGet("user:" + id)`. Если miss — `SELECT` из PostgreSQL, `CacheSet` результат, верни. Обработай **cache stampede**: если ключ протухает, 100 запросов одновременно ломятся в БД.

76. **Множества для проверки уникальности (Sets)**: Напишите счетчик уникальных посетителей сайта. Используйте структуру данных Set в Redis и команды `SAdd` (добавление IP-адреса посетителя) и `SCard` (получение общего количества уникальных адресов).

77. **Rate Limiter (Token Bucket)**: Реализуйте middleware для HTTP-сервера, который использует Lua-скрипт в Redis для ограничения количества запросов от одного API-ключа (например, 100 запросов в минуту).

78. **Паттерн Cache-Aside (Интеграция)**: Напиши функцию `GetUser(id)`. Алгоритм:

79. Реализуй **cache stampede protection** через `SetNX`: при cache miss — один запрос ставит lock `"lock:user:123"`, грузит из БД, кладёт в кеш, удаляет lock. Остальные ждут lock'а или берут stale-данные.

80. **Sorted Set**: Используй `client.ZAdd` для создания таблицы лидеров (игроки и их очки). Выведи топ-3 игрока с наибольшим количеством очков (`client.ZRevRangeWithScores`).

81. Rate limiter на Redis: используйте скользящее окно или token bucket для ограничения числа запросов от пользователя.

82. **Сессии пользователей**: Храните `session_id` в HttpOnly Cookie, а саму сессию (user_id, roles, preferences) — в Redis Hash с TTL. Реализуйте механизм "продления" сессии при активности.

83. Реализуй **Write-Through cache**: `CreateUser` сначала пишет в БД, затем инвалидирует/обновляет кеш. `UpdateUser` — то же. Покажи консистентность.

84. Ищем юзера в Redis. Если есть — возвращаем (Cache Hit).

85. Реализуй **Read-Through cache** с TTL: данные в кеше живут 5 минут. При обновлении в БД — `CacheDelete` (invalidate). Покажи **eventual consistency**: между `UPDATE` и `CacheDelete` читатели видят stale-данные.

86. **Expiration**: Установи TTL на конкретное поле внутри Hash (до Redis 7.2 это было невозможно через стандартные команды. Используй структуру данных в виде строки JSON с `SET ... EX`, чтобы сэмулировать TTL для одного поля, или обнови до Redis 7.2+ и используй `HEXPIRE`).

87. Распределённая блокировка: захватите ключ с `SETNX` и TTL, выполните критическую секцию, освободите блокировку.

88. **Пакетные запросы (Pipeline)**: Если вам нужно отправить много команд в Redis, отправка каждой команды по отдельности создает накладные расходы на сетевые запросы (RTT). Напишите код, который записывает 1000 ключей в Redis двумя способами:
    * Обычными последовательными вызовами `client.Set`.
    * С использованием `client.Pipeline()` (когда все команды собираются в пакет и отправляются за один сетевой запрос).
    Замерьте и сравните время выполнения обоих вариантов.

89. Реализуй **Rate Limiter** через Redis: `SLIDE_WINDOW`. Ключ = `rate_limit:ip:minute:YYYY-MM-DD-HH-MM`. `INCR` + `EXPIRE 60`. Если счётчик > limit — `429 Too Many Requests`. Используй pipeline для атомарности `INCR` + `EXPIRE`.

90. Реализуй **Rate Limiter (sliding window log)**: `ZADD` timestamp'ов, `ZREMRANGEBYSCORE` старых (старше window), `ZCARD` для подсчёта. Более точный, но дороже по памяти.

91. Если нет (Cache Miss) — идем в PostgreSQL (сделай мок функции).

92. **[Каверзный кейс — Пайплайны]**: Запиши 1000 ключей в Redis. Сначала сделай это в цикле 1000 раз через `client.Set`. Затем сделай то же самое через `client.Pipeline` (одним сетевым запросом). Измерь время выполнения `time.Since()` и оцени разницу.

93. Реализуйте сохранение сессии пользователя в Redis с TTL, обновляемым при каждом запросе.

94. **Testcontainers (Redis)**: Сделайте то же самое для Redis.

95. Реализуй **Session Store** в Redis: `HSET session:abc123 user_id 123 expires 1234567890`. При каждом запросе — `HGETALL` + проверка TTL. `HDEL` при logout. Используй `EXPIRE` на весь ключ.

96. **Распределенная блокировка (Distributed Lock)**: В распределенных системах мьютексы внутри программы не работают на уровне нескольких серверов. Реализуйте распределенную блокировку на Redis:
    * Захват блокировки: вызов команды `SetNX` (Set if Not Exists) с определенным ключом, уникальным токеном (UUID) в качестве значения и TTL 5 секунд.
    * Если блокировка успешно захвачена — выполните работу.
    * Освобождение блокировки: напишите Lua-скрипт (или выполните команду), который безопасно удаляет ключ только в том случае, если значение в Redis по-прежнему совпадает с вашим UUID (чтобы случайно не удалить чужую блокировку, если ваша истекла по таймауту).

97. Реализуй **Pub/Sub** через Redis: `rdb.Subscribe(ctx, "channel1", "channel2")`. В горутине читай `sub.Channel()`. Другой клиент публикует через `rdb.Publish(ctx, "channel1", "message")`. Покажи real-time messaging.

98. Кладем результат из БД в Redis с TTL = 10 минут.

99. **[Pub/Sub]**: Напиши две программы (или две горутины). Первая подписывается на канал `client.Subscribe(ctx, "news")` и читает сообщения в бесконечном цикле. Вторая публикует сообщение `client.Publish(ctx, "news", "Hello")`.

100. Используйте Redis для хранения состояния WebSocket-соединений (связь user_id с идентификаторами сокетов).

101. Реализуй **Redlock** (упрощённый): несколько Redis-нод (3). Для получения lock'а — `SetNX` на большинство нод с одинаковым UUID и TTL. Для release — `Del` на тех же нодах, где получил. Обсуди ограничения Redlock.

102. **Мокание (miniredis)**: Используйте `github.com/alicebob/miniredis` для in-memory эмуляции Redis в unit-тестах.

103. Реализуй **CQRS (упрощённый)**: `WriteModel` — PostgreSQL, `ReadModel` — Redis. При создании заказа — пиши в PostgreSQL, публикуй событие в Redis Pub/Sub. Consumer обновляет Redis (denormalized view). Чтение — из Redis.

---

104. Возвращаем результат.

105. **[Высокая сложность — Распределенная блокировка (Distributed Lock)]**: Реализуй безопасную блокировку с помощью `SET lock_key 1 NX PX 10000` (Set if Not eXists, с истечением 10с). Запусти две горутины, пытающиеся захватить блокировку. Вторая должна ждать или получать ошибку. (Дополнительно: изучи библиотеку Redsync для корректной реализации алгоритма Redlock).

106. Настройте кластер Redis и подключитесь через `redis.NewClusterClient`. Выполните базовые операции.

107. **Pub/Sub в Redis**: Напишите два независимых приложения (или две горутины):
    * Издатель (Publisher) раз в секунду отправляет системные метрики в канал `"system_events"` через `client.Publish`.
    * Подписчик (Subscriber) слушает этот канал через `client.Subscribe` и выводит получаемые метрики на экран в реальном времени.

108. Напиши **"Order Service"** с транзакциями:
- `CreateOrder(userID int, items []OrderItem) (*Order, error)` — транзакция: INSERT order, INSERT items, UPDATE inventory (проверка наличия), `SELECT FOR UPDATE` на inventory
- `GetOrder(id)` — JOIN order + items, cache-aside
- `CancelOrder(id)` — транзакция: UPDATE status, UPDATE inventory (возврат), инвалидация

109. **Распределенная блокировка (Distributed Lock)**: У тебя 3 инстанса приложения, и крон-задачу должен выполнить только один. Используй `client.SetNX(ctx, "cron_lock", "locked", 10*time.Second)` (Set if Not eXists). Тот инстанс, которому Redis ответит `true`, выполняет задачу. Остальные пропускают.

110. **[Транзакции в Redis (MULTI/EXEC)]**: Используй `client.TxPipeline`. В одной транзакции инкрементируй счетчик и добавь элемент в список. Если инкремент пройдет, а добавление в список упадет — откатись.

111. Обработайте таймауты и отмену контекста: передавайте `context.Context` во все операции с БД и Redis.

112. **Rate Limiter (Ограничитель запросов)**: Напиши Middleware для HTTP-сервера (из предыдущего блока). При каждом запросе увеличивай счетчик по ключу `rate:limit:IP` через `Incr`. Если это первый запрос — ставь `Expire` в 1 минуту. Если значение счетчика > 60 — возвращай `http.StatusTooManyRequests`.

---
**Совет к этому блоку:** При работе с базами данных (и SQL, и Redis) всегда помни о **закрытии ресурсов** (`defer rows.Close()`), **таймаутах** (всегда используй методы с `Context`) и **пулах соединений**. База данных — это самое узкое горлышко любого приложения, и то, как твой Go-код с ней общается, напрямую определяет, "ляжет" твой бэкенд под нагрузкой или нет.

113. **[Финальное испытание — Rate Limiter на Redis]**: Реализуй ограничитель скорости (Token Bucket или Sliding Window) для HTTP-сервера. При каждом запросе к API используй Redis для подсчета запросов от конкретного IP-адреса за последнюю минуту. Если запросов больше 10 — возвращай `429 Too Many Requests`.

---

Твой список теперь включает **425 упражнений**. 
Этот план охватывает весь путь от "Что такое переменная в Go" до "Как написать микросервис с кэшированием в Redis, транзакциями в PostgreSQL и конкурентной обработкой". Если проработаешь большую часть этого списка, ты будешь уверенно чувствовать себя на позициях Middle+/Senior. Удачи в покорении баз данных и сетей!

114. Напишите тесты для слоя работы с БД: используйте тестовую базу или `testcontainers-go` для поднятия реального PostgreSQL в контейнере.

---

Теперь полный план насчитывает **360 упражнений**. Этот блок делает вас уверенным backend-разработчиком, способным надёжно работать с двумя ключевыми типами хранилищ. Успешной тренировки!

115. **Инвалидация кэша**: Напишите функцию обновления баланса пользователя в PostgreSQL. Чтобы избежать ситуации "грязного чтения" (когда в базе данных баланс уже обновился, а в кэше Redis висит старое значение), добавьте в функцию обязательное удаление соответствующего ключа из Redis (`client.Del`) в рамках одной логической операции обновления данных.
