# -*- coding: utf-8 -*-
"""Exercises 1..50 of Chapter 42."""

exercises = [
  {
    "num": 1,
    "title": "Плоская структура проекта (Flat Layout) для легковесных микросервисов и CLI-утилит",
    "task": "**Плоская структура (Flat Layout) для микро-сервисов**: Напишите небольшую CLI-утилиту, структура которой намеренно сделана плоской: все `.go` файлы лежат в корне проекта (без папок `pkg`, `internal`, `cmd`). Обсудите, когда это лучший выбор, а когда превратится в кошмар.",
    "theory": "Плоская структура (Flat Layout) в философии Go:\n- Все `.go` файлы находятся в одном пакете в корне репозитория (например, `package main`).\n- **Когда это лучший выбор:**\n  - Небольшие CLI-утилиты (до 1000–2000 строк кода), сервисы-агенты, лямбда-функции (AWS Lambda, Yandex Cloud Functions).\n  - Отсутствие оверхеда на иерархию папок, круговые импорты (Circular Imports) невозможны физически, так как весь проект компилируется как единый пакет.\n- **Когда это превращается в кошмар:**\n  - При росте кодовой базы свыше 10 файлов: размываются границы ответственности, глобальное пространство имен засоряется, усложняется командная разработка и разграничение приватного API.",
    "step_by_step": "1. Создайте доменную структуру конфигурации и команду CLI в одном пространстве имен.\n2. Реализуйте функционал утилиты без дробления на подпакеты.\n3. Продемонстрируйте исполнение и модульное тестирование плоской структуры.\n4. Верифицируйте корректность работы без лишних абстракций.",
    "code_blocks": [
      {
        "filename": "flat_layout_cli_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"flag\"\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\n// Конфигурация и логика в одном плоском пространстве имен пакета main\ntype CLIApp struct {\n\tGreeting string\n\tTarget   string\n\tUppercase bool\n}\n\nfunc (app *CLIApp) Run(out *bytes.Buffer) error {\n\tmsg := fmt.Sprintf(\"%s, %s!\", app.Greeting, app.Target)\n\tif app.Uppercase {\n\t\tmsg = strings.ToUpper(msg)\n\t}\n\t_, err := out.WriteString(msg)\n\treturn err\n}\n\nfunc TestFlatLayoutCLI(t *testing.T) {\n\tapp := CLIApp{\n\t\tGreeting:  \"Привет\",\n\t\tTarget:    \"Gopher\",\n\t\tUppercase: true,\n\t}\n\n\tvar buf bytes.Buffer\n\tif err := app.Run(&buf); err != nil {\n\t\tt.Fatalf(\"Ошибка выполнения CLI утилиты: %v\", err)\n\t}\n\n\texpected := \"ПРИВЕТ, GOPHER!\"\n\tif buf.String() != expected {\n\t\tt.Fatalf(\"Ожидалось %q, получено %q\", expected, buf.String())\n\t}\n\n\tfmt.Println(\"Плоская структура проекта (Flat Layout) успешно подтверждена:\")\n\tfmt.Printf(\"  • Результат CLI: %s\\n\", buf.String())\n\tfmt.Println(\"  • Все файлы скомпилированы в package main без искусственных папок pkg/cmd\")\n}",
        "note": "Реализация CLI-утилиты в плоской структуре без избыточных пакетов"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v flat_layout_cli_test.go\n# Вывод:\n# === RUN   TestFlatLayoutCLI\n# Плоская структура проекта (Flat Layout) успешно подтверждена:\n#   • Результат CLI: ПРИВЕТ, GOPHER!\n#   • Все файлы скомпилированы в package main без искусственных папок pkg/cmd\n# --- PASS: TestFlatLayoutCLI (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Компилятор `compile` компилирует все `.go` файлы одного пакета одновременно, объединяя их символы в единый AST (Abstract Syntax Tree). В плоской структуре компиляция выполняется максимально быстро, так как компилятору не требуется загружать промежуточные `.a` архивные файлы зависимых пакетов.",
    "pitfalls": "Использовать Flat Layout для крупных enterprise-проектов: все функции и структуры видны друг другу, программисты начинают вызывать приватную логику баз данных напрямую из HTTP-хендлеров, порождая спагетти-код.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в Go не рекомендуется преждевременно дробить проект на десятки микропакетов (Over-engineering)?»\n**Ответ:** Преждевременная пакетизация в Go наказывается ошибками `import cycle not allowed`. В Go запрещены циклические зависимости. Если разделить логику не по естественным доменным границам, а механически, разработчик упрется в циклические импорты и будет вынужден создавать бесполезные пакеты-свалки (`common/`, `utils/`). Идиоматичный подход Go — начинать с плоской структуры и выделять пакеты только по мере роста независимых абстракций."
  },
  {
    "num": 2,
    "title": "Пакетная организация по слоям (Package by Layer): models, repository, service и transport",
    "task": "**Организация по слоям (Package by Layer)**: Создайте проект, в котором пакеты разделены по техническим слоям: `models/`, `repository/`, `service/`, `transport/`. Реализуйте в этой структуре сервис заметок (Note Service).",
    "theory": "Концепция Package by Layer (Горизонтальное разделение):\n- Классический подход корпоративной разработки (Spring, Django, ASP.NET):\n  - `transport/`: парсинг HTTP/gRPC запросов, валидация DTO, сериализация JSON.\n  - `service/`: сценарии бизнес-логики (Use Cases).\n  - `repository/`: хранение и выборка данных из базы (SQL, Redis, In-Memory).\n  - `models/`: DTO и базовые структуры данных.\n- **Преимущества:** Понятная и предсказуемая структура для начинающих разработчиков.\n- **Недостатки:** Низкая сцепленность (Cohesion): чтобы добавить одно поле в сущность Note, приходится модифицировать файлы в 4 разных папках.",
    "step_by_step": "1. Определите сущность `Note` в доменном слое.\n2. Создайте интерфейс и реализацию слоя `repository`.\n3. Напишите бизнес-сервис `service`, управляющий заметками.\n4. Смоделируйте транспортный слой `transport` и верифицируйте поток управления.",
    "code_blocks": [
      {
        "filename": "layered_architecture_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\n// 1. Layer: Models\ntype Note struct {\n\tID        int64\n\tTitle     string\n\tContent   string\n\tCreatedAt time.Time\n}\n\n// 2. Layer: Repository (хранилище)\ntype NoteRepository interface {\n\tSave(ctx context.Context, note *Note) error\n\tFindByID(ctx context.Context, id int64) (*Note, error)\n}\n\ntype InMemoryNoteRepo struct {\n\tmu    sync.RWMutex\n\tstore map[int64]*Note\n\tseq   int64\n}\n\nfunc NewInMemoryNoteRepo() *InMemoryNoteRepo {\n\treturn &InMemoryNoteRepo{store: make(map[int64]*Note)}\n}\n\nfunc (r *InMemoryNoteRepo) Save(ctx context.Context, note *Note) error {\n\tr.mu.Lock()\n\tdefer r.mu.Unlock()\n\tr.seq++\n\tnote.ID = r.seq\n\tnote.CreatedAt = time.Now()\n\tr.store[note.ID] = note\n\treturn nil\n}\n\nfunc (r *InMemoryNoteRepo) FindByID(ctx context.Context, id int64) (*Note, error) {\n\tr.mu.RLock()\n\tdefer r.mu.RUnlock()\n\tn, ok := r.store[id]\n\tif !ok {\n\t\treturn nil, errors.New(\"note not found\")\n\t}\n\treturn n, nil\n}\n\n// 3. Layer: Service (бизнес-логика)\ntype NoteService struct {\n\trepo NoteRepository\n}\n\nfunc NewNoteService(repo NoteRepository) *NoteService {\n\treturn &NoteService{repo: repo}\n}\n\nfunc (s *NoteService) CreateNote(ctx context.Context, title, content string) (*Note, error) {\n\tif title == \"\" {\n\t\treturn nil, errors.New(\"title cannot be empty\")\n\t}\n\tnote := &Note{Title: title, Content: content}\n\tif err := s.repo.Save(ctx, note); err != nil {\n\t\treturn nil, err\n\t}\n\treturn note, nil\n}\n\nfunc TestPackageByLayer(t *testing.T) {\n\trepo := NewInMemoryNoteRepo()\n\tsvc := NewNoteService(repo)\n\n\tctx := context.Background()\n\tnote, err := svc.CreateNote(ctx, \"Архитектура Go\", \"Слои: transport -> service -> repo\")\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка создания заметки: %v\", err)\n\t}\n\n\tif note.ID != 1 || note.Title != \"Архитектура Go\" {\n\t\tt.Fatalf(\"Некорректные данные заметки: %+v\", note)\n\t}\n\n\tfmt.Println(\"Слоистая организация (Package by Layer) успешно верифицирована:\")\n\tfmt.Printf(\"  • Создана заметка ID=%d, Заголовок: %q\\n\", note.ID, note.Title)\n\tfmt.Printf(\"  • Поток вызовов: Handler -> Service -> Repository -> Storage\\n\")\n}",
        "note": "Разделение логики сервиса на строгие технические слои"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v layered_architecture_test.go\n# Вывод:\n# === RUN   TestPackageByLayer\n# Слоистая организация (Package by Layer) успешно верифицирована:\n#   • Создана заметка ID=1, Заголовок: \"Архитектура Go\"\n#   • Поток вызовов: Handler -> Service -> Repository -> Storage\n# --- PASS: TestPackageByLayer (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go слоистая архитектура опирается на композицию структур. Связывание слоев выполняется во время компиляции через интерфейсные таблицы `itab`, что позволяет подменять реальные репозитории на моки в юнит-тестах с нулевым оверхедом рантайма.",
    "pitfalls": "Помещать доменную логику в слой `repository` (например, валидацию полей или расчет скидок): репозиторий должен отвечать исключительно за персистентность (CRUD), иначе бизнес-логика размазывается по всей системе.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему структура models/ часто считается антипаттерном в идиоматичном Go?»\n**Ответ:** Пакет с именем `models` не несет смысловой нагрузки (пакет «всего подряд»). Он провоцирует циклические импорты и хранение анемичных структур данных без поведения. В идиоматичном Go пакеты называют по бизнес-доменам: `user`, `order`, `billing`, где структура и методы работы с ней живут рядом."
  },
  {
    "num": 3,
    "title": "Организация по фичам (Package by Feature): изоляция доменов и высокая связность",
    "task": "**Организация по фичам (Package by Feature)**: Перепишите сервис заметок так, чтобы код был сгруппирован не по слоям, а по фичам: папка `notes/` содержит и хэндлер, и сервис, и репозиторий для заметок. Сравните с Package by Layer.",
    "theory": "Принцип высокой связности (High Cohesion, Low Coupling):\n- **Package by Feature (Вертикальные срезы):**\n  - Папка `notes/` содержит все, что относится к заметкам: `note.go` (модель), `service.go` (логика), `repository.go` (БД), `handler.go` (HTTP).\n  - Папка `users/` содержит все компоненты пользователя.\n- **Главное преимущество:**\n  - Изменение фичи локализовано в одной папке!\n  - Простота удаления или рефакторинга: если функционал заметок больше не нужен, удаляется одна папка `notes/` без поиска хвостов по 5 слоям.\n  - Идеально для масштабирования продуктовых команд.",
    "step_by_step": "1. Создайте изолированный пакет `notes` со всеми компонентами.\n2. Инкапсулируйте внутренние детали хранения внутри пакета.\n3. Экспортируйте наружу только фасад сервиса.\n4. Проверьте независимость фичи от других частей приложения.",
    "code_blocks": [
      {
        "filename": "package_by_feature_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Моделирование структуры notes/feature.go\ntype NoteFeature struct {\n\tstore map[string]string\n}\n\nfunc NewNoteFeature() *NoteFeature {\n\treturn &NoteFeature{store: make(map[string]string)}\n}\n\n// Слой UseCase + Repo внутри одного пакета фичи\nfunc (f *NoteFeature) Create(ctx context.Context, key, val string) error {\n\tf.store[key] = val\n\treturn nil\n}\n\nfunc (f *NoteFeature) Get(ctx context.Context, key string) (string, bool) {\n\tval, ok := f.store[key]\n\treturn val, ok\n}\n\nfunc TestPackageByFeature(t *testing.T) {\n\tnotes := NewNoteFeature()\n\tctx := context.Background()\n\n\t_ = notes.Create(ctx, \"feature-arch\", \"High Cohesion: все файлы фичи в одной папке\")\n\tval, ok := notes.Get(ctx, \"feature-arch\")\n\n\tif !ok || val == \"\" {\n\t\tt.Fatal(\"Фича заметок не сохранила данные\")\n\t}\n\n\tfmt.Println(\"Организация по фичам (Package by Feature) успешно подтверждена:\")\n\tfmt.Printf(\"  • Доменная фича 'Notes' полностью инкапсулирована\\n\")\n\tfmt.Printf(\"  • Значение: %s\\n\", val)\n\tfmt.Println(\"  • Удаление фичи из кодовой базы сводится к удалению одной папки notes/\")\n}",
        "note": "Инкапсуляция сущности, логики и персистентности внутри пакета фичи"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v package_by_feature_test.go\n# Вывод:\n# === RUN   TestPackageByFeature\n# Организация по фичам (Package by Feature) успешно подтверждена:\n#   • Доменная фича 'Notes' полностью инкапсулирована\n#   • Значение: High Cohesion: все файлы фичи в одной папке\n#   • Удаление фичи из кодовой базы сводится к удалению одной папки notes/\n# --- PASS: TestPackageByFeature (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Package by Feature уменьшает когнитивную нагрузку на разработчика (Cognitive Load). В Go область видимости идентификаторов с маленькой буквы (`unexported`) ограничена пакетом. В Package by Feature вы можете скрыть методы репозитория от внешнего мира, оставив их приватными для пакета фичи.",
    "pitfalls": "Попытка расшарить приватные структуры между фичами через взаимный импорт: если `users` начнет импортировать `notes`, а `notes` — `users`, проект не скомпилируется из-за `import cycle`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как в архитектуре Package by Feature организовать связь между заказами (Orders) и пользователями (Users) без циклического импорта?»\n**Ответ:** Связь организуется через слабую связанность по идентификаторам (`UserID int64` в структуре заказа, а не ссылка на объект `*User`), либо через доменные события (Domain Events) в шине событий, либо через вынесение общих контрактов в независимый слой интерфейсов."
  },
  {
    "num": 4,
    "title": "Слоистая архитектура «handler → service → repository»: строгий CRUD и изоляция зависимостей",
    "task": "**Слоистая архитектура «handler → service → repository»**. Реализуйте CRUD для сущности User с in-memory хранилищем, строго разделяя транспортный, бизнес-логический и storage слои.",
    "theory": "Правило направления зависимостей в 3-слойной архитектуре:\n- **Зависимости направлены строго сверху вниз:**\n  - `Handler` зависит от интерфейса `Service`.\n  - `Service` зависит от интерфейса `Repository`.\n  - `Repository` не знает ни о сервисе, ни о хендлере.\n  - `Domain Model` (сущность User) находится на самом глубоком уровне и не импортирует внешние пакеты.\n- **Изоляция:** Изменение SQL-запросов или замена PostgreSQL на MongoDB не должна приводить к изменению ни единой строчки в слое `Service` или `Handler`.",
    "step_by_step": "1. Создайте сущность `User` с валидацией email.\n2. Опишите интерфейс репозитория `UserRepository` и in-memory реализацию.\n3. Реализуйте методы CRUD в `UserService`.\n4. Смоделируйте `UserHandler` и верифицируйте корректность обработки ошибок.",
    "code_blocks": [
      {
        "filename": "three_tier_user_crud_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\n// Доменная сущность\ntype User struct {\n\tID    int64\n\tName  string\n\tEmail string\n}\n\n// Слой Storage: интерфейс\ntype UserRepository interface {\n\tCreate(ctx context.Context, u *User) error\n\tGetByID(ctx context.Context, id int64) (*User, error)\n\tDelete(ctx context.Context, id int64) error\n}\n\ntype MemoryUserRepo struct {\n\tmu    sync.RWMutex\n\tusers map[int64]*User\n\tseq   int64\n}\n\nfunc NewMemoryUserRepo() *MemoryUserRepo {\n\treturn &MemoryUserRepo{users: make(map[int64]*User)}\n}\n\nfunc (r *MemoryUserRepo) Create(ctx context.Context, u *User) error {\n\tr.mu.Lock()\n\tdefer r.mu.Unlock()\n\tr.seq++\n\tu.ID = r.seq\n\tr.users[u.ID] = u\n\treturn nil\n}\n\nfunc (r *MemoryUserRepo) GetByID(ctx context.Context, id int64) (*User, error) {\n\tr.mu.RLock()\n\tdefer r.mu.RUnlock()\n\tu, ok := r.users[id]\n\tif !ok {\n\t\treturn nil, errors.New(\"user not found\")\n\t}\n\treturn u, nil\n}\n\nfunc (r *MemoryUserRepo) Delete(ctx context.Context, id int64) error {\n\tr.mu.Lock()\n\tdefer r.mu.Unlock()\n\tdelete(r.users, id)\n\treturn nil\n}\n\n// Слой Service: бизнес-логика\ntype UserService struct {\n\trepo UserRepository\n}\n\nfunc NewUserService(repo UserRepository) *UserService {\n\treturn &UserService{repo: repo}\n}\n\nfunc (s *UserService) Register(ctx context.Context, name, email string) (*User, error) {\n\tif name == \"\" || email == \"\" {\n\t\treturn nil, errors.New(\"invalid user payload\")\n\t}\n\tu := &User{Name: name, Email: email}\n\tif err := s.repo.Create(ctx, u); err != nil {\n\t\treturn nil, err\n\t}\n\treturn u, nil\n}\n\nfunc TestThreeTierUserCRUD(t *testing.T) {\n\trepo := NewMemoryUserRepo()\n\tsvc := NewUserService(repo)\n\n\tctx := context.Background()\n\tuser, err := svc.Register(ctx, \"Алексей Смирнов\", \"alex@bigtech.ru\")\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка регистрации: %v\", err)\n\t}\n\n\tfound, err := repo.GetByID(ctx, user.ID)\n\tif err != nil || found.Email != \"alex@bigtech.ru\" {\n\t\tt.Fatalf(\"Пользователь не найден в репозитории: %v\", err)\n\t}\n\n\tfmt.Println(\"Слоистая архитектура CRUD успешно подтверждена:\")\n\tfmt.Printf(\"  • Пользователь ID=%d, Имя: %s, Email: %s\\n\", found.ID, found.Name, found.Email)\n\tfmt.Println(\"  • Зависимости: Handler -> UserService -> UserRepository -> InMemoryStorage\")\n}",
        "note": "Строгая 3-слойная реализация CRUD пользователя с изоляцией хранилища"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v three_tier_user_crud_test.go\n# Вывод:\n# === RUN   TestThreeTierUserCRUD\n# Слоистая архитектура CRUD успешно подтверждена:\n#   • Пользователь ID=1, Имя: Алексей Смирнов, Email: alex@bigtech.ru\n#   • Зависимости: Handler -> UserService -> UserRepository -> InMemoryStorage\n# --- PASS: TestThreeTierUserCRUD (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Использование контекста `context.Context` в качестве первого параметра каждого метода репозитория и сервиса гарантирует сквозную отмену операций при отмене клиентского HTTP запроса или срабатывании дедлайна базы данных.",
    "pitfalls": "Пробрасывать типы конкретных СУБД (например `*sql.Rows`, `pgconn.PgError`) в слой `Service`: это нарушает инкапсуляцию, заставляя бизнес-логику знать о деталях реализации репозитория.",
    "bigtech_interview": "**Вопрос с собеседования:** «Каким образом слой сервиса должен обрабатывать ситуацию, когда сущность в БД не найдена (sql.ErrNoRows)?»\n**Ответ:** Репозиторий обязан перехватить `sql.ErrNoRows` и обернуть его в доменную ошибку (например `domain.ErrNotFound` или `ErrUserNotFound`). Слой сервиса сравнивает ошибку через `errors.Is(err, domain.ErrNotFound)`, оставаясь полностью изолированным от драйвера базы данных."
  },
  {
    "num": 5,
    "title": "Минимально жизнеспособная архитектура (3-Tier Architecture): почему это де-факто стандарт",
    "task": "**Трёхслойная архитектура (3-Tier)**: Создайте проект со слоями: Handler (HTTP) → Service (бизнес-логика) → Repository (БД). Изучите, почему это минимально жизнеспособная структура.",
    "theory": "Почему 3-Tier является золотым стандартом бэкенда:\n- 1 слой (все в хэндлере) — превращается в нетестируемый спагетти-монолит при первом же расширении.\n- 5+ слоев (Clean/Hexagonal с 10 DTO и мапперами на запрос) — избыточный оверхед для 80% типовых CRUD-микросервисов.\n- **3-Tier — точка идеального баланса (Sweet Spot):**\n  1. `Handler`: знает только про транспортный протокол (HTTP статус-коды, JSON, заголовки).\n  2. `Service`: содержит чистые правила бизнеса, не зависит от HTTP и SQL.\n  3. `Repository`: содержит только логику персистентности (SQL, транзакции).",
    "step_by_step": "1. Создайте интерфейс бизнес-сервиса.\n2. Смоделируйте HTTP хендлер, делегирующий работу сервису.\n3. Продемонстрируйте подмену сервиса моком для изолированного тестирования хендлера.\n4. Верифицируйте статус 200 OK и изоляцию бизнес-логики.",
    "code_blocks": [
      {
        "filename": "three_tier_standard_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"strings\"\n\t\"testing\"\n)\n\ntype BalanceService interface {\n\tGetBalance(accountID string) (int64, error)\n}\n\ntype MockBalanceService struct {\n\tbalance int64\n}\n\nfunc (m *MockBalanceService) GetBalance(accountID string) (int64, error) {\n\treturn m.balance, nil\n}\n\ntype AccountHandler struct {\n\tsvc BalanceService\n}\n\nfunc (h *AccountHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {\n\taccID := r.URL.Query().Get(\"account_id\")\n\tif accID == \"\" {\n\t\thttp.Error(w, \"missing account_id\", http.StatusBadRequest)\n\t\treturn\n\t}\n\tbal, _ := h.svc.GetBalance(accID)\n\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t_ = json.NewEncoder(w).Encode(map[string]any{\"account_id\": accID, \"balance\": bal})\n}\n\nfunc TestThreeTierStandard(t *testing.T) {\n\tmockSvc := &MockBalanceService{balance: 50_000}\n\thandler := &AccountHandler{svc: mockSvc}\n\n\treq := httptest.NewRequest(http.MethodGet, \"/balance?account_id=acc-777\", nil)\n\trec := httptest.NewRecorder()\n\n\thandler.ServeHTTP(rec, req)\n\n\tif rec.Code != http.StatusOK {\n\t\tt.Fatalf(\"Ожидался код 200, получено: %d\", rec.Code)\n\t}\n\n\tif !strings.Contains(rec.Body.String(), \"50000\") {\n\t\tt.Fatalf(\"Некорректный баланс в ответе: %s\", rec.Body.String())\n\t}\n\n\tfmt.Println(\"Минимально жизнеспособная архитектура 3-Tier успешно подтверждена:\")\n\tfmt.Printf(\"  • Handler изолированно протестирован через Mock-сервис\\n\")\n\tfmt.Printf(\"  • Ответ HTTP: %s\\n\", strings.TrimSpace(rec.Body.String()))\n}",
        "note": "Изолированное тестирование HTTP хендлера с подменой слоя сервиса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v three_tier_standard_test.go\n# Вывод:\n# === RUN   TestThreeTierStandard\n# Минимально жизнеспособная архитектура 3-Tier успешно подтверждена:\n#   • Handler изолированно протестирован через Mock-сервис\n#   • Ответ HTTP: {\"account_id\":\"acc-777\",\"balance\":50000}\n# --- PASS: TestThreeTierStandard (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go абстракция `httptest.ResponseRecorder` реализует интерфейс `http.ResponseWriter`, аккумулируя байты в `bytes.Buffer`. Это позволяет тестировать транспортный слой целиком в памяти без открытия реальных сокетов ОС, ускоряя выполнение тестов в сотни раз.",
    "pitfalls": "Вызывать методы репозитория из хэндлера в обход сервиса («зачем мне сервис, если тут простой SELECT?»): это ломает границы архитектуры; при появлении первого требования к проверке прав или отправке события придется переписывать хэндлер.",
    "bigtech_interview": "**Вопрос с собеседования:** «Где в 3-слойной архитектуре должны открываться и коммититься транзакции базы данных?»\n**Ответ:** Границы транзакции определяются в слое **Service** (бизнес-транзакция Use Case), но реализация транзакции не должна завязываться на конкретный `*sql.Tx`. Используют паттерн Unit of Work или передают транзакционный контекст `context.Context` через замыкание `repo.WithTx(ctx, func(txRepo) error)`."
  },
  {
    "num": 6,
    "title": "Направление зависимостей в слоистой архитектуре: принцип обращения зависимостей внутрь",
    "task": "Раздели на **слои (layered architecture)**: `handlers/` (HTTP), `services/` (бизнес-логика), `repositories/` (данные), `models/` (сущности). Каждый слой зависит только от нижележащего. Покажи направление зависимостей (внутрь).",
    "theory": "Принцип центростремительных зависимостей (Inward Dependency Rule):\n- В классической слоистой архитектуре зависимости направлены внутрь ядра:\n  $$\\text{Handlers} \\longrightarrow \\text{Services} \\longrightarrow \\text{Repositories} \\longrightarrow \\text{Models (Core)}$$\n- `Models` — не зависит ни от чего (чистые типы Go).\n- `Repositories` — зависит от `Models`.\n- `Services` — зависит от `Repositories` и `Models`.\n- `Handlers` — зависит от `Services` и `Models`.\n- Никакой внутренний слой никогда не должен импортировать внешний!",
    "step_by_step": "1. Создайте независимое ядро данных `Product`.\n2. Создайте слой хранения `ProductRepository`.\n3. Создайте бизнес-слой `ProductService`, рассчитывающий налог.\n4. Продемонстрируйте вызов через транспортный фасад и проверьте инварианты.",
    "code_blocks": [
      {
        "filename": "inward_dependency_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Слой 1: Models (Ядро, нулевые внешние зависимости)\ntype Product struct {\n\tID       string\n\tBaseCost float64\n}\n\n// Слой 2: Repositories\ntype ProductCatalog interface {\n\tFind(id string) (Product, error)\n}\n\ntype CatalogStub struct{}\n\nfunc (c *CatalogStub) Find(id string) (Product, error) {\n\treturn Product{ID: id, BaseCost: 1000.0}, nil\n}\n\n// Слой 3: Services (Зависит от Catalog и Product)\ntype PricingService struct {\n\tcatalog ProductCatalog\n\ttaxRate float64\n}\n\nfunc (s *PricingService) CalculateTotal(id string) (float64, error) {\n\tp, err := s.catalog.Find(id)\n\tif err != nil {\n\t\treturn 0, err\n\t}\n\treturn p.BaseCost * (1.0 + s.taxRate), nil\n}\n\nfunc TestInwardDependency(t *testing.T) {\n\tcatalog := &CatalogStub{}\n\tpricing := &PricingService{catalog: catalog, taxRate: 0.20} // 20% НДС\n\n\ttotal, err := pricing.CalculateTotal(\"sku-apple-macbook\")\n\tif err != nil || total != 1200.0 {\n\t\tt.Fatalf(\"Ошибка расчета стоимости: total=%v, err=%v\", total, err)\n\t}\n\n\tfmt.Println(\"Направление зависимостей внутрь успешно продемонстрировано:\")\n\tfmt.Printf(\"  • Товар: sku-apple-macbook, Базовая цена: 1000.0, Итого с налогом: %.2f\\n\", total)\n\tfmt.Println(\"  • Models не импортирует ни один слой; PricingService зависит от интерфейса Catalog\")\n}",
        "note": "Строгое направление вызовов от внешних уровней к доменному ядру"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v inward_dependency_test.go\n# Вывод:\n# === RUN   TestInwardDependency\n# Направление зависимостей внутрь успешно продемонстрировано:\n#   • Товар: sku-apple-macbook, Базовая цена: 1000.0, Итого с налогом: 1200.00\n#   • Models не импортирует ни один слой; PricingService зависит от интерфейса Catalog\n# --- PASS: TestInwardDependency (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Соблюдение направления зависимостей внутрь позволяет компилятору Go собирать пакеты параллельно. Компилятор строит направленный ациклический граф (DAG). Если зависимости запутаны, время сборки резко возрастает, так как компилятор не может распараллелить сборку на ядрах CPU.",
    "pitfalls": "Импортировать пакет `handlers` в пакет `services` (например, чтобы переиспользовать константу или структуру ответа): это мгновенно создает архитектурную дыру и риск циклического импорта.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в Clean Architecture Uncle Bob слой Use Cases не должен знать о формате HTTP (например r.Header, status codes)?»\n**Ответ:** Потому что бизнес-сценарий (Use Case) должен выполняться одинаково независимо от источника вызова: пришел ли запрос из HTTP REST API, gRPC вызова, сообщения Kafka или CLI консоли. Если бизнес-слой привязан к `*http.Request`, переиспользовать его в CLI или Kafka consumer будет невозможно."
  },
  {
    "num": 7,
    "title": "Стандартная раскладка проекта (Standard Go Project Layout): /cmd, /internal, /pkg и /api",
    "task": "**Структура проекта**: Организуйте код по стандарту `golang-standards/project-layout`: `/cmd`, `/internal`, `/pkg`, `/api`, `/migrations`, `/scripts`.",
    "theory": "Каноническая структура репозитория в Go:\n- **`/cmd/`:** Точки входа в приложение (`/cmd/api-server/main.go`, `/cmd/migrator/main.go`). Код здесь минимален: парсинг флагов, сборка графа зависимостей и вызов `app.Run()`.\n- **`/internal/`:** Приватный код приложения. **Спецификация компилятора Go гарантирует:** пакеты внутри `/internal/` не могут быть импортированы сторонними проектами.\n- **`/pkg/`:** Публичные библиотеки, которые разрешено импортировать внешним сервисам.\n- **`/api/`:** Контракты Protobuf, спецификации OpenAPI/Swagger.\n- **`/migrations/`:** SQL-файлы миграций схемы базы данных.",
    "step_by_step": "1. Создайте модель структуры репозитория.\n2. Продемонстрируйте разграничение ответственности между `cmd/` и `internal/`.\n3. Реализуйте проверку правила компилятора Go для каталога `internal`.\n4. Верифицируйте соответствие стандарту кодовой базы BigTech.",
    "code_blocks": [
      {
        "filename": "standard_layout_tree_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\ntype ProjectDirectory struct {\n\tPath        string\n\tPurpose     string\n\tIsProtected bool\n}\n\nfunc GetStandardLayout() []ProjectDirectory {\n\treturn []ProjectDirectory{\n\t\t{Path: \"/cmd/<app-name>/main.go\", Purpose: \"Точки входа, парсинг CLI флагов и DI сборка\", IsProtected: false},\n\t\t{Path: \"/internal/app/...\", Purpose: \"Приватная бизнес-логика (защищена компилятором Go)\", IsProtected: true},\n\t\t{Path: \"/pkg/client/...\", Purpose: \"Публичный SDK для внешних потребителей\", IsProtected: false},\n\t\t{Path: \"/api/proto/...\", Purpose: \"Схемы OpenAPI, gRPC proto файлы\", IsProtected: false},\n\t\t{Path: \"/migrations/...\", Purpose: \"Версионированные SQL миграции схемы БД\", IsProtected: false},\n\t\t{Path: \"/scripts/...\", Purpose: \"Bash скрипты сборки, линтинга и CI/CD\", IsProtected: false},\n\t}\n}\n\nfunc TestStandardLayoutTree(t *testing.T) {\n\tlayout := GetStandardLayout()\n\n\tinternalFound := false\n\tfor _, dir := range layout {\n\t\tif strings.HasPrefix(dir.Path, \"/internal\") && dir.IsProtected {\n\t\t\tinternalFound = true\n\t\t\tbreak\n\t\t}\n\t}\n\n\tif !internalFound {\n\t\tt.Fatal(\"Каталог /internal обязан присутствовать и иметь статус protected\")\n\t}\n\n\tfmt.Println(\"Стандартная структура проекта (golang-standards/project-layout):\")\n\tfor _, dir := range layout {\n\t\tfmt.Printf(\"  • %-26s -> %s\\n\", dir.Path, dir.Purpose)\n\t}\n\tfmt.Println(\"  • Компилятор Go запрещает внешним модулям импортировать пути с префиксом /internal/\")\n}",
        "note": "Структурное дерево каталогов корпоративного микросервиса на Go"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v standard_layout_tree_test.go\n# Вывод:\n# === RUN   TestStandardLayoutTree\n# Стандартная структура проекта (golang-standards/project-layout):\n#   • /cmd/<app-name>/main.go    -> Точки входа, парсинг CLI флагов и DI сборка\n#   • /internal/app/...          -> Приватная бизнес-логика (защищена компилятором Go)\n#   • /pkg/client/...            -> Публичный SDK для внешних потребителей\n#   • /api/proto/...             -> Схемы OpenAPI, gRPC proto файлы\n#   • /migrations/...            -> Версионированные SQL миграции схемы БД\n#   • /scripts/...               -> Bash скрипты сборки, линтинга и CI/CD\n#   • Компилятор Go запрещает внешним модулям импортировать пути с префиксом /internal/\n# --- PASS: TestStandardLayoutTree (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Правило `internal` вшито в компилятор Go начиная с версии 1.4: если модуль `A` пытается импортировать `github.com/company/project/internal/secret`, компилятор выдает ошибку `use of internal package not allowed`, защищая приватный код приложения от несанкционированного переиспользования.",
    "pitfalls": "Помещать весь код в папку `/pkg`: это превращает все внутренности сервиса в публичный контракт. Любое изменение внутренней структуры сломает внешних пользователей, если они успели завязаться на ваш `pkg`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в /cmd/server/main.go не должно быть бизнес-логики?»\n**Ответ:** Потому что `main.go` — это всего лишь «клей» (Composition Root), связывающий конфиг, базу данных, зависимости и запускающий HTTP/gRPC сервер. Если бизнес-логика живет в `main`, ее невозможно импортировать в интеграционные тесты или переиспользовать в альтернативной CLI-команде."
  },
  {
    "num": 8,
    "title": "Принцип инверсии зависимостей (DIP): объявление интерфейсов на стороне потребителя",
    "task": "Инвертируй зависимости через **интерфейсы**: `repository.UserRepository` — интерфейс, `repository.PostgresUserRepository` — реализация. `service.UserService` принимает `UserRepository`, не знает о PostgreSQL. Покажи DIP (Dependency Inversion Principle).",
    "theory": "Принцип инверсии зависимостей (Dependency Inversion Principle, DIP):\n1. Модули верхних уровней не должны зависеть от модулей нижних уровней. Оба должны зависеть от абстракций (интерфейсов).\n2. Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.\n- **В Go действует правило Consumer-Driven Interfaces:**\n  Интерфейс `UserRepository` должен объявляться в пакете `service`, которому он нужен, а не в пакете базы данных `postgres`!\n  Пакет `postgres` просто реализует нужные методы, даже не импортируя пакет `service` (неявная реализация интерфейсов).",
    "step_by_step": "1. Создайте доменную структуру `Customer`.\n2. Объявите интерфейс `CustomerReader` в сервисе-потребителе.\n3. Реализуйте Postgres-адаптер без явных связей.\n4. Продемонстрируйте DIP и подменяемость базы данных.",
    "code_blocks": [
      {
        "filename": "dip_consumer_interfaces_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype Customer struct {\n\tID   string\n\tName string\n}\n\n// DIP: Интерфейс объявляется на стороне ПОТРЕБИТЕЛЯ (Consumer)\ntype CustomerReader interface {\n\tFindCustomer(ctx context.Context, id string) (*Customer, error)\n}\n\n// Сервис зависит только от абстракции CustomerReader\ntype CustomerService struct {\n\treader CustomerReader\n}\n\nfunc NewCustomerService(reader CustomerReader) *CustomerService {\n\treturn &CustomerService{reader: reader}\n}\n\nfunc (s *CustomerService) GreetCustomer(ctx context.Context, id string) (string, error) {\n\tc, err := s.reader.FindCustomer(ctx, id)\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn fmt.Sprintf(\"Здравствуйте, %s!\", c.Name), nil\n}\n\n// Деталь реализации: Postgres адаптер (не знает о CustomerService!)\ntype PostgresCustomerRepo struct{}\n\nfunc (p *PostgresCustomerRepo) FindCustomer(ctx context.Context, id string) (*Customer, error) {\n\t// Имитация SQL запроса SELECT * FROM customers WHERE id = $1\n\treturn &Customer{ID: id, Name: \"Иван Иванов\"}, nil\n}\n\nfunc TestDIPConsumerInterfaces(t *testing.T) {\n\tdbRepo := &PostgresCustomerRepo{}\n\tsvc := NewCustomerService(dbRepo)\n\n\tgreeting, err := svc.GreetCustomer(context.Background(), \"cust-101\")\n\tif err != nil || greeting != \"Здравствуйте, Иван Иванов!\" {\n\t\tt.Fatalf(\"Ошибка приветствия: %v\", err)\n\t}\n\n\tfmt.Println(\"Принцип инверсии зависимостей (DIP) успешно подтвержден:\")\n\tfmt.Printf(\"  • Сервис выполнил бизнес-логику: %q\\n\", greeting)\n\tfmt.Println(\"  • CustomerService ничего не знает о PostgresCustomerRepo!\")\n\tfmt.Println(\"  • Зависимость инвертирована: реализация БД зависит от абстракции сервиса\")\n}",
        "note": "Объявление интерфейса в пакете бизнес-логики и его реализация в слое адаптера БД"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v dip_consumer_interfaces_test.go\n# Вывод:\n# === RUN   TestDIPConsumerInterfaces\n# Принцип инверсии зависимостей (DIP) успешно подтвержден:\n#   • Сервис выполнил бизнес-логику: \"Здравствуйте, Иван Иванов!\"\n#   • CustomerService ничего не знает о PostgresCustomerRepo!\n#   • Зависимость инвертирована: реализация БД зависит от абстракции сервиса\n# --- PASS: TestDIPConsumerInterfaces (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Благодаря неявной типизации («Duck Typing») в Go структура удовлетворяет интерфейсу автоматически, если она реализует его сигнатуры методов. Это полностью разрывает зависимость между пакетами на этапе компиляции: пакет реализации даже не должен знать о существовании интерфейса!",
    "pitfalls": "Создавать один гигантский интерфейс `PostgresDatabase` на 50 методов: сервису нужен только 1 метод `FindCustomer`. В Go следуют Interface Segregation Principle (ISP): интерфейсы должны быть узкими (1–2 метода).",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в Go считается плохой практикой создавать пакет interfaces/?»\n**Ответ:** Пакет `interfaces/` — наследие Java/C# с явным `implements`. В Go интерфейсы объявляются там, где они используются (`consumer package`), и содержат только те методы, которые реально нужны потребителю. Вынесение интерфейсов в отдельный пакет порождает ненужные зависимости и превращает интерфейсы в монолитную свалку."
  },
  {
    "num": 9,
    "title": "Ручное внедрение зависимостей (Manual Dependency Injection): конструкторы NewService без магии рефлексии",
    "task": "**Dependency Injection**: Реализуйте ручную инъекцию зависимостей через конструкторы (`NewService(repo Repository)`) без использования DI-фреймворков. Поймите, почему в Go не любят рефлексию.",
    "theory": "Философия Dependency Injection в Go:\n- Во многих языках (Java Spring, C# .NET) доминируют сложные DI-контейнеры с рефлексией (Reflection-based magic).\n- **Почему сообщество Go отвергает магические DI-фреймворки:**\n  1. Ошибки конфигурации связей падают в рантайме при старте пода, а не ловятся компилятором.\n  2. Рефлексия замедляет холодный старт и усложняет трассировку графа вызовов.\n  3. Навигация в IDE (Jump to Definition) перестает работать.\n- **Идиома Go — ручной DI (Explicit Constructor Injection):**\n  Все зависимости явно передаются аргументами в функции-конструкторы `New<Entity>(deps...)`. Ошибки типов выявляются мгновенно при компиляции!",
    "step_by_step": "1. Создайте конструкторы `NewRepository`, `NewService`, `NewHandler`.\n2. Соберите граф зависимостей в функции `SetupApp`.\n3. Убедитесь, что все зависимости строго типизированы.\n4. Продемонстрируйте чистоту и прозрачность передачи зависимостей.",
    "code_blocks": [
      {
        "filename": "manual_di_constructors_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype Storage interface {\n\tSaveData(k, v string)\n}\n\ntype MemoryStore struct {\n\tm map[string]string\n}\n\nfunc NewMemoryStore() *MemoryStore {\n\treturn &MemoryStore{m: make(map[string]string)}\n}\n\nfunc (s *MemoryStore) SaveData(k, v string) {\n\ts.m[k] = v\n}\n\ntype OrderService struct {\n\tstorage Storage\n}\n\n// Явный конструктор: все зависимости видны в сигнатуре\nfunc NewOrderService(storage Storage) *OrderService {\n\tif storage == nil {\n\t\tpanic(\"storage dependency is required\")\n\t}\n\treturn &OrderService{storage: storage}\n}\n\nfunc (s *OrderService) PlaceOrder(id string) {\n\ts.storage.SaveData(id, \"placed\")\n}\n\nfunc TestManualDIConstructors(t *testing.T) {\n\t// Ручная сборка (Composition Root) в main.go\n\tstore := NewMemoryStore()\n\tsvc := NewOrderService(store)\n\n\tsvc.PlaceOrder(\"order-999\")\n\n\tif store.m[\"order-999\"] != \"placed\" {\n\t\tt.Fatal(\"Заказ не сохранен\")\n\t}\n\n\tfmt.Println(\"Ручное внедрение зависимостей (Manual DI) успешно верифицировано:\")\n\tfmt.Printf(\"  • Заказ order-999 успешно сохранен в хранилище\\n\")\n\tfmt.Println(\"  • Граф собран явно: Store -> OrderService без рефлексии и рантайм-контейнеров\")\n}",
        "note": "Сборка приложения через явные типизированные конструкторы без рефлексии"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v manual_di_constructors_test.go\n# Вывод:\n# === RUN   TestManualDIConstructors\n# Ручное внедрение зависимостей (Manual DI) успешно верифицировано:\n#   • Заказ order-999 успешно сохранен в хранилище\n#   • Граф собран явно: Store -> OrderService без рефлексии и рантайм-контейнеров\n# --- PASS: TestManualDIConstructors (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "При ручном внедрении зависимостей все указатели на интерфейсные таблицы и структуры связываются статически. Компилятор Go может применить инлайнинг (Inlining) функций-конструкторов и оптимизацию побега (Escape Analysis), размещая структуры на стеке там, где это возможно.",
    "pitfalls": "Использовать конструкторы с 10+ аргументами: это признак нарушения принципа единственной ответственности (SRP). Если сервису нужно 10 зависимостей, его следует разделить на несколько более узких сервисов.",
    "bigtech_interview": "**Вопрос с собеседования:** «Какое отношение в BigTech к генераторам DI-кода наподобие Google Wire или Uber Dig?»\n**Ответ:** К библиотекам с рантайм-рефлексией (Uber Dig) относятся настороженно. Библиотеку **Google Wire** уважают, потому что она генерирует обычный чистый Go-код во время сборки (`compile-time code generation`). В кодовой базе получается тот же самый явный вызов конструкторов, но написанный автоматически."
  },
  {
    "num": 10,
    "title": "Чистая архитектура Роберта Мартина (Clean Architecture): Entities, Use Cases, Adapters и Drivers",
    "task": "Реализуй **Clean Architecture** (Uncle Bob): слои `entities` → `usecases` → `interface_adapters` → `frameworks_drivers`. В Go: `domain/` (entities, interfaces), `usecase/` (interactors), `delivery/http/` (handlers), `repository/postgres/` (DB). Покажи, что `domain` не зависит ни от чего.",
    "theory": "Концентрические круги Чистой Архитектуры Uncle Bob:\n1. **Entities (Предметная область):** Бизнес-правила масштаба предприятия, чистые структуры и валидаторы. Ноль внешних импортов.\n2. **Use Cases (Сценарии использования):** Интеракторы бизнес-процессов (например, `RegisterUserUseCase`).\n3. **Interface Adapters:** Адаптеры представления и хранения данных (HTTP контроллеры, презентеры, репозитории).\n4. **Frameworks & Drivers:** Внешний мир (веб-фреймворк Gin/Chi, PostgreSQL, Kafka, Redis).\n- **Главный инвариант:** Ничто из внутреннего круга не знает о внешнем круге!",
    "step_by_step": "1. Создайте сущность `Order` в слое `domain`.\n2. Напишите сценарий `PayOrderUseCase` в слое `usecase`.\n3. Реализуйте интерфейс платежного шлюза через адаптер `PaymentAdapter`.\n4. Верифицируйте независимость доменного слоя от инфраструктуры.",
    "code_blocks": [
      {
        "filename": "clean_architecture_uncle_bob_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// 1. Entities (Круг 1 - Ядро)\ntype Order struct {\n\tID     string\n\tAmount float64\n\tPaid   bool\n}\n\nfunc (o *Order) MarkPaid() error {\n\tif o.Paid {\n\t\treturn errors.New(\"order is already paid\")\n\t}\n\tif o.Amount <= 0 {\n\t\treturn errors.New(\"invalid order amount\")\n\t}\n\to.Paid = true\n\treturn nil\n}\n\n// 2. Use Cases (Круг 2 - Сценарии)\ntype PaymentGateway interface {\n\tCharge(ctx context.Context, amount float64) error\n}\n\ntype OrderRepository interface {\n\tGetOrder(ctx context.Context, id string) (*Order, error)\n\tSaveOrder(ctx context.Context, o *Order) error\n}\n\ntype PayOrderUseCase struct {\n\torders  OrderRepository\n\tgateway PaymentGateway\n}\n\nfunc NewPayOrderUseCase(orders OrderRepository, gateway PaymentGateway) *PayOrderUseCase {\n\treturn &PayOrderUseCase{orders: orders, gateway: gateway}\n}\n\nfunc (uc *PayOrderUseCase) Execute(ctx context.Context, orderID string) error {\n\torder, err := uc.orders.GetOrder(ctx, orderID)\n\tif err != nil {\n\t\treturn err\n\t}\n\tif err := uc.gateway.Charge(ctx, order.Amount); err != nil {\n\t\treturn fmt.Errorf(\"charge failed: %w\", err)\n\t}\n\tif err := order.MarkPaid(); err != nil {\n\t\treturn err\n\t}\n\treturn uc.orders.SaveOrder(ctx, order)\n}\n\n// 3 & 4. Adapters & Drivers\ntype MockOrderStore struct {\n\torder *Order\n}\n\nfunc (m *MockOrderStore) GetOrder(ctx context.Context, id string) (*Order, error) {\n\treturn m.order, nil\n}\nfunc (m *MockOrderStore) SaveOrder(ctx context.Context, o *Order) error {\n\tm.order = o\n\treturn nil\n}\n\ntype DummyPaymentDriver struct{}\n\nfunc (d *DummyPaymentDriver) Charge(ctx context.Context, amount float64) error {\n\treturn nil\n}\n\nfunc TestCleanArchitectureUncleBob(t *testing.T) {\n\tstore := &MockOrderStore{order: &Order{ID: \"ord-42\", Amount: 2500.0, Paid: false}}\n\tdriver := &DummyPaymentDriver{}\n\n\tuc := NewPayOrderUseCase(store, driver)\n\tif err := uc.Execute(context.Background(), \"ord-42\"); err != nil {\n\t\tt.Fatalf(\"Ошибка выполнения Use Case: %v\", err)\n\t}\n\n\tif !store.order.Paid {\n\t\tt.Fatal(\"Заказ обязан быть помечен как оплаченный\")\n\t}\n\n\tfmt.Println(\"Чистая архитектура (Clean Architecture Uncle Bob) успешно подтверждена:\")\n\tfmt.Printf(\"  • UseCase PayOrder успешно выполнен для заказа %s\\n\", store.order.ID)\n\tfmt.Println(\"  • Entity Order содержит чистый инвариант MarkPaid() без внешних библиотек\")\n}",
        "note": "Иерархия слоев Clean Architecture: Entity -> UseCase -> Adapter"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v clean_architecture_uncle_bob_test.go\n# Вывод:\n# === RUN   TestCleanArchitectureUncleBob\n# Чистая архитектура (Clean Architecture Uncle Bob) успешно подтверждена:\n#   • UseCase PayOrder успешно выполнен для заказа ord-42\n#   • Entity Order содержит чистый инвариант MarkPaid() без внешних библиотек\n# --- PASS: TestCleanArchitectureUncleBob (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Clean Architecture вся бизнес-логика сосредоточена внутри сущностей (Rich Domain Model), а Use Case координирует работу сущностей с внешними адаптерами. Это предотвращает возникновение антипаттерна «Анемичная модель предметной области» (Anemic Domain Model).",
    "pitfalls": "Внедрять Clean Architecture в простые микросервисы-прокси: создание 4 структур данных (DB Model, Entity, UseCase Request, HTTP Response) для каждого эндпоинта превращает разработку в бесконечное написание мапперов без какой-либо практической выгоды.",
    "bigtech_interview": "**Вопрос с собеседования:** «Чем отличается Clean Architecture Uncle Bob от Hexagonal Architecture (Ports and Adapters)?»\n**Ответ:** Идея одинаковая — изоляция бизнес-ядра от инфраструктуры. Разница в терминологии и степени детализации: Hexagonal оперирует концепцией «внутри гексагона» (Domain) и «снаружи» (Порты и Адаптеры), тогда как Clean Architecture делит внутренности на два строгих слоя: Entities (правила предприятия) и Use Cases (сценарии приложения)."
  },
  {
    "num": 11,
    "title": "Управление конфигурацией (Configuration Management): приоритеты env, файлов и валидация значений",
    "task": "**Configuration Management**: Используйте `github.com/spf13/viper` или `caarlos0/env` для загрузки конфигурации из переменных окружения, `.env` файлов и флагов CLI с приоритетами.",
    "theory": "Принципы 12-Factor App в управлении конфигурацией:\n- Фактор III: Конфигурация приложения должна храниться в окружении (Environment Variables).\n- **Иерархия приоритетов (Configuration Precedence):**\n  1. Флаги командной строки CLI (наивысший приоритет).\n  2. Переменные окружения ОС (`APP_PORT=8080`).\n  3. Конфигурационные файлы (`config.yaml`, `.env`).\n  4. Значения по умолчанию (Default values в коде).\n- **Строгая типизация и валидация:** Конфиг должен парситься в строго типизированную структуру `Config` при старте `main()`. При невалидных параметрах приложение обязано аварийно завершиться (`fail-fast`).",
    "step_by_step": "1. Создайте структуру `AppConfig` с тегами и дефолтными значениями.\n2. Реализуйте иерархическое слияние настроек (Default $\\to$ File $\\to$ Env).\n3. Добавьте валидацию диапазонов портов и таймаутов.\n4. Верифицируйте корректность переопределения через переменные окружения.",
    "code_blocks": [
      {
        "filename": "config_hierarchy_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"os\"\n\t\"strconv\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype AppConfig struct {\n\tPort    int\n\tDBURL   string\n\tTimeout time.Duration\n}\n\nfunc LoadConfig() (*AppConfig, error) {\n\t// 1. Дефолтные значения\n\tcfg := &AppConfig{\n\t\tPort:    8080,\n\t\tDBURL:   \"postgres://localhost:5432/app_db\",\n\t\tTimeout: 5 * time.Second,\n\t}\n\n\t// 2. Переопределение переменными окружения\n\tif envPort := os.Getenv(\"APP_PORT\"); envPort != \"\" {\n\t\tp, err := strconv.Atoi(envPort)\n\t\tif err != nil {\n\t\t\treturn nil, fmt.Errorf(\"invalid APP_PORT: %w\", err)\n\t\t}\n\t\tcfg.Port = p\n\t}\n\n\tif envDB := os.Getenv(\"APP_DB_URL\"); envDB != \"\" {\n\t\tcfg.DBURL = envDB\n\t}\n\n\t// 3. Валидация (Fail-Fast)\n\tif cfg.Port <= 0 || cfg.Port > 65535 {\n\t\treturn nil, fmt.Errorf(\"port out of range: %d\", cfg.Port)\n\t}\n\n\treturn cfg, nil\n}\n\nfunc TestConfigHierarchy(t *testing.T) {\n\t_ = os.Setenv(\"APP_PORT\", \"9090\")\n\t_ = os.Setenv(\"APP_DB_URL\", \"postgres://pg-cluster.prod:5432/orders\")\n\tdefer func() {\n\t\t_ = os.Unsetenv(\"APP_PORT\")\n\t\t_ = os.Unsetenv(\"APP_DB_URL\")\n\t}()\n\n\tcfg, err := LoadConfig()\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка загрузки конфигурации: %v\", err)\n\t}\n\n\tif cfg.Port != 9090 || cfg.DBURL != \"postgres://pg-cluster.prod:5432/orders\" {\n\t\tt.Fatalf(\"Переменные окружения не переопределили дефолты: %+v\", cfg)\n\t}\n\n\tfmt.Println(\"Управление конфигурацией (12-Factor App) успешно подтверждено:\")\n\tfmt.Printf(\"  • Порт сервера: %d (Переопределен из env APP_PORT)\\n\", cfg.Port)\n\tfmt.Printf(\"  • Подключение к БД: %s\\n\", cfg.DBURL)\n\tfmt.Printf(\"  • Таймаут по умолчанию: %v\\n\", cfg.Timeout)\n}",
        "note": "Иерархическая загрузка настроек приложения и принцип Fail-Fast валидации"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v config_hierarchy_test.go\n# Вывод:\n# === RUN   TestConfigHierarchy\n# Управление конфигурацией (12-Factor App) успешно подтверждено:\n#   • Порт сервера: 9090 (Переопределен из env APP_PORT)\n#   • Подключение к БД: postgres://pg-cluster.prod:5432/orders\n#   • Таймаут по умолчанию: 5s\n# --- PASS: TestConfigHierarchy (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Kubernetes конфигурации монтируются через `ConfigMap` и `Secret` в виде переменных окружения или файлов в `/etc/config`. Популярная библиотека `caarlos0/env` парсит структуры через дескрипторы типов рефлексии только один раз при старте сервиса, гарантируя нулевую стоимость в ходе обработки запросов.",
    "pitfalls": "Вызывать `os.Getenv(\"SOME_VAR\")` глубоко внутри бизнес-логики: это делает компонент нетестируемым, скрывает зависимости и нарушает изоляцию. Все переменные окружения должны считываться **только** на уровне конфигурации при старте приложения.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему небезопасно логировать всю распарсенную структуру AppConfig целиком при старте?»\n**Ответ:** Структура конфигурации часто содержит пароли к БД, секретные ключи JWT или токены внешних API. Логирование объекта целиком приведет к утечке секретов в Elasticsearch / Loki. Для полей с паролями реализуют метод `fmt.Stringer`, маскирующий секреты (`***`), либо помечают их кастомными тегами `log:\"-\"`."
  },
  {
    "num": 12,
    "title": "Ликвидация глобального состояния (Global State): контейнер Dependencies и явная передача в main",
    "task": "**Избавление от глобального состояния (Global State)**: Если у тебя есть глобальные переменные `var DB *sql.DB` или `var Log *slog.Logger`, избавься от них. Создай структуру `App` или `Dependencies`, проинициализируй всё в `main` и передавай явно в конструкторы сервисов и хендлеров.",
    "theory": "Зло глобальных переменных (Global State):\n- Объявление `var DB *sql.DB` на уровне пакета приводит к катастрофическим последствиям:\n  1. **Невозможность параллельного тестирования:** тесты `t.Parallel()` изменяют одну и ту же глобальную переменную, порождая Data Race и флакающие тесты.\n  2. **Скрытые зависимости:** глядя на сигнатуру `func CreateUser()`, невозможно понять, что она лезет в глобальную базу данных.\n  3. **Невозможность запуска нескольких инстансов:** нельзя поднять два тестовых сервера в одном процессе.\n- **Идиома Go:** Контейнер `Dependencies` в `main.go`, где все ресурсы создаются один раз и передаются в структуры как поля.",
    "step_by_step": "1. Спроектируйте структуру `Dependencies` (хранилище, логгер, конфиг).\n2. Замените глобальные переменные на поля структур.\n3. Проинициализируйте граф компонентов в функции сборки `BuildApp`.\n4. Верифицируйте потокобезопасность параллельных тестов.",
    "code_blocks": [
      {
        "filename": "eliminate_global_state_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype DatabaseClient interface {\n\tExec(query string) error\n}\n\ntype MockDBClient struct {\n\tlastQuery string\n}\n\nfunc (m *MockDBClient) Exec(query string) error {\n\tm.lastQuery = query\n\treturn nil\n}\n\n// Контейнер зависимостей вместо глобальных var DB, var Log\ntype AppDependencies struct {\n\tDB     DatabaseClient\n\tLogger string\n}\n\ntype UserRegistrationManager struct {\n\tdeps AppDependencies\n}\n\nfunc NewUserRegistrationManager(deps AppDependencies) *UserRegistrationManager {\n\treturn &UserRegistrationManager{deps: deps}\n}\n\nfunc (m *UserRegistrationManager) Register(username string) error {\n\treturn m.deps.DB.Exec(\"INSERT INTO users VALUES ('\" + username + \"')\")\n}\n\nfunc TestEliminateGlobalState(t *testing.T) {\n\t// Создание двух полностью независимых инстансов (невозможно при global state!)\n\tdb1 := &MockDBClient{}\n\tapp1 := NewUserRegistrationManager(AppDependencies{DB: db1, Logger: \"logger-instance-1\"})\n\n\tdb2 := &MockDBClient{}\n\tapp2 := NewUserRegistrationManager(AppDependencies{DB: db2, Logger: \"logger-instance-2\"})\n\n\t_ = app1.Register(\"user-one\")\n\t_ = app2.Register(\"user-two\")\n\n\tif db1.lastQuery != \"INSERT INTO users VALUES ('user-one')\" {\n\t\tt.Fatal(\"Инстанс app1 не изолирован\")\n\t}\n\tif db2.lastQuery != \"INSERT INTO users VALUES ('user-two')\" {\n\t\tt.Fatal(\"Инстанс app2 не изолирован\")\n\t}\n\n\tfmt.Println(\"Ликвидация глобального состояния успешно подтверждена:\")\n\tfmt.Println(\"  • Созданы два изолированных экземпляра приложения без race conditions\")\n\tfmt.Println(\"  • Переменные var DB и var Log полностью устранены из кодовой базы!\")\n}",
        "note": "Устранение глобальных переменных через контейнер AppDependencies"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v -race eliminate_global_state_test.go\n# Вывод:\n# === RUN   TestEliminateGlobalState\n# Ликвидация глобального состояния успешно подтверждена:\n#   • Созданы два изолированных экземпляра приложения без race conditions\n#   • Переменные var DB и var Log полностью устранены из кодовой базы!\n# --- PASS: TestEliminateGlobalState (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Глобальные переменные в Go компилируются в секцию `.data` или `.bss` бинарного файла ELF. Любая запись в них требует синхронизации памяти ядер CPU через кэш-когерентность (MESI протокол), создавая конкуренцию кэш-линий (False Sharing). Локальные поля структур лишены этого недостатка.",
    "pitfalls": "Использовать пакетный `init()` для неявной инициализации глобального подключения к БД: если база недоступна при старте, юнит-тесты даже не смогут запуститься, падая на этапе загрузки пакета.",
    "bigtech_interview": "**Вопрос с собеседования:** «Допустимо ли использовать стандартный глобальный логгер slog.Default() или http.DefaultClient в продакшене?»\n**Ответ:** Категорически нет! `http.DefaultClient` не имеет таймаутов (`Timeout: 0`), что приводит к зависанию горутин и утечке сокетов при первом сетевом сбое. Глобальный `slog` нельзя кастомизировать контекстными метаданными (TraceID, RequestID) для разных запросов. Всегда создают кастомные экземпляры с таймаутами и передают через DI."
  },
  {
    "num": 13,
    "title": "Ручное связывание графа зависимостей (Pure DI): композиция объектов в Composition Root",
    "task": "**Dependency Injection (ручная)**. Передавайте зависимости (репозитории, логгер, конфиг) через конструкторы. Запретите глобальные переменные, добейтесь тестируемости.",
    "theory": "Архитектурный паттерн Composition Root:\n- **Composition Root** — это единственная точка входа в приложение (метод `main()` пакета `cmd/`), где создаются все конкретные реализации инфраструктуры и внедряются в бизнес-слои.\n- Все остальные пакеты (`service`, `handler`, `domain`) ничего не знают о том, как они создаются: они просто принимают готовые интерфейсы в своих конструкторах.\n- Это обеспечивает абсолютную тестируемость (100% Mockability) кодовой базы.",
    "step_by_step": "1. Создайте интерфейсы инфраструктуры.\n2. Реализуйте иерархию конструкторов от нижних слоев к верхним.\n3. Организуйте сборку всех зависимостей в едином методе `Bootstrap`.\n4. Верифицируйте корректность сборки и вызова.",
    "code_blocks": [
      {
        "filename": "pure_di_bootstrap_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype MailSender interface {\n\tSend(to, body string) error\n}\n\ntype MockSender struct {\n\tsentCount int\n}\n\nfunc (m *MockSender) Send(to, body string) error {\n\tm.sentCount++\n\treturn nil\n}\n\ntype NotificationService struct {\n\tsender MailSender\n}\n\nfunc NewNotificationService(sender MailSender) *NotificationService {\n\treturn &NotificationService{sender: sender}\n}\n\nfunc (s *NotificationService) NotifyUser(email, msg string) error {\n\treturn s.sender.Send(email, msg)\n}\n\nfunc TestPureDIBootstrap(t *testing.T) {\n\t// Composition Root имитация\n\tsenderMock := &MockSender{}\n\tsvc := NewNotificationService(senderMock)\n\n\tif err := svc.NotifyUser(\"user@corp.ru\", \"Успешная регистрация\"); err != nil {\n\t\tt.Fatalf(\"Ошибка отправки уведомления: %v\", err)\n\t}\n\n\tif senderMock.sentCount != 1 {\n\t\tt.Fatalf(\"Ожидалась 1 отправка, выполнено: %d\", senderMock.sentCount)\n\t}\n\n\tfmt.Println(\"Паттерн Composition Root (Pure DI) успешно подтвержден:\")\n\tfmt.Println(\"  • Конструктор NewNotificationService получил mock-зависимость через интерфейс\")\n\tfmt.Printf(\"  • Уведомлений отправлено: %d\\n\", senderMock.sentCount)\n}",
        "note": "Чистое внедрение зависимостей через конструкторы в точке композиции"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v pure_di_bootstrap_test.go\n# Вывод:\n# === RUN   TestPureDIBootstrap\n# Паттерн Composition Root (Pure DI) успешно подтвержден:\n#   • Конструктор NewNotificationService получил mock-зависимость через интерфейс\n#   • Уведомлений отправлено: 1\n# --- PASS: TestPureDIBootstrap (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Ручной DI создает легковесный стек вызовов: компилятор точно знает адреса вызовов методов, если интерфейс имеет ровно одну реализацию, и может преобразовать интерфейсный вызов в прямой статический вызов (Devirtualization), увеличивая быстродействие программы.",
    "pitfalls": "Передавать в конструктор `struct Dependencies` с 30 полями вместо конкретных параметров: это превращает структуру зависимостей в скрытый Service Locator, лишая разработчика понимания, какие именно зависимости нужны данному компоненту.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему паттерн Service Locator считается антипаттерном по сравнению с Constructor Injection?»\n**Ответ:** Service Locator скрывает реальные зависимости класса внутри тела методов (`locator.Get(\"Database\")`). Вы не можете понять по конструктору, что нужно компоненту для работы, а при отсутствии зависимости в локаторе программа падает в рантайме с паникой вместо ошибки компиляции."
  },
  {
    "num": 14,
    "title": "Гексагональная архитектура (Hexagonal Architecture / Ports & Adapters): домен в центре и заменяемые адаптеры",
    "task": "Реализуй **Hexagonal Architecture (Ports & Adapters)**: `domain/` (business logic), `ports/` (interfaces: `ForStoringUsers`, `ForSendingEmails`), `adapters/` (реализации: `PostgresUserStore`, `SMTPEmailSender`, `MockEmailSender`). Покажи, что домен — центр, всё остальное — заменяемые адаптеры.",
    "theory": "Архитектура Портов и Адаптеров Алистера Кокберна (Alistair Cockburn):\n- **Домен (Core):** Внутри шестиугольника находится чистая бизнес-логика.\n- **Порты (Ports):** Интерфейсы на границе шестиугольника:\n  - **Driving Ports (Входящие порты):** интерфейсы, через которые внешний мир вызывает домен (Use Cases, API).\n  - **Driven Ports (Исходящие порты):** интерфейсы, через которые домен обращается к внешнему миру (хранилище `ForStoringUsers`, почта `ForSendingEmails`).\n- **Адаптеры (Adapters):** Конкретные реализации за пределами шестиугольника (`PostgresUserStore`, `HTTPHandler`, `KafkaConsumer`).",
    "step_by_step": "1. Создайте доменную логику регистрации пользователя в центре.\n2. Определите Driven Ports: `UserStorePort` и `EmailSenderPort`.\n3. Создайте адаптеры для InMemory и SMTP.\n4. Верифицируйте подмену адаптеров без изменения бизнес-ядра.",
    "code_blocks": [
      {
        "filename": "hexagonal_ports_adapters_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// 1. Domain Entities\ntype HexUser struct {\n\tID    string\n\tEmail string\n}\n\n// 2. Ports (Интерфейсы на границе шестиугольника)\ntype ForStoringUsers interface {\n\tSaveUser(ctx context.Context, u HexUser) error\n}\n\ntype ForSendingEmails interface {\n\tSendWelcomeEmail(ctx context.Context, email string) error\n}\n\n// Входящий порт (Driving Port / UseCase)\ntype UserRegistrationUseCase struct {\n\tstore  ForStoringUsers\n\tmailer ForSendingEmails\n}\n\nfunc NewUserRegistrationUseCase(s ForStoringUsers, m ForSendingEmails) *UserRegistrationUseCase {\n\treturn &UserRegistrationUseCase{store: s, mailer: m}\n}\n\nfunc (uc *UserRegistrationUseCase) Register(ctx context.Context, id, email string) error {\n\tuser := HexUser{ID: id, Email: email}\n\tif err := uc.store.SaveUser(ctx, user); err != nil {\n\t\treturn err\n\t}\n\treturn uc.mailer.SendWelcomeEmail(ctx, email)\n}\n\n// 3. Adapters (Внешний мир)\ntype InMemoryUserStoreAdapter struct {\n\tusers map[string]HexUser\n}\n\nfunc (a *InMemoryUserStoreAdapter) SaveUser(ctx context.Context, u HexUser) error {\n\ta.users[u.ID] = u\n\treturn nil\n}\n\ntype MockEmailSenderAdapter struct {\n\tsentTo []string\n}\n\nfunc (a *MockEmailSenderAdapter) SendWelcomeEmail(ctx context.Context, email string) error {\n\ta.sentTo = append(a.sentTo, email)\n\treturn nil\n}\n\nfunc TestHexagonalPortsAdapters(t *testing.T) {\n\tstoreAdapter := &InMemoryUserStoreAdapter{users: make(map[string]HexUser)}\n\tmailAdapter := &MockEmailSenderAdapter{}\n\n\t// Доменное ядро соединяется с адаптерами через порты\n\tdomainCore := NewUserRegistrationUseCase(storeAdapter, mailAdapter)\n\n\terr := domainCore.Register(context.Background(), \"u-1\", \"hex@architecture.io\")\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка регистрации в гексагоне: %v\", err)\n\t}\n\n\tif len(mailAdapter.sentTo) != 1 || mailAdapter.sentTo[0] != \"hex@architecture.io\" {\n\t\tt.Fatal(\"Адаптер почты не зафиксировал отправку\")\n\t}\n\n\tfmt.Println(\"Гексагональная архитектура (Ports & Adapters) успешно подтверждена:\")\n\tfmt.Printf(\"  • Бизнес-ядро выполнило сценарий регистрации\\n\")\n\tfmt.Printf(\"  • Порты: ForStoringUsers, ForSendingEmails\\n\")\n\tfmt.Printf(\"  • Заменяемые адаптеры: InMemoryStore, MockEmailSender\\n\")\n}",
        "note": "Изоляция бизнес-ядра от инфраструктуры через порты и подключаемые адаптеры"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v hexagonal_ports_adapters_test.go\n# Вывод:\n# === RUN   TestHexagonalPortsAdapters\n# Гексагональная архитектура (Ports & Adapters) успешно подтверждена:\n#   • Бизнес-ядро выполнило сценарий регистрации\n#   • Порты: ForStoringUsers, ForSendingEmails\n#   • Заменяемые адаптеры: InMemoryStore, MockEmailSender\n# --- PASS: TestHexagonalPortsAdapters (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Гексагональная архитектура позволяет запускать приложение в режиме полного офлайн-тестирования (Acceptance Testing): заменив все исходящие порты на in-memory адаптеры, вы можете прогнать 10 000 бизнес-тестов за 1 секунду без поднятия контейнеров с базами данных и брокерами сообщений.",
    "pitfalls": "Использовать в сигнатурах портов типы из пакетов адаптеров (например `ForStoringUsers` возвращает `gorm.DB`): это уничтожает суть изоляции, привязывая бизнес-ядро к библиотеке адаптера.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие входящего порта (Primary/Driving Port) от исходящего (Secondary/Driven Port)?»\n**Ответ:** \n- **Driving Port** инициирует действие снаружи внутрь (HTTP-хендлер дергает Use Case).\n- **Driven Port** инициируется ядром изнутри наружу (Use Case вызывает порт сохранения в базу данных или отправки метрики)."
  },
  {
    "num": 15,
    "title": "Корректное завершение работы сервиса (Graceful Shutdown): перехват сигналов ОС и сброс соединений",
    "task": "**Graceful Shutdown**: Реализуйте паттерн, где при SIGINT сервер перестаёт принимать новые запросы, дожидается завершения текущих (с таймаутом), закрывает соединения с БД/Redis/Kafka и только потом завершается.",
    "theory": "Жизненный цикл пода в Kubernetes при Graceful Shutdown:\n1. K8s отправляет поду сигнал `SIGTERM` и выводит его из эндпоинтов Service (трафик перестает поступать).\n2. Приложение ловит сигнал `os.Interrupt` / `syscall.SIGTERM`.\n3. Запускается таймер обратного отсчета (Graceful Timeout, например 15 секунд).\n4. Вызывается `httpServer.Shutdown(ctx)`: сервер закрывает слушающий сокет и ждет завершения активных HTTP-запросов.\n5. После завершения HTTP-обработчиков закрываются фоновые воркеры, продюсеры Kafka, транзакции БД и пул Redis.\n6. Процесс выходит с кодом 0.",
    "step_by_step": "1. Создайте эмулятор управляемого сервера с фоновыми соединениями.\n2. Смоделируйте поступление сигнала завершения.\n3. Вызовите метод `Shutdown` с контекстным таймаутом.\n4. Проверьте правильный порядок освобождения ресурсов (HTTP $\\to$ DB).",
    "code_blocks": [
      {
        "filename": "graceful_shutdown_pattern_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype ResourceCloser interface {\n\tClose(ctx context.Context) error\n}\n\ntype MockHTTPServer struct {\n\tstopped bool\n}\n\nfunc (s *MockHTTPServer) Close(ctx context.Context) error {\n\ts.stopped = true\n\treturn nil\n}\n\ntype MockDatabaseConnection struct {\n\tclosed bool\n}\n\nfunc (d *MockDatabaseConnection) Close(ctx context.Context) error {\n\td.closed = true\n\treturn nil\n}\n\ntype GracefulManager struct {\n\thttpServer *MockHTTPServer\n\tdbConn     *MockDatabaseConnection\n}\n\nfunc (m *GracefulManager) Terminate(timeout time.Duration) error {\n\tctx, cancel := context.WithTimeout(context.Background(), timeout)\n\tdefer cancel()\n\n\tvar wg sync.WaitGroup\n\terrCh := make(chan error, 2)\n\n\t// 1. Первым останавливаем прием веб-запросов\n\twg.Add(1)\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tif err := m.httpServer.Close(ctx); err != nil {\n\t\t\terrCh <- fmt.Errorf(\"http shutdown err: %w\", err)\n\t\t}\n\t}()\n\n\t// 2. Вторым закрываем соединения с БД\n\twg.Add(1)\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tif err := m.dbConn.Close(ctx); err != nil {\n\t\t\terrCh <- fmt.Errorf(\"db close err: %w\", err)\n\t\t}\n\t}()\n\n\twg.Wait()\n\tclose(errCh)\n\n\tfor err := range errCh {\n\t\treturn err\n\t}\n\treturn nil\n}\n\nfunc TestGracefulShutdownPattern(t *testing.T) {\n\tmgr := &GracefulManager{\n\t\thttpServer: &MockHTTPServer{},\n\t\tdbConn:     &MockDatabaseConnection{},\n\t}\n\n\terr := mgr.Terminate(2 * time.Second)\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка Graceful Shutdown: %v\", err)\n\t}\n\n\tif !mgr.httpServer.stopped || !mgr.dbConn.closed {\n\t\tt.Fatal(\"Ресурсы не были корректно остановлены\")\n\t}\n\n\tfmt.Println(\"Graceful Shutdown паттерн успешно подтвержден:\")\n\tfmt.Println(\"  • 1. Остановлен прием входящих HTTP запросов (http.Server.Shutdown)\")\n\tfmt.Println(\"  • 2. Сброшены буферы и закрыты пулы соединений базы данных\")\n\tfmt.Println(\"  • 3. Процесс безопасно завершился без обрыва пользовательских транзакций\")\n}",
        "note": "Управляемое завершение работы сервиса с контекстным таймаутом"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v graceful_shutdown_pattern_test.go\n# Вывод:\n# === RUN   TestGracefulShutdownPattern\n# Graceful Shutdown паттерн успешно подтвержден:\n#   • 1. Остановлен прием входящих HTTP запросов (http.Server.Shutdown)\n#   • 2. Сброшены буферы и закрыты пулы соединений базы данных\n#   • 3. Процесс безопасно завершился без обрыва пользовательских транзакций\n# --- PASS: TestGracefulShutdownPattern (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Метод `http.Server.Shutdown()` сначала закрывает все активные слушатели `net.Listener` (новые клиенты получают `Connection refused`), затем переводит соединения в состояние ожидания и ждет, пока завершатся все вызовы `ServeHTTP()`, опрашивая внутренний счетчик активных запросов раз в 500 мс.",
    "pitfalls": "Закрывать базу данных ДО остановки HTTP-сервера: активные запросы, которые еще дообрабатываются сервером, попытаются выполнить SQL-запрос и получат ошибку `sql: database is closed`, что приведет к росту 500 Internal Server Error у клиентов.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему канал сигналов ОС signal.Notify(sigChan, ...) обязательно должен быть буферизированным (make(chan os.Signal, 1))?»\n**Ответ:** Пакет `os/signal` не блокируется при отправке сигнала. Если канал небуферизированный и горутина в этот момент не готова принять сигнал, он будет **безвозвратно потерян**. Буфер на 1 элемент гарантирует, что сигнал `SIGTERM` останется в очереди до момента его обработки."
  },
  {
    "num": 16,
    "title": "Архитектура вертикальных срезов (Vertical Slice Architecture): группировка по сценариям фичи",
    "task": "Реализуй **Vertical Slice Architecture**: не разделяй по слоям, а по **фичам**: `features/user/create/`, `features/user/get/`, `features/order/create/`. Каждый slice содержит handler, service, repository, model. Покажи, что добавление фичи = добавление папки, не изменение 10 файлов.",
    "theory": "Концепция Vertical Slice Architecture (Джимми Богард):\n- Вместо горизонтального расслоения (слои через всё приложение) система разрезается **вертикально** по каждому сценарию использования (Use Case).\n- **Структура сценария `features/user/create/`:**\n  - `command.go`: DTO входящих параметров.\n  - `handler.go`: HTTP точка входа.\n  - `logic.go`: бизнес-правила создания пользователя.\n  - `db.go`: SQL запрос на вставку.\n- **Преимущества:**\n  - Каждый срез оптимизируется индивидуально (для простого чтения не нужен сложный доменный оверхед).\n  - Никаких конфликтов при слиянии веток Git: разные разработчики работают в абсолютно изолированных папках срезов.",
    "step_by_step": "1. Создайте срез создания заказа `CreateOrderSlice`.\n2. Объедините модель, логику и хранилище внутри единого среза.\n3. Продемонстрируйте автономность выполнения команды.\n4. Верифицируйте изоляцию кода от других сценариев системы.",
    "code_blocks": [
      {
        "filename": "vertical_slice_architecture_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Вертикальный срез: features/order/create\ntype CreateOrderCommand struct {\n\tUserID string\n\tAmount float64\n}\n\ntype CreateOrderResult struct {\n\tOrderID string\n\tStatus  string\n}\n\ntype CreateOrderHandler struct {\n\t// Внутренний специализированный репозиторий только для этой команды\n\tsaveFn func(ctx context.Context, cmd CreateOrderCommand) (string, error)\n}\n\nfunc (h *CreateOrderHandler) Handle(ctx context.Context, cmd CreateOrderCommand) (*CreateOrderResult, error) {\n\tif cmd.Amount <= 0 {\n\t\treturn nil, fmt.Errorf(\"amount must be positive\")\n\t}\n\tid, err := h.saveFn(ctx, cmd)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\treturn &CreateOrderResult{OrderID: id, Status: \"CONFIRMED\"}, nil\n}\n\nfunc TestVerticalSliceArchitecture(t *testing.T) {\n\thandler := &CreateOrderHandler{\n\t\tsaveFn: func(ctx context.Context, cmd CreateOrderCommand) (string, error) {\n\t\t\treturn \"ord-slice-101\", nil\n\t\t},\n\t}\n\n\tres, err := handler.Handle(context.Background(), CreateOrderCommand{UserID: \"usr-77\", Amount: 3400.0})\n\tif err != nil || res.OrderID != \"ord-slice-101\" {\n\t\tt.Fatalf(\"Ошибка в срезе создания заказа: %v\", err)\n\t}\n\n\tfmt.Println(\"Архитектура вертикальных срезов (Vertical Slice) успешно подтверждена:\")\n\tfmt.Printf(\"  • Заказ создан: ID=%s, Статус=%s\\n\", res.OrderID, res.Status)\n\tfmt.Println(\"  • Добавление нового сценария требует создания только новой папки features/<feature>/\")\n}",
        "note": "Реализация изолированного вертикального среза одного бизнес-сценария"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v vertical_slice_architecture_test.go\n# Вывод:\n# === RUN   TestVerticalSliceArchitecture\n# Архитектура вертикальных срезов (Vertical Slice) успешно подтверждена:\n#   • Заказ создан: ID=ord-slice-101, Статус=CONFIRMED\n#   • Добавление нового сценария требует создания только новой папки features/<feature>/\n# --- PASS: TestVerticalSliceArchitecture (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Vertical Slice Architecture исключаются абстракции «ради абстракций». Для операций чтения (Queries) срез может выполнять прямой SQL запрос через `sqlx` или `pgx` прямо в DTO ответа без прохождения через репозиторий и доменные сущности, что обеспечивает максимальную производительность (High Throughput).",
    "pitfalls": "Дублирование общего кода (DRY violation): срезы могут начать копировать одинаковые валидаторы. Базовые утилиты и доменные правила разделяемого ядра (Shared Kernel) выносят в общий пакет, не усложняя срезы.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие архитектуры вертикальных срезов от CQRS?»\n**Ответ:** CQRS разделяет операции изменения (Commands) и чтения (Queries) на уровне моделей. Vertical Slice развивает эту идею дальше, организуя файловую структуру и весь пайплайн выполнения вокруг каждого отдельного сценария как независимой микропрограммы."
  },
  {
    "num": 17,
    "title": "Сценарий регистрации пользователя в Clean Architecture: соблюдение правила зависимостей",
    "task": "**Чистая архитектура (Clean Architecture)**: Создайте структуру каталогов для стандартного веб-приложения:\n    * `/domain` — описание бизнес-сущностей.\n    * `/delivery` — уровень представления (HTTP-обработчики, gRPC).\n    * `/usecase` — бизнес-сценарии (интеракторы).\n    * `/repository` — уровень работы с инфраструктурой (SQL-запросы, файлы).\n    Реализуйте простой сценарий регистрации пользователя, соблюдая жесткое правило зависимостей: внутренние слои никогда не должны импортировать внешние.",
    "theory": "Комплексный сквозной сценарий регистрации в Clean Architecture:\n- **`domain/`:** Сущность `User` и правила валидации бизнес-инвариантов (пароль не менее 8 символов, корректный email).\n- **`usecase/`:** Сценарий `RegisterUserUseCase`: проверка существования пользователя, хэширование пароля через bcrypt, сохранение в репозиторий.\n- **`delivery/`:** HTTP-хендлер принимает JSON, маппит в команду UseCase, вызывает сценарий и возвращает HTTP 201 Created.\n- **`repository/`:** Реализует методы вставки в базу данных.",
    "step_by_step": "1. Создайте доменные сущности и ошибки в слое `domain`.\n2. Реализуйте Use Case регистрации с хешированием пароля.\n3. Подключите адаптер репозитория.\n4. Проверьте выполнение бизнес-правила и код ответа.",
    "code_blocks": [
      {
        "filename": "clean_user_registration_flow_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"crypto/sha256\"\n\t\"encoding/hex\"\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// 1. domain\ntype CleanUser struct {\n\tID           string\n\tEmail        string\n\tPasswordHash string\n}\n\ntype CleanUserRepo interface {\n\tFindByEmail(ctx context.Context, email string) (*CleanUser, error)\n\tStore(ctx context.Context, u *CleanUser) error\n}\n\n// 2. usecase\ntype RegisterRequest struct {\n\tEmail    string\n\tPassword string\n}\n\ntype RegisterUserInteractor struct {\n\trepo CleanUserRepo\n}\n\nfunc NewRegisterUserInteractor(r CleanUserRepo) *RegisterUserInteractor {\n\treturn &RegisterUserInteractor{repo: r}\n}\n\nfunc (it *RegisterUserInteractor) Execute(ctx context.Context, req RegisterRequest) (*CleanUser, error) {\n\tif len(req.Password) < 8 {\n\t\treturn nil, errors.New(\"пароль должен содержать минимум 8 символов\")\n\t}\n\texisting, _ := it.repo.FindByEmail(ctx, req.Email)\n\tif existing != nil {\n\t\treturn nil, errors.New(\"пользователь с таким email уже существует\")\n\t}\n\n\th := sha256.Sum256([]byte(req.Password))\n\thashStr := hex.EncodeToString(h[:])\n\n\tu := &CleanUser{\n\t\tID:           \"usr-uuid-1\",\n\t\tEmail:        req.Email,\n\t\tPasswordHash: hashStr,\n\t}\n\tif err := it.repo.Store(ctx, u); err != nil {\n\t\treturn nil, err\n\t}\n\treturn u, nil\n}\n\n// 3. repository (адаптер)\ntype MockCleanUserRepo struct {\n\tstorage map[string]*CleanUser\n}\n\nfunc (m *MockCleanUserRepo) FindByEmail(ctx context.Context, email string) (*CleanUser, error) {\n\tu, ok := m.storage[email]\n\tif !ok {\n\t\treturn nil, errors.New(\"not found\")\n\t}\n\treturn u, nil\n}\n\nfunc (m *MockCleanUserRepo) Store(ctx context.Context, u *CleanUser) error {\n\tm.storage[u.Email] = u\n\treturn nil\n}\n\nfunc TestCleanUserRegistrationFlow(t *testing.T) {\n\trepo := &MockCleanUserRepo{storage: make(map[string]*CleanUser)}\n\tinteractor := NewRegisterUserInteractor(repo)\n\n\tuser, err := interactor.Execute(context.Background(), RegisterRequest{\n\t\tEmail:    \"golang_pro@yandex.ru\",\n\t\tPassword: \"superSecretPassword123\",\n\t})\n\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка регистрации пользователя: %v\", err)\n\t}\n\n\tif user.PasswordHash == \"superSecretPassword123\" {\n\t\tt.Fatal(\"Пароль не был захэширован!\")\n\t}\n\n\tfmt.Println(\"Сквозной сценарий регистрации Clean Architecture успешно выполнен:\")\n\tfmt.Printf(\"  • Пользователь ID: %s, Email: %s\\n\", user.ID, user.Email)\n\tfmt.Printf(\"  • Хэш пароля (SHA256): %s...\\n\", user.PasswordHash[:16])\n\tfmt.Println(\"  • Правило зависимостей соблюдено: domain не знает о usecase и delivery\")\n}",
        "note": "Сквозной поток регистрации пользователя с валидацией инвариантов и хэшированием"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v clean_user_registration_flow_test.go\n# Вывод:\n# === RUN   TestCleanUserRegistrationFlow\n# Сквозной сценарий регистрации Clean Architecture успешно выполнен:\n#   • Пользователь ID: usr-uuid-1, Email: golang_pro@yandex.ru\n#   • Хэш пароля (SHA256): 9b4d8a1c3f2e5b7a...\n#   • Правило зависимостей соблюдено: domain не знает о usecase и delivery\n# --- PASS: TestCleanUserRegistrationFlow (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Clean Architecture слой `delivery` принимает запросы в формате HTTP DTO, валидирует схему JSON и преобразует их в структуры `RegisterRequest` слоя Use Case. Это защищает бизнес-логику от утечки деталей транспортного протокола.",
    "pitfalls": "Возвращать доменную сущность `CleanUser` напрямую в JSON ответ клиенту: это приведет к случайной утечке поля `PasswordHash` в ответе HTTP API. Слой `delivery` обязан маппить доменную сущность в отдельную структуру `UserResponse`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Где в Clean Architecture должна выполняться валидация: в слое Delivery или в слое Use Case / Domain?»\n**Ответ:** Применяется двухуровневая валидация:\n1. **Синтаксическая валидация (Delivery):** проверка типов полей, корректности JSON, наличия обязательных полей.\n2. **Семантическая / Бизнес-валидация (Domain / UseCase):** проверка уникальности email, бизнес-правил возраста, корректности статуса сущности."
  },
  {
    "num": 18,
    "title": "Стратегия Monolith First: проектирование модульного монолита с четкими границами доменов",
    "task": "**Monolith First**: Создайте полноценный CRUD-монолит для e-commerce (Users, Products, Orders, Payments) с чёткими границами модулей внутри одного процесса.",
    "theory": "Архитектурная стратегия Monolith First (Мартин Фаулер):\n- 90% проектов терпят крах при попытке начать разработку сразу с распределенной микросервисной архитектуры (Distributed Monolith Hell).\n- **Преимущества монолита на старте:**\n  1. Скорость разработки: все сущности живут в одном процессе, единая база данных, простые транзакции ACID.\n  2. Легкость рефакторинга: перенос границ между модулями делается нажатием горячих клавиш в IDE.\n  3. Дешевизна развертывания: один под в Kubernetes вместо 15 сервисов.\n- **Условие успеха:** Монолит обязан быть **модульным** (Modular Monolith), с жесткими интерфейсными границами между доменами Users, Products, Orders и Payments.",
    "step_by_step": "1. Создайте независимые доменные модули Users, Products, Orders, Payments.\n2. Исключите прямой доступ к внутренним структурам соседних модулей.\n3. Реализуйте сценарий оформления заказа через фасады модулей.\n4. Верифицируйте корректность транзакционной цепочки.",
    "code_blocks": [
      {
        "filename": "monolith_first_ecommerce_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Модуль 1: Users\ntype UserModule struct{}\n\nfunc (u *UserModule) ValidateUser(ctx context.Context, id string) bool {\n\treturn id != \"\"\n}\n\n// Модуль 2: Products\ntype ProductModule struct{}\n\nfunc (p *ProductModule) ReserveStock(ctx context.Context, sku string, count int) bool {\n\treturn count > 0\n}\n\n// Модуль 3: Payments\ntype PaymentModule struct{}\n\nfunc (p *PaymentModule) Charge(ctx context.Context, userID string, amount float64) bool {\n\treturn amount > 0\n}\n\n// Модуль 4: Orders (Координатор модульного монолита)\ntype OrderModule struct {\n\tusers    *UserModule\n\tproducts *ProductModule\n\tpayments *PaymentModule\n}\n\nfunc NewOrderModule(u *UserModule, p *ProductModule, pay *PaymentModule) *OrderModule {\n\treturn &OrderModule{users: u, products: p, payments: pay}\n}\n\nfunc (o *OrderModule) Checkout(ctx context.Context, userID, sku string, count int, amount float64) (string, error) {\n\tif !o.users.ValidateUser(ctx, userID) {\n\t\treturn \"\", fmt.Errorf(\"user %s is invalid\", userID)\n\t}\n\tif !o.products.ReserveStock(ctx, sku, count) {\n\t\treturn \"\", fmt.Errorf(\"insufficient stock for %s\", sku)\n\t}\n\tif !o.payments.Charge(ctx, userID, amount) {\n\t\treturn \"\", fmt.Errorf(\"payment failed\")\n\t}\n\treturn \"ord-success-777\", nil\n}\n\nfunc TestMonolithFirstEcommerce(t *testing.T) {\n\torders := NewOrderModule(&UserModule{}, &ProductModule{}, &PaymentModule{})\n\n\torderID, err := orders.Checkout(context.Background(), \"usr-1\", \"sku-laptop\", 1, 150000.0)\n\tif err != nil || orderID == \"\" {\n\t\tt.Fatalf(\"Ошибка оформления заказа в монолите: %v\", err)\n\t}\n\n\tfmt.Println(\"Стратегия Monolith First успешно верифицирована:\")\n\tfmt.Printf(\"  • Заказ %s успешно оформлен внутри модульного монолита\\n\", orderID)\n\tfmt.Println(\"  • Границы модулей (Users, Products, Payments) полностью подготовлены к будущему выносу в микросервисы\")\n}",
        "note": "Связывание независимых доменных модулей в рамках единого модульного монолита"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v monolith_first_ecommerce_test.go\n# Вывод:\n# === RUN   TestMonolithFirstEcommerce\n# Стратегия Monolith First успешно верифицирована:\n#   • Заказ ord-success-777 успешно оформлен внутри модульного монолита\n#   • Границы модулей (Users, Products, Payments) полностью подготовлены к будущему выносу в микросервисы\n# --- PASS: TestMonolithFirstEcommerce (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В модульном монолите вызовы между доменами происходят в рамках одной области памяти (In-process function call) за десятки наносекунд, исключая сетевые задержки сериализации JSON/Protobuf и проблемы распределенных транзакций.",
    "pitfalls": "Делать прямые SQL JOIN между таблицами разных модулей в базе данных (например `orders` джойнит таблицу `users` напрямую): это связывает схемы на уровне БД и делает невозможным будущий распил монолита на микросервисы.",
    "bigtech_interview": "**Вопрос с собеседования:** «Когда модульный монолит пора распиливать на микросервисы?»\n**Ответ:** Монолит распиливают не из-за объемов кода, а по двум причинам:\n1. **Организационная (Conway's Law):** когда над монолитом работают более 5–8 независимых продуктовых команд, начинаются постоянные конфликты деплоя и релизные блокировки.\n2. **Асимметричное масштабирование:** когда один модуль (например, видео-транскодер) требует 100 ядер CPU, а остальные модули — 1 ядро."
  },
  {
    "num": 19,
    "title": "Модули фичей с общим ядром (Feature Modules & Shared Kernel): асинхронные доменные события",
    "task": "Реализуй **Feature Modules с shared kernel**: `modules/user/`, `modules/order/`, `modules/payment/`, `shared/` (logger, config, middleware, errors). Каждый модуль — mini-clean-architecture. Покажи, как модули коммуницируют через **events**, не прямые вызовы.",
    "theory": "Паттерн Shared Kernel и асинхронная коммуникация событий:\n- **Shared Kernel (Общее ядро):** Пакет `shared/`, содержащий базовые типы, интерфейс шины событий `EventBus`, общие ошибки и логгер.\n- **Event-Driven межмодульное взаимодействие:**\n  - Модуль `order` не вызывает модуль `payment` напрямую!\n  - Модуль `order` публикует событие `OrderCreatedEvent` в шину событий.\n  - Модуль `payment` подписан на это событие и списывает деньги асинхронно.\n- Это обеспечивает абсолютную слабую связанность (Loose Coupling): модуль заказа может работать, даже если платежный модуль временно недоступен.",
    "step_by_step": "1. Создайте интерфейс `EventBus` в общем ядре `shared`.\n2. Реализуйте публикацию события `OrderCreatedEvent`.\n3. Подпишите модуль `PaymentModule` на шину событий.\n4. Верифицируйте асинхронную реакцию без прямых вызовов между модулями.",
    "code_blocks": [
      {
        "filename": "feature_modules_event_bus_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\n// 1. Shared Kernel\ntype DomainEvent interface {\n\tEventName() string\n}\n\ntype EventBus interface {\n\tPublish(ctx context.Context, evt DomainEvent)\n\tSubscribe(eventName string, handler func(ctx context.Context, evt DomainEvent))\n}\n\ntype InMemoryEventBus struct {\n\tmu       sync.RWMutex\n\thandlers map[string][]func(ctx context.Context, evt DomainEvent)\n}\n\nfunc NewInMemoryEventBus() *InMemoryEventBus {\n\treturn &InMemoryEventBus{handlers: make(map[string][]func(ctx context.Context, evt DomainEvent))}\n}\n\nfunc (b *InMemoryEventBus) Publish(ctx context.Context, evt DomainEvent) {\n\tb.mu.RLock()\n\tdefer b.mu.RUnlock()\n\tfor _, h := range b.handlers[evt.EventName()] {\n\t\th(ctx, evt)\n\t}\n}\n\nfunc (b *InMemoryEventBus) Subscribe(eventName string, handler func(ctx context.Context, evt DomainEvent)) {\n\tb.mu.Lock()\n\tdefer b.mu.Unlock()\n\tb.handlers[eventName] = append(b.handlers[eventName], handler)\n}\n\n// 2. Order Module\ntype OrderCreatedEvent struct {\n\tOrderID string\n\tAmount  float64\n}\n\nfunc (e OrderCreatedEvent) EventName() string { return \"order.created\" }\n\nfunc TestFeatureModulesEventBus(t *testing.T) {\n\tbus := NewInMemoryEventBus()\n\n\tpaymentReceived := false\n\t// Модуль Payment подписывается на событие модуля Order\n\tbus.Subscribe(\"order.created\", func(ctx context.Context, evt DomainEvent) {\n\t\te := evt.(OrderCreatedEvent)\n\t\tif e.OrderID == \"ord-event-1\" && e.Amount == 5000.0 {\n\t\t\tpaymentReceived = true\n\t\t}\n\t})\n\n\t// Модуль Order публикует событие\n\tbus.Publish(context.Background(), OrderCreatedEvent{OrderID: \"ord-event-1\", Amount: 5000.0})\n\n\tif !paymentReceived {\n\t\tt.Fatal(\"Модуль платежей не обработал событие создания заказа\")\n\t}\n\n\tfmt.Println(\"Межмодульная коммуникация через Domain Events успешно подтверждена:\")\n\tfmt.Println(\"  • Модуль Order не имеет прямых зависимостей от модуля Payment\")\n\tfmt.Println(\"  • Событие order.created успешно доставлено через Shared EventBus\")\n}",
        "note": "Слабосвязанное взаимодействие модулей монолита через шину доменных событий"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v feature_modules_event_bus_test.go\n# Вывод:\n# === RUN   TestFeatureModulesEventBus\n# Межмодульная коммуникация через Domain Events успешно подтверждена:\n#   • Модуль Order не имеет прямых зависимостей от модуля Payment\n#   • Событие order.created успешно доставлено через Shared EventBus\n# --- PASS: TestFeatureModulesEventBus (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Переход на доменные события внутри монолита готовит архитектуру к безболезненному выносу модулей в микросервисы: реализация `InMemoryEventBus` в одной строчке кода заменяется на продюсер Kafka или RabbitMQ, при этом весь код доменных модулей остается неизменным.",
    "pitfalls": "Превращать Shared Kernel в мусорную свалку (Dump Package): если поместить в `shared` половину бизнес-моделей системы, модульность будет потеряна, и любое изменение в `shared` сломает все модули приложения.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Eventual Consistency (согласованность в конечном счете) при межмодульной коммуникации через события?»\n**Ответ:** Это модель, при которой данные в разных модулях согласуются не мгновенно в рамках одной ACID-транзакции, а с задержкой (пока событие дойдет и обработается подписчиком). Система гарантирует, что при отсутствии новых изменений в конечном счете все связанные модули придут к единому консистентному состоянию."
  },
  {
    "num": 20,
    "title": "Модульный монолит (Modular Monolith): приватные internal-пакеты и публичные контракты модулей",
    "task": "**Modular Monolith**: Разделите монолит на независимые модули (`/internal/users`, `/internal/orders`), которые общаются только через публичные интерфейсы, а не импортируют внутренние структуры друг друга.",
    "theory": "Инкапсуляция модулей в модульном монолите Go:\n- Каждый модуль имеет собственную структуру:\n  - `internal/users/internal/`: скрытые детали реализации (SQL-запросы, таблицы, DTO).\n  - `internal/users/public.go`: публичный интерфейс `Service` и экспортируемые структуры, через которые модуль взаимодействует с внешним миром.\n- **Запрет глубокого связывания:** Модуль `orders` не имеет права импортировать приватные репозитории `internal/users/internal/storage`. Компилятор Go предотвращает это на уровне сборки!",
    "step_by_step": "1. Создайте публичный фасад модуля пользователей `UserServiceFacade`.\n2. Сокройте внутренние структуры хранения в приватной области.\n3. Продемонстрируйте вызов только через открытый интерфейсный контракт.\n4. Верифицируйте изоляцию внутренних инвариантов модуля.",
    "code_blocks": [
      {
        "filename": "modular_monolith_facade_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Публичный контракт модуля: internal/users/public.go\ntype UserPublicDTO struct {\n\tID   string\n\tRole string\n}\n\ntype UserModuleAPI interface {\n\tGetUserPublicInfo(ctx context.Context, id string) (*UserPublicDTO, error)\n}\n\n// Приватная реализация модуля\ntype usersInternalService struct {\n\tsecretData map[string]string // Пароли и паспортные данные скрыты внутри модуля!\n}\n\nfunc (s *usersInternalService) GetUserPublicInfo(ctx context.Context, id string) (*UserPublicDTO, error) {\n\treturn &UserPublicDTO{ID: id, Role: \"VIP_CUSTOMER\"}, nil\n}\n\n// Потребитель: модуль internal/orders\ntype OrdersCoordinator struct {\n\tusersAPI UserModuleAPI\n}\n\nfunc (c *OrdersCoordinator) CanPlaceOrder(ctx context.Context, userID string) bool {\n\tinfo, err := c.usersAPI.GetUserPublicInfo(ctx, userID)\n\tif err != nil {\n\t\treturn false\n\t}\n\treturn info.Role == \"VIP_CUSTOMER\"\n}\n\nfunc TestModularMonolithFacade(t *testing.T) {\n\tusersService := &usersInternalService{secretData: map[string]string{\"u-77\": \"passport-12345\"}}\n\tordersModule := &OrdersCoordinator{usersAPI: usersService}\n\n\tallowed := ordersModule.CanPlaceOrder(context.Background(), \"u-77\")\n\tif !allowed {\n\t\tt.Fatal(\"Пользователь должен иметь право создания заказа\")\n\t}\n\n\tfmt.Println(\"Инкапсуляция модульного монолита успешно подтверждена:\")\n\tfmt.Println(\"  • Orders общается с Users строго через публичный интерфейс UserModuleAPI\")\n\tfmt.Println(\"  • Приватные поля (паспортные данные, SQL схемы) надежно скрыты внутри модуля Users\")\n}",
        "note": "Разграничение публичного API и скрытых приватных данных модуля"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v modular_monolith_facade_test.go\n# Вывод:\n# === RUN   TestModularMonolithFacade\n# Инкапсуляция модульного монолита успешно подтверждена:\n#   • Orders общается с Users строго через публичный интерфейс UserModuleAPI\n#   • Приватные поля (паспортные данные, SQL схемы) надежно скрыты внутри модуля Users\n# --- PASS: TestModularMonolithFacade (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Поддиректория `internal` внутри любого каталога Go изолирует видимость: пакет `a/b/internal/c` может импортироваться только кодом, находящимся внутри дерева `a/b`. Код из `a/d` при попытке импорта получит фатальную ошибку компиляции.",
    "pitfalls": "Экспортировать из публичного фасада модуля структуры GORM или SQL-модели: любая миграция БД сломает контракт с соседними модулями. Экспортировать разрешено только чистые DTO.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как предотвратить случайное добавление запрещенных межпакетных импортов в большом проекте на Go?»\n**Ответ:** Используют специализированные линтеры архитектуры в CI/CD:\n1. `go-cleanarch` — валидирует соблюдение кругов Clean Architecture.\n2. `depguard` (в составе `golangci-lint`) — запрещает импорт конкретных пакетов в заданных каталогах.\n3. `import-boss` — объявляет правила разрешенных зависимостей прямо в файлах `README.md` или `.import-restrictions`."
  },
  {
    "num": 21,
    "title": "Инверсия зависимостей в стиле Go: размещение интерфейса в пакете бизнес-логики",
    "task": "**Принцип инверсии зависимостей (DIP в Go)**: Интерфейсы в Go определяются там, где они *используются*, а не там, где реализуются! Перенеси интерфейс `UserRepository` из пакета базы данных в пакет бизнес-логики (`service`). Пакет `service` не должен ничего знать о пакете `postgres`.",
    "theory": "Разница между Java-style DIP и Go-style DIP:\n- **Java/C# традиция:** Интерфейс создается рядом с реализацией (`package postgres; public interface UserRepository`). Это привязывает пользователя к поставщику!\n- **Идиома Go:** Интерфейс создается **рядом с потребителем** (`package service; type UserRepository interface`).\n- Пакет `service` объявляет **только те методы, которые реально использует**.\n- Пакет `postgres` может реализовывать 50 методов, но если сервису нужен только `GetByID`, интерфейс состоит из ровно одного метода!",
    "step_by_step": "1. Создайте пакет сервиса с узким интерфейсом потребителя.\n2. Создайте пакет базы данных с широким репозиторием.\n3. Продемонстрируйте автоматическое удовлетворение интерфейса без взаимного импорта.\n4. Верифицируйте изоляцию компиляции.",
    "code_blocks": [
      {
        "filename": "go_style_dip_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Пакет: service (Потребитель)\ntype UserGetter interface {\n\tGetUser(ctx context.Context, id string) (string, error)\n}\n\ntype UserProfileService struct {\n\tgetter UserGetter\n}\n\nfunc NewUserProfileService(g UserGetter) *UserProfileService {\n\treturn &UserProfileService{getter: g}\n}\n\nfunc (s *UserProfileService) RenderProfile(ctx context.Context, id string) (string, error) {\n\tname, err := s.getter.GetUser(ctx, id)\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn \"Профиль пользователя: \" + name, nil\n}\n\n// Пакет: postgres (Реализация имеет 10 методов, но сервису нужен 1)\ntype FullPostgresRepository struct{}\n\nfunc (p *FullPostgresRepository) GetUser(ctx context.Context, id string) (string, error) {\n\treturn \"Дмитрий Ковалев\", nil\n}\n\nfunc (p *FullPostgresRepository) DeleteUser(ctx context.Context, id string) error { return nil }\nfunc (p *FullPostgresRepository) UpdatePassword(ctx context.Context, id, pass string) error {\n\treturn nil\n}\n\nfunc TestGoStyleDIP(t *testing.T) {\n\tdb := &FullPostgresRepository{}\n\tsvc := NewUserProfileService(db)\n\n\tres, err := svc.RenderProfile(context.Background(), \"usr-99\")\n\tif err != nil || res != \"Профиль пользователя: Дмитрий Ковалев\" {\n\t\tt.Fatalf(\"Ошибка рендеринга профиля: %v\", err)\n\t}\n\n\tfmt.Println(\"Инверсия зависимостей в стиле Go успешно подтверждена:\")\n\tfmt.Printf(\"  • Результат: %s\\n\", res)\n\tfmt.Println(\"  • Service объявил интерфейс UserGetter на 1 метод\")\n\tfmt.Println(\"  • PostgresRepository содержит лишние методы, но автоматически удовлетворяет интерфейсу!\")\n}",
        "note": "Неявное удовлетворение узкого интерфейса потребителя широким репозиторием"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v go_style_dip_test.go\n# Вывод:\n# === RUN   TestGoStyleDIP\n# Инверсия зависимостей в стиле Go успешно подтверждена:\n#   • Результат: Профиль пользователя: Дмитрий Ковалев\n#   • Service объявил интерфейс UserGetter на 1 метод\n#   • PostgresRepository содержит лишние методы, но автоматически удовлетворяет интерфейсу!\n# --- PASS: TestGoStyleDIP (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Компилятор Go генерирует таблицу виртуальных методов (`itab`) в момент присваивания конкретного типа интерфейсной переменной: он сопоставляет методы структуры и методы интерфейса. Если совпадение найдено, в рантайме вызов идет через быстрый косвенный вызов функции по указателю.",
    "pitfalls": "Экспортировать интерфейс из пакета реализации (`package postgres; type Repo interface`): это заставляет всех пользователей импортировать пакет `postgres`, порождая лишние зависимости от драйверов БД.",
    "bigtech_interview": "**Вопрос с собеседования:** «Какое золотое правило Роб Пайк сформулировал относительно интерфейсов в Go?»\n**Ответ:** «Do not design with interfaces, discover them» (Не проектируйте систему сразу интерфейсами — открывайте их по мере необходимости). Интерфейсы должны появляться тогда, когда возникает реальная потребность в полиморфизме или мокировании, а не заранее ради абстрактной красоты."
  },
  {
    "num": 22,
    "title": "Гибкая конфигурация сервиса: слияние настроек YAML/JSON файлов и переменных окружения",
    "task": "**Конфигурация приложения**. Загружайте настройки из YAML/JSON-файла и переменных окружения с приоритетом env. Используйте библиотеку `envconfig` или `viper`.",
    "theory": "Паттерн многоуровневого парсинга конфигурации:\n- В облачной инфраструктуре микросервис развертывается с базовым файлом конфигурации `config.yaml`, запеченным в Docker-образ.\n- Специфичные для окружения параметры (staging, production) переопределяются через переменные окружения Kubernetes Pod Spec (`env: ...`).\n- **Схема парсинга:**\n  1. Загрузка JSON/YAML в промежуточную структуру.\n  2. Обход полей структуры и проверка наличия соответствующих `ENV_` переменных.\n  3. Если переменная окружения задана, она перезаписывает значение из файла.\n  4. Финальная валидация и блокировка структуры от мутаций.",
    "step_by_step": "1. Создайте JSON-строку с базовой конфигурацией.\n2. Распарсите JSON в структуру настроек.\n3. Переопределите параметр базы данных через переменную окружения.\n4. Верифицируйте корректный приоритет переменных окружения над файлом.",
    "code_blocks": [
      {
        "filename": "env_override_config_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"os\"\n\t\"testing\"\n)\n\ntype ServerConfig struct {\n\tHost string `json:\"host\"`\n\tPort int    `json:\"port\"`\n\tMode string `json:\"mode\"`\n}\n\nfunc LoadConfigWithEnvOverride(jsonContent string) (*ServerConfig, error) {\n\tvar cfg ServerConfig\n\tif err := json.Unmarshal([]byte(jsonContent), &cfg); err != nil {\n\t\treturn nil, err\n\t}\n\n\t// Переменные окружения имеют абсолютный приоритет\n\tif envMode := os.Getenv(\"APP_MODE\"); envMode != \"\" {\n\t\tcfg.Mode = envMode\n\t}\n\n\treturn &cfg, nil\n}\n\nfunc TestEnvOverrideConfig(t *testing.T) {\n\tjsonBase := `{\"host\":\"0.0.0.0\",\"port\":8080,\"mode\":\"development\"}`\n\n\t_ = os.Setenv(\"APP_MODE\", \"production\")\n\tdefer func() { _ = os.Unsetenv(\"APP_MODE\") }()\n\n\tcfg, err := LoadConfigWithEnvOverride(jsonBase)\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка парсинга конфигурации: %v\", err)\n\t}\n\n\tif cfg.Mode != \"production\" {\n\t\tt.Fatalf(\"Переменная окружения не переопределила JSON режим: %s\", cfg.Mode)\n\t}\n\n\tfmt.Println(\"Конфигурация приложения с приоритетом env успешно подтверждена:\")\n\tfmt.Printf(\"  • Базовый JSON: host=%s, port=%d\\n\", cfg.Host, cfg.Port)\n\tfmt.Printf(\"  • Итоговый режим mode=%s (Успешно переопределен из APP_MODE=production)\\n\", cfg.Mode)\n}",
        "note": "Слияние параметров файла конфигурации и переменных окружения"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v env_override_config_test.go\n# Вывод:\n# === RUN   TestEnvOverrideConfig\n# Конфигурация приложения с приоритетом env успешно подтверждена:\n#   • Базовый JSON: host=0.0.0.0, port=8080\n#   • Итоговый режим mode=production (Успешно переопределен из APP_MODE=production)\n# --- PASS: TestEnvOverrideConfig (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Библиотека `viper` использует карту `map[string]any` без учета регистра ключей (Case-Insensitive), что позволяет приводить ключи вида `database.max_connections` к переменным окружения `DATABASE_MAX_CONNECTIONS`.",
    "pitfalls": "Использовать hot-reload конфигурации (viper.WatchConfig) в HighLoad без потокобезопасного мьютекса или `atomic.Value`: фоновое перезаписывание полей структуры при запросе приведет к мгновенному падению с Data Race.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как организовать безопасную ротацию секретов (токены, пароли) в работающем Go сервисе без рестарта процесса?»\n**Ответ:** Секреты монтируются через Kubernetes Secret как файл. Приложение подписывается на события файловой системы через `fsnotify`. При обновлении файла вызывается атомарная смена указателя на структуру конфигурации через `atomic.Pointer[Config]` или `atomic.Value`, гарантируя отсутствие гонок."
  },
  {
    "num": 23,
    "title": "Эволюция модульного монолита в микросервисы: скрытый internal и коммуникация через Event Bus",
    "task": "Реализуй **Modular Monolith**: монолит, но с чёткими границами модулей. Каждый модуль имеет свой `internal/` (скрыт от других модулей). Коммуникация только через `pkg/events/` (event bus). Покажи путь к микросервисам: вырезай модуль, добавляй gRPC/HTTP API.",
    "theory": "Бесшовный переход от Modular Monolith к Microservices:\n- Если монолит спроектирован правильно:\n  1. Вся персистентность модуля изолирована в собственных таблицах БД.\n  2. Входящие вызовы принимаются через чистые интерфейсы.\n  3. Исходящие вызовы отправляются как события в `EventBus`.\n- **Шаги выноса модуля в независимый микросервис:**\n  - Шаг 1: Копируем папку модуля в новый Git-репозиторий.\n  - Шаг 2: Заменяем входящий Go-интерфейс на gRPC сервер (`api.proto`).\n  - Шаг 3: Заменяем внутрипроцессный `EventBus` на продюсер Kafka.\n  - Шаг 4: В исходном монолите подменяем вызовы на gRPC-клиент.\n- **Ноль изменений в бизнес-логике домена!**",
    "step_by_step": "1. Создайте модуль `BillingModule` с шиной событий.\n2. Продемонстрируйте слабую связанность через контракт событий.\n3. Смоделируйте адаптер gRPC-транспорта для будущего выноса.\n4. Верифицируйте готовность модуля к автономной жизни.",
    "code_blocks": [
      {
        "filename": "monolith_to_microservice_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype InvoiceCreatedEvent struct {\n\tInvoiceID string\n\tAmount    float64\n}\n\ntype EventPublisher interface {\n\tPublishEvent(ctx context.Context, topic string, payload any) error\n}\n\ntype BillingService struct {\n\tevents EventPublisher\n}\n\nfunc NewBillingService(events EventPublisher) *BillingService {\n\treturn &BillingService{events: events}\n}\n\nfunc (s *BillingService) CreateInvoice(ctx context.Context, id string, amount float64) error {\n\t// Бизнес-логика создания инвойса\n\tevt := InvoiceCreatedEvent{InvoiceID: id, Amount: amount}\n\treturn s.events.PublishEvent(ctx, \"invoices\", evt)\n}\n\ntype MockEventPublisher struct {\n\tpublished []any\n}\n\nfunc (m *MockEventPublisher) PublishEvent(ctx context.Context, topic string, payload any) error {\n\tm.published = append(m.published, payload)\n\treturn nil\n}\n\nfunc TestMonolithToMicroservice(t *testing.T) {\n\tpub := &MockEventPublisher{}\n\tbilling := NewBillingService(pub)\n\n\terr := billing.CreateInvoice(context.Background(), \"inv-900\", 75000.0)\n\tif err != nil || len(pub.published) != 1 {\n\t\tt.Fatalf(\"Ошибка создания инвойса: %v\", err)\n\t}\n\n\tfmt.Println(\"Готовность к микросервисам (Monolith to Microservice) подтверждена:\")\n\tfmt.Printf(\"  • Модуль Billing отправляет события через EventPublisher\\n\")\n\tfmt.Printf(\"  • Событие: %+v\\n\", pub.published[0])\n\tfmt.Println(\"  • Для выноса в микросервис достаточно заменить MockEventPublisher на Kafka/NATS продюсер!\")\n}",
        "note": "Подготовка доменного модуля к автономному выносу в отдельный микросервис"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v monolith_to_microservice_test.go\n# Вывод:\n# === RUN   TestMonolithToMicroservice\n# Готовность к микросервисам (Monolith to Microservice) подтверждена:\n#   • Модуль Billing отправляет события через EventPublisher\n#   • Событие: {InvoiceID:inv-900 Amount:75000}\n#   • Для выноса в микросервис достаточно заменить MockEventPublisher на Kafka/NATS продюсер!\n# --- PASS: TestMonolithToMicroservice (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Использование интерфейса `EventPublisher` реализует архитектурный шаблон «Порты и Адаптеры». В монолите реализацией служит канал памяти `chan any`, а в распределенной системе — кластер Apache Kafka с сериализацией в Avro или Protobuf.",
    "pitfalls": "Связывать модули через общие транзакции БД (`BEGIN ... COMMIT` на таблицы двух модулей): при выносе модуля в микросервис ACID транзакция разорвется, и система останется в несогласованном состоянии.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое паттерн Strangler Fig (Душитель) при миграции монолита на микросервисы?»\n**Ответ:** Это стратегия постепенной миграции, при которой функционал монолита не переписывается целиком, а вырезается по одной фиче. На API Gateway трафик к новому микросервису постепенно переключается с монолита (например 10%, 50%, 100%), пока старый код монолита полностью не отомрет."
  },
  {
    "num": 24,
    "title": "Версионирование публичного API: подходы URI Path, Headers и управление обратной совместимостью",
    "task": "**API Versioning**: Реализуйте версионирование API через URL (`/v1/users`, `/v2/users`) или через заголовки (`Accept: application/vnd.myapi.v2+json`). Обсудите trade-offs.",
    "theory": "Стратегии версионирования REST API:\n1. **URI Path Versioning (`/api/v1/users`, `/api/v2/users`):**\n   - *Плюсы:* Максимальная прозрачность, простота маршрутизации в NGINX/Ingress, кэширование на CDN работает из коробки.\n   - *Минусы:* Нарушает чистоту REST (URI должен идентифицировать ресурс, а не версию представления).\n2. **Header Versioning (`Accept: application/vnd.company.v2+json` или `X-API-Version: 2`):**\n   - *Плюсы:* Истинный REST-подход (Content Negotiation), стабильный URI ресурса.\n   - *Минусы:* Сложнее тестировать через браузер, требует настройки заголовков `Vary` для CDN-кэшей.\n- **В BigTech 95% сервисов используют URI Path Versioning** из-за надежности и простоты маршрутизации.",
    "step_by_step": "1. Создайте роутер с поддержкой версионирования `/v1/` и `/v2/`.\n2. Реализуйте разные форматы ответа для v1 (плоская строка) и v2 (структурированный объект).\n3. Протестируйте оба эндпоинта.\n4. Верифицируйте сохранение обратной совместимости.",
    "code_blocks": [
      {
        "filename": "api_versioning_routes_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"testing\"\n)\n\ntype UserResponseV1 struct {\n\tID   string `json:\"id\"`\n\tName string `json:\"name\"`\n}\n\ntype UserResponseV2 struct {\n\tID        string `json:\"id\"`\n\tFirstName string `json:\"first_name\"`\n\tLastName  string `json:\"last_name\"`\n}\n\nfunc BuildVersionedMux() *http.ServeMux {\n\tmux := http.NewServeMux()\n\n\t// V1 Handler (Обратная совместимость со старыми клиентами)\n\tmux.HandleFunc(\"/api/v1/users\", func(w http.ResponseWriter, r *http.Request) {\n\t\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t\t_ = json.NewEncoder(w).Encode(UserResponseV1{ID: \"usr-1\", Name: \"Иван Смирнов\"})\n\t})\n\n\t// V2 Handler (Новый контракт с разбиением имени)\n\tmux.HandleFunc(\"/api/v2/users\", func(w http.ResponseWriter, r *http.Request) {\n\t\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t\t_ = json.NewEncoder(w).Encode(UserResponseV2{ID: \"usr-1\", FirstName: \"Иван\", LastName: \"Смирнов\"})\n\t})\n\n\treturn mux\n}\n\nfunc TestAPIVersioningRoutes(t *testing.T) {\n\tmux := BuildVersionedMux()\n\n\t// 1. Проверяем v1\n\treq1 := httptest.NewRequest(http.MethodGet, \"/api/v1/users\", nil)\n\trec1 := httptest.NewRecorder()\n\tmux.ServeHTTP(rec1, req1)\n\n\tvar v1 UserResponseV1\n\t_ = json.NewDecoder(rec1.Body).Decode(&v1)\n\tif v1.Name != \"Иван Смирнов\" {\n\t\tt.Fatalf(\"Сбой v1: %+v\", v1)\n\t}\n\n\t// 2. Проверяем v2\n\treq2 := httptest.NewRequest(http.MethodGet, \"/api/v2/users\", nil)\n\trec2 := httptest.NewRecorder()\n\tmux.ServeHTTP(rec2, req2)\n\n\tvar v2 UserResponseV2\n\t_ = json.NewDecoder(rec2.Body).Decode(&v2)\n\tif v2.FirstName != \"Иван\" || v2.LastName != \"Смирнов\" {\n\t\tt.Fatalf(\"Сбой v2: %+v\", v2)\n\t}\n\n\tfmt.Println(\"Версионирование API (API Versioning) успешно подтверждено:\")\n\tfmt.Printf(\"  • V1 ответ: Name=%q (Для старых мобильных клиентов)\\n\", v1.Name)\n\tfmt.Printf(\"  • V2 ответ: FirstName=%q, LastName=%q (Для нового веб-портала)\\n\", v2.FirstName, v2.LastName)\n}",
        "note": "Маршрутизация версий API и сосуществование контрактов v1 и v2"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v api_versioning_routes_test.go\n# Вывод:\n# === RUN   TestAPIVersioningRoutes\n# Версионирование API (API Versioning) успешно подтверждено:\n#   • V1 ответ: Name=\"Иван Смирнов\" (Для старых мобильных клиентов)\n#   • V2 ответ: FirstName=\"Иван\", LastName=\"Смирнов\" (Для нового веб-портала)\n# --- PASS: TestAPIVersioningRoutes (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go 1.22+ стандартный `http.ServeMux` поддерживает сопоставление путей с шаблонами и методами (`mux.HandleFunc(\"GET /api/v2/users/{id}\", ...)`), что позволяет реализовывать версионирование маршрутов без использования тяжелых сторонних фреймворков.",
    "pitfalls": "Вводить новую версию API `v2` ради добавления одного необязательного поля: добавление новых полей в JSON является обратно совместимым (Non-breaking change). Новую версию создают **только при ломающих изменениях** (удаление или переименование полей).",
    "bigtech_interview": "**Вопрос с собеседования:** «Как долго в продакшене поддерживают устаревшие версии API (v1) после релиза v2?»\n**Ответ:** По регламенту Deprecation Policy:\n1. Выставляется заголовок `Deprecation: true` и `Sunset: Wed, 11 Nov 2026 00:00:00 GMT` по стандарту RFC 8594.\n2. Анализируются логи и метрики обращений к v1.\n3. Проводится Brownout (учебное отключение v1 на 15 минут в непиковые часы для выявления забытых клиентов).\n4. Окончательное удаление через 6–12 месяцев после уведомления всех потребителей."
  },
  {
    "num": 25,
    "title": "Декомпозиция по DDD Bounded Contexts: определение границ контекстов и модели предметной области",
    "task": "Спроектируй **декомпозицию по DDD Bounded Contexts**: `User Context` (профиль, аутентификация), `Order Context` (заказы, корзина), `Payment Context` (списание, возврат), `Inventory Context` (склад, резервирование), `Shipping Context` (доставка, трекинг). Покажи, что каждый контекст — потенциальный микросервис.",
    "theory": "Ограниченный контекст (Bounded Context) в Domain-Driven Design (Эрик Эванс):\n- Bounded Context — это явная граница, внутри которой модель предметной области имеет единый, однозначный смысл (Единый язык — Ubiquitous Language).\n- **Одна и та же реальная сущность в разных контекстах моделируется по-разному:**\n  - В `User Context`: пользователь — это учетная запись (`Email`, `PasswordHash`, `Role`).\n  - В `Order Context`: пользователь — это покупатель (`Customer`, `DiscountLevel`, `DeliveryAddress`).\n  - В `Shipping Context`: пользователь — это получатель посылки (`Recipient`, `PhoneNumber`, `PassportID`).\n- Попытка создать одну глобальную структуру `User` на всю компанию — фатальная ошибка монолитного мышления!",
    "step_by_step": "1. Создайте доменные модели сущности в контекстах Order, Payment, Shipping.\n2. Продемонстрируйте различие атрибутов одной бизнес-сущности.\n3. Опишите интерфейсы взаимодействия между Bounded Contexts.\n4. Верифицируйте границы контекстов.",
    "code_blocks": [
      {
        "filename": "ddd_bounded_contexts_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// 1. User Context: Фокус на аутентификации\ntype AuthUser struct {\n\tID           string\n\tEmail        string\n\tPasswordHash string\n}\n\n// 2. Order Context: Тот же человек, но как покупатель (Customer)\ntype OrderCustomer struct {\n\tID            string\n\tDiscountLevel int\n}\n\n// 3. Shipping Context: Тот же человек, но как получатель груза (Recipient)\ntype ShippingRecipient struct {\n\tID              string\n\tDeliveryAddress string\n\tContactPhone    string\n}\n\nfunc TestDDDBoundedContexts(t *testing.T) {\n\tpersonID := \"person-99\"\n\n\tuser := AuthUser{ID: personID, Email: \"client@mail.ru\", PasswordHash: \"hash-secret\"}\n\tcustomer := OrderCustomer{ID: personID, DiscountLevel: 10}\n\trecipient := ShippingRecipient{ID: personID, DeliveryAddress: \"г. Москва, ул. Ленина, д. 5\", ContactPhone: \"+79991234567\"}\n\n\tif user.ID != customer.ID || customer.ID != recipient.ID {\n\t\tt.Fatal(\"Идентификаторы должны указывать на одну персону\")\n\t}\n\n\tfmt.Println(\"Декомпозиция по DDD Bounded Contexts успешно продемонстрирована:\")\n\tfmt.Printf(\"  • Auth Context:     ID=%s, Email=%s (Фокус: безопасность)\\n\", user.ID, user.Email)\n\tfmt.Printf(\"  • Order Context:    ID=%s, Скидка=%d%% (Фокус: коммерция)\\n\", customer.ID, customer.DiscountLevel)\n\tfmt.Printf(\"  • Shipping Context: ID=%s, Адрес=%q (Фокус: логистика)\\n\", recipient.ID, recipient.DeliveryAddress)\n\tfmt.Println(\"  • Каждый Bounded Context автономен и готов стать независимым микросервисом!\")\n}",
        "note": "Разделение моделей одной сущности по ограниченным контекстам DDD"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v ddd_bounded_contexts_test.go\n# Вывод:\n# === RUN   TestDDDBoundedContexts\n# Декомпозиция по DDD Bounded Contexts успешно продемонстрирована:\n#   • Auth Context:     ID=person-99, Email=client@mail.ru (Фокус: безопасность)\n#   • Order Context:    ID=person-99, Скидка=10% (Фокус: коммерция)\n#   • Shipping Context: ID=person-99, Адрес=\"г. Москва, ул. Ленина, д. 5\" (Фокус: логистика)\n#   • Каждый Bounded Context автономен и готов стать независимым микросервисом!\n# --- PASS: TestDDDBoundedContexts (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Изоляция Bounded Contexts на уровне Go выражается в независимых Go-модулях (или пакетах), где общие понятия связываются только строковыми или целочисленными ID (`CustomerID string`), исключая передачу ссылочных типов указателей между границами контекстов.",
    "pitfalls": "Создавать «Каноническую модель данных» (Canonical Data Model) с объединением 100 полей в одну гигантскую структуру `User`: любое изменение поля в логистике потребует перекомпиляции и тестирования сервиса аутентификации.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Context Map (Карта контекстов) в Domain-Driven Design и какие виды связей между контекстами бывают?»\n**Ответ:** Context Map фиксирует границы контекстов и отношения между командами:\n1. **Shared Kernel:** общее разделяемое ядро кода.\n2. **Customer-Supplier / Leader-Follower:** один контекст зависит от контрактов другого.\n3. **Conformist:** зависимый контекст безоговорочно принимает модель поставщика.\n4. **Anti-Corruption Layer (ACL):** защитный трансляционный слой между внешним сервисом и чистым доменом.\n5. **Separate Ways:** контексты не взаимодействуют вовсе."
  },
  {
    "num": 26,
    "title": "Антикоррупционный слой (Anti-Corruption Layer, ACL): защита домена от внешних и устаревших моделей",
    "task": "Реализуй **Anti-Corruption Layer (ACL)**: `Order Service` интегрируется с внешним legacy-сервисом `Payment Gateway`. Создай транслятор, который конвертирует грязные внешние DTO в чистые доменные сущности. Защити домен от утечки чужих концепций.",
    "theory": "Паттерн Anti-Corruption Layer (Эрик Эванс):\n- При интеграции с внешними API (банковские шлюзы, 1С, SAP, старые монолиты) внешний сервис часто навязывает архаичные или некорректные структуры данных (поля `TXT_CLI_NAME_1`, строковые даты `DD/MM/YYYY`, XML-обертки).\n- **Опасность:** Если разработчик пустит эти структуры напрямую в доменное ядро, вся кодовая база окажется «отравлена» чужой плохой моделью.\n- **Решение — ACL (Транслятор-Изолятор):**\n  1. `External DTO` парсится только на границе адаптера.\n  2. `ACL Translator` преобразует его в строгий доменный тип `domain.PaymentStatus`.\n  3. Внутренний домен работает только со своими чистыми типами.",
    "step_by_step": "1. Создайте доменную модель `PaymentResult` с перечислением статусов.\n2. Создайте структуру устаревшего ответа `LegacyBankXMLResponse`.\n3. Напишите функцию трансляции ACL `TranslateLegacyResponse`.\n4. Верифицируйте изоляцию и чистоту доменных инвариантов.",
    "code_blocks": [
      {
        "filename": "anti_corruption_layer_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n)\n\n// Чистый домен\ntype PaymentStatus int\n\nconst (\n\tStatusUnknown PaymentStatus = iota\n\tStatusSuccess\n\tStatusFailed\n)\n\ntype PaymentResult struct {\n\tTransactionID string\n\tAmountKopecks int64\n\tStatus        PaymentStatus\n\tProcessedAt   time.Time\n}\n\n// \"Грязный\" legacy DTO стороннего банка\ntype LegacyBankDTO struct {\n\tTXT_TX_ID   string\n\tSTR_AMOUNT  string // \"1500.50\"\n\tCODE_STATUS string // \"OK_00\", \"ERR_99\"\n\tDATE_RAW    string // \"2026-09-04 12:00:00\"\n}\n\n// Anti-Corruption Layer (ACL) Translator\nfunc TranslateLegacyToDomain(dto LegacyBankDTO) (*PaymentResult, error) {\n\tstatus := StatusUnknown\n\tswitch dto.CODE_STATUS {\n\tcase \"OK_00\":\n\t\tstatus = StatusSuccess\n\tcase \"ERR_99\":\n\t\tstatus = StatusFailed\n\tdefault:\n\t\treturn nil, fmt.Errorf(\"unknown legacy status code: %s\", dto.CODE_STATUS)\n\t}\n\n\t// Парсинг суммы в копейки без потери точности float\n\tvar rub, kop int64\n\t_, _ = fmt.Sscanf(dto.STR_AMOUNT, \"%d.%d\", &rub, &kop)\n\ttotalKopecks := rub*100 + kop\n\n\tt, err := time.Parse(\"2006-01-02 15:04:05\", dto.DATE_RAW)\n\tif err != nil {\n\t\tt = time.Now()\n\t}\n\n\treturn &PaymentResult{\n\t\tTransactionID: strings.TrimSpace(dto.TXT_TX_ID),\n\t\tAmountKopecks: totalKopecks,\n\t\tStatus:        status,\n\t\tProcessedAt:   t,\n\t}, nil\n}\n\nfunc TestAntiCorruptionLayer(t *testing.T) {\n\tlegacy := LegacyBankDTO{\n\t\tTXT_TX_ID:   \"  TX-BANK-88899  \",\n\t\tSTR_AMOUNT:  \"250.75\",\n\t\tCODE_STATUS: \"OK_00\",\n\t\tDATE_RAW:    \"2026-09-04 10:30:00\",\n\t}\n\n\tres, err := TranslateLegacyToDomain(legacy)\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка трансляции в ACL: %v\", err)\n\t}\n\n\tif res.TransactionID != \"TX-BANK-88899\" || res.AmountKopecks != 25075 || res.Status != StatusSuccess {\n\t\tt.Fatalf(\"Некорректная трансляция доменных данных: %+v\", res)\n\t}\n\n\tfmt.Println(\"Антикоррупционный слой (ACL) успешно подтвержден:\")\n\tfmt.Printf(\"  • Legacy ответ успешно транслирован в доменный PaymentResult\\n\")\n\tfmt.Printf(\"  • ID: %s, Сумма: %d коп, Статус: SUCCESS\\n\", res.TransactionID, res.AmountKopecks)\n\tfmt.Println(\"  • Домен полностью защищен от полей вида TXT_TX_ID и CODE_STATUS\")\n}",
        "note": "Трансляция неструктурированных внешних DTO в строгие доменные структуры"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v anti_corruption_layer_test.go\n# Вывод:\n# === RUN   TestAntiCorruptionLayer\n# Антикоррупционный слой (ACL) успешно подтвержден:\n#   • Legacy ответ успешно транслирован в доменный PaymentResult\n#   • ID: TX-BANK-88899, Сумма: 25075 коп, Статус: SUCCESS\n#   • Домен полностью защищен от полей вида TXT_TX_ID и CODE_STATUS\n# --- PASS: TestAntiCorruptionLayer (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "ACL реализуется в виде отдельного адаптера (Adapter + Facade + Translator). Домен определяет порт `PaymentPort`, а адаптер реализует его, скрывая сетевой клиент и парсинг внутри себя. Ни одна структура из SDK стороннего банка не экспортируется за пределы адаптера.",
    "pitfalls": "Пропускать DTO внешнего сервиса в Use Cases ради экономии строк кода: через 3 месяца внешний банк обновит формат API с JSON на Protobuf, и вам придется переписывать половину бизнес-логики сервиса.",
    "bigtech_interview": "**Вопрос с собеседования:** «Где физически должен располагаться Anti-Corruption Layer: в отдельном микросервисе или внутри вызывающего сервиса?»\n**Ответ:** Зависит от масштаба. Если интеграция с внешней системой нужна только одному сервису — ACL оформляется как пакет адаптера внутри этого сервиса. Если к внешней тяжелой системе (например SAP или Core Banking) обращаются 10 микросервисов компании — ACL выносят в выделенный сервис-фасад (BFF или Integration Gateway)."
  },
  {
    "num": 27,
    "title": "Богатая модель предметной области (Rich Domain Model) против анемичной модели (Anemic Domain Model)",
    "task": "**Богатая модель (Rich Domain Model) vs Анемичная (Anemic)**: Реализуйте сущность `BankAccount`. Сравните два подхода: анемичный (структура без методов, вся логика в Service) и богатый (структура сама контролирует инварианты, методы `Withdraw`, `Deposit`). Обсудите инкапсуляцию.",
    "theory": "Антипаттерн Anemic Domain Model (Мартин Фаулер):\n- **Анемичная модель:** Структура содержит только публичные поля `Balance float64`, а вся логика валидации и изменения баланса размазана по `AccountService`. Сущность превращается в простой мешок с данными (Data Bag).\n  - *Опасность:* Любой программист может напрямую написать `acc.Balance -= 1000` без проверки овердрафта.\n- **Богатая доменная модель (Rich Domain Model):**\n  - Поля приватны (инкапсуляция).\n  - Изменение состояния возможно **только через методы доменной сущности**: `account.Withdraw(amount)`.\n  - Сущность сама гарантирует свои инварианты (баланс не может стать отрицательным).",
    "step_by_step": "1. Создайте богатую сущность `BankAccount` с приватным балансом.\n2. Реализуйте методы `Deposit` и `Withdraw` с защитой инвариантов.\n3. Продемонстрируйте невозможность перевода счета в невалидное состояние.\n4. Верифицируйте корректность инкапсуляции.",
    "code_blocks": [
      {
        "filename": "rich_domain_model_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Rich Domain Model: поля инкапсулированы, инварианты защищены\ntype BankAccount struct {\n\tid      string\n\tbalance int64 // В копейках / центах (целое число!)\n}\n\nfunc NewBankAccount(id string, initialDeposit int64) (*BankAccount, error) {\n\tif initialDeposit < 0 {\n\t\treturn nil, errors.New(\"initial deposit cannot be negative\")\n\t}\n\treturn &BankAccount{id: id, balance: initialDeposit}, nil\n}\n\nfunc (a *BankAccount) Balance() int64 {\n\treturn a.balance\n}\n\nfunc (a *BankAccount) Deposit(amount int64) error {\n\tif amount <= 0 {\n\t\treturn errors.New(\"deposit amount must be positive\")\n\t}\n\ta.balance += amount\n\treturn nil\n}\n\nfunc (a *BankAccount) Withdraw(amount int64) error {\n\tif amount <= 0 {\n\t\treturn errors.New(\"withdraw amount must be positive\")\n\t}\n\tif a.balance < amount {\n\t\treturn errors.New(\"insufficient funds for withdrawal\")\n\t}\n\ta.balance -= amount\n\treturn nil\n}\n\nfunc TestRichDomainModel(t *testing.T) {\n\tacc, err := NewBankAccount(\"acc-123\", 10_000)\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка создания счета: %v\", err)\n\t}\n\n\t// Успешное снятие\n\tif err := acc.Withdraw(4_000); err != nil {\n\t\tt.Fatalf(\"Ошибка снятия средств: %v\", err)\n\t}\n\n\t// Попытка уйти в минус (инвариант защищен сущностью!)\n\terr = acc.Withdraw(10_000)\n\tif err == nil {\n\t\tt.Fatal(\"Ожидалась ошибка превышения баланса\")\n\t}\n\n\tif acc.Balance() != 6_000 {\n\t\tt.Fatalf(\"Некорректный остаток на счете: %d\", acc.Balance())\n\t}\n\n\tfmt.Println(\"Богатая модель предметной области (Rich Domain Model) подтверждена:\")\n\tfmt.Printf(\"  • Баланс счета: %d коп. Инвариант защищен от прямого изменения извне\\n\", acc.Balance())\n\tfmt.Println(\"  • Попытка овердрафта успешно отклонена бизнес-методом Withdraw()\")\n}",
        "note": "Инкапсуляция бизнес-инвариантов внутри богатой доменной модели BankAccount"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v rich_domain_model_test.go\n# Вывод:\n# === RUN   TestRichDomainModel\n# Богатая модель предметной области (Rich Domain Model) подтверждена:\n#   • Баланс счета: 6000 коп. Инвариант защищен от прямого изменения извне\n#   • Попытка овердрафта успешно отклонена бизнес-методом Withdraw()\n# --- PASS: TestRichDomainModel (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go инкапсуляция обеспечивается не ключевым словом `private`, а первой строчной буквой идентификатора (`balance`). Все методы и структуры одного пакета видят приватные поля друг друга, но для всех внешних пакетов доступ к `balance` закрыт на уровне компилятора.",
    "pitfalls": "Добавлять в доменную сущность методы ввода-вывода (например `acc.SaveToDB()`): это нарушает SRP и превращает сущность в антипаттерн Active Record. Доменная сущность должна менять только свое состояние в памяти; сохранением в БД занимается репозиторий.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в Go часто пишут анемичные модели (Anemic Domain Model), хотя все книги по DDD советуют Rich Model?»\n**Ответ:** Из-за особенностей сериализации `encoding/json` и баз данных: чтобы библиотека могла прочитать поле, оно обязано быть экспортируемым (с большой буквы). Если делать все поля публичными, инкапсуляция теряется. В идиоматичном Rich Go DDD доменные сущности имеют приватные поля и фабричные методы `New()`, а для JSON и БД создают отдельные DTO с экспортируемыми полями."
  },
  {
    "num": 28,
    "title": "Корень агрегата (Aggregate Root): управление транзакционными границами сущностей Order и OrderItem",
    "task": "**Агрегаты (Aggregates) и Aggregate Root**: Спроектируйте агрегат `Order` с позициями `OrderItem`. Покажите, что внешний мир может взаимодействовать со строками заказа **только** через корень агрегата (`Order`), а не напрямую.",
    "theory": "Концепция Агрегатов в DDD (Aggregate & Aggregate Root):\n- **Агрегат** — это кластер связанных доменных объектов (Entities и Value Objects), которые рассматриваются как единое целое при любых изменениях данных.\n- **Корень агрегата (Aggregate Root):**\n  1. Единственная точка входа для внешнего мира.\n  2. Внешние объекты не имеют права хранить постоянные ссылки на внутренние сущности (`OrderItem`).\n  3. Добавление или удаление позиций выполняется **только** через методы корня: `order.AddItem(productID, qty, price)`.\n  4. Корень агрегата гарантирует инварианты всего дерева (например: максимальная сумма заказа 1 млн руб, не более 50 позиций).\n  5. **Правило транзакций:** Одна транзакция в БД должна изменять **ровно один агрегат**!",
    "step_by_step": "1. Создайте внутреннюю сущность `OrderItem`.\n2. Создайте корень агрегата `Order` с приватным срезом позиций.\n3. Реализуйте метод `AddItem` с пересчетом общей суммы заказа.\n4. Верифицируйте запрет прямого манипулирования строками заказа извне.",
    "code_blocks": [
      {
        "filename": "aggregate_root_order_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Внутренняя сущность агрегата\ntype OrderItem struct {\n\tsku      string\n\tquantity int\n\tprice    float64\n}\n\n// Корень агрегата (Aggregate Root)\ntype OrderAggregate struct {\n\tid         string\n\titems      []OrderItem\n\ttotalPrice float64\n}\n\nfunc NewOrderAggregate(id string) *OrderAggregate {\n\treturn &OrderAggregate{id: id}\n}\n\nfunc (o *OrderAggregate) TotalPrice() float64 {\n\treturn o.totalPrice\n}\n\nfunc (o *OrderAggregate) ItemsCount() int {\n\treturn len(o.items)\n}\n\n// Изменение состояния ДОСТУПНО ТОЛЬКО ЧЕРЕЗ КОРЕНЬ\nfunc (o *OrderAggregate) AddItem(sku string, qty int, price float64) error {\n\tif qty <= 0 {\n\t\treturn errors.New(\"quantity must be greater than zero\")\n\t}\n\tif price <= 0 {\n\t\treturn errors.New(\"price must be positive\")\n\t}\n\t// Инвариант агрегата: не более 5 позиций\n\tif len(o.items) >= 5 {\n\t\treturn errors.New(\"order cannot contain more than 5 items\")\n\t}\n\n\to.items = append(o.items, OrderItem{sku: sku, quantity: qty, price: price})\n\to.totalPrice += float64(qty) * price\n\treturn nil\n}\n\nfunc TestAggregateRootOrder(t *testing.T) {\n\torder := NewOrderAggregate(\"ord-root-1\")\n\n\t_ = order.AddItem(\"sku-mouse\", 1, 1500.0)\n\t_ = order.AddItem(\"sku-keyboard\", 1, 4500.0)\n\n\tif order.TotalPrice() != 6000.0 || order.ItemsCount() != 2 {\n\t\tt.Fatalf(\"Инвариант суммы агрегата нарушен: total=%v, count=%d\", order.TotalPrice(), order.ItemsCount())\n\t}\n\n\tfmt.Println(\"Паттерн Aggregate Root (Корень агрегата) успешно подтвержден:\")\n\tfmt.Printf(\"  • Заказ ID: %s, Позиций: %d, Итоговая сумма: %.2f руб\\n\", order.id, order.ItemsCount(), order.TotalPrice())\n\tfmt.Println(\"  • Позиции OrderItem защищены от прямого изменения в обход инвариантов Order\")\n}",
        "note": "Управление инвариантами дерева сущностей через корень агрегата Order"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v aggregate_root_order_test.go\n# Вывод:\n# === RUN   TestAggregateRootOrder\n# Паттерн Aggregate Root (Корень агрегата) успешно подтвержден:\n#   • Заказ ID: ord-root-1, Позиций: 2, Итоговая сумма: 6000.00 руб\n#   • Позиции OrderItem защищены от прямого изменения в обход инвариантов Order\n# --- PASS: TestAggregateRootOrder (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Агрегаты задают границы согласованности данных в распределенных системах. За счет того, что репозиторий загружает и сохраняет агрегат целиком (`repo.Save(order)`), исключаются аномалии частичного обновления (Lost Updates).",
    "pitfalls": "Делать гигантские агрегаты (например `User` содержит все свои 100 000 заказов): загрузка такого агрегата из БД съест гигабайты памяти и приведет к блокировкам строк в СУБД. Агрегаты должны быть максимально компактными.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как агрегат Order должен ссылаться на другой агрегат Customer в правильном DDD?»\n**Ответ:** Только по уникальному идентификатору (`CustomerID string`), но **никогда не по ссылке-указателю** (`*Customer`). Ссылки по указателям стирают границы агрегатов и провоцируют каскадную загрузку всей базы данных в память."
  },
  {
    "num": 29,
    "title": "Доменные события (Domain Events): генерация событий внутри агрегата и публикация во внешний мир",
    "task": "**Доменные события (Domain Events)**: Реализуйте паттерн, где при изменении состояния агрегата (`Order.Cancel()`) агрегат генерирует событие `OrderCancelledEvent` и складывает его во внутренний список. Слой Use Case достаёт события и отправляет их в брокер.",
    "theory": "Паттерн накопления доменных событий (Internal Event Accumulation):\n- Доменная сущность не должна иметь сетевых зависимостей (не должна импортировать Kafka продюсер).\n- **Как публиковать события из домена?**\n  1. Внутри агрегата объявляется слайс `events []any`.\n  2. Бизнес-метод `order.Cancel(reason)` меняет статус и добавляет событие:\n     `o.RecordEvent(OrderCancelled{OrderID: o.id, Reason: reason})`.\n  3. Репозиторий сохраняет заказ в транзакции БД.\n  4. Слой Use Case вызывает `events := order.PullEvents()` и публикует их в EventBus/Outbox.",
    "step_by_step": "1. Создайте событие `OrderCancelledEvent`.\n2. Добавьте в агрегат хранилище незакоммиченных событий.\n3. Вызовите отмену заказа с фиксацией события.\n4. Верифицируйте извлечение событий через метод `PullEvents`.",
    "code_blocks": [
      {
        "filename": "domain_events_pattern_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype DomainEventPayload interface {\n\tEventName() string\n}\n\ntype OrderCancelledEvent struct {\n\tOrderID   string\n\tReason    string\n\tOccurredAt time.Time\n}\n\nfunc (e OrderCancelledEvent) EventName() string { return \"order.cancelled\" }\n\ntype OrderWithEvents struct {\n\tid     string\n\tstatus string\n\tevents []DomainEventPayload\n}\n\nfunc NewOrderWithEvents(id string) *OrderWithEvents {\n\treturn &OrderWithEvents{id: id, status: \"NEW\"}\n}\n\nfunc (o *OrderWithEvents) Cancel(reason string) error {\n\tif o.status == \"CANCELLED\" {\n\t\treturn fmt.Errorf(\"order is already cancelled\")\n\t}\n\to.status = \"CANCELLED\"\n\t// Накопление доменного события\n\to.events = append(o.events, OrderCancelledEvent{\n\t\tOrderID:   o.id,\n\t\tReason:    reason,\n\t\tOccurredAt: time.Now(),\n\t})\n\treturn nil\n}\n\nfunc (o *OrderWithEvents) PullEvents() []DomainEventPayload {\n\tevts := o.events\n\to.events = nil // Очищаем список после выгрузки\n\treturn evts\n}\n\nfunc TestDomainEventsPattern(t *testing.T) {\n\torder := NewOrderWithEvents(\"ord-devt-99\")\n\n\tif err := order.Cancel(\"клиент передумал\"); err != nil {\n\t\tt.Fatalf(\"Ошибка отмены заказа: %v\", err)\n\t}\n\n\tevents := order.PullEvents()\n\tif len(events) != 1 {\n\t\tt.Fatalf(\"Ожидалось 1 доменное событие, получено: %d\", len(events))\n\t}\n\n\tevt, ok := events[0].(OrderCancelledEvent)\n\tif !ok || evt.Reason != \"клиент передумал\" {\n\t\tt.Fatalf(\"Некорректные параметры события: %+v\", events[0])\n\t}\n\n\t// Повторная выгрузка должна вернуть пустой слайс\n\tif len(order.PullEvents()) != 0 {\n\t\tt.Fatal(\"События не были очищены при вызове PullEvents\")\n\t}\n\n\tfmt.Println(\"Паттерн Domain Events успешно подтвержден:\")\n\tfmt.Printf(\"  • Агрегат сгенерировал событие: %s (Причина: %q)\\n\", evt.EventName(), evt.Reason)\n\tfmt.Println(\"  • Агрегат сохраняет чистоту от сетевых брокеров: публикацию выполняет Use Case\")\n}",
        "note": "Генерация и накопление доменных событий внутри агрегата с последующей выгрузкой"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v domain_events_pattern_test.go\n# Вывод:\n# === RUN   TestDomainEventsPattern\n# Паттерн Domain Events успешно подтвержден:\n#   • Агрегат сгенерировал событие: order.cancelled (Причина: \"клиент передумал\")\n#   • Агрегат сохраняет чистоту от сетевых брокеров: публикацию выполняет Use Case\n# --- PASS: TestDomainEventsPattern (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Этот паттерн предотвращает потерю событий: события выгружаются только после того, как репозиторий подтвердил успешный `COMMIT` транзакции в реляционной базе данных (например через таблицу Outbox).",
    "pitfalls": "Отправлять события в сеть прямо из тела доменного метода `Cancel()`: если транзакция базы данных упадет с ошибкой отката (ROLLBACK), сообщение в Kafka уже улетит, вызвав фатальное рассинхронизирование систем.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие Domain Event от Integration Event?»\n**Ответ:** \n- **Domain Event:** отражает событие внутри ограниченного контекста (Bounded Context) в терминах единого языка домена (`OrderItemsReserved`). Часто содержит детальные внутренние структуры.\n- **Integration Event:** публикуется наружу для других микросервисов компании. Содержит только минимальный стабильный публичный контракт без внутренних деталей реализации."
  },
  {
    "num": 30,
    "title": "Разрешение циклических зависимостей (Import Cycle): слияние пакетов или выделение независимого core",
    "task": "**Циклические зависимости (Import Cycle)**: Намеренно создай ситуацию: пакет `user` импортирует `order`, а `order` импортирует `user`. Получи ошибку компилятора. Реши проблему двумя способами: 1) объединением в один пакет, 2) вынесением общих структур/интерфейсов в третий независимый пакет `domain` или `core`.",
    "theory": "Запрет циклических импортов в компиляторе Go:\n- В Go строго запрещены графы зависимостей с циклами ($A \\to B \\to A$).\n- **Почему компилятор Go запрещает циклы:**\n  1. Гарантия молниеносной скорости компиляции: пакеты компилируются в строгом топологическом порядке.\n  2. Исключение неразрешимых циклических вызовов функций инициализации `init()`.\n- **Два архитектурных способа решения:**\n  - **Способ 1 (Merge):** Объединить тесно связанные структуры в один пакет. Если сущности не могут жить друг без друга, это один домен!\n  - **Способ 2 (Core/Domain Extraction):** Вынести разделяемые интерфейсы или идентификаторы в третий пакет нижнего уровня `core/types`.",
    "step_by_step": "1. Смоделируйте структуру типов, провоцирующую циклический импорт.\n2. Реализуйте разрыв связи через выделение интерфейсов в пакет `core`.\n3. Скомпонуйте зависимости без взаимного циклического импорта.\n4. Верифицируйте успешную компиляцию.",
    "code_blocks": [
      {
        "filename": "break_import_cycle_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Выделенный пакет: core (Нижний уровень)\ntype UserID string\ntype OrderID string\n\ntype UserInfo struct {\n\tID   UserID\n\tName string\n}\n\n// Пакет orders: зависит только от core\ntype Order struct {\n\tID     OrderID\n\tUserID UserID\n\tAmount float64\n}\n\n// Пакет users: зависит только от core\ntype UserProfile struct {\n\tInfo UserInfo\n}\n\nfunc TestBreakImportCycle(t *testing.T) {\n\tuid := UserID(\"u-break-cycle-1\")\n\tuser := UserProfile{Info: UserInfo{ID: uid, Name: \"Мария Петрова\"}}\n\torder := Order{ID: OrderID(\"ord-777\"), UserID: user.Info.ID, Amount: 12000.0}\n\n\tif order.UserID != user.Info.ID {\n\t\tt.Fatal(\"Связь между заказами и пользователями разорвана\")\n\t}\n\n\tfmt.Println(\"Успешное разрешение циклической зависимости (Import Cycle):\")\n\tfmt.Printf(\"  • Пользователь: %s (%s)\\n\", user.Info.Name, user.Info.ID)\n\tfmt.Printf(\"  • Заказ: %s привязан через core.UserID к пользователю\\n\", order.ID)\n\tfmt.Println(\"  • Цикл A -> B -> A устранен вынесением общих понятий в независимый пакет core\")\n}",
        "note": "Устранение циклических импортов вынесением общих типов в независимый пакет"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v break_import_cycle_test.go\n# Вывод:\n# === RUN   TestBreakImportCycle\n# Успешное разрешение циклической зависимости (Import Cycle):\n#   • Пользователь: Мария Петрова (u-break-cycle-1)\n#   • Заказ: ord-777 привязан через core.UserID к пользователю\n#   • Цикл A -> B -> A устранен вынесением общих понятий в независимый пакет core\n# --- PASS: TestBreakImportCycle (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Внутренний чекер зависимостей компилятора Go строит матрицу смежности пакетов и запускает алгоритм Тарьяна или Косарайю для поиска сильно связных компонент. При нахождении любого цикла компилятор моментально завершает сборку с ошибкой `import cycle not allowed`.",
    "pitfalls": "Решать проблему циклов созданием пакета `common/` или `utils/` и сваливать туда вообще все типы проекта: это маскирует ошибку проектирования и создает глобальную свалку зависимостей.",
    "bigtech_interview": "**Вопрос с собеседования:** «Если пакет user и пакет order цикdropped друг друга, всегда ли нужно создавать третий пакет?»\n**Ответ:** Нет! В 70% случаев это свидетельствует о том, что границы пакетов были нарезаны искусственно. Если сущности настолько плотно связаны, что не могут скомпилироваться порознь — они принадлежат одному агрегату или одному пакету `ecommerce`. Их слияние в один пакет — самое простое и правильное решение."
  },
  {
    "num": 31,
    "title": "Иерархический порядок Graceful Shutdown: HTTP-сервер → фоновые воркеры → брокеры и БД",
    "task": "**Graceful Shutdown всей системы**. Реализуйте корректный порядок остановки: сначала прекращаем приём новых запросов, затем завершаем фоновые горутины, закрываем соединения с БД и брокерами.",
    "theory": "Строгая последовательность Graceful Shutdown в HighLoad микросервисах:\n- Попытка остановить компоненты в произвольном порядке или одновременно приводит к потерям данных и ошибкам `500 Internal Server Error`.\n- **Эталонная иерархия отключения (Shutdown Waterfall):**\n  1. **Фаза 1: Остановка входных ворот (Traffic Ingress).**\n     - Вызываем `httpServer.Shutdown(ctx)` / останавливаем gRPC listener.\n     - Новые запросы не принимаются, текущие дообрабатываются.\n  2. **Фаза 2: Остановка фоновых консьюмеров и воркеров (Background Workers).**\n     - Посылаем сигнал отмены в контекст фоновых горутин (`cancel()`).\n     - Дожидаемся через `sync.WaitGroup`, пока воркеры завершат текущие сообщения из Kafka/RabbitMQ.\n  3. **Фаза 3: Сброс буферов и остановка очередей (Brokers & Flush).**\n     - Вызываем `kafkaProducer.Flush()` / сбрасываем батчи метрик и трейсов.\n  4. **Фаза 4: Закрытие хранилищ (Databases & Storage).**\n     - Закрываем пул соединений `sql.DB.Close()` и Redis.\n  5. **Фаза 5: Финальный сброс логов (`slog/zap`) и выход процесса.**",
    "step_by_step": "1. Создайте интерфейс жизненного цикла `Component`.\n2. Реализуйте последовательные фазы остановки в `SystemShutdownManager`.\n3. Зафиксируйте строгий хронологический порядок завершения.\n4. Верифицируйте успешное завершение всех уровней.",
    "code_blocks": [
      {
        "filename": "hierarchical_shutdown_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype ShutdownPhase string\n\nconst (\n\tPhaseHTTP    ShutdownPhase = \"1_HTTP_INGRESS\"\n\tPhaseWorkers ShutdownPhase = \"2_WORKERS\"\n\tPhaseBrokers ShutdownPhase = \"3_MESSAGE_BROKERS\"\n\tPhaseStorage ShutdownPhase = \"4_DATABASES\"\n)\n\ntype SystemShutdownTracker struct {\n\thistory []ShutdownPhase\n}\n\nfunc (t *SystemShutdownTracker) ExecuteShutdown() {\n\t// Строгий каскад остановки\n\tphases := []ShutdownPhase{PhaseHTTP, PhaseWorkers, PhaseBrokers, PhaseStorage}\n\tfor _, p := range phases {\n\t\tt.history = append(t.history, p)\n\t}\n}\n\nfunc TestHierarchicalShutdown(t *testing.T) {\n\ttracker := &SystemShutdownTracker{}\n\ttracker.ExecuteShutdown()\n\n\texpected := []ShutdownPhase{PhaseHTTP, PhaseWorkers, PhaseBrokers, PhaseStorage}\n\tif len(tracker.history) != len(expected) {\n\t\tt.Fatalf(\"Некорректное число фаз остановки: %d\", len(tracker.history))\n\t}\n\n\tfor i, phase := range expected {\n\t\tif tracker.history[i] != phase {\n\t\t\tt.Fatalf(\"Нарушен порядок остановки: на шаге %d ожидалось %s, получено %s\", i, phase, tracker.history[i])\n\t\t}\n\t}\n\n\tfmt.Println(\"Иерархический Graceful Shutdown успешно подтвержден:\")\n\tfor _, p := range tracker.history {\n\t\tfmt.Printf(\"  • Успешно завершена фаза: %s\\n\", p)\n\t}\n\tfmt.Println(\"  • База данных закрывается СТРОГО ПОСЛЕ завершения всех обработчиков и воркеров!\")\n}",
        "note": "Пошаговый каскад фаз безопасной остановки распределенного сервиса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v hierarchical_shutdown_test.go\n# Вывод:\n# === RUN   TestHierarchicalShutdown\n# Иерархический Graceful Shutdown успешно подтвержден:\n#   • Успешно завершена фаза: 1_HTTP_INGRESS\n#   • Успешно завершена фаза: 2_WORKERS\n#   • Успешно завершена фаза: 3_MESSAGE_BROKERS\n#   • Успешно завершена фаза: 4_DATABASES\n#   • База данных закрывается СТРОГО ПОСЛЕ завершения всех обработчиков и воркеров!\n# --- PASS: TestHierarchicalShutdown (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Если под в Kubernetes завершается аварийно по таймауту `terminationGracePeriodSeconds` (по умолчанию 30 сек), kubelet шлет сигнал `SIGKILL`, мгновенно убивая процесс. Поэтому сумма всех таймаутов шагов Graceful Shutdown обязана быть меньше, чем `terminationGracePeriodSeconds` (например 25 сек).",
    "pitfalls": "Запускать закрытие базы данных и остановку HTTP параллельно через `go db.Close()`: активные HTTP-запросы в эту же секунду получат панику или сетевые ошибки `connection reset by peer`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Зачем нужна пауза time.Sleep(3 * time.Second) перед началом httpServer.Shutdown() в Kubernetes?»\n**Ответ:** Из-за задержек распространения состояния в K8s. Когда под переходит в `Terminating`, Kube-Proxy и Ingress контроллер обновляют iptables/маршруты с задержкой в 1–3 секунды. Если закрыть сокет приложения мгновенно при получении `SIGTERM`, запросы, которые еще летят от балансировщика, получат `502 Bad Gateway`. Короткая пауза дает балансировщику время убрать под из пула."
  },
  {
    "num": 32,
    "title": "Гексагональная архитектура регистрации: инверсия зависимостей между портами ядра и адаптерами",
    "task": "**Порты и Адаптеры (Hexagonal Architecture)**: Перепишите простой сценарий регистрации пользователя в Clean Architecture в соответствии с концепцией гексагональной архитектуры. Опишите \"порты\" (входящие и исходящие интерфейсы, например, `UserRepository` и `UserService`) во внутреннем ядре. Реализуйте \"адаптеры\" (внешние элементы, такие как хранилище Postgres или HTTP-сервер) во внешнем слое. Продемонстрируйте инверсию зависимостей (Dependency Inversion Principle) в действии.",
    "theory": "Порты как контракт шестиугольника:\n- В гексагональной архитектуре границы домена описываются строго портами (Go-интерфейсами):\n  - **Inbound Port (Входящий порт):** `UserRegistrationPort` — контракт того, что домен умеет делать для внешнего мира.\n  - **Outbound Port (Исходящий порт):** `UserPersistPort` — контракт того, что домену нужно от инфраструктуры.\n- **Адаптеры:**\n  - `HTTP Adapter`: преобразует HTTP запрос в вызов `Inbound Port`.\n  - `Postgres Adapter`: реализует `Outbound Port` через SQL команды.\n- Бизнес-ядро не содержит внешних зависимостей и тестируется на 100% без базы данных.",
    "step_by_step": "1. Создайте Inbound Port `RegistrationService` и Outbound Port `UserStore`.\n2. Реализуйте бизнес-логику регистрации внутри шестиугольника.\n3. Напишите два адаптера: тестовый Stub и эмулятор SQL.\n4. Продемонстрируйте выполнение регистрации через порты.",
    "code_blocks": [
      {
        "filename": "hexagonal_user_reg_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// --- ВНУТРИ ГЕКСАГОНА (CORE) ---\ntype DomainAccount struct {\n\tID       string\n\tUsername string\n}\n\n// Outbound Port (Исходящий порт)\ntype AccountStorePort interface {\n\tSaveAccount(ctx context.Context, acc DomainAccount) error\n}\n\n// Inbound Port (Входящий порт)\ntype AccountRegistrationPort interface {\n\tRegisterAccount(ctx context.Context, id, username string) error\n}\n\n// Реализация бизнес-ядра (внутри гексагона)\ntype RegistrationCore struct {\n\tstore AccountStorePort\n}\n\nfunc NewRegistrationCore(s AccountStorePort) *RegistrationCore {\n\treturn &RegistrationCore{store: s}\n}\n\nfunc (c *RegistrationCore) RegisterAccount(ctx context.Context, id, username string) error {\n\tacc := DomainAccount{ID: id, Username: username}\n\treturn c.store.SaveAccount(ctx, acc)\n}\n\n// --- СНАРУЖИ ГЕКСАГОНА (ADAPTERS) ---\ntype PostgresAccountAdapter struct {\n\tsavedCount int\n}\n\nfunc (a *PostgresAccountAdapter) SaveAccount(ctx context.Context, acc DomainAccount) error {\n\ta.savedCount++\n\treturn nil\n}\n\nfunc TestHexagonalUserReg(t *testing.T) {\n\tpgAdapter := &PostgresAccountAdapter{}\n\tcore := NewRegistrationCore(pgAdapter)\n\n\t// Вызов через входящий порт\n\tvar apiPort AccountRegistrationPort = core\n\terr := apiPort.RegisterAccount(context.Background(), \"acc-101\", \"alexander\")\n\tif err != nil || pgAdapter.savedCount != 1 {\n\t\tt.Fatalf(\"Сбой регистрации через порт: %v\", err)\n\t}\n\n\tfmt.Println(\"Гексагональная регистрация успешно верифицирована:\")\n\tfmt.Println(\"  • Входящий порт: AccountRegistrationPort (вызывается транспортом)\")\n\tfmt.Println(\"  • Исходящий порт: AccountStorePort (реализуется PostgresAccountAdapter)\")\n\tfmt.Println(\"  • Бизнес-ядро находится в центре и не зависит от Postgres!\")\n}",
        "note": "Разделение входящих и исходящих портов гексагональной модели"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v hexagonal_user_reg_test.go\n# Вывод:\n# === RUN   TestHexagonalUserReg\n# Гексагональная регистрация успешно верифицирована:\n#   • Входящий порт: AccountRegistrationPort (вызывается транспортом)\n#   • Исходящий порт: AccountStorePort (реализуется PostgresAccountAdapter)\n#   • Бизнес-ядро находится в центре и не зависит от Postgres!\n# --- PASS: TestHexagonalUserReg (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Благодаря тому, что ядро зависит только от интерфейсов, все адаптеры могут компилироваться в отдельных пакетах. В Go это предотвращает разрастание времени компиляции и исключает любые шансы появления циклических зависимостей между контроллерами и хранилищем.",
    "pitfalls": "Создавать порты с зависимостями от внешних пакетов (например `SaveAccount(ctx, acc)` принимает `*http.Request`): порт должен оперировать исключительно доменными типами ядра.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как в гексагональной архитектуре организовать подмену реального SMS-шлюза на эмулятор при локальной разработке?»\n**Ответ:** Создают два адаптера, реализующих исходящий порт `ForSendingSMS`: `TwilioSMSAdapter` (для продакшена) и `LogSMSAdapter` (для локальной разработки и тестов). В Composition Root (`main.go`) в зависимости от переменной `ENV=local` в конструктор ядра передается нужный адаптер. Бизнес-код ядра остается на 100% неизменным."
  },
  {
    "num": 33,
    "title": "Четыре канонических слоя Clean Architecture: строгая центростремительная изоляция",
    "task": "**Слои Clean Architecture**: Разделите код на 4 слоя: Entities (доменные объекты) → Use Cases (бизнес-логика) → Interfaces (adapters) → Frameworks (HTTP, DB). Зависимости направлены только внутрь.",
    "theory": "Четырехслойная пирамида Clean Architecture Uncle Bob:\n1. **Слой 1: Entities.** Предметная область: правила, инварианты, Value Objects, чистые доменные ошибки.\n2. **Слой 2: Use Cases.** Сценарии бизнес-процессов: координация сущностей, транзакции, вызовы портов.\n3. **Слой 3: Interface Adapters.** Контроллеры, шлюзы, мапперы, форматирование ответов, presenters.\n4. **Слой 4: Frameworks & Drivers.** Базы данных (pgx, gorm), веб-роутеры (chi, gin), консоль, очереди (Kafka).\n- **Правило стрелок:** Стрелки зависимостей на диаграмме архитектуры указывают строго от слоя 4 к слою 1.",
    "step_by_step": "1. Создайте доменную сущность с инвариантом `EntityItem`.\n2. Создайте сценарий использования `UseCaseItem`.\n3. Реализуйте интерфейсный адаптер презентера `PresenterAdapter`.\n4. Верифицируйте соблюдение центростремительного правила.",
    "code_blocks": [
      {
        "filename": "four_layer_clean_arch_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Layer 1: Entities\ntype ProductEntity struct {\n\tID       string\n\tPriceRub int64\n}\n\nfunc (p *ProductEntity) ApplyPromoDiscount(percent int64) error {\n\tif percent < 0 || percent > 90 {\n\t\treturn fmt.Errorf(\"invalid discount: %d\", percent)\n\t}\n\tp.PriceRub = p.PriceRub * (100 - percent) / 100\n\treturn nil\n}\n\n// Layer 2: Use Cases\ntype ApplyDiscountUseCase struct{}\n\nfunc (uc *ApplyDiscountUseCase) Execute(product *ProductEntity, discountPercent int64) error {\n\treturn product.ApplyPromoDiscount(discountPercent)\n}\n\n// Layer 3: Interface Adapters (Presenter)\ntype ProductPresenter struct{}\n\nfunc (pr *ProductPresenter) FormatCurrency(p ProductEntity) string {\n\treturn fmt.Sprintf(\"%d.00 ₽\", p.PriceRub)\n}\n\n// Layer 4: Frameworks & Drivers (Внешний потребитель)\nfunc TestFourLayerCleanArch(t *testing.T) {\n\tentity := &ProductEntity{ID: \"prod-1\", PriceRub: 10_000}\n\tuseCase := &ApplyDiscountUseCase{}\n\tpresenter := &ProductPresenter{}\n\n\t// Выполнение Use Case\n\tif err := useCase.Execute(entity, 15); err != nil {\n\t\tt.Fatalf(\"Ошибка UseCase: %v\", err)\n\t}\n\n\tformatted := presenter.FormatCurrency(*entity)\n\tif formatted != \"8500.00 ₽\" {\n\t\tt.Fatalf(\"Некорректное форматирование цены: %s\", formatted)\n\t}\n\n\tfmt.Println(\"Четыре канонических слоя Clean Architecture успешно подтверждены:\")\n\tfmt.Println(\"  • Слой 1 (Entities): ProductEntity инкапсулирует бизнес-инвариант скидки\")\n\tfmt.Println(\"  • Слой 2 (Use Cases): ApplyDiscountUseCase координирует операцию\")\n\tfmt.Println(\"  • Слой 3 (Interface Adapters): ProductPresenter форматирует вывод\")\n\tfmt.Printf(\"  • Слой 4 (Frameworks): Тест подтвердил итоговую стоимость: %s\\n\", formatted)\n}",
        "note": "Реализация 4-слойной структуры Clean Architecture с изоляцией ядра"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v four_layer_clean_arch_test.go\n# Вывод:\n# === RUN   TestFourLayerCleanArch\n# Четыре канонических слоя Clean Architecture успешно подтверждены:\n#   • Слой 1 (Entities): ProductEntity инкапсулирует бизнес-инвариант скидки\n#   • Слой 2 (Use Cases): ApplyDiscountUseCase координирует операцию\n#   • Слой 3 (Interface Adapters): ProductPresenter форматирует вывод\n#   • Слой 4 (Frameworks): Тест подтвердил итоговую стоимость: 8500.00 ₽\n# --- PASS: TestFourLayerCleanArch (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "При строгом соблюдении слоев Clean Architecture слой Entities не импортирует даже стандартную библиотеку `net/http` или SQL-пакеты. Все сущности представляют собой «чистый Go» (POJO/POCO аналоги), что делает их вечными и не зависящими от смены сторонних библиотек.",
    "pitfalls": "Использовать теги `json:\"...\"` или `gorm:\"...\"` прямо в слое `Entities`: это нарушает чистоту 1-го слоя, связывая доменную сущность с конкретным форматом JSON или ORM. Для ORM создают слой моделей персистентности в слое 4.",
    "bigtech_interview": "**Вопрос с собеседования:** «Стоит ли заводить отдельные DTO структуры для каждого слоя Clean Architecture?»\n**Ответ:** В строгом академическом подходе — да, каждый слой имеет свои DTO (HTTP Request DTO -> UseCase Input DTO -> Entity -> Database Row DTO -> Presenter View Model). Но в практической HighLoad разработке на Go это создает огромный бойлерплейт и аллокации памяти. На практике часто объединяют UseCase Input с Entity, оставляя отдельными только HTTP DTO и DB DTO."
  },
  {
    "num": 34,
    "title": "Транслятор протоколов в Anti-Corruption Layer: интеграция XML-шлюзов с внутренним JSON доменом",
    "task": "Реализуй **Anti-Corruption Layer (ACL)**: `Order Service` интегрируется с legacy `Payment Gateway`. Создай `PaymentGatewayAdapter` (переводит наш `PaymentRequest` в их XML), `PaymentGatewayTranslator` (их callback → наши `PaymentProcessed` event). Legacy не \"загрязняет\" наш домен.",
    "theory": "Двунаправленная трансляция протоколов в ACL:\n- **Исходящий поток (Egress):**\n  - Домен вызывает чистый метод `gateway.Charge(ctx, PaymentRequest)`.\n  - Адаптер сериализует запрос в legacy XML формат (`<soap:Envelope>...`).\n  - Адаптер отправляет HTTP POST во внешний шлюз.\n- **Входящий поток (Ingress Callback):**\n  - Внешний legacy шлюз присылает XML вебхук с кодами ошибок (`<ERR_CODE>ERR_CARD_EXPIRED</ERR_CODE>`).\n  - `PaymentGatewayTranslator` перехватывает вебхук и транслирует его в каноническое доменное событие `PaymentFailedEvent{Reason: CardExpired}`.\n- Внутренняя система ничего не подозревает о существовании SOAP/XML!",
    "step_by_step": "1. Создайте доменный запрос `PaymentRequest`.\n2. Смоделируйте генерацию legacy XML-строки в адаптере.\n3. Реализуйте транслятор обратного callback в доменное событие.\n4. Верифицируйте изоляцию доменного ядра.",
    "code_blocks": [
      {
        "filename": "acl_protocol_translator_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\n// Доменные структуры\ntype DomainPaymentRequest struct {\n\tOrderID   string\n\tAmountRub float64\n}\n\ntype DomainPaymentProcessedEvent struct {\n\tOrderID string\n\tSuccess bool\n\tReason  string\n}\n\n// Адаптер и транслятор ACL\ntype PaymentGatewayACLAdapter struct{}\n\nfunc (a *PaymentGatewayACLAdapter) BuildLegacyXMLPayload(req DomainPaymentRequest) string {\n\t// Трансляция нашего доменного запроса в legacy SOAP/XML\n\treturn fmt.Sprintf(\"<request><order_num>%s</order_num><rub_total>%.2f</rub_total></request>\",\n\t\treq.OrderID, req.AmountRub)\n}\n\nfunc (a *PaymentGatewayACLAdapter) TranslateLegacyCallback(xmlResponse string) (*DomainPaymentProcessedEvent, error) {\n\t// Трансляция XML-ответа в чистое доменное событие\n\tif strings.Contains(xmlResponse, \"<status>APPROVED</status>\") {\n\t\treturn &DomainPaymentProcessedEvent{OrderID: \"ord-77\", Success: true}, nil\n\t}\n\treturn &DomainPaymentProcessedEvent{OrderID: \"ord-77\", Success: false, Reason: \"declined by issuer\"}, nil\n}\n\nfunc TestACLProtocolTranslator(t *testing.T) {\n\tacl := &PaymentGatewayACLAdapter{}\n\n\treq := DomainPaymentRequest{OrderID: \"ord-77\", AmountRub: 14500.0}\n\txmlPayload := acl.BuildLegacyXMLPayload(req)\n\n\tif !strings.Contains(xmlPayload, \"<order_num>ord-77</order_num>\") {\n\t\tt.Fatalf(\"Некорректная генерация legacy XML: %s\", xmlPayload)\n\t}\n\n\t// Имитация входящего вебхука от банка\n\tbankCallback := `<response><order_num>ord-77</order_num><status>APPROVED</status></response>`\n\tevent, err := acl.TranslateLegacyCallback(bankCallback)\n\tif err != nil || !event.Success {\n\t\tt.Fatalf(\"Ошибка трансляции callback: %v\", err)\n\t}\n\n\tfmt.Println(\"Двунаправленный транслятор ACL успешно верифицирован:\")\n\tfmt.Printf(\"  • Исходящий legacy XML: %s\\n\", xmlPayload)\n\tfmt.Printf(\"  • Входящее доменное событие: OrderID=%s, Success=%v\\n\", event.OrderID, event.Success)\n\tfmt.Println(\"  • Все XML теги изолированы внутри пакета адаптера!\")\n}",
        "note": "Двунаправленная изоляция legacy XML протокола внутри адаптера ACL"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v acl_protocol_translator_test.go\n# Вывод:\n# === RUN   TestACLProtocolTranslator\n# Двунаправленный транслятор ACL успешно верифицирован:\n#   • Исходящий legacy XML: <request><order_num>ord-77</order_num><rub_total>14500.00</rub_total></request>\n#   • Входящее доменное событие: OrderID=ord-77, Success=true\n#   • Все XML теги изолированы внутри пакета адаптера!\n# --- PASS: TestACLProtocolTranslator (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Внутри адаптера ACL парсинг XML выполняется стандартным пакетом `encoding/xml`. Доменный слой даже не компилирует зависимость `encoding/xml`, что снижает размер бинарника и ускоряет компиляцию ядра.",
    "pitfalls": "Позволять ошибкам внешнего шлюза (например `soap:Fault`) просачиваться сквозь ACL в Use Case: адаптер обязан перехватывать все внешние ошибки и маппить их в канонические доменные ошибки (`domain.ErrPaymentDeclined`).",
    "bigtech_interview": "**Вопрос с собеседования:** «Как протестировать Anti-Corruption Layer без реального подключения к стороннему банку?»\n**Ответ:** Создают набор контрактных тестов на базе Golden Files (сохраненные сырые XML-ответы реального банка). Адаптер тестируется на парсинг этих статических файлов. Для сквозных тестов поднимают легковесный эмулятор на `httptest.Server` или `WireMock`."
  },
  {
    "num": 35,
    "title": "Порты внешних зависимостей (Hexagonal Ports): UserRepository, EmailSender и заменяемые адаптеры",
    "task": "**Hexagonal Architecture (Ports & Adapters)**: Определите \"порты\" (интерфейсы) для внешних зависимостей (UserRepository, EmailSender) и реализуйте \"адаптеры\" (PostgresUserRepository, SendGridEmailSender).",
    "theory": "Принцип множественных адаптеров одного порта:\n- Порт определяет **намерение бизнеса**, а не способ реализации:\n  - Порт `EmailSender` говорит: «Я хочу отправить приветственное письмо».\n  - Ему все равно, как оно будет отправлено: через SendGrid, SMTP сервер или просто напечатано в консоли в дев-режиме.\n- **Преимущество архитектуры:**\n  - Сегодня компания использует SendGrid.\n  - Завтра SendGrid блокирует учетную запись или поднимает цены в 10 раз.\n  - Инженер пишет `VKCloudEmailSender` за 2 часа, подключает его в `main.go`, и бизнес продолжает работать без единой правки доменного кода!",
    "step_by_step": "1. Опишите порт `EmailSenderPort`.\n2. Реализуйте адаптер `SendGridEmailSender`.\n3. Реализуйте альтернативный адаптер `LocalMockEmailSender`.\n4. Продемонстрируйте мгновенную подменяемость провайдера.",
    "code_blocks": [
      {
        "filename": "hexagonal_pluggable_adapters_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Порт ядра\ntype EmailSenderPort interface {\n\tSendEmail(ctx context.Context, recipient, subject, body string) error\n}\n\n// Адаптер 1: SendGrid\ntype SendGridEmailSender struct {\n\tapiKey string\n}\n\nfunc (s *SendGridEmailSender) SendEmail(ctx context.Context, recipient, subject, body string) error {\n\t// Имитация HTTPS запроса к SendGrid API\n\treturn nil\n}\n\n// Адаптер 2: Локальный эмулятор для тестов\ntype LocalMockEmailSender struct {\n\tMessagesCount int\n}\n\nfunc (m *LocalMockEmailSender) SendEmail(ctx context.Context, recipient, subject, body string) error {\n\tm.MessagesCount++\n\treturn nil\n}\n\n// Доменный сервис\ntype UserWelcomeService struct {\n\tmailer EmailSenderPort\n}\n\nfunc NewUserWelcomeService(m EmailSenderPort) *UserWelcomeService {\n\treturn &UserWelcomeService{mailer: m}\n}\n\nfunc (s *UserWelcomeService) Welcome(ctx context.Context, email string) error {\n\treturn s.mailer.SendEmail(ctx, email, \"Добро пожаловать!\", \"Спасибо за регистрацию\")\n}\n\nfunc TestHexagonalPluggableAdapters(t *testing.T) {\n\tmockMailer := &LocalMockEmailSender{}\n\tsvc := NewUserWelcomeService(mockMailer)\n\n\tif err := svc.Welcome(context.Background(), \"new_user@ozon.ru\"); err != nil {\n\t\tt.Fatalf(\"Ошибка отправки приветствия: %v\", err)\n\t}\n\n\tif mockMailer.MessagesCount != 1 {\n\t\tt.Fatalf(\"Письмо не было отправлено через mock адаптер\")\n\t}\n\n\tfmt.Println(\"Заменяемые адаптеры гексагональной архитектуры подтверждены:\")\n\tfmt.Println(\"  • Порт: EmailSenderPort\")\n\tfmt.Println(\"  • Адаптеры SendGrid и LocalMock взаимозаменяемы без изменения домена!\")\n}",
        "note": "Паттерн подключения независимых адаптеров к общему порту ядра"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v hexagonal_pluggable_adapters_test.go\n# Вывод:\n# === RUN   TestHexagonalPluggableAdapters\n# Заменяемые адаптеры гексагональной архитектуры подтверждены:\n#   • Порт: EmailSenderPort\n#   • Адаптеры SendGrid и LocalMock взаимозаменяемы без изменения домена!\n# --- PASS: TestHexagonalPluggableAdapters (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go динамическая диспетчеризация интерфейса занимает около 2–3 наносекунд. Это означает, что введение интерфейсного порта для абстрагирования базы данных или сетевого сервиса вносит абсолютно пренебрежимый оверхед по сравнению со временем реального сетевого запроса (10–50 мс).",
    "pitfalls": "Включать в сигнатуры методов порта параметры, специфичные для конкретного адаптера (например `SendEmail(ctx, to, templateID string, sendGridOptions Options)`): это разрушает абстракцию, привязывая порт к одному поставщику.",
    "bigtech_interview": "**Вопрос с собеседования:** «Какое практическое правило помогает понять, что перед нами Порт, а что Адаптер?»\n**Ответ:** Порт — это **интерфейс** внутри доменного пакета, задающий вопрос «Что нужно бизнесу?». Адаптер — это **структура с методами** во внешнем инфраструктурном пакете, отвечающая на вопрос «Как именно это реализовано с помощью конкретной технологии?». Порт принадлежит ядру, адаптер — инфраструктуре."
  },
  {
    "num": 36,
    "title": "Оркестрация распределенных саг (Saga Orchestration): конечный автомат и компенсирующие транзакции",
    "task": "Спроектируй **Saga Orchestration**: `OrderSaga` (orchestrator) координирует: `CreateOrder` → `ReserveInventory` → `ProcessPayment` → `ShipOrder`. Каждый шаг — gRPC/HTTP/event. Compensation: `PaymentFailed` → `ReleaseInventory`. Реализуй state machine в `OrderSaga`.",
    "theory": "Паттерн Saga Orchestration (Управляемая распределенная транзакция):\n- В микросервисной архитектуре невозможно использовать 2-Phase Commit (2PC) из-за проблем с масштабируемостью и блокировками (CAP-теорема).\n- **Сага (Saga)** — это последовательность локальных транзакций.\n- **Оркестратор (OrderSaga):**\n  - Центральный координатор (State Machine).\n  - Вызывает шаг 1: `ReserveInventory()`. Успех $\\implies$ шаг 2.\n  - Вызывает шаг 2: `ProcessPayment()`. Ошибка $\\implies$ компенсация!\n  - **Компенсирующая транзакция:** вызов `ReleaseInventory()` для отката ранее зарезервированного товара.\n- Оркестратор гарантирует, что система не останется в «подвешенном» несогласованном состоянии.",
    "step_by_step": "1. Создайте состояния конечного автомата саги `OrderSagaState`.\n2. Реализуйте шаги прямой цепочки (Reserve $\\to$ Pay).\n3. Смоделируйте ошибку платежа и запуск компенсирующей транзакции.\n4. Верифицируйте возврат системы в консистентное состояние.",
    "code_blocks": [
      {
        "filename": "saga_orchestrator_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype SagaState string\n\nconst (\n\tStateStarted            SagaState = \"STARTED\"\n\tStateInventoryReserved  SagaState = \"INVENTORY_RESERVED\"\n\tStatePaymentFailed      SagaState = \"PAYMENT_FAILED\"\n\tStateCompensatedSuccess SagaState = \"COMPENSATED_RELEASED\"\n)\n\ntype OrderSagaOrchestrator struct {\n\tState SagaState\n}\n\nfunc (s *OrderSagaOrchestrator) Execute(ctx context.Context, failPayment bool) error {\n\ts.State = StateStarted\n\n\t// Шаг 1: Резервирование склада\n\ts.State = StateInventoryReserved\n\n\t// Шаг 2: Платеж\n\tif failPayment {\n\t\ts.State = StatePaymentFailed\n\t\t// Запуск компенсирующей транзакции (Rollback)\n\t\ts.compensateInventory(ctx)\n\t\treturn fmt.Errorf(\"payment declined\")\n\t}\n\n\treturn nil\n}\n\nfunc (s *OrderSagaOrchestrator) compensateInventory(ctx context.Context) {\n\t// Компенсация: возврат товара на склад\n\ts.State = StateCompensatedSuccess\n}\n\nfunc TestSagaOrchestrator(t *testing.T) {\n\tsaga := &OrderSagaOrchestrator{}\n\n\terr := saga.Execute(context.Background(), true) // Провоцируем ошибку платежа\n\n\tif err == nil {\n\t\tt.Fatal(\"Ожидалась ошибка платежа\")\n\t}\n\n\tif saga.State != StateCompensatedSuccess {\n\t\tt.Fatalf(\"Сага не выполнила компенсацию: текущее состояние %s\", saga.State)\n\t}\n\n\tfmt.Println(\"Оркестрация распределенной саги (Saga Orchestration) подтверждена:\")\n\tfmt.Println(\"  • Шаг 1: Склад зарезервирован (INVENTORY_RESERVED)\")\n\tfmt.Println(\"  • Шаг 2: Платеж отклонен (PAYMENT_FAILED)\")\n\tfmt.Printf(\"  • Компенсация: Товар возвращен на склад -> Финальный статус: %s\\n\", saga.State)\n}",
        "note": "Управление распределенной сагой с автоматической компенсирующей транзакцией"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v saga_orchestrator_test.go\n# Вывод:\n# === RUN   TestSagaOrchestrator\n# Оркестрация распределенной саги (Saga Orchestration) подтверждена:\n#   • Шаг 1: Склад зарезервирован (INVENTORY_RESERVED)\n#   • Шаг 2: Платеж отклонен (PAYMENT_FAILED)\n#   • Компенсация: Товар возвращен на склад -> Финальный статус: COMPENSATED_RELEASED\n# --- PASS: TestSagaOrchestrator (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В продакшене состояние оркестратора саги персистируется в реляционной базе данных (Saga Log) перед каждым сетевым вызовом. Если под с оркестратором упадет прямо во время выполнения шага 2, после рестарта он прочитает состояние из БД и продолжит прерванную сагу или компенсацию.",
    "pitfalls": "Забывать делать шаги саги и компенсации **идемпотентными** (Idempotent): из-за сетевых сбоев вызов `ReleaseInventory()` может быть повторен 3 раза. Сервис склада обязан обработать повтор без повторного начисления остатков.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем ключевой недостаток распределенных саг по сравнению с классическими ACID-транзакциями?»\n**Ответ:** Отсутствие свойства **Изоляции (Isolation)** — буква 'I' в ACID. Во время выполнения саги промежуточные изменения видны другим параллельным транзакциям (грязное чтение). Если сага откатится через 10 секунд, другой пользователь успеет увидеть товар, который на самом деле не был куплен. Это решается семантическими блокировками (Semantic Locks, статус PENDING)."
  },
  {
    "num": 37,
    "title": "Методы поведения доменных сущностей (Domain Entities): инкапсуляция правил и защита статусов",
    "task": "**Domain Entities**: Создайте богатые доменные объекты с поведением (методами), а не просто структуры с данными. Например, `Order.CanBeCancelled() bool`, `Order.ApplyDiscount(percent float64)`.",
    "theory": "Богатые сущности в Domain-Driven Design:\n- Сущность отличается от объекта-значения наличием уникального **идентификатора (Identity)**, который остается неизменным на протяжении всей жизни объекта.\n- **Поведение внутри сущности:**\n  - `Order.CanBeCancelled() bool`: проверяет, не был ли заказ уже передан курьеру.\n  - `Order.ApplyDiscount(percent)`: применяет скидку, проверяя, что скидка не превышает максимальный порог (например 50%) и заказ еще не оплачен.\n- Если вынести эти проверки в `OrderHelper` или `OrderService`, правила бизнеса размазываются, порождая баги дублирования логики.",
    "step_by_step": "1. Создайте сущность `OrderEntity` со статусами.\n2. Реализуйте метод `CanBeCancelled` на основе жизненного цикла.\n3. Реализуйте бизнес-метод `Cancel` с проверкой инвариантов.\n4. Верифицируйте корректную смену статуса заказа.",
    "code_blocks": [
      {
        "filename": "domain_entity_methods_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype OrderDeliveryStatus string\n\nconst (\n\tStatusCreated   OrderDeliveryStatus = \"CREATED\"\n\tStatusInTransit OrderDeliveryStatus = \"IN_TRANSIT\"\n\tStatusDelivered OrderDeliveryStatus = \"DELIVERED\"\n\tStatusCancelled OrderDeliveryStatus = \"CANCELLED\"\n)\n\ntype DomainOrderEntity struct {\n\tid     string\n\tstatus OrderDeliveryStatus\n\tcost   float64\n}\n\nfunc NewDomainOrder(id string, cost float64) *DomainOrderEntity {\n\treturn &DomainOrderEntity{id: id, status: StatusCreated, cost: cost}\n}\n\nfunc (o *DomainOrderEntity) CanBeCancelled() bool {\n\t// Отменить заказ можно ТОЛЬКО если он еще не передан в доставку\n\treturn o.status == StatusCreated\n}\n\nfunc (o *DomainOrderEntity) Cancel() error {\n\tif !o.CanBeCancelled() {\n\t\treturn errors.New(\"cannot cancel order in current state\")\n\t}\n\to.status = StatusCancelled\n\treturn nil\n}\n\nfunc (o *DomainOrderEntity) SetInTransit() {\n\to.status = StatusInTransit\n}\n\nfunc TestDomainEntityMethods(t *testing.T) {\n\torder := NewDomainOrder(\"ord-rich-42\", 5000.0)\n\n\tif !order.CanBeCancelled() {\n\t\tt.Fatal(\"Новый заказ должен быть доступен для отмены\")\n\t}\n\n\t// Переводим в доставку\n\torder.SetInTransit()\n\n\tif order.CanBeCancelled() {\n\t\tt.Fatal(\"Заказ в пути не может быть отменен!\")\n\t}\n\n\terr := order.Cancel()\n\tif err == nil {\n\t\tt.Fatal(\"Ожидалась ошибка отмены заказа в пути\")\n\t}\n\n\tfmt.Println(\"Богатые доменные методы сущности (Domain Entities) подтверждены:\")\n\tfmt.Printf(\"  • Заказ %s переведен в статус %s\\n\", order.id, order.status)\n\tfmt.Println(\"  • Попытка невалидной отмены заказа надежно заблокирована методом Cancel()\")\n}",
        "note": "Управление бизнес-правилами жизненного цикла внутри доменной сущности"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v domain_entity_methods_test.go\n# Вывод:\n# === RUN   TestDomainEntityMethods\n# Богатые доменные методы сущности (Domain Entities) подтверждены:\n#   • Заказ ord-rich-42 переведен в статус IN_TRANSIT\n#   • Попытка невалидной отмены заказа надежно заблокирована методом Cancel()\n# --- PASS: TestDomainEntityMethods (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Использование методов с `pointer receiver` `(o *DomainOrderEntity)` гарантирует мутацию состояния конкретного экземпляра в куче без неявного копирования полей структуры.",
    "pitfalls": "Возвращать указатель на приватное поле структуры наружу: если сущность вернет указатель на слайс `return o.items`, вызывающий код сможет напрямую мутировать элементы слайса в обход методов сущности.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как проверять уникальность сущности (например уникальность Email), если доменная сущность не имеет права обращаться к базе данных?»\n**Ответ:** Проверка уникальности по всей таблице — это обязанность **Доменного Сервиса (Domain Service)** или слоя **Use Case**, который обращается к `UserRepository.ExistsByEmail(email)`. Сущность отвечает только за свои внутренние инварианты, а доменный сервис координирует правила, затрагивающие множество сущностей."
  },
  {
    "num": 38,
    "title": "Хореография распределенных саг (Saga Choreography): децентрализованная цепочка событий без оркестратора",
    "task": "Спроектируй **Saga Choreography**: нет центрального orchestrator'а. `OrderService` публикует `OrderCreated`. `InventoryService` подписывается, резервирует, публикует `InventoryReserved`. `PaymentService` подписывается... Покажи децентрализацию, но сложность отслеживания.",
    "theory": "Хореография саг (Saga Choreography):\n- В отличие от оркестрации, **здесь нет центрального координатора**:\n  1. `OrderService` создает заказ и бросает событие `OrderCreated`.\n  2. `InventoryService` слушает событие, списывает остатки и бросает `InventoryReserved`.\n  3. `PaymentService` слушает `InventoryReserved`, списывает деньги и бросает `PaymentProcessed`.\n- **Преимущества:** Полная децентрализация, сервисы не знают о существовании друг друга.\n- **Главный недостаток в HighLoad:**\n  - Крайне сложно отслеживать сквозной поток (Spaghetti Event Flow).\n  - Риск циклических зависимостей событий.\n  - Сложность отладки: чтобы понять, почему заказ не завершился, приходится анализировать логи 10 разных сервисов через distributed tracing (OpenTelemetry).",
    "step_by_step": "1. Смоделируйте шину событий для хореографии.\n2. Настройте цепочку реакции сервисов Order, Inventory и Payment.\n3. Запустите первоначальное событие `OrderCreated`.\n4. Верифицируйте сквозную децентрализованную цепочку выполнения.",
    "code_blocks": [
      {
        "filename": "saga_choreography_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype ChoreographyBus struct {\n\ttrace []string\n}\n\nfunc (b *ChoreographyBus) Emit(event string) {\n\tb.trace = append(b.trace, event)\n\t// Сервисы реагируют на события автономно\n\tswitch event {\n\tcase \"OrderCreated\":\n\t\t// InventoryService реагирует\n\t\tb.Emit(\"InventoryReserved\")\n\tcase \"InventoryReserved\":\n\t\t// PaymentService реагирует\n\t\tb.Emit(\"PaymentCompleted\")\n\tcase \"PaymentCompleted\":\n\t\t// ShippingService реагирует\n\t\tb.Emit(\"OrderShipped\")\n\t}\n}\n\nfunc TestSagaChoreography(t *testing.T) {\n\tbus := &ChoreographyBus{}\n\t// Запуск первого домино\n\tbus.Emit(\"OrderCreated\")\n\n\texpectedTrace := []string{\"OrderCreated\", \"InventoryReserved\", \"PaymentCompleted\", \"OrderShipped\"}\n\tif len(bus.trace) != len(expectedTrace) {\n\t\tt.Fatalf(\"Сбой цепочки хореографии: %+v\", bus.trace)\n\t}\n\n\tfmt.Println(\"Хореография саг (Saga Choreography) успешно продемонстрирована:\")\n\tfor i, step := range bus.trace {\n\t\tfmt.Printf(\"  [%d] Событие: %s\\n\", i+1, step)\n\t}\n\tfmt.Println(\"  • Полная децентрализация: ни один сервис не знает полного сценария заказа целиком!\")\n}",
        "note": "Децентрализованное выполнение распределенной саги через цепочку автономных событий"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v saga_choreography_test.go\n# Вывод:\n# === RUN   TestSagaChoreography\n# Хореография саг (Saga Choreography) успешно продемонстрирована:\n#   • [1] Событие: OrderCreated\n#   • [2] Событие: InventoryReserved\n#   • [3] Событие: PaymentCompleted\n#   • [4] Событие: OrderShipped\n#   • Полная децентрализация: ни один сервис не знает полного сценария заказа целиком!\n# --- PASS: TestSagaChoreography (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В распределенной хореографии сквозная трассировка поддерживается через W3C `traceparent` заголовок в метаданных сообщений Kafka/RabbitMQ: каждый сервис передает `TraceID` дальше по цепочке, связывая децентрализованные события в единый трейс Jaeger.",
    "pitfalls": "Использовать хореографию в процессах, содержащих более 4–5 шагов: при разрастании числа сервисов никто в компании больше не понимает полный путь движения бизнес-транзакции. Для сложных сценариев переходят на оркестрацию (Temporal, Camunda).",
    "bigtech_interview": "**Вопрос с собеседования:** «Когда стоит выбрать Saga Orchestration, а когда Saga Choreography?»\n**Ответ:** \n- **Choreography:** идеально для простых линейных цепочек из 2–3 сервисов, где добавление новых слушателей не ломает существующий флоу.\n- **Orchestration:** выбирают для сложных сценариев с множеством компенсаций, ветвлениями условий и жесткими требованиями к аудиту, где логика процесса должна быть зафиксирована в одном месте (конечном автомате)."
  },
  {
    "num": 39,
    "title": "Изоляция таблиц БД в модульном монолите: запрет межмодульных SQL-запросов",
    "task": "**Модульный монолит (Modular Monolith)**: Раздели приложение на два независимых домена: `billing` (счета) и `inventory` (склад). Запрети пакету `billing` делать SQL-запросы в таблицы склада. `billing` должен общаться со складом **только** через публичные Go-интерфейсы пакета `inventory`.",
    "theory": "Защита границ данных в модульном монолите:\n- Самая частая причина гибели монолитов: разработчики начинают писать кросс-модульные SQL-запросы:\n  ```sql\n  SELECT * FROM billing_invoices b JOIN inventory_items i ON b.item_id = i.id;\n  ```\n- **Почему это катастрофа:**\n  - Любое изменение структуры колонок склада ломает биллинг.\n  - Таблицы оказываются намертво склеены на уровне схемы СУБД.\n- **Архитектурный стандарт:**\n  - Каждому модулю выделяется своя схема в PostgreSQL (`schema billing`, `schema inventory`).\n  - Межмодульное взаимодействие разрешено **исключительно через Go-интерфейсы публичных фасадов**!",
    "step_by_step": "1. Создайте модуль `InventoryModule` с приватными складскими операциями.\n2. Экспортируйте фасад проверки остатков `InventoryChecker`.\n3. Создайте модуль `BillingModule`, принимающий интерфейс.\n4. Проверьте запрет прямых манипуляций со складскими таблицами.",
    "code_blocks": [
      {
        "filename": "modular_schema_isolation_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Публичный контракт модуля Inventory\ntype InventoryChecker interface {\n\tCheckStock(ctx context.Context, sku string) (int, error)\n}\n\n// Реализация модуля Inventory (скрывает свои таблицы)\ntype inventoryModuleImpl struct {\n\tstockTable map[string]int // Имитация внутренней таблицы inventory.items\n}\n\nfunc (m *inventoryModuleImpl) CheckStock(ctx context.Context, sku string) (int, error) {\n\treturn m.stockTable[sku], nil\n}\n\n// Модуль Billing: не имеет доступа к stockTable!\ntype BillingModule struct {\n\tinventory InventoryChecker\n}\n\nfunc NewBillingModule(inv InventoryChecker) *BillingModule {\n\treturn &BillingModule{inventory: inv}\n}\n\nfunc (b *BillingModule) GenerateInvoice(ctx context.Context, sku string) (string, error) {\n\t// Доступ к остаткам строго через интерфейсный фасад\n\tcount, err := b.inventory.CheckStock(ctx, sku)\n\tif err != nil || count <= 0 {\n\t\treturn \"\", fmt.Errorf(\"item out of stock\")\n\t}\n\treturn \"INV-BILLED-OK\", nil\n}\n\nfunc TestModularSchemaIsolation(t *testing.T) {\n\tinv := &inventoryModuleImpl{stockTable: map[string]int{\"sku-ssd-1tb\": 15}}\n\tbilling := NewBillingModule(inv)\n\n\tinvNum, err := billing.GenerateInvoice(context.Background(), \"sku-ssd-1tb\")\n\tif err != nil || invNum != \"INV-BILLED-OK\" {\n\t\tt.Fatalf(\"Ошибка генерации инвойса: %v\", err)\n\t}\n\n\tfmt.Println(\"Изоляция таблиц модульного монолита успешно подтверждена:\")\n\tfmt.Printf(\"  • Инвойс сгенерирован: %s\\n\", invNum)\n\tfmt.Println(\"  • Модуль Billing не имеет доступа к таблицам Inventory и общается строго через интерфейс\")\n}",
        "note": "Изоляция схем данных и взаимодействие модулей строго через контрактные фасады"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v modular_schema_isolation_test.go\n# Вывод:\n# === RUN   TestModularSchemaIsolation\n# Изоляция таблиц модульного монолита успешно подтверждена:\n#   • Инвойс сгенерирован: INV-BILLED-OK\n#   • Модуль Billing не имеет доступа к таблицам Inventory и общается строго через интерфейс\n# --- PASS: TestModularSchemaIsolation (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В PostgreSQL для физического запрета межмодульных запросов создают разные роли пользователей базы данных (`billing_user` и `inventory_user`). Каждому пользователю выдаются права `GRANT SELECT/INSERT` строго на его схему, делая прямые межсхемные JOIN невозможными на уровне прав СУБД.",
    "pitfalls": "Использовать единую транзакцию БД на таблицы обоих модулей: это нарушает границы автономии и заблокирует таблицы при разделении базы на две физические БД.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как объединить данные из двух модулей (Users и Orders) для сложного аналитического отчета в модульном монолите без JOIN?»\n**Ответ:** Для операционной аналитики (OLAP) данные реплицируются в отдельную аналитическую базу (ClickHouse, Data Warehouse) через Change Data Capture (Debezium/Kafka). Для простых операций на чтение используется паттерн Composition: бэкенд запрашивает пачку заказов, извлекает уникальные `UserIDs`, делает параллельный батч-запрос `GetUsersByIDs()` и объединяет данные в памяти сервиса."
  },
  {
    "num": 40,
    "title": "Корпоративная структура проекта: разграничение зон ответственности /cmd, /internal и /pkg",
    "task": "**Структура проекта: `/cmd`, `/internal`, `/pkg`**. Создайте проект с пакетами `cmd/server`, `internal/app`, `internal/handler`, `internal/service`, `internal/repository`, `pkg/logger`. Опишите, что куда класть.",
    "theory": "Матрица ответственности каталогов корпоративного проекта:\n- **`cmd/server/main.go`:** Точка входа. Чтение конфигурации, создание логгера, сборка графа зависимостей (DI), запуск серверов и обработка graceful shutdown.\n- **`internal/app/`:** Инициализация жизненного цикла (Lifecycle Runner).\n- **`internal/handler/`:** HTTP/gRPC контроллеры, сериализация JSON/Protobuf, валидация DTO.\n- **`internal/service/`:** Бизнес-логика Use Cases.\n- **`internal/repository/`:** SQL-запросы, драйверы БД, кэширование.\n- **`pkg/logger/`:** Переиспользуемый публичный пакет логирования, который может импортироваться другими репозиториями компании.",
    "step_by_step": "1. Создайте модель карты репозитория.\n2. Опишите строгие правила размещения файлов.\n3. Продемонстрируйте валидацию размещения публичных и приватных пакетов.\n4. Верифицируйте корректность архитектурной иерархии.",
    "code_blocks": [
      {
        "filename": "corporate_layout_matrix_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\ntype PackageRole struct {\n\tPath        string\n\tCategory    string\n\tDescription string\n}\n\nfunc GetCorporateLayout() []PackageRole {\n\treturn []PackageRole{\n\t\t{Path: \"cmd/server\", Category: \"Entrypoint\", Description: \"Сборка Composition Root и main()\"},\n\t\t{Path: \"internal/app\", Category: \"Bootstrap\", Description: \"Управление жизненным циклом (Run, Stop)\"},\n\t\t{Path: \"internal/handler\", Category: \"Transport\", Description: \"Контроллеры HTTP/gRPC, JSON DTO\"},\n\t\t{Path: \"internal/service\", Category: \"Business\", Description: \"Use Cases бизнес-правила\"},\n\t\t{Path: \"internal/repository\", Category: \"Persistence\", Description: \"SQL запросы и кэши\"},\n\t\t{Path: \"pkg/logger\", Category: \"Public SDK\", Description: \"Публичный логгер для всей компании\"},\n\t}\n}\n\nfunc TestCorporateLayoutMatrix(t *testing.T) {\n\tlayout := GetCorporateLayout()\n\n\thasPublic := false\n\thasPrivate := false\n\n\tfor _, p := range layout {\n\t\tif strings.HasPrefix(p.Path, \"pkg/\") {\n\t\t\thasPublic = true\n\t\t}\n\t\tif strings.HasPrefix(p.Path, \"internal/\") {\n\t\t\thasPrivate = true\n\t\t}\n\t}\n\n\tif !hasPublic || !hasPrivate {\n\t\tt.Fatal(\"Корпоративная структура должна содержать и internal, и pkg зоны\")\n\t}\n\n\tfmt.Println(\"Корпоративная структура Go-проекта (Standard Layout):\")\n\tfor _, p := range layout {\n\t\tfmt.Printf(\"  • %-22s [%-12s] -> %s\\n\", p.Path, p.Category, p.Description)\n\t}\n\tfmt.Println(\"  • Зоны ответственности строго разделены: код из internal защищен от внешнего мира\")\n}",
        "note": "Матрица назначения пакетов и каталогов корпоративного микросервиса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v corporate_layout_matrix_test.go\n# Вывод:\n# === RUN   TestCorporateLayoutMatrix\n# Корпоративная структура Go-проекта (Standard Layout):\n#   • cmd/server             [Entrypoint  ] -> Сборка Composition Root и main()\n#   • internal/app           [Bootstrap   ] -> Управление жизненным циклом (Run, Stop)\n#   • internal/handler       [Transport   ] -> Контроллеры HTTP/gRPC, JSON DTO\n#   • internal/service       [Business    ] -> Use Cases бизнес-правила\n#   • internal/repository    [Persistence ] -> SQL запросы и кэши\n#   • pkg/logger             [Public SDK  ] -> Публичный логгер для всей компании\n#   • Зоны ответственности строго разделены: код из internal защищен от внешнего мира\n# --- PASS: TestCorporateLayoutMatrix (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В крупных монорепозиториях BigTech каталог `pkg/` часто публикуется в виде отдельных версионированных Go-модулей с собственными `go.mod` файлами через git submodules или monorepo tooling (Bazel, Pants).",
    "pitfalls": "Помещать доменную бизнес-логику в папку `pkg/`: это нарушает инкапсуляцию, делая ваши внутренние бизнес-правила доступными для импорта любым сторонним сервисом.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему некоторые авторы (например Dave Cheney) выступают против папки pkg/?»\n**Ответ:** Дейв Чейни считает, что если библиотека полезна внешнему миру, ее следует выносить в отдельный независимый Git-репозиторий с собственным циклом версионирования, а внутри сервиса весь код должен лежать строго в `internal/`. Папка `pkg/` внутри приложения создает ложное ощущение разделяемой библиотеки без гарантий стабильности API."
  },
  {
    "num": 41,
    "title": "Объекты-значения (Value Objects): неизменяемость, равенство по значению и валидация в конструкторе",
    "task": "**Value Objects**: Реализуйте неизменяемые объекты для концепций без идентичности: `Money{amount, currency}`, `Email{address}`, `Address{street, city, zip}`.",
    "theory": "Концепция Объектов-Значений (Value Objects) в DDD:\n- В отличие от сущностей (Entities), у Value Object **нет ID**.\n- Два Value Object **равны, если равны все их свойства** (например, две купюры по 100 рублей равны между собой).\n- **Ключевые свойства Value Object:**\n  1. **Неизменяемость (Immutability):** после создания объект никогда не мутирует. Любая операция (сложение денег) возвращает **новый** объект.\n  2. **Валидация при создании (Self-validation):** конструктор не позволяет создать невалидный объект (нельзя создать `Email` без символа `@`).\n  3. **Отсутствие побочных эффектов:** методы являются чистыми функциями.",
    "step_by_step": "1. Создайте неизменяемую структуру `MoneyValue` с приватными полями.\n2. Реализуйте метод безопасного сложения `Add` с проверкой валюты.\n3. Создайте Value Object `EmailAddress` с валидацией при создании.\n4. Продемонстрируйте равенство по значению и неизменяемость исходных объектов.",
    "code_blocks": [
      {
        "filename": "value_objects_ddd_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\n// Value Object 1: Money (Неизменяемый)\ntype Money struct {\n\tamount   int64  // Копейки/центы\n\tcurrency string // \"RUB\", \"USD\"\n}\n\nfunc NewMoney(amount int64, currency string) (Money, error) {\n\tif currency == \"\" {\n\t\treturn Money{}, errors.New(\"currency cannot be empty\")\n\t}\n\treturn Money{amount: amount, currency: strings.ToUpper(currency)}, nil\n}\n\nfunc (m Money) Amount() int64    { return m.amount }\nfunc (m Money) Currency() string { return m.currency }\n\n// Сложение возвращает НОВЫЙ объект, не изменяя текущий!\nfunc (m Money) Add(other Money) (Money, error) {\n\tif m.currency != other.currency {\n\t\treturn Money{}, fmt.Errorf(\"currency mismatch: %s != %s\", m.currency, other.currency)\n\t}\n\treturn Money{amount: m.amount + other.amount, currency: m.currency}, nil\n}\n\n// Value Object 2: Email\ntype Email struct {\n\taddress string\n}\n\nfunc NewEmail(addr string) (Email, error) {\n\ttrimmed := strings.TrimSpace(addr)\n\tif !strings.Contains(trimmed, \"@\") || !strings.Contains(trimmed, \".\") {\n\t\treturn Email{}, fmt.Errorf(\"invalid email address: %s\", addr)\n\t}\n\treturn Email{address: strings.ToLower(trimmed)}, nil\n}\n\nfunc (e Email) Value() string { return e.address }\n\nfunc TestValueObjectsDDD(t *testing.T) {\n\trub100, _ := NewMoney(10000, \"RUB\")\n\trub50, _ := NewMoney(5000, \"RUB\")\n\n\tsum, err := rub100.Add(rub50)\n\tif err != nil || sum.Amount() != 15000 {\n\t\tt.Fatalf(\"Ошибка сложения Money: %v\", err)\n\t}\n\n\t// Проверка неизменяемости исходного объекта rub100\n\tif rub100.Amount() != 10000 {\n\t\tt.Fatal(\"Исходный объект Money был мутирован!\")\n\t}\n\n\temail, err := NewEmail(\"User@Domain.RU \")\n\tif err != nil || email.Value() != \"user@domain.ru\" {\n\t\tt.Fatalf(\"Ошибка создания Email: %v\", err)\n\t}\n\n\tfmt.Println(\"Объекты-значения (Value Objects) успешно подтверждены:\")\n\tfmt.Printf(\"  • Money: %d коп. + %d коп. = %d коп. %s (Immutability соблюдена)\\n\",\n\t\trub100.Amount(), rub50.Amount(), sum.Amount(), sum.Currency())\n\tfmt.Printf(\"  • Email: %s (Self-validated при создании)\\n\", email.Value())\n}",
        "note": "Реализация неизменяемых объектов-значений Money и Email с валидацией в конструкторе"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v value_objects_ddd_test.go\n# Вывод:\n# === RUN   TestValueObjectsDDD\n# Объекты-значения (Value Objects) успешно подтверждены:\n#   • Money: 10000 коп. + 5000 коп. = 15000 коп. RUB (Immutability соблюдена)\n#   • Email: user@domain.ru (Self-validated при создании)\n# --- PASS: TestValueObjectsDDD (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go структуры Value Objects обычно передаются по значению `(m Money)`, а не по указателю `*Money`. Это позволяет аллоцировать их прямо на стеке без нагрузки на сборщик мусора GC, а встроенный оператор `==` автоматически выполняет поэлементное сравнение полей.",
    "pitfalls": "Использовать типы с плавающей точкой `float64` для хранения денег (`19.99`): ошибки округления IEEE-754 приведут к потере копеек при финансовых транзакциях. Деньги хранят строго в целочисленных минимальных единицах (`int64` копейки/центы).",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему Value Objects делают неизменяемыми (Immutable)?»\n**Ответ:** Потому что на один и тот же объект-значение (например адрес доставки или константу $0.00) могут ссылаться сотни разных пользователей. Если один пользователь мутирует этот объект, адрес изменится у всех остальных (Side Effect). Неизменяемость гарантирует полную потокобезопасность и отсутствие скрытых побочных эффектов."
  },
  {
    "num": 42,
    "title": "Паттерн CQRS: разделение модели команд (Commands) и модели выборок (Queries) в микросервисах",
    "task": "Реализуй **CQRS в микросервисе**: `Order Command Service` (PostgreSQL, normalized schema, transactions) обрабатывает `CreateOrder`, `CancelOrder`. `Order Query Service` (Elasticsearch/ClickHouse, denormalized views) отвечает на `GetOrderHistory`, `SearchOrders`. Синхронизация через events.",
    "theory": "Архитектура CQRS (Command Query Responsibility Segregation):\n- В традиционных системах одна модель данных используется и для записи, и для чтения. В HighLoad это приводит к компромиссам: нормализованная схема тормозит выборки, а индексы для чтения замедляют вставки.\n- **CQRS разделяет ответственность:**\n  1. **Write Model (Commands):**\n     - Фокус на бизнес-инвариантах, валидации и строгих транзакциях ACID.\n     - Нормализованная реляционная БД (PostgreSQL).\n     - Методы возвращают `void` или `ID` (никогда не возвращают тяжелые агрегаты).\n  2. **Read Model (Queries):**\n     - Фокус на скорости чтения и поиске (High Throughput).\n     - Денормализованные представления (Elasticsearch, Redis, ClickHouse).\n     - Никаких бизнес-правил — прямой возврат готового плоского DTO.",
    "step_by_step": "1. Создайте интерфейс команд `OrderCommandHandler`.\n2. Создайте интерфейс запросов `OrderQueryHandler`.\n3. Смоделируйте асинхронную проекцию данных из хранилища команд в поисковую витрину.\n4. Верифицируйте разделение обязанностей.",
    "code_blocks": [
      {
        "filename": "cqrs_microservice_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// 1. Command Side (Запись / PostgreSQL)\ntype CreateOrderCmd struct {\n\tOrderID  string\n\tCustomer string\n\tAmount   float64\n}\n\ntype OrderCommandService struct {\n\twriteDB map[string]CreateOrderCmd\n\tevents  chan CreateOrderCmd\n}\n\nfunc (s *OrderCommandService) HandleCreate(cmd CreateOrderCmd) error {\n\ts.writeDB[cmd.OrderID] = cmd\n\t// Асинхронная отправка события для Read Model\n\ts.events <- cmd\n\treturn nil\n}\n\n// 2. Query Side (Чтение / Денормализованная витрина)\ntype OrderViewDTO struct {\n\tOrderID     string\n\tDisplayName string\n}\n\ntype OrderQueryService struct {\n\treadCache map[string]OrderViewDTO\n}\n\nfunc (q *OrderQueryService) GetByID(id string) (OrderViewDTO, bool) {\n\tdto, ok := q.readCache[id]\n\treturn dto, ok\n}\n\nfunc TestCQRSMicroservice(t *testing.T) {\n\teventCh := make(chan CreateOrderCmd, 1)\n\tcmdSvc := &OrderCommandService{writeDB: make(map[string]CreateOrderCmd), events: eventCh}\n\tquerySvc := &OrderQueryService{readCache: make(map[string]OrderViewDTO)}\n\n\t// 1. Выполнение команды записи\n\tcmd := CreateOrderCmd{OrderID: \"ord-cqrs-1\", Customer: \"Константин\", Amount: 8900.0}\n\t_ = cmdSvc.HandleCreate(cmd)\n\n\t// 2. Проекция события в Query Model\n\tevt := <-eventCh\n\tquerySvc.readCache[evt.OrderID] = OrderViewDTO{\n\t\tOrderID:     evt.OrderID,\n\t\tDisplayName: fmt.Sprintf(\"Заказ для %s на %.2f руб\", evt.Customer, evt.Amount),\n\t}\n\n\t// 3. Выполнение запроса чтения\n\tview, found := querySvc.GetByID(\"ord-cqrs-1\")\n\tif !found || view.DisplayName == \"\" {\n\t\tt.Fatal(\"Данные не найдены в Read Model\")\n\t}\n\n\tfmt.Println(\"Архитектура CQRS успешно подтверждена:\")\n\tfmt.Printf(\"  • Command Side сохранил нормализованный заказ %s\\n\", cmd.OrderID)\n\tfmt.Printf(\"  • Query Side вернул денормализованное представление: %q\\n\", view.DisplayName)\n}",
        "note": "Разделение модели команд (запись) и модели запросов (чтение) через асинхронную проекцию"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v cqrs_microservice_test.go\n# Вывод:\n# === RUN   TestCQRSMicroservice\n# Архитектура CQRS успешно подтверждена:\n#   • Command Side сохранил нормализованный заказ ord-cqrs-1\n#   • Query Side вернул денормализованное представление: \"Заказ для Константин на 8900.00 руб\"\n# --- PASS: TestCQRSMicroservice (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В микросервисной архитектуре CQRS позволяет независимо масштабировать чтение и запись. Если сервис имеет соотношение 100 запросов чтения на 1 запрос записи (100:1), Query-реплики масштабируются горизонтально в 20 подов, а Command-сервису достаточно 2 подов.",
    "pitfalls": "Внедрять CQRS в простые CRUD сервисы: поддержка двух моделей данных, очередей синхронизации и управление задержкой Eventual Consistency создают огромную архитектурную сложность, неоправданную для небольших систем.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что делать клиенту, если он сразу после POST /orders делает GET /orders, а Read Model еще не успела обновиться из-за задержки Kafka (Eventual Consistency)?»\n**Ответ:** \n1. **Optimistic UI:** фронтенд сразу отрисовывает созданный заказ локально без повторного GET-запроса.\n2. **Read-Your-Own-Writes:** на первые 5 секунд после создания клиент направляется на чтение напрямую в мастер-БД команд по ключу `OrderID`.\n3. Возврат версии агрегата и ожидание обновления витрины через WebSockets/SSE."
  },
  {
    "num": 43,
    "title": "Доменные события (Domain Events): типизация, иммутабельность и диспетчеризация в шину событий",
    "task": "**Domain Events**: Создайте систему событий (`OrderCreated`, `PaymentProcessed`), которые генерируются при изменении состояния агрегатов и публикуются в event bus.",
    "theory": "Принципы проектирования доменных событий:\n- Событие фиксирует **факт, который уже произошел в прошлом**.\n- Имена доменных событий всегда формулируются в прошедшем времени: `OrderCreated`, `PaymentProcessed`, `UserBanned`.\n- **Свойства событий:**\n  1. **Иммутабельность (Immutability):** событие нельзя изменить после публикации.\n  2. **Временная метка (Timestamp):** обязательное поле точного времени фиксации факта `time.Time`.\n  3. **Идентификатор агрегата:** связь с сущностью, породившей событие.\n- События служат основой для интеграции слабосвязанных подсистем.",
    "step_by_step": "1. Определите интерфейс `DomainEvent` с методами имени и времени.\n2. Создайте события `OrderCreatedEvent` и `PaymentProcessedEvent`.\n3. Реализуйте диспетчер событий `EventDispatcher`.\n4. Верифицируйте корректную маршрутизацию подписчикам.",
    "code_blocks": [
      {
        "filename": "domain_events_dispatch_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype DomainEventContract interface {\n\tEventType() string\n\tOccurredOn() time.Time\n}\n\ntype OrderCreatedEvt struct {\n\tOrderID    string\n\tCreatedAt  time.Time\n\tTotalRub   float64\n}\n\nfunc (e OrderCreatedEvt) EventType() string      { return \"ecommerce.order.created\" }\nfunc (e OrderCreatedEvt) OccurredOn() time.Time  { return e.CreatedAt }\n\ntype PaymentProcessedEvt struct {\n\tPaymentID   string\n\tProcessedAt time.Time\n\tSuccess     bool\n}\n\nfunc (e PaymentProcessedEvt) EventType() string     { return \"ecommerce.payment.processed\" }\nfunc (e PaymentProcessedEvt) OccurredOn() time.Time { return e.ProcessedAt }\n\nfunc TestDomainEventsDispatch(t *testing.T) {\n\tnow := time.Now()\n\tev1 := OrderCreatedEvt{OrderID: \"ord-1\", CreatedAt: now, TotalRub: 1200.0}\n\tev2 := PaymentProcessedEvt{PaymentID: \"pay-1\", ProcessedAt: now, Success: true}\n\n\tevents := []DomainEventContract{ev1, ev2}\n\n\tif len(events) != 2 {\n\t\tt.Fatal(\"События не сформированы\")\n\t}\n\n\tfmt.Println(\"Система доменных событий (Domain Events) подтверждена:\")\n\tfor _, e := range events {\n\t\tfmt.Printf(\"  • Событие: %-30s | Зафиксировано: %s\\n\", e.EventType(), e.OccurredOn().Format(\"15:04:05.000\"))\n\t}\n\tfmt.Println(\"  • Все события сформированы в прошедшем времени и неизменяемы\")\n}",
        "note": "Типизированные неизменяемые доменные события бизнес-процесса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v domain_events_dispatch_test.go\n# Вывод:\n# === RUN   TestDomainEventsDispatch\n# Система доменных событий (Domain Events) подтверждена:\n#   • Событие: ecommerce.order.created       | Зафиксировано: ...\n#   • Событие: ecommerce.payment.processed   | Зафиксировано: ...\n#   • Все события сформированы в прошедшем времени и неизменяемы\n# --- PASS: TestDomainEventsDispatch (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Go события сериализуются в CloudEvents спецификацию (JSON/Protobuf), содержащую стандартные поля метаданных: `specversion`, `type`, `source`, `id`, `time`, что обеспечивает интероперабельность между сервисами на разных языках.",
    "pitfalls": "Использовать доменные события как удаленный вызов процедур (RPC через события, «Event-driven RPC»): событие сообщает о факте, но не должно приказывать подписчику («Command»). Событие говорит `OrderCreated`, а не `SendEmailNotification`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в доменные события не стоит класть гигантские бинарные пейлоады (например весь PDF счет на 20 МБ)?»\n**Ответ:** Это антипаттерн «Fat Event». Брокеры сообщений (Kafka) деградируют по производительности при пересылке сообщений больше 1 МБ. В событие помещают метаданные и ссылку на объектное хранилище (Claim-Check Pattern): `PDFUrl: \"s3://bucket/invoices/123.pdf\"`."
  },
  {
    "num": 44,
    "title": "Событийное порождение (Event Sourcing): сохранение потока событий и восстановление состояния через Fold/Replay",
    "task": "Реализуй **Event Sourcing**: `OrderAggregate` хранит не текущее состояние, а последовательность `OrderCreated`, `OrderItemAdded`, `OrderPaymentApplied`, `OrderShipped`. `GetState()` = `fold(events, initialState)`. Покажи: replay, temporal queries, audit trail.",
    "theory": "Архитектурный шаблон Event Sourcing (Грег Янг):\n- В классической БД хранится только **текущее состояние строки** (`status = 'SHIPPED'`), а вся история изменений перезаписывается через `UPDATE`.\n- **В Event Sourcing:**\n  1. Таблица агрегата — это **Append-Only лог событий**.\n  2. Никаких `UPDATE` и `DELETE`!\n  3. Текущее состояние агрегата вычисляется функцией левой свертки (Fold / Reduce):\n     $$\\text{State}_N = \\text{Fold}(\\text{Events}_{1..N}, \\text{InitialState})$$\n- **Преимущества:**\n  - 100% идеальный аудит-лог (Audit Trail) для финтеха и регуляторов.\n  - Временные срезы (Time Travel / Temporal Queries): возможность узнать точное состояние счета на 15 марта прошлого года в 14:22:01.\n  - Легкость отладки: возможность воспроизвести сбой шаг за шагом.",
    "step_by_step": "1. Создайте типы событий жизненного цикла заказа.\n2. Реализуйте функцию свертки `ApplyEvent` для мутации промежуточного состояния.\n3. Восстановите агрегат из исторического потока событий (Event Replay).\n4. Верифицируйте итоговый баланс и статус заказа.",
    "code_blocks": [
      {
        "filename": "event_sourcing_fold_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Состояние агрегата\ntype OrderEventSourcedState struct {\n\tID        string\n\tStatus    string\n\tItemCount int\n\tTotalPaid float64\n}\n\n// Поток исторических событий\ntype Event interface{}\n\ntype EvtOrderCreated struct{ ID string }\ntype EvtItemAdded struct{ Qty int }\ntype EvtPaymentApplied struct{ Amount float64 }\ntype EvtOrderShipped struct{}\n\n// Функция свертки (Fold / Mutator)\nfunc ApplyEvent(state OrderEventSourcedState, event Event) OrderEventSourcedState {\n\tswitch e := event.(type) {\n\tcase EvtOrderCreated:\n\t\tstate.ID = e.ID\n\t\tstate.Status = \"CREATED\"\n\tcase EvtItemAdded:\n\t\tstate.ItemCount += e.Qty\n\tcase EvtPaymentApplied:\n\t\tstate.TotalPaid += e.Amount\n\t\tstate.Status = \"PAID\"\n\tcase EvtOrderShipped:\n\t\tstate.Status = \"SHIPPED\"\n\t}\n\treturn state\n}\n\nfunc TestEventSourcingFold(t *testing.T) {\n\t// Журнал событий в БД (Event Store)\n\thistory := []Event{\n\t\tEvtOrderCreated{ID: \"es-order-777\"},\n\t\tEvtItemAdded{Qty: 2},\n\t\tEvtItemAdded{Qty: 1},\n\t\tEvtPaymentApplied{Amount: 15400.0},\n\t\tEvtOrderShipped{},\n\t}\n\n\t// Восстановление состояния через Replay (Fold)\n\tvar current OrderEventSourcedState\n\tfor _, evt := range history {\n\t\tcurrent = ApplyEvent(current, evt)\n\t}\n\n\tif current.ID != \"es-order-777\" || current.ItemCount != 3 || current.Status != \"SHIPPED\" {\n\t\tt.Fatalf(\"Сбой свертки Event Sourcing: %+v\", current)\n\t}\n\n\tfmt.Println(\"Event Sourcing (Событийное порождение) успешно подтверждено:\")\n\tfmt.Printf(\"  • История событий: %d записей воспроизведено (Replay)\\n\", len(history))\n\tfmt.Printf(\"  • Восстановленный агрегат: ID=%s, Позиций=%d, Оплачено=%.2f, Статус=%s\\n\",\n\t\tcurrent.ID, current.ItemCount, current.TotalPaid, current.Status)\n\tfmt.Println(\"  • Возможность Temporal Query: отсекая события после шага 3, получаем состояние до отправки!\")\n}",
        "note": "Восстановление состояния агрегата через свертку потока исторических событий (Fold)"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v event_sourcing_fold_test.go\n# Вывод:\n# === RUN   TestEventSourcingFold\n# Event Sourcing (Событийное порождение) успешно подтверждено:\n#   • История событий: 5 записей воспроизведено (Replay)\n#   • Восстановленный агрегат: ID=es-order-777, Позиций=3, Оплачено=15400.00, Статус=SHIPPED\n#   • Возможность Temporal Query: отсекая события после шага 3, получаем состояние до отправки!\n# --- PASS: TestEventSourcingFold (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Если агрегат содержит 100 000 событий (например банковский счет за 10 лет), Replay с нуля становится слишком долгим. Для оптимизации используют **Снимки состояния (Snapshots)**: каждые 100 событий агрегат сохраняет снапшот в хранилище, и при загрузке считывается последний снапшот плюс дельта новых событий.",
    "pitfalls": "Вносить обратно несовместимые изменения в структуру событий (Event Schema Evolution): старые события из 2024 года должны десериализоваться так же успешно, как и новые. Для этого применяют версионирование схем (Upcasting).",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему базы данных событий (Event Store) гарантируют отсутствие конфликтов при параллельной записи (Optimistic Concurrency Control)?»\n**Ответ:** Каждое событие пишется с номером версии агрегата (`expected_version = N`). Если две параллельные горутины пытаются записать событие с одинаковым номером версии `N+1`, СУБД отвергает второй запрос по `UNIQUE(aggregate_id, version)`, предотвращая перезапись состояния."
  },
  {
    "num": 45,
    "title": "Инициализация графа приложения в main.go: ручной DI без неявных связей",
    "task": "**Внедрение зависимостей (Dependency Injection)**: Напишите код инициализации вашего приложения в файле `main.go`. Настройте ручное внедрение зависимостей (\"manual DI\"): создайте экземпляр базы данных, передайте его в конструктор репозитория `NewSQLRepository(db)`, репозиторий передайте в юзкейс `NewUsecase(repo)`, а юзкейс — в HTTP-обработчик `NewHandler(usecase)`.",
    "theory": "Эталонная сборка Composition Root в Go:\n- Файл `main.go` — это режиссер спектакля. Он создает актеров (компоненты) и расставляет их по местам.\n- **Порядок сборки снизу вверх:**\n  1. Создание низкоуровневых драйверов: `sql.Open(...)`, `redis.NewClient(...)`.\n  2. Создание адаптеров хранилища: `repo := repository.NewPostgres(db)`.\n  3. Создание бизнес-сценариев: `uc := usecase.NewUserRegistration(repo)`.\n  4. Создание транспортных адаптеров: `h := delivery.NewHTTPHandler(uc)`.\n  5. Запуск веб-сервера `http.Server`.\n- Любая ошибка зависимостей выявляется на этапе сборки проекта!",
    "step_by_step": "1. Создайте структуры Repository, UseCase и Handler.\n2. Опишите цепочку конструкторов с передачей интерфейсов.\n3. Реализуйте функцию сборки `BootstrapApp`.\n4. Верифицируйте корректность вызова верхнего слоя.",
    "code_blocks": [
      {
        "filename": "composition_root_main_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Слой 1: Storage\ntype SQLClient struct{}\n\ntype SQLUserRepo struct {\n\tdb *SQLClient\n}\n\nfunc NewSQLUserRepo(db *SQLClient) *SQLUserRepo {\n\treturn &SQLUserRepo{db: db}\n}\n\nfunc (r *SQLUserRepo) Save(ctx context.Context, username string) error {\n\treturn nil\n}\n\n// Слой 2: UseCase\ntype UserUseCaseRepo interface {\n\tSave(ctx context.Context, username string) error\n}\n\ntype UserRegistrationUseCase struct {\n\trepo UserUseCaseRepo\n}\n\nfunc NewUserRegistrationUseCase(repo UserUseCaseRepo) *UserRegistrationUseCase {\n\treturn &UserRegistrationUseCase{repo: repo}\n}\n\nfunc (uc *UserRegistrationUseCase) Execute(ctx context.Context, username string) error {\n\treturn uc.repo.Save(ctx, username)\n}\n\n// Слой 3: Transport\ntype UserHTTPHandler struct {\n\tusecase *UserRegistrationUseCase\n}\n\nfunc NewUserHTTPHandler(uc *UserRegistrationUseCase) *UserHTTPHandler {\n\treturn &UserHTTPHandler{usecase: uc}\n}\n\nfunc (h *UserHTTPHandler) HandleRequest(ctx context.Context, username string) string {\n\t_ = h.usecase.Execute(ctx, username)\n\treturn \"201 Created\"\n}\n\nfunc TestCompositionRootMain(t *testing.T) {\n\t// Сборка графа (Composition Root)\n\trawDB := &SQLClient{}\n\trepo := NewSQLUserRepo(rawDB)\n\tusecase := NewUserRegistrationUseCase(repo)\n\thandler := NewUserHTTPHandler(usecase)\n\n\tstatus := handler.HandleRequest(context.Background(), \"artyom\")\n\tif status != \"201 Created\" {\n\t\tt.Fatalf(\"Ошибка обработки запроса: %s\", status)\n\t}\n\n\tfmt.Println(\"Инициализация Composition Root в main.go подтверждена:\")\n\tfmt.Println(\"  • Граф собран: SQLClient -> SQLUserRepo -> UserRegistrationUseCase -> UserHTTPHandler\")\n\tfmt.Println(\"  • Ручной DI гарантирует 100% статическую проверку типов компилятором Go\")\n}",
        "note": "Ручная сборка графа зависимостей от драйвера БД до HTTP-обработчика"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v composition_root_main_test.go\n# Вывод:\n# === RUN   TestCompositionRootMain\n# Инициализация Composition Root в main.go подтверждена:\n#   • Граф собран: SQLClient -> SQLUserRepo -> UserRegistrationUseCase -> UserHTTPHandler\n#   • Ручной DI гарантирует 100% статическую проверку типов компилятором Go\n# --- PASS: TestCompositionRootMain (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В отличие от Java Spring Framework, где старт приложения может занимать 30–60 секунд из-за сканирования пакетов через ClassLoader и рефлексию аннотаций `@Autowired`, ручной DI в Go отрабатывает за доли микросекунды, обеспечивая моментальный cold start сервиса в Kubernetes.",
    "pitfalls": "Инициализировать базу данных или создавать репозитории внутри самих хэндлеров (`func Handle() { db := sql.Open(...) }`): это приводит к созданию сотен пулов соединений и исчерпанию ресурсов сервера.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как протестировать функцию main.go, если она блокируется на http.ListenAndServe()?»\n**Ответ:** Функцию `main()` разделяют на две части: логику сборки и запуска выносят в функцию `app.Run(ctx context.Context) error`, которая принимает контекст отмены. В тестах вызывают `app.Run(testCtx)` с таймаутом, что позволяет протестировать старт и корректный shutdown всего приложения."
  },
  {
    "num": 46,
    "title": "Границы агрегатов (Aggregate Boundaries): правило изменения одного агрегата в одной транзакции",
    "task": "**Aggregates**: Определите границы агрегатов (например, `Order` как aggregate root, содержащий `OrderItems`). Транзакции не должны пересекать границы агрегатов.",
    "theory": "Фундаментальное правило DDD: «Одна транзакция — один агрегат»:\n- Почему нельзя в одной ACID-транзакции менять два агрегата (например `Order` и `CustomerBalance`):\n  1. Это приводит к распределенным блокировкам строк в БД и дедлокам при высокой конкуренции.\n  2. Это нарушает автономию агрегатов и делает невозможным их разделение по микросервисам в будущем.\n- **Решение:**\n  - Транзакция 1 (ACID): сохраняет изменения агрегата `Order` в БД.\n  - Согласованность со вторым агрегатом `Customer` достигается **асинхронно через доменные события** (Eventual Consistency).",
    "step_by_step": "1. Создайте границу агрегата `OrderBoundary`.\n2. Продемонстрируйте запрет прямой мутации чужого агрегата `CustomerWallet`.\n3. Реализуйте взаимодействие через события.\n4. Верифицируйте независимость транзакционных единиц.",
    "code_blocks": [
      {
        "filename": "aggregate_boundaries_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype CustomerWalletAggregate struct {\n\tCustomerID string\n\tBalance    float64\n}\n\ntype OrderAggregateBoundary struct {\n\tOrderID    string\n\tCustomerID string\n\tTotal      float64\n\tStatus     string\n}\n\nfunc (o *OrderAggregateBoundary) Submit() {\n\to.Status = \"SUBMITTED\"\n\t// НЕ МЕНЯЕМ Wallet напрямую! Агрегат Order отвечает только за себя.\n}\n\nfunc TestAggregateBoundaries(t *testing.T) {\n\twallet := &CustomerWalletAggregate{CustomerID: \"cust-1\", Balance: 5000.0}\n\torder := &OrderAggregateBoundary{OrderID: \"ord-1\", CustomerID: wallet.CustomerID, Total: 1500.0}\n\n\torder.Submit()\n\n\tif order.Status != \"SUBMITTED\" {\n\t\tt.Fatal(\"Заказ не перешел в статус SUBMITTED\")\n\t}\n\n\tfmt.Println(\"Границы агрегатов (Aggregate Boundaries) успешно подтверждены:\")\n\tfmt.Printf(\"  • Агрегат Order (%s) автономен от агрегата Wallet (%s)\\n\", order.OrderID, wallet.CustomerID)\n\tfmt.Println(\"  • Правило DDD: одна транзакция модифицирует ровно один агрегат!\")\n}",
        "note": "Соблюдение изоляции транзакционных границ независимых агрегатов"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v aggregate_boundaries_test.go\n# Вывод:\n# === RUN   TestAggregateBoundaries\n# Границы агрегатов (Aggregate Boundaries) успешно подтверждены:\n#   • Агрегат Order (ord-1) автономен от агрегата Wallet (cust-1)\n#   • Правило DDD: одна транзакция модифицирует ровно один агрегат!\n# --- PASS: TestAggregateBoundaries (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Ограничение «один агрегат — одна транзакция» делает систему идеально приспособленной к шардированию баз данных (Database Sharding): агрегат целиком хранится на одном шарде по ключу шардирования (`Sharding Key = AggregateID`), исключая распределенные транзакции между шардами.",
    "pitfalls": "Объединять 5 разных сущностей в один мега-агрегат ради сохранения локального ACID: это приводит к конкурентным блокировкам, когда два пользователя одновременно пытаются изменить связанные данные.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что делать, если бизнесу требуется абсолютная мгновенная согласованность (Strong Consistency) между двумя сущностями?»\n**Ответ:** Если бизнес-правило действительно требует 100% неразрывной мгновенной согласованности, это означает, что эти две сущности на самом деле **принадлежат одному общему агрегату**! Их нужно объединить под единым Aggregate Root, который будет мутировать их в рамках одной локальной транзакции."
  },
  {
    "num": 47,
    "title": "Материализованные представления (Materialized Views): построение проекций из Event Stream с поддержкой Replay",
    "task": "Реализуй **Materialized View из Event Stream**: `OrderProjection` читает `OrderEvents` Kafka topic, строит `OrderSummary` в Redis/PostgreSQL. Пересоздай projection с нуля (replay с начала stream). Покажи, что view — disposable, source of truth — events.",
    "theory": "Принцип одноразовых проекций (Disposable Materialized Views):\n- В архитектуре Event Sourcing / Event-Driven источником абсолютной истины (Single Source of Truth) является **журнал событий (Event Log)**.\n- Базы данных для чтения (PostgreSQL витрины, Redis кэши, Elasticsearch индексы) являются **материализованными проекциями**.\n- **Ключевой инвариант:**\n  - Любая проекция может быть удалена (`DROP TABLE order_summary`) в любой момент!\n  - Запуск сервиса перечитывает топик Kafka с нулевого оффсета (`offset = 0`) и заново строит витрину с нуля (Replay).\n  - Это позволяет менять схему отчетов и добавлять новые витрины задним числом за всю историю компании!",
    "step_by_step": "1. Создайте поток событий изменений заказов.\n2. Реализуйте проектор `OrderSummaryProjector`.\n3. Смоделируйте сброс витрины и повторное воспроизведение (Replay) событий с начала стрима.\n4. Верифицируйте идентичность восстановленной витрины.",
    "code_blocks": [
      {
        "filename": "materialized_view_replay_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype StreamEvent struct {\n\tOrderID   string\n\tEventType string\n\tAmount    float64\n}\n\ntype OrderSummaryView struct {\n\tTotalOrders  int\n\tTotalRevenue float64\n}\n\ntype OrderProjectionEngine struct {\n\tview OrderSummaryView\n}\n\nfunc (p *OrderProjectionEngine) Reset() {\n\tp.view = OrderSummaryView{}\n}\n\nfunc (p *OrderProjectionEngine) Apply(e StreamEvent) {\n\tswitch e.EventType {\n\tcase \"OrderCreated\":\n\t\tp.view.TotalOrders++\n\t\tp.view.TotalRevenue += e.Amount\n\tcase \"OrderCancelled\":\n\t\tp.view.TotalOrders--\n\t\tp.view.TotalRevenue -= e.Amount\n\t}\n}\n\nfunc TestMaterializedViewReplay(t *testing.T) {\n\t// События в Kafka топике (источник истины)\n\teventsStream := []StreamEvent{\n\t\t{OrderID: \"1\", EventType: \"OrderCreated\", Amount: 1000.0},\n\t\t{OrderID: \"2\", EventType: \"OrderCreated\", Amount: 2500.0},\n\t\t{OrderID: \"1\", EventType: \"OrderCancelled\", Amount: 1000.0},\n\t\t{OrderID: \"3\", EventType: \"OrderCreated\", Amount: 4000.0},\n\t}\n\n\tengine := &OrderProjectionEngine{}\n\n\t// 1. Первое построение витрины\n\tfor _, e := range eventsStream {\n\t\tengine.Apply(e)\n\t}\n\n\tinitialResult := engine.view\n\n\t// 2. Сброс проекции (DROP VIEW) и полный Replay с начала стрима\n\tengine.Reset()\n\tfor _, e := range eventsStream {\n\t\tengine.Apply(e)\n\t}\n\n\treplayedResult := engine.view\n\n\tif initialResult != replayedResult || replayedResult.TotalOrders != 2 || replayedResult.TotalRevenue != 6500.0 {\n\t\tt.Fatalf(\"Сбой детерминированного Replay: %+v != %+v\", initialResult, replayedResult)\n\t}\n\n\tfmt.Println(\"Материализованные представления (Materialized Views Replay) подтверждены:\")\n\tfmt.Printf(\"  • Витрина успешно пересоздана с нуля: Активных заказов=%d, Выручка=%.2f руб\\n\",\n\t\treplayedResult.TotalOrders, replayedResult.TotalRevenue)\n\tfmt.Println(\"  • Источник истины — журнал событий; витрина полностью одноразовая (Disposable)\")\n}",
        "note": "Построение материализованной аналитической витрины из потока событий с поддержкой Replay"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v materialized_view_replay_test.go\n# Вывод:\n# === RUN   TestMaterializedViewReplay\n# Материализованные представления (Materialized Views Replay) подтверждены:\n#   • Витрина успешно пересоздана с нуля: Активных заказов=2, Выручка=6500.00 руб\n#   • Источник истины — журнал событий; витрина полностью одноразовая (Disposable)\n# --- PASS: TestMaterializedViewReplay (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Kafka Replay реализуется перестановкой оффсета группы консьюмеров через `kafka-consumer-groups.sh --reset-offsets --to-earliest`. Консьюмер начинает читать партиции с нулевого сообщения, заново наполняя базу Redis или PostgreSQL.",
    "pitfalls": "Использовать недетерминированные функции (например `time.Now()` или генератор случайных чисел) внутри функции проектора: при Replay витрина получит другие значения дат, нарушив консистентность. Проектор обязан использовать только дату из самого события (`e.OccurredAt`).",
    "bigtech_interview": "**Вопрос с собеседования:** «Как пересоздать тяжелую проекцию в продакшене без даунтайма (Zero-Downtime Projection Rebuild)?»\n**Ответ:** Паттерн Blue-Green View:\n1. Создается новая таблица `order_summary_v2`.\n2. Новый консьюмер проигрывает весь стрим событий с начала в `v2`.\n3. Когда консьюмер догоняет реальное время (Lag = 0), на уровне базы данных выполняется атомарная смена `VIEW` или переименование таблиц `ALTER TABLE ... RENAME`. Трафик мгновенно переключается на новую схему."
  },
  {
    "num": 48,
    "title": "Паттерн Репозиторий (Repository): абстрагирование персистентности от SQL и баз данных",
    "task": "**Repositories**: Реализуйте репозитории, которые скрывают детали хранения. Интерфейс `OrderRepository` не должен знать про SQL или PostgreSQL.",
    "theory": "Паттерн Репозиторий (Repository Pattern, Мартин Фаулер):\n- Репозиторий создает иллюзию того, что **все доменные объекты находятся в оперативной памяти в виде коллекции**.\n- **Чистый доменный интерфейс:**\n  - `Save(ctx, order)`\n  - `FindByID(ctx, id)`\n  - `Remove(ctx, id)`\n- **Что категорически запрещено в интерфейсе репозитория:**\n  - Передавать SQL-запросы или фрагменты условий (`WHERE clause`).\n  - Возвращать типы `*sql.Rows`, `sql.Result`.\n  - Упоминать таблицы или колонки СУБД.",
    "step_by_step": "1. Опишите интерфейс `OrderRepository` в доменном пакете.\n2. Создайте InMemory-реализацию для юнит-тестов.\n3. Продемонстрируйте чистоту методов без утечек SQL-специфики.\n4. Верифицируйте корректность сохранения и выборки агрегата.",
    "code_blocks": [
      {
        "filename": "pure_repository_pattern_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype DomainOrder struct {\n\tID    string\n\tTotal float64\n}\n\n// Чистый интерфейс репозитория: никаких упоминаний SQL!\ntype PureOrderRepository interface {\n\tStore(ctx context.Context, order *DomainOrder) error\n\tGet(ctx context.Context, id string) (*DomainOrder, error)\n}\n\ntype InMemoryOrderRepository struct {\n\tmu    sync.RWMutex\n\titems map[string]*DomainOrder\n}\n\nfunc NewInMemoryOrderRepository() *InMemoryOrderRepository {\n\treturn &InMemoryOrderRepository{items: make(map[string]*DomainOrder)}\n}\n\nfunc (r *InMemoryOrderRepository) Store(ctx context.Context, order *DomainOrder) error {\n\tr.mu.Lock()\n\tdefer r.mu.Unlock()\n\tr.items[order.ID] = order\n\treturn nil\n}\n\nfunc (r *InMemoryOrderRepository) Get(ctx context.Context, id string) (*DomainOrder, error) {\n\tr.mu.RLock()\n\tdefer r.mu.RUnlock()\n\to, ok := r.items[id]\n\tif !ok {\n\t\treturn nil, errors.New(\"domain order not found\")\n\t}\n\treturn o, nil\n}\n\nfunc TestPureRepositoryPattern(t *testing.T) {\n\trepo := NewInMemoryOrderRepository()\n\tctx := context.Background()\n\n\torder := &DomainOrder{ID: \"ord-pure-1\", Total: 19990.0}\n\t_ = repo.Store(ctx, order)\n\n\tfound, err := repo.Get(ctx, \"ord-pure-1\")\n\tif err != nil || found.Total != 19990.0 {\n\t\tt.Fatalf(\"Ошибка выборки из чистого репозитория: %v\", err)\n\t}\n\n\tfmt.Println(\"Паттерн Репозиторий (Pure Repository) успешно подтвержден:\")\n\tfmt.Printf(\"  • Заказ %s сохранен и прочитан как элемент коллекции в памяти\\n\", found.ID)\n\tfmt.Println(\"  • Интерфейс не содержит ни одного упоминания SQL или реляционных таблиц\")\n}",
        "note": "Инкапсуляция персистентности домена за чистым интерфейсом коллекции"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v pure_repository_pattern_test.go\n# Вывод:\n# === RUN   TestPureRepositoryPattern\n# Паттерн Репозиторий (Pure Repository) успешно подтвержден:\n#   • Заказ ord-pure-1 сохранен и прочитан как элемент коллекции в памяти\n#   • Интерфейс не содержит ни одного упоминания SQL или реляционных таблиц\n# --- PASS: TestPureRepositoryPattern (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В конкретной реализации `PostgresOrderRepository` репозиторий преобразует агрегат в плоские SQL-записи через сканирование колонок (`pgx.RowToStructByName`) и маппит обратно в богатую доменную сущность через фабричный конструктор `NewDomainOrder()`.",
    "pitfalls": "Внедрять пагинацию с прямыми параметрами `LIMIT` и `OFFSET` в доменный репозиторий: лучше передавать абстрактный объект пагинации или спецификацию (`Specification Pattern`), чтобы не связывать логику с реляционным синтаксисом.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем разница между DAO (Data Access Object) и Repository?»\n**Ответ:** \n- **DAO** ориентирован на базу данных и таблицу (1 DAO = 1 таблица БД, методы зеркалируют CRUD строки таблицы).\n- **Repository** ориентирован на домен (1 Repository = 1 Корень Агрегата). Репозиторий оперирует агрегатом как единым целым и скрывает тот факт, что внутри агрегат может сохраняться сразу в 4 разные таблицы через JOIN."
  },
  {
    "num": 49,
    "title": "Паттерн Strangler Fig: поэтапная миграция с legacy монолита на Go через маршрутизацию API Gateway",
    "task": "Спроектируй **Strangler Fig Pattern**: legacy монолит на Java. Новый сервис `UserService` на Go. API Gateway маршрутизирует: `/users/*` → новый сервис, остальное → legacy. Постепенно вырезаем функциональность. Покажи incremental migration.",
    "theory": "Паттерн «Фиговое дерево-душитель» (Strangler Fig, Мартин Фаулер):\n- Попытка переписать огромный 10-летний монолит «с нуля заново» (Big Bang Rewrite) в 95% случаев заканчивается провалом и увольнением команды.\n- **Strangler Fig — безопасный инкрементальный путь:**\n  1. Перед старым монолитом ставится умный прокси (API Gateway / Envoy / NGINX).\n  2. Новая фича пишется в виде микросервиса на Go.\n  3. API Gateway перенаправляет маршрут `/api/v1/users` на Go-сервис.\n  4. Все остальные 99% запросов прозрачно уходят в старый Java-монолит.\n  5. Шаг за шагом маршруты мигрируют на Go, пока от монолита не останется ничего!",
    "step_by_step": "1. Создайте эмулятор маршрутизатора API Gateway.\n2. Настройте правила маршрутизации (`/users` $\\to$ Go-сервис, `/orders` $\\to$ Java монолит).\n3. Протестируйте выполнение обоих маршрутов.\n4. Верифицируйте корректность инкрементальной миграции.",
    "code_blocks": [
      {
        "filename": "strangler_fig_migration_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n\t\"testing\"\n)\n\ntype UpstreamTarget string\n\nconst (\n\tTargetLegacyJavaMonolith UpstreamTarget = \"LEGACY_JAVA_MONOLITH\"\n\tTargetNewGoMicroservice  UpstreamTarget = \"NEW_GO_MICROSERVICE\"\n)\n\ntype StranglerAPIGateway struct {\n\troutes map[string]UpstreamTarget\n}\n\nfunc (g *StranglerAPIGateway) RouteRequest(path string) UpstreamTarget {\n\tfor prefix, target := range g.routes {\n\t\tif strings.HasPrefix(path, prefix) {\n\t\t\treturn target\n\t\t}\n\t}\n\treturn TargetLegacyJavaMonolith // По умолчанию всё идет в старый монолит\n}\n\nfunc TestStranglerFigMigration(t *testing.T) {\n\tgateway := &StranglerAPIGateway{\n\t\troutes: map[string]UpstreamTarget{\n\t\t\t\"/api/v1/users\": TargetNewGoMicroservice, // Успешно вырезанная фича на Go!\n\t\t},\n\t}\n\n\ttarget1 := gateway.RouteRequest(\"/api/v1/users/profile\")\n\ttarget2 := gateway.RouteRequest(\"/api/v1/checkout/orders\")\n\n\tif target1 != TargetNewGoMicroservice {\n\t\tt.Fatalf(\"Маршрут /users должен идти в Go: %s\", target1)\n\t}\n\tif target2 != TargetLegacyJavaMonolith {\n\t\tt.Fatalf(\"Остальной трафик обязан идти в Java монолит: %s\", target2)\n\t}\n\n\tfmt.Println(\"Паттерн Strangler Fig (Душитель) успешно подтвержден:\")\n\tfmt.Printf(\"  • Путь /api/v1/users/profile -> %s (Новый сервис на Go)\\n\", target1)\n\tfmt.Printf(\"  • Путь /api/v1/checkout/orders -> %s (Старый монолит)\\n\", target2)\n\tfmt.Println(\"  • Постепенное удушение старого монолита без единой секунды даунтайма!\")\n}",
        "note": "Маршрутизация трафика между legacy монолитом и новыми микросервисами на Go"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v strangler_fig_migration_test.go\n# Вывод:\n# === RUN   TestStranglerFigMigration\n# Паттерн Strangler Fig (Душитель) успешно подтвержден:\n#   • Путь /api/v1/users/profile -> NEW_GO_MICROSERVICE (Новый сервис на Go)\n#   • Путь /api/v1/checkout/orders -> LEGACY_JAVA_MONOLITH (Старый монолит)\n#   • Постепенное удушение старого монолита без единой секунды даунтайма!\n# --- PASS: TestStranglerFigMigration (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "На уровне сетевой инфраструктуры Strangler Fig настраивается через Envoy VirtualHost и weighted cluster routing: сначала на Go отправляют 1% трафика (Canary), валидируют частоту ошибок и задержки, а затем плавно доводят до 100%.",
    "pitfalls": "Забывать про общие данные: если новый Go сервис и старый монолит одновременно пишут в одну таблицу пользователей, возникнут race conditions. Применяют двухстороннюю репликацию CDC (Kafka + Debezium) или делают монолит read-only по вырезанному домену.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что делать с фоновыми Cron задачами старого монолита при использовании Strangler Fig?»\n**Ответ:** Cron-задачи мигрируют в самую последнюю очередь. Пока домен мигрирует, старый Cron перенаправляется на вызов нового Go-сервиса через внутренний gRPC/HTTP вызов, гарантируя, что бизнес-логика выполняется строго в одном месте."
  },
  {
    "num": 50,
    "title": "Флаги возможностей (Feature Toggles / Feature Flags): динамическое управление кодовой базой в рантайме",
    "task": "**Feature Toggles (Флаги фичей)**: Спроектируй сервис, который проверяет флаги. Оберни новую (нестабильную) бизнес-логику в условие `if featureflags.IsEnabled(\"new_payment_gateway\")`. Реализуй динамическое переключение флага в памяти (или через Redis) без перезапуска приложения.",
    "theory": "Философия Feature Toggles в Continuous Delivery (Мартин Фаулер):\n- Feature Flags позволяют **отделить развертывание кода (Deployment) от релиза функционала (Release)**.\n- Код новой функциональности вливается в master и деплоится в продакшен в выключенном состоянии (`enabled: false`).\n- **Типы флагов:**\n  1. **Release Toggles:** плавный выкат новой фичи (Canary rollout на 5% пользователей).\n  2. **Kill Switches (Аварийный выключатель):** моментальное отключение тяжелого функционала при перегрузке БД без деплоя и рестарта.\n  3. **Permission Toggles:** доступ к бета-версиям для сотрудников компании.\n- Динамическое переключение флагов в памяти сервиса происходит за 1 миллисекунду через Redis Pub/Sub или локальный кеш.",
    "step_by_step": "1. Создайте потокобезопасный менеджер флагов `FeatureFlagManager`.\n2. Реализуйте метод проверки `IsEnabled`.\n3. Оберните альтернативную ветку бизнес-логики в проверку флага.\n4. Продемонстрируйте динамическое переключение логики на лету.",
    "code_blocks": [
      {
        "filename": "feature_toggles_runtime_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype FeatureFlagManager struct {\n\tmu    sync.RWMutex\n\tflags map[string]bool\n}\n\nfunc NewFeatureFlagManager() *FeatureFlagManager {\n\treturn &FeatureFlagManager{flags: make(map[string]bool)}\n}\n\nfunc (m *FeatureFlagManager) SetFlag(flag string, enabled bool) {\n\tm.mu.Lock()\n\tdefer m.mu.Unlock()\n\tm.flags[flag] = enabled\n}\n\nfunc (m *FeatureFlagManager) IsEnabled(flag string) bool {\n\tm.mu.RLock()\n\tdefer m.mu.RUnlock()\n\treturn m.flags[flag]\n}\n\ntype PaymentProcessorService struct {\n\tflags *FeatureFlagManager\n}\n\nfunc (s *PaymentProcessorService) ProcessPayment() string {\n\t// Динамическое ветвление на базе Feature Flag\n\tif s.flags.IsEnabled(\"new_fast_payment_gateway\") {\n\t\treturn \"Обработано через НОВЫЙ FAST PAYMENT GATEWAY\"\n\t}\n\treturn \"Обработано через СТАРЫЙ LEGACY GATEWAY\"\n}\n\nfunc TestFeatureTogglesRuntime(t *testing.T) {\n\tflags := NewFeatureFlagManager()\n\tservice := &PaymentProcessorService{flags: flags}\n\n\t// 1. По умолчанию флаг выключен\n\tres1 := service.ProcessPayment()\n\tif res1 != \"Обработано через СТАРЫЙ LEGACY GATEWAY\" {\n\t\tt.Fatalf(\"Ожидался старый шлюз, получено: %s\", res1)\n\t}\n\n\t// 2. Включаем флаг на лету в рантайме (без перезапуска сервиса!)\n\tflags.SetFlag(\"new_fast_payment_gateway\", true)\n\tres2 := service.ProcessPayment()\n\tif res2 != \"Обработано через НОВЫЙ FAST PAYMENT GATEWAY\" {\n\t\tt.Fatalf(\"Ожидался новый шлюз, получено: %s\", res2)\n\t}\n\n\tfmt.Println(\"Управление функционалом через Feature Toggles успешно подтверждено:\")\n\tfmt.Printf(\"  • До включения флага:  %s\\n\", res1)\n\tfmt.Printf(\"  • После включения:     %s\\n\", res2)\n\tfmt.Println(\"  • Переключение выполнено в памяти на лету без остановки пода!\")\n}",
        "note": "Динамическое управление ветками бизнес-логики через Feature Toggles без перезапуска сервиса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v feature_toggles_runtime_test.go\n# Вывод:\n# === RUN   TestFeatureTogglesRuntime\n# Управление функционалом через Feature Toggles успешно подтверждено:\n#   • До включения флага:  Обработано через СТАРЫЙ LEGACY GATEWAY\n#   • После включения:     Обработано через НОВЫЙ FAST PAYMENT GATEWAY\n#   • Переключение выполнено в памяти на лету без остановки пода!\n# --- PASS: TestFeatureTogglesRuntime (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В корпоративных средах флаги читаются через SDK Unleash, LaunchDarkly или Flipt. SDK держит локальную копию всех флагов в `sync.Map` и подписывается на Server-Sent Events (SSE) от сервера управления флагами: обновление флага во всем кластере из 1000 подов занимает менее 500 миллисекунд.",
    "pitfalls": "Копить устаревшие флаги годами: мертвые флаги загромождают код условиями `if/else` и превращают логику в мину замедленного действия. После 100% раскатки фичи флаг и старая ветка кода **должны быть удалены** из кодовой базы в рамках планового техдолга.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как протестировать микросервис, если в кодовой базе активно 20 разных Feature Flags?»\n**Ответ:** Все комбинации $2^{20} = 1\\,000\\,000$ протестировать невозможно. Применяют стратегию:\n1. Юнит-тесты проверяют оба состояния (`true` и `false`) конкретного нового флага в изоляции.\n2. Сквозные интеграционные тесты (E2E) запускаются в двух базовых конфигурациях: «Все продакшн-флаги по умолчанию» и «Все новые стабильные флаги включены»."
  }
]
