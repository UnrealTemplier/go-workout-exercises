# Section 1: Exercises 1 to 15

exercises = [
    {
        "num": 1,
        "title": "Первый запуск Go-программы и концепция модулей",
        "task": "Hello, World!: Инициализируй модуль (go mod init), создай main.go, напиши вывод строки и запусти через go run.",
        "theory": """
В других языках программирования запуск простейшей программы часто не требует манипуляций с проектом: в Python или JS достаточно создать один файл и передать его интерпретатору (`python script.py`, `node script.js`). В C и C++ компилятор (`gcc`, `clang`) собирает исходники напрямую в объектные файлы.

**В Go концепция модульности является фундаментальной.** 
Начиная с версии Go 1.11 (и окончательно с 1.16+), Go перешел на систему **Go Modules**. Модуль — это совокупность пакетов Go, версионируемых как единое целое, с файлом-манифестом `go.mod` в корне.

Почему Go требует модуль даже для простого скрипта?
1. **Детерминизм и воспроизводимость сборки:** компилятор точно знает границы проекта, версию языка и корень путей импорта.
2. **Отказ от устаревшего `$GOPATH`:** вам больше не нужно хранить весь код в фиксированной глобальной директории `~/go/src/`.
3. **Точка отсчета для импортов:** имя модуля становится корневым префиксом для всех ваших будущих внутренних пакетов.

Любая исполняемая программа в Go обязана иметь:
- `package main` в первой строке файла;
- функцию точки входа `func main()` без аргументов и возвращаемых значений.
""",
        "step_by_step": """
1. **Создаем рабочую директорию:** Создадим отдельную папку под наш первый проект, чтобы не захламлять окружение.
2. **Инициализируем модуль:** Команда `go mod init <module-name>` создает файл `go.mod`. В качестве имени модуля можно указать простое имя (например, `hello`) или канонический путь репозитория (`github.com/username/hello`).
3. **Пишем код:** Создаем файл `main.go`. Импортируем стандартный пакет форматированного ввода/вывода `fmt` и вызываем функцию `fmt.Println()`.
4. **Запускаем:** Выполняем команду `go run .` или `go run main.go`.
""",
        "code_blocks": [
            {
                "filename": "Терминал (Bash)",
                "lang": "bash",
                "code": "mkdir -p hello_world && cd hello_world\ngo mod init hello_world",
                "note": "Инициализация Go-модуля hello_world"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import "fmt"

func main() {
	fmt.Println("Hello, World!")
}""",
                "note": "Минимальная исполняемая программа на Go"
            },
            {
                "filename": "Терминал (Запуск)",
                "lang": "bash",
                "code": "go run .\n# Вывод:\n# Hello, World!",
                "note": "Компиляция во временный каталог и моментальный запуск"
            }
        ],
        "under_the_hood": """
Что на самом деле делает `go run`?
1. Анализирует исходный код и зависимости.
2. Вызывает компилятор `go tool compile` для генерации бинарных объектных файлов (`.a`).
3. Вызывает линкер `go tool link` для сборки независимого исполняемого файла во временной директории (например, `/tmp/go-build...`).
4. Запускает скомпилированный бинарник, передавая ему потоки stdin/stdout/stderr.
5. По завершении программы автоматически удаляет временный исполняемый файл.

В продакшене `go run` **никогда не используется** — собирается статический бинарник через `go build`.
""",
        "pitfalls": """
- **Ошибка `package main; func main`:** Если объявить `package mypkg` вместо `package main`, команда `go run` выдаст ошибку `cannot run non-main package`.
- **Лишняя точка с запятой:** В Go точки с запятой расставляются лексером автоматически. Не ставьте их вручную на концах строк.
- **Неиспользуемый импорт:** Если вы импортируете `"fmt"`, но не вызовете функцию из него, Go выдаст ошибку компиляции: `imported and not used: "fmt"`. В Go неиспользуемые импорты запрещены на уровне компилятора!
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему в функции main() в Go нет аргументов (как `int argc, char *argv[]` в C или `String[] args` в Java/C#) и нет возврата кода ошибки (`return 0`)?»
**Ответ:** Аргументы командной строки в Go доступны глобально через срез строк `os.Args` из пакета `os`. Код завершения программы передается через вызов `os.Exit(code)`. Если функция `main()` завершается естественным образом, рантайм Go автоматически возвращает код 0 операционной системе.
"""
    },
    {
        "num": 2,
        "title": "Анатомия Go SDK: GOROOT, GOPATH, PATH и go version",
        "task": "Установите Go, настройте окружение (GOROOT, GOPATH, PATH), проверьте командой go version.",
        "theory": """
Для эффективной разработки на Go крайне важно понимать, как организовано рабочее окружение компилятора:

1. **`GOROOT` (Go Root):** Путь к установленной директории самого компилятора Go и его стандартной библиотеки (например, `/usr/local/go` или `/opt/homebrew/opt/go/libexec`). В 99% случаев Go вычисляет его автоматически относительно расположения бинарника `go`. Вручную задавать `GOROOT` в `.bashrc`/`.zshrc` **не рекомендуется**, если только вы не разрабатываете сам компилятор Go.
2. **`GOPATH` (Go Path):** Исторически (до Go 1.11) это было место, где лежали все исходники проектов. Сегодня `GOPATH` используется для двух целей:
   - `$GOPATH/pkg/mod` — глобальный локальный кэш скачанных внешних модулей и их контрольных сумм.
   - `$GOPATH/bin` (или `$GOBIN`) — место установки исполняемых утилит, собранных через `go install` (например, `golangci-lint`, `mockgen`, `protoc-gen-go`).
3. **`PATH`:** Системная переменная путей поиска исполняемых файлов. Чтобы установленные через `go install` линтеры и генераторы кода были доступны в терминале, директорию `$GOPATH/bin` обязательно нужно добавить в системный `PATH`.
""",
        "step_by_step": """
1. Проверим установленную версию Go через `go version`.
2. Посмотрим детальную сводку всех переменных окружения Go через `go env`.
3. Убедимся, что `$GOPATH/bin` добавлен в переменную `PATH`.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Проверка версии
go version
# Вывод: go version go1.22.5 linux/amd64

# Проверка GOROOT и GOPATH
go env GOROOT
go env GOPATH

# Добавление bin в PATH (например, в ~/.bashrc или ~/.zshrc)
export PATH=$PATH:$(go env GOPATH)/bin""",
                "note": "Проверка окружения и настройка PATH"
            }
        ],
        "under_the_hood": """
При вызове любой команды тулчейна Go (`go build`, `go test`) бинарник `go` считывает переменные окружения, переопределяющие поведение компилятора. Например:
- `CGO_ENABLED=0` — полностью отключает привязку к системным библиотекам C (libc), формируя 100% статически слинкованный бинарник.
- `GOOS` и `GOARCH` — определяют целевую ОС и процессорную архитектуру.
""",
        "pitfalls": """
- **Устаревшие гайды:** В интернете полно статей 2015–2017 годов, утверждающих, что любой проект нужно создавать строго внутри `$GOPATH/src/github.com/...`. Это устарело! В современном Go с поддержкой модулей проекты создаются в **любой** удобной директории на диске.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что произойдет, если запустить `go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest`? Где окажется бинарник и будет ли затронут текущий `go.mod`?»
**Ответ:** Начиная с Go 1.16, запуск `go install` с версией `@latest` компилирует утилиту в изолированном режиме и сохраняет бинарник в `$GOBIN` (или `$GOPATH/bin`), **не модифицируя** файл `go.mod` текущего каталога.
"""
    },
    {
        "num": 3,
        "title": "Многофайловый пакет main и единица компиляции в Go",
        "task": "Измени модуль: добавь второй файл .go в тот же пакет main. Раздели программу между двумя файлами (например, main.go и greet.go). Убедись, что go run работает с обоими файлами.",
        "theory": """
В C/C++ для связи файлов используют заголовочные файлы `#include "greet.h"`. В PHP/JS используют конструкции `require`/`import './greet.js'`.

**В Go единица компиляции — это ПАКЕТ (package), а не отдельный файл.**
Все `.go` файлы, находящиеся в **одной директории** и объявляющие одно и то же имя пакета (например, `package main`), являются единым целым.

Это означает:
- Любая функция, структура, переменная или константа, объявленная в `greet.go`, **автоматически доступна** в `main.go` без каких-либо `import` и директив связывания!
- Идентификаторы внутри одного пакета видны друг другу независимо от того, начинаются они с заглавной или строчной буквы.
""",
        "step_by_step": """
1. В текущем модуле создаем файл `greet.go` с `package main`.
2. Объявляем функцию `sayGreeting(name string)`.
3. В `main.go` вызываем `sayGreeting("Алексей")`.
4. Запускаем проект командой `go run .` (обратите внимание на точку!).
""",
        "code_blocks": [
            {
                "filename": "greet.go",
                "lang": "go",
                "code": """package main

import "fmt"

// sayGreeting доступна во всем пакете main
func sayGreeting(name string) {
	fmt.Printf("Привет, %s! Добро пожаловать в Go.\\n", name)
}""",
                "note": "Файл greet.go в том же пакете main"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

func main() {
	// Вызов функции из greet.go напрямую без импортов
	sayGreeting("Алексей")
}""",
                "note": "Файл main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# ПРАВИЛЬНЫЙ запуск всего пакета:
go run .
# Вывод: Привет, Алексей! Добро пожаловать в Go.

# Если перечислить файлы явно:
go run main.go greet.go

# ОШИБКА новичка:
go run main.go
# # command-line-arguments
# ./main.go:4:2: undefined: sayGreeting""",
                "note": "Разница между go run . и go run main.go"
            }
        ],
        "under_the_hood": """
Когда вы выполняете `go run main.go`, компилятор парсит **только** `main.go`. Он не заглядывает в соседние файлы каталога, поэтому символ `sayGreeting` остается неразрешенным (`undefined`).
Когда вы выполняете `go run .`, тулчейн Go берет **все** файлы каталога с `package main`, строит единый AST (Abstract Syntax Tree) пакета и успешно связывает все идентификаторы.
""",
        "pitfalls": """
- **Не запускайте отдельные файлы:** Привыкайте всегда использовать `go run .` или `go build .`, а не указывать имена отдельных файлов.
- **Разные имена пакетов в одной папке:** В Go строго запрещено иметь файлы с разными объявлениями пакетов (например, `package main` и `package utils`) в одной и той же директории (исключение — тестовые пакеты с суффиксом `_test`).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Можно ли объявить в одном пакете две функции с одинаковым именем, но в разных `.go` файлах?»
**Ответ:** Нет. Поскольку областью видимости объявления на уровне пакета является весь пакет целиком, компилятор выдаст ошибку переопределения (`redeclared in this block`). Перегрузка функций (function overloading) по типам параметров в Go также отсутствует.
"""
    },
    {
        "num": 4,
        "title": "Структура программы main.go и стандартный вывод",
        "task": "Напишите файл main.go с package main и func main(), выводящий Hello, World!. Запустите через go run main.go.",
        "theory": """
Разберем анатомию базового Go-файла:
```go
package main // 1. Объявление пакета

import "fmt" // 2. Секция импорта

func main() { // 3. Точка входа в программу
    fmt.Println("Hello, World!")
}
```

В Go стандартная библиотека предоставляет пакет `fmt` (formatting), который реализует ввод и вывод по аналогии с функциями `printf`/`scanf` из языка C, но с типобезопасностью и автоматическим определением типов через рефлексию/интерфейсы `fmt.Stringer`.
""",
        "step_by_step": """
1. Создаем минимальный файл `main.go`.
2. Запускаем команду `go run main.go`.
""",
        "code_blocks": [
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import "fmt"

func main() {
	fmt.Println("Hello, World!")
}""",
                "note": "Минимальный рабочий файл"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run main.go",
                "note": "Запуск одиночного файла"
            }
        ],
        "under_the_hood": """
Функция `fmt.Println` принимает вариативный интерфейсный аргумент `...any` (или `...interface{}`). Внутри она блокирует мьютекс стандартного потока вывода `os.Stdout`, форматирует аргументы в буфер и делает системный вызов `write(2)` к файловому дескриптору `1`.
""",
        "pitfalls": """
- В Go открывающая фигурная скобка `{` функции **обязана** находиться на той же строке, что и объявление `func main()`. Если перенести её на новую строку, компилятор выдаст синтаксическую ошибку `syntax error: unexpected semicolon or newline before {`, так как лексер Go автоматически вставит `;` после круглых скобок!
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему лексер Go вставляет точки с запятой сам и как это влияет на синтаксис языка?»
**Ответ:** Разработчики Go стремились сделать язык лаконичным без визуального шума `;`. По правилу языка, если перевод строки следует за идентификатором, базовым литералом, ключевыми словами `break`, `continue`, `fallthrough`, `return` или скобками `)`, `]`, `}`, лексер подставляет точку с запятой. Поэтому `{` нельзя переносить на новую строку.
"""
    },
    {
        "num": 5,
        "title": "Первый запуск: main.go в корне модуля",
        "task": "Первый запуск: Создайте файл main.go в корне вашего модуля. Напишите базовую функцию main, которая выводит строку \"Hello, Go!\". Запустите программу напрямую без компиляции с помощью команды go run main.go.",
        "theory": """
Хотя в задаче сказано «напрямую без компиляции», следует четко понимать: **Go — это 100% компилируемый язык.** В Go нет виртуальной машины (как в JVM) и нет интерпретатора (как в Python/Node.js).

Команда `go run` компилирует код в нативный машинный код вашей архитектуры «на лету» в кэш-директории и сразу запускает получившийся бинарник. Скорость компиляции Go настолько высока (миллисекунды для небольших программ), что у разработчика создается ощущение работы со скриптовым языком.
""",
        "step_by_step": """
1. Убеждаемся, что находимся в корне модуля с файлом `go.mod`.
2. Пишем код вывода строки `"Hello, Go!"`.
3. Запускаем через `go run main.go`.
""",
        "code_blocks": [
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import "fmt"

func main() {
	fmt.Println("Hello, Go!")
}""",
                "note": "Вывод строки Hello, Go!"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run main.go",
                "note": "Запуск программы"
            }
        ],
        "under_the_hood": """
Компилятор Go (`cmd/compile`) спроектирован с упором на максимальную скорость компиляции. Он намеренно избегает тяжелых макросов C++ и сложных шаблонов подстановки на этапе компиляции, благодаря чему миллионы строк кода на Go собираются за секунды.
""",
        "pitfalls": """
- Запуск `go run` в продакшен-контейнерах Docker — антипаттерн, приводящий к утечке памяти и избыточному размеру образа (так как в образ приходится тащить весь Go SDK вместо одиночного бинарника).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «В чем разница между `go run`, `go build` и `go test` с точки зрения процесса сборки?»
**Ответ:** Все три команды используют один и тот же внутренний пайплайн компиляции (`go/build`, `cmd/compile`, `cmd/link`). `go build` сохраняет бинарник в текущую папку; `go run` компилирует во временный каталог и запускает; `go test` компилирует специальный тестовый раннер с вызовами функций `Test*` и `Benchmark*`.
"""
    },
    {
        "num": 6,
        "title": "Инициализация модуля с каноническим доменным именем",
        "task": "Инициализируйте новый модуль в пустой папке с помощью go mod init example.com/myproject.",
        "theory": """
В экосистеме Go модуль идентифицируется глобальным уникальным путем. Общепринятый стандарт — использовать URL репозитория или домен вашей компании:
- `github.com/gin-gonic/gin`
- `gitlab.ozon.ru/payments/billing-service`
- `example.com/myproject`

Зачем нужен доменный префикс?
1. **Предотвращение коллизий имен:** Если тысячи людей создадут пакет `utils`, в коде они будут четко различимы (`github.com/alice/utils` vs `github.com/bob/utils`).
2. **Децентрализованное скачивание:** Go умеет автоматически скачивать зависимости по их импортному пути через VCS (Git, Mercurial, SVN).
""",
        "step_by_step": """
1. Создаем пустую папку `myproject`.
2. Выполняем `go mod init example.com/myproject`.
3. Анализируем сгенерированный файл `go.mod`.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """mkdir -p myproject && cd myproject
go mod init example.com/myproject
cat go.mod""",
                "note": "Инициализация модуля и просмотр go.mod"
            },
            {
                "filename": "go.mod",
                "lang": "text",
                "code": """module example.com/myproject

go 1.22.5""",
                "note": "Содержимое go.mod"
            }
        ],
        "under_the_hood": """
Файл `go.mod` определяет корень модуля (module root). Любой подкаталог внутри `myproject` будет импортироваться относительно этого префикса, например `example.com/myproject/internal/handler`.
Директива `go 1.22.5` указывает компилятору минимальную поддерживаемую языковую спецификацию.
""",
        "pitfalls": """
- Не используйте в имени модуля заглавные буквы (например, `example.com/MyProject`), так как некоторые файловые системы (macOS/Windows) регистронезависимы, что может приводить к рассинхрону чексумм в кэше модулей.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что означает версия `go 1.X` в файле `go.mod` начиная с Go 1.21?»
**Ответ:** Начиная с Go 1.21, директива `go` в `go.mod` задает жесткую минимальную версию тулчейна. Если установленный компилятор Go старше (например, Go 1.20), тулчейн Go автоматически скачает нужную версию Go 1.22 через механизм Toolchain Switching!
"""
    },
    {
        "num": 7,
        "title": "Создание собственного подпакета utils и правила экспорта",
        "task": "Создай внутри модуля поддиректорию utils/ с пакетом utils. Вынеси туда функцию приветствия. Импортируй и используй её из main. Запусти программу.",
        "theory": """
Как устроен импорт локальных пакетов в Go?
1. В корне модуля объявлен `module example.com/myproject`.
2. Создавая подпапку `utils/`, мы создаем пакет `utils` (объявляемый как `package utils` в файлах внутри этой папки).
3. Путь импорта для `main.go` будет равен: `<имя_модуля>/<путь_к_папке>`, то есть `"example.com/myproject/utils"`.

**Золотое правило видимости в Go:**
- Идентификатор (функция, структура, тип, константа), начинающийся с **ЗАГЛАВНОЙ** буквы (например, `SayHello`), является **экспортируемым** (public). Он виден вне своего пакета.
- Идентификатор со **строчной** буквы (например, `sayHello`) является **неэкспортируемым** (private). Он виден только внутри пакета `utils`.
""",
        "step_by_step": """
1. Создаем папку `utils/`.
2. Создаем файл `utils/greeting.go` с `package utils` и экспортируемой функцией `SayHello(name string) string`.
3. В `main.go` импортируем `"example.com/myproject/utils"`.
4. Вызываем `utils.SayHello("Мир")`.
""",
        "code_blocks": [
            {
                "filename": "utils/greeting.go",
                "lang": "go",
                "code": """package utils

import "fmt"

// SayHello экспортируется, так как начинается с большой буквы 'S'
func SayHello(name string) string {
	return fmt.Sprintf("Здравствуйте, %s! Привет из пакета utils.", name)
}""",
                "note": "Подпакет utils"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"example.com/myproject/utils" // Полный путь импорта от корня модуля
)

func main() {
	msg := utils.SayHello("Инженер")
	fmt.Println(msg)
}""",
                "note": "Импорт и вызов из main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .\n# Вывод:\n# Здравствуйте, Инженер! Привет из пакета utils.",
                "note": "Запуск программы с подпакетом"
            }
        ],
        "under_the_hood": """
При компиляции Go строит граф зависимостей пакетов (DAG — Directed Acyclic Graph). Пакет `utils` компилируется первым в объектный файл. Затем компилируется `main`, и линкер связывает ссылки на функцию `example.com/myproject/utils.SayHello`.
""",
        "pitfalls": """
- **Попытка относительного импорта:** `import "./utils"` — в режиме Go Modules относительные пути запрещены и вызовут ошибку `relative import not allowed in module mode`. Всегда указывайте полный путь от имени модуля!
- **Имя функции с маленькой буквы:** Если назвать функцию `sayHello`, компилятор выдаст ошибку `cannot refer to unexported name utils.sayHello`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему в Go нет ключевых слов `public`, `private`, `protected`?»
**Ответ:** Простота и читаемость кода — фундаментальные ценности Go. Глядя на идентификатор в любом месте чужого кода (`pkg.DoWork()` vs `pkg.internalWork()`), вы мгновенно знаете его область видимости по первому символу без перехода к определению.
"""
    },
    {
        "num": 8,
        "title": "Инициализация модуля hello_mod и разбор структуры go.mod",
        "task": "Инициализация модуля: Создайте пустую директорию hello_mod. Инициализируйте в ней Go-модуль с помощью команды go mod init hello_mod. Убедитесь, что файл go.mod успешно создался, и изучите его содержимое.",
        "theory": """
Файл `go.mod` — это манифест проекта. Он всегда хранится в корне репозитория и содержит текстовый декларативный формат:
- `module <path>` — объявляет путь модуля;
- `go <version>` — декларирует версию языка;
- `require (...)` — фиксирует список внешних зависимостей и их версий;
- `replace (...)` — позволяет подменять пути зависимостей (например, на локальные папки);
- `exclude (...)` — исключает нежелательные/сломанные версии зависимостей.
""",
        "step_by_step": """
1. Создаем директорию `hello_mod`.
2. Выполняем `go mod init hello_mod`.
3. Смотрим и анализируем получившийся `go.mod`.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """mkdir -p hello_mod && cd hello_mod
go mod init hello_mod
cat go.mod""",
                "note": "Инициализация hello_mod"
            },
            {
                "filename": "go.mod",
                "lang": "text",
                "code": """module hello_mod

go 1.22.5""",
                "note": "Файл манифеста модуля"
            }
        ],
        "under_the_hood": """
Файл `go.mod` парсится встроенным в тулчейн пакетом `cmd/go/internal/modfile`. Он строго валидирует синтаксис. Любое ручное форматирование автоматически нормализуется командой `go mod tidy`.
""",
        "pitfalls": """
- Не создавайте файл `go.mod` вручную через текстовый редактор с нуля — всегда используйте `go mod init`, чтобы избежать синтаксических неточностей.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Зачем файл `go.mod` коммитить в Git-репозиторий?»
**Ответ:** `go.mod` гарантирует 100% воспроизводимость сборки на машинах всех разработчиков и на серверах CI/CD. Без `go.mod` проект не сможет однозначно определить используемые версии библиотек.
"""
    },
    {
        "num": 9,
        "title": "Вложенные подпакеты (utils/strings) и безопасный разворот UTF-8 строк",
        "task": "Создай ещё один подпакет utils/strings (вложенный пакет). Напиши функцию Reverse(s string) string. Импортируй оба пакета utils и utils/strings в main. Обрати внимание на пути импорта.",
        "theory": """
В Go пакеты могут быть вложены в поддиректории любой глубины:
- `hello_mod/utils`
- `hello_mod/utils/strings`

Обратите внимание: **пакет `strings` внутри `utils` никак не зависит от пакета `utils`**! Вложенность в файловой системе не создает иерархии наследования пакетов в языке. Это два абсолютно изолированных независимых пакета.

**Работа со строками в Go (Rune vs Byte):**
В Go строка `string` — это неизменяемая последовательность байт (`byte` aka `uint8`), закодированных в UTF-8.
Русские буквы в UTF-8 занимают 2 байта! Если просто развернуть байты задом наперед, как в C, русские буквы превратятся в «битые» нечитаемые символы.
Для корректной работы со строками нужно приводить строку к срезу рун `[]rune` (где `rune` — это `int32`, представляющий один символ Unicode/Code Point).
""",
        "step_by_step": """
1. Создаем структуру директорий `utils/strings/`.
2. Пишем функцию `Reverse(s string) string`, конвертируя строку в `[]rune`, разворачивая срез двумя указателями (two pointers) и возвращая `string(runes)`.
3. В `main.go` импортируем `hello_mod/utils` и `hello_mod/utils/strings`.
4. Запускаем программу и тестируем как с ASCII, так и с русским текстом.
""",
        "code_blocks": [
            {
                "filename": "utils/strings/reverse.go",
                "lang": "go",
                "code": """package strings

// Reverse корректно разворачивает UTF-8 строку по рунам (Unicode Code Points)
func Reverse(s string) string {
	runes := []rune(s)
	for i, j := 0, len(runes)-1; i < j; i, j = i+1, j-1 {
		runes[i], runes[j] = runes[j], runes[i]
	}
	return string(runes)
}""",
                "note": "Вложенный пакет utils/strings"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/utils"
	strutils "hello_mod/utils/strings" // Используем алиас, чтобы не путать со стандартным strings
)

func main() {
	greeting := utils.SayHello("Инженер")
	fmt.Println(greeting)

	original := "Привет, Go-разработчик!"
	reversed := strutils.Reverse(original)
	fmt.Printf("Исходная: %s\\n", original)
	fmt.Printf("Развернутая: %s\\n", reversed)
}""",
                "note": "Импорт обоих пакетов в main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# Здравствуйте, Инженер! Привет из пакета utils.
# Исходная: Привет, Go-разработчик!
# Развернутая: !кичтобазрар-oG ,тевирП""",
                "note": "Корректный результат разворота UTF-8"
            }
        ],
        "under_the_hood": """
Приведение `[]rune(s)` выполняет декодирование UTF-8 байтов в рантайме Go (функция `runtime.slicerunetostring` и `runtime.stringtoslicerune`), выделяя память в куче под массив `int32`. Это гарантирует корректную работу с любыми Unicode-символами и эмодзи.
""",
        "pitfalls": """
- Если бы вы назвали пакет `strings` и импортировали его без алиаса, он бы перекрыл стандартный пакет `"strings"`. При совпадении имен пакетов рекомендуется использовать явный алиас при импорте (например, `strutils "hello_mod/utils/strings"`).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Сколько байт занимает символ 'Я' в строке Go и что вернет `len(\"Я\")`?»
**Ответ:** `len(\"Я\")` вернет `2`, потому что встроенная функция `len()` для строк возвращает количество **байт**, а не символов. Символ 'Я' в кодировке UTF-8 занимает 2 байта (`0xD0 0xAF`). Количество символов можно узнать через `utf8.RuneCountInString(\"Я\")` — оно вернет `1`.
"""
    },
    {
        "num": 10,
        "title": "Разделение логики пакета: helper.go в пакете main",
        "task": "Разделение файлов: В папке с main.go создай файл helper.go (с тем же пакетом main). Напиши там простую функцию и вызови её в main(). Запусти через go run .",
        "theory": """
Частая ошибка начинающих — создавать отдельную папку под каждую мелкую функцию. В Go это считается антипаттерном («пакетомания»).
Если логика тесно связана с пакетом (в данном случае `main`), правильнее разделить код на логические файлы в той же директории:
- `main.go` — точка входа, инициализация сервера, парсинг конфигурации;
- `helper.go` (или `router.go`, `handlers.go`) — вспомогательные функции пакета `main`.
""",
        "step_by_step": """
1. В корне проекта создаем `helper.go` с `package main`.
2. Объявляем функцию `formatOutput(title, value string) string`.
3. В `main.go` вызываем `formatOutput()`.
4. Запускаем через `go run .`.
""",
        "code_blocks": [
            {
                "filename": "helper.go",
                "lang": "go",
                "code": """package main

import "fmt"

func formatOutput(title, value string) string {
	return fmt.Sprintf("=== [%s] ===\\n-> %s", title, value)
}""",
                "note": "Файл helper.go в пакете main"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import "fmt"

func main() {
	res := formatOutput("СИСТЕМНЫЙ СТАТУС", "Сервис успешно инициализирован")
	fmt.Println(res)
}""",
                "note": "Вызов из main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .\n# Вывод:\n# === [СИСТЕМНЫЙ СТАТУС] ===\n# -> Сервис успешно инициализирован",
                "note": "Запуск многофайлового пакета main"
            }
        ],
        "under_the_hood": """
Все файлы в одном пакете имеют доступ к приватным функциям и переменным друг друга. Область видимости неэкспортированных сущностей — весь **пакет**, а не файл.
""",
        "pitfalls": """
- Попытка запустить `go run main.go` вместо `go run .` приведет к ошибке линковки `undefined: formatOutput`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как компилятор определяет порядок компиляции файлов внутри одного пакета?»
**Ответ:** Файлы внутри пакета сортируются по алфавиту имен файлов. Однако благодаря двухпроходному анализу компилятора Go порядок объявления функций и их нахождение в разных файлах не влияют на их видимость: вы можете вызвать функцию в `main.go`, даже если она объявлена в `z_helper.go`.
"""
    },
    {
        "num": 11,
        "title": "Компиляция бинарного файла с кастомным именем (go build -o)",
        "task": "Используйте флаг -o при компиляции, чтобы задать кастомное имя для исполняемого файла.",
        "theory": """
Для поставки сервиса на боевой сервер (или упаковки в Docker-образ) программа компилируется в автономный бинарник командой `go build`.
По умолчанию `go build` создает бинарный файл с именем текущей директории или модуля.

Флаг `-o` (output) позволяет явно указать:
1. Имя выходного файла (например, `server`, `api_app`).
2. Директорию назначения (например, `bin/server` или `/tmp/mybin`).
""",
        "step_by_step": """
1. Создаем папку `bin/` (если нужно сложить бинарник туда).
2. Выполняем `go build -o bin/my_service .`.
3. Проверяем созданный файл через команду `ls -lh` и запускаем его напрямую: `./bin/my_service`.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Сборка бинарника в папку bin/ с именем my_service
go build -o bin/my_service .

# Проверяем размер и права исполняемого файла
ls -la bin/my_service
# -rwxr-xr-x 1 ut ut 2145632 Sep 2 12:00 bin/my_service

# Прямой запуск бинарника (без Go SDK!)
./bin/my_service""",
                "note": "Сборка и прямой запуск исполняемого файла"
            }
        ],
        "under_the_hood": """
Скомпилированный Go-бинарник содержит:
- Машинный код вашей программы;
- Машинный код всех используемых функций стандартной библиотеки;
- Рантайм Go (планировщик горутин GMP, сборщик мусора GC, аллокатор памяти).
Бинарнику не требуется установленный Go или дополнительные интерпретаторы на целевом сервере!
""",
        "pitfalls": """
- Если скомпилировать бинарник на macOS, он по умолчанию соберется под Mach-O (Darwin) и не запустится на сервере с Linux. Для серверов используется кросс-компиляция (`GOOS=linux go build`).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как уменьшить размер собранного Go-бинарника для минимального Docker-образа?»
**Ответ:** Использовать флаги линкера для удаления отладочной таблицы символов и информации DWARF: `go build -ldflags="-s -w" -o server .`. Это уменьшает размер бинарника на 30–40%.
"""
    },
    {
        "num": 12,
        "title": "Пакет mathops и идиоматичная обработка ошибок (float64, error)",
        "task": "Создай пакет mathops с функциями Add, Sub, Mul, Div. Сделай так, чтобы Div возвращала (float64, error) при делении на ноль. Протестируй все функции из main.",
        "theory": """
В Go **НЕТ исключений** (`try-catch-finally`, `throw`), как в Java, Python, C++ или PHP.

Философия Go: **Ошибки — это обычные значения (Errors are values).**
Если функция может завершиться неудачно, она возвращает пару значений: `(результат, error)`.
Если операция прошла успешно, `error` равен `nil`.
Если произошла ошибка, возвращается нулевое значение для результата и экземпляр ошибки `errors.New(...)` или `fmt.Errorf(...)`.

Вызывающая сторона **обязана** явно проверить ошибку:
```go
res, err := mathops.Div(10, 0)
if err != nil {
    // Обработка ошибки
}
```
""",
        "step_by_step": """
1. Создаем директорию `mathops/`.
2. Создаем файл `mathops/calc.go`.
3. Объявляем экспортируемые функции `Add`, `Sub`, `Mul` и `Div`.
4. Для создания ошибки используем стандартный пакет `errors`.
5. В `main.go` вызываем все операции и корректно обрабатываем деление на 0.
""",
        "code_blocks": [
            {
                "filename": "mathops/calc.go",
                "lang": "go",
                "code": """package mathops

import "errors"

// ErrDivisionByZero — стандартная экспортируемая ошибка (Sentinel Error)
var ErrDivisionByZero = errors.New("деление на ноль недопустимо")

func Add(a, b float64) float64 { return a + b }
func Sub(a, b float64) float64 { return a - b }
func Mul(a, b float64) float64 { return a * b }

func Div(a, b float64) (float64, error) {
	if b == 0 {
		return 0, ErrDivisionByZero
	}
	return a / b, nil
}""",
                "note": "Пакет mathops с обработкой ошибок"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/mathops"
)

func main() {
	fmt.Printf("Add: %.2f\\n", mathops.Add(10, 5))
	fmt.Printf("Sub: %.2f\\n", mathops.Sub(10, 5))
	fmt.Printf("Mul: %.2f\\n", mathops.Mul(10, 5))

	// Успешное деление
	val, err := mathops.Div(10, 2)
	if err != nil {
		fmt.Printf("Ошибка: %v\\n", err)
	} else {
		fmt.Printf("Div (10/2): %.2f\\n", val)
	}

	// Деление на ноль
	val0, err0 := mathops.Div(10, 0)
	if err0 != nil {
		fmt.Printf("Поймана ошибка: %v\\n", err0)
	} else {
		fmt.Printf("Div (10/0): %.2f\\n", val0)
	}
}""",
                "note": "Проверка функций в main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# Add: 15.00
# Sub: 5.00
# Mul: 50.00
# Div (10/2): 5.00
# Поймана ошибка: деление на ноль недопустимо""",
                "note": "Результат вызова mathops"
            }
        ],
        "under_the_hood": """
Интерфейс `error` в Go объявлен во встроенном типе языка:
```go
type error interface {
    Error() string
}
```
Любая структура или тип, реализующий метод `Error() string`, автоматически удовлетворяет интерфейсу `error`. Это пример утиной типизации (Structural Subtyping) в Go.
""",
        "pitfalls": """
- Игнорирование ошибки через `val, _ := mathops.Div(a, b)` — опасный антипаттерн в бэкенд-разработке. Всегда обрабатывайте ошибки явно!
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему Go использует возврат ошибок `(T, error)` вместо исключений `try-catch`?»
**Ответ:** Исключения делают поток выполнения программы невидимым и нелинейным (функция может выбросить исключение на 10 уровней стека вверх). В Go поток управления абсолютно прозрачен: каждая строка с `if err != nil` наглядно показывает возможную точку отказа и гарантирует локальную обработку.
"""
    },
    {
        "num": 13,
        "title": "Пакет geometry и правила именования пакетов (geom2d и geom3d)",
        "task": "Создай пакет geometry с подпакетами 2d и 3d. В 2d — функции площади круга и прямоугольника. В 3d — объём сферы и куба. Импортируй оба подпакета в main. Обрати внимание на то, что имена пакетов не должны начинаться с цифры — придумай правильные имена.",
        "theory": """
**Правила именования пакетов в спецификации языка Go:**
1. Имя пакета должно быть допустимым идентификатором Go: оно должно начинаться с буквы Unicode или подчеркивания `_`, но **НЕ МОЖЕТ начинаться с цифры**.
   - `package 2d` — **синтаксическая ошибка компилятора!**
   - `package geom2d` или `package twod` — корректные имена.
2. Имена пакетов в Go принято делать:
   - Короткими и ясными;
   - Строчными буквами (lowercase);
   - Без camelCase и без snake_case (не `geom_2d`, а `geom2d`).
   - В единственном числе (`user`, а не `users`).
""",
        "step_by_step": """
1. Создаем структуру директорий `geometry/geom2d` и `geometry/geom3d`.
2. В `geometry/geom2d/shapes.go` реализуем `CircleArea(r float64)` и `RectangleArea(w, h float64)` с использованием константы `math.Pi`.
3. В `geometry/geom3d/solids.go` реализуем `SphereVolume(r float64)` и `CubeVolume(a float64)`.
4. Импортируем оба пакета в `main.go` и рассчитываем значения.
""",
        "code_blocks": [
            {
                "filename": "geometry/geom2d/shapes.go",
                "lang": "go",
                "code": """package geom2d

import "math"

func CircleArea(radius float64) float64 {
	return math.Pi * radius * radius
}

func RectangleArea(width, height float64) float64 {
	return width * height
}""",
                "note": "Подпакет geom2d"
            },
            {
                "filename": "geometry/geom3d/solids.go",
                "lang": "go",
                "code": """package geom3d

import "math"

func SphereVolume(radius float64) float64 {
	return (4.0 / 3.0) * math.Pi * math.Pow(radius, 3)
}

func CubeVolume(side float64) float64 {
	return math.Pow(side, 3)
}""",
                "note": "Подпакет geom3d"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/geometry/geom2d"
	"hello_mod/geometry/geom3d"
)

func main() {
	circle := geom2d.CircleArea(5)
	rect := geom2d.RectangleArea(4, 6)
	sphere := geom3d.SphereVolume(3)
	cube := geom3d.CubeVolume(3)

	fmt.Printf("Площадь круга (r=5): %.2f\\n", circle)
	fmt.Printf("Площадь прямоуг. (4x6): %.2f\\n", rect)
	fmt.Printf("Объем сферы (r=3): %.2f\\n", sphere)
	fmt.Printf("Объем куба (a=3): %.2f\\n", cube)
}""",
                "note": "Импорт геометрических пакетов"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# Площадь круга (r=5): 78.54
# Площадь прямоуг. (4x6): 24.00
# Объем сферы (r=3): 113.10
# Объем куба (a=3): 27.00""",
                "note": "Расчет геометрических формул"
            }
        ],
        "under_the_hood": """
Путь к директории в файловой системе (`geometry/geom2d`) может отличаться от имени пакета внутри файла, но по канонам Go **имя последней директории должно в точности совпадать с именем объявленного пакета**. Это исключает путаницу при импорте.
""",
        "pitfalls": """
- Попытка назвать пакет `package 2d` приведет к ошибке: `expected 'IDENT', found 2d`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему в Go не рекомендуется называть пакеты абстрактными именами вроде `util`, `helpers`, `common`, `base`?»
**Ответ:** Пакеты с размытыми именами становятся «мусорными ведрами» для разнородного кода, нарушая принцип единственной ответственности (SRP) и быстро приводя к циклическим зависимостям (`import cycle not allowed`). Пакет должен решать одну конкретную задачу (например, `geom2d`, `httpclient`, `hasher`).
"""
    },
    {
        "num": 14,
        "title": "Пакет mathutils и экспортируемые функции",
        "task": "Свой пакет: Создай директорию mathutils. В ней создай пакет mathutils с экспортируемой функцией Add (начинается с большой буквы). Импортируй её в main.go и используй.",
        "theory": """
Закрепляем фундаментальный принцип организации библиотечного кода:
- Имя пакета объявляется через `package mathutils`.
- Функция `Add(a, b int) int` доступна внешнему миру, потому что `A` — заглавная буква.
- При вызове из `main.go` мы пишем: `mathutils.Add(10, 20)`.
""",
        "step_by_step": """
1. Создаем папку `mathutils`.
2. Создаем `mathutils/mathutils.go`.
3. Импортируем в `main.go` и проверяем вычисление.
""",
        "code_blocks": [
            {
                "filename": "mathutils/mathutils.go",
                "lang": "go",
                "code": """package mathutils

// Add складывает два целых числа и возвращает результат
func Add(a, b int) int {
	return a + b
}""",
                "note": "Пакет mathutils"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/mathutils"
)

func main() {
	sum := mathutils.Add(40, 2)
	fmt.Printf("Результат mathutils.Add: %d\\n", sum)
}""",
                "note": "Вызов Add в main.go"
            }
        ],
        "under_the_hood": """
Символ `Add` помещается компилятором в таблицу экспортируемых символов объектного файла с полным квалифицированным именем `hello_mod/mathutils.Add`.
""",
        "pitfalls": """
- Забыть букву `A` в верхнем регистре (`func add`) — внешние пакеты не увидят эту функцию.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Экспортируются ли поля структуры, если сама структура экспортируема, а поля начинаются с маленькой буквы?»
**Ответ:** Нет. В Go экспортируемость гранулярна. Если структура `User` объявлена с полями `Name string` и `passwordHash string`, то поле `Name` будет видно вне пакета, а `passwordHash` останется доступным исключительно внутри пакета `User`.
"""
    },
    {
        "num": 15,
        "title": "Многофайловый пакет и компиляция через go build",
        "task": "Создайте второй файл в той же папке с тем же package main и разнесите логику между двумя файлами. Скомпилируйте проект.",
        "theory": """
При разработке реальных микросервисов пакет `main` часто разбивают на:
- `main.go` — создание контекста приложения, перехват сигналов ОС (SIGINT, SIGTERM для Graceful Shutdown);
- `app.go` — создание зависимостей (подключение к БД, инициализация роутеров, запуск HTTP-сервера).

При сборке через `go build .` компилятор автоматически объединит все `.go` файлы текущей папки в один бинарный артефакт.
""",
        "step_by_step": """
1. Создаем файл `app.go` с функцией `runApp()`.
2. В `main.go` вызываем `runApp()`.
3. Компилируем проект командой `go build .` и проверяем готовый бинарник.
""",
        "code_blocks": [
            {
                "filename": "app.go",
                "lang": "go",
                "code": """package main

import "fmt"

func runApp() {
	fmt.Println("[APP] Инициализация сервиса...")
	fmt.Println("[APP] Загрузка конфигурации завершена.")
	fmt.Println("[APP] Сервис готов принимать трафик.")
}""",
                "note": "Файл app.go"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

func main() {
	runApp()
}""",
                "note": "Файл main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Компиляция всего пакета main
go build -o server .

# Запуск скомпилированного сервиса
./server
# Вывод:
# [APP] Инициализация сервиса...
# [APP] Загрузка конфигурации завершена.
# [APP] Сервис готов принимать трафик.""",
                "note": "Сборка и запуск бинарника"
            }
        ],
        "under_the_hood": """
Компилятор Go компилирует файлы `app.go` и `main.go` параллельно в промежуточные представления SSA (Static Single Assignment), а затем линкер компонует их вместе с рантаймом Go в единый исполняемый файл ELF (на Linux) или Mach-O (на macOS).
""",
        "pitfalls": """
- Создание файлов с суффиксами `_test.go` — они игнорируются при обычной сборке `go build` и компилируются только при вызове `go test`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что такое Dead Code Elimination в компиляторе Go?»
**Ответ:** На этапе линковки компилятор строит граф вызовов от функции `main.main` и инициализаторов `init()`. Любые неиспользуемые функции, методы и структуры полностью вырезаются из итогового бинарника, что уменьшает его размер и оптимизирует кэш инструкций процессора.
"""
    }
]

if __name__ == '__main__':
    print(f"Loaded Section 1: {len(exercises)} exercises.")
