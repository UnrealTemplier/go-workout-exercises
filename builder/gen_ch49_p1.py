# -*- coding: utf-8 -*-
exercises = [
  {
    "num": 1,
    "title": "Архитектура аллокатора памяти Go (TCMalloc)",
    "task": "**Архитектура аллокатора (TCMalloc)**: Опишите трехуровневую иерархию аллокатора кучи Go в комментариях к коду [483]:\n    * `mcache` — персональный локальный кэш каждого процессора `P` для сверхбыстрой аллокации мелких объектов без блокировок [483].\n    * `mcentral` — центральный пул свободных спанов, сгруппированных по классам размеров (size classes), защищенный мьютексом [483].\n    * `mheap` — глобальная куча, запрашивающая страницы памяти напрямую у операционной системы [483].",
    "theory": "Аллокатор кучи Go основан на архитектуре TCMalloc (Thread-Caching Malloc), адаптированной для работы в рантайме Go поверх абстракции планировщика GMP.\n\nВместо привязки к потокам ОС (Threads), как в классическом C++ TCMalloc, локальный кэш в Go привязан к логическим процессорам $P$ (Processor). Это обеспечивает трехуровневую иерархию:\n1. **`mcache` (L1 кэш):** У каждого $P$ есть собственный дескриптор `mcache`. Поскольку горутина выполняется на конкретном $P$ в однопоточном режиме, выделение памяти из `mcache` происходит **без единой атомарной блокировки** (Lock-Free Fast Path).\n2. **`mcentral` (L2 пул):** Центральное хранилище спанов (`mspan`), сгруппированное по классам размеров (всего 67 классов). Доступ к `mcentral` защищен мьютексом. Сюда $P$ обращается, только когда в его `mcache` закончились свободные ячейки заданного размера.\n3. **`mheap` (L3 глобальная куча):** Управляет всей виртуальной памятью процесса, разбитой на страницы по 8 КБ. Выделяет спаны для `mcentral` и запрашивает арены памяти у ядра ОС через системные вызовы `mmap` (Linux) или `VirtualAlloc` (Windows).",
    "step_by_step": "1. Напишите демонстрационную программу с подробным документированием трехуровневой архитектуры в комментариях.\n2. Продемонстрируйте выделение объектов различных размеров в куче.\n3. С помощью `runtime.ReadMemStats` снимите показания системных счетчиков кучи `HeapAlloc`, `HeapSys` и `HeapInuse`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n)\n\n// Демонстрация трехуровневой модели аллокатора TCMalloc в Go:\n//\n// 1. mcache:  Per-P кэш (Lock-free). Запрос до 32 КБ обслуживается за единицы наносекунд.\n// 2. mcentral: Central pool по классам размеров (с блокировкой спинлока).\n// 3. mheap:    Глобальная куча (8 КБ страницы, arenas, обращение к ОС через mmap).\n\ntype SmallObject struct {\n\tID   int64\n\tData [24]byte\n}\n\nfunc main() {\n\tvar m1, m2 runtime.MemStats\n\truntime.ReadMemStats(&m1)\n\n\t// Аллоцируем 1000 небольших объектов (размер 32 байта, обслуживаются mcache)\n\tholders := make([]*SmallObject, 1000)\n\tfor i := 0; i < len(holders); i++ {\n\t\tholders[i] = &SmallObject{ID: int64(i)}\n\t}\n\n\truntime.ReadMemStats(&m2)\n\n\tfmt.Println(\"=== Архитектура TCMalloc в Go ===\")\n\tfmt.Printf(\"Логических P (mcache пулов): %d\\n\", runtime.GOMAXPROCS(0))\n\tfmt.Printf(\"HeapAlloc (живые объекты)  : %d байт\\n\", m2.HeapAlloc-m1.HeapAlloc)\n\tfmt.Printf(\"HeapInuse (занятые спаны)  : %d байт\\n\", m2.HeapInuse)\n\tfmt.Printf(\"HeapSys   (память от ОС)   : %d байт\\n\", m2.HeapSys)\n\tfmt.Printf(\"Всего mallocs              : %d\\n\", m2.Mallocs-m1.Mallocs)\n\n\t_ = holders\n}\n",
        "note": "Структура аллокатора кучи Go и чтение метрик runtime.MemStats"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n"
      }
    ],
    "under_the_hood": "В исходниках Go (`src/runtime/malloc.go`) функция `mallocgc(size uintptr, typ *_type, needzero bool) unsafe.Pointer` координирует все три уровня:\n- Если `size <= maxSmallSize` (32 КБ), запрашивается span нужного класса из `getg().m.p.ptr().mcache.alloc[spc]`.\n- Если спан в `mcache` пуст, вызывается `mcache.refill(spc)`, обращающийся к `mcentral[spc]` с захватом блокировки `mcentral.lock`.\n- Если и в `mcentral` нет свободных спанов, вызывается `mheap_.alloc(npages, spc)`, которая выделяет страницы из bitmap-аллокатора `pageAlloc`.",
    "pitfalls": "1. Ошибочно полагать, что `mcache` привязан к потокам ОС $M$. В Go `mcache` привязан к логическим процессорам $P$. При переключении горутины на другой $M$ горутина использует `mcache` того $P$, на котором она сейчас исполняется.\n2. Объекты размером более 32 КБ считаются Large Objects и никогда не попадают в `mcache` или `mcentral` — они сразу выделяются из `mheap` под глобальным локом.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Go аллокатор памяти привязан к абстракции $P$ (Processor), а не к $M$ (OS Thread) или $G$ (Goroutine)?\n**Ответ:** \n- Если бы кэш был у $G$, миллионы горутин расходовали бы гигабайты памяти на структуры аллокатора.\n- Если бы кэш был у $M$, то при блокировке потока на системном вызове память в его кэше простаивала бы впустую.\n- Число $P$ строго ограничено значением `GOMAXPROCS` (обычно равно числу ядер CPU). Привязка к $P$ гарантирует, что число локальных кэшей минимально, конкуренция между ядрами отсутствует, а память расходуется предельно эффективно."
  },
  {
    "num": 2,
    "title": "Анализ побега переменных в кучу Escape Analysis",
    "task": "**Анализ ухода переменных в кучу (Escape Analysis)**: Напишите три примера кода:\n    * Где переменная гарантированно остается на стеке горутины.\n    * Где переменная \"утекает\" (escapes) в кучу (например, возврат указателя на локальную переменную из функции).\n    * Где переменная утекает в кучу из-за динамического выделения памяти (слайс с переменной длиной `make([]int, n)`).\n    Запустите компиляцию с подробным выводом анализа escape-анализа: `go build -gcflags=\"-m\"` и проверьте правильность ваших гипотез по отчету компилятора.",
    "theory": "Escape Analysis (анализ побега) — это фаза компилятора Go, которая на этапе сборки статически определяет, может ли время жизни переменной превысить время жизни фрейма стека функции, в которой она создана.\n\nПравила анализа побега:\n1. **Стек (Stack):** Если компилятор может доказать, что переменная не используется после возврата из функции, и её размер известен на этапе компиляции, память выделяется на стеке горутины. Выделение сводится к вычитанию из регистра `SP` (0 накладных расходов, не требует работы GC).\n2. **Куча (Heap):** Если указатель на переменную возвращается наружу, сохраняется в глобальную переменную, передается в замыкание или размер среза неизвестен на этапе компиляции (`make([]T, n)`), память выделяется в куче (`runtime.newobject`).",
    "step_by_step": "1. Напишите функцию `stackAlloc`, где локальная структура не выходит за пределы функции.\n2. Напишите функцию `escapePointer`, возвращающую указатель на локальную структуру.\n3. Напишите функцию `escapeDynamicSlice`, аллоцирующую срез переменной длины `n`.\n4. Скомпилируйте код с флагом `-gcflags=\"-m\"` и найдите диагностические сообщения компилятора.",
    "code_blocks": [
      {
        "filename": "escape.go",
        "lang": "go",
        "code": "package main\n\ntype Item struct {\n\tID   int\n\tVal  int64\n}\n\n// 1. Гарантированно остается на стеке: передача по значению, размер фиксирован\n//go:noinline\nfunc stackAlloc() int64 {\n\tit := Item{ID: 1, Val: 100}\n\treturn it.Val * 2\n}\n\n// 2. Утекает в кучу: возврат указателя наружу из стекового фрейма\n//go:noinline\nfunc escapePointer() *Item {\n\tit := Item{ID: 2, Val: 200}\n\treturn &it // &it escapes to heap\n}\n\n// 3. Утекает в кучу: размер n динамический, неизвестен на этапе компиляции\n//go:noinline\nfunc escapeDynamicSlice(n int) []int {\n\tslice := make([]int, n) // make([]int, n) escapes to heap\n\tfor i := 0; i < n; i++ {\n\t\tslice[i] = i\n\t}\n\treturn slice\n}\n\nfunc main() {\n\tv := stackAlloc()\n\tptr := escapePointer()\n\tsl := escapeDynamicSlice(5)\n\n\t_ = v\n\t_ = ptr\n\t_ = sl\n}\n",
        "note": "Три классических сценария Escape Analysis в компиляторе Go"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "# Запуск компилятора с флагом escape analysis\ngo build -gcflags=\"-m\" escape.go\n"
      }
    ],
    "under_the_hood": "Компилятор строит граф потока данных (Directed Data Flow Graph).\nУзлами графа являются переменные и выражения, ребрами — операции присваивания и взятия адреса `&`.\nКомпилятор выполняет поиск связей с корнями (внешними указателями).\nЕсли от переменной существует путь до возвращаемого значения, глобальной переменной или интерфейса `any`, вес узла становится бесконечным (`EscHeap`), и генератор SSA вставляет вызов `runtime.newobject`.",
    "pitfalls": "1. Передача примитивных переменных в `fmt.Println(x)` заставляет `x` утекать в кучу, так как `Println` принимает аргументы типа `...any`.\n2. Слишком большие структуры (больше 64 КБ) даже при локальном использовании в функции могут вытесняться в кучу, чтобы не переполнять стартовый 2 КБ стек горутины.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему возврат указателя на локальную переменную в C приводит к Segmentation Fault (висячий указатель), а в Go является абсолютно безопасной и идиоматичной операцией?\n**Ответ:** В C компилятор всегда размещает локальные переменные на стеке вызываемой функции, который уничтожается при возврате инструкции `RET`. В Go компилятор проводит Escape Analysis: обнаружив возврат указателя, он гарантированно аллоцирует переменную не на стеке, а в управляемой куче (heap), где за её временем жизни следит Garbage Collector."
  },
  {
    "num": 3,
    "title": "Структура mspan и внутренняя фрагментация памяти",
    "task": "**Понимание спанов (`mspan`) и фрагментации**: Память в Go нарезается на страницы (pages) размером 8 КБ, которые объединяются в структуры `mspan`. Каждый спан нарезается на ячейки фиксированного размера (size classes, всего около 67 классов). Объясните в комментариях концепцию внутренней фрагментации памяти: почему при аллокации структуры размером 33 байта рантайм выделит под неё ячейку из класса 48 байт, и куда деваются \"лишние\" 15 байт.",
    "theory": "Основой аллокатора Go является структура `mspan` — непрерывный блок виртуальной памяти, состоящий из одной или нескольких страниц фиксированного размера **8192 байта (8 КБ)**.\n\nКаждый `mspan` привязан к определенному **классу размера (Size Class)**. Например:\n- Класс 1: 8 байт\n- Класс 2: 16 байт\n- Класс 3: 24 байта\n- Класс 4: 32 байта\n- Класс 5: 48 байт\n- ... всего 67 классов вплоть до 32 КБ.\n\n**Внутренняя фрагментация (Internal Fragmentation):**\nКогда программа запрашивает объект размером 33 байта, аллокатор не может нарезать память произвольным куском. Он округляет запрос вверх до ближайшего класса размера — 48 байт.\nРазница между размером слота (48 байт) и реальным размером объекта (33 байта) составляет 15 байт. Этот остаток остается неиспользованным внутри слота — это плата за устранение внешней фрагментации и скорость аллокации $O(1)$.",
    "step_by_step": "1. Напишите код со структурами пограничных размеров (например, 32 байта и 33 байта).\n2. Замерьте аллокацию памяти для 100 000 таких структур.\n3. Продемонстрируйте математический расчет потерь памяти на внутреннюю фрагментацию.",
    "code_blocks": [
      {
        "filename": "fragmentation.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"unsafe\"\n)\n\ntype Exact32 struct {\n\tData [32]byte\n}\n\ntype Size33 struct {\n\tData [33]byte\n}\n\nfunc main() {\n\tfmt.Printf(\"Размер Exact32: %d байт (попадает в класс 32Б, оверхед = 0 байт)\\n\",\n\t\tunsafe.Sizeof(Exact32{}))\n\tfmt.Printf(\"Размер Size33 : %d байт (попадает в класс 48Б, оверхед = 15 байт / 31.2%%)\\n\\n\",\n\t\tunsafe.Sizeof(Size33{}))\n\n\tconst count = 100_000\n\n\t// Тест 1: Аллокация 100k объектов по 32 байта\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\ts32 := make([]*Exact32, count)\n\tfor i := 0; i < count; i++ {\n\t\ts32[i] = new(Exact32)\n\t}\n\truntime.ReadMemStats(&m2)\n\talloc32 := m2.HeapAlloc - m1.HeapAlloc\n\n\t// Тест 2: Аллокация 100k объектов по 33 байта\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\ts33 := make([]*Size33, count)\n\tfor i := 0; i < count; i++ {\n\t\ts33[i] = new(Size33)\n\t}\n\truntime.ReadMemStats(&m2)\n\talloc33 := m2.HeapAlloc - m1.HeapAlloc\n\n\tfmt.Printf(\"Память под 100k объектов Exact32 : %d КБ (в среднем %.1f байт/объект)\\n\",\n\t\talloc32/1024, float64(alloc32)/count)\n\tfmt.Printf(\"Память под 100k объектов Size33  : %d КБ (в среднем %.1f байт/объект)\\n\",\n\t\talloc33/1024, float64(alloc33)/count)\n\n\t_ = s32\n\t_ = s33\n}\n",
        "note": "Экспериментальный замер потерь памяти на внутреннюю фрагментацию mspan"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run fragmentation.go\n"
      }
    ],
    "under_the_hood": "В структуре `mspan` (файл `runtime2.go`):\n- `startAddr uintptr`: начальный виртуальный адрес спана.\n- `npages uintptr`: число 8 КБ страниц в спане (от 1 до десятков страниц).\n- `spanclass spanClass`: класс размера (младший бит определяет наличие указателей noscan/scan).\n- `allocBits *gcBits`: битовая карта свободных/занятых слотов. Аллокация свободного слота выполняется процессорной инструкцией `CTZ` (Count Trailing Zeros) за 1 такт!\n- `nelems uintptr`: суммарное количество объектов, умещающихся в спане.",
    "pitfalls": "1. Увеличение структуры всего на 1 байт (например, с 32 до 33 байт) может привести к скачку потребления памяти приложением на 50% из-за перехода в следующий класс размера.\n2. Проектируя структуры, которые создаются миллионами штук, всегда проверяйте попадание `unsafe.Sizeof(T)` точно в границы size classes Go (8, 16, 24, 32, 48, 64, 80, 96...).",
    "bigtech_interview": "**Вопрос с собеседования:** В чем фундаментальная разница между внутренней (internal) и внешней (external) фрагментацией памяти? Какую проблему решил Go?\n**Ответ:** \n- **Внешняя фрагментация:** память дробится на разношерстные кусочки между занятыми блоками так, что суммарно свободного места много, но непрерывный кусок выделить невозможно. Традиционные аллокаторы `malloc` страдают от этого.\n- **Внутренняя фрагментация:** неиспользованное пространство внутри выделенного слота фиксированного размера.\nGo нарезает спаны на фиксированные классы размеров (size classes), полностью побеждая внешнюю фрагментацию ценой контролируемой внутренней фрагментации (в среднем не более 12.5%)."
  },
  {
    "num": 4,
    "title": "Динамический рост стека горутин: Copystack и newstack",
    "task": "**Динамический рост стека**: Стек горутины в Go начинается всего с 2 КБ. Напишите рекурсивную функцию глубокой вложенности (например, вычисление факториала без оптимизации хвостовой рекурсии). Объясните в комментариях, как Go расширяет стек: при достижении лимита рантайм выделяет новый сегмент памяти в куче (в 2 раза больше предыдущего), полностью копирует туда текущий стек и автоматически корректирует все указатели (stack split/growth).",
    "theory": "В отличие от большинства языков программирования (C, Java, Rust), где каждому потоку выделяется фиксированный стек размером от 1 до 8 МБ, в Go используется механизм **непрерывных стеков (Contiguous Stacks)**:\n1. Стартовый размер стека горутины составляет всего **2048 байт (2 КБ)**.\n2. Перед вызовом каждой функции компилятор вставляет проверку преамбулы (сравнение регистра `SP` с `g.stackguard0`).\n3. Если текущего стека недостаточно, вызывается переход на системный стек `g0` и функция `runtime.newstack()`.\n4. Рантайм аллоцирует новый блок памяти удвоенного размера (4 КБ, 8 КБ, 16 КБ ... до 1 ГБ на 64-битных ОС).\n5. Функция `runtime.copystack` копирует фреймы и выполняет обход всех указателей, корректируя адреса со старого стека на новый (Pointer Adjustment).\n6. Старый блок памяти стека освобождается.",
    "step_by_step": "1. Напишите рекурсивную функцию с локальным массивом, гарантированно превышающим 2 КБ при погружении.\n2. Сохраняйте адреса локальных переменных на разных глубинах рекурсии.\n3. Продемонстрируйте факт переноса стека в новую область виртуальной памяти.",
    "code_blocks": [
      {
        "filename": "stack_growth.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"unsafe\"\n)\n\n// Рекурсивная функция для провокации удвоения стека\n//go:noinline\nfunc recursiveCall(depth int, parentAddr uintptr) {\n\tvar localData [128]byte // 128 байт на каждый стек-фрейм\n\tcurrentAddr := uintptr(unsafe.Pointer(&localData[0]))\n\n\tif parentAddr != 0 {\n\t\tdiff := int64(currentAddr - parentAddr)\n\t\t// Если разница адресов резко изменила порядок или знак, значит стек переехал!\n\t\tif diff < -4096 || diff > 4096 {\n\t\t\tfmt.Printf(\"[Глубина %2d] СТЕК ПЕРЕМЕЩЕН! Старый адрес: 0x%x, Новый адрес: 0x%x\\n\",\n\t\t\t\tdepth, parentAddr, currentAddr)\n\t\t}\n\t}\n\n\tif depth < 30 {\n\t\trecursiveCall(depth+1, currentAddr)\n\t}\n}\n\nfunc main() {\n\tfmt.Println(\"Наблюдение за динамическим ростом и перемещением стека:\")\n\trecursiveCall(1, 0)\n\tfmt.Println(\"Рекурсия завершена успешно.\")\n}\n",
        "note": "Отслеживание скачка адресов локальных переменных при удвоении стека"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run stack_growth.go\n"
      }
    ],
    "under_the_hood": "В фазе `copystack()` (файл `stack.go`):\n1. Выделяется новый стек размера `newsize = oldsize * 2`.\n2. Содержимое старого стека копируется через `memmove`.\n3. Запускается функция `gentraceback`, которая проходит по всем фреймам вызовов.\n4. По метаданным типов (`gcwork`) рантайм находит все указатели, которые указывали на переменные, лежавшие на старом стеке, и прибавляет к ним дельту смещения: `*p = *p + (new.lo - old.lo)`.\n5. Переключаются регистры `SP` и `FP`, после чего выполнение продолжается без единой ошибки.",
    "pitfalls": "1. Сохранение адреса стековой переменной в тип `uintptr` (например, для последующего каста в `unsafe.Pointer`) смертельно опасно! При росте стека рантайм не обновляет переменные типа `uintptr`, и они превращаются в висячие указатели.\n2. Предельный размер стека по умолчанию — 1 ГБ на 64-битных системах. При превышении этого лимита рантайм аварийно завершает процесс с фатальной ошибкой `fatal error: stack overflow`.",
    "bigtech_interview": "**Вопрос с собеседования:** С какой версии Go перешел на непрерывные стеки (Contiguous Stacks) и какую фундаментальную проблему старых сегментированных стеков (Segmented Stacks) это решило?\n**Ответ:** Go перешел на непрерывные стеки в релизе **Go 1.4**. До этого использовались сегментированные стеки (связный список блоков по 2–4 КБ). Это приводило к «проблеме горячего разделения» (Hot Split Problem): если вызов функции внутри высоконагруженного цикла попадал ровно на границу сегмента, рантайм на каждой итерации циклически аллоцировал новый сегмент и тут же освобождал его при возврате, замедляя выполнение в сотни раз. Непрерывные стеки удваиваются плавно с амортизированным временем $O(1)$."
  },
  {
    "num": 5,
    "title": "Классы размеров Size Classes и наблюдение в runtime.MemStats",
    "task": "**Size classes**: Изучите size classes в Go (8, 16, 32, 48, 64, 80, 96, ... до 32KB). Каждая аллокация округляется до ближайшего size class. Используйте `runtime.MemStats` для наблюдения.",
    "theory": "Вся память мелких объектов (Small Objects) от 1 байта до 32768 байт (32 КБ) в Go квантуется на **67 классов размеров (Size Classes)**.\nОни жестко зафиксированы в исходном файле `src/runtime/sizeclasses.go`.\n\nПримеры базовых классов:\n- Класс 0: резерв (крупные объекты)\n- Класс 1: 8 байт\n- Класс 2: 16 байт\n- Класс 3: 24 байта\n- Класс 4: 32 байта\n- Класс 5: 48 байт\n- Класс 6: 64 байта\n- ...\n- Класс 67: 32768 байт (32 КБ).\n\nВ структуре `runtime.MemStats` массив `BySize [68]struct { Size uint32; Mallocs uint64; Frees uint64 }` ведет накопительный аудит количества аллокаций и освобождений памяти по каждому классу размера.",
    "step_by_step": "1. Запросите текущие метрики аллокатора через `runtime.ReadMemStats`.\n2. Создайте контролируемую нагрузку из объектов определенного размера (например, по 24 байта).\n3. Снова считайте `runtime.ReadMemStats` и найдите в массиве `BySize` класс, в котором увеличился счетчик `Mallocs`.",
    "code_blocks": [
      {
        "filename": "sizeclasses.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n)\n\ntype Object24 struct {\n\tA, B, C int64 // 3 * 8 = 24 байта\n}\n\nfunc main() {\n\tvar m1, m2 runtime.MemStats\n\truntime.ReadMemStats(&m1)\n\n\tconst count = 50_000\n\tslice := make([]*Object24, count)\n\tfor i := 0; i < count; i++ {\n\t\tslice[i] = &Object24{A: int64(i)}\n\t}\n\n\truntime.ReadMemStats(&m2)\n\n\tfmt.Println(\"Анализ распределения объектов по Size Classes (runtime.MemStats):\")\n\tfmt.Printf(\"%-10s %-12s %-12s %-12s\\n\", \"Class\", \"Размер (B)\", \"Mallocs\", \"Дельта\")\n\tfmt.Println(\"--------------------------------------------------\")\n\n\tfor i := 1; i < len(m2.BySize); i++ {\n\t\tb1 := m1.BySize[i]\n\t\tb2 := m2.BySize[i]\n\t\tdelta := b2.Mallocs - b1.Mallocs\n\n\t\tif delta > 0 || b2.Size == 24 || b2.Size == 32 {\n\t\t\tfmt.Printf(\"Class %-5d %-12d %-12d +%-12d\\n\",\n\t\t\t\ti, b2.Size, b2.Mallocs, delta)\n\t\t}\n\t}\n\n\t_ = slice\n}\n",
        "note": "Инспекция статистики аллокаций по size classes через runtime.MemStats.BySize"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run sizeclasses.go\n"
      }
    ],
    "under_the_hood": "Таблица классов в `sizeclasses.go` генерируется автоматически специальным скриптом при компиляции инструментария Go:\n`go run mksizeclasses.go > sizeclasses.go`.\nШаг между классами размеров составляет около 12.5% (плюс округление до размеров машинного слова и страниц).\nЭто обеспечивает максимальный теоретический предел внутренней фрагментации не более 12.5% при любом распределении размеров объектов в приложении.",
    "pitfalls": "1. `runtime.ReadMemStats()` останавливает мир (STW пауза на доли миллисекунды) для агрегации метрик из всех $P$. Не вызывайте его на каждый HTTP-запрос в production!\n2. Для частого мониторинга в Go 1.16+ используйте пакет `runtime/metrics`, который читает счетчики без STW.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему верхняя граница мелких объектов (Small Objects) в Go выбрана равной именно 32 КБ, а не 4 КБ или 1 МБ?\n**Ответ:** По статистике Google и авторов Go, более 95% аллокаций в типичных веб-сервисах имеют размер от десятков байт до нескольких килобайт. Лимит в 32 КБ позволяет покрыть подавляющее большинство рабочих объектов кэшем `mcache`, сохраняя размер структуры `mcache` компактным (не более десятков килобайт на одно ядро процессора)."
  },
  {
    "num": 6,
    "title": "Категории аллокаций: Tiny, Small и Large Allocations",
    "task": "**Tiny, Small, Large allocations:**\n    * Tiny: < 16 bytes (special handling, может объединять несколько аллокаций)\n    * Small: 16 bytes - 32 KB (через mspan)\n    * Large: > 32 KB (напрямую из mheap)",
    "theory": "Аллокатор Go делит все запросы памяти на 3 принципиально разные категории в зависимости от размера:\n\n1. **Tiny Allocations (< 16 байт без указателей):**\n   - Мелкие структуры, строки, числовые переменные.\n   - Обслуживаются специальным **Tiny Allocator** внутри `mcache`.\n   - Несколько независимых мелких объектов объединяются (упаковываются) в один 16-байтный блок, что экономит память и снижает фрагментацию.\n2. **Small Allocations (16 байт – 32 КБ):**\n   - Округляются до ближайшего из 67 классов размеров.\n   - Выделяются из соответствующего `mspan` в `mcache` за 1-2 процессорных такта без локов.\n3. **Large Allocations (> 32 КБ):**\n   - Большие срезы, буферы и крупные структуры.\n   - Полностью минуют `mcache` и `mcentral`.\n   - Выделяются напрямую из глобальной кучи `mheap` страницами по 8 КБ под глобальной блокировкой `mheap.lock`.",
    "step_by_step": "1. Напишите код, выполняющий аллокации всех трех типов: Tiny (4 байта), Small (512 байт) и Large (128 КБ).\n2. Зафиксируйте адреса памяти и убедитесь в упаковке крошечных объектов.\n3. Исследуйте накладные расходы времени выполнения на каждый тип аллокации.",
    "code_blocks": [
      {
        "filename": "alloc_types.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"unsafe\"\n)\n\ntype TinyObj struct {\n\tX uint16 // 2 байта\n\tY uint16 // 2 байта (итого 4 байта < 16B, без указателей)\n}\n\ntype SmallObj struct {\n\tBuffer [512]byte // 512 байт (16B <= size <= 32KB)\n}\n\nfunc main() {\n\tfmt.Println(\"=== Демонстрация категорий аллокаций в Go ===\")\n\n\t// 1. Tiny Allocation (< 16B)\n\tt1 := new(TinyObj)\n\tt2 := new(TinyObj)\n\tt1.X, t2.X = 1, 2\n\taddr1 := uintptr(unsafe.Pointer(t1))\n\taddr2 := uintptr(unsafe.Pointer(t2))\n\n\tfmt.Printf(\"Tiny 1: адрес 0x%x (размер %d байт)\\n\", addr1, unsafe.Sizeof(*t1))\n\tfmt.Printf(\"Tiny 2: адрес 0x%x (размер %d байт)\\n\", addr2, unsafe.Sizeof(*t2))\n\tfmt.Printf(\"Дельта адресов Tiny объектов: %d байт (упакованы в один блок!)\\n\\n\", addr2-addr1)\n\n\t// 2. Small Allocation (16B - 32KB)\n\ts := new(SmallObj)\n\tfmt.Printf(\"Small: адрес %p (размер %d байт, класс 512B в mcache)\\n\\n\",\n\t\ts, unsafe.Sizeof(*s))\n\n\t// 3. Large Allocation (> 32KB)\n\tconst largeSize = 64 * 1024 // 64 КБ > 32 КБ\n\tlargeSlice := make([]byte, largeSize)\n\tfmt.Printf(\"Large: адрес %p (размер %d КБ, выделен напрямую из mheap)\\n\",\n\t\t&largeSlice[0], len(largeSlice)/1024)\n\n\truntime.KeepAlive(t1)\n\truntime.KeepAlive(t2)\n\truntime.KeepAlive(s)\n\truntime.KeepAlive(largeSlice)\n}\n",
        "note": "Практическая демонстрация Tiny, Small и Large аллокаций памяти"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run alloc_types.go\n"
      }
    ],
    "under_the_hood": "В `mallocgc` ветвление выглядит следующим образом:\n```go\nif size <= maxSmallSize {\n    if noscan && size < maxTinySize {\n        // Tiny Allocator: упаковка в mcache.tiny\n    } else {\n        // Small Allocator: mcache.alloc[spanclass]\n    }\n} else {\n    // Large Allocator: largeAlloc(size, needzero, noscan) -> mheap_.alloc\n}\n```\nФлаг `noscan` критически важен: Tiny Allocator объединяет объекты **только если в них нет указателей**, иначе сборщику мусора пришлось бы сканировать весь 16-байтный блок целиком даже при освобождении одного из объектов.",
    "pitfalls": "1. Если в крошечной структуре (даже размером 4 байта) есть указатель `*int`, она **не попадает** в Tiny Allocator и идет в общий Small Allocator (класс 16 байт).\n2. Частые аллокации Large объектов (> 32 КБ) нагружают глобальный спинлок `mheap.lock`, вызывая деградацию параллелизма на многоядерных серверах.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему Tiny Allocator упаковывает только объекты без указателей (`noscan`) и никогда не упаковывает структуры, содержащие указатели?\n**Ответ:** Если упаковать в один 16-байтный блок несколько объектов, содержащих указатели, сборщик мусора не сможет понять, к какому именно логическому объекту относится каждый указатель. Более того, пока хотя бы один из упакованных объектов жив, весь 16-байтный блок удерживается в памяти, что при наличии указателей могло бы вызвать задержку освобождения целого поддерева объектов в куче."
  },
  {
    "num": 7,
    "title": "Группировка объектов одинакового размера в mspan",
    "task": "**[Size Classes]**: Изучи, что объекты в Go делятся на классы по размеру (до 32KB). Напиши программу, которая аллоцирует срезы размером 8 байт, 16 байт, 24 байта и 32 байта. Выведи адреса в лог. Объясни, как `mspan` группирует объекты одинакового размера.",
    "theory": "Каждый `mspan` в Go предназначен для хранения объектов **строго одного фиксированного размера**.\n\nКогда программа последовательно аллоцирует несколько объектов одинакового класса (например, несколько слайсов по 16 байт), аллокатор достает их из одного и того же активного `mspan`.\nВ результате:\n1. Адреса таких объектов следуют друг за другом с шагом, в точности равным размеру слота спана.\n2. Объекты разных классов размеров физически попадают в разные спаны и имеют существенно отличающиеся базовые адреса в памяти.\nТакая организация обеспечивает локальность данных в процессорном кэше L1/L2 и позволяет рантайму освобождать ячейки за константное время $O(1)$ без поиска свободных блоков.",
    "step_by_step": "1. Аллоцируйте серии срезов размером 8, 16, 24 и 32 байта.\n2. Выведите в консоль шестнадцатеричные адреса первых элементов каждого среза.\n3. Проанализируйте шаг приращения адресов внутри одной группы и разницу между разными группами.",
    "code_blocks": [
      {
        "filename": "span_grouping.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"unsafe\"\n)\n\nfunc printPointers(name string, ptrs []uintptr, expectedStep int) {\n\tfmt.Printf(\"--- Группа: %s (размер слота %d Б) ---\\n\", name, expectedStep)\n\tfor i := 0; i < len(ptrs); i++ {\n\t\tif i > 0 {\n\t\t\tdiff := int(ptrs[i] - ptrs[i-1])\n\t\t\tfmt.Printf(\"  [%d] Адрес: 0x%x (дельта: %d байт)\\n\", i, ptrs[i], diff)\n\t\t} else {\n\t\t\tfmt.Printf(\"  [%d] Адрес: 0x%x\\n\", i, ptrs[i])\n\t\t}\n\t}\n\tfmt.Println()\n}\n\nfunc main() {\n\tconst n = 4\n\n\t// Аллокация объектов 8 байт\n\tptrs8 := make([]uintptr, n)\n\tfor i := 0; i < n; i++ {\n\t\tsl := make([]byte, 8)\n\t\tptrs8[i] = uintptr(unsafe.Pointer(&sl[0]))\n\t}\n\n\t// Аллокация объектов 16 байт\n\tptrs16 := make([]uintptr, n)\n\tfor i := 0; i < n; i++ {\n\t\tsl := make([]byte, 16)\n\t\tptrs16[i] = uintptr(unsafe.Pointer(&sl[0]))\n\t}\n\n\t// Аллокация объектов 24 байта\n\tptrs24 := make([]uintptr, n)\n\tfor i := 0; i < n; i++ {\n\t\tsl := make([]byte, 24)\n\t\tptrs24[i] = uintptr(unsafe.Pointer(&sl[0]))\n\t}\n\n\t// Аллокация объектов 32 байта\n\tptrs32 := make([]uintptr, n)\n\tfor i := 0; i < n; i++ {\n\t\tsl := make([]byte, 32)\n\t\tptrs32[i] = uintptr(unsafe.Pointer(&sl[0]))\n\t}\n\n\tprintPointers(\"Слайсы 8 байт\", ptrs8, 8)\n\tprintPointers(\"Слайсы 16 байт\", ptrs16, 16)\n\tprintPointers(\"Слайсы 24 байта\", ptrs24, 24)\n\tprintPointers(\"Слайсы 32 байта\", ptrs32, 32)\n}\n",
        "note": "Анализ шага адресов объектов внутри одного mspan"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run span_grouping.go\n"
      }
    ],
    "under_the_hood": "Когда `mcache` запрашивает новый объект размера $S$, он вычисляет индекс слота: `elem = mspan.base() + freeIndex * elemsize`.\nТак как слоты нарезаны строго подряд, шаг между соседними аллокациями в пределах одного спана в точности равен `elemsize`.\nПри освобождении объекта сборщиком мусора вычисляется:\n`index = (addr - span.base()) / elemsize`.\nЗатем в битовой карте `allocBits` соответствующий бит сбрасывается в 0 за 1 такт процессора! Никаких поисков по спискам и перемещений памяти.",
    "pitfalls": "1. Не рассчитывайте, что дельта между адресами будет строго постоянной вечно: когда спан заполняется, следующий объект выделяется из другого спана, и адрес может совершить большой скачок.\n2. Объекты одного размера гарантированно попадают в один `mspan` только в пределах одного потока $P$ (так как у каждого $P$ свой независимый `mcache`).",
    "bigtech_interview": "**Вопрос с собеседования:** Как рантайм Go по произвольному адресу указателя `p` мгновенно определяет, какому объекту и какому спану он принадлежит, без обхода метаданных?\n**Ответ:** Рантайм использует двухуровневую структуру каталога страниц (Page Directory / Radix Tree) в `mheap`. Старшие биты адреса указателя транслируются в номер 8 КБ страницы. По номеру страницы из массива `mheap.spans[pageIdx]` за $O(1)$ извлекается указатель на структуру `*mspan`, из которой мгновенно считывается класс размера, базовый адрес и битовая карта занятости."
  },
  {
    "num": 8,
    "title": "Локальный кэш процессора mcache и Lock-free аллокации",
    "task": "**mcache**: Каждый P имеет свой `mcache` — локальный кэш `mspan`'ов для разных size classes. Это позволяет аллоцировать без блокировок.",
    "theory": "`mcache` — это фундаментальная структура аллокатора, отвечающая за высочайшую скорость выделения памяти в Go.\n\nКаждый логический процессор $P$ в рантайме владеет собственной структурой `mcache`:\n- Массив `alloc [numSpanClasses]*mspan`: указатели на активные спаны каждого из 136 подклассов (67 классов скан/носкан + пустые).\n- В `mcache` также расположен буфер `tiny` и смещение `tinyoffset` для Tiny-аллокатора.\n\nПоскольку поток ОС $M$ монопольно владеет ассоциированным с ним $P$ во время исполнения Go-кода, горутины, работающие на данном $P$, аллоцируют память **абсолютно без мьютексов, без spinlock'ов и без атомарных CAS инструкций**. Выделение памяти занимает всего ~5-15 наносекунд.",
    "step_by_step": "1. Смоделируйте конкурентную аллокацию памяти в нескольких горутинах при `GOMAXPROCS=1` и `GOMAXPROCS=4`.\n2. Замерьте пропускную способность аллокаций.\n3. Продемонстрируйте масштабируемость независимых кэшей `mcache` при росте числа доступных ядер CPU.",
    "code_blocks": [
      {
        "filename": "mcache_bench_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"runtime\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype DataPoint struct {\n\tID    int64\n\tScore float64\n}\n\n// Бенчмарк параллельной аллокации памяти\nfunc BenchmarkParallelAlloc(b *testing.B) {\n\tb.ReportAllocs()\n\tb.ResetTimer()\n\n\tb.RunParallel(func(pb *testing.PB) {\n\t\tfor pb.Next() {\n\t\t\t// Выделение памяти из локального mcache текущего P\n\t\t\td := &DataPoint{ID: 100, Score: 99.5}\n\t\t\truntime.KeepAlive(d)\n\t\t}\n\t})\n}\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\tconst workers = 4\n\twg.Add(workers)\n\n\t// Проверяем работу на всех P\n\tfor i := 0; i < workers; i++ {\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\t\t\tfor j := 0; j < 100_000; j++ {\n\t\t\t\t_ = &DataPoint{ID: int64(j), Score: float64(j) * 1.5}\n\t\t\t}\n\t\t}(i)\n\t}\n\n\twg.Wait()\n}\n",
        "note": "Бенчмарк параллельного выделения памяти из изолированных кэшей mcache"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -bench=BenchmarkParallelAlloc -cpu=1,2,4,8 mcache_bench_test.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/mcache.go` метод `mcache.allocLarge` или `nextFreeFast`:\n```go\nfunc (c *mcache) nextFree(spc spanClass) (gclinkptr, uintptr, bool) {\n    s := c.alloc[spc]\n    freeIndex := s.nextFreeIndex()\n    if freeIndex == s.nelems {\n        // Спан полон — вызываем пополнение mcache из mcentral\n        c.refill(spc)\n    }\n    // Быстрое извлечение адреса\n}\n```\nМетод `nextFreeFast` инлайнится компилятором. При наличии свободных слотов он выполняется всего за 3 процессорные инструкции: битовый сдвиг, чтение и инкремент индекса.",
    "pitfalls": "1. Если горутина мигрирует между разными $P$ (например, после системного вызова или yield), она начинает использовать `mcache` нового $P$. Это нормальное поведение рантайма и не приводит к утечкам.\n2. `mcache` очищается и сбрасывается обратно в `mcentral` во время фазы Stop-The-World (STW) сборщика мусора перед фазой разметки.",
    "bigtech_interview": "**Вопрос с собеседования:** Что происходит с объектами в `mcache`, когда сборщик мусора GC начинает цикл сборки?\n**Ответ:** Перед началом фазы разметки (Mark Phase) рантайм выполняет сброс (flush) кэшей `mcache` всех процессоров $P$ в функции `mcache.releaseAll()`. Все спаны возвращаются в `mcentral`, чтобы сборщик мусора видел единое целостное состояние всех объектов в куче и мог корректно отслеживать неиспользуемую память."
  },
  {
    "num": 9,
    "title": "Центральный пул mcentral и пополнение спанов",
    "task": "**mcentral**: Когда `mcache` истощается, он запрашивает новые `mspan`'ы из `mcentral` (глобальный для каждого size class, с блокировкой).",
    "theory": "`mcentral` — это второй уровень иерархии аллокатора памяти Go (L2 пул).\n\nХарактеристики `mcentral`:\n- В рантайме существует ровно **136 экземпляров `mcentral`** (по два на каждый из 68 классов: один для объектов без указателей `noscan`, другой для объектов с указателями `scan`).\n- Каждый `mcentral` управляет двумя списками спанов:\n  1. `partial`: спаны, в которых есть как минимум один свободный слот для аллокации.\n  2. `full`: спаны, в которых все слоты заняты.\n- Поскольку к одному `mcentral` обращаются все процессоры $P$ при исчерпании своих `mcache`, доступ к нему защищен спинлоком `mcentral.lock`.",
    "step_by_step": "1. Напишите код с массовой аллокацией объектов одного размера большим числом параллельных горутин.\n2. Смоделируйте опустошение локальных кэшей `mcache`.\n3. Объясните логику перехода из `partial` в `full` списки `mcentral`.",
    "code_blocks": [
      {
        "filename": "mcentral_demo.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\n// Структура размером ровно 64 байта (Size Class 6)\ntype Block64 struct {\n\tData [8]int64\n}\n\nfunc main() {\n\tfmt.Printf(\"Число доступных P: %d\\n\", runtime.GOMAXPROCS(0))\n\n\tconst goroutines = 16\n\tconst allocsPerG = 20_000\n\n\tvar wg sync.WaitGroup\n\twg.Add(goroutines)\n\n\tstart := time.Now()\n\n\t// Запускаем 16 горутин, одновременно требующих сотни тысяч объектов класса 64B\n\t// Это многократно опустошит mcache каждого P и заставит их синхронизироваться на mcentral[6]\n\tfor i := 0; i < goroutines; i++ {\n\t\tgo func(workerID int) {\n\t\t\tdefer wg.Done()\n\t\t\tstorage := make([]*Block64, allocsPerG)\n\t\t\tfor j := 0; j < allocsPerG; j++ {\n\t\t\t\tstorage[j] = &Block64{Data: [8]int64{int64(workerID), int64(j)}}\n\t\t\t}\n\t\t\t_ = storage\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\telapsed := time.Since(start)\n\n\tvar mem runtime.MemStats\n\truntime.ReadMemStats(&mem)\n\n\tfmt.Printf(\"Время параллельной аллокации: %v\\n\", elapsed)\n\tfmt.Printf(\"Всего аллокаций в куче: %d\\n\", mem.Mallocs)\n\tfmt.Println(\"Все P успешно пополнили спаны через mcentral.\")\n}\n",
        "note": "Массовая параллельная аллокация для провокации обращений к mcentral"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run mcentral_demo.go\n"
      }
    ],
    "under_the_hood": "Когда `mcache.refill(spc)` вызывает `mcentral.cacheSpan()`:\n1. Захватывается `mcentral.lock`.\n2. Проверяется список `partial swept`: спаны, уже очищенные от мусора (swept), имеющие свободные ячейки. Если найден, он отдается в `mcache`.\n3. Если там пусто, проверяется список `partial unswept`: вызывается `mspan.sweep()` для немедленной очистки спана прямо на месте!\n4. Если свободных спанов нет ни в swept, ни в unswept, вызывается `mcentral.grow()`, который идет на L3 уровень — в глобальный `mheap.alloc()` за новыми страницами памяти.\n5. Лок освобождается.",
    "pitfalls": "1. Блокировка `mcentral.lock` является спинлоком рантайма, а не пользовательским мьютексом. При высокой конкуренции (тысячи горутин, аллоцирующих один класс размера) процессорные ядра могут кратковременно прогреваться на спин-циклах.\n2. Использование объектов разных классов снижает contention на `mcentral`, так как разные классы используют разные структуры `mcentral` со своими независимыми спинлоками.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему списки спанов в `mcentral` разделены на `swept` (очищенные) и `unswept` (неочищенные)?\n**Ответ:** Go использует алгоритм ленивой очистки мусора (Lazy Sweeping). После фазы разметки Mark GC не очищает сразу все гигабайты кучи (это вызвало бы долгую паузу). Вместо этого спаны помечаются как `unswept`. Когда конкретному $P$ требуется новый спан из `mcentral`, он сам доочищает один `unswept` спан (On-demand sweeping), распределяя нагрузку по времени и ядрам процессора."
  },
  {
    "num": 10,
    "title": "Tiny Allocator: Упаковка мелких объектов до 16 байт",
    "task": "**[Tiny Allocator (< 16 bytes)]**: Напиши структуру из двух полей `bool` и `int8` (занимает 2 байта). Создай миллион таких структур. Объясни, как Tiny Allocator объединяет маленькие объекты (< 16B) в один блок 16B, чтобы уменьшить фрагментацию памяти.",
    "theory": "Tiny Allocator в Go — это специализированный субаллокатор внутри `mcache`, разработанный специально для упаковки крошечных объектов размером **менее 16 байт без указателей** (`noscan`).\n\nМеханика работы:\n1. В `mcache` хранятся два поля: `tiny uintptr` (адрес текущего 16-байтного блока) и `tinyoffset uintptr` (текущее смещение внутри этого блока).\n2. При запросе объекта размера $N$ байт (например, 2 байта):\n   - Проверяется выравнивание (alignment).\n   - Если в текущем 16-байтном блоке есть место (`tinyoffset + N <= 16`), объект размещается прямо в этом блоке, а `tinyoffset` сдвигается на $N$.\n   - Если места нет, выделяется новый 16-байтный блок из `mcache.alloc[tinySpanClass]`.\n3. Благодаря этому несколько независимых мелких переменных делят один общий 16-байтный слот спана, снижая расход памяти в 4–8 раз!",
    "step_by_step": "1. Создайте структуру размером 2 байта: `struct { a bool; b int8 }`.\n2. Создайте массив указателей на миллион таких структур.\n3. Проанализируйте адреса соседних элементов и подтвердите, что они упакованы в общие 16-байтные слоты.",
    "code_blocks": [
      {
        "filename": "tiny_allocator.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"unsafe\"\n)\n\ntype MiniNode struct {\n\tFlag bool // 1 байт\n\tCode int8 // 1 байт (итого 2 байта)\n}\n\nfunc main() {\n\tfmt.Printf(\"Размер структуры MiniNode: %d байт\\n\\n\", unsafe.Sizeof(MiniNode{}))\n\n\t// Демонстрация упаковки нескольких объектов в один 16-байтный блок\n\tnodes := make([]*MiniNode, 8)\n\tfor i := 0; i < len(nodes); i++ {\n\t\tnodes[i] = &MiniNode{Flag: true, Code: int8(i)}\n\t}\n\n\tfmt.Println(\"Адреса последовательно созданных MiniNode:\")\n\tfor i, n := range nodes {\n\t\taddr := uintptr(unsafe.Pointer(n))\n\t\tif i > 0 {\n\t\t\tprev := uintptr(unsafe.Pointer(nodes[i-1]))\n\t\t\tfmt.Printf(\"  [%d] 0x%x (дельта с предыдущим: %d байт)\\n\", i, addr, addr-prev)\n\t\t} else {\n\t\t\tfmt.Printf(\"  [%d] 0x%x\\n\", i, addr)\n\t\t}\n\t}\n\n\t// Замер памяти на миллион структур\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\tconst count = 1_000_000\n\tmillionNodes := make([]*MiniNode, count)\n\tfor i := 0; i < count; i++ {\n\t\tmillionNodes[i] = &MiniNode{Flag: true, Code: int8(i % 128)}\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tallocated := m2.HeapAlloc - m1.HeapAlloc\n\n\tfmt.Printf(\"\\nПамять под %d MiniNode: %d КБ (в среднем %.2f байт на узел)\\n\",\n\t\tcount, allocated/1024, float64(allocated)/count)\n\tfmt.Println(\"Без Tiny Allocator каждый объект занимал бы минимум 8-16 байт в спане!\")\n\n\truntime.KeepAlive(nodes)\n\truntime.KeepAlive(millionNodes)\n}\n",
        "note": "Упаковка мелких 2-байтных структур Tiny Allocator в блоки по 16 байт"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run tiny_allocator.go\n"
      }
    ],
    "under_the_hood": "В функции `mallocgc`:\n```go\noff := c.tinyoffset\n// Выравнивание смещения\noff = alignUp(off, size)\nif off+size <= maxTinySize && c.tiny != 0 {\n    // Успех! Объект помещается в текущий tiny блок\n    x = unsafe.Pointer(c.tiny + off)\n    c.tinyoffset = off + size\n    c.tinyAllocs++\n    return x\n}\n// Иначе аллоцируем новый 16-байтный слот из spanclass tiny\n```\nПоскольку весь 16-байтный блок не содержит указателей (`noscan`), GC маркирует его целиком одним битом в `gcmarkBits`.",
    "pitfalls": "1. Если хотя бы один из крошечных объектов внутри 16-байтного блока остается достижимым (живым), сборщик мусора не может освободить весь 16-байтный блок. Это может вызывать небольшое удержание неиспользуемой памяти.\n2. Не пытайтесь складывать в Tiny Allocator структуры с указателями или интерфейсами — они автоматически дисквалифицируются и идут в Small Allocator.",
    "bigtech_interview": "**Вопрос с собеседования:** Какой выигрыш дает Tiny Allocator в реальных сценариях (например, сетевые сервисы, парсеры JSON/Protobuf)?\n**Ответ:** В сетевых сервисах создаются миллиарды короткоживущих строк, слайсов чисел, булевых флагов и мелких структур размером 1–4 байта. Без Tiny Allocator минимальный слот аллокатора составлял бы 8 байт (а на 64-битных ОС часто 16 байт), что приводило бы к потере 75–87% памяти на внутреннюю фрагментацию. Tiny Allocator пакует их в один блок, снижая расход RAM в 3–5 раз и ускоряя аллокацию до пары процессорных инструкций."
  },
  {
    "num": 11,
    "title": "Глобальная куча mheap и структура управления страницами",
    "task": "**mheap**: Глобальный heap, управляющий всей памятью процесса. Содержит все `mspan`'ы, организованные в radix tree для быстрого поиска.",
    "theory": "`mheap` — это вершина иерархии памяти Go (L3 глобальная куча). В рантайме существует ровно один глобальный экземпляр `mheap_`.\n\nАрхитектурные обязанности `mheap`:\n1. **Управление виртуальной памятью (Arenas):** Go резервирует виртуальную память гигантскими сегментами — аренами размером **64 МБ** (на 64-битных ОС). Арены отображаются на физическую память по мере необходимости.\n2. **Аллокатор страниц (Page Allocator):** Память внутри арен нарезается на страницы по **8192 байта (8 КБ)**. Для поиска свободных непрерывных диапазонов страниц используется битовая карта и структура Radix Tree (`pageAlloc`).\n3. **Хранилище `mcentral`:** Внутри `mheap` находится массив из 136 структур `mcentral`.\n4. **Связка адресов со спанами:** Через трехуровневый массив `arenas[L1][L2]` рантайм мгновенно по адресу любого байта находит дескриптор его `*mspan`.",
    "step_by_step": "1. Напишите код, аллоцирующий память крупными порциями (> 32 КБ), заставляя задействовать страницы `mheap`.\n2. Измерьте изменения `HeapSys` и `HeapIdle` через `runtime.ReadMemStats`.\n3. Изучите карту арен виртуальной памяти процесса.",
    "code_blocks": [
      {
        "filename": "mheap_demo.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc main() {\n\tvar m1, m2 runtime.MemStats\n\truntime.ReadMemStats(&m1)\n\n\tfmt.Println(\"=== Исследование глобальной кучи mheap ===\")\n\tfmt.Printf(\"Базовый HeapSys  : %d МБ (виртуальная память у ОС)\\n\", m1.HeapSys/(1024*1024))\n\tfmt.Printf(\"Базовый HeapAlloc: %d МБ (живые объекты)\\n\\n\", m1.HeapAlloc/(1024*1024))\n\n\t// Выделяем крупный объект (50 МБ), который минует mcache и запрашивается напрямую у mheap\n\tfmt.Println(\"Аллоцируем крупный массив 50 МБ напрямую из mheap...\")\n\tlargeBuffer := make([]byte, 50*1024*1024)\n\tfor i := range largeBuffer {\n\t\tlargeBuffer[i] = 1 // физически трогаем страницы памяти\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tfmt.Printf(\"HeapSys после аллокации  : %d МБ\\n\", m2.HeapSys/(1024*1024))\n\tfmt.Printf(\"HeapAlloc после аллокации: %d МБ\\n\", m2.HeapAlloc/(1024*1024))\n\tfmt.Printf(\"HeapInuse (занятые спаны): %d МБ\\n\", m2.HeapInuse/(1024*1024))\n\n\t// Освобождаем ссылку\n\tlargeBuffer = nil\n\truntime.GC()\n\n\tvar m3 runtime.MemStats\n\truntime.ReadMemStats(&m3)\n\tfmt.Printf(\"\\nПосле GC:\\n\")\n\tfmt.Printf(\"HeapAlloc (живая память): %d МБ (память очищена!)\\n\", m3.HeapAlloc/(1024*1024))\n\tfmt.Printf(\"HeapIdle  (в пуле mheap): %d МБ (удерживается в аренах)\\n\", m3.HeapIdle/(1024*1024))\n\tfmt.Printf(\"HeapReleased (отдано ОС): %d МБ\\n\", m3.HeapReleased/(1024*1024))\n\n\ttime.Sleep(100 * time.Millisecond)\n}\n",
        "note": "Прямая аллокация крупных блоков страниц в mheap и поведение HeapSys/HeapIdle"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run mheap_demo.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/mpagealloc.go` аллокатор страниц `pageAlloc` использует компактное побитовое Radix Tree:\n- Каждый лист дерева представляет 64 страницы (512 КБ).\n- Промежуточные узлы хранят сводную информацию: максимальное непрерывное количество свободных страниц в поддереве.\n- Поиск диапазона из $K$ свободных страниц выполняется за $O(\\log N)$ процессорными инструкциями поиска битов `BSR`/`BSF` без линейного сканирования памяти!\nЕсли страниц в текущих аренах не хватает, вызывается `sysAlloc` -> `mmap(PROT_READ|PROT_WRITE)`.",
    "pitfalls": "1. Рост метрики `HeapSys` означает, что процесс запросил виртуальную память у ядра Linux. Даже после сборки мусора `HeapSys` не уменьшается автоматически — страницы переходят в статус `HeapIdle` и возвращаются ядру только фоновым потоком `scavenger`.\n2. Прямые аллокации в `mheap` блокируют глобальный мьютекс `mheap_.lock`, поэтому множественные параллельные аллокации объектов > 32 КБ создают бутылочное горлышко.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое `HeapIdle` и почему после вызова `runtime.GC()` объем памяти процесса в выводе команды `top` (колонка RES) часто вообще не уменьшается?\n**Ответ:** `HeapIdle` — это объем памяти страниц, которые принадлежат аренам Go, но в данный момент не содержат живых объектов. Go намеренно не возвращает эту память ядру ОС немедленно, так как системные вызовы `munmap`/`madvise` дороги по CPU, а сервису вскоре снова может понадобиться память. Память возвращается ядру фоновым сборщиком (Scavenger) постепенно через `madvise(MADV_DONTNEED)`."
  },
  {
    "num": 12,
    "title": "Иерархия аллокации mcache → mcentral → mheap и pprof",
    "task": "**Иерархия: mcache → mcentral → mheap.**: Напишите программу, интенсивно выделяющую объекты разного размера (маленькие <32KB, большие). Соберите профиль памяти (`pprof`), определите, из каких классов размеров (size classes) выделялась память, и в каких компонентах аллокатора.",
    "theory": "Иерархия памяти Go работает по принципу каскадного кэширования:\n1. **L1: `mcache`:** Объекты $\\le 32$ КБ выделяются локально на текущем $P$. Это Fast-Path без локов.\n2. **L2: `mcentral`:** Когда в спане `mcache` заканчиваются свободные ячейки, спан возвращается в `mcentral`, а взамен забирается спан со свободными слотами. Это Slow-Path с блокировкой `mcentral.lock`.\n3. **L3: `mheap`:** Если в `mcentral` нет свободных спанов (или объект $> 32$ КБ), запрос идет в `mheap`. Выделяются 8 КБ страницы под глобальной блокировкой `mheap.lock`.\n\nС помощью `pprof.WriteHeapProfile` можно зафиксировать как текущее распределение объектов (`inuse_space`), так и суммарный объем выделенной памяти за время работы (`alloc_space`).",
    "step_by_step": "1. Напишите код, аллоцирующий мелкие объекты (128 байт), средние (4 КБ) и крупные (> 64 КБ).\n2. Сохраните профиль кучи в файл `heap.pprof` с помощью пакета `runtime/pprof`.\n3. Запустите анализ профиля через `go tool pprof -sample_index=alloc_space heap.pprof`.",
    "code_blocks": [
      {
        "filename": "hierarchy.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"os\"\n\t\"runtime\"\n\t\"runtime/pprof\"\n\t\"sync\"\n)\n\n// Мелкий объект (128B) -> mcache\ntype SmallItem struct {\n\tData [128]byte\n}\n\n// Средний объект (4KB) -> mcache/mcentral\ntype MediumItem struct {\n\tData [4096]byte\n}\n\n// Крупный объект (128KB > 32KB) -> напрямую mheap\ntype LargeItem struct {\n\tData [128 * 1024]byte\n}\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\tconst iterations = 1000\n\n\t// 1. Аллокация мелких объектов\n\twg.Add(1)\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tvar items []*SmallItem\n\t\tfor i := 0; i < iterations*10; i++ {\n\t\t\titems = append(items, new(SmallItem))\n\t\t}\n\t\truntime.KeepAlive(items)\n\t}()\n\n\t// 2. Аллокация средних объектов\n\twg.Add(1)\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tvar items []*MediumItem\n\t\tfor i := 0; i < iterations; i++ {\n\t\t\titems = append(items, new(MediumItem))\n\t\t}\n\t\truntime.KeepAlive(items)\n\t}()\n\n\t// 3. Аллокация крупных объектов\n\twg.Add(1)\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tvar items []*LargeItem\n\t\tfor i := 0; i < 50; i++ {\n\t\t\titems = append(items, new(LargeItem))\n\t\t}\n\t\truntime.KeepAlive(items)\n\t}()\n\n\twg.Wait()\n\n\t// Сохраняем heap profile\n\tf, err := os.Create(\"heap.pprof\")\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tdefer f.Close()\n\n\truntime.GC() // принудительно для фиксации актуального состояния\n\tif err := pprof.WriteHeapProfile(f); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfmt.Println(\"Профиль кучи сохранен в heap.pprof\")\n}\n",
        "note": "Сбор профиля кучи при разнокалиберных аллокациях памяти"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run hierarchy.go\ngo tool pprof -top -alloc_space heap.pprof\n"
      }
    ],
    "under_the_hood": "В `src/runtime/malloc.go` функция `mallocgc`:\n- Для `SmallItem`: `spc = makeSpanClass(size_to_class8[128], false)` -> обращение к `c.alloc[spc]`.\n- Если `c.alloc[spc]` пуст -> `c.refill(spc)` -> `mcentral[spc].cacheSpan()`.\n- Для `LargeItem`: размер 131072 байт > `maxSmallSize` (32768) -> вызов `largeAlloc(131072, needzero, noscan)`.\n`largeAlloc` вычисляет число страниц `npages = 131072 / 8192 = 16` и вызывает `mheap_.alloc(16, spanClass(0))`.",
    "pitfalls": "1. По умолчанию pprof heap сэмплирует одну аллокацию на каждые 512 КБ выделенной памяти (`runtime.MemProfileRate = 512 * 1024`). Мелкие аллокации отображаются со статистической погрешностью.\n2. Для точного анализа в тестах можно установить `runtime.MemProfileRate = 1`, но в production это сильно просадит RPS.",
    "bigtech_interview": "**Вопрос с собеседования:** В профиле `pprof heap` есть 4 режима: `inuse_space`, `inuse_objects`, `alloc_space`, `alloc_objects`. Для поиска каких проблем предназначен каждый из них?\n**Ответ:** \n- `inuse_space`: объем памяти, занятый прямо сейчас. Используется для поиска утечек памяти (Memory Leaks).\n- `inuse_objects`: количество живых объектов. Позволяет выявить раздувание графа объектов, замедляющее фазу Mark GC.\n- `alloc_space`: суммарный объем выделенной памяти за всё время. Используется для оптимизации темпа генерации мусора (GC pressure).\n- `alloc_objects`: суммарное число созданных объектов. Показывает узкие места частых аллокаций, требующих внедрения `sync.Pool`."
  },
  {
    "num": 13,
    "title": "Устройство структуры mspan и битовые карты занятости",
    "task": "**mspan**: Непрерывный блок памяти, состоящий из нескольких страниц (8KB каждая). Содержит объекты одного size class.",
    "theory": "`mspan` (Memory Span) — базовый кирпич управления памятью в Go.\n\nКлючевые поля структуры `mspan` (определена в `src/runtime/mspan.go`):\n- `startAddr uintptr`: начальный виртуальный адрес спана.\n- `npages uintptr`: число страниц (по 8192 байта), выделенных под данный спан.\n- `elemsize uintptr`: размер слота под объект в байтах (например, 24 байта).\n- `nelems uintptr`: общее количество объектов, помещающихся в спане (`(npages * 8192) / elemsize`).\n- `allocBits *gcBits`: битовая карта, где 1 означает занятый слот, 0 — свободный.\n- `gcmarkBits *gcBits`: битовая карта, в которой сборщик мусора выставляет 1 для каждого достижимого (живого) объекта.\n- `freeindex uintptr`: индекс первого потенциально свободного слота (ускоряет поиск до $O(1)$).",
    "step_by_step": "1. Напишите код, эмулирующий расчет параметров `mspan` для разных классов размеров.\n2. Рассчитайте число страниц, емкость объектов и внутреннюю фрагментацию для классов 16Б, 48Б, 512Б и 4КБ.\n3. Продемонстрируйте работу побитовой маски `allocBits`.",
    "code_blocks": [
      {
        "filename": "mspan_calc.go",
        "lang": "go",
        "code": "package main\n\nimport \"fmt\"\n\nconst pageSize = 8192 // 8 КБ\n\ntype SpanSpec struct {\n\tClassID  int\n\tElemSize int\n\tPages    int\n}\n\nfunc main() {\n\tspecs := []SpanSpec{\n\t\t{ClassID: 2, ElemSize: 16, Pages: 1},\n\t\t{ClassID: 5, ElemSize: 48, Pages: 1},\n\t\t{ClassID: 15, ElemSize: 512, Pages: 1},\n\t\t{ClassID: 35, ElemSize: 4096, Pages: 2},\n\t}\n\n\tfmt.Printf(\"%-8s %-10s %-8s %-10s %-12s %-10s\\n\",\n\t\t\"Класс\", \"Объект (Б)\", \"Страниц\", \"Всего (Б)\", \"Вместимость\", \"Потери (Б)\")\n\tfmt.Println(\"----------------------------------------------------------------\")\n\n\tfor _, s := range specs {\n\t\ttotalSpanBytes := s.Pages * pageSize\n\t\tcapacity := totalSpanBytes / s.ElemSize\n\t\tusedBytes := capacity * s.ElemSize\n\t\twasteBytes := totalSpanBytes - usedBytes\n\n\t\tfmt.Printf(\"Class %-2d %-10d %-8d %-10d %-12d %-10d\\n\",\n\t\t\ts.ClassID, s.ElemSize, s.Pages, totalSpanBytes, capacity, wasteBytes)\n\t}\n\n\t// Эмуляция битовой карты allocBits (64 бита на uint64)\n\tvar allocBits uint64 = 0b0000_1111 // первые 4 слота заняты\n\tfreeIndex := 4                     // следующий свободный слот\n\tfmt.Printf(\"\\nЭмуляция allocBits: 0x%016x (следующий свободный слот: %d)\\n\",\n\t\tallocBits, freeIndex)\n}\n",
        "note": "Математическая модель параметров и емкости mspan"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run mspan_calc.go\n"
      }
    ],
    "under_the_hood": "Во время фазы Sweep (очистка мусора):\nРантайм не очищает память байт за байтом!\nОн просто меняет местами указатели на битовые карты:\n`s.allocBits = s.gcmarkBits`\n`s.gcmarkBits = newMarkBits(s.nelems)`\nВсе немаркированные объекты мгновенно становятся «свободными» для будущих аллокаций за **1 такт процессора**! Это гениальная оптимизация алгоритма триколор GC в Go.",
    "pitfalls": "1. Потери памяти в хвосте спана (`totalBytes - nelems * elemsize`) возникают, когда размер страницы (8192) не делится нацело на размер объекта. Чтобы минимизировать этот остаток, крупные классы объединяют несколько 8 КБ страниц в один спан.\n2. Не путайте `allocBits` со стековыми фреймами: спаны используются исключительно в куче.",
    "bigtech_interview": "**Вопрос с собеседования:** Как рантайм Go выполняет освобождение памяти целого `mspan` в фазе Sweep за константное время $O(1)$?\n**Ответ:** Благодаря битовым картам `allocBits` и `gcmarkBits`. В ходе маркировки живые объекты помечаются битами в `gcmarkBits`. В фазе Sweep рантайм просто делает указатель `allocBits` равным старому `gcmarkBits`, а под новый `gcmarkBits` аллоцирует чистый нулевой массив. Если все биты в `gcmarkBits` оказались нулями (ни один объект не выжил), весь `mspan` целиком возвращается в `mcentral/mheap`."
  },
  {
    "num": 14,
    "title": "Выделение страниц у операционной системы: mmap и madvise",
    "task": "**Page allocation**: Изучите, как runtime запрашивает страницы у ОС через `mmap`/`VirtualAlloc` и возвращает через `munmap`/`VirtualFree`.",
    "theory": "Рантайм Go не использует стандартную сишную функцию `malloc()` из `glibc`. Вместо этого Go общается с ядром операционной системы напрямую через быстрые системные вызовы:\n- **Linux:**\n  - `mmap(addr, length, PROT_READ|PROT_WRITE, MAP_ANON|MAP_PRIVATE, -1, 0)`: выделяет новый диапазон виртуальной памяти.\n  - `madvise(addr, length, MADV_DONTNEED)`: сообщает ядру Linux, что страницы не используются. Ядро сбрасывает физические фреймы памяти, обнуляя RSS процесса.\n  - `munmap(addr, length)`: освобождает диапазон адресов виртуальной памяти (в Go практически не используется, так как виртуальные арены удерживаются для повторного использования).\n- **Windows:**\n  - `VirtualAlloc(MEM_RESERVE / MEM_COMMIT)` и `VirtualFree(MEM_DECOMMIT)`.\n- **macOS / BSD:**\n  - `mmap` и `madvise(MADV_FREE_REUSABLE)`.",
    "step_by_step": "1. Напишите код на Go, который с помощью системного вызова `syscall.Mmap` выделяет чистые страницы виртуальной памяти в обход кучи Go.\n2. Запишите данные в выделенные страницы.\n3. Очистите страницы через `syscall.Madvise` с флагом `MADV_DONTNEED`.\n4. Освободите память через `syscall.Munmap`.",
    "code_blocks": [
      {
        "filename": "direct_mmap.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"syscall\"\n)\n\nfunc main() {\n\tconst pageSize = 4096\n\tnumPages := 4\n\ttotalBytes := numPages * pageSize\n\n\tfmt.Println(\"=== Прямой системный вызов mmap в Linux/Unix ===\")\n\n\t// 1. Выделяем анонимную приватную память у ядра ОС (как делает mheap)\n\tdata, err := syscall.Mmap(\n\t\t0,\n\t\t0,\n\t\ttotalBytes,\n\t\tsyscall.PROT_READ|syscall.PROT_WRITE,\n\t\tsyscall.MAP_ANON|syscall.MAP_PRIVATE,\n\t)\n\tif err != nil {\n\t\tpanic(fmt.Sprintf(\"Ошибка mmap: %v\", err))\n\t}\n\tfmt.Printf(\"Выделено %d байт по адресу %p через mmap\\n\", len(data), &data[0])\n\n\t// 2. Пишем данные (провоцируем page fault и выделение физических страниц ядра)\n\tdata[0] = 42\n\tdata[pageSize] = 43\n\tfmt.Println(\"Данные успешно записаны в физические фреймы памяти.\")\n\n\t// 3. Возвращаем физические страницы ядру ОС через madvise\n\t// MADV_DONTNEED = 4 в Linux\n\tconst madvDontNeed = 4\n\t_, _, errno := syscall.Syscall(\n\t\tsyscall.SYS_MADVISE,\n\t\tuintptr(data[0]),\n\t\tuintptr(totalBytes),\n\t\tuintptr(madvDontNeed),\n\t)\n\tif errno != 0 {\n\t\tfmt.Printf(\"Предупреждение madvise: %v\\n\", errno)\n\t} else {\n\t\tfmt.Println(\"madvise(MADV_DONTNEED) выполнен: физическая память возвращена ядру!\")\n\t}\n\n\t// 4. Освобождаем виртуальный диапазон\n\tif err := syscall.Munmap(data); err != nil {\n\t\tpanic(err)\n\t}\n\tfmt.Println(\"munmap выполнен: виртуальный диапазон закрыт.\")\n}\n",
        "note": "Прямое выделение и возврат страниц ядру ОС через системные вызовы mmap и madvise"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run direct_mmap.go\n"
      }
    ],
    "under_the_hood": "При старте Go рантайм резервирует гигантский непрерывный диапазон виртуальных адресов (на 64-битных Linux до нескольких терабайт) флагом `mmap(..., PROT_NONE, MAP_ANON, ...)`.\nЭто действие потребляет 0 байт физической RAM!\nЗатем, когда `mheap` запрашивает новую арену 64 МБ, рантайм вызывает `mmap(..., PROT_READ|PROT_WRITE, MAP_FIXED|MAP_ANON, ...)`, активируя этот блок. Физическая RAM выделяется ядром процессора лениво — только при первом обращении к странице (Minor Page Fault).",
    "pitfalls": "1. Виртуальная память процесса (VIRT) в Go может составлять сотни гигабайт или терабайт. Это нормальное поведение рантайма Go. Никогда не судите о потреблении памяти Go-сервиса по колонке VIRT — смотрите только на RES (Resident Set Size).\n2. На 32-битных архитектурах адресное пространство ограничено 4 ГБ, поэтому на них арены и структуры метаданных Go устроены иначе.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в `top` колонка VIRT у Go-процесса сразу после запуска может показывать 500 ГБ, хотя на сервере всего 16 ГБ RAM?\n**Ответ:** Потому что Go резервирует виртуальное адресное пространство (Virtual Address Space) на будущее с помощью `mmap` без выделения физических страниц (lazy paging). Это позволяет размещать арены памяти кучи по фиксированным виртуальным смещениям и исключить конфликт адресов, не расходуя ни одного мегабайта физической оперативной памяти."
  },
  {
    "num": 15,
    "title": "Возврат памяти операционной системе Memory Scavenging",
    "task": "**Возврат памяти операционной системе (Memory Scavenging)**: Напишите программу, которая сначала выделяет в куче 1 ГБ памяти (слайс крупных структур), затем очищает ссылки на эти данные и принудительно запускает GC с помощью `runtime.GC()`. Запустите программу с флагом `GODEBUG=madvdontneed=1` (или `=0`) и исследуйте, как быстро фоновый поток рантайма (scavenger) возвращает неиспользуемые страницы памяти обратно операционной системе.",
    "theory": "Memory Scavenging (утилизация пустых страниц памяти) — это фоновый механизм рантайма Go, который возвращает свободные страницы памяти ядру ОС, снижая Resident Set Size (RSS) процесса.\n\nИсторический контекст флага `madvdontneed`:\n- **Go 1.12 – 1.15:** Go по умолчанию использовал `MADV_FREE` на Linux. Это было очень быстро, но ядро Linux освобождало страницы только при дефиците RAM на хосте. В результате мониторинг (Prometheus, K8s) видел высокий RSS, и поды необоснованно падали по OOMKilled.\n- **Go 1.16+:** Go вернулся к `MADV_DONTNEED` по умолчанию. При `MADV_DONTNEED` ядро Linux немедленно аннулирует отображение страниц, и RSS процесса в `top` падает сразу же при вызове скейвенджера.\n- Переменная `GODEBUG=madvdontneed=1` (или `0`) позволяет принудительно управлять этим поведением.",
    "step_by_step": "1. Напишите код, выделяющий 500 МБ памяти, а затем обнуляющий ссылку.\n2. Вызовите `runtime.GC()` и `debug.FreeOSMemory()`.\n3. С помощью `runtime.ReadMemStats` отследите переход памяти из `HeapAlloc` в `HeapIdle`, а затем в `HeapReleased`.",
    "code_blocks": [
      {
        "filename": "scavenge.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"runtime/debug\"\n\t\"time\"\n)\n\nfunc printStats(tag string) {\n\tvar m runtime.MemStats\n\truntime.ReadMemStats(&m)\n\tfmt.Printf(\"[%s]\\n\", tag)\n\tfmt.Printf(\"  HeapAlloc    : %4d МБ (активные объекты)\\n\", m.HeapAlloc/(1024*1024))\n\tfmt.Printf(\"  HeapIdle     : %4d МБ (свободные страницы в аренах)\\n\", m.HeapIdle/(1024*1024))\n\tfmt.Printf(\"  HeapReleased : %4d МБ (физически возвращено ОС)\\n\", m.HeapReleased/(1024*1024))\n\tfmt.Println()\n}\n\nfunc main() {\n\tprintStats(\"Старт программы\")\n\n\t// 1. Аллоцируем 500 МБ памяти\n\tfmt.Println(\"Выделяем 500 МБ памяти в куче...\")\n\tbuffer := make([]byte, 500*1024*1024)\n\tfor i := 0; i < len(buffer); i += 4096 {\n\t\tbuffer[i] = 1 // трогаем каждую страницу\n\t}\n\tprintStats(\"После аллокации 500 МБ\")\n\n\t// 2. Сбрасываем ссылку и вызываем GC\n\tfmt.Println(\"Обнуляем ссылку и вызываем runtime.GC()...\")\n\tbuffer = nil\n\truntime.GC()\n\tprintStats(\"После GC (память перешла в HeapIdle)\")\n\n\t// 3. Принудительно запускаем скейвенджер возврата памяти ОС\n\tfmt.Println(\"Вызываем debug.FreeOSMemory() для немедленного madvise...\")\n\tdebug.FreeOSMemory()\n\tprintStats(\"После FreeOSMemory (память перешла в HeapReleased)\")\n\n\ttime.Sleep(100 * time.Millisecond)\n}\n",
        "note": "Демонстрация перехода памяти из HeapAlloc -> HeapIdle -> HeapReleased"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "GODEBUG=madvdontneed=1 go run scavenge.go\n"
      }
    ],
    "under_the_hood": "Фоновый поток Scavenger (`src/runtime/mgcscavenge.go`):\n- Спит и периодически просыпается в фоне, управляемый потоком `sysmon`.\n- Рассчитывает целевой объем памяти, который нужно вернуть (Pacer).\n- Чтобы не грузить CPU, Scavenger настроен на потребление не более **1% одного ядра CPU**!\n- Он обходит Radix Tree свободных страниц `pageAlloc` и вызывает `sysUnused` -> `madvise(MADV_DONTNEED)` для диапазонов свободных страниц.",
    "pitfalls": "1. Вызов `debug.FreeOSMemory()` синхронен и запускает полный цикл GC и очистки страниц, вызывая существенную задержку. В боевом коде HighLoad его использовать не следует — положитесь на штатный фоновый scavenger.\n2. `HeapReleased` страницы остаются в виртуальном адресном пространстве Go! При необходимости рантайм снова задействует их без системного вызова `mmap`.",
    "bigtech_interview": "**Вопрос с собеседования:** В чем разница между системными флагами `MADV_DONTNEED` и `MADV_FREE` в Linux и почему поведение Go 1.12 приводило к сбоям в Kubernetes?\n**Ответ:** \n- `MADV_DONTNEED`: ядро Linux немедленно освобождает физические страницы RAM. При следующем чтении возвращаются нули. RSS процесса мгновенно уменьшается.\n- `MADV_FREE`: ядро только помечает страницы как «кандидаты на очистку», но физически держит их в памяти, пока на сервере нет дефицита RAM.\nВ Kubernetes kubelet следит за метрикой `container_memory_working_set_bytes`. При использовании `MADV_FREE` ядро не обнуляло счетчик, Kubernetes ошибочно считал, что контейнер исчерпал лимит памяти, и прибивал pod сигналом `OOMKilled` (Exit Code 137). Поэтому в Go 1.16 вернули `MADV_DONTNEED`."
  },
  {
    "num": 16,
    "title": "Масштабируемость аллокаций: mcache при GOMAXPROCS=1 vs 8",
    "task": "**[mcache (Per-P Cache)]**: Напиши бенчмарк, который аллоцирует много мелких объектов. Установи `GOMAXPROCS=1` и `GOMAXPROCS=8`. Объясни, почему при большем числе P аллокация быстрее (у каждого P свой локальный кэш `mcache`, не нужны блокировки).",
    "theory": "Главная причина масштабируемости Go на многоядерных архитектурах (NUMA, 64-128 ядер) — архитектура **Per-P Cache (`mcache`)**.\n\nВ классических аллокаторах с глобальной блокировкой кучи (Global Heap Lock) увеличение числа потоков приводит к лавинообразному росту contention (соперничества за мьютекс), и производительность деградирует.\nВ Go:\n- У каждого $P$ свой персональный массив спанов `mcache`.\n- Когда 8 горутин работают на 8 $P$, они выделяют память **в 8 независимых регионах кэша одновременно**, не обращаясь к шине памяти и не вызывая инвалидации кэш-линий (False Sharing).\n- Время на одну аллокацию остается константным, а суммарная пропускная способность растет линейно.",
    "step_by_step": "1. Создайте параллельный бенчмарк `BenchmarkAllocScale`.\n2. Запустите его с флагами `-cpu=1` и `-cpu=8`.\n3. Сравните суммарную пропускную способность (`ns/op`) и убедитесь в отсутствии блокировок.",
    "code_blocks": [
      {
        "filename": "scale_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"runtime\"\n\t\"testing\"\n)\n\ntype WorkItem struct {\n\tA, B, C, D int64\n}\n\n// Параллельный бенчмарк аллокации\nfunc BenchmarkAllocScale(b *testing.B) {\n\tb.ReportAllocs()\n\tb.ResetTimer()\n\n\tb.RunParallel(func(pb *testing.PB) {\n\t\tfor pb.Next() {\n\t\t\t// Выделение памяти из локального mcache текущего P\n\t\t\titem := &WorkItem{A: 1, B: 2, C: 3, D: 4}\n\t\t\truntime.KeepAlive(item)\n\t\t}\n\t})\n}\n",
        "note": "Бенчмарк масштабируемости аллокатора при разном числе P"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -bench=BenchmarkAllocScale -cpu=1,2,4,8 scale_test.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/proc.go` при инициализации или изменении `GOMAXPROCS` вызывается `procresize(nprocs int32)`.\nДля каждого нового $P$ рантайм вызывает `allocmcache()`:\n`pp.mcache = allocmcache()`\nДескриптор `mcache` аллоцируется в специальной области памяти рантайма (`mheap_.cachealloc`), не подверженной сборке мусора.\nБлагодаря этому доступ к полям `p.mcache` гарантированно локален для текущего процессорного ядра.",
    "pitfalls": "1. Линейный рост скорости аллокаций возможен только до тех пор, пока хватает пропускной способности аппаратной шины памяти процессора (Memory Bandwidth). При сотнях ядер шина памяти DRAM становится физическим ограничителем.\n2. Частые аллокации создают работу для GC, который периодически отбирает 25% CPU на разметку (GC CPU fraction).",
    "bigtech_interview": "**Вопрос с собеседования:** Что произойдет с `mcache`, если в рантайме на лету динамически уменьшить `runtime.GOMAXPROCS(2)` с прежних 8?\n**Ответ:** Лишние процессоры $P$ (с индексами от 2 до 7) будут переведены в статус `_Pidle` и отвязаны. Для каждого деактивированного $P$ рантайм вызовет `freemcache(p.mcache)`. Все удерживаемые спаны из их `mcache` будут аккуратно сброшены обратно в соответствующие `mcentral`, предотвращая утечку свободных слотов памяти."
  },
  {
    "num": 17,
    "title": "Слияние спанов Span Coalescing и устранение внешней фрагментации",
    "task": "**Span coalescing**: Когда смежные `mspan`'ы освобождаются, runtime объединяет их для снижения фрагментации.",
    "theory": "Одной из главных угроз для долгоживущих процессов является **внешняя фрагментация памяти** — ситуация, когда в памяти образуется множество свободных мелких блоков, разделенных занятыми, из-за чего невозможно выделить один крупный непрерывный сегмент.\n\nДля решения этой проблемы рантайм Go реализует алгоритм **Span Coalescing (слияние смежных спанов)**:\n1. Когда спан полностью освобождается в фазе Sweep (все объекты в нем стали мусором), он возвращается из `mcentral` в глобальный `pageAlloc` кучи `mheap`.\n2. Аллокатор проверяет соседние страницы слева и справа в Radix Tree.\n3. Если смежные страницы также свободны, они объединяются в один большой непрерывный диапазон страниц (Chunk/Span).\n4. Полученный крупный блок в будущем может быть нарезан под любой другой класс размера или отдан под крупный срез.",
    "step_by_step": "1. Напишите код, моделирующий паттерн создания и освобождения мелких смежных спанов.\n2. Продемонстрируйте объединение страниц памяти в более крупные свободные блоки.\n3. Исследуйте показатели `HeapIdle` и структуру свободных чанков в `pageAlloc`.",
    "code_blocks": [
      {
        "filename": "coalescing.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc main() {\n\tfmt.Println(\"=== Демонстрация Span Coalescing в mheap ===\")\n\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\t// 1. Выделяем 1000 разнородных блоков, которые займут смежные страницы в арене\n\tchunks := make([][]byte, 1000)\n\tfor i := 0; i < len(chunks); i++ {\n\t\tchunks[i] = make([]byte, 16*1024) // по 16 КБ (по 2 страницы на блок)\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tfmt.Printf(\"Выделено памяти под чанки: %d КБ\\n\", (m2.HeapAlloc-m1.HeapAlloc)/1024)\n\n\t// 2. Освобождаем все чанки одновременно\n\tfor i := 0; i < len(chunks); i++ {\n\t\tchunks[i] = nil\n\t}\n\n\t// 3. Запускаем GC: спаны очищаются и сливаются в mheap\n\truntime.GC()\n\n\tvar m3 runtime.MemStats\n\truntime.ReadMemStats(&m3)\n\n\tfmt.Printf(\"Память после GC (HeapAlloc): %d КБ (очищена!)\\n\", m3.HeapAlloc/1024)\n\tfmt.Printf(\"Память в пуле mheap (HeapIdle): %d КБ (слита в непрерывные блоки)\\n\", m3.HeapIdle/1024)\n\n\t// 4. Проверяем, что теперь можно выделить один крупный сплошной блок\n\t// из тех же самых слившихся страниц!\n\thugeChunk := make([]byte, 14*1024*1024) // 14 МБ сплошной блок\n\thugeChunk[0] = 1\n\n\tvar m4 runtime.MemStats\n\truntime.ReadMemStats(&m4)\n\tfmt.Printf(\"HeapSys не вырос при аллокации 14 МБ: переиспользованы слитые страницы! (%d МБ)\\n\",\n\t\tm4.HeapSys/(1024*1024))\n\n\t_ = hugeChunk\n\ttime.Sleep(50 * time.Millisecond)\n}\n",
        "note": "Слияние свободных страниц и выделение крупного блока из пула HeapIdle"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run coalescing.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/mpagealloc.go` метод `pageAlloc.free(base, npages)`:\n1. Вычисляет битовые индексы освобождаемых страниц в битовой карте `chunks`.\n2. Сбрасывает биты занятости в 0.\n3. Проверяет префиксные и суффиксные нули в соседних 64-страничных чанках.\n4. Обновляет сводные поля в промежуточных узлах Radix Tree (`pallocSum`), объединяя свободные интервалы в один максимальный диапазон.\nБлагодаря битовым операциям слияние спанов в Go не требует перестроения сложных связанных списков.",
    "pitfalls": "1. Если хотя бы один маленький объект в спане удерживается живой ссылкой, весь спан не может быть освобожден и слит с соседями (проблема Pinning).\n2. Чрезмерное дробление на разношерстные размеры в долгоживущих коллекциях может препятствовать эффективному слиянию спанов.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое проблема «Pinning» памяти в спанах и как она мешает работе алгоритма Span Coalescing?\n**Ответ:** Если в спане размером 8 КБ размещено 100 объектов по 80 байт, и 99 из них стали мусором, но 1 объект удерживается глобальной переменной, весь 8 КБ спан считается активным (`HeapInuse`). Его страницы нельзя вернуть в `mheap`, нельзя слить с соседями (coalesce) и нельзя вернуть операционной системе. Решение — избегать хранения одиночных долгоживущих мелких объектов внутри кратковременных коллекций."
  },
  {
    "num": 18,
    "title": "Сравнение возврата по значению и по указателю в Escape Analysis",
    "task": "**Escape Analysis (Анализ побега)**: Стек быстрый (выделение памяти сводится к сдвигу указателя регистра CPU), куча — медленная. Напиши две функции: одна возвращает `int`, другая `*int`. Скомпилируй с флагом `go build -gcflags=\"-m\"`. Изучи вывод компилятора: найди фразу `escapes to heap`.",
    "theory": "Размещение данных на стеке и в куче имеет принципиально разные накладные расходы:\n- **Стек (Stack Allocation):**\n  - Выделение памяти: ровно одна ассемблерная инструкция вычитания из указателя стека `SUBQ $N, SP`. Занимает $\\approx 0$ наносекунд.\n  - Очистка: сдвиг `SP` обратно при возврате `ADDQ $N, SP`.\n  - Нулевая нагрузка на сборщик мусора GC.\n- **Куча (Heap Allocation):**\n  - Вызов `runtime.newobject` / `mallocgc`.\n  - Поиск слота в `mcache`, обновление битовых карт `allocBits`.\n  - Учет в графе объектов, барьеры записи и сканирование в фазе GC Mark.\n  - Занимает 10–30 наносекунд на аллокацию + скрытые расходы на сборку мусора.\n\nКогда функция возвращает примитив `int` по значению, значение просто копируется через регистр процессора `AX`. Когда возвращается `*int`, локальная переменная не может умереть вместе со стеком и «убегает» в кучу.",
    "step_by_step": "1. Напишите функцию `valFunc() int`, создающую локальную переменную и возвращающую её по значению.\n2. Напишите функцию `ptrFunc() *int`, возвращающую указатель `&x`.\n3. Скомпилируйте с флагом `go build -gcflags=\"-m -l\"` (флаг `-l` отключает инлайнинг функций для чистоты эксперимента).\n4. Проверьте вывод компилятора на наличие строки `escapes to heap`.",
    "code_blocks": [
      {
        "filename": "escape_simple.go",
        "lang": "go",
        "code": "package main\n\nimport \"fmt\"\n\n//go:noinline\nfunc valFunc() int {\n\tx := 42\n\treturn x // x передается по значению через регистр CPU, остается на стеке\n}\n\n//go:noinline\nfunc ptrFunc() *int {\n\tx := 42\n\treturn &x // &x escapes to heap: компилятор переносит x в кучу\n}\n\nfunc main() {\n\tv := valFunc()\n\tp := ptrFunc()\n\n\tfmt.Printf(\"Значение со стека: %d\\n\", v)\n\tfmt.Printf(\"Значение из кучи : %d (адрес: %p)\\n\", *p, p)\n}\n",
        "note": "Сравнение передачи по значению и побега указателя в кучу"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go build -gcflags=\"-m -l\" escape_simple.go\n"
      }
    ],
    "under_the_hood": "В ассемблерном коде (`go build -gcflags=\"-S -l\"`):\n- В функции `valFunc`:\n  ```text\n  MOVL $42, AX  ; помещаем 42 в регистр возврата\n  RET           ; мгновенный возврат\n  ```\n- В функции `ptrFunc`:\n  ```text\n  LEAQ type:int(SB), AX\n  CALL runtime.newobject(SB) ; аллокация в mcache кучи!\n  MOVQ $42, (AX)             ; запись значения в кучу\n  RET\n  ```\nВызов `runtime.newobject` требует сотен процессорных тактов против одной инструкции `MOVL`!",
    "pitfalls": "1. Преждевременная «оптимизация» передачей указателей на мелкие структуры (`*struct{ a, b int }`) вместо передачи по значению часто ухудшает производительность сервиса, вызывая лишние аллокации в куче и нагружая GC.\n2. Передавайте по указателю только если: 1) объект действительно большой (> 64 байт); 2) метод обязан мутировать состояние структуры; 3) структура содержит мьютекс `sync.Mutex`.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Go правило «передавать по указателю всегда быстрее» является опасным заблуждением?\n**Ответ:** Передача по указателю часто заставляет объект «убежать» в кучу (Escape to heap). Выделение в куче требует работы аллокатора, ухудшает кэш-локальность (L1/L2 cache misses) из-за разыменования указателей и многократно увеличивает накладные расходы сборщика мусора на сканирование графа объектов. Передача небольших структур (до 64–128 байт) по значению происходит прямо на регистрах CPU без единой аллокации в памяти."
  },
  {
    "num": 19,
    "title": "Моделирование фрагментации памяти Pattern Allocation",
    "task": "**Memory fragmentation**: Создайте паттерн аллокаций, вызывающий фрагментацию (множество мелких аллокаций с последующим освобождением через одного). Изучите влияние на memory usage.",
    "theory": "Фрагментация кучи возникает, когда чередующиеся аллокации и освобождения памяти приводят к тому, что спаны не могут быть освобождены целиком.\n\nСценарий «дырявой кучи» (Checkerboard pattern):\n1. Программа аллоцирует $N$ мелких объектов (например, 100 000 объектов класса 32 байта).\n2. Затем программа обнуляет ссылки на каждый **четный** объект ($0, 2, 4, 6 \\dots$), оставляя нечетные живыми.\n3. В результате:\n   - Фактически полезных данных осталось ровно 50%.\n   - Однако в каждом `mspan` ровно половина слотов занята.\n   - Ни один `mspan` не освобожден целиком, поэтому ни одна страница не может быть возвращена в `mheap` или ОС!\n   - Метрика `HeapInuse` остается на уровне 100%, демонстрируя эффект фрагментации.",
    "step_by_step": "1. Выделите массив из 100 000 объектов.\n2. Замерьте `HeapAlloc` и `HeapInuse`.\n3. Освободите ровно 50% объектов через одного и вызовите `runtime.GC()`.\n4. Сравните изменение `HeapAlloc` (упал вдвое) и `HeapInuse` (остался прежним из-за фрагментации).",
    "code_blocks": [
      {
        "filename": "fragmentation_demo.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n)\n\ntype Element struct {\n\tID   int64\n\tData [24]byte // 32 байта суммарно\n}\n\nfunc main() {\n\tconst count = 200_000\n\n\tvar m1, m2, m3 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\t// 1. Создаем 200k объектов\n\tptrs := make([]*Element, count)\n\tfor i := 0; i < count; i++ {\n\t\tptrs[i] = &Element{ID: int64(i)}\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tfmt.Println(\"=== Фаза 1: 100% объектов создано ===\")\n\tfmt.Printf(\"HeapAlloc (живые объекты): %d КБ\\n\", (m2.HeapAlloc-m1.HeapAlloc)/1024)\n\tfmt.Printf(\"HeapInuse (занятые спаны): %d КБ\\n\\n\", m2.HeapInuse/1024)\n\n\t// 2. Освобождаем каждый четный элемент (50% мусора)\n\tfor i := 0; i < count; i += 2 {\n\t\tptrs[i] = nil\n\t}\n\n\truntime.GC()\n\truntime.ReadMemStats(&m3)\n\n\tfmt.Println(\"=== Фаза 2: 50% объектов освобождено через одного ===\")\n\tfmt.Printf(\"HeapAlloc (упал вдвое!)   : %d КБ\\n\", (m3.HeapAlloc-m1.HeapAlloc)/1024)\n\tfmt.Printf(\"HeapInuse (спаны удерживаются): %d КБ\\n\", m3.HeapInuse/1024)\n\n\twaste := float64(m3.HeapInuse-(m3.HeapAlloc-m1.HeapAlloc)) / float64(m3.HeapInuse) * 100\n\tfmt.Printf(\"Потери памяти из-за фрагментации спанов: %.1f%%\\n\", waste)\n\n\truntime.KeepAlive(ptrs)\n}\n",
        "note": "Экспериментальная демонстрация удержания спанов при фрагментации памяти"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run fragmentation_demo.go\n"
      }
    ],
    "under_the_hood": "В структуре `mspan` поле `allocCount` хранит количество занятых слотов.\nВ фазе Sweep GC пересчитывает:\n`s.allocCount = countBits(s.allocBits)`\nЕсли `s.allocCount == 0`, спан возвращается в `mcentral.partial` или `mheap`.\nНо если `s.allocCount > 0` (хотя бы 1 живой объект), спан помечается как частично занятый и остается в памяти процесса, не позволяя высвободить 8 КБ физическую страницу ОС.",
    "pitfalls": "1. Хранение долгоживущих кэшей в виде срезов указателей `[]*Item` с частичным удалением — главная причина фрагментации памяти в микросервисах.\n2. Решение: использовать плоские слайсы структур по значению `[]Item`, циклические буферы или арены.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему срез указателей `[]*MyStruct` порождает фрагментацию памяти, а срез структур `[]MyStruct` принципиально защищен от нее?\n**Ответ:** В срезе указателей каждый элемент аллоцируется независимым вызовом в произвольных спанах кучи. При частичной очистке живые элементы удерживают десятки спанов по всей памяти. В срезе значений `[]MyStruct` все элементы лежат в одном едином непрерывном массиве байт. Память выделяется и освобождается одним куском, что полностью исключает фрагментацию и дает огромный выигрыш по кэш-локальности процессора."
  },
  {
    "num": 20,
    "title": "Конкуренция за блокировки mcentral при истощении mcache",
    "task": "**[mcentral and Locks]**: Напиши программу, которая во всех горутинах на всех P аллоцирует объекты одного и того же размера. Заставь `mcache` исчерпаться, чтобы он пошел за памятью в `mcentral` (глобальный для всех P, с мьютексами). Напиши тест с `go test -race`, чтобы увидеть contention (хотя напрямую мьютексы mcentral в race не светятся, можно оценить падение пропускной способности).",
    "theory": "Хотя Fast-Path аллокации в `mcache` свободен от блокировок, при исчерпании свободных слотов в спане рантайм вынужден идти в Slow-Path — запрашивать новый спан из `mcentral[spc]`.\n\nКаждый `mcentral` защищен внутренним спинлоком `mcentral.lock`.\nЕсли сотни параллельных горутин на всех доступных $P$ одновременно выполняют миллионы аллокаций **строго одного и того же класса размера**:\n1. Их локальные `mcache` для этого класса опустошаются одновременно.\n2. Все $P$ одновременно пытаются захватить блокировку одного и того же `mcentral`.\n3. Возникает аппаратный contention процессорных ядер на шине когерентности кэшей (Cache Coherence Bus contention).",
    "step_by_step": "1. Напишите тест, запускающий горутины по числу ядер CPU, непрерывно аллоцирующие объекты одинакового размера (например, 128 байт).\n2. Замерьте время выполнения при работе с одним общим классом размера.\n3. Разбавьте аллокации объектами разных классов и оцените прирост скорости за счет распределения по разным `mcentral`.",
    "code_blocks": [
      {
        "filename": "mcentral_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"runtime\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype TargetSize128 struct {\n\tData [128]byte\n}\n\ntype VariedSize1 struct {\n\tData [64]byte\n}\n\ntype VariedSize2 struct {\n\tData [256]byte\n}\n\n// Тест 1: Все горутины долбятся в один mcentral[128]\nfunc BenchmarkSingleSizeContention(b *testing.B) {\n\tb.ResetTimer()\n\tb.RunParallel(func(pb *testing.PB) {\n\t\tfor pb.Next() {\n\t\t\tobj := new(TargetSize128)\n\t\t\truntime.KeepAlive(obj)\n\t\t}\n\t})\n}\n\n// Тест 2: Горутины распределяют нагрузку по разным mcentral\nfunc BenchmarkDistributedSizes(b *testing.B) {\n\tb.ResetTimer()\n\tb.RunParallel(func(pb *testing.PB) {\n\t\ti := 0\n\t\tfor pb.Next() {\n\t\t\tif i%2 == 0 {\n\t\t\t\tobj := new(VariedSize1)\n\t\t\t\truntime.KeepAlive(obj)\n\t\t\t} else {\n\t\t\t\tobj := new(VariedSize2)\n\t\t\t\truntime.KeepAlive(obj)\n\t\t\t}\n\t\t\ti++\n\t\t}\n\t})\n}\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\tconst workers = 8\n\twg.Add(workers)\n\n\tfor i := 0; i < workers; i++ {\n\t\tgo func() {\n\t\t\tdefer wg.Done()\n\t\t\tfor j := 0; j < 50_000; j++ {\n\t\t\t\tx := new(TargetSize128)\n\t\t\t\truntime.KeepAlive(x)\n\t\t\t}\n\t\t}()\n\t}\n\twg.Wait()\n}\n",
        "note": "Бенчмарк конкуренции горутин за один mcentral против распределения по классам"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -bench=. -benchtime=2s mcentral_test.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/mcentral.go`:\n```go\nfunc (c *mcentral) cacheSpan() *mspan {\n    lock(&c.lock) // Атомарный спинлок рантайма\n    // Поиск спана в c.partial\n    unlock(&c.lock)\n}\n```\nСпинлок `lock(&c.lock)` использует инструкцию `LOCK CMPXCHG`. При высокой частоте обращений линии кэша процессора переходят в статус Invalidated в протоколе MESI, замедляя обращения всех ядер к оперативной памяти.",
    "pitfalls": "1. Мьютексы рантайма (`runtime.mutex`) не видны в стандартном `sync.Mutex` профиле (`pprof mutex`), так как они реализованы на уровне ядра рантайма.\n2. Их задержки можно увидеть только через системные события в `go tool trace` (события `proc steal` и паузы планировщика).",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Go сделано 136 отдельных структур `mcentral`, а не один общий глобальный пул?\n**Ответ:** Разделение на 136 независимых пулов (по числу подклассов размеров) является классическим паттерном шардирования блокировок (Lock Striping). Запросы на выделение 16 байт, 64 байт и 512 байт конкурируют за совершенно разные мьютексы `mcentral`, что в сотни раз снижает вероятность межъядерной блокировки на многопроцессорных серверах."
  },
  {
    "num": 21,
    "title": "Архитектура арен виртуальной памяти heapArena",
    "task": "**Arena**: Go резервирует большие регионы виртуальной памяти (arenas, 64MB на 64-bit системах) для эффективного управления памятью.",
    "theory": "Куча `mheap` в Go не является одним сплошным неразрывным массивом памяти. Она организована как совокупность независимых регионов фиксированного размера — **Arenas (Арены)**:\n- На 64-битных системах размер одной арены составляет **64 МБ** (`heapArenaBytes = 64 << 20`).\n- На 32-битных системах — **4 МБ**.\n- Каждая арена описывается служебной структурой `heapArena`.\n\nВнутри структуры `heapArena` хранятся метаданные для всех 8192 страниц этой арены:\n- `spans [pagesPerArena]*mspan`: массив указателей, сопоставляющий каждую страницу арены с её спаном.\n- `pageMarks [pagesPerArena / 8]byte`: битовая карта страниц, содержащих живые объекты для GC.\n- `pageInUse [pagesPerArena / 8]byte`: битовая карта распределенных страниц.",
    "step_by_step": "1. Напишите программу, постепенно аллоцирующую память шагами по 64 МБ.\n2. Отследите скачкообразный рост показателя `HeapSys` в `runtime.MemStats`.\n3. Рассчитайте количество активных арен рантайма.",
    "code_blocks": [
      {
        "filename": "arena_growth.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n)\n\nconst arenaSize = 64 * 1024 * 1024 // 64 МБ\n\nfunc main() {\n\tvar m runtime.MemStats\n\truntime.ReadMemStats(&m)\n\n\tfmt.Println(\"=== Исследование роста heapArena (шаг 64 МБ) ===\")\n\tinitialArenas := m.HeapSys / arenaSize\n\tfmt.Printf(\"Стартовый HeapSys: %d МБ (~%d арен по 64 МБ)\\n\\n\",\n\t\tm.HeapSys/(1024*1024), initialArenas)\n\n\tvar storage [][]byte\n\n\t// Выделяем память порциями, провоцируя подключение новых 64 МБ арен\n\tfor step := 1; step <= 3; step++ {\n\t\t// Аллоцируем 70 МБ (гарантированно требует новую 64 МБ арену)\n\t\tchunk := make([]byte, 70*1024*1024)\n\t\tfor i := 0; i < len(chunk); i += 4096 {\n\t\t\tchunk[i] = byte(step)\n\t\t}\n\t\tstorage = append(storage, chunk)\n\n\t\truntime.ReadMemStats(&m)\n\t\tcurrentArenas := m.HeapSys / arenaSize\n\t\tfmt.Printf(\"[Шаг %d] Выделено +70 МБ: HeapAlloc=%4d МБ | HeapSys=%4d МБ | Арен=%2d\\n\",\n\t\t\tstep, m.HeapAlloc/(1024*1024), m.HeapSys/(1024*1024), currentArenas)\n\t}\n\n\tfmt.Println(\"\\nВывод: HeapSys прирастает дискретными квантами арен по 64 МБ.\")\n\truntime.KeepAlive(storage)\n}\n",
        "note": "Дискретный рост памяти кучи квантами heapArena по 64 МБ"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run arena_growth.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/mheap.go`:\nДвухуровневый массив арен:\n`arenas [1 << arenaL1Bits]*[1 << arenaL2Bits]*heapArena`\nПри вычислении дескриптора арены по адресу `p`:\n`arenaIndex := p / heapArenaBytes`\n`l1 := arenaIndex >> arenaL1Shift`\n`l2 := arenaIndex & arenaL2Mask`\n`ha := mheap_.arenas[l1][l2]`\nТакая двухуровневая разреженная таблица (Sparse Array) позволяет адресовать гигантские адресные пространства без предварительного выделения памяти под саму таблицу метаданных.",
    "pitfalls": "1. Если адреса арен в виртуальной памяти фрагментированы сторонними Cgo библиотеками, Go выделяет новые арены по другим свободным адресам, что может приводить к разрывам в адресном пространстве.\n2. Рост `HeapSys` происходит скачками, кратными 64 МБ, поэтому график потребления системной памяти выглядит ступенчатым.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему Go перешел от единой монолитной арены к разреженным аренам (Sparse Arenas) в версии Go 1.11?\n**Ответ:** До Go 1.11 рантайм требовал непрерывного адресного пространства (один гигантский кусок до 512 ГБ). В сложных средах (Cgo, контейнеры Docker с ограничениями адресов, динамические библиотеки) ядро ОС часто не могло выделить такой непрерывный виртуальный кусок из-за конфликтов адресов загрузки библиотек. Sparse Arenas позволяют куче Go собираться из множества независимых 64-мегабайтных кусочков в любых доступных местах виртуальной памяти."
  },
  {
    "num": 22,
    "title": "Внутренняя структура mspan и отладка битовых карт",
    "task": "**Span-ы и mspan.**: Изучите внутреннюю структуру mspan через отладчик (delve) или через анализ `runtime/pprof`: найдите, как распределяются объекты одного класса размера внутри спана, проверьте битовую карту занятости.",
    "theory": "Для максимальной скорости работы сборщика мусора и аллокатора структура `mspan` хранит метаданные в виде компактных 64-битных слов:\n- `allocBits`: текущая битовая карта аллокатора. Бит $i = 1$ означает, что $i$-й слот спана занят объектом.\n- `gcmarkBits`: битовая карта сборщика мусора. Во время фазы Mark воркеры выставляют бит в 1 при достижении объекта.\n- `freeindex`: кешированный индекс первого свободного бита в `allocBits`.\n- `allocCache`: 64-битный процессорный буфер (копия текущего куска `allocBits`). Позволяет находить свободный слот инструкцией `CTZ` (Count Trailing Zeros) без обращения к оперативной памяти!",
    "step_by_step": "1. Напишите программу, эмулирующую битовые операции аллокатора внутри `mspan`.\n2. Смоделируйте поиск свободного слота через процессорный поиск битов.\n3. Продемонстрируйте проверку занятости ячейки по смещению адреса.",
    "code_blocks": [
      {
        "filename": "span_bits.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"math/bits\"\n\t\"unsafe\"\n)\n\n// Эмуляция структуры заголовка mspan\ntype MockSpan struct {\n\tbaseAddr   uintptr\n\telemSize   uintptr\n\tnelems     uintptr\n\tallocBits  uint64 // битовая карта слотов (1 = занят, 0 = свободен)\n\tgcmarkBits uint64 // битовая карта GC (1 = живой)\n}\n\nfunc (s *MockSpan) alloc() (uintptr, error) {\n\t// Инвертируем биты: свободные слоты становятся единицами\n\tfreeBits := ^s.allocBits\n\tif freeBits == 0 {\n\t\treturn 0, fmt.Errorf(\"span is full\")\n\t}\n\n\t// Находим индекс младшего свободного бита за 1 такт процессора\n\tidx := bits.TrailingZeros64(freeBits)\n\tif uintptr(idx) >= s.nelems {\n\t\treturn 0, fmt.Errorf(\"span is full\")\n\t}\n\n\t// Помечаем слот как занятый\n\ts.allocBits |= (1 << idx)\n\taddr := s.baseAddr + uintptr(idx)*s.elemSize\n\treturn addr, nil\n}\n\nfunc main() {\n\tvar memory [64 * 32]byte // 64 слота по 32 байта\n\tspan := MockSpan{\n\t\tbaseAddr: uintptr(unsafe.Pointer(&memory[0])),\n\t\telemSize: 32,\n\t\tnelems:   64,\n\t}\n\n\tfmt.Println(\"=== Эмуляция Lock-Free поиска слотов в mspan ===\")\n\n\t// Выделяем 5 объектов\n\tfor i := 0; i < 5; i++ {\n\t\taddr, err := span.alloc()\n\t\tif err != nil {\n\t\t\tpanic(err)\n\t\t}\n\t\tslot := (addr - span.baseAddr) / span.elemSize\n\t\tfmt.Printf(\"Выделен слот %2d по адресу 0x%x (allocBits: 0x%016x)\\n\",\n\t\t\tslot, addr, span.allocBits)\n\t}\n\n\t// Пометим слоты 0, 2 и 4 как живые в GC\n\tspan.gcmarkBits = 0b10101\n\n\t// Эмуляция фазы Sweep: мгновенная смена масок\n\tfmt.Println(\"\\nФаза Sweep: замена allocBits = gcmarkBits:\")\n\tspan.allocBits = span.gcmarkBits\n\tfmt.Printf(\"Новый allocBits: 0x%016x (слоты 1 и 3 мгновенно освобождены!)\\n\",\n\t\tspan.allocBits)\n}\n",
        "note": "Побитовая логика allocBits и мгновенная очистка слотов в mspan"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run span_bits.go\n"
      }
    ],
    "under_the_hood": "Поле `allocCache` в реальном `mspan` (файл `mspan.go`):\nПри аллокации выполняется:\n```go\nb := s.allocCache\ni := sys.TrailingZeros64(b)\ns.allocCache >>= (i + 1)\ns.freeindex += (i + 1)\n```\nЭто полностью исключает любые циклы и ветвления в Fast-Path! Аллокация одного объекта в Go сводится к 3 машинным инструкциям процессора.",
    "pitfalls": "1. Попытка вмешаться во внутренние структуры `mspan` через `unsafe` на работающем приложении сломает инварианты сборщика мусора и вызовет немедленный Crash процесса (`fatal error: bad sweep`).\n2. Для детальной низкоуровневой отладки структур используйте отладчик `dlv` с командой `p *s`.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое `allocCache` в структуре `mspan` и почему он инвертирован по сравнению с `allocBits`?\n**Ответ:** `allocCache` кэширует 64 бита `allocBits`, начиная с `freeindex`. В нем биты инвертированы: 1 означает **свободный** слот, а 0 — занятый. Это позволяет использовать стандартную аппаратную инструкцию процессора `CTZ` (Count Trailing Zeros, команда `TZCNT` / `BSF` на x86) для мгновенного получения смещения первого свободного слота за 1 такт процессора без поиска в цикле."
  },
  {
    "num": 23,
    "title": "Эвристика роста кучи Heap Growth и GC Pacer",
    "task": "**Heap growth**: Изучите, как runtime решает, когда запрашивать больше памяти у ОС (heuristic based on GC cycles and memory usage).",
    "theory": "Рост кучи Go (`Heap Growth`) управляется контроллером с обратной связью — **GC Pacer** (файл `src/runtime/mgcpacer.go`).\n\nПринцип работы:\n1. В конце каждого цикла GC рантайм фиксирует объем живых данных в куче (`heap_marked`).\n2. На основе параметра `GOGC` (по умолчанию 100%) рассчитывается целевой порог следующей сборки мусора (`trigger` и `goal`):\n   $$\\text{NextGCGoal} = \\text{HeapMarked} \\times \\left(1 + \\frac{\\text{GOGC}}{100}\\right)$$\n3. Если приложение аллоцирует память быстрее, чем работают воркеры GC, или целевой порог превышает текущий размер арен, рантайм в функции `mheap.grow()` запрашивает новые 64 МБ арены у операционной системы.\n4. Начиная с Go 1.19, в эту формулу вмешивается `GOMEMLIMIT`: при приближении к лимиту памяти Pacer игнорирует `GOGC` и запускает GC раньше, предотвращая рост кучи и спасая процесс от OOM.",
    "step_by_step": "1. Напишите программу, которая циклически аллоцирует данные, измеряя динамику `NextGC` и `HeapSys`.\n2. Измените целевое поведение с помощью функции `debug.SetGCPercent`.\n3. Проанализируйте, как частота сборок мусора влияет на потребность в запросе новых страниц у ОС.",
    "code_blocks": [
      {
        "filename": "heap_growth.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"runtime/debug\"\n)\n\nfunc printHeapState(tag string) {\n\tvar m runtime.MemStats\n\truntime.ReadMemStats(&m)\n\tfmt.Printf(\"[%s]\\n\", tag)\n\tfmt.Printf(\"  HeapAlloc: %4d МБ (живые объекты)\\n\", m.HeapAlloc/(1024*1024))\n\tfmt.Printf(\"  NextGC   : %4d МБ (порог триггера следующего GC)\\n\", m.NextGC/(1024*1024))\n\tfmt.Printf(\"  HeapSys  : %4d МБ (память у ОС)\\n\", m.HeapSys/(1024*1024))\n\tfmt.Printf(\"  NumGC    : %4d сборок мусора\\n\\n\", m.NumGC)\n}\n\nfunc main() {\n\tfmt.Println(\"=== Эвристика роста кучи (GC Pacer) ===\")\n\tprintHeapState(\"Старт\")\n\n\t// Устанавливаем GOGC = 100 (цель = x2 от живой памяти)\n\tdebug.SetGCPercent(100)\n\n\t// Аллоцируем 50 МБ данных, удерживая ссылки\n\tdata := make([][]byte, 50)\n\tfor i := 0; i < 50; i++ {\n\t\tdata[i] = make([]byte, 1024*1024)\n\t\tdata[i][0] = 1\n\t}\n\n\truntime.GC()\n\tprintHeapState(\"После аллокации 50 МБ и GC (GOGC=100)\")\n\n\t// Устанавливаем агрессивный GOGC = 20 (цель = +20% от живой памяти)\n\tdebug.SetGCPercent(20)\n\truntime.GC()\n\tprintHeapState(\"После переключения GOGC=20 (NextGC снизился!)\")\n\n\t_ = data\n}\n",
        "note": "Управление эвристикой порога NextGC и ростом кучи через debug.SetGCPercent"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run heap_growth.go\n"
      }
    ],
    "under_the_hood": "В функции `mheap.grow(npages uintptr)`:\n1. Проверяется, есть ли свободные страницы в текущих аренах `pageAlloc`.\n2. Если свободных непрерывных страниц нужного размера нет, рассчитывается объем расширения (минимум 64 МБ — размер одной арены).\n3. Вызывается `sysAlloc` -> системный вызов `mmap(PROT_READ|PROT_WRITE)`.\n4. Инициализируется дескриптор `heapArena` и связывается с Radix Tree свободных страниц.\n5. Новые страницы помечаются в битовой карте как свободные и объединяются со смежными спанами.",
    "pitfalls": "1. Высокое значение `GOGC` (например, 400) снижает нагрузку на процессор, но раздувает кучу в 5 раз, что в контейнерах с жесткими лимитами памяти приведет к аварийному OOMKilled.\n2. При постоянном создании разнокалиберных объектов рост `HeapSys` может происходить быстрее, чем рост `HeapAlloc`, из-за фрагментации свободных страниц внутри арен.",
    "bigtech_interview": "**Вопрос с собеседования:** Как работает механизм GC Pacer в современных версиях Go (1.19+) при одновременной установке `GOGC` и `GOMEMLIMIT`?\n**Ответ:** GC Pacer вычисляет два целевых порога запуска GC: один на основе формулы `HeapMarked * (1 + GOGC/100)`, а второй — на основе оставшегося пространства до `GOMEMLIMIT`. Pacer выбирает **минимальный** из двух порогов. В результате, пока памяти много, приложение работает по правилу `GOGC`; но при приближении к `GOMEMLIMIT` частота сборок мусора плавно возрастает, удерживая память внутри лимита без падения процесса по OOM."
  },
  {
    "num": 24,
    "title": "Ловушка пустого интерфейса: Боксинг и побег в кучу",
    "task": "**Ловушка пустого интерфейса**: Передай простую локальную переменную типа `int` в `fmt.Println(a)`. Запусти Escape Analysis (`-m`). Ты увидишь, что переменная \"сбегает\" в кучу. Почему? Потому что `Println` принимает `interface{}` (any), а передача значения в интерфейс почти всегда требует аллокации в куче на этапе компиляции.",
    "theory": "Передача конкретного значения в параметр типа `interface{}` (или `any`) называется **упаковкой (Boxing / Interface Boxing)**.\n\nИнтерфейс в рантайме Go представляется структурой `eface`:\n```go\ntype eface struct {\n    _type *_type         // указатель на метаданные типа\n    data  unsafe.Pointer // указатель на сами данные\n}\n```\nПоскольку поле `data` является **указателем**, компилятор обязан передать туда адрес в памяти.\nЕсли размер переменной больше размера машинного слова (или компилятор не может доказать, что вызываемая функция не сохранит этот указатель на будущее), компилятор вынужден:\n1. Выделить память в куче (`runtime.convT64`, `runtime.convTstring` и т.д.).\n2. Скопировать значение со стека в эту новую ячейку памяти кучи.\n3. Поместить адрес кучи в `eface.data`.\n\nФункция `fmt.Println(a ...any)` принимает срез пустых интерфейсов, поэтому вызов `fmt.Println(x)` гарантированно отправляет `x` в кучу!",
    "step_by_step": "1. Напишите функцию, передающую `int` в функцию с параметром `int` (без интерфейса).\n2. Напишите функцию, передающую `int` в `fmt.Println`.\n3. Скомпилируйте с `go build -gcflags=\"-m\"` и сравните отчеты Escape Analysis.\n4. Исследуйте вызовы функций `runtime.convT*` в ассемблере.",
    "code_blocks": [
      {
        "filename": "interface_trap.go",
        "lang": "go",
        "code": "package main\n\nimport \"fmt\"\n\n// 1. Быстрая функция без интерфейса: останется на стеке/регистрах\n//go:noinline\nfunc fastPrint(x int) {\n\t_ = x\n}\n\n// 2. Функция, принимающая any: провоцирует boxing\n//go:noinline\nfunc anyPrint(x any) {\n\t_ = x\n}\n\nfunc main() {\n\tnum := 42\n\n\t// Без боксинга: num на стеке\n\tfastPrint(num)\n\n\t// Ловушка интерфейса: num аллоцируется в куче через runtime.convT64\n\tanyPrint(num)\n\n\t// Стандартный fmt.Println: двойной побег (аргумент + срез ...any)\n\tfmt.Println(num)\n}\n",
        "note": "Сравнение прямой передачи и боксинга в interface{} (any)"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go build -gcflags=\"-m\" interface_trap.go\n"
      }
    ],
    "under_the_hood": "В ассемблере функции `main`:\nПри вызове `anyPrint(num)` компилятор генерирует:\n```text\nMOVQ $42, AX\nCALL runtime.convT64(SB)  ; аллоцирует 8 байт в mcache кучи!\nMOVQ AX, (SP)             ; передача адреса в eface.data\nCALL main.anyPrint(SB)\n```\nФункция `runtime.convT64` из `src/runtime/iface.go` выделяет 8-байтный слот из `mcache.alloc` (класс 8 байт) и записывает туда число 42. Каждое логирование в горячем цикле порождает мусор в куче!",
    "pitfalls": "1. Логирование через `log.Printf` или `fmt.Sprintf` внутри циклов с миллионами итераций — классическая причина падения RPS в 5–10 раз из-за интерфейсного боксинга.\n2. В Go 1.21+ появились специализированные логгеры (например, `log/slog` с типом `slog.Int`), позволяющие логировать примитивы без выделения памяти в куче.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему высокопроизводительные библиотеки логирования (Uber Zap, Zerolog) избегают использования `interface{}` и `fmt.Sprintf`?\n**Ответ:** Потому что передача аргументов через `interface{}` приводит к Interface Boxing: на каждое логируемое число, булевый флаг или структуру компилятор Go вызывает `runtime.convT*`, выделяя память в куче. Zap и Zerolog используют типизированные методы (`zap.Int(\"id\", 42)`), которые пишут бинарные данные напрямую в байтовый кольцевой буфер без боксинга и с нулевыми аллокациями в куче (Zero Allocation Logging)."
  },
  {
    "num": 25,
    "title": "Профилирование памяти: inuse_space vs alloc_space в pprof",
    "task": "**Memory profiling**: Используйте `pprof heap profile` с флагом `-inuse_space` и `-alloc_space`. Изучите разницу: \"что сейчас в памяти\" vs \"что было аллоцировано за всё время\".",
    "theory": "Профиль кучи `pprof heap` содержит 4 различных представления (Sample Types):\n1. **`inuse_space`:** Объем виртуальной памяти в байтах, который занят живыми (достижимыми) объектами **в момент снятия профиля**. Идеален для расследования утечек памяти (Memory Leaks) и поиска причин раздувания RSS.\n2. **`alloc_space`:** Суммарный объем памяти в байтах, который был выделен функцией **за всё время работы приложения** (включая уже собранный мусор). Идеален для оптимизации производительности, снижения нагрузки на GC и уменьшения частоты сборок мусора.\n3. **`inuse_objects`:** Количество живых объектов на момент снятия профиля.\n4. **`alloc_objects`:** Суммарное количество созданных объектов за всё время.",
    "step_by_step": "1. Напишите сервис с двумя функциями: одна удерживает долгоживущий буфер (`inuse_space`), другая генерирует временный мусор в цикле (`alloc_space`).\n2. Сохраните профиль кучи с помощью `pprof.WriteHeapProfile`.\n3. Исследуйте профиль с переключением флагов `-inuse_space` и `-alloc_space`.",
    "code_blocks": [
      {
        "filename": "pprof_spaces.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"os\"\n\t\"runtime\"\n\t\"runtime/pprof\"\n\t\"time\"\n)\n\n// Долгоживущая память: останется в inuse_space\nvar persistentData [][]byte\n\nfunc retainMemory() {\n\tfmt.Println(\"Выделяем 20 МБ долгоживущей памяти...\")\n\tfor i := 0; i < 20; i++ {\n\t\tpersistentData = append(persistentData, make([]byte, 1024*1024))\n\t}\n}\n\n// Временный мусор: много alloc_space, но 0 в inuse_space\nfunc generateGarbage() {\n\tfmt.Println(\"Генерируем 100 МБ временного мусора...\")\n\tfor i := 0; i < 100; i++ {\n\t\ttmp := make([]byte, 1024*1024)\n\t\ttmp[0] = 1\n\t\t_ = tmp\n\t}\n}\n\nfunc main() {\n\tretainMemory()\n\tgenerateGarbage()\n\n\t// Принудительно вызываем GC, чтобы временный мусор ушел из inuse\n\truntime.GC()\n\n\tf, err := os.Create(\"spaces.pprof\")\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tdefer f.Close()\n\n\tif err := pprof.WriteHeapProfile(f); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfmt.Println(\"Профиль сохранен в spaces.pprof.\")\n\tfmt.Println(\"Сравните вывод команд:\")\n\tfmt.Println(\"  go tool pprof -top -inuse_space spaces.pprof\")\n\tfmt.Println(\"  go tool pprof -top -alloc_space spaces.pprof\")\n\n\ttime.Sleep(50 * time.Millisecond)\n}\n",
        "note": "Сравнение долгоживущей памяти и суммарных аллокаций в pprof"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run pprof_spaces.go\ngo tool pprof -top -inuse_space spaces.pprof\ngo tool pprof -top -alloc_space spaces.pprof\n"
      }
    ],
    "under_the_hood": "Сборщик профиля кучи рантайма (`src/runtime/mprof.go`):\nВ хеш-таблице `mbuckets` для каждого стектрейса аллокации хранятся 4 счетчика:\n- `alloc_bytes`, `alloc_objects`: увеличиваются при каждом вызове `mallocgc`.\n- `free_bytes`, `free_objects`: увеличиваются при освобождении объекта в фазе Sweep GC.\n- `inuse_bytes = alloc_bytes - free_bytes`.\n- `inuse_objects = alloc_objects - free_objects`.\nПри вызове `WriteHeapProfile` эти счетчики упаковываются в стандартный gzip proto-буфер.",
    "pitfalls": "1. Оптимизация только по `inuse_space` может скрыть факт того, что функция выделяет 10 ГБ/сек временных объектов, вызывая троттлинг CPU сборщиком мусора.\n2. Оптимизация только по `alloc_space` не поможет решить проблему утечки памяти, если утекает всего 10 КБ в минуту.",
    "bigtech_interview": "**Вопрос с собеседования:** Если сервис периодически убивается по OOM в Kubernetes, но в профиле `inuse_space` занято всего 200 МБ из лимита в 1 ГБ, куда смотреть в первую очередь?\n**Ответ:** \n1. Проверить `alloc_space` и темп аллокаций: при высокой скорости создания объектов GC может отставать (GC Debt), вызывая кратковременный всплеск потребления памяти до следующей сборки.\n2. Проверить фрагментацию кучи через `runtime.MemStats`: разницу между `HeapAlloc` (200 МБ) и `HeapInuse`/`HeapSys` (1 ГБ).\n3. Проверить утечки памяти вне кучи Go: память Cgo, дескрипторы сокетов или память рантайма (стеки горутин)."
  },
  {
    "num": 26,
    "title": "10 паттернов Escape Analysis: Глубокий анализ побега переменных",
    "task": "Напишите 10 функций с разными паттернами (возврат указателя, слайс, замыкание, интерфейс). Используйте `go build -gcflags=\"-m -m\"` и для каждой функции объясните, почему переменная «убегает» (escapes to heap) или остаётся на стеке.",
    "theory": "Анализ побега компилятора Go (`-gcflags=\"-m -m\"`) выполняет глубокий статический анализ потока графа управления (SSA Dataflow).\n\nКлючевые правила побега в кучу:\n1. **Возврат адреса (`&x`):** время жизни объекта превышает стек вызова.\n2. **Передача в интерфейс (`any(x)`):** стирание типов требует размещения адреса.\n3. **Замыкание (Closure):** переменная захватывается замыканием, вызываемым после возврата.\n4. **Динамический размер (`make([]T, n)`):** размер неизвестен на этапе компиляции.\n5. **Слишком большой размер:** структуры $> 64$ КБ вытесняются с 2 КБ стека в кучу.\n6. **Отправка указателя в канал (`ch <- &x`):** время жизни объекта передается другой горутине.\n7. **Передача в горутину (`go func(&x)`):** стек горутины независим.\n8. **Косвенное сохранение (`*ptr = &x`):** утечка через косвенный указатель.\n9. **Интерфейс без побега:** компилятор может доказать безопасность, если функция инлайнится.\n10. **Слайс константного размера:** `make([]int, 10)` остается на стеке при отсутствии побега.",
    "step_by_step": "1. Напишите файл `patterns.go` с 10 функциями, реализующими каждый из указанных паттернов.\n2. Скомпилируйте файл с флагом `go build -gcflags=\"-m -m\" patterns.go`.\n3. Проанализируйте подробный вывод компилятора с объяснением причин каждого побега.",
    "code_blocks": [
      {
        "filename": "patterns.go",
        "lang": "go",
        "code": "package main\n\n// 1. Возврат указателя наружу -> ESCAPES\n//go:noinline\nfunc p1_returnPtr() *int {\n\tx := 10\n\treturn &x\n}\n\n// 2. Локальная передача по значению -> STACK\n//go:noinline\nfunc p2_valOnly() int {\n\tx := 20\n\treturn x\n}\n\n// 3. Замыкание, возвращаемое наружу -> ESCAPES\n//go:noinline\nfunc p3_closureEscape() func() int {\n\tx := 30\n\treturn func() int {\n\t\treturn x\n\t}\n}\n\n// 4. Слайс с динамическим размером n -> ESCAPES\n//go:noinline\nfunc p4_dynamicSlice(n int) []int {\n\treturn make([]int, n)\n}\n\n// 5. Слайс с малым константным размером (без побега) -> STACK\n//go:noinline\nfunc p5_constSlice() int {\n\ts := make([]int, 4)\n\ts[0] = 50\n\treturn s[0]\n}\n\n// 6. Слишком большой массив (> 64KB) -> ESCAPES\n//go:noinline\nfunc p6_largeArray() int {\n\tvar big [70 * 1024]byte\n\tbig[0] = 60\n\treturn int(big[0])\n}\n\n// 7. Передача в пустой интерфейс -> ESCAPES\n//go:noinline\nfunc p7_interfaceEscape(val any) any {\n\treturn val\n}\n\n// 8. Отправка указателя в канал -> ESCAPES\n//go:noinline\nfunc p8_channelEscape(ch chan *int) {\n\tx := 80\n\tch <- &x\n}\n\n// 9. Запись указателя в переданную структуру -> ESCAPES\ntype Container struct {\n\tPtr *int\n}\n\n//go:noinline\nfunc p9_structEscape(c *Container) {\n\tx := 90\n\tc.Ptr = &x\n}\n\n// 10. Локальный указатель без побега наружу -> STACK\n//go:noinline\nfunc p10_localPtr() int {\n\tx := 100\n\tp := &x\n\treturn *p\n}\n\nfunc main() {\n\t_ = p1_returnPtr()\n\t_ = p2_valOnly()\n\tf := p3_closureEscape()\n\t_ = f()\n\t_ = p4_dynamicSlice(5)\n\t_ = p5_constSlice()\n\t_ = p6_largeArray()\n\t_ = p7_interfaceEscape(70)\n\tch := make(chan *int, 1)\n\tp8_channelEscape(ch)\n\tvar c Container\n\tp9_structEscape(&c)\n\t_ = p10_localPtr()\n}\n",
        "note": "10 фундаментальных паттернов Escape Analysis в компиляторе Go"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go build -gcflags=\"-m -m\" patterns.go 2>&1 | grep -E \"escapes to heap|does not escape\"\n"
      }
    ],
    "under_the_hood": "Флаг `-m -m` включает режим максимальной подробности алгоритма анализа побега.\nКомпилятор печатает причины решений:\n- `from &x (address-of) to return (return)`\n- `make([]int, n) escapes to heap: non-constant size`\n- `too large for stack` (если размер массива превышает порог `maxStackVarSize = 64 * 1024` байт)\n- `&x does not escape` (если компилятор доказал, что локальный указатель разыменовывается в том же стеке).",
    "pitfalls": "1. Замыкания в Go могут захватывать переменные по значению или по ссылке. Если замыкание переживает родительскую функцию, все захваченные переменные немедленно перемещаются в кучу.\n2. Небольшие слайсы до 64 КБ остаются на стеке только если компилятор на 100% уверен в константности их размера.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему локальный слайс `s := make([]byte, 65536)` может выделиться на стеке, а `s := make([]byte, 65537)` гарантированно уйдет в кучу?\n**Ответ:** В компиляторе Go константа `maxStackVarSize` равна 64 КБ (65536 байт). Стартовый стек горутины составляет всего 2 КБ, и хотя он динамически расширяется, компилятор защищает горутины от мгновенного переполнения стека крупными переменными. Любая локальная переменная, чей размер строго превышает 64 КБ, принудительно перенаправляется в кучу аллокатором `mallocgc`."
  },
  {
    "num": 27,
    "title": "Выделение крупных объектов Large Objects (> 32 КБ) напрямую из mheap",
    "task": "**[mheap and Arenas]**: Аллоцируй очень большой слайс (например, 100 MB). Это Large Object (> 32KB). Объясни, как `mheap` выделяет под это непрерывную арену страниц напрямую у ОС (через `mmap`).",
    "theory": "Объекты размером более **32768 байт (32 КБ)** классифицируются рантаймом Go как **Large Objects (Крупные объекты)**.\n\nМеханика работы с Large Objects:\n1. Крупные объекты полностью исключены из локальных кэшей `mcache` и `mcentral`.\n2. Запрос направляется непосредственно в глобальную кучу `mheap`.\n3. Рассчитывается необходимое количество 8 КБ страниц:\n   $$N_{\\text{pages}} = \\lceil \\text{size} / 8192 \\rceil$$\n4. Аллокатор страниц `pageAlloc` находит в Radix Tree диапазон из $N$ непрерывных страниц.\n5. Создается индивидуальный дескриптор `mspan` специального нулевого класса (`spanclass = 0`).\n6. Если диапазона страниц в существующих аренах нет, вызывается системный вызов `mmap` для выделения новых 64 МБ арен.",
    "step_by_step": "1. Выделите слайс размером 100 МБ.\n2. Отследите изменение системных метрик памяти через `runtime.ReadMemStats`.\n3. Подтвердите, что аллокация крупного объекта не затронула счетчики `mcache` мелких классов размеров.",
    "code_blocks": [
      {
        "filename": "large_alloc.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc main() {\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\tconst size100MB = 100 * 1024 * 1024 // 100 МБ > 32 КБ\n\n\tfmt.Println(\"=== Аллокация Large Object (100 МБ) напрямую из mheap ===\")\n\tfmt.Printf(\"До аллокации   : HeapAlloc=%d МБ | HeapSys=%d МБ\\n\",\n\t\tm1.HeapAlloc/(1024*1024), m1.HeapSys/(1024*1024))\n\n\t// Выделяем 100 МБ: mheap аллоцирует 12800 страниц по 8 КБ\n\tlargeSlice := make([]byte, size100MB)\n\tfor i := 0; i < len(largeSlice); i += 1024 * 1024 {\n\t\tlargeSlice[i] = 1 // физически активируем страницы\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tfmt.Printf(\"После аллокации: HeapAlloc=%d МБ | HeapSys=%d МБ\\n\",\n\t\tm2.HeapAlloc/(1024*1024), m2.HeapSys/(1024*1024))\n\tfmt.Printf(\"Дельта HeapAlloc: %d МБ\\n\", (m2.HeapAlloc-m1.HeapAlloc)/(1024*1024))\n\n\t// Проверяем количество страниц\n\tpagesNeeded := size100MB / 8192\n\tfmt.Printf(\"Выделено страниц по 8 КБ: %d страниц непрерывного спана\\n\", pagesNeeded)\n\n\truntime.KeepAlive(largeSlice)\n\ttime.Sleep(50 * time.Millisecond)\n}\n",
        "note": "Прямая аллокация Large Object (100 МБ) через mheap"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run large_alloc.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/malloc.go`:\n```go\nfunc largeAlloc(size uintptr, needzero bool, noscan bool) *mspan {\n    npages := size >> _PageShift\n    if size&_PageMask != 0 {\n        npages++\n    }\n    // Захват глобального лока кучи\n    lock(&mheap_.lock)\n    s := mheap_.alloc(npages, spanClass(0))\n    unlock(&mheap_.lock)\n    return s\n}\n```\nПоскольку `spanClass = 0`, спан не привязан ни к одному из 67 классов размеров и существует как единичный монолитный блок памяти.",
    "pitfalls": "1. Захват `mheap_.lock` блокирует все остальные потоки $M$, пытающиеся выделить крупные объекты или пополнить `mcache`. Частые аллокации объектов $> 32$ КБ создают серьезный contention.\n2. Для крупных буферов передачи данных (сеть, сжатие, дисковый I/O) обязательно используйте переиспользуемые пулы `sync.Pool`.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Go крупные объекты (> 32 КБ) не кэшируются в `mcache`, а выделяются каждый раз напрямую из `mheap`?\n**Ответ:** Крупные объекты слишком велики, чтобы держать их в локальных кэшах $P$: кэширование 100-мегабайтных блоков на каждом из 64 процессоров $P$ мгновенно привело бы к исчерпанию физической памяти сервера. Кроме того, частота аллокаций крупных объектов в нормальных сервисах на порядки ниже, чем мелких, поэтому накладные расходы на обращение к `mheap` остаются пренебрежимо малы."
  },
  {
    "num": 28,
    "title": "Escape Analysis при передаче данных в горутины и каналы",
    "task": "**Escape analysis**: Напишите код с переменными, которые \"убегают\" в heap (возвращаются из функции, передаются в горутины). Используйте `go build -gcflags=\"-m\"` для наблюдения escape analysis.",
    "theory": "Передача указателей между горутинами через аргументы `go func()` или через каналы `chan *T` — один из ключевых триггеров побега в кучу.\n\nПричина побега:\n1. Каждая горутина имеет собственный независимый стек (начинается с 2 КБ).\n2. Стек родительской горутины может завершиться и быть освобожден (или вырасти и переместиться) в любой произвольный момент времени независимо от дочерней горутины.\n3. Компилятор не может гарантировать, что родительская функция не завершится до того, как дочерняя горутина прочитает данные по указателю.\n4. Поэтому **любой указатель, переданный в другую горутину, гарантированно вытесняется в кучу**.",
    "step_by_step": "1. Напишите функцию, передающую указатель на локальную переменную в фоновую горутину.\n2. Напишите функцию, отправляющую указатель в канал.\n3. Скомпилируйте с `-gcflags=\"-m\"` и убедитесь в наличии строки `escapes to heap`.",
    "code_blocks": [
      {
        "filename": "goroutine_escape.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n)\n\ntype Job struct {\n\tID   int\n\tData string\n}\n\n// 1. Побег через запуск горутины\nfunc spawnWorker() {\n\tjob := Job{ID: 1, Data: \"async task\"}\n\tvar wg sync.WaitGroup\n\twg.Add(1)\n\n\t// job утекает в кучу, так как &job передается в независимый стек горутины\n\tgo func(j *Job) {\n\t\tdefer wg.Done()\n\t\tfmt.Printf(\"Worker processing job: %d\\n\", j.ID)\n\t}(&job)\n\n\twg.Wait()\n}\n\n// 2. Побег через отправку указателя в канал\nfunc sendThroughChannel(ch chan<- *Job) {\n\tlocalJob := Job{ID: 2, Data: \"channel task\"}\n\t// localJob утекает в кучу, так как получатель канала находится в неизвестном стеке\n\tch <- &localJob\n}\n\nfunc main() {\n\tspawnWorker()\n\n\tch := make(chan *Job, 1)\n\tsendThroughChannel(ch)\n\treceived := <-ch\n\tfmt.Printf(\"Received job from channel: %d\\n\", received.ID)\n}\n",
        "note": "Побег переменных в кучу при взаимодействии с горутинами и каналами"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go build -gcflags=\"-m\" goroutine_escape.go\n"
      }
    ],
    "under_the_hood": "В графе SSA компилятора:\nОперация `go func(...)` транслируется в `runtime.newproc`.\nПараметры горутины копируются в фрейм создания. Если среди параметров есть указатель на локальную переменную текущей функции, ребро графа связывает локальную переменную с глобальным контекстом планировщика (`EscScopeHeap`).\nАналогично операция отправки в канал `runtime.chansend` рассматривается компилятором как потенциальная передача в параллельный поток выполнения.",
    "pitfalls": "1. Передача структур по значению через канал (`chan Job` вместо `chan *Job`) для небольших объектов позволяет избежать аллокации в куче! Данные просто копируются в кольцевой буфер `hchan`.\n2. Передача указателей на переменные цикла в горутину `go func() { ...(&v) }` приводит не только к аллокации в куче, но и к классическому Data Race.",
    "bigtech_interview": "**Вопрос с собеседования:** Что эффективнее передавать через канал: `chan T` (по значению) или `chan *T` (по указателю)?\n**Ответ:** \n- Если структура небольшая (до 64–128 байт), эффективнее передавать `chan T` по значению: данные копируются напрямую внутри кольцевого буфера канала без единой аллокации в куче и без создания работы для сборщика мусора.\n- Если структура крупная (> 512 байт), копирование байтов в буфер канала становится дорогим, и предпочтительнее передавать `chan *T` по указателю (желательно используя `sync.Pool` для переиспользования памяти)."
  },
  {
    "num": 29,
    "title": "Профилирование 1 млн мелких объектов в pprof heap",
    "task": "Создайте программу, которая аллоцирует 1 млн маленьких объектов (`struct{ a, b int }`). Снимите `go tool pprof` (heap). Объясните, почему в профиле видны «span» размером 8, 16, 32 байта.",
    "theory": "Структура `struct{ a, b int }` на 64-битной архитектуре занимает ровно:\n$$2 \\times 8 = 16 \\text{ байт}$$\nПри создании 1 000 000 таких структур в куче:\n1. Аллокатор Go сопоставляет 16 байт с **Size Class 2 (16 байт)**.\n2. В куче аллоцируются спаны класса 16 байт. В одном 8 КБ спане помещается ровно:\n   $$8192 / 16 = 512 \\text{ объектов}$$\n3. Суммарно для 1 млн объектов требуется около $1\\,000\\,000 / 512 \\approx 1953$ спанов.\n4. В профиле `pprof heap` отчетливо видно распределение памяти по соответствующим спанам и классам размеров.",
    "step_by_step": "1. Создайте 1 миллион структур `struct{ a, b int }` в куче.\n2. Сохраните профиль кучи через `pprof.WriteHeapProfile`.\n3. Откройте профиль в консольном `pprof` и найдите строку функции создания объектов.",
    "code_blocks": [
      {
        "filename": "million_objects.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"os\"\n\t\"runtime\"\n\t\"runtime/pprof\"\n\t\"time\"\n\t\"unsafe\"\n)\n\ntype Pair struct {\n\tA int\n\tB int\n}\n\nfunc main() {\n\tfmt.Printf(\"Размер структуры Pair: %d байт (Size Class 2 в Go)\\n\", unsafe.Sizeof(Pair{}))\n\n\tconst count = 1_000_000\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\t// Аллоцируем 1 млн маленьких объектов\n\tslice := make([]*Pair, count)\n\tfor i := 0; i < count; i++ {\n\t\tslice[i] = &Pair{A: i, B: i * 2}\n\t}\n\n\truntime.ReadMemStats(&m2)\n\tfmt.Printf(\"Выделено памяти: %d МБ\\n\", (m2.HeapAlloc-m1.HeapAlloc)/(1024*1024))\n\tfmt.Printf(\"Всего Mallocs  : %d\\n\", m2.Mallocs-m1.Mallocs)\n\n\t// Сохраняем heap profile\n\tf, err := os.Create(\"million.pprof\")\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tdefer f.Close()\n\n\tif err := pprof.WriteHeapProfile(f); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfmt.Println(\"Профиль сохранен в million.pprof\")\n\truntime.KeepAlive(slice)\n\ttime.Sleep(50 * time.Millisecond)\n}\n",
        "note": "Массовое создание 1 млн объектов и снятие heap profile"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run million_objects.go\ngo tool pprof -top -inuse_space million.pprof\n"
      }
    ],
    "under_the_hood": "В `src/runtime/sizeclasses.go`:\nКласс 2:\n- `size = 16`\n- `npages = 1` (8 КБ)\n- `nelems = 512`\n- `tail waste = 0 байт` (8192 делится на 16 нацело без остатка!).\nПоэтому класс 16 байт является одним из самых эффективных классов в рантайме Go с 0% внутренней фрагментации.",
    "pitfalls": "1. Хотя сами структуры заняли 16 МБ, срез указателей `[]*Pair` на 1 млн элементов сам по себе весит 8 МБ (`1_000_000 * 8 байт`). Итого память удваивается.\n2. 1 млн отдельных объектов создают колоссальную нагрузку на фазу Mark сборщика мусора, так как GC обязан обойти 1 миллион указателей.",
    "bigtech_interview": "**Вопрос с собеседования:** Как оптимизировать структуру данных для хранения 1 миллиона пар `(int, int)`, чтобы снизить расход памяти и нагрузку на GC в десятки раз?\n**Ответ:** Заменить срез указателей `[]*Pair` на срез структур по значению `[]Pair`:\n1. Память выделится одним монолитным куском (16 МБ) вместо 1 млн разрозненных аллокаций.\n2. Поскольку в структуре `Pair` нет указателей, весь срез помечается флагом `noscan`.\n3. Сборщик мусора пропустит этот срез целиком за 1 шаг, сократив паузы GC с миллисекунд до микросекунд."
  },
  {
    "num": 30,
    "title": "Сравнение динамического стека Go со стеками Java и C++",
    "task": "**Stack allocation**: Изучите, как Go аллоцирует стеки горутин (начальный размер 2KB, динамически растёт/сжимается). Сравните с фиксированными стеками в Java/C++.",
    "theory": "Сравнение моделей работы со стеком:\n\n| Характеристика | Go Goroutines | C++ (pthreads) / Java Threads |\n| :--- | :--- | :--- |\n| **Начальный размер** | **2 КБ** (2048 байт) | **1 МБ – 8 МБ** (фиксированный) |\n| **Модель роста** | Динамический непрерывный стек (x2 при нехватке) | Фиксированный стек с Guard Page |\n| **Память на 100k потоков**| $\\approx \\mathbf{200 \\text{ МБ}}$ | $\\approx \\mathbf{100 \\text{ ГБ}}$ (невозможно запустить!) |\n| **Сжатие стека** | Да (GC ужимает вдвое, если занято $< 1/4$) | Нет (память удерживается процессом) |\n| **Переполнение стека** | Плавное удвоение (до 1 ГБ) | Падение `SIGSEGV` / `StackOverflowError` |\n\nБлагодаря 2 КБ динамическому стеку один Go-процесс может легко удерживать сотни тысяч активных горутин на обычном ноутбуке.",
    "step_by_step": "1. Напишите код, запускающий 50 000 легковесных горутин.\n2. Замерьте суммарное потребление памяти через `runtime.MemStats`.\n3. Рассчитайте фактический расход памяти на один стек горутины.",
    "code_blocks": [
      {
        "filename": "stack_comparison.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\tconst goroutines = 50_000\n\n\tvar m1, m2 runtime.MemStats\n\truntime.GC()\n\truntime.ReadMemStats(&m1)\n\n\tvar wg sync.WaitGroup\n\twg.Add(goroutines)\n\trelease := make(chan struct{})\n\n\t// Запускаем 50 000 горутин со стартовым 2 КБ стеком\n\tfor i := 0; i < goroutines; i++ {\n\t\tgo func() {\n\t\t\twg.Done()\n\t\t\t<-release // удерживаем горутину спящей\n\t\t}()\n\t}\n\n\twg.Wait()\n\truntime.ReadMemStats(&m2)\n\n\tstackBytes := m2.StackInuse - m1.StackInuse\n\tfmt.Printf(\"=== Сравнение стеков памяти ===\\n\")\n\tfmt.Printf(\"Запущено горутин: %d\\n\", goroutines)\n\tfmt.Printf(\"Память под стеки (StackInuse): %d МБ\\n\", stackBytes/(1024*1024))\n\tfmt.Printf(\"Средний размер стека: %.2f КБ на горутину\\n\", float64(stackBytes)/goroutines/1024)\n\tfmt.Printf(\"\\nДля сравнения: 50 000 потоков Java/C++ с 2 МБ стеком потребовали бы 100 ГБ RAM!\\n\")\n\n\tclose(release)\n\ttime.Sleep(50 * time.Millisecond)\n}\n",
        "note": "Замер потребления памяти 50 000 горутин со стартовыми стеками по 2 КБ"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run stack_comparison.go\n"
      }
    ],
    "under_the_hood": "Стек горутины выделяется из пула стеков рантайма (`src/runtime/stack.go`).\nРантайм держит многоуровневый пул кэшей свободных стеков для размеров 2 КБ, 4 КБ, 8 КБ, 16 КБ, 32 КБ.\nПри завершении горутины её 2 КБ стек не возвращается в ОС, а кладется в локальный список `p.gFree.stack`, готовый к повторному использованию за несколько наносекунд.",
    "pitfalls": "1. Если горутина временно потребовала большой стек (например, 1 МБ при парсинге XML), а затем вернулась в легкий режим, сборщик мусора сожмет её стек обратно только в следующем цикле GC при условии `used < capacity / 4`.\n2. Рекурсивные функции без базового случая переполнят стек до лимита 1 ГБ, после чего процесс упадет.",
    "bigtech_interview": "**Вопрос с собеседования:** Как устроен механизм сжатия стека (Stack Shrinking) в Go и почему он необходим?\n**Ответ:** Если горутина временно выполнила глубокий вызов (раздув стек, например, до 64 КБ), а затем вернулась в режим ожидания сообщений из канала, удержание 64 КБ на миллион горутин привело бы к исчерпанию десятков гигабайт памяти. Поэтому во время фазы Stop-The-World или конкурентного обхода GC проверяет: если фактическое использование стека горутины меньше 1/4 его емкости, рантайм выделяет новый стек в 2 раза меньше, копирует фреймы через `copystack` и возвращает лишнюю память в пул."
  },
  {
    "num": 31,
    "title": "Ложное разделение False Sharing и выравнивание по кэш-линиям",
    "task": "**[Каверзный кейс — False Sharing]**: Напиши структуру `Counter` с полем `n int64`. Создай массив из 8 таких счетчиков. Запусти 8 горутин, каждая инкрементирует свой счетчик (`counters[i].n`). Замерь время. Затем добавь padding (отступы), чтобы каждый счетчик занимал ровно 64 байта (размер кэш-линии процессора). Замерь время снова. Оцени колоссальную разницу в скорости.",
    "theory": "**False Sharing (Ложное разделение кэш-линий)** — один из самых коварных аппаратных эффектов многопоточности.\n\nМеханика процессора:\n1. Процессор обменивается данными с оперативной памятью не отдельными байтами, а блоками фиксированного размера — **кэш-линиями (Cache Lines)** размером **64 байта** (на архитектурах x86-64 и ARM64).\n2. Если две разные переменные лежат рядом в памяти и попадают в одну общую 64-байтную кэш-линию, ядра процессора вынуждены непрерывно синхронизировать её состояние через протокол когерентности кэшей (MESI/MOESI).\n3. Когда ядро 1 пишет в переменную $A$, вся кэш-линия в кэше ядра 2 помечается как `Invalidated`, заставляя ядро 2 перезагружать данные из общей шины, даже если оно работает с совершенно другой независимой переменной $B$!\n\nРешение — **Cache Line Padding**: добавление фиктивных байтов отступа, чтобы каждый счетчик гарантированно занимал ровно 64 байта.",
    "step_by_step": "1. Создайте структуру `NaiveCounter` (8 байт).\n2. Создайте структуру `PaddedCounter` с отступом `_ [56]byte` (ровно 64 байта).\n3. Напишите бенчмарк параллельного инкремента 8 независимых счетчиков в 8 горутинах.\n4. Сравните скорость выполнения: версия с паддингом в 3–10 раз быстрее!",
    "code_blocks": [
      {
        "filename": "false_sharing_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"sync\"\n\t\"testing\"\n\t\"unsafe\"\n)\n\n// 8 байт: 8 счетчиков помещаются в одну общую 64-байтную кэш-линию!\ntype NaiveCounter struct {\n\tN int64\n}\n\n// 64 байта: каждый счетчик занимает свою собственную кэш-линию CPU\ntype PaddedCounter struct {\n\tN   int64\n\tPad [56]byte // 8 + 56 = 64 байта\n}\n\nfunc BenchmarkFalseSharing(b *testing.B) {\n\tvar counters [8]NaiveCounter\n\n\tb.ResetTimer()\n\tb.RunParallel(func(pb *testing.PB) {\n\t\tvar wg sync.WaitGroup\n\t\tfor i := 0; i < 8; i++ {\n\t\t\twg.Add(1)\n\t\t\tgo func(idx int) {\n\t\t\t\tdefer wg.Done()\n\t\t\t\tfor j := 0; j < b.N; j++ {\n\t\t\t\t\tcounters[idx].N++ // Жесточайший False Sharing!\n\t\t\t\t}\n\t\t\t}(i)\n\t\t}\n\t\twg.Wait()\n\t})\n}\n\nfunc BenchmarkPaddedNoSharing(b *testing.B) {\n\tvar counters [8]PaddedCounter\n\n\tb.ResetTimer()\n\tb.RunParallel(func(pb *testing.PB) {\n\t\tvar wg sync.WaitGroup\n\t\tfor i := 0; i < 8; i++ {\n\t\t\twg.Add(1)\n\t\t\tgo func(idx int) {\n\t\t\t\tdefer wg.Done()\n\t\t\t\tfor j := 0; j < b.N; j++ {\n\t\t\t\t\tcounters[idx].N++ // Нет False Sharing!\n\t\t\t\t}\n\t\t\t}(i)\n\t\t}\n\t\twg.Wait()\n\t})\n}\n\nfunc main() {\n\tprintln(\"Размер NaiveCounter :\", unsafe.Sizeof(NaiveCounter{}))\n\tprintln(\"Размер PaddedCounter:\", unsafe.Sizeof(PaddedCounter{}))\n}\n",
        "note": "Бенчмарк устранения эффекта False Sharing через Cache Line Padding"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -bench=. -benchtime=2s false_sharing_test.go\n"
      }
    ],
    "under_the_hood": "В структуре `PaddedCounter`:\nРазмер равен ровно 64 байтам. В массиве `[8]PaddedCounter` адрес каждого следующего счетчика смещен ровно на 64 байта: `0x00, 0x40, 0x80, 0xC0...`\nКаждый счетчик попадает в отдельную аппаратную строку кэша L1/L2 соответствующего процессорного ядра.\nСтатус строки в протоколе MESI остается `Modified` локально для ядра, исключая трафик по шине межпроцессорной когерентности.",
    "pitfalls": "1. Паддинг расходует дополнительную оперативную память. Используйте его только в высококонкурентных горячих структурах (метрики, счетчики, кольцевые очереди lock-free).\n2. На некоторых архитектурах (например, IBM zSeries или Apple M-серия) размер кэш-линии может составлять 128 байт.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое False Sharing (ложное разделение кэш-линий) и как компилятор или стандартная библиотека Go решают эту проблему?\n**Ответ:** False Sharing возникает, когда независимые переменные, модифицируемые параллельными потоками, оказываются в одной 64-байтной кэш-линии процессора, вызывая взаимную инвалидацию кэшей L1/L2. В стандартной библиотеке Go эта проблема решается явным добавлением полей отступа (Padding). Например, в реализации `sync.Pool` структура пула `poolLocal` выравнивается ровно под размер кэш-линии (64 или 128 байт) через поле `pad [128 - unsafe.Sizeof(poolLocalInternal{})%128]byte`."
  },
  {
    "num": 32,
    "title": "Округление классов размеров и трассировка GODEBUG=allocfreetrace",
    "task": "**Классы размеров (Size Classes)**: Go не выделяет память байт в байт. Аллокатор разбивает кучу на `mspan` (страницы памяти) для объектов определенных классов (8 байт, 16 байт, 32 байта...). Выдели объект на 25 байт. Включи `GODEBUG=allocfreetrace=1`. Найди в логах, что для твоего объекта на самом деле был выделен слот из Size Class = 32 байта (потеря 7 байт памяти ради скорости аллокации).",
    "theory": "В традиционном представлении программистов память выделяется «байт в байт». Однако в Go куча дискретна:\n- При запросе 25 байт рантайм сопоставляет размер с таблицей `size_to_class8`:\n  Ближайший поддерживаемый класс размера, больший или равный 25 — это **Size Class 4 (32 байта)**.\n- Рантайм выделяет 32-байтный слот.\n- Реальный объект занимает первые 25 байт, а оставшиеся 7 байт остаются неиспользованным технологическим запасом.\n\nФлаг `GODEBUG=allocfreetrace=1` логирует каждую операцию аллокации и освобождения в реальном времени с выводом размера слота и стектрейса вызова.",
    "step_by_step": "1. Создайте структуру размером ровно 25 байт.\n2. Выделите экземпляр этой структуры в куче.\n3. Запустите программу с переменной окружения `GODEBUG=allocfreetrace=1`.\n4. Найдите в выводе stderr строку аллокации слота 32 байта.",
    "code_blocks": [
      {
        "filename": "size_round.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"unsafe\"\n)\n\n// Структура ровно 25 байт\ntype Struct25 struct {\n\tData [25]byte\n}\n\n//go:noinline\nfunc allocate25() *Struct25 {\n\treturn &Struct25{Data: [25]byte{1, 2, 3}}\n}\n\nfunc main() {\n\tfmt.Printf(\"Размер структуры Struct25: %d байт\\n\", unsafe.Sizeof(Struct25{}))\n\n\tobj := allocate25()\n\tfmt.Printf(\"Объект создан по адресу: %p\\n\", obj)\n\tfmt.Println(\"Для проверки запустите с GODEBUG=allocfreetrace=1 и найдите аллокацию 32B.\")\n}\n",
        "note": "Выделение объекта 25 байт, округляемого аллокатором до класса 32 байта"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "GODEBUG=allocfreetrace=1 go run size_round.go 2>&1 | grep -A 5 \"allocate25\"\n"
      }
    ],
    "under_the_hood": "В `src/runtime/malloc.go`:\n```go\nvar sizeclass uint8\nif size <= smallSizeMax-8 {\n    sizeclass = size_to_class8[divRoundUp(size, smallSizeDiv)]\n}\n```\nДля размера 25: `divRoundUp(25, 8) = 4`.\nВ массиве `size_to_class8[4]` хранится индекс класса 4.\nКласс 4 в таблице `class_to_size` соответствует размеру 32 байта.\nВычисление класса размера выполняется за **1 чтение из массива** без циклов и делений!",
    "pitfalls": "1. Флаг `allocfreetrace=1` собирает полный стектрейс на каждую операцию выделения памяти, замедляя выполнение в 100 раз. Не запускайте его на нагруженных программах.\n2. Проектируя сетевые протоколы и DTO, старайтесь укладывать размеры структур в степени двойки или точные значения size classes Go (8, 16, 24, 32, 48, 64, 80, 96, 112, 128...).",
    "bigtech_interview": "**Вопрос с собеседования:** Как рантайм Go за $O(1)$ находит класс размера по произвольному целому числу байт $N \\le 32768$?\n**Ответ:** Рантайм использует две быстрые таблицы прямого поиска:\n1. Для объектов до 1024 байт: массив `size_to_class8[divRoundUp(N, 8)]` (шаг 8 байт).\n2. Для объектов от 1024 до 32768 байт: массив `size_to_class128[divRoundUp(N-1024, 128)]` (шаг 128 байт).\nПоиск класса сводится к битовому сдвигу и одному чтению из массива в памяти, занимая менее 1 наносекунды."
  },
  {
    "num": 33,
    "title": "Глубокая рекурсия, копирование стека и Stack Shrinking",
    "task": "**Stack growth**: Создайте глубокую рекурсию. Изучите, как runtime копирует стек в новое место при переполнении (stack copying).",
    "theory": "Рост стека в Go реализован через операцию **Stack Copying (Копирование стека)**:\n1. При старте горутины выделяется непрерывный фрейм 2 КБ.\n2. В прологе функции компилятор проверяет:\n   `SP < g.stackguard0`\n3. Если места не хватает, вызывается `runtime.morestack`.\n4. `morestack` переключается на системный стек `g0` и вызывает `runtime.newstack()`.\n5. `newstack` выделяет новый блок памяти `newsize = oldsize * 2` и вызывает `runtime.copystack`.\n6. Функция `copystack` копирует стек-фреймы и корректирует все внутренние указатели.\n7. После завершения глубоких вызовов, когда горутина освобождает стек, сборщик мусора GC в фазе очистки выполняет **Stack Shrinking**: если используется менее 1/4 емкости, стек ужимается вдвое.",
    "step_by_step": "1. Напишите рекурсивную функцию глубокой вложенности (глубина 100+).\n2. Сохраняйте адреса локальных переменных на разных уровнях рекурсии.\n3. Продемонстрируйте факт скачков базового адреса стека при удвоениях (2KB -> 4KB -> 8KB -> 16KB).",
    "code_blocks": [
      {
        "filename": "deep_stack.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"unsafe\"\n)\n\n// Рекурсивная функция с локальным буфером для провокации нескольких удвоений стека\n//go:noinline\nfunc recurse(depth int, maxDepth int, lastAddr uintptr) {\n\tvar frameData [256]byte // 256 байт на каждый уровень вызова\n\tcurrentAddr := uintptr(unsafe.Pointer(&frameData[0]))\n\n\tif lastAddr != 0 {\n\t\tdiff := int64(currentAddr - lastAddr)\n\t\t// Если расстояние между адресами вышло за пределы стандартного шага 256B,\n\t\t// значит произошло копирование всего стека в новый увеличенный блок!\n\t\tif diff < -2048 || diff > 2048 {\n\t\t\tfmt.Printf(\"[Глубина %3d] Стек скопирован в новый блок! Адрес: 0x%x (дельта: %d Б)\\n\",\n\t\t\t\tdepth, currentAddr, diff)\n\t\t}\n\t}\n\n\tif depth < maxDepth {\n\t\trecurse(depth+1, maxDepth, currentAddr)\n\t}\n}\n\nfunc main() {\n\tfmt.Println(\"=== Исследование копирования стека при глубокой рекурсии ===\")\n\trecurse(1, 100, 0)\n\tfmt.Println(\"Рекурсия 100 уровней успешно завершена.\")\n}\n",
        "note": "Фиксация перемещения стека в новые блоки памяти при росте"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run deep_stack.go\n"
      }
    ],
    "under_the_hood": "В `src/runtime/stack.go`:\nФункция `copystack(gp *g, newsize uintptr)`:\n1. `old := gp.stack`\n2. `new := stackalloc(uint32(newsize))`\n3. Вычисляется дельта смещения: `adjinfo.delta = new.lo - old.lo`\n4. Выполняется `gentraceback` по всем кадрам вызовов, обновляя указатели аргументов и локальных переменных через карты типов (Bitmaps).\n5. Освобождается старый блок памяти `stackfree(old)`.\n6. Выполнение возобновляется в новом стеке с той же инструкции, на которой сработал чек.",
    "pitfalls": "1. Нельзя передавать адреса стековых переменных в код Cgo без `runtime.Pinner` или `runtime.LockOSThread`, так как при росте стека адрес переменной изменится, а C-библиотека продолжит писать по старому адресу (повреждение памяти).\n2. Максимальный лимит стека регулируется через `debug.SetMaxStack` (по умолчанию 1 ГБ).",
    "bigtech_interview": "**Вопрос с собеседования:** Что произойдет, если в Cgo-функцию передать указатель на стековую переменную Go, и в это время стек горутины решит вырасти?\n**Ответ:** Во время выполнения вызова Cgo горутина привязана к потоку ОС и переведена в режим системного вызова (`_Gsyscall`). В этом режиме стек не может расти, так как C-код выполняется на системном стеке потока `g0`. Однако передавать стековые указатели в Cgo, сохраняющие их для последующего асинхронного использования, категорически запрещено правилами Cgo Pointer Passing Rules, так как при возврате в Go стек может вырасти и переместиться, сделав C-указатель невалидным."
  }
]
