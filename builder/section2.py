# Section 2: Exercises 16 to 30

exercises = [
    {
        "num": 16,
        "title": "Подключение внешней зависимости github.com/fatih/color",
        "task": "Добавьте внешнюю зависимость, например github.com/fatih/color, выполните go get и используйте её для цветного вывода строки.",
        "theory": """
В других языках зависимости устанавливаются через сторонние пакетные менеджеры (`npm install`, `composer require`, `pip install`).
В Go пакетный менеджер встроен прямо в ядро языка и управляется командой `go get`.

Когда вы выполняете `go get github.com/fatih/color`:
1. Go связывается с прокси-сервером `proxy.golang.org`.
2. Скачивает исходники библиотеки нужной версии в глобальный локальный кэш `$GOPATH/pkg/mod`.
3. Добавляет запись о библиотеке и её точной семантической версии в файл `go.mod`.
4. Записывает криптографические контрольные суммы файлов библиотеки в `go.sum`.
""",
        "step_by_step": """
1. Скачиваем библиотеку: `go get github.com/fatih/color`.
2. В `main.go` импортируем `"github.com/fatih/color"`.
3. Используем функции `color.Cyan()`, `color.Green()`, `color.Red()` для стилизации консольного вывода.
4. Запускаем программу.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Скачивание пакета
go get github.com/fatih/color
# go: added github.com/fatih/color v1.17.0
# go: added github.com/mattn/go-colorable v0.1.13
# go: added github.com/mattn/go-isatty v0.0.20
# go: added golang.org/x/sys v0.25.0""",
                "note": "Скачивание библиотеки и её транзитивных зависимостей"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"github.com/fatih/color"
)

func main() {
	color.Green("✔ [УСПЕХ] База данных PostgreSQL успешно подключена!")
	color.Yellow("⚠ [ВНИМАНИЕ] Высокая нагрузка на пул соединений.")
	color.Red("✖ [ОШИБКА] Не удалось отправить webhook в платёжный шлюз.")
}""",
                "note": "Использование fatih/color"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .",
                "note": "Вывод цветных строк в терминал"
            }
        ],
        "under_the_hood": """
Библиотека `fatih/color` под капотом проверяет, подключен ли терминал (через вызов `isatty` к файловому дескриптору stdout). Если stdout перенаправлен в файл или пайп (`./app > output.log`), она автоматически отключает ANSI escape-последовательности, чтобы логи не засорялись служебными символами.
""",
        "pitfalls": """
- Начиная с Go 1.18, команда `go get` используется **только для добавления/обновления зависимостей в `go.mod`**. Установка исполняемых CLI-бинарников выполняется через `go install <path>@latest`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что такое транзитивные зависимости (transitive dependencies) и как Go управляет ими?»
**Ответ:** Транзитивная зависимость — это зависимость вашей зависимости (например, `fatih/color` зависит от `mattn/go-isatty`). Тулчейн Go автоматически скачивает их и записывает в `go.mod` с комментарием `// indirect`. Go использует алгоритм MVS (Minimal Version Selection) для детерминированного выбора минимально достаточной версии.
"""
    },
    {
        "num": 17,
        "title": "Пакет shapes: инкапсуляция и ошибки компиляции",
        "task": "Создай пакет shapes с экспортируемыми и неэкспортируемыми функциями. Попробуй вызвать неэкспортируемую функцию из main — убедись, что получаешь ошибку компиляции. Объясни, почему.",
        "theory": """
Инкапсуляция в Go реализована на уровне пакетов (package-level encapsulation).
- Если символ начинается со строчной буквы (например, `validateRadius`), он **приватен для пакета**.
- Любая попытка обратиться к `shapes.validateRadius` из пакета `main` приведет к ошибке компиляции на этапе синтаксического и типового анализа.

Это предотвращает утечку деталей внутренней реализации наружу, заставляя разработчиков проектировать четкий и стабильный публичный API пакета.
""",
        "step_by_step": """
1. Создаем директорию `shapes/` и файл `shapes/circle.go`.
2. Объявляем публичную функцию `Area(radius float64) (float64, error)`.
3. Объявляем приватную функцию `isValidRadius(r float64) bool`.
4. В `main.go` пробуем вызвать `shapes.isValidRadius(10)` и фиксируем ошибку компилятора.
""",
        "code_blocks": [
            {
                "filename": "shapes/circle.go",
                "lang": "go",
                "code": """package shapes

import (
	"errors"
	"math"
)

// Area — экспортируемая функция (начинается с 'A')
func Area(radius float64) (float64, error) {
	if !isValidRadius(radius) {
		return 0, errors.New("радиус должен быть положительным числом")
	}
	return math.Pi * radius * radius, nil
}

// isValidRadius — неэкспортируемая вспомогательная функция (начинается с 'i')
func isValidRadius(r float64) bool {
	return r > 0
}""",
                "note": "Пакет shapes с публичными и приватными функциями"
            },
            {
                "filename": "main.go (Демонстрация ошибки)",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/shapes"
)

func main() {
	// 1. Корректный вызов публичной функции
	area, _ := shapes.Area(5)
	fmt.Printf("Площадь: %.2f\\n", area)

	// 2. Попытка вызова приватной функции (вызовет ошибку компиляции!):
	// shapes.isValidRadius(5)
}""",
                "note": "Попытка обращения к неэкспортированному символу"
            },
            {
                "filename": "Вывод компилятора Go при раскомментировании",
                "lang": "text",
                "code": """./main.go:13:8: cannot refer to unexported name shapes.isValidRadius""",
                "note": "Ошибка компилятора Go"
            }
        ],
        "under_the_hood": """
Компилятор Go (`cmd/compile/internal/typecheck`) при проверке области видимости сверяет свойство `sym.IsExported()`. Если символ не экспортирован и импортирующий пакет не совпадает с исходным пакетом, компиляция немедленно прерывается с ошибкой.
""",
        "pitfalls": """
- В Go нет концепции «дружественных классов» (C++ friend) или защищенных членов (protected). Либо идентификатор экспортирован для всех, либо он виден только внутри своего пакета.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Можно ли обойти ограничение неэкспортируемых символов в Go?»
**Ответ:** Напрямую — нет. Однако для низкоуровневых системных нужд компилятор Go поддерживает специальную директиву линкера `//go:linkname` (используется внутри рантайма Go для связывания приватных символов между пакетами `runtime` и `net` / `os`, но в прикладном коде её использование строго не рекомендуется).
"""
    },
    {
        "num": 18,
        "title": "Создание подпакета calculator и чистый интерфейс",
        "task": "Создание подпакета: Внутри модуля создайте новую папку calculator. Внутри нее создайте файл calc.go с пакетом package calculator. Напишите там простую экспортируемую функцию Add(a, b int) int. Импортируйте этот пакет в свой main.go и вызовите функцию.",
        "theory": """
При создании пакетов придерживайтесь принципа простоты:
- Пакет должен выполнять логически завершенную роль.
- Имя функции не должно повторять имя пакета (не `calculator.CalculatorAdd`, а лаконично `calculator.Add`).
""",
        "step_by_step": """
1. Создаем папку `calculator/`.
2. Создаем `calculator/calc.go`.
3. Реализуем функцию `Add(a, b int) int`.
4. Импортируем в `main.go` и выводим результат.
""",
        "code_blocks": [
            {
                "filename": "calculator/calc.go",
                "lang": "go",
                "code": """package calculator

// Add складывает два целых числа
func Add(a, b int) int {
	return a + b
}""",
                "note": "Пакет calculator"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/calculator"
)

func main() {
	res := calculator.Add(15, 27)
	fmt.Printf("15 + 27 = %d\\n", res)
}""",
                "note": "Вызов calculator.Add"
            }
        ],
        "under_the_hood": """
При импорте пакета `hello_mod/calculator` идентификатором доступа в коде становится имя, объявленное в `package calculator`, а не имя папки (хотя по правилам они должны совпадать).
""",
        "pitfalls": """
- Тавтология в именах: объявление `func CalculatorAdd(...)` приводит к уродливому вызову `calculator.CalculatorAdd(...)`. В Go принято писать `calculator.Add(...)`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Каковы главные соглашения Go по именованию пакетов и функций (Effective Go)?»
**Ответ:** 1. Имя пакета — существительное в единственном числе в нижнем регистре. 2. Имя функции не должно повторять имя пакета (stuttering). 3. Геттеры не содержат префикса `Get` (`user.Name()`, а не `user.GetName()`).
"""
    },
    {
        "num": 19,
        "title": "Диагностика версии Go и гарантии совместимости (Go 1 Compatibility Promise)",
        "task": "Проверьте версию Go, установленную в системе, используя команду go version.",
        "theory": """
Команда `go version` выводит текущую версию компилятора, операционную систему и архитектуру процессора.

**Go 1 Compatibility Promise:**
Одно из величайших преимуществ Go перед другими языками — официальная гарантия обратной совместимости Go 1.
Код, написанный на Go 1.0 в 2012 году, **гарантированно скомпилируется и будет работать** на самой современной версии Go (Go 1.22+). Разработчики языка никогда не ломают обратную совместимость спецификации Go 1.
""",
        "step_by_step": """
1. Выполняем `go version` в терминале.
2. Анализируем вывод: версия тулчейна, ОС (`linux`, `darwin`, `windows`), архитектура (`amd64`, `arm64`).
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go version
# Вывод:
# go version go1.22.5 linux/amd64""",
                "note": "Проверка версии Go"
            }
        ],
        "under_the_hood": """
Утилита `go` умеет определять версию любого скомпилированного Go-бинарника!
Команда `go version ./bin/server` прочитает встроенные метаданные сборки ELF/Mach-O и покажет, какой версией Go и с какими флагами был собран сторонний бинарник.
""",
        "pitfalls": """
- Не путайте версию установленного компилятора Go в системе с директивой `go 1.XX` в файле `go.mod`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как в Go инспектировать метаданные зависимостей уже скомпилированного бинарника в проде?»
**Ответ:** Командой `go version -m /path/to/binary`. Go вшивает в бинарник полный список модулей, их версии и хэши коммитов, что позволяет моментально проводить аудит уязвимостей на сервере.
"""
    },
    {
        "num": 20,
        "title": "Изолированный подкаталог mypkg и экспортные границы",
        "task": "Создайте в проекте подкаталог mypkg с файлом helper.go (пакет mypkg). Экспортируйте из него функцию и вызовите её из main.",
        "theory": """
Разделение кодовой базы на независимые функциональные пакеты — основа поддерживаемости микросервисов.
Каждый пакет в Go представляет собой отдельное пространство имен (namespace).
""",
        "step_by_step": """
1. Создаем директорию `mypkg/`.
2. Создаем файл `mypkg/helper.go` с функцией `FormatLog(level, message string) string`.
3. Импортируем `"hello_mod/mypkg"` в `main.go`.
4. Вызываем `mypkg.FormatLog("INFO", "Сервер стартовал")`.
""",
        "code_blocks": [
            {
                "filename": "mypkg/helper.go",
                "lang": "go",
                "code": """package mypkg

import "fmt"

// FormatLog форматирует строковое сообщение лога с меткой уровня
func FormatLog(level, message string) string {
	return fmt.Sprintf("[%s] %s", level, message)
}""",
                "note": "Пакет mypkg"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/mypkg"
)

func main() {
	logMsg := mypkg.FormatLog("DEBUG", "Чтение конфигурационного файла завершено.")
	fmt.Println(logMsg)
}""",
                "note": "Импорт и вызов mypkg"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .\n# Вывод:\n# [DEBUG] Чтение конфигурационного файла завершено.",
                "note": "Вывод"
            }
        ],
        "under_the_hood": """
При импорте компилятор проверяет наличие циклов в зависимостях. Если `mypkg` попытается импортировать `main`, компилятор выдаст фатальную ошибку `import cycle not allowed`.
""",
        "pitfalls": """
- В Go циклические зависимости строго запрещены на уровне компилятора. Если пакет A импортирует B, то B ни прямо, ни косвенно не может импортировать A.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как разрешить циклическую зависимость между пакетами в Go?»
**Ответ:** 1. Вынести общие интерфейсы/модели в отдельный третий пакет (например, `domain` или `models`). 2. Использовать инверсию зависимостей (Dependency Inversion) через интерфейсы.
"""
    },
    {
        "num": 21,
        "title": "Пакет data, глобальные переменные и опасность гонок данных (Data Races)",
        "task": "Создай пакет data с глобальной экспортируемой переменной GlobalCounter (тип int). Увеличивай её из main и из другого пакета. Проследи, как изменяется значение. Объясни поведение.",
        "theory": """
В Go можно объявлять переменные на уровне пакета:
`var GlobalCounter int = 0`

Если имя начинается с большой буквы, эта переменная становится **глобально изменяемым состоянием** (shared mutable state), доступным из любого пакета проекта.

Почему глобальные переменные считаются антипаттерном в бэкенде:
1. **Состояние гонки (Data Race):** если две горутины одновременно выполнят `GlobalCounter++`, произойдет состояние гонки, приводящее к потере данных или повреждению памяти.
2. **Сложность тестирования:** тесты не изолированы друг от друга и влияют на глобальное состояние.
3. **Неявные зависимости:** сложно отследить, какая часть системы изменила значение.
""",
        "step_by_step": """
1. Создаем пакет `data/` с переменной `var GlobalCounter int`.
2. Создаем пакет `worker/`, функция которого увеличивает `data.GlobalCounter`.
3. В `main.go` увеличиваем `data.GlobalCounter`, вызываем `worker`, и смотрим итоговое значение.
""",
        "code_blocks": [
            {
                "filename": "data/state.go",
                "lang": "go",
                "code": """package data

// GlobalCounter — глобальная экспортируемая переменная на уровне пакета
var GlobalCounter int = 0""",
                "note": "Пакет с глобальной переменной"
            },
            {
                "filename": "worker/task.go",
                "lang": "go",
                "code": """package worker

import "hello_mod/data"

func DoWork() {
	data.GlobalCounter += 10
}""",
                "note": "Пакет worker модифицирует data.GlobalCounter"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/data"
	"hello_mod/worker"
)

func main() {
	fmt.Printf("Начальное значение: %d\\n", data.GlobalCounter)

	data.GlobalCounter++
	fmt.Printf("После инкремента в main: %d\\n", data.GlobalCounter)

	worker.DoWork()
	fmt.Printf("После вызова worker.DoWork(): %d\\n", data.GlobalCounter)
}""",
                "note": "Модификация глобального счетчика"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# Начальное значение: 0
# После инкремента в main: 1
# После вызова worker.DoWork(): 11""",
                "note": "Результат работы"
            }
        ],
        "under_the_hood": """
Глобальная переменная `GlobalCounter` размещается в сегменте данных (`.data` или `.bss`) исполняемого файла. Все пакеты ссылаются на один и тот же виртуальный адрес в памяти.
Операция `data.GlobalCounter++` не является атомарной — на уровне ассемблера это три инструкции: `MOVQ (чтение)`, `INCQ (увеличение)`, `MOVQ (запись)`.
""",
        "pitfalls": """
- В многопоточных программах параллельный доступ к `GlobalCounter` приведет к Data Race. Для потокобезопасного счетчика в Go используют пакет `sync/atomic` (`atomic.AddInt64`) или `sync.Mutex`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как обнаружить гонки данных в Go-сервисе?»
**Ответ:** Компилятор Go включает встроенный детектор гонок ThreadSanitizer. Запуск тестов или бинарника с флагом `-race` (`go test -race ./...` или `go run -race .`) моментально отслеживает несинхронизированный параллельный доступ к памяти и выдает точный трейс с номерами строк.
"""
    },
    {
        "num": 22,
        "title": "Полный цикл: fatih/color, цветной вывод, go mod tidy, анализ go.mod и go.sum",
        "task": "Создай новый модуль. Импортируй github.com/fatih/color. Напиши программу, которая выводит цветной текст в консоль (красный, зелёный, жёлтый). Запусти go mod tidy и изучи изменения в go.mod и go.sum.",
        "theory": """
Команда `go mod tidy` — важнейший инструмент инженера:
1. Она сканирует весь исходный код проекта.
2. Добавляет в `go.mod` все недостающие модули, которые вы импортировали в коде.
3. Удаляет из `go.mod` и `go.sum` все неиспользуемые зависимости.
4. Синхронизирует контрольные суммы в `go.sum`.
""",
        "step_by_step": """
1. Инициализируем модуль: `go mod init color_app`.
2. Пишем программу с цветным выводом без предварительного `go get`.
3. Запускаем `go mod tidy` — Go сам найдет и скачает всё необходимое!
4. Изучаем получившиеся файлы `go.mod` и `go.sum`.
""",
        "code_blocks": [
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"github.com/fatih/color"
)

func main() {
	color.Red("==> [КРИТИЧЕСКАЯ ОШИБКА] База данных недоступна")
	color.Yellow("==> [ПРЕДУПРЕЖДЕНИЕ] Высокое потребление памяти")
	color.Green("==> [ИНФО] Запрос успешно обработан за 4.2ms")
}""",
                "note": "Цветной вывод логов"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go mod tidy
# go: finding module for package github.com/fatih/color
# go: downloading github.com/fatih/color v1.17.0
# go: found github.com/fatih/color in github.com/fatih/color v1.17.0

cat go.mod""",
                "note": "Запуск go mod tidy"
            },
            {
                "filename": "go.mod",
                "lang": "text",
                "code": """module color_app

go 1.22.5

require github.com/fatih/color v1.17.0

require (
	github.com/mattn/go-colorable v0.1.13 // indirect
	github.com/mattn/go-isatty v0.0.20 // indirect
	golang.org/x/sys v0.25.0 // indirect
)""",
                "note": "Файл go.mod с прямыми и косвенными зависимостями"
            }
        ],
        "under_the_hood": """
В `go.mod` блок `require` разделен:
- Прямые зависимости (те, что импортированы непосредственно в вашем коде);
- Блок с комментарием `// indirect` — зависимости ваших зависимостей, которые нужны для сборки, но не используются вами напрямую.
""",
        "pitfalls": """
- Коммит в репозиторий кода без запуска `go mod tidy` часто приводит к сбою CI-сборки (`go.mod is not clean`).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как в CI пайплайне проверить, что разработчик не забыл запустить `go mod tidy`?»
**Ответ:** Командой `go mod tidy && git diff --exit-code go.mod go.sum`. Если файлы изменились, команда завершится с ненулевым кодом ошибки и заблокирует слияние Pull Request.
"""
    },
    {
        "num": 23,
        "title": "Разделение логики пакета: go run . против go run main.go helper.go",
        "task": "Разделение одного пакета: Создайте файл helper.go в той же директории, где находится main.go (оба файла должны объявлять package main). Перенесите часть логики вывода в helper.go. Запустите проект с помощью go run main.go helper.go или go run .",
        "theory": """
При разработке микросервисов в одном каталоге часто находится несколько файлов.
Сравним способы запуска:
1. `go run .` — компилирует текущую директорию как единый пакет. **Это стандарт де-факто.**
2. `go run main.go helper.go` — явно передает список файлов. Неудобно, если файлов 10+.
3. `go run *.go` — подставляет файлы через shell globing, но может случайно захватить тестовые файлы или файлы сборки для другой ОС.
""",
        "step_by_step": """
1. Создаем `helper.go` с функцией `renderBanner(appName string)`.
2. В `main.go` вызываем `renderBanner`.
3. Запускаем проект через `go run .`.
""",
        "code_blocks": [
            {
                "filename": "helper.go",
                "lang": "go",
                "code": """package main

import "fmt"

func renderBanner(appName string) {
	fmt.Println("========================================")
	fmt.Printf("   СЕРВИС: %s\\n", appName)
	fmt.Println("========================================")
}""",
                "note": "Файл helper.go"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

func main() {
	renderBanner("AUTH_SERVICE_V2")
}""",
                "note": "Файл main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# ========================================
#    СЕРВИС: AUTH_SERVICE_V2
# ========================================

go run main.go helper.go
# Дает аналогичный результат""",
                "note": "Запуск программы"
            }
        ],
        "under_the_hood": """
Синтаксис точки `.` в Go обозначает путь к локальному пакету. Тулчейн Go парсит манифест пакета в текущем каталоге, отфильтровывает файлы сборки по билд-тегам и собирает проект.
""",
        "pitfalls": """
- Не используйте `go run main.go` для многофайловых пакетов — это приведет к ошибке `undefined`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что означает синтаксис `./...` в командах `go test ./...` или `go vet ./...`?»
**Ответ:** `./...` — это специальный паттерн тулчейна Go, обозначающий «текущий пакет и **рекурсивно все вложенные подпакеты** проекта».
"""
    },
    {
        "num": 24,
        "title": "Стандартная библиотека: math/rand и криптографическая безопасность",
        "task": "Стандартная библиотека: Импортируй стандартный пакет math/rand. Сгенерируй и выведи случайное число.",
        "theory": """
В Go стандартная библиотека предоставляет два пакета для генерации случайных чисел:
1. `math/rand` (и новый `math/rand/v2` в Go 1.22+) — **псевдослучайные числа (PRNG)**. Быстрый генератор для симуляций, тестов и игр. **НЕ БЕЗОПАСЕН ДЛЯ КРИПТОГРАФИИ, ТОКЕНОВ И ПАРОЛЕЙ!**
2. `crypto/rand` — **криптографически стойкий генератор (CSPRNG)**. Читает энтропию операционной системы (`/dev/urandom` в Linux, `getrandom(2)`). Используется для ключей шифрования, JWT токенов и UUID.
""",
        "step_by_step": """
1. Импортируем `"math/rand"`.
2. Генерируем случайное число в диапазоне `[0, 100)` через `rand.Intn(100)`.
3. Выводим результат.
""",
        "code_blocks": [
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"math/rand"
	"time"
)

func main() {
	// До Go 1.20 требовалось вручную инициализировать сид:
	// rand.Seed(time.Now().UnixNano()) 
	// Начиная с Go 1.20+, глобальный генератор сидируется автоматически!

	randomNum := rand.Intn(100) // Диапазон [0, 100)
	randomFloat := rand.Float64() // Диапазон [0.0, 1.0)

	fmt.Printf("Случайное число: %d\\n", randomNum)
	fmt.Printf("Случайный float: %.4f\\n", randomFloat)
	fmt.Printf("Случайная задержка: %v\\n", time.Duration(rand.Intn(500))*time.Millisecond)
}""",
                "note": "Использование math/rand"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .",
                "note": "Запуск"
            }
        ],
        "under_the_hood": """
Глобальный генератор `math/rand` защищен внутренним мьютексом `sync.Mutex`, поэтому вызовы `rand.Intn()` потокобезопасны. Однако при тысячах параллельных горутин мьютекс становится узким местом (lock contention). В Go 1.22 появился пакет `math/rand/v2` с генератором ChaCha8/PCG, который работает в разы быстрее и эффективнее масштабируется.
""",
        "pitfalls": """
- Использование `math/rand` для генерации кодов восстановления доступа, SMS-кодов или сессионных кук — грубая уязвимость безопасности. Для этого всегда используйте `crypto/rand`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему в Go 1.22 появился пакет `math/rand/v2` вместо обновления старого `math/rand`?»
**Ответ:** Следуя гарантии обратной совместимости Go 1, создатели не могли изменить сигнатуры и поведение старого пакета `math/rand` (например, удалить устаревший генератор Source). Новый пакет `math/rand/v2` стал первым пакетом стандартной библиотеки с суффиксом `/v2`.
"""
    },
    {
        "num": 25,
        "title": "Инспекция переменных окружения через go env",
        "task": "Выведите информацию о переменных окружения Go с помощью go env и найдите пути к GOPATH и GOROOT.",
        "theory": """
Команда `go env` выводит текущую конфигурацию тулчейна Go.
Ключевые переменные:
- `GOROOT` — директория компилятора и стандартной библиотеки;
- `GOPATH` — рабочая область и кэш модулей;
- `GOCACHE` — кэш сборки объектных файлов (ускоряет повторные билды);
- `GOMODCACHE` — кэш скачанных zip-архивов модулей (`$GOPATH/pkg/mod`);
- `GOPROXY` — адрес прокси-сервера модулей (по умолчанию `https://proxy.golang.org,direct`);
- `GOPRIVATE` — маска приватных репозиториев компании, для которых отключается публичный прокси.
""",
        "step_by_step": """
1. Выполняем `go env` для просмотра всех параметров.
2. Запрашиваем конкретные переменные: `go env GOROOT GOPATH GOMODCACHE`.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Вывод конкретных переменных
go env GOROOT
# /usr/lib/golang

go env GOPATH
# /home/ut/go

go env GOMODCACHE
# /home/ut/go/pkg/mod

# Изменение переменной тулчейна (сохраняется в файл конфигурации Go):
# go env -w GOPRIVATE=gitlab.mycompany.com/*""",
                "note": "Инспекция окружения через go env"
            }
        ],
        "under_the_hood": """
Начиная с Go 1.13, переменные, заданные через `go env -w`, записываются в специальный файл конфигурации (`os.UserConfigDir()/go/env`). Они имеют приоритет над системными дефолтами, но уступают явно переданным переменным окружения шелла (`export FOO=BAR`).
""",
        "pitfalls": """
- Если случайно задать некорректный `GOROOT` через `go env -w GOROOT=...`, компилятор Go может перестать запускаться. Сбросить параметр можно командой `go env -u GOROOT`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как работает `GOCACHE` и почему инкрементальная сборка в Go происходит почти мгновенно?»
**Ответ:** `GOCACHE` хранит хэши содержимого исходных файлов, флагов компилятора и скомпилированных объектных файлов. Если файл не менялся с прошлой сборки, компилятор берет готовый бинарный артефакт из `GOCACHE` без повторного парсинга и компиляции.
"""
    },
    {
        "num": 26,
        "title": "Пакет config, функция init() и лексический порядок инициализации",
        "task": "Создай пакет config с функцией init(). Внутри init() установи глобальную переменную. Добавь init() в main. Проследи порядок выполнения init()-функций (используй fmt.Println с метками). Создай несколько файлов в одном пакете с init() — в каком порядке они выполняются?",
        "theory": """
В Go существует специальная предопределенная функция **`init()`**:
- Она не принимает аргументов и ничего не возвращает (`func init()`).
- Она вызывается рантаймом Go **автоматически до старта функции `main()`**.
- Нельзя вызвать `init()` вручную из кода!

**Жизненный цикл инициализации программы Go:**
1. Инициализируются переменные импортированных пакетов.
2. Выполняются функции `init()` импортированных пакетов (снизу вверх по дереву зависимостей).
3. Инициализируются переменные пакета `main`.
4. Выполняются функции `init()` пакета `main`.
5. Запускается функция `main.main()`.

**Порядок выполнения нескольких `init()` внутри одного пакета:**
Файлы внутри одного пакета компилируются и инициализируются в **лексикографическом (алфавитном) порядке имен файлов**! Внутри одного файла функции `init()` выполняются сверху вниз.
""",
        "step_by_step": """
1. Создаем пакет `config/` с файлами `a_init.go` и `b_init.go`.
2. В каждом файле объявляем `func init()` с отладочным выводом.
3. В `main.go` объявляем `init()` и `main()`.
4. Запускаем и фиксируем точную хронологию вызовов.
""",
        "code_blocks": [
            {
                "filename": "config/a_init.go",
                "lang": "go",
                "code": """package config

import "fmt"

var AppEnvironment string

func init() {
	fmt.Println("[INIT 1] config/a_init.go: инициализация базовых переменных")
	AppEnvironment = "PRODUCTION"
}""",
                "note": "Первый файл пакета config"
            },
            {
                "filename": "config/b_init.go",
                "lang": "go",
                "code": """package config

import "fmt"

var DBTimeoutSeconds int

func init() {
	fmt.Println("[INIT 2] config/b_init.go: настройка таймаутов БД")
	DBTimeoutSeconds = 30
}""",
                "note": "Второй файл пакета config (по алфавиту после a_init.go)"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/config"
)

func init() {
	fmt.Println("[INIT 3] main.go: инициализация пакета main")
}

func main() {
	fmt.Println("[MAIN] Функция main() начала выполнение!")
	fmt.Printf("[MAIN] Среда: %s, Таймаут: %ds\\n", config.AppEnvironment, config.DBTimeoutSeconds)
}""",
                "note": "Файл main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# [INIT 1] config/a_init.go: инициализация базовых переменных
# [INIT 2] config/b_init.go: настройка таймаутов БД
# [INIT 3] main.go: инициализация пакета main
# [MAIN] Функция main() начала выполнение!
# [MAIN] Среда: PRODUCTION, Таймаут: 30s""",
                "note": "Строгий детерминированный порядок выполнения"
            }
        ],
        "under_the_hood": """
При сборке компилятор генерирует для каждого пакета внутреннюю функцию `main..inittask`, которая связывает инициализацию глобальных переменных и вызовы всех функций `init()`. Рантайм выполняет их строго в одном основном потоке ОС до запуска планировщика для пользовательского кода.
""",
        "pitfalls": """
- Завязываться на алфавитный порядок файлов в продакшене — **опасная практика**, так как случайное переименование файла (`a_init.go` -> `z_init.go`) сломает порядок инициализации. Вся связанная логика должна быть явной!
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Можно ли запускать горутины (`go func()`) внутри функции `init()`?»
**Ответ:** Да, синтаксически это разрешено. Однако горутина не начнет реально исполняться, пока текущий поток инициализации не передаст управление или не завершится инициализация пакета. Более того, паника в `init()` немедленно крашит все приложение до старта `main()`.
"""
    },
    {
        "num": 27,
        "title": "Культура качества кода: утилиты go fmt и go vet",
        "task": "Выполните go fmt и go vet для своего кода, исправьте все замечания.",
        "theory": """
В Go нет споров о пробелах, табах или расположении скобок.
Создатели языка разработали официальный инструмент **`gofmt`**, который форматирует весь Go-код в мире по единому каноническому стандарту.

**Инструменты статического анализа в Go:**
1. **`go fmt ./...`** — автоматически форматирует все файлы проекта (расставляет табы, выравнивает структуры, упорядочивает импорты).
2. **`go vet ./...`** — официальный статический анализатор компилятора Go. Он находит потенциальные баги:
   - Несоответствие аргументов форматирования (`fmt.Printf("%d", "не число")`);
   - Копирование мьютексов (`sync.Mutex` pass by value);
   - Недостижимый код (unreachable code);
   - Забытые вызовы `cancel()` для контекстов.
""",
        "step_by_step": """
1. Создаем файл с заведомо ошибочным кодом форматирования и аргументов `Printf`.
2. Запускаем `go fmt ./...` — смотрим, как код выравнивается.
3. Запускаем `go vet ./...` — исправляем найденные предупреждения.
""",
        "code_blocks": [
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """# Форматирование всего проекта рекурсивно
go fmt ./...

# Проверка кода статическим анализатором
go vet ./...
# Если в коде есть Printf("%d", "строка"), go vet выдаст:
# ./main.go:10:2: fmt.Printf format %d has arg "строка" of wrong type string""",
                "note": "Запуск go fmt и go vet"
            }
        ],
        "under_the_hood": """
`go vet` строит SSA-представление кода и использует набор встроенных анализаторов (из пакета `golang.org/x/tools/go/analysis`). Анализаторы работают без выполнения кода, анализируя семантическую модель типов.
""",
        "pitfalls": """
- Игнорирование предупреждений `go vet` в CI/CD недопустимо. В современных проектах `go vet` и линтер `golangci-lint` являются обязательными проверками перед слиянием PR.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «В чем отличие `go fmt` от `goimports`?»
**Ответ:** `go fmt` форматирует существующий код. `goimports` делает все то же самое, плюс **автоматически добавляет недостающие импорты и удаляет неиспользуемые импорты**, группируя стандартную библиотеку отдельно от внешних зависимостей.
"""
    },
    {
        "num": 28,
        "title": "Экспорт и видимость в calculator (Sub vs multiply)",
        "task": "Экспорт и видимость: В пакете calculator создайте две функции: одну с заглавной буквы (Sub), другую со строчной (multiply). Попробуйте вызвать обе из пакета main. Зафиксируйте ошибку компиляции для неэкспортируемой функции.",
        "theory": """
Наглядная демонстрация строгости компилятора:
- `calculator.Sub(10, 5)` — компилируется успешно.
- `calculator.multiply(10, 5)` — ошибка компиляции: `cannot refer to unexported name calculator.multiply`.
""",
        "step_by_step": """
1. В `calculator/calc.go` добавляем `Sub` (публичная) и `multiply` (приватная).
2. В `main.go` вызываем `Sub`, убеждаемся в работоспособности.
3. Раскомментируем вызов `multiply` и смотрим ошибку.
""",
        "code_blocks": [
            {
                "filename": "calculator/calc.go",
                "lang": "go",
                "code": """package calculator

// Sub экспортируется
func Sub(a, b int) int {
	return a - b
}

// multiply — внутренняя реализация, скрыта от внешних пакетов
func multiply(a, b int) int {
	return a * b
}""",
                "note": "Экспортируемая и неэкспортируемая функции"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	"hello_mod/calculator"
)

func main() {
	res := calculator.Sub(100, 42)
	fmt.Printf("Sub: %d\\n", res)

	// Попытка вызвать неэкспортированную функцию:
	// calculator.multiply(10, 5) // ОШИБКА КОМПИЛЯЦИИ
}""",
                "note": "Файл main.go"
            }
        ],
        "under_the_hood": """
В Go проверка видимости имен происходит на этапе парсинга и связывания имен AST-дерева до генерации машинного кода.
""",
        "pitfalls": """
- Опечатка в первой букве названия экспортируемой функции — самая частая ошибка новичков, переходящих с языков с ключевым словом `public`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Как протестировать неэкспортируемую функцию `multiply` из файла тестов?»
**Ответ:** Если тестовый файл объявляет тот же пакет `package calculator` (в файле `calc_test.go`), он имеет полный доступ ко всем приватным функциям пакета. Если объявляет `package calculator_test` (внешний black-box тест), то видит только экспортируемый API.
"""
    },
    {
        "num": 29,
        "title": "Многоуровневая цепочка инициализации (logger -> app -> main)",
        "task": "Создай пакет logger с несколькими файлами, каждый со своей init(). Проверь порядок инициализации. Затем создай зависимость: пакет app импортирует logger, main импортирует app. Проследи полный порядок init().",
        "theory": """
Дерево зависимостей определяет строгий порядок инициализации в рантайме Go:
- `main` импортирует `app`
- `app` импортирует `logger`

Порядок инициализации:
1. `logger` (все его файлы по алфавиту) ->
2. `app` (все его файлы по алфавиту) ->
3. `main` (все его файлы по алфавиту) ->
4. `main.main()`

Ни один пакет не может начать исполнять свой код, пока полностью не завершена инициализация всех пакетов, от которых он зависит!
""",
        "step_by_step": """
1. Создаем пакет `logger/` с `logger_init.go`.
2. Создаем пакет `app/` с `app_init.go` (импортирует `logger`).
3. В `main.go` импортируем `app`.
4. Запускаем и наблюдаем восходящую цепочку инициализации (Bottom-Up).
""",
        "code_blocks": [
            {
                "filename": "logger/logger_init.go",
                "lang": "go",
                "code": """package logger

import "fmt"

func init() {
	fmt.Println("[ШАГ 1] Инициализация пакета logger (самый глубокий уровень)")
}""",
                "note": "Пакет logger"
            },
            {
                "filename": "app/app_init.go",
                "lang": "go",
                "code": """package app

import (
	"fmt"
	_ "hello_mod/logger" // Импортируем logger ради побочного эффекта инициализации
)

func init() {
	fmt.Println("[ШАГ 2] Инициализация пакета app (зависит от logger)")
}""",
                "note": "Пакет app"
            },
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	"fmt"
	_ "hello_mod/app"
)

func init() {
	fmt.Println("[ШАГ 3] Инициализация пакета main")
}

func main() {
	fmt.Println("[ШАГ 4] Старт функции main.main()")
}""",
                "note": "Точка входа main.go"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": """go run .
# Вывод:
# [ШАГ 1] Инициализация пакета logger (самый глубокий уровень)
# [ШАГ 2] Инициализация пакета app (зависит от logger)
# [ШАГ 3] Инициализация пакета main
# [ШАГ 4] Старт функции main.main()""",
                "note": "Точный порядок исполнения по графу DAG"
            }
        ],
        "under_the_hood": """
Go строит топологическую сортировку графа зависимостей (Topological Sort of DAG). Если в графе есть хотя бы один цикл (`A -> B -> C -> A`), алгоритм топологической сортировки не может быть завершен, и компилятор сообщает об ошибке циклического импорта.
""",
        "pitfalls": """
- Выполнение тяжелых сетевых запросов или долгих операций ввода-вывода внутри `init()` — антипаттерн, так как это блокирует старт сервиса и лишает возможности управлять таймаутами через `context.Context`.
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Почему в Go не рекомендуется инициализировать подключение к базе данных внутри `func init()`?»
**Ответ:** 1. Невозможно передать конфигурацию или `context.Context` с таймаутом. 2. Ошибку подключения нельзя вернуть вызывающему коду (приходится делать `panic`). 3. Невозможно мокировать подключение в юнит-тестах. Инициализация ресурсов должна выполняться явно через конструкторы (`NewService(...)`).
"""
    },
    {
        "num": 30,
        "title": "Алиасы импорта: локальное переименование пакетов (import f \"fmt\")",
        "task": "Алиасы импорта: Импортируй пакет fmt с псевдонимом f (например, import f \"fmt\"). Выведи текст, используя f.Println.",
        "theory": """
При импорте пакета перед строкой пути можно указать **псевдоним (алиас)**:
```go
import (
    f "fmt"
    myrand "crypto/rand"
    fastrand "math/rand"
)
```

Зачем нужны алиасы:
1. **Разрешение коллизий имен:** если вам нужны одновременно `crypto/rand` и `math/rand`, без алиаса оба пакета назывались бы `rand`.
2. **Сокращение длинных имен:** например, `k8serrors "k8s.io/apimachinery/pkg/api/errors"`.
3. **Миграция библиотек:** можно подменить реализацию пакета без изменения сотен строк кода.
""",
        "step_by_step": """
1. В `main.go` импортируем `fmt` с алиасом `f`.
2. Вызываем `f.Println("...")`.
3. Запускаем программу.
""",
        "code_blocks": [
            {
                "filename": "main.go",
                "lang": "go",
                "code": """package main

import (
	f "fmt" // Задаем псевдоним f для пакета fmt
)

func main() {
	f.Println("Использование пакета fmt через краткий алиас f!")
}""",
                "note": "Использование алиаса импорта"
            },
            {
                "filename": "Терминал",
                "lang": "bash",
                "code": "go run .\n# Вывод:\n# Использование пакета fmt через краткий алиас f!",
                "note": "Вывод"
            }
        ],
        "under_the_hood": """
Алиас импорта существует только на этапе компиляции в области видимости текущего файла `.go`. В других файлах того же пакета пакет `fmt` по-прежнему будет доступен под своим стандартным именем, если там не объявлен собственный алиас.
""",
        "pitfalls": """
- Чрезмерное использование однобуквенных алиасов вроде `f` для стандартных пакетов снижает читаемость кода для коллег. Используйте алиасы только при реальной необходимости (коллизии имен).
""",
        "bigtech_interview": """
**Вопрос с собеседования:** «Что означает импорт с точкой: `import . \"fmt\"`?»
**Ответ:** Dot-import (`.`) импортирует все экспортируемые идентификаторы пакета напрямую в текущее пространство имен файла, позволяя вызывать `Println()` вместо `fmt.Println()`. В продакшене dot-import считается антипаттерном из-за замусоривания пространства имен и риска коллизий (исключение — DSL-тесты вроде Gomega).
"""
    }
]

if __name__ == '__main__':
    print(f"Loaded Section 2: {len(exercises)} exercises.")
