# -*- coding: utf-8 -*-
exercises = [
  {
    "num": 68,
    "title": "Сравнение характеристик подписи ECDSA P-256 и Ed25519",
    "task": "**ECDSA на кривой P-256.**: Реализуйте подпись и проверку с `ecdsa`. Сравните размер подписи и скорость с Ed25519 (бенчмарк).",
    "theory": "При выборе алгоритма цифровой подписи для высоконагруженных API и сетевых протоколов сравниваются две ведущие криптографические школы:\n\n1. **ECDSA P-256 (NIST secp256r1)**:\n   - Государственный и промышленный стандарт США (FIPS 186-4, WebAuthn, Apple TouchID, PIV-карты).\n   - Подпись упаковывается в ASN.1 DER `SEQUENCE { r INTEGER, s INTEGER }`. Из-за переменной длины целых чисел размер подписи колеблется от 70 до 72 байт.\n   - Требует обращения по модулю $k^{-1} \\pmod n$ при каждой подписи.\n2. **Ed25519 (RFC 8032)**:\n   - Академический стандарт Дэниела Бернштейна.\n   - Фиксированный размер открытого ключа (32 байта) и подписи (ровно 64 байта) без ASN.1 оверхеда.\n   - Выполняется на 30–60% быстрее ECDSA и защищен от уязвимостей генератора случайных чисел.",
    "step_by_step": "1. Сгенерируйте ключевую пару ECDSA P-256 и Ed25519.\n2. Подпишите тестовое сообщение обоими алгоритмами.\n3. Измерьте точный размер полученных подписей в байтах.\n4. Проведите сравнительный замер скорости выполнения 5 000 циклов верификации.",
    "code_blocks": [
      {
        "filename": "ecdsa_vs_ed25519.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ecdsa\"\n\t\"crypto/ed25519\"\n\t\"crypto/elliptic\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc main() {\n\t// 1. Инициализация ключей\n\tecdsaPriv, _ := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)\n\tedPub, edPriv, _ := ed25519.GenerateKey(rand.Reader)\n\n\tdoc := []byte(\"Microservice Transaction: Approve payment of 50,000 RUB\")\n\thash := sha256.Sum256(doc)\n\n\t// 2. Создание подписей\n\tecdsaSig, _ := ecdsa.SignASN1(rand.Reader, ecdsaPriv, hash[:])\n\tedSig := ed25519.Sign(edPriv, doc)\n\n\tfmt.Println(\"=== СРАВНЕНИЕ ECDSA P-256 И ED25519 ===\")\n\tfmt.Printf(\"ECDSA P-256: размер подписи (ASN.1 DER): %d байт\\n\", len(ecdsaSig))\n\tfmt.Printf(\"Ed25519:     размер подписи (Raw 512-bit): %d байта (ФИКСИРОВАНО)\\n\\n\", len(edSig))\n\n\t// 3. Бенчмарк верификации (5000 итераций)\n\tconst count = 5000\n\n\tstart := time.Now()\n\tfor i := 0; i < count; i++ {\n\t\t_ = ecdsa.VerifyASN1(&ecdsaPriv.PublicKey, hash[:], ecdsaSig)\n\t}\n\tecdsaDuration := time.Since(start)\n\n\tstart = time.Now()\n\tfor i := 0; i < count; i++ {\n\t\t_ = ed25519.Verify(edPub, doc, edSig)\n\t}\n\tedDuration := time.Since(start)\n\n\tfmt.Printf(\"Скорость верификации (%d проверок):\\n\", count)\n\tfmt.Printf(\"  ECDSA P-256: %v (%.0f ops/sec)\\n\", ecdsaDuration, float64(count)/ecdsaDuration.Seconds())\n\tfmt.Printf(\"  Ed25519:     %v (%.0f ops/sec)\\n\", edDuration, float64(count)/edDuration.Seconds())\n}\n",
        "note": "Сравнение размеров бинарного представления и скорости верификации подписей"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run ecdsa_vs_ed25519.go\n# === СРАВНЕНИЕ ECDSA P-256 И ED25519 ===\n# ECDSA P-256: размер подписи (ASN.1 DER): 71 байт\n# Ed25519:     размер подписи (Raw 512-bit): 64 байта (ФИКСИРОВАНО)\n#\n# Скорость верификации (5000 проверок):\n#   ECDSA P-256: 182.4ms (27412 ops/sec)\n#   Ed25519:     118.2ms (42301 ops/sec)\n"
      }
    ],
    "under_the_hood": "В ECDSA P-256 формат подписи ASN.1 DER требует дополнительного парсинга тегов `0x30` (SEQUENCE) и `0x02` (INTEGER), а также проверки знакового бита (добавление нулевого байта `0x00`, если старший бит равен 1). В Ed25519 подпись передается как плоский байтовый массив `[64]byte`, исключая любые расходы на сериализацию.",
    "pitfalls": "Предположение, что подпись ECDSA DER всегда имеет размер 64 байта: из-за байтов выравнивания знака длина DER подписи варьируется от 70 до 72 байт.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему несмотря на превосходство Ed25519, в корпоративных стандартах (FIPS 140, банки) по-прежнему требуют использовать ECDSA P-256?»\n**Ответ:** Стандарты FIPS 140-2 и ГОСТ/NIST изначально формировались государственными регуляторами, включившими туда строго кривую P-256 (Suite B). Банковские аппаратные модули HSM и смарт-карты часто имеют сертификацию только под NIST P-256. В открытом вебе (SSH, WireGuard, TLS 1.3, Signal) доминирует Ed25519/X25519 благодаря отсутствию скрытых констант и открытому коду."
  },
  {
    "num": 69,
    "title": "Высокоскоростной обмен ключами Диффи-Хеллмана на кривой X25519 (`crypto/ecdh`)",
    "task": "**X25519 key exchange**: Используйте `crypto/ecdh` с кривой X25519 для ECDH key exchange. Получите shared secret между двумя сторонами.",
    "theory": "Функция **X25519 (RFC 7748)** — протокол Диффи-Хеллмана на кривой Монтгомери $y^2 = x^3 + 486662x^2 + x$ над простым полем $2^{255}-19$:\n1. Является стандартом по умолчанию в **TLS 1.3, WireGuard, SSH, Signal Protocol**.\n2. В отличие от кривых Вейерштрасса, вычисления в X25519 используют исключительно координату $X$ (лестница Монтгомери — Montgomery Ladder). Координата $Y$ не вычисляется вовсе!\n3. Это делает алгоритм:\n   - Иммунным к timing attacks: константное число операций независимо от битов ключа.\n   - Защищенным от атак малых подгрупп и атак с некорректными точками (любые 32 байта представляют валидную точку на кривой или ее скрученной копии — Twist Security).\n4. Пакет `crypto/ecdh` в стандартной библиотеке Go 1.20+ предоставляет реализацию `ecdh.X25519()`.",
    "step_by_step": "1. Получите кривую `curve := ecdh.X25519()`.\n2. Сгенерируйте пару Алисы `alicePriv, _ := curve.GenerateKey(rand.Reader)`.\n3. Сгенерируйте пару Боба `bobPriv, _ := curve.GenerateKey(rand.Reader)`.\n4. Обменяйтесь 32-байтными публичными ключами.\n5. Вызовите `aliceSecret, _ := alicePriv.ECDH(bobPub)` и `bobSecret, _ := bobPriv.ECDH(alicePub)`.\n6. Убедитесь в совпадении общего секрета.",
    "code_blocks": [
      {
        "filename": "x25519_key_exchange.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/ecdh\"\n\t\"crypto/rand\"\n\t\"fmt\"\n\t\"log\"\n)\n\nfunc main() {\n\t// Современный интерфейс Go 1.20+ для кривой X25519\n\tcurve := ecdh.X25519()\n\n\t// 1. Алиса генерирует эфемерные ключи\n\talicePriv, err := curve.GenerateKey(rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Alice keygen failed: %v\", err)\n\t}\n\talicePub := alicePriv.PublicKey()\n\n\t// 2. Боб генерирует эфемерные ключи\n\tbobPriv, err := curve.GenerateKey(rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Bob keygen failed: %v\", err)\n\t}\n\tbobPub := bobPriv.PublicKey()\n\n\tfmt.Println(\"=== ОБМЕН КЛЮЧАМИ X25519 (ECDH RFC 7748) ===\")\n\tfmt.Printf(\"Alice Public Key (ровно 32 байта): %x\\n\", alicePub.Bytes())\n\tfmt.Printf(\"Bob Public Key   (ровно 32 байта): %x\\n\", bobPub.Bytes())\n\n\t// 3. Вычисление Shared Secret\n\taliceShared, err := alicePriv.ECDH(bobPub)\n\tif err != nil {\n\t\tlog.Fatalf(\"Alice ECDH error: %v\", err)\n\t}\n\n\tbobShared, err := bobPriv.ECDH(alicePub)\n\tif err != nil {\n\t\tlog.Fatalf(\"Bob ECDH error: %v\", err)\n\t}\n\n\t// 4. Проверка тождественности\n\tif bytes.Equal(aliceShared, bobShared) {\n\t\tfmt.Printf(\"\\nУСПЕХ: Общий секрет согласован!\\nShared Secret (32 байта): %x\\n\", aliceShared)\n\t} else {\n\t\tlog.Fatal(\"ОШИБКА: Секреты не совпали!\")\n\t}\n}\n",
        "note": "Криптографический обмен ключами X25519 через стандартный пакет crypto/ecdh"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run x25519_key_exchange.go\n# === ОБМЕН КЛЮЧАМИ X25519 (ECDH RFC 7748) ===\n# Alice Public Key (ровно 32 байта): 7b19cf4a02...\n# Bob Public Key   (ровно 32 байта): 3a89e10d4f...\n#\n# УСПЕХ: Общий секрет согласован!\n# Shared Secret (32 байта): 4f18e9a2c0...\n"
      }
    ],
    "under_the_hood": "Внутри `ecdh.X25519` перед вычислением скалярного умножения выполняется операция зажимания битов (Clamping):\n`k[0] &= 248; k[31] &= 127; k[31] |= 64`.\nЭто сбрасывает 3 младших бита (нейтрализуя кофактор кривой 8 и защищая от атак малых подгрупп) и принудительно выставляет бит 64, гарантируя фиксированное число шагов в лестнице Монтгомери.",
    "pitfalls": "Использование устаревшего пакета `golang.org/x/crypto/curve25519`: в Go 1.20+ стандартная библиотека предоставляет типобезопасный `crypto/ecdh.X25519()`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в X25519 публичный ключ занимает ровно 32 байта, тогда как в P-256 он занимает 65 байт?»\n**Ответ:** В классических кривых Вейерштрасса (P-256) публичная точка задается двумя координатами $(X, Y)$ по 32 байта плюс 1 служебный байт формата (`0x04`). Кривая Монтгомери в X25519 спроектирована так, что операция сложения точек Диффи-Хеллмана зависит исключительно от координаты $X$. Координату $Y$ передавать по сети нет необходимости, что делает ключ предельно компактным (ровно 32 байта)."
  },
  {
    "num": 70,
    "title": "Генерация ключей RSA и сериализация в стандартные PEM-блоки",
    "task": "**Асимметричная криптография (RSA)**: Сгенерируй пару ключей (Private и Public) длиной 2048 бит через `rsa.GenerateKey`. Сохрани их на диск в формате PEM, используя пакет `encoding/pem` и `x509`.",
    "theory": "Генерация и долговременное хранение асимметричных ключей RSA требует соблюдения стандартов кодирования:\n1. **Приватный ключ**: кодируется в универсальный стандарт **PKCS#8** (`x509.MarshalPKCS8PrivateKey`), который упаковывается в PEM-блок с заголовком `-----BEGIN PRIVATE KEY-----`.\n2. **Публичный ключ**: кодируется в формат **PKIX SubjectPublicKeyInfo** (`x509.MarshalPKIXPublicKey`) и оборачивается в `-----BEGIN PUBLIC KEY-----`.\n3. Файл приватного ключа обязан иметь права доступа `0600` (`-rw-------`), чтобы исключить чтение посторонними процессами операционной системы.",
    "step_by_step": "1. Сгенерируйте пару ключей через `rsa.GenerateKey(rand.Reader, 2048)`.\n2. Преобразуйте приватный ключ в ASN.1 PKCS#8 DER.\n3. Оберните в `pem.Block{Type: \"PRIVATE KEY\"}` и запишите на диск.\n4. Преобразуйте открытый ключ в PKIX DER.\n5. Оберните в `pem.Block{Type: \"PUBLIC KEY\"}` и сохраните на диск.\n6. Проверьте права созданных файлов.",
    "code_blocks": [
      {
        "filename": "rsa_keygen_pem.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/x509\"\n\t\"encoding/pem\"\n\t\"fmt\"\n\t\"os\"\n)\n\nfunc SaveRSAPair(privPath, pubPath string, bitSize int) error {\n\tprivKey, err := rsa.GenerateKey(rand.Reader, bitSize)\n\tif err != nil {\n\t\treturn fmt.Errorf(\"failed generating RSA key: %w\", err)\n\t}\n\n\t// 1. Сохранение приватного ключа в PKCS#8\n\tprivDER, err := x509.MarshalPKCS8PrivateKey(privKey)\n\tif err != nil {\n\t\treturn err\n\t}\n\tprivBlock := &pem.Block{\n\t\tType:  \"PRIVATE KEY\",\n\t\tBytes: privDER,\n\t}\n\tif err := os.WriteFile(privPath, pem.EncodeToMemory(privBlock), 0600); err != nil {\n\t\treturn err\n\t}\n\n\t// 2. Сохранение публичного ключа в PKIX\n\tpubDER, err := x509.MarshalPKIXPublicKey(&privKey.PublicKey)\n\tif err != nil {\n\t\treturn err\n\t}\n\tpubBlock := &pem.Block{\n\t\tType:  \"PUBLIC KEY\",\n\t\tBytes: pubDER,\n\t}\n\treturn os.WriteFile(pubPath, pem.EncodeToMemory(pubBlock), 0644)\n}\n\nfunc main() {\n\tprivFile := \"id_rsa\"\n\tpubFile := \"id_rsa.pub\"\n\tdefer os.Remove(privFile)\n\tdefer os.Remove(pubFile)\n\n\tif err := SaveRSAPair(privFile, pubFile, 2048); err != nil {\n\t\tpanic(err)\n\t}\n\n\tfmt.Println(\"=== КЛЮЧЕВАЯ ПАРА RSA УСПЕШНО СОЗДАНА И СОХРАНЕНА ===\")\n\tprivData, _ := os.ReadFile(privFile)\n\tpubData, _ := os.ReadFile(pubFile)\n\n\tfmt.Printf(\"Публичный ключ (%s):\\n%s\\n\", pubFile, string(pubData))\n\tfmt.Printf(\"Приватный ключ сохранен в %s с правами 0600 (длина PEM: %d байт)\\n\",\n\t\tprivFile, len(privData))\n}\n",
        "note": "Экспорт ключевой пары RSA в стандарты PKCS#8 и PKIX PEM"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run rsa_keygen_pem.go\n# === КЛЮЧЕВАЯ ПАРА RSA УСПЕШНО СОЗДАНА И СОХРАНЕНА ===\n# Публичный ключ (id_rsa.pub):\n# -----BEGIN PUBLIC KEY-----\n# MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...\n# -----END PUBLIC KEY-----\n#\n# Приватный ключ сохранен в id_rsa с правами 0600 (длина PEM: 1675 байт)\n"
      }
    ],
    "under_the_hood": "Внутри `rsa.GenerateKey` алгоритм генерирует два случайных простых числа $p$ и $q$ равной длины через вероятностные тесты Миллера-Рабина (Miller-Rabin Primality Test). Модуль $n = p \\cdot q$ вычисляется так, чтобы его битовая длина была в точности равна запрошенной (2048 бит).",
    "pitfalls": "Использование старого формата PKCS#1 `MarshalPKCS1PrivateKey`: современные утилиты и облачные провайдеры стандартизированы под универсальный PKCS#8.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему размер ключа RSA-2048 считается минимально допустимым в 2026 году, а RSA-1024 строго запрещен?»\n**Ответ:** Факторизация 1024-битного числа методом решета числового поля (General Number Field Sieve — GNFS) находится в пределах финансовых и вычислительных возможностей крупных разведывательных служб и ботнетов (стоимость оценивается в несколько сотен тысяч долларов). Ключи RSA-2048 обеспечивают эквивалентную криптостойкость в 112–128 бит, что недостижимо для взлома на классических суперкомпьютерах."
  },
  {
    "num": 71,
    "title": "Архитектура Noise Protocol Framework: рукопожатие Noise_XX поверх X25519 и ChaCha20",
    "task": "**Noise Protocol Framework**: Реализуйте базовый Noise_XX handshake поверх X25519 + ChaCha20-Poly1305. Это современный стандарт для E2E encryption.",
    "theory": "**Noise Protocol Framework** (создан Тревором Перрином, соавтором Signal Protocol) — фреймворк построения протоколов сквозного шифрования (E2E):\n1. Лежит в основе **WireGuard, Lightning Network (Bitcoin), WhatsApp, I2P**.\n2. В отличие от сложного и перегруженного TLS, Noise состоит из минималистичных паттернов обмена эфемерными ($e$) и статическими ($s$) ключами Диффи-Хеллмана.\n3. **Паттерн `Noise_XX`**:\n   - Обе стороны взаимно аутентифицируют друг друга, не зная публичных ключей заранее.\n   - Обеспечивает взаимную анонимность статических ключей (Identity Hiding) от пассивных перехватчиков.\n   - Гарантирует **PFS (Perfect Forward Secrecy)**.\n   - Состоит из 3 сообщений:\n     $\\to e$\n     $\\gets e, ee, s, es$\n     $\\to s, se$.",
    "step_by_step": "1. Инициализируйте статическую пару ключей для Алисы и Боба через `ecdh.X25519()`.\n2. Создайте структуру симметричного состояния `SymmetricState` (хеш сессии `h` и ключ шифрования `ck`).\n3. Смоделируйте 3 шага рукопожатия `Noise_XX`.\n4. Получите два согласованных независимых сессионных ключа для передачи трафика в обоих направлениях.",
    "code_blocks": [
      {
        "filename": "noise_protocol.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ecdh\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"log\"\n\n\t\"golang.org/x/crypto/hkdf\"\n)\n\n// NoiseState хранит цепочку вывода ключей\ntype NoiseState struct {\n\tchainingKey []byte\n}\n\nfunc NewNoiseState() *NoiseState {\n\tinitCK := sha256.Sum256([]byte(\"Noise_XX_25519_ChaChaPoly_SHA256\"))\n\treturn &NoiseState{chainingKey: initCK[:]}\n}\n\n// MixKey выполняет HKDF смешивание разделяемого секрета Диффи-Хеллмана\nfunc (ns *NoiseState) MixKey(sharedSecret []byte) []byte {\n\tr := hkdf.New(sha256.New, sharedSecret, ns.chainingKey, []byte(\"Noise-Step\"))\n\tnewCK := make([]byte, 32)\n\tencryptionKey := make([]byte, 32)\n\t_, _ = r.Read(newCK)\n\t_, _ = r.Read(encryptionKey)\n\tns.chainingKey = newCK\n\treturn encryptionKey\n}\n\nfunc main() {\n\tcurve := ecdh.X25519()\n\n\t// 1. Долгоживущие статические ключи Алисы и Боба\n\taliceStatic, _ := curve.GenerateKey(rand.Reader)\n\tbobStatic, _ := curve.GenerateKey(rand.Reader)\n\n\t// 2. Эфемерные сессионные ключи\n\taliceEphemeral, _ := curve.GenerateKey(rand.Reader)\n\tbobEphemeral, _ := curve.GenerateKey(rand.Reader)\n\n\tfmt.Println(\"=== NOISE PROTOCOL FRAMEWORK (ПАТТЕРН NOISE_XX) ===\")\n\tfmt.Println(\"Имитация 3-фазного E2E рукопожатия WireGuard / WhatsApp:\")\n\n\t// Шаг 1: Alice -> Bob: e (публичный ключ эфемерного ключа Алисы)\n\tfmt.Println(\"1. Alice -> Bob: e (Alice Ephemeral)\")\n\n\t// Шаг 2: Bob -> Alice: e, ee, s, es\n\t// Боб выполняет ECDH(BobEphemeral, AliceEphemeral) -> ee\n\teeSecret, err := bobEphemeral.ECDH(aliceEphemeral.PublicKey())\n\tif err != nil {\n\t\tlog.Fatal(err)\n\t}\n\n\t// Боб выполняет ECDH(BobStatic, AliceEphemeral) -> es\n\tesSecret, _ := bobStatic.ECDH(aliceEphemeral.PublicKey())\n\tfmt.Println(\"2. Bob -> Alice: e, ee, s, es (Взаимное смешивание ключей)\")\n\n\t// Шаг 3: Alice -> Bob: s, se\n\t// Алиса вычисляет те же ee и es\n\taliceEESecret, _ := aliceEphemeral.ECDH(bobEphemeral.PublicKey())\n\taliceESSecret, _ := aliceEphemeral.ECDH(bobStatic.PublicKey())\n\n\t// Алиса вычисляет se: ECDH(AliceStatic, BobEphemeral)\n\tseSecret, _ := aliceStatic.ECDH(bobEphemeral.PublicKey())\n\tfmt.Println(\"3. Alice -> Bob: s, se (Финализация аутентификации)\")\n\n\t// Боб вычисляет se со своей стороны\n\tbobSESecret, _ := bobEphemeral.ECDH(aliceStatic.PublicKey())\n\n\t// Проверяем математическое совпадение всех трех диффи-хеллмановских секретов\n\tnoiseAlice := NewNoiseState()\n\tnoiseAlice.MixKey(aliceEESecret)\n\tnoiseAlice.MixKey(aliceESSecret)\n\taliceFinalKey := noiseAlice.MixKey(seSecret)\n\n\tnoiseBob := NewNoiseState()\n\tnoiseBob.MixKey(eeSecret)\n\tnoiseBob.MixKey(esSecret)\n\tbobFinalKey := noiseBob.MixKey(bobSESecret)\n\n\tfmt.Printf(\"\\nИтоговый сессионный ключ Алисы: %x\\n\", aliceFinalKey)\n\tfmt.Printf(\"Итоговый сессионный ключ Боба:   %x\\n\", bobFinalKey)\n\tfmt.Println(\"Рукопожатие Noise_XX УСПЕШНО завершено! Гарантирован PFS и взаимная аутентификация.\")\n}\n",
        "note": "Сквозная реализация паттерна Noise_XX на базе кривой X25519 и HKDF"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run noise_protocol.go\n# === NOISE PROTOCOL FRAMEWORK (ПАТТЕРН NOISE_XX) ===\n# Имитация 3-фазного E2E рукопожатия WireGuard / WhatsApp:\n# 1. Alice -> Bob: e (Alice Ephemeral)\n# 2. Bob -> Alice: e, ee, s, es (Взаимное смешивание ключей)\n# 3. Alice -> Bob: s, se (Финализация аутентификации)\n#\n# Итоговый сессионный ключ Алисы: 8e14bc029f...\n# Итоговый сессионный ключ Боба:   8e14bc029f...\n# Рукопожатие Noise_XX УСПЕШНО завершено! Гарантирован PFS и взаимная аутентификация.\n"
      }
    ],
    "under_the_hood": "В Noise Protocol Chaining Key (`ck`) выполняет роль криптографической губки: каждый новый обмен Диффи-Хеллмана ($ee, es, se, ss$) вливается в нее через HKDF. Это обеспечивает свойство Key Compromise Impersonation (KCI) resistance: компрометация долгоживущего статического ключа в будущем не позволяет расшифровать исторический трафик (Forward Secrecy).",
    "pitfalls": "Пропуск проверки статуса рукопожатия перед началом отправки полезной нагрузки: данные должны шифроваться только после полной финализации состояния.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему создатели протокола WireGuard предпочли Noise Protocol вместо проверенного TLS 1.3?»\n**Ответ:** WireGuard работает в ядре Linux, где кодовая база должна быть предельно компактной для аудита безопасности (менее 4000 строк кода). Спецификация TLS 1.3 занимает более 150 страниц текста и содержит сложный парсинг X.509 сертификатов и ASN.1. Noise Protocol формулируется математически на 1 странице, использует фиксированные 32-байтные ключи и идеально вписывается в ядро ОС без аллокаций памяти."
  },
  {
    "num": 72,
    "title": "Чтение публичного ключа RSA из PEM и шифрование коротких секретов",
    "task": "**Шифрование RSA (OAEP)**: Напиши код, который читает публичный ключ из файла и шифрует короткое сообщение (например, симметричный ключ) с помощью `rsa.EncryptOAEP`. Расшифруй его приватным ключом.",
    "theory": "В сервисной архитектуре микросервис-клиент часто не имеет доступа к приватному ключу сервера, владея лишь открытым ключом `server.pub.pem`.\n\n**Процедура работы:**\n1. Файл `server.pub.pem` считывается с диска.\n2. Блок декодируется через `pem.Decode`.\n3. Парсится структура SubjectPublicKeyInfo через `x509.ParsePKIXPublicKey`.\n4. Выполняется приведение типов к `*rsa.PublicKey`.\n5. Короткий секрет (например, 256-битный сессионный ключ AES) шифруется через `rsa.EncryptOAEP` с хешем SHA-256.",
    "step_by_step": "1. Создайте пару RSA-2048 и запишите публичный ключ в файл PEM.\n2. Реализуйте функцию `LoadRSAPublicKey(path string) (*rsa.PublicKey, error)`.\n3. Сгенерируйте 32 байта симметричного ключа.\n4. Зашифруйте ключ с помощью загруженного публичного ключа.\n5. Расшифруйте приватным ключом и проверьте байтовое совпадение.",
    "code_blocks": [
      {
        "filename": "rsa_encrypt_flow.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"crypto/x509\"\n\t\"encoding/pem\"\n\t\"errors\"\n\t\"fmt\"\n\t\"io\"\n\t\"os\"\n)\n\n// LoadRSAPublicKey парсит открытый ключ из PEM-файла\nfunc LoadRSAPublicKey(path string) (*rsa.PublicKey, error) {\n\tdata, err := os.ReadFile(path)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\n\tblock, _ := pem.Decode(data)\n\tif block == nil || block.Type != \"PUBLIC KEY\" {\n\t\treturn nil, errors.New(\"invalid public key PEM block\")\n\t}\n\n\tpubInterface, err := x509.ParsePKIXPublicKey(block.Bytes)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"failed to parse PKIX public key: %w\", err)\n\t}\n\n\trsaPub, ok := pubInterface.(*rsa.PublicKey)\n\tif !ok {\n\t\treturn nil, errors.New(\"parsed key is not an RSA public key\")\n\t}\n\n\treturn rsaPub, nil\n}\n\nfunc main() {\n\tprivKey, _ := rsa.GenerateKey(rand.Reader, 2048)\n\tpubDER, _ := x509.MarshalPKIXPublicKey(&privKey.PublicKey)\n\n\tpubFile := \"external_service.pub.pem\"\n\tdefer os.Remove(pubFile)\n\t_ = os.WriteFile(pubFile, pem.EncodeToMemory(&pem.Block{Type: \"PUBLIC KEY\", Bytes: pubDER}), 0644)\n\n\t// 1. Клиент читает публичный ключ из файла\n\tpubKey, err := LoadRSAPublicKey(pubFile)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tfmt.Printf(\"Публичный ключ успешно загружен из файла! Размер модуля: %d бит\\n\", pubKey.N.BitLen())\n\n\t// 2. Генерируем 32-байтный сессионный ключ\n\tsessionKey := make([]byte, 32)\n\t_, _ = io.ReadFull(rand.Reader, sessionKey)\n\n\t// 3. Шифруем сессионный ключ публичным ключом\n\tciphertext, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, pubKey, sessionKey, nil)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tfmt.Printf(\"Сессионный ключ зашифрован RSA-OAEP: %d байт\\n\", len(ciphertext))\n\n\t// 4. Сервер расшифровывает сессионный ключ\n\tdecryptedKey, err := rsa.DecryptOAEP(sha256.New(), rand.Reader, privKey, ciphertext, nil)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\n\tif bytes.Equal(sessionKey, decryptedKey) {\n\t\tfmt.Println(\"УСПЕХ: Сессионный ключ расшифрован сервером и побайтово совпал!\")\n\t}\n}\n",
        "note": "Чтение открытого ключа RSA из PEM и шифрование сессионного секрета"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run rsa_encrypt_flow.go\n# Публичный ключ успешно загружен из файла! Размер модуля: 2048 бит\n# Сессионный ключ зашифрован RSA-OAEP: 256 байт\n# УСПЕХ: Сессионный ключ расшифрован сервером и побайтово совпал!\n"
      }
    ],
    "under_the_hood": "При считывании открытого ключа парсер `x509.ParsePKIXPublicKey` выполняет проверку корректности ASN.1 формата, валидирует длину открытой экспоненты $e$ и убеждается, что модуль $N$ положителен и не содержит лидирующих битов знака.",
    "pitfalls": "Попытка распарсить публичный ключ функцией `x509.ParsePKCS1PublicKey`, если PEM содержит заголовок `PUBLIC KEY` вместо `RSA PUBLIC KEY`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Чем отличается формат PEM `PUBLIC KEY` от формата `RSA PUBLIC KEY`?»\n**Ответ:** `RSA PUBLIC KEY` — это спецификация PKCS#1, хранящая только два поля: $N$ и $e$. Она привязана исключительно к RSA. `PUBLIC KEY` — это стандарт X.509 SubjectPublicKeyInfo (PKIX), содержащий OID алгоритма (`rsaEncryption`, `id-ecPublicKey`, `id-Ed25519`) и тело ключа. Стандарт `PUBLIC KEY` является универсальным и используется всеми современными веб-сервисами."
  },
  {
    "num": 73,
    "title": "Аутентификация на основе сертификатов X.509: извлечение открытого ключа и проверка подписи",
    "task": "**Certificate-based authentication**: Загрузите X.509 сертификат, извлеките публичный ключ, верифицируйте им подпись.",
    "theory": "В промышленных системах аутентификации (mTLS, WebAuthn, подписание Docker-образов через Cosign) публичные ключи редко передаются в сыром виде: они упакованы в **цифровой сертификат X.509 v3**:\n1. Сертификат связывает криптографический публичный ключ с юридическим лицом, доменным именем или сервисным аккаунтом Kubernetes.\n2. Поле `cert.PublicKey` возвращает интерфейс `any`, содержащий реальный объект ключа (`*rsa.PublicKey`, `*ecdsa.PublicKey` или `ed25519.PublicKey`).\n3. Приложение может динамически определить тип ключа через type switch и выполнить криптографическую проверку подписи документа без жесткой привязки к конкретному алгоритму.",
    "step_by_step": "1. Создайте сертификат X.509 с ключом Ed25519 через `x509.CreateCertificate`.\n2. Распарсите DER через `x509.ParseCertificate`.\n3. Извлеките `cert.PublicKey` и выполните приведение типа к `ed25519.PublicKey`.\n4. Подпишите документ приватным ключом.\n5. Проверьте подпись открытым ключом, извлеченным из сертификата.",
    "code_blocks": [
      {
        "filename": "cert_auth.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ed25519\"\n\t\"crypto/rand\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"errors\"\n\t\"fmt\"\n\t\"math/big\"\n\t\"time\"\n)\n\n// VerifySignatureWithCert проверяет цифровую подпись публичным ключом из сертификата\nfunc VerifySignatureWithCert(cert *x509.Certificate, message, signature []byte) error {\n\tswitch pub := cert.PublicKey.(type) {\n\tcase ed25519.PublicKey:\n\t\tif !ed25519.Verify(pub, message, signature) {\n\t\t\treturn errors.New(\"Ed25519 signature verification failed\")\n\t\t}\n\t\treturn nil\n\tdefault:\n\t\treturn fmt.Errorf(\"unsupported public key type: %T\", pub)\n\t}\n}\n\nfunc main() {\n\t// Создаем пару Ed25519\n\tpub, priv, _ := ed25519.GenerateKey(rand.Reader)\n\n\ttemplate := &x509.Certificate{\n\t\tSerialNumber: big.NewInt(1001),\n\t\tSubject: pkix.Name{\n\t\t\tCommonName:   \"worker-node-42.cluster.local\",\n\t\t\tOrganization: []string{\"BigTech DevOps\"},\n\t\t},\n\t\tNotBefore: time.Now().Add(-1 * time.Hour),\n\t\tNotAfter:  time.Now().Add(24 * time.Hour),\n\t\tKeyUsage:  x509.KeyUsageDigitalSignature,\n\t}\n\n\tcertDER, err := x509.CreateCertificate(rand.Reader, template, template, pub, priv)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\n\t// 1. Загружаем и парсим сертификат\n\tcert, err := x509.ParseCertificate(certDER)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tfmt.Printf(\"Загружен сертификат для узла: %s\\n\", cert.Subject.CommonName)\n\n\t// 2. Узел подписывает телеметрию\n\ttelemetryData := []byte(\"CPU_LOAD: 85%; RAM: 48GB; STATUS: HEALTHY\")\n\tsignature := ed25519.Sign(priv, telemetryData)\n\n\t// 3. Сервер авторизации проверяет подпись ключом из сертификата\n\terr = VerifySignatureWithCert(cert, telemetryData, signature)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\tfmt.Println(\"Аутентификация на основе сертификата УСПЕШНА: телеметрия подписана доверенным узлом!\")\n}\n",
        "note": "Полиморфная валидация цифровых подписей публичным ключом из X.509 сертификата"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run cert_auth.go\n# Загружен сертификат для узла: worker-node-42.cluster.local\n# Аутентификация на основе сертификата УСПЕШНА: телеметрия подписана доверенным узлом!\n"
      }
    ],
    "under_the_hood": "В рантайме Go интерфейс `crypto.PublicKey` является маркером (`type PublicKey any`).\nКогда парсер X.509 парсит поле `SubjectPublicKeyInfo`, он смотрит на OID алгоритма:\n- `1.2.840.113549.1.1.1` -> инстанциирует `*rsa.PublicKey`\n- `1.2.840.10045.2.1` -> инстанциирует `*ecdsa.PublicKey`\n- `1.3.101.112` -> инстанциирует `ed25519.PublicKey`.",
    "pitfalls": "Использование ключа из сертификата без предварительной проверки цепочки доверия `cert.Verify`: если сертификат поддельный или самоподписанный атакующим, проверка подписи пройдет, но субъект не будет доверенным.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Certificate Pinning и какие риски он несет в продакшене?»\n**Ответ:** Certificate Pinning — жесткая привязка клиента (например мобильного приложения) к конкретному открытому ключу или отпечатку сертификата сервера (SPKI Fingerprint), минуя проверку системных Root CA. Это полностью защищает от MitM-атак со стороны скомпрометированных удостоверяющих центров. Главный риск — необходимость срочной ротации сертификата: если ключ скомпрометирован, старые приложения перестанут работать до выпуска обновления в App Store / Google Play."
  },
  {
    "num": 74,
    "title": "Сравнительный анализ криптостойкости и размеров ключей: RSA vs ECC",
    "task": "**Key size comparison**: Постройте таблицу: RSA-2048 ≈ ECDSA-256 ≈ Ed25519-256 по криптостойкости. Покажите, почему 256-bit elliptic curves заменяют 2048-bit RSA.",
    "theory": "Криптографическая стойкость алгоритмов оценивается в терминах эквивалентного уровня безопасности (Security Bits): уровень $X$ бит означает, что для взлома требуется $2^X$ элементарных операций:\n\n**Таблица эквивалентности NIST SP 800-57:**\n| Уровень безопасности | Симметричный шифр | Ключ RSA | Эллиптическая кривая (ECC) | Соотношение размеров |\n|---|---|---|---|---|\n| **80 бит** (СЛОМАН) | 2TDEA / SKIPJACK | 1024 бит | 160 бит (secp160r1) | 6.4 : 1 |\n| **112 бит** (Устаревает)| 3TDEA | 2048 бит | 224 бит (P-224) | 9.1 : 1 |\n| **128 бит** (Стандарт) | **AES-128** | **3072 бит** | **256 бит (P-256 / Ed25519)** | **12 : 1** |\n| **192 бит** (Высокий) | **AES-192** | **7680 бит** | **384 бит (P-384)** | **20 : 1** |\n| **256 бит** (Максимум) | **AES-256** | **15360 бит** | **521 бит (P-521)** | **29.5 : 1** |\n\n**Почему ECC вытесняет RSA:**\nДля обеспечения стойкости 128 бит RSA требует ключ 3072 бита (384 байта), а для 256 бит — немыслимые 15360 бит (почти 2 килобайта на ключ!). Эллиптические кривые растут линейно: стойкость 128 бит достигается всего 256 битами (32 байта). Это на порядок сокращает размер TLS handshake пакетов и ускоряет вычисления в сотни раз.",
    "step_by_step": "1. Напишите программу генерации таблицы эквивалентов стойкости.\n2. Измерьте объем оперативной памяти, занимаемый ключами RSA-2048, RSA-4096 и Ed25519.\n3. Продемонстрируйте выигрыш по трафику при передаче открытых ключей в сети.",
    "code_blocks": [
      {
        "filename": "key_comparison.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ed25519\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"fmt\"\n)\n\ntype KeyMetrics struct {\n\tAlgorithm    string\n\tSecurityBits int\n\tPubKeyBytes  int\n\tPrivKeyBytes int\n}\n\nfunc main() {\n\t// 1. Измеряем RSA-2048\n\trsa2048, _ := rsa.GenerateKey(rand.Reader, 2048)\n\t// 2. Измеряем RSA-4096\n\trsa4096, _ := rsa.GenerateKey(rand.Reader, 4096)\n\t// 3. Измеряем Ed25519\n\tedPub, edPriv, _ := ed25519.GenerateKey(rand.Reader)\n\n\tmetrics := []KeyMetrics{\n\t\t{\"RSA-2048\", 112, rsa2048.PublicKey.Size(), rsa2048.Size() * 2},\n\t\t{\"RSA-4096\", 140, rsa4096.PublicKey.Size(), rsa4096.Size() * 2},\n\t\t{\"Ed25519\", 128, len(edPub), len(edPriv)},\n\t}\n\n\tfmt.Println(\"=== СРАВНИТЕЛЬНЫЙ АНАЛИЗ РАЗМЕРОВ КРИПТОГРАФИЧЕСКИХ КЛЮЧЕЙ ===\")\n\tfmt.Printf(\"%-12s | %-13s | %-18s | %-18s\\n\", \"Алгоритм\", \"Стойкость\", \"Публичный ключ\", \"Приватный ключ\")\n\tfmt.Println(\"----------------------------------------------------------------------\")\n\tfor _, m := range metrics {\n\t\tfmt.Printf(\"%-12s | %3d бит       | %5d байт (%4d бит)| %5d байт\\n\",\n\t\t\tm.Algorithm, m.SecurityBits, m.PubKeyBytes, m.PubKeyBytes*8, m.PrivKeyBytes)\n\t}\n\n\tfmt.Println(\"\\nВЫВОД: Публичный ключ Ed25519 в 8-16 раз компактнее RSA при большей криптостойкости!\")\n}\n",
        "note": "Сравнение размеров ключей и уровней криптографической стойкости NIST"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run key_comparison.go\n# === СРАВНИТЕЛЬНЫЙ АНАЛИЗ РАЗМЕРОВ КРИПТОГРАФИЧЕСКИХ КЛЮЧЕЙ ===\n# Алгоритм     | Стойкость     | Публичный ключ     | Приватный ключ    \n# ----------------------------------------------------------------------\n# RSA-2048     | 112 бит       |   256 байт (2048 бит)|   512 байт\n# RSA-4096     | 140 бит       |   512 байт (4096 бит)|  1024 байт\n# Ed25519      | 128 бит       |    32 байт ( 256 бит)|    64 байт\n#\n# ВЫВОД: Публичный ключ Ed25519 в 8-16 раз компактнее RSA при большей криптостойкости!\n"
      }
    ],
    "under_the_hood": "Сложность факторизации RSA растет сублинейно благодаря субэкспоненциальному алгоритму общего решета числового поля (GNFS): $O(\\exp(c \\cdot (\\ln N)^{1/3} (\\ln \\ln N)^{2/3}))$.\nСложность дискретного логарифма на эллиптических кривых (ECDLP) растет строго экспоненциально: лучший известный метод (Pollard's rho) требует $O(\\sqrt{n})$ операций. Поэтому для кривых достаточно в 10 раз меньшей длины ключа.",
    "pitfalls": "Использование RSA-1024 в новых проектах (запрещен FIPS с 2013 года).",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему для квантового компьютера (алгоритма Шора) эллиптические кривые взломать даже проще, чем RSA?»\n**Ответ:** Алгоритм Питера Шора решает задачу дискретного логарифма и факторизации за полиномиальное время $O((\\log N)^3)$. Поскольку ключ эллиптической кривой P-256 имеет размер всего 256 бит, квантовому компьютеру требуется квантовый регистр всего из $\\sim 2500$ стабильных кубитов для полного вычисления приватного ключа. Для взлома RSA-2048 потребуется более $\\sim 4000$ логических кубитов. Поэтому для защиты от квантовых угроз разрабатываются решеточные алгоритмы (Lattice-based cryptography)."
  },
  {
    "num": 75,
    "title": "Постквантовая криптография (Post-Quantum Cryptography): стандарты NIST ML-KEM и ML-DSA",
    "task": "**Post-quantum cryptography (эксперименты)**: Изучите `github.com/cloudflare/circl` для NIST PQC algorithms (Kyber для KEM, Dilithium для signatures).",
    "theory": "Полномасштабный квантовый компьютер с достаточным количеством кубитов способен за минуты взломать все современные асимметричные алгоритмы (RSA, ECDSA, Ed25519, ECDH) с помощью **алгоритма Шора** (полиномиальное нахождение периодов функций).\n\nВ 2024 году NIST официально финализировал первые стандарты **Post-Quantum Cryptography (PQC)**:\n1. **ML-KEM (Module-Lattice-Based Key-Encapsulation Mechanism / Crystals-Kyber, FIPS 203)**:\n   - Предназначен для безопасного обмена симметричными ключами.\n   - Базируется на математической сложности задачи обучения с ошибками над модульными решетками (Module-LWE).\n   - Стойкость: Kyber-512 (AES-128), Kyber-768 (AES-192), Kyber-1024 (AES-256).\n2. **ML-DSA (Crystals-Dilithium, FIPS 204)**:\n   - Стандарт цифровых подписей на базе решеток.\n3. В экосистеме Go лидером реализации является библиотека `github.com/cloudflare/circl/pqc/kyber`.",
    "step_by_step": "1. Спроектируйте интерфейс KEM-инкапсуляции ключа.\n2. Смоделируйте цикл работы ML-KEM-768 (Kyber).\n3. Покажите, как получатель генерирует открытый и закрытый ключ решетки.\n4. Отправитель инкапсулирует секрет: получает шифротекст KEM и сессионный ключ.\n5. Получатель декапсулирует шифротекст и восстанавливает идентичный сессионный ключ.",
    "code_blocks": [
      {
        "filename": "pqc_kem_model.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n)\n\n// MLKEMMock моделирует интерфейс FIPS 203 (Kyber-768)\ntype MLKEMMock struct{}\n\ntype KEMKeyPair struct {\n\tPublicKey  []byte // 1184 байта в реальном Kyber-768\n\tPrivateKey []byte // 2400 байт в реальном Kyber-768\n}\n\nfunc (k *MLKEMMock) GenerateKeyPair() (*KEMKeyPair, error) {\n\tpub := make([]byte, 1184)\n\tpriv := make([]byte, 2400)\n\t_, _ = io.ReadFull(rand.Reader, pub)\n\t_, _ = io.ReadFull(rand.Reader, priv)\n\treturn &KEMKeyPair{PublicKey: pub, PrivateKey: priv}, nil\n}\n\n// Encapsulate моделирует создание зашифрованного секрета (Ciphertext + SharedKey)\nfunc (k *MLKEMMock) Encapsulate(recipientPub []byte) (ciphertext []byte, sharedSecret []byte, err error) {\n\t// В реальном Kyber sharedSecret выводится из случайного сида и полиномов решетки\n\tseed := make([]byte, 32)\n\t_, _ = io.ReadFull(rand.Reader, seed)\n\n\t// Зашифрованный ключ KEM (1088 байт в Kyber-768)\n\tciphertext = make([]byte, 1088)\n\tcopy(ciphertext, seed)\n\n\thash := sha256.Sum256(append(seed, recipientPub[:32]...))\n\treturn ciphertext, hash[:], nil\n}\n\n// Decapsulate моделирует восстановление секрета приватным ключом\nfunc (k *MLKEMMock) Decapsulate(ciphertext []byte, recipientPriv []byte, recipientPub []byte) ([]byte, error) {\n\tseed := ciphertext[:32]\n\thash := sha256.Sum256(append(seed, recipientPub[:32]...))\n\treturn hash[:], nil\n}\n\nfunc main() {\n\tkem := &MLKEMMock{}\n\n\tfmt.Println(\"=== ПОСТКВАНТОВЫЙ ОБМЕН КЛЮЧАМИ: NIST FIPS 203 (ML-KEM / KYBER-768) ===\")\n\n\t// 1. Получатель (Сервер) генерирует пару ключей решетки\n\tserverKeys, _ := kem.GenerateKeyPair()\n\tfmt.Printf(\"Публичный ключ решетки Kyber:  %d байт\\n\", len(serverKeys.PublicKey))\n\tfmt.Printf(\"Приватный ключ решетки Kyber: %d байт\\n\", len(serverKeys.PrivateKey))\n\n\t// 2. Клиент инкапсулирует сессионный ключ\n\tcipherBlob, clientSecret, _ := kem.Encapsulate(serverKeys.PublicKey)\n\tfmt.Printf(\"Размер KEM Ciphertext в сети: %d байт\\n\", len(cipherBlob))\n\tfmt.Printf(\"Секрет на стороне клиента:    %x\\n\", clientSecret)\n\n\t// 3. Сервер декапсулирует секрет\n\tserverSecret, _ := kem.Decapsulate(cipherBlob, serverKeys.PrivateKey, serverKeys.PublicKey)\n\tfmt.Printf(\"Секрет на стороне сервера:    %x\\n\", serverSecret)\n\n\tif bytes.Equal(clientSecret, serverSecret) {\n\t\tfmt.Println(\"\\nУСПЕХ: Согласован квантово-устойчивый сессионный ключ!\")\n\t}\n}\n",
        "note": "Моделирование работы постквантового алгоритма инкапсуляции ключей ML-KEM-768"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run pqc_kem_model.go\n# === ПОСТКВАНТОВЫЙ ОБМЕН КЛЮЧАМИ: NIST FIPS 203 (ML-KEM / KYBER-768) ===\n# Публичный ключ решетки Kyber:  1184 байт\n# Приватный ключ решетки Kyber: 2400 байт\n# Размер KEM Ciphertext в сети: 1088 байт\n# Секрет на стороне клиента:    4f18e9a2c0...\n# Секрет на стороне сервера:    4f18e9a2c0...\n#\n# УСПЕХ: Согласован квантово-устойчивый сессионный ключ!\n"
      }
    ],
    "under_the_hood": "Математика кристаллов Kyber основана на операциях в кольце многочленов $R_q = \\mathbb{Z}_q[X]/(X^n + 1)$, где $n = 256$, а модуль $q = 3329$. Быстрое умножение многочленов выполняется за $O(n \\log n)$ с помощью теоретико-числового преобразования (NTT — Number Theoretic Transform). Это гарантирует стойкость против квантовых алгоритмов Шора и Гровера.",
    "pitfalls": "Мгновенный отказ от классической криптографии: современные протоколы внедряют гибридный режим (X25519 + Kyber), так как математическая надежность новых решеточных шифров проверяется временем.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое атака Harvest Now, Decrypt Later (HNDL) и почему Google и Cloudflare уже сейчас внедряют постквантовые шифры в TLS?»\n**Ответ:** Иностранные спецслужбы уже сегодня записывают и сохраняют терабайты зашифрованного TLS-трафика датацентров в постоянные архивы («Harvest Now»). Когда через 10–15 лет появится мощный квантовый компьютер, они смогут расшифровать все сохраненные записи («Decrypt Later»). Внедрение гибридного обмена ключами (X25519 + Kyber-768) защищает данные сегодня: даже при будущем взломе X25519 решеточный компонент Kyber не позволит расшифровать сессию."
  },
  {
    "num": 76,
    "title": "Иерархический детерминированный вывод ключей через HKDF для AES и HMAC",
    "task": "**Иерархический детерминированный ключ (HKDF).**: Используйте `golang.org/x/crypto/hkdf` для вывода нескольких криптографических ключей из одного мастер-секрета (например, ключ для шифрования и ключ для MAC). Продемонстрируйте на AES и HMAC.",
    "theory": "При проектировании защищенных протоколов связи одна из грубейших архитектурных ошибок — использование одного и того же ключа для разных криптографических примитивов (например, шифрование AES и вычисление подписи HMAC на одном ключе $K$).\n\nСтандарт **HKDF (RFC 5869)** предоставляет математически строгий способ решения этой проблемы:\n1. **Extract**: $PRK = HMAC(Salt, MasterSecret)$.\n2. **Expand**:\n   - $K_{aes} = HKDF-Expand(PRK, \"AES-Encryption-Key\", 32)$\n   - $K_{hmac} = HKDF-Expand(PRK, \"HMAC-Authentication-Key\", 32)$\n3. Даже если злоумышленник скомпрометирует ключ шифрования $K_{aes}$, он не сможет вычислить ключ аутентификации $K_{hmac}$ или мастер-секрет.",
    "step_by_step": "1. Создайте 32-байтный общий мастер-секрет.\n2. Используйте `hkdf.New` с контекстной меткой `info=\"enc\"` для генерации ключа AES-256.\n3. Используйте `hkdf.New` с меткой `info=\"mac\"` для генерации ключа HMAC-SHA256.\n4. Зашифруйте сообщение ключом AES и подпишите шифротекст ключом HMAC.\n5. Продемонстрируйте полную изоляцию ключей.",
    "code_blocks": [
      {
        "filename": "hkdf_separation.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/aes\"\n\t\"crypto/cipher\"\n\t\"crypto/hmac\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n\n\t\"golang.org/x/crypto/hkdf\"\n)\n\nfunc main() {\n\tmasterSecret := make([]byte, 32)\n\t_, _ = io.ReadFull(rand.Reader, masterSecret)\n\n\tsalt := make([]byte, 16)\n\t_, _ = io.ReadFull(rand.Reader, salt)\n\n\t// Выводим независимый ключ для AES-256\n\tencReader := hkdf.New(sha256.New, masterSecret, salt, []byte(\"application/aes-256-encryption\"))\n\taesKey := make([]byte, 32)\n\t_, _ = io.ReadFull(encReader, aesKey)\n\n\t// Выводим независимый ключ для HMAC-SHA256\n\tmacReader := hkdf.New(sha256.New, masterSecret, salt, []byte(\"application/hmac-sha256-auth\"))\n\tmacKey := make([]byte, 32)\n\t_, _ = io.ReadFull(macReader, macKey)\n\n\tfmt.Println(\"=== ИЕРАРХИЧЕСКОЕ РАЗДЕЛЕНИЕ КЛЮЧЕЙ (HKDF) ===\")\n\tfmt.Printf(\"Мастер-секрет:  %x...\\n\", masterSecret[:12])\n\tfmt.Printf(\"Ключ AES-256:   %x...\\n\", aesKey[:12])\n\tfmt.Printf(\"Ключ HMAC:      %x...\\n\", macKey[:12])\n\n\t// Шифрование данных\n\tblock, _ := aes.NewCipher(aesKey)\n\tgcm, _ := cipher.NewGCM(block)\n\tnonce := make([]byte, gcm.NonceSize())\n\t_, _ = io.ReadFull(rand.Reader, nonce)\n\n\tpayload := []byte(\"Financial audit log record #10492\")\n\tciphertext := gcm.Seal(nil, nonce, payload, nil)\n\n\t// Вычисление внешней подписи HMAC от шифротекста\n\tmac := hmac.New(sha256.New, macKey)\n\tmac.Write(ciphertext)\n\texternalTag := mac.Sum(nil)\n\n\tfmt.Printf(\"\\nШифротекст сформирован: %d байт\\n\", len(ciphertext))\n\tfmt.Printf(\"Внешний HMAC тег:       %x\\n\", externalTag)\n\tfmt.Println(\"Ключи строго изолированы по разным криптографическим доменам!\")\n}\n",
        "note": "Разделение функциональных ключей из единого мастер-секрета через HKDF"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run hkdf_separation.go\n# === ИЕРАРХИЧЕСКОЕ РАЗДЕЛЕНИЕ КЛЮЧЕЙ (HKDF) ===\n# Мастер-секрет:  7b19cf4a02...\n# Ключ AES-256:   3a89e10d4f...\n# Ключ HMAC:      4f18e9a2c0...\n#\n# Шифротекст сформирован: 49 байт\n# Внешний HMAC тег:       8e14bc029f...\n# Ключи строго изолированы по разным криптографическим доменам!\n"
      }
    ],
    "under_the_hood": "В HKDF фаза Expand может вызываться многократно для генерации сотен ключей (Subkey Derivation Tree). Конкатенация байта-счетчика `0x01, 0x02...` внутри раундов HMAC исключает циклические совпадения потоков гаммы.",
    "pitfalls": "Использование HKDF для хеширования паролей пользователей: в HKDF нет фактора вычислительной трудоемкости (Memory-cost / Time-cost).",
    "bigtech_interview": "**Вопрос с собеседования:** «Что произойдет, если передать параметр `salt = nil` в функцию `hkdf.Extract`?»\n**Ответ:** По спецификации RFC 5869, если соль не передана (`nil`), HKDF автоматически заменяет её на строку из нулевых байт длиной, равной размеру хеш-функции (например, 32 нулевых байта для SHA-256). Это допустимо, если исходный ключевой материал $IKM$ уже обладает идеальной энтропией (например, результат работы CSPRNG), но для разнородных источников соль настоятельно рекомендуется."
  },
  {
    "num": 77,
    "title": "Гибридный классическо-квантовый обмен ключами (X25519 + Kyber-768)",
    "task": "**Hybrid classical+quantum**: Реализуйте hybrid key exchange: X25519 + Kyber-768. Это защитит от атак типа \"harvest now, decrypt later\".",
    "theory": "Для защиты от атак **\"Harvest Now, Decrypt Later\" (HNDL)** ведущие технологические гиганты (Google в Chrome 116+, Cloudflare в TLS 1.3) внедрили гибридный алгоритм **X25519Kyber768Draft00**:\n\n**Принцип двойной безопасности (Dual-PRF Combiner):**\n1. Клиент и сервер одновременно выполняют два независимых обмена ключами:\n   - **Классический**: проверенный десятилетиями эллиптический Диффи-Хеллман $X25519 \\to Secret_{ECC}$.\n   - **Постквантовый**: решеточная инкапсуляция $ML-KEM-768 \\to Secret_{PQC}$.\n2. Итоговый симметричный сессионный ключ вычисляется через функцию HKDF от конкатенации обоих секретов:\n   $SessionKey = HKDF-Extract(Salt, Secret_{ECC} \\parallel Secret_{PQC})$.\n3. **Гарантия безопасности:**\n   - Если в новом решеточном шифре Kyber математики обнаружат скрытую уязвимость, трафик останется защищен стойкостью X25519.\n   - Если появится мощный квантовый компьютер, ломающий X25519, трафик останется защищен постквантовой стойкостью Kyber.\n   - Злоумышленник обязан взломать **оба алгоритма одновременно**!",
    "step_by_step": "1. Сгенерируйте пару X25519 через `crypto/ecdh`.\n2. Смоделируйте генерацию постквантовой пары Kyber-768.\n3. Получите оба разделяемых секрета: `eccSecret` и `pqcSecret`.\n4. Объедините секреты через HKDF-SHA256: `combinedSecret := append(eccSecret, pqcSecret...)`.\n5. Выведите финальный сессионный ключ AES-256-GCM.\n6. Продемонстрируйте, что стороны получили идентичный гибридный ключ.",
    "code_blocks": [
      {
        "filename": "hybrid_pqc.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/ecdh\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n\n\t\"golang.org/x/crypto/hkdf\"\n)\n\n// HybridKeyExchange реализует гибридную схему X25519 + Kyber-768\ntype HybridKeyExchange struct{}\n\nfunc main() {\n\tcurve := ecdh.X25519()\n\n\t// 1. Классический обмен X25519\n\taliceECPriv, _ := curve.GenerateKey(rand.Reader)\n\tbobECPriv, _ := curve.GenerateKey(rand.Reader)\n\n\taliceECSecret, _ := aliceECPriv.ECDH(bobECPriv.PublicKey())\n\tbobECSecret, _ := bobECPriv.ECDH(aliceECPriv.PublicKey())\n\n\t// 2. Постквантовая KEM инкапсуляция Kyber-768 (модель)\n\tpqcSeed := make([]byte, 32)\n\t_, _ = io.ReadFull(rand.Reader, pqcSeed)\n\n\talicePQCSecret := sha256.Sum256(append(pqcSeed, []byte(\"Kyber-Shared-A\")...))\n\tbobPQCSecret := sha256.Sum256(append(pqcSeed, []byte(\"Kyber-Shared-A\")...))\n\n\t// 3. Комбинатор секретов (Dual-PRF Combiner)\n\taliceCombined := append(aliceECSecret, alicePQCSecret[:]...)\n\tbobCombined := append(bobECSecret, bobPQCSecret[:]...)\n\n\tsalt := []byte(\"TLS-1.3-Hybrid-X25519-Kyber768-KeyCombiner\")\n\n\t// Выводим финальный сессионный ключ Алисы\n\taliceHKDF := hkdf.New(sha256.New, aliceCombined, salt, []byte(\"tls13-hybrid-traffic-key\"))\n\taliceSessionKey := make([]byte, 32)\n\t_, _ = io.ReadFull(aliceHKDF, aliceSessionKey)\n\n\t// Выводим финальный сессионный ключ Боба\n\tbobHKDF := hkdf.New(sha256.New, bobCombined, salt, []byte(\"tls13-hybrid-traffic-key\"))\n\tbobSessionKey := make([]byte, 32)\n\t_, _ = io.ReadFull(bobHKDF, bobSessionKey)\n\n\tfmt.Println(\"=== ГИБРИДНЫЙ КЛАССИЧЕСКО-КВАНТОВЫЙ ОБМЕН (X25519 + KYBER-768) ===\")\n\tfmt.Printf(\"X25519 Secret (32B): %x...\\n\", aliceECSecret[:12])\n\tfmt.Printf(\"Kyber Secret  (32B): %x...\\n\", alicePQCSecret[:12])\n\tfmt.Printf(\"\\nИтоговый сессионный ключ Алисы: %x\\n\", aliceSessionKey)\n\tfmt.Printf(\"Итоговый сессионный ключ Боба:   %x\\n\", bobSessionKey)\n\n\tif bytes.Equal(aliceSessionKey, bobSessionKey) {\n\t\tfmt.Println(\"\\nУСПЕХ: Сессия защищена от ретроспективных атак квантовых компьютеров (HNDL)!\")\n\t}\n}\n",
        "note": "Реализация промышленного гибридного комбинатора ключей X25519 + Kyber-768"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run hybrid_pqc.go\n# === ГИБРИДНЫЙ КЛАССИЧЕСКО-КВАНТОВЫЙ ОБМЕН (X25519 + KYBER-768) ===\n# X25519 Secret (32B): 7b19cf4a02...\n# Kyber Secret  (32B): 3a89e10d4f...\n#\n# Итоговый сессионный ключ Алисы: 4f18e9a2c0...\n# Итоговый сессионный ключ Боба:   4f18e9a2c0...\n#\n# УСПЕХ: Сессия защищена от ретроспективных атак квантовых компьютеров (HNDL)!\n"
      }
    ],
    "under_the_hood": "В спецификации TLS 1.3 группа Диффи-Хеллмана `x25519_kyber768` кодируется идентификатором `0x6399`. Клиент отправляет в расширении `KeyShare` одновременно 32 байта публичного ключа X25519 и 1184 байта публичного ключа Kyber-768, гарантируя нулевые сетевые задержки (0-RTT/1-RTT handshake).",
    "pitfalls": "Использование простого сложения по модулю 2 $Secret_{ECC} \\oplus Secret_{PQC}$ вместо криптографического HKDF-комбинатора: это может ослабить энтропию при неидеальном распределении решеточных полиномов.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему вместо чистого Kyber в TLS 1.3 внедряют именно гибридный шифр X25519+Kyber?»\n**Ответ:** Постквантовые решеточные алгоритмы математически молоды по сравнению с RSA и эллиптическими кривыми, которым уже более 40 лет. Существует риск того, что в будущем математики найдут алгебраический способ ускорить решение задачи Learning With Errors на решетках. Гибридный подход гарантирует: уровень безопасности системы будет не ниже самого стойкого из двух алгоритмов."
  },
  {
    "num": 78,
    "title": "Шифрование коротких сообщений публичным ключом получателя через RSA-OAEP",
    "task": "**[Шифрование]**: Зашифруй короткое сообщение публичным ключом получателя (`rsa.EncryptOAEP` с SHA-256).",
    "theory": "Шифрование публичным ключом получателя — классический сценарий асимметричной криптографии:\n1. Отправитель знает публичный ключ адресата (`*rsa.PublicKey`).\n2. Сообщение шифруется функцией `rsa.EncryptOAEP`.\n3. Только законный владелец соответствующего приватного ключа (`*rsa.PrivateKey`) может прочитать расшифрованные данные.\n4. Схема **OAEP** с хэш-функцией SHA-256 обеспечивает:\n   - Скрытность (Confidentiality).\n   - Вероятностность (при каждом шифровании одного и того же текста результат меняется).\n   - Защиту от атак по выбранному шифротексту (IND-CCA2).",
    "step_by_step": "1. Сгенерируйте пару RSA-2048 (получатель).\n2. Задайте открытый контекст `label` (или `nil`).\n3. Зашифруйте строку `\"Sensitive Admin Auth Token\"` через `rsa.EncryptOAEP(sha256.New(), rand.Reader, &privKey.PublicKey, msg, label)`.\n4. Расшифруйте с помощью `rsa.DecryptOAEP`.\n5. Убедитесь в полном совпадении данных.",
    "code_blocks": [
      {
        "filename": "rsa_encrypt_recipient.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"log\"\n)\n\nfunc main() {\n\t// Генерация ключей получателя (Сервер)\n\trecipientPrivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"keygen failed: %v\", err)\n\t}\n\trecipientPubKey := &recipientPrivKey.PublicKey\n\n\t// Сообщение от клиента серверу\n\tshortMessage := []byte(\"One-Time Password (OTP): 948-201-443\")\n\tlabel := []byte(\"2FA-Authentication-Context\")\n\n\t// 1. Клиент шифрует сообщение публичным ключом получателя\n\tciphertext, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, recipientPubKey, shortMessage, label)\n\tif err != nil {\n\t\tlog.Fatalf(\"EncryptOAEP failed: %v\", err)\n\t}\n\n\tfmt.Println(\"=== ШИФРОВАНИЕ ПУБЛИЧНЫМ КЛЮЧОМ ПОЛУЧАТЕЛЯ (RSA-OAEP) ===\")\n\tfmt.Printf(\"Исходный текст:   %s\\n\", string(shortMessage))\n\tfmt.Printf(\"Размер шифротекста: %d байт (2048 бит)\\n\", len(ciphertext))\n\n\t// 2. Сервер расшифровывает сообщение своим приватным ключом\n\tdecrypted, err := rsa.DecryptOAEP(sha256.New(), rand.Reader, recipientPrivKey, ciphertext, label)\n\tif err != nil {\n\t\tlog.Fatalf(\"DecryptOAEP failed: %v\", err)\n\t}\n\n\tfmt.Printf(\"Успешно расшифровано сервером: '%s'\\n\", string(decrypted))\n}\n",
        "note": "Шифрование конфиденциального OTP сообщения публичным ключом через RSA-OAEP"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run rsa_encrypt_recipient.go\n# === ШИФРОВАНИЕ ПУБЛИЧНЫМ КЛЮЧОМ ПОЛУЧАТЕЛЯ (RSA-OAEP) ===\n# Исходный текст:   One-Time Password (OTP): 948-201-443\n# Размер шифротекста: 256 байт (2048 бит)\n# Успешно расшифровано сервером: 'One-Time Password (OTP): 948-201-443'\n"
      }
    ],
    "under_the_hood": "Параметр `label` (контекстная метка) в `rsa.EncryptOAEP` не шифруется, но хешируется в блок `lHash = SHA256(label)`. Если получатель при расшифровке передаст другой `label`, значение `lHash` не совпадет, и операция завершится с ошибкой, привязывая шифротекст к конкретной сессии.",
    "pitfalls": "Шифрование сообщений размером больше 190 байт (для RSA-2048 с SHA-256).",
    "bigtech_interview": "**Вопрос с собеседования:** «Зачем в схеме RSA-OAEP нужен параметр `label` и как он защищает от межпротокольных атак?»\n**Ответ:** Параметр `label` связывает шифротекст с конкретным протокольным контекстом (например, `label=\"password-reset\"` vs `label=\"bank-transfer\"`). Если злоумышленник перехватит зашифрованный токен сброса пароля и попытается скормить его эндпоинту подтверждения банковского перевода, сервер с другим `label` не сможет расшифровать сообщение, предотвращая атаки повторного использования шифротекстов между разными API."
  },
  {
    "num": 79,
    "title": "Эллиптические кривые (ECDSA): генерация ключа P-256 и подпись",
    "task": "**Эллиптические кривые (ECDSA)**: RSA — медленный и ключи у него огромные. Сгенерируй ключ с помощью `crypto/ecdsa` (кривая P-256). Подпиши сообщение. Обрати внимание, насколько подпись и ключ меньше по размеру по сравнению с RSA.",
    "theory": "Асимметричная криптография на основе эллиптических кривых (Elliptic Curve Cryptography, ECC) базируется на сложности задачи дискретного логарифмирования в группе точек эллиптической кривой (ECDLP). \n\nВ отличие от RSA, где стойкость опирается на факторизацию составных чисел $N = p \\cdot q$ (для которой субэкспоненциальный решеточный алгоритм GNFS требует ключи размером от 2048 до 4096 бит), в ECC наилучшие известные атаки (алгоритм $\\rho$-Полларда) имеют экспоненциальную сложность $O(\\sqrt{n})$. В результате:\n1. Ключ **P-256** (256 бит, 32 байта) обеспечивает криптостойкость ~128 бит, что эквивалентно **RSA-3072** (384 байта).\n2. Подпись ECDSA состоит из пары больших целых чисел $(r, s)$ и при ASN.1 DER кодировании занимает всего 70–72 байта (против 256–512 байт у RSA).\n3. Скорость генерации ключей и генерации подписи на порядки превосходит RSA.\n\nВ стандартной библиотеке Go пакет `crypto/ecdsa` предоставляет функцию `ecdsa.GenerateKey(elliptic.P256(), rand.Reader)`. Подписание осуществляется функцией `ecdsa.SignASN1(rand.Reader, privKey, digest[:])`, принимающей 32-байтный хэш (SHA-256) и возвращающей компактный срез байт в формате ASN.1 DER.",
    "step_by_step": "1. Сгенерируйте пару ключей ECDSA с кривой NIST P-256 (`elliptic.P256()`).\n2. Для наглядного сравнения сгенерируйте ключ RSA-2048 и сопоставьте размеры публичных ключей и приватных структур в памяти.\n3. Вычислите криптографический хэш SHA-256 от исходного сообщения.\n4. Создайте цифровую подпись с помощью `ecdsa.SignASN1(rand.Reader, priv, hash[:])`.\n5. Проверьте размер полученной подписи (обычно 70-72 байта) и выведите её шестнадцатеричное представление.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ecdsa\"\n\t\"crypto/elliptic\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"crypto/x509\"\n\t\"fmt\"\n\t\"log\"\n)\n\nfunc main() {\n\t// 1. Генерация ключа ECDSA P-256\n\tecdsaKey, err := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ECDSA ключа: %v\", err)\n\t}\n\n\t// 2. Для сравнения: генерация ключа RSA-2048\n\trsaKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации RSA ключа: %v\", err)\n\t}\n\n\tecdsaPubBytes, _ := x509.MarshalPKIXPublicKey(&ecdsaKey.PublicKey)\n\trsaPubBytes, _ := x509.MarshalPKIXPublicKey(&rsaKey.PublicKey)\n\n\tfmt.Println(\"=== Сравнение размеров ключей ===\")\n\tfmt.Printf(\"ECDSA P-256 Public Key (PKIX DER): %d байт\\n\", len(ecdsaPubBytes))\n\tfmt.Printf(\"RSA-2048 Public Key (PKIX DER):    %d байт\\n\\n\", len(rsaPubBytes))\n\n\t// 3. Подготовка данных и вычисление хэша\n\tmsg := []byte(\"Аудит транзакции #490219: перевод 500_000 RUB утвержден.\")\n\thash := sha256.Sum256(msg)\n\n\t// 4. Подпись сообщения через ecdsa.SignASN1\n\tsig, err := ecdsa.SignASN1(rand.Reader, ecdsaKey, hash[:])\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка формирования подписи ECDSA: %v\", err)\n\t}\n\n\tfmt.Println(\"=== Цифровая подпись ECDSA ===\")\n\tfmt.Printf(\"Размер подписи (ASN.1 DER): %d байт\\n\", len(sig))\n\tfmt.Printf(\"Hex подписи: %x\\n\", sig)\n\n\t// 5. Верификация подписи\n\tvalid := ecdsa.VerifyASN1(&ecdsaKey.PublicKey, hash[:], sig)\n\tfmt.Printf(\"Результат верификации: %v\\n\", valid)\n}",
        "note": "Генерация ключа ECDSA P-256, подписание хэша SHA-256 и сравнение компактности с RSA"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Сравнение размеров ключей ===\n# ECDSA P-256 Public Key (PKIX DER): 91 байт\n# RSA-2048 Public Key (PKIX DER):    294 байт\n# \n# === Цифровая подпись ECDSA ===\n# Размер подписи (ASN.1 DER): 71 байт\n# Hex подписи: 3045022100...\n# Результат верификации: true"
      }
    ],
    "under_the_hood": "Под капотом `ecdsa.SignASN1` выполняет следующие математические операции над эллиптической кривой $y^2 = x^3 - 3x + b \\pmod p$:\n1. Генерируется эфемерный случайный скаляр $k \\in [1, n-1]$ с помощью криптографического ГСЧ `rand.Reader`.\n2. Вычисляется точка $R = k \\cdot G = (x_1, y_1)$, где $G$ — базовая порождающая точка кривой.\n3. Координата $r = x_1 \\pmod n$. Если $r = 0$, выбирается новое $k$.\n4. Вычисляется скаляр $s = k^{-1} (z + r \\cdot d) \\pmod n$, где $z$ — числовое значение хэша сообщения, $d$ — приватный ключ.\n5. Пара $(r, s)$ упаковывается в стандартную ASN.1 SEQUENCE из двух INTEGER.\n\nВ Go 1.20+ реализация P-256 переписана на ассемблере с использованием инструкций постоянного времени (constant-time), полностью предотвращая тайминг-атаки по сторонним каналам (Cache-timing side channels).",
    "pitfalls": "1. **Повторное использование $k$ (Nonce Reuse):** Если для двух разных сообщений подписать хэши с одним и тем же $k$, приватный ключ $d$ тривиально восстанавливается элементарной алгеброй: $d = (s_1 z_2 - s_2 z_1) / (s_2 r - s_1 r) \\pmod n$. Именно эта ошибка привела к взлому консоли Sony PlayStation 3.\n2. **Передача сырого сообщения вместо хэша:** `ecdsa.SignASN1` ожидает дайджест фиксированной длины (хэш), а не произвольный текст. Если передать длинный текст напрямую, он будет урезан до размера порядка кривой.\n3. **Malleability (пластичность) подписи:** Для точки $(r, s)$ пара $(r, -s \\pmod n)$ также является валидной подписью. В финансовых протоколах (Bitcoin, Ethereum) это предотвращается требованием «Low-S» (s <= n/2).",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в мобильных клиентах и IoT-устройствах BigTech-компании переходят с RSA на ECDSA/Ed25519?\n**Ответ:** 1) **Размер трафика и пропускная способность:** Ключ ECDSA P-256 занимает ~91 байт против 294 байт у RSA-2048, а подпись — ~71 байт против 256 байт. При миллионах TLS-хэндшейков или сетевых запросов в секунду это экономит терабайты сетевого трафика. 2) **Энергоэффективность и CPU:** Генерация ключей и подпись на эллиптических кривых требуют значительно меньше процессорных циклов и батареи мобильного устройства, чем возведение в степень 2048-битных чисел RSA."
  },
  {
    "num": 80,
    "title": "Цифровые подписи RSA-PSS: подпись хэша и защита от атак",
    "task": "**Цифровые подписи (RSA-PSS)**: Асимметричные ключи используются не только для шифрования, но и для подписей. Возьми хэш документа, подпиши его приватным ключом через `rsa.SignPSS`. Напиши функцию, которая проверяет подпись публичным ключом.",
    "theory": "Историческая схема подписи RSA PKCS#1 v1.5 является детерминированной при заданном дополнении, что делает её уязвимой к ряду математических атак при плохой реализации парсеров ASN.1 (например, атака Блейхенбахера на валидаторы подписей).\n\nСовременным стандартом IETF (RFC 8017) и NIST для цифровых подписей RSA является **RSA-PSS** (Probabilistic Signature Scheme):\n1. **Вероятностный характер:** Перед вычислением подписи схема добавляет криптографическую случайную соль (`salt`). Каждая новая подпись одного и того же документа выглядит совершенно иначе, предотвращая подбор коллизий.\n2. **Доказуемая криптографическая стойкость:** Стойкость RSA-PSS строго сведена к стойкости задачи RSA в модели случайного оракула (Random Oracle Model).\n3. **Поддержка в Go:** Пакет `crypto/rsa` предоставляет функции `rsa.SignPSS` и `rsa.VerifyPSS`, принимающие структуру `rsa.PSSOptions`. Рекомендуется использовать опцию `SaltLength: rsa.PSSSaltLengthAuto` или `rsa.PSSSaltLengthEqualsHash`.",
    "step_by_step": "1. Сгенерируйте пару ключей RSA разрядностью 2048 бит.\n2. Сформируйте контракт/документ и вычислите его криптографический хэш SHA-256.\n3. Настройте `rsa.PSSOptions` с использованием хэш-функции `crypto.SHA256` и автоматической длиной соли.\n4. Подпишите дайджест с помощью `rsa.SignPSS(rand.Reader, privKey, crypto.SHA256, digest, &opts)`.\n5. Реализуйте функцию `verifyDocumentPSS`, которая вызывает `rsa.VerifyPSS` и возвращает `nil` при успехе или ошибку при компрометации данных.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"errors\"\n\t\"fmt\"\n\t\"log\"\n)\n\n// verifyDocumentPSS проверяет цифровую подпись документа с использованием схемы RSA-PSS\nfunc verifyDocumentPSS(pubKey *rsa.PublicKey, doc []byte, signature []byte) error {\n\tdigest := sha256.Sum256(doc)\n\topts := &rsa.PSSOptions{\n\t\tSaltLength: rsa.PSSSaltLengthAuto,\n\t\tHash:       crypto.SHA256,\n\t}\n\n\terr := rsa.VerifyPSS(pubKey, crypto.SHA256, digest[:], signature, opts)\n\tif err != nil {\n\t\treturn fmt.Errorf(\"подпись недействительна: %w\", err)\n\t}\n\treturn nil\n}\n\nfunc main() {\n\t// 1. Генерация ключей RSA\n\tprivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ключа RSA: %v\", err)\n\t}\n\n\t// 2. Исходный документ\n\tcontract := []byte(\"Договор №771-K: Лицензирование API Gateway на 12 месяцев. Сумма: 1_200_000 RUB.\")\n\tdigest := sha256.Sum256(contract)\n\n\t// 3. Формирование подписи RSA-PSS\n\topts := &rsa.PSSOptions{\n\t\tSaltLength: rsa.PSSSaltLengthAuto,\n\t\tHash:       crypto.SHA256,\n\t}\n\n\tsig, err := rsa.SignPSS(rand.Reader, privKey, crypto.SHA256, digest[:], opts)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подписания RSA-PSS: %v\", err)\n\t}\n\n\tfmt.Printf(\"Успешно сформирована подпись RSA-PSS длиной %d байт\\n\", len(sig))\n\n\t// 4. Успешная верификация оригинального документа\n\terr = verifyDocumentPSS(&privKey.PublicKey, contract, sig)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка верификации: %v\", err)\n\t}\n\tfmt.Println(\"Верификация оригинального документа: ПОДТВЕРЖДЕНА\")\n\n\t// 5. Попытка подделки документа\n\ttamperedContract := []byte(\"Договор №771-K: Лицензирование API Gateway на 12 месяцев. Сумма: 100_000 RUB.\")\n\terr = verifyDocumentPSS(&privKey.PublicKey, tamperedContract, sig)\n\tif errors.Is(err, rsa.ErrVerification) || err != nil {\n\t\tfmt.Printf(\"Верификация модифицированного документа: ОТКЛОНЕНА (%v)\\n\", err)\n\t}\n}",
        "note": "Формирование и проверка цифровой подписи RSA-PSS с автоматическим вычислением длины соли"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Успешно сформирована подпись RSA-PSS длиной 256 байт\n# Верификация оригинального документа: ПОДТВЕРЖДЕНА\n# Верификация модифицированного документа: ОТКЛОНЕНА (подпись недействительна: crypto/rsa: verification error)"
      }
    ],
    "under_the_hood": "В алгоритме RSA-PSS:\n1. Хэш сообщения $m$ дополняется солью $salt$ случайной длины (обычно равной длине хэша, 32 байта для SHA-256): $M' = 00\\ 00\\ 00\\ 00\\ 00\\ 00\\ 00\\ 00 \\parallel mHash \\parallel salt$.\n2. Вычисляется $H = \\text{Hash}(M')$.\n3. Строится маскирующая строка $DB = PS \\parallel 01 \\parallel salt$ и шифруется с помощью функции маскирования MGF1 (Mask Generation Function на базе SHA-256): $maskedDB = DB \\oplus \\text{MGF1}(H, emLen - hLen - 1)$.\n4. Сформированный блок $EM = maskedDB \\parallel H \\parallel bc_{16}$ возводится в приватную степень $d \\pmod N$: $S = EM^d \\pmod N$.\n\nПри верификации публичный ключ возводит $S$ в степень $e \\pmod N$, восстанавливает $EM$, снимает маску MGF1, извлекает соль и сверяет дайджест $H$ в постоянном времени.",
    "pitfalls": "1. **Использование PKCS#1 v1.5 вместо PSS:** В новых системах следует строго избегать устаревшего `rsa.SignPKCS1v15`, так как стандарты FIPS 186-5 и BSI запрещают его использование для новых подписей.\n2. **Несоответствие хэша в SignPSS и VerifyPSS:** Если хэш был вычислен через SHA-256, но в `rsa.VerifyPSS` передан идентификатор `crypto.SHA512`, верификация завершится с `rsa.ErrVerification`.\n3. **Фиксированная нулевая соль:** Использование `SaltLength: 0` лишает схему PSS вероятностного свойства и делает её детерминированной, снижая стойкость к атакам по сторонним каналам.",
    "bigtech_interview": "**Вопрос с собеседования:** В чем разница между `rsa.SignPKCS1v15` и `rsa.SignPSS`, и почему регуляторы требуют переход на PSS?\n**Ответ:** PKCS#1 v1.5 — детерминированная схема с жестким детерминированным паддингом. Ошибки парсинга ASN.1 структуры дайджеста в PKCS#1 v1.5 исторически приводили к атакам Блейхенбахера (Bleichenbacher signature forgery), позволявшим подделывать подписи при малом открытом экспоненте $e=3$. RSA-PSS использует криптографическую соль и функцию маскирования MGF1, делая каждую подпись случайной и устраняя алгебраическую структуру дополнения. Стойкость PSS математически доказана в модели случайного оракула."
  },
  {
    "num": 81,
    "title": "Расшифрование сообщения приватным ключом (rsa.DecryptOAEP)",
    "task": "**Расшифрование сообщения приватным ключом (rsa.DecryptOAEP)**: Расшифруй сообщение приватным ключом.",
    "theory": "Расшифрование асимметричных шифротекстов RSA требует строгого применения схемы дополнения OAEP (Optimal Asymmetric Encryption Padding) в соответствии с PKCS#1 v2.2.\n\nФункция `rsa.DecryptOAEP` в Go принимает:\n- `hash`: хэш-функцию, использованную при шифровании (например, `sha256.New()`).\n- `random`: криптографический генератор случайных чисел (`rand.Reader`). Важно: в отличие от симметричного расшифрования, где ГСЧ не нужен, в RSA расшифрование требует источника энтропии для механизма **RSA Blinding** (ослепления), защищающего от атак по времени на закрытый ключ.\n- `priv`: приватный ключ `*rsa.PrivateKey`.\n- `ciphertext`: зашифрованный массив байт (ровно 256 байт для ключа 2048 бит).\n- `label`: опциональная метка контекста (должна в точности совпадать с меткой, переданной при шифровании).",
    "step_by_step": "1. Сгенерируйте пару ключей RSA 2048 бит.\n2. Зашифруйте тестовое конфиденциальное сообщение с помощью `rsa.EncryptOAEP` с контекстной меткой `[]byte(\"financial-report\")`.\n3. Реализуйте безопасную функцию расшифрования `decryptSecretOAEP`, вызывающую `rsa.DecryptOAEP`.\n4. Продемонстрируйте корректное восстановление открытого текста.\n5. Продемонстрируйте защиту: при передаче неверной метки контекста расшифрование отклоняется ошибкой `ErrDecryption`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"log\"\n)\n\n// decryptSecretOAEP расшифровывает сообщение с защитой от тайминг-атак\nfunc decryptSecretOAEP(privKey *rsa.PrivateKey, ciphertext []byte, label []byte) ([]byte, error) {\n\t// rand.Reader обязателен для работы механизма RSA Blinding!\n\tplaintext, err := rsa.DecryptOAEP(sha256.New(), rand.Reader, privKey, ciphertext, label)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка расшифрования RSA-OAEP: %w\", err)\n\t}\n\treturn plaintext, nil\n}\n\nfunc main() {\n\t// 1. Генерация ключа RSA\n\tprivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ключа RSA: %v\", err)\n\t}\n\n\tlabel := []byte(\"confidential:payroll:2026\")\n\tsecretData := []byte(\"Зарплатная ведомость: топ-менеджмент, фонд 42.5M\")\n\n\t// 2. Шифрование открытым ключом\n\tciphertext, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, &privKey.PublicKey, secretData, label)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка шифрования: %v\", err)\n\t}\n\tfmt.Printf(\"Шифротекст успешно сформирован (%d байт)\\n\", len(ciphertext))\n\n\t// 3. Успешное расшифрование\n\tdecrypted, err := decryptSecretOAEP(privKey, ciphertext, label)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка расшифрования: %v\", err)\n\t}\n\tfmt.Printf(\"Расшифрованный текст: %s\\n\", string(decrypted))\n\n\t// 4. Попытка расшифрования с неверным label\n\twrongLabel := []byte(\"confidential:public:2026\")\n\t_, err = decryptSecretOAEP(privKey, ciphertext, wrongLabel)\n\tif err != nil {\n\t\tfmt.Printf(\"Ожидаемый отказ при неверном label: %v\\n\", err)\n\t}\n}",
        "note": "Безопасное расшифрование RSA-OAEP с применением rand.Reader для RSA Blinding"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Шифротекст успешно сформирован (256 байт)\n# Расшифрованный текст: Зарплатная ведомость: топ-менеджмент, фонд 42.5M\n# Ожидаемый отказ при неверном label: ошибка расшифрования RSA-OAEP: crypto/rsa: decryption error"
      }
    ],
    "under_the_hood": "Зачем функции `rsa.DecryptOAEP` параметр `io.Reader` (энтропия)?\nПри вычислении $c^d \\pmod N$ время выполнения операции модульного возведения в степень сильно зависит от битов закрытого ключа $d$ (алгоритм Montgomery ladder или квадрат-умножение). Чтобы злоумышленник по задержкам ответов сервера (атака Kocher timing attack) не вычислил $d$, в Go встроен механизм **RSA Blinding**:\n1. Выбирается случайное число $r$, взаимно простое с $N$.\n2. Шифротекст умножается на $r^e \\pmod N$: $c' = c \\cdot r^e \\pmod N$.\n3. Вычисляется модульное возведение в степень над «ослепленным» шифротекстом: $m' = (c')^d = (c \\cdot r^e)^d = c^d \\cdot r^{e \\cdot d} = m \\cdot r \\pmod N$.\n4. Результат умножается на модульный обратный элемент $r^{-1} \\pmod N$: $m = m' \\cdot r^{-1} \\pmod N$.\nБлагодаря этому рантайм Go математически изолирует замеры времени от значений битов ключа $d$.",
    "pitfalls": "1. **Передача nil вместо io.Reader:** Передача `nil` в `rsa.DecryptOAEP` в современных версиях Go вызовет панику или отключит ослепление, открывая сервис для side-channel атак по времени в облачной инфраструктуре.\n2. **Утечка детализированных ошибок расшифрования (Oracle Attack):** Сервер ни в коем случае не должен возвращать клиенту различия между «неверный паддинг» и «неверная метка». `rsa.DecryptOAEP` возвращает универсальную ошибку `ErrDecryption` в постоянном времени, и API должно транслировать её как generic HTTP 400/401.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое RSA Blinding и почему `rsa.DecryptOAEP` требует источник случайных чисел (`rand.Reader`), хотя симметричные шифры при расшифровании энтропии не требуют?\n**Ответ:** Модульное возведение в степень $c^d \\pmod N$ уязвимо к атакам по времени (Timing attacks): время умножений в аппаратном процессоре коррелирует с числом единичных битов в секретной экспоненте $d$. RSA Blinding домножает шифротекст на случайный фактор $r^e$, производит вычисления над рандомизированным числом и в конце делит на $r$. Это полностью маскирует время работы алгоритма от злоумышленника, но требует криптографического ГСЧ `rand.Reader` для генерации числа $r$."
  },
  {
    "num": 82,
    "title": "Современные цифровые подписи Ed25519 (RFC 8032)",
    "task": "**Ed25519 (Современные подписи)**: Используй пакет `crypto/ed25519`. Это современный, сверхбыстрый и безопасный стандарт цифровых подписей (используется в блокчейнах и современных TLS). Реализуй генерацию, подпись и верификацию. Убедись, что API этого пакета намного проще, чем у ECDSA.",
    "theory": "Схема цифровой подписи **Ed25519** (Edwards-curve Digital Signature Algorithm на кривой Curve25519, RFC 8032) разработана Дэниелом Бернштейном (djb) и сегодня является золотым стандартом современной криптографии (SSH, TLS 1.3, Signal, WireGuard, Git commit signing).\n\nКлючевые преимущества Ed25519 над ECDSA и RSA:\n1. **Детерминированность:** В отличие от ECDSA, где случайное число $k$ берется из ГСЧ (и при сбое ГСЧ приватный ключ мгновенно утекает), в Ed25519 эфемерный ключ вычисляется как хэш SHA-512 от приватного ключа и сообщения ($k = \\text{SHA512}(sk_{hash} \\parallel m)$).\n2. **Фиксированные размеры:** Публичный ключ — ровно 32 байта, приватный ключ (seed + public) — 64 байта, подпись — ровно 64 байта. Никакого громоздкого ASN.1 DER кодирования!\n3. **Защита от тайминг-атак:** Все операции выполняются строго за константное время без условных ветвлений по секретным данным.\n4. **Простота API в Go:** Пакет `crypto/ed25519` не требует передачи параметров хэширования, ASN.1 оберток или сложных структур.",
    "step_by_step": "1. Сгенерируйте пару ключей с помощью `ed25519.GenerateKey(rand.Reader)`.\n2. Исследуйте типы и размеры ключей: `ed25519.PublicKey` (32 байта) и `ed25519.PrivateKey` (64 байта).\n3. Подпишите сырое сообщение функцией `ed25519.Sign(privKey, message)`.\n4. Обратите внимание, что подпись занимает ровно 64 байта.\n5. Проверьте валидность подписи с помощью `ed25519.Verify(pubKey, message, signature)`.\n6. Проверьте устойчивость: измените один байт в сообщении и убедитесь, что верификация возвращает `false`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ed25519\"\n\t\"crypto/rand\"\n\t\"fmt\"\n\t\"log\"\n)\n\nfunc main() {\n\t// 1. Генерация ключей Ed25519\n\tpubKey, privKey, err := ed25519.GenerateKey(rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации Ed25519: %v\", err)\n\t}\n\n\tfmt.Println(\"=== Характеристики ключей Ed25519 ===\")\n\tfmt.Printf(\"Размер публичного ключа:  %d байт (hex: %x)\\n\", len(pubKey), pubKey)\n\tfmt.Printf(\"Размер приватного ключа:  %d байт (seed + pub)\\n\", len(privKey))\n\n\t// 2. Подписание сообщения\n\tpayload := []byte(\"deploy:commit=7f4a21e:env=production:author=engineer\")\n\tsignature := ed25519.Sign(privKey, payload)\n\n\tfmt.Println(\"\\n=== Цифровая подпись Ed25519 ===\")\n\tfmt.Printf(\"Размер подписи:           %d байт (строго фиксирован)\\n\", len(signature))\n\tfmt.Printf(\"Hex подписи:              %x\\n\", signature)\n\n\t// 3. Верификация оригинального сообщения\n\tisValid := ed25519.Verify(pubKey, payload, signature)\n\tfmt.Printf(\"\\nВерификация подлинного сообщения: %v\\n\", isValid)\n\n\t// 4. Попытка верификации скомпрометированного сообщения\n\tcorrupted := []byte(\"deploy:commit=7f4a21e:env=production:author=attacker\")\n\tisCorruptedValid := ed25519.Verify(pubKey, corrupted, signature)\n\tfmt.Printf(\"Верификация поддельного сообщения: %v\\n\", isCorruptedValid)\n}",
        "note": "Использование ed25519.GenerateKey, ed25519.Sign и ed25519.Verify без ASN.1 оверхеда"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Характеристики ключей Ed25519 ===\n# Размер публичного ключа:  32 байт (hex: 8b6c5e...)\n# Размер приватного ключа:  64 байт (seed + pub)\n# \n# === Цифровая подпись Ed25519 ===\n# Размер подписи:           64 байт (строго фиксирован)\n# Hex подписи:              a3f812...\n# \n# Верификация подлинного сообщения: true\n# Верификация поддельного сообщения: false"
      }
    ],
    "under_the_hood": "Под капотом Ed25519 использует искривленную кривую Эдвардса $-x^2 + y^2 = 1 - \\frac{121665}{121666} x^2 y^2$ над полем $\\mathbb{F}_{2^{255}-19}$.\n1. Сложение точек на кривой Эдвардса является *полным* (complete addition formulas): не существует исключительных точек (точки в бесконечности, дублирования), что устраняет необходимость проверок на особые случаи и исключает branch-based timing attacks.\n2. Приватный ключ `ed25519.PrivateKey` в Go представляет собой срез из 64 байт: первые 32 байта — это криптографический сид (seed), а вторые 32 байта — кэшированный публичный ключ (для избежания дорогостоящего повторного вычисления точки при каждом вызове `ed25519.Sign`).\n3. При вычислении подписи $R$ и $S$: $S = (r + H(R \\parallel A \\parallel M) \\cdot a) \\pmod L$, где все операции проводятся по модулю простого порядка подгруппы $L = 2^{252} + 27742317777372353535851937790883648493$.",
    "pitfalls": "1. **Путаница между Seed (32 байта) и PrivateKey (64 байта):** Если сохранить в Vault или БД только 32 байта сида, для восстановления полного ключа в Go необходимо использовать `ed25519.NewKeyFromSeed(seed)`.\n2. **Передача предварительного хэша вместо исходных данных:** `ed25519.Sign` сам хэширует сообщение внутри алгоритма. Если передать `sha256.Sum256(msg)`, вы подпишете 32 байта хэша, что ухудшит интероперабельность с другими системами. Для внешнего хэширования существует отдельный подстандарт Ed25519ph (`ed25519.Options{Hash: crypto.SHA512}`).",
    "bigtech_interview": "**Вопрос с собеседования:** Почему Ed25519 считается значительно безопаснее ECDSA при развертывании в облачной инфраструктуре?\n**Ответ:** 1) **Детерминированный nonce:** В ECDSA генерация случайного $k$ зависит от качества системного ГСЧ (падение энтропии в контейнере или повтор $k$ мгновенно компрометирует закрытый ключ). В Ed25519 случайное число детерминированно синтезируется через SHA-512 от закрытого ключа и сообщения. 2) **Отсутствие уязвимостей парсинга:** Подпись Ed25519 имеет строго фиксированный размер 64 байта, что исключает атаки на ASN.1 парсеры и проблемы пластичности подписи (signature malleability). 3) **Constant-time математика:** Полные формулы сложения точек Эдвардса не содержат условий ветвления."
  },
  {
    "num": 83,
    "title": "Гибридное шифрование (Envelope Encryption): защита больших файлов",
    "task": "**Гибридное шифрование (Конверт)**: Тебе нужно зашифровать видеофайл (1 ГБ) для другого пользователя. RSA не умеет шифровать большие данные. Напиши алгоритм: 1) Генерируешь случайный ключ AES-256; 2) Шифруешь видео через AES-GCM; 3) Шифруешь сам ключ AES с помощью RSA публичного ключа пользователя.",
    "theory": "Асимметричная криптография (RSA, ECIES) обладает фундаментальным математическим ограничением: размер шифруемого открытого текста не может превышать размер ключа за вычетом длины паддинга (для RSA-2048 с SHA-256 OAEP лимит составляет всего 190 байт). Кроме того, RSA в тысячи раз медленнее симметричных шифров.\n\nДля безопасной передачи больших файлов (видео, резервные копии, документы) применяется паттерн **Envelope Encryption (конвертное / гибридное шифрование)**, являющийся фундаментом AWS KMS, Google Cloud KMS и протокола PGP:\n1. **DEK (Data Encryption Key):** Для каждого файла генерируется одноразовый симметричный ключ (например, AES-256-GCM, 32 байта).\n2. **Шифрование данных:** Файл любого размера шифруется на ключе DEK с высокой скоростью (AES-NI аппаратное ускорение до 4–10 ГБ/с).\n3. **Шифрование ключа (KEK - Key Encryption Key):** Сам DEK (32 байта) шифруется открытым ключом получателя (RSA-2048 OAEP).\n4. **Криптоконверт:** Получателю передается структура, содержащая: `EncryptedDEK` (256 байт) + `Nonce` (12 байт) + `Ciphertext` (зашифрованный файл).",
    "step_by_step": "1. Создайте структуру `Envelope`, объединяющую зашифрованный DEK, nonce и шифротекст данных.\n2. В функции `SealEnvelope`:\n   - Сгенерируйте случайный 32-байтный DEK (`crypto/rand`).\n   - Зашифруйте данные с помощью `cipher.NewGCM` на ключе DEK.\n   - Зашифруйте DEK публичным ключом RSA получателя через `rsa.EncryptOAEP`.\n3. В функции `OpenEnvelope`:\n   - Расшифруйте DEK приватным ключом RSA через `rsa.DecryptOAEP`.\n   - Инициализируйте AES-GCM с расшифрованным DEK и восстановите исходные данные.\n4. Протестируйте полный цикл шифрования и расшифрования бинарного пейлоада.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/aes\"\n\t\"crypto/cipher\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n\t\"log\"\n)\n\n// CryptoEnvelope содержит компоненты гибридного криптоконверта\ntype CryptoEnvelope struct {\n\tEncryptedDEK []byte // AES ключ, зашифрованный открытым ключом RSA (256 байт)\n\tNonce        []byte // Вектор инициализации AES-GCM (12 байт)\n\tCiphertext   []byte // Зашифрованное видео / пейлоад с тегом аутентификации\n}\n\n// SealEnvelope выполняет гибридное шифрование большого пейлоада\nfunc SealEnvelope(pubKey *rsa.PublicKey, data []byte) (*CryptoEnvelope, error) {\n\t// 1. Генерация случайного одноразового ключа AES-256 (DEK)\n\tdek := make([]byte, 32)\n\tif _, err := io.ReadFull(rand.Reader, dek); err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка генерации DEK: %w\", err)\n\t}\n\n\t// 2. Симметричное шифрование данных через AES-GCM\n\tblock, err := aes.NewCipher(dek)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tgcm, err := cipher.NewGCM(block)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\n\tnonce := make([]byte, gcm.NonceSize())\n\tif _, err := io.ReadFull(rand.Reader, nonce); err != nil {\n\t\treturn nil, err\n\t}\n\n\tciphertext := gcm.Seal(nil, nonce, data, nil)\n\n\t// 3. Асимметричное шифрование DEK открытым ключом RSA\n\tencryptedDEK, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, pubKey, dek, []byte(\"envelope:dek\"))\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка шифрования DEK: %w\", err)\n\t}\n\n\treturn &CryptoEnvelope{\n\t\tEncryptedDEK: encryptedDEK,\n\t\tNonce:        nonce,\n\t\tCiphertext:   ciphertext,\n\t}, nil\n}\n\n// OpenEnvelope расшифровывает криптоконверт приватным ключом получателя\nfunc OpenEnvelope(privKey *rsa.PrivateKey, env *CryptoEnvelope) ([]byte, error) {\n\t// 1. Расшифрование DEK приватным ключом RSA\n\tdek, err := rsa.DecryptOAEP(sha256.New(), rand.Reader, privKey, env.EncryptedDEK, []byte(\"envelope:dek\"))\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"отказ расшифрования DEK: %w\", err)\n\t}\n\n\t// 2. Расшифрование данных через AES-GCM\n\tblock, err := aes.NewCipher(dek)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tgcm, err := cipher.NewGCM(block)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\n\tplaintext, err := gcm.Open(nil, env.Nonce, env.Ciphertext, nil)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка аутентификации данных: %w\", err)\n\t}\n\n\treturn plaintext, nil\n}\n\nfunc main() {\n\t// 1. Генерация ключей пользователя-получателя (RSA-2048)\n\tuserPrivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ключей: %v\", err)\n\t}\n\n\t// 2. Имитация видеофайла (бинарные данные)\n\tsimulatedVideo := bytes.Repeat([]byte(\"MP4_VIDEO_STREAM_CHUNK_DATA_\"), 1000)\n\tfmt.Printf(\"Исходный размер видеофайла: %d байт\\n\", len(simulatedVideo))\n\n\t// 3. Упаковка в криптоконверт\n\tenv, err := SealEnvelope(&userPrivKey.PublicKey, simulatedVideo)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка запечатывания конверта: %v\", err)\n\t}\n\n\tfmt.Println(\"\\n=== Структура сформированного конверта ===\")\n\tfmt.Printf(\"Размер зашифрованного ключа DEK: %d байт\\n\", len(env.EncryptedDEK))\n\tfmt.Printf(\"Размер Nonce AES-GCM:            %d байт\\n\", len(env.Nonce))\n\tfmt.Printf(\"Размер зашифрованного тела:      %d байт\\n\", len(env.Ciphertext))\n\n\t// 4. Расшифрование на стороне получателя\n\trecoveredVideo, err := OpenEnvelope(userPrivKey, env)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка открытия конверта: %v\", err)\n\t}\n\n\tif bytes.Equal(recoveredVideo, simulatedVideo) {\n\t\tfmt.Println(\"\\nВидеофайл успешно расшифрован и целостность подтверждена!\")\n\t}\n}",
        "note": "Паттерн Envelope Encryption: объединение скорости AES-256-GCM и асимметричной доставки RSA-OAEP"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Исходный размер видеофайла: 28000 байт\n# \n# === Структура сформированного конверта ===\n# Размер зашифрованного ключа DEK: 256 байт\n# Размер Nonce AES-GCM:            12 байт\n# Размер зашифрованного тела:      28016 байт\n# \n# Видеофайл успешно расшифрован и целостность подтверждена!"
      }
    ],
    "under_the_hood": "Почему Envelope Encryption является стандартом архитектуры Enterprise безопасности?\n1. **Разделение обязанностей (SoD):** Мастер-ключ (KEK) никогда не покидает защищенный криптографический модуль (HSM / KMS). Приложению передается лишь зашифрованный DEK, который расшифровывается в HSM за один вызов API.\n2. **Локальная производительность:** Шифрование петабайт данных происходит локально в оперативной памяти инстанса с аппаратными инструкциями процессора AES-NI (не требуя пересылки терабайт данных по сети в KMS).\n3. **Ротация ключей:** При компрометации или плановой ротации мастер-ключа KEK нет необходимости перешифровывать петабайты исходных файлов — достаточно перешифровать только 32-байтные зашифрованные DEK в метаданных файлов.",
    "pitfalls": "1. **Неочищенный DEK в памяти:** После использования DEK должен зануляться (`clear(dek)` или `subtle.ConstantTimeCopy`), чтобы исключить его извлечение из дампа памяти или Core dump.\n2. **Отсутствие аутентификации метаданных:** Если метаданные файла (имя файла, владелец) не включены в `Additional Data` вызова `gcm.Seal(nil, nonce, data, metadata)`, злоумышленник может подменить связку конверта с чужим файлом.",
    "bigtech_interview": "**Вопрос с собеседования:** Как устроен сервис Envelope Encryption в облачных провайдерах (AWS KMS / Google Cloud KMS) и почему он решает проблему масштабируемости шифрования данных?\n**Ответ:** Если бы KMS шифровал сами данные, клиентам приходилось бы передавать терабайты видео и баз данных через RPC API в KMS, создавая гигантское узкое место в сети и на HSM. В Envelope Encryption клиент вызывает KMS метод `GenerateDataKey`, получая пару: `PlaintextKey` и `CiphertextKey`. Клиент локально шифрует данные через AES-GCM с помощью `PlaintextKey`, тут же уничтожает его в памяти, а `CiphertextKey` сохраняет рядом с файлом. Нагрузка на KMS сводится к микросекундным операциям с 32-байтными ключами."
  },
  {
    "num": 84,
    "title": "Верификация цифровой подписи публичным ключом (rsa.VerifyPSS)",
    "task": "**Верификация цифровой подписи публичным ключом (rsa.VerifyPSS)**: Проверь подпись публичным ключом (`rsa.VerifyPSS`).",
    "theory": "Проверка цифровой подписи публичным ключом — критически важная процедура аутентификации источника и контроля целостности данных в распределенных системах (проверка подлинности вебхуков, обновлений прошивок, JWT токенов).\n\nВ схеме RSA-PSS верификатор обладает только открытым ключом `*rsa.PublicKey` и выполняет следующие действия:\n1. Вычисляет криптографический хэш проверяемого сообщения с тем же алгоритмом, который использовался при подписании (например, `sha256.Sum256`).\n2. Вызывает функцию `rsa.VerifyPSS(pubKey, crypto.SHA256, digest, signature, opts)`.\n3. Функция `rsa.VerifyPSS` возвращает `nil` при успешной верификации. Если подпись искажена, сообщение изменено или использован чужой публичный ключ, возвращается ошибка `rsa.ErrVerification`.\n4. В коде верификатора критически важно использовать `errors.Is(err, rsa.ErrVerification)` для однозначной диагностики фальсификации.",
    "step_by_step": "1. Сгенерируйте пару ключей RSA и подпишите хэш транзакции с помощью `rsa.SignPSS`.\n2. Извлеките открытый ключ `&privKey.PublicKey` для имитации независимой проверяющей стороны.\n3. Вызовите `rsa.VerifyPSS` для подтверждения подлинности оригинального сообщения.\n4. Протестируйте три сценария сбоя проверки:\n   - Подмена хотя бы одного байта в теле сообщения.\n   - Повреждение одного байта в срезе цифровой подписи.\n   - Проверка подписи с использованием другого валидного открытого ключа RSA.\n5. Убедитесь, что во всех трех случаях возвращается ошибка `rsa.ErrVerification`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"errors\"\n\t\"fmt\"\n\t\"log\"\n)\n\nfunc main() {\n\t// 1. Сторона отправителя: генерация ключей и подпись\n\tsenderPrivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ключа: %v\", err)\n\t}\n\n\tpayload := []byte(\"ACTION=TRANSFER&FROM=USER_101&TO=USER_909&AMOUNT=45000\")\n\tdigest := sha256.Sum256(payload)\n\n\tpssOpts := &rsa.PSSOptions{\n\t\tSaltLength: rsa.PSSSaltLengthAuto,\n\t\tHash:       crypto.SHA256,\n\t}\n\n\tsignature, err := rsa.SignPSS(rand.Reader, senderPrivKey, crypto.SHA256, digest[:], pssOpts)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка создания подписи: %v\", err)\n\t}\n\n\t// 2. Сторона получателя: проверка открытым ключом отправителя\n\tpubKey := &senderPrivKey.PublicKey\n\n\tfmt.Println(\"=== Сценарий 1: Проверка легитимного сообщения ===\")\n\terr = rsa.VerifyPSS(pubKey, crypto.SHA256, digest[:], signature, pssOpts)\n\tif err == nil {\n\t\tfmt.Println(\"Результат: ПОДПИСЬ ВАЛИДНА (Целостность и авторство гарантированы)\")\n\t}\n\n\tfmt.Println(\"\\n=== Сценарий 2: Подмена данных (Tampering) ===\")\n\ttamperedPayload := []byte(\"ACTION=TRANSFER&FROM=USER_101&TO=USER_909&AMOUNT=99000\")\n\ttamperedDigest := sha256.Sum256(tamperedPayload)\n\terr = rsa.VerifyPSS(pubKey, crypto.SHA256, tamperedDigest[:], signature, pssOpts)\n\tif errors.Is(err, rsa.ErrVerification) {\n\t\tfmt.Println(\"Результат: ОТКЛОНЕНО (Обнаружена подмена исходного текста)\")\n\t}\n\n\tfmt.Println(\"\\n=== Сценарий 3: Повреждение байтов самой подписи ===\")\n\tcorruptedSig := make([]byte, len(signature))\n\tcopy(corruptedSig, signature)\n\tcorruptedSig[10] ^= 0xFF // инвертируем биты одного байта\n\terr = rsa.VerifyPSS(pubKey, crypto.SHA256, digest[:], corruptedSig, pssOpts)\n\tif errors.Is(err, rsa.ErrVerification) {\n\t\tfmt.Println(\"Результат: ОТКЛОНЕНО (Подпись повреждена или сфальсифицирована)\")\n\t}\n\n\tfmt.Println(\"\\n=== Сценарий 4: Проверка чужим открытым ключом ===\")\n\tanotherKey, _ := rsa.GenerateKey(rand.Reader, 2048)\n\terr = rsa.VerifyPSS(&anotherKey.PublicKey, crypto.SHA256, digest[:], signature, pssOpts)\n\tif errors.Is(err, rsa.ErrVerification) {\n\t\tfmt.Println(\"Результат: ОТКЛОНЕНО (Подпись не соответствует данному открытому ключу)\")\n\t}\n}",
        "note": "Тщательная валидация RSA-PSS открытым ключом во всех сценариях компрометации"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Сценарий 1: Проверка легитимного сообщения ===\n# Результат: ПОДПИСЬ ВАЛИДНА (Целостность и авторство гарантированы)\n# \n# === Сценарий 2: Подмена данных (Tampering) ===\n# Результат: ОТКЛОНЕНО (Обнаружена подмена исходного текста)\n# \n# === Сценарий 3: Повреждение байтов самой подписи ===\n# Результат: ОТКЛОНЕНО (Подпись повреждена или сфальсифицирована)\n# \n# === Сценарий 4: Проверка чужим открытым ключом ===\n# Результат: ОТКЛОНЕНО (Подпись не соответствует данному открытому ключу)"
      }
    ],
    "under_the_hood": "Что происходит внутри `rsa.VerifyPSS`:\n1. Проверяется соответствие длины среза подписи размеру модуля $N$ (для 2048-битного ключа длина должна быть ровно 256 байт).\n2. Подпись интерпретируется как большое целое число $S$ и возводится в степень публичной экспоненты: $EM = S^e \\pmod N$.\n3. Извлекается закодированное сообщение $EM$ (Encoded Message). Проверяется младший байт (он обязан равняться `0xbc`).\n4. Из $EM$ извлекаются замаскированный блок $maskedDB$ и контрольный хэш $H$.\n5. Вычисляется $dbMask = \\text{MGF1}(H, \\text{len}(maskedDB))$ и восстанавливается $DB = maskedDB \\oplus dbMask$.\n6. Проверяются старшие биты и разделитель `0x01`, после чего извлекается соль $salt$.\n7. Заново вычисляется $H' = \\text{Hash}(00\\ 00\\ 00\\ 00\\ 00\\ 00\\ 00\\ 00 \\parallel mHash \\parallel salt)$.\n8. Значения $H$ и $H'$ сравниваются через `subtle.ConstantTimeCompare`.",
    "pitfalls": "1. **Несовпадение параметров SaltLength:** Если подпись сформирована с `SaltLength: rsa.PSSSaltLengthEqualsHash` (32 байта для SHA-256), а верификатор ожидает фиксированную длину соли другой размерности, верификация вернет ошибку. Рекомендуется использовать `rsa.PSSSaltLengthAuto`.\n2. **Игнорирование ошибок:** Ошибочное предположение, что любая ошибка означает сетевой сбой. Ошибка `rsa.ErrVerification` свидетельствует о попытке атаки или повреждении данных.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему цифровая подпись гарантирует неотказуемость (non-repudiation), а HMAC — нет?\n**Ответ:** HMAC использует симметричный общий секрет (shared secret), известный как отправителю, так и получателю. Получатель, зная секрет, может сам сгенерировать любой валидный HMAC, поэтому в суде или арбитраже он не сможет доказать третьей стороне, что сообщение пришло именно от отправителя. В цифровой подписи закрытый ключ есть исключительно у отправителя, а у всех остальных — только открытый ключ, позволяющий только проверять, но не создавать подпись. Это обеспечивает математически строгую неотказуемость."
  },
  {
    "num": 85,
    "title": "[Каверзный кейс] Ограничение размера RSA (RSA Size Limit) и сообщение об ошибке",
    "task": "**[Каверзный кейс — RSA Size Limit]**: Попробуй зашифровать сообщение длиннее 190 байт напрямую через RSA. Поймай ошибку `message too long`. Изучи гибридную криптографию: как RSA используется только для шифрования сессионного ключа AES, а данные шифруются симметрично.",
    "theory": "Одной из самых частых ошибок джуниор- и мидл-разработчиков является попытка зашифровать открытым ключом RSA пользовательский JSON-документ, PDF-файл или JWT-токен целиком.\n\nРазмер блока RSA ограничен длиной модуля $k$ в байтах. Для 2048-битного ключа $k = 2048 / 8 = 256$ байт.\nПри использовании безопасного дополнения **OAEP** с хэш-функцией SHA-256 максимальная длина шифруемого сообщения вычисляется по формуле:\n$$\\text{MaxPlaintextLen} = k - 2 \\cdot hLen - 2$$\nДля SHA-256 ($hLen = 32$ байта):\n$$\\text{MaxPlaintextLen} = 256 - 2 \\cdot 32 - 2 = 256 - 64 - 2 = 190 \\text{ байт}$$\n\nЕсли длина входного среза байт составляет хотя бы 191 байт, вызов `rsa.EncryptOAEP` завершится ошибкой:\n`crypto/rsa: message too long for RSA public key size`.\n\nЕдинственным правильным архитектурным решением является гибридная схема (Envelope Encryption).",
    "step_by_step": "1. Сгенерируйте тестовый ключ RSA-2048.\n2. Создайте срез байт длиной ровно 190 байт и убедитесь, что `rsa.EncryptOAEP` успешно шифрует его.\n3. Создайте срез байт длиной 191 байт и вызовите `rsa.EncryptOAEP`.\n4. Поймайте и выведите точный текст ошибки `message too long for RSA public key size`.\n5. Реализуйте переключение на гибридное шифрование (AES-GCM сессионный ключ), если объем данных превышает 190 байт.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"bytes\"\n\t\"crypto/aes\"\n\t\"crypto/cipher\"\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n\t\"log\"\n)\n\n// smartEncrypt автоматически выбирает RSA-OAEP для коротких данных (<=190 байт)\n// или гибридное шифрование (AES-256-GCM + RSA) для длинных\nfunc smartEncrypt(pub *rsa.PublicKey, data []byte) ([]byte, bool, error) {\n\t// Лимит для RSA-2048 с OAEP SHA-256: 256 - 2*32 - 2 = 190 байт\n\tmaxRSALen := pub.Size() - 2*sha256.Size - 2\n\n\tif len(data) <= maxRSALen {\n\t\tciphertext, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, pub, data, nil)\n\t\treturn ciphertext, false, err\n\t}\n\n\t// Данные превышают 190 байт -> Гибридное шифрование\n\tdek := make([]byte, 32)\n\tif _, err := io.ReadFull(rand.Reader, dek); err != nil {\n\t\treturn nil, false, err\n\t}\n\n\tblock, _ := aes.NewCipher(dek)\n\tgcm, _ := cipher.NewGCM(block)\n\tnonce := make([]byte, gcm.NonceSize())\n\tio.ReadFull(rand.Reader, nonce)\n\n\tencryptedData := gcm.Seal(nil, nonce, data, nil)\n\tencryptedDEK, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, pub, dek, nil)\n\tif err != nil {\n\t\treturn nil, false, err\n\t}\n\n\t// Упаковываем: [2 байта длина DEK][EncryptedDEK][12 байт Nonce][EncryptedData]\n\tvar buf bytes.Buffer\n\tbuf.Write(encryptedDEK)\n\tbuf.Write(nonce)\n\tbuf.Write(encryptedData)\n\n\treturn buf.Bytes(), true, nil\n}\n\nfunc main() {\n\tprivKey, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации: %v\", err)\n\t}\n\tpubKey := &privKey.PublicKey\n\n\t// 1. Пограничный успешный случай: ровно 190 байт\n\tdata190 := bytes.Repeat([]byte(\"A\"), 190)\n\tcipher190, err := rsa.EncryptOAEP(sha256.New(), rand.Reader, pubKey, data190, nil)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка: %v\", err)\n\t}\n\tfmt.Printf(\"190 байт успешно зашифрованы RSA напрямую: размер шифротекста = %d байт\\n\", len(cipher190))\n\n\t// 2. Превышение лимита: ровно 191 байт\n\tdata191 := bytes.Repeat([]byte(\"B\"), 191)\n\t_, err = rsa.EncryptOAEP(sha256.New(), rand.Reader, pubKey, data191, nil)\n\tif err != nil {\n\t\tfmt.Printf(\"Поймана ожидаемая ошибка на 191 байте: %v\\n\", err)\n\t}\n\n\t// 3. Работа смарт-шифратора с большим пейлоадом (500 байт)\n\tlargeData := bytes.Repeat([]byte(\"HighLoad API Payload Data \"), 20)\n\tresult, isHybrid, err := smartEncrypt(pubKey, largeData)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка смарт-шифрования: %v\", err)\n\t}\n\tfmt.Printf(\"Большой пейлоад (%d байт) обработан. Гибридный режим: %v, размер контейнера: %d байт\\n\",\n\t\tlen(largeData), isHybrid, len(result))\n}",
        "note": "Демонстрация лимита 190 байт для RSA-2048 OAEP и элегантный переход на гибридный режим"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# 190 байт успешно зашифрованы RSA напрямую: размер шифротекста = 256 байт\n# Поймана ожидаемая ошибка на 191 байте: crypto/rsa: message too long for RSA public key size\n# Большой пейлоад (520 байт) обработан. Гибридный режим: true, размер контейнера: 804 байт"
      }
    ],
    "under_the_hood": "Формула ограничения длины в PKCS#1 v2.2 OAEP:\nСтруктура дополненного блока перед модульным возведением в степень выглядит так:\n$$EM = 00_{16} \\parallel \\text{maskedSeed} \\parallel \\text{maskedDB}$$\nгде:\n- Первым идет обязательный ведущий нулевой байт `0x00` (чтобы гарантировать $EM < N$).\n- `maskedSeed` имеет длину $hLen$ (32 байта для SHA-256).\n- `maskedDB` содержит: $lHash$ ($hLen$ байт), блок заполнителя $PS$ из нулей, разделитель `0x01` (1 байт) и открытый текст $M$.\nИтого минимальный оверхед паддинга:\n$$1 + hLen + hLen + 1 = 2 \\cdot hLen + 2 \\text{ байта}$$\nПри ключе 2048 бит ($k = 256$) и SHA-256 ($hLen = 32$) оверхед составляет $2 \\cdot 32 + 2 = 66$ байт.\n$256 - 66 = 190$ байт.",
    "pitfalls": "1. **Попытка разбиения данных на чанки RSA:** Распространенный антипаттерн новичков — нарезать большой файл на куски по 190 байт и каждый шифровать через RSA. Это катастрофически неэффективно по CPU (в 1000 раз медленнее AES), увеличивает размер шифротекста и уязвимо к атакам перестановки блоков.\n2. **Использование SHA-1 ради увеличения полезной нагрузки:** При SHA-1 ($hLen = 20$) лимит возрастает до $256 - 42 = 214$ байт, однако SHA-1 признан криптографически небезопасным и запрещен современными политиками безопасности.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему нельзя нарезать гигабайтный файл на чанки по 190 байт и шифровать их циклом в несколько горутин с помощью `rsa.EncryptOAEP`?\n**Ответ:** Это антипаттерн по трем причинам: 1) **Катастрофическое падение производительности:** RSA выполняет модульное возведение в степень 2048-битных чисел. AES с инструкциями AES-NI обрабатывает гигабайт за доли секунды, а RSA заблокирует все CPU ядра сервера на десятки минут. 2) **Раздувание объема (Ciphertext Bloat):** Каждые 190 байт превращаются в 256 байт (+35% оверхед). 3) **Отсутствие аутентифицированного связывания:** Отдельные блоки RSA не защищены от перестановки, удаления или подмены злоумышленником в канале передачи (в отличие от AEAD AES-GCM)."
  },
  {
    "num": 86,
    "title": "[ECDSA] Генерация, подпись, проверка и криптографическая эквивалентность",
    "task": "**[ECDSA]**: Сгенерируй ключи для эллиптической кривой (`crypto/ecdsa`). Подпиши данные и проверь подпись. Объясни, почему ECDSA предпочтительнее RSA в современных системах (меньший размер ключа при той же криптостойкости).",
    "theory": "Алгоритм цифровой подписи на эллиптических кривых (ECDSA) регламентирован стандартами ANSI X9.62 и FIPS 186-4.\n\nВ отличие от RSA, криптостойкость которого падает из-за совершенствования субэкспоненциального метода решета числового поля (General Number Field Sieve), криптостойкость эллиптических кривых без спариваний опирается на экспоненциальную сложность дискретного логарифма (ECDLP). \n\nСравнительная таблица эквивалентной стойкости (NIST SP 800-57):\n| Уровень стойкости | Симметричный шифр | Длина ключа RSA | Длина ключа ECDSA | Экономия размера |\n|:---:|:---:|:---:|:---:|:---:|\n| 80 бит (устарел) | 2TDEA | 1024 бит | 160 бит | 84% |\n| 112 бит | 3TDEA | 2048 бит | 224 бит | 89% |\n| **128 бит** | **AES-128** | **3072 бит** | **256 бит (P-256)** | **91.6%** |\n| **192 бит** | **AES-192** | **7680 бит** | **384 бит (P-384)** | **95.0%** |\n| **256 бит** | **AES-256** | **15360 бит**| **521 бит (P-521)** | **96.6%** |\n\nТаким образом, ECDSA P-256 обеспечивает такой же запас прочности против современных суперкомпьютеров, как монструозный RSA-3072, оставаясь в десятки раз легче для передачи по сети и вычислений.",
    "step_by_step": "1. Сгенерируйте пару ключей ECDSA на стандартной кривой P-256 (`elliptic.P256()`).\n2. Подготовьте произвольный срез данных и вычислите его дайджест через `crypto/sha256`.\n3. Подпишите дайджест с помощью `ecdsa.SignASN1`.\n4. Реализуйте проверку подлинности с помощью `ecdsa.VerifyASN1`.\n5. Сравните время выполнения операций и размеры структур ECDSA и эквивалентного RSA-3072.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ecdsa\"\n\t\"crypto/elliptic\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"log\"\n\t\"time\"\n)\n\nfunc main() {\n\t// 1. Генерация ключа ECDSA (P-256)\n\tt0 := time.Now()\n\tecdsaKey, err := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ECDSA: %v\", err)\n\t}\n\tecdsaGenTime := time.Since(t0)\n\n\t// 2. Данные для подписи\n\tauditLog := []byte(\"AUDIT: User admin changed firewall rules at 2026-09-04T12:00:00Z\")\n\tdigest := sha256.Sum256(auditLog)\n\n\t// 3. Подписание\n\tt0 = time.Now()\n\tsignature, err := ecdsa.SignASN1(rand.Reader, ecdsaKey, digest[:])\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка создания подписи: %v\", err)\n\t}\n\tsignTime := time.Since(t0)\n\n\t// 4. Верификация\n\tt0 = time.Now()\n\tisValid := ecdsa.VerifyASN1(&ecdsaKey.PublicKey, digest[:], signature)\n\tverifyTime := time.Since(t0)\n\n\tfmt.Println(\"=== ECDSA P-256 Benchmarks & Details ===\")\n\tfmt.Printf(\"Время генерации ключа: %v\\n\", ecdsaGenTime)\n\tfmt.Printf(\"Время создания подписи: %v\\n\", signTime)\n\tfmt.Printf(\"Время проверки подписи: %v\\n\", verifyTime)\n\tfmt.Printf(\"Размер подписи (ASN.1): %d байт\\n\", len(signature))\n\tfmt.Printf(\"Статус верификации:     %v\\n\", isValid)\n\n\t// 5. Демонстрация отказа при нарушении целостности\n\tcorruptedDigest := sha256.Sum256([]byte(\"AUDIT: User admin logged out\"))\n\tisInvalidValid := ecdsa.VerifyASN1(&ecdsaKey.PublicKey, corruptedDigest[:], signature)\n\tfmt.Printf(\"Проверка при модификации данных: %v (успешно отвергнуто)\\n\", isInvalidValid)\n}",
        "note": "Генерация ключей ECDSA P-256, замер скорости операций и проверка подлинности через ecdsa.VerifyASN1"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === ECDSA P-256 Benchmarks & Details ===\n# Время генерации ключа: 124.5µs\n# Время создания подписи: 45.2µs\n# Время проверки подписи: 82.1µs\n# Размер подписи (ASN.1): 71 байт\n# Статус верификации:     true\n# Проверка при модификации данных: false (успешно отвергнуто)"
      }
    ],
    "under_the_hood": "Арифметика эллиптической кривой P-256 в рантайме Go:\n1. Кривая задается уравнением Вейерштрасса: $y^2 = x^3 - 3x + b \\pmod p$, где $p = 2^{256} - 2^{224} + 2^{192} + 2^{96} - 1$ — псевдопростое число Мерсенна специального вида (NIST prime).\n2. Благодаря специальному виду простого модуля $p$, редукция по модулю выполняется не через классическое медленное деление в столбик, а через быстрые битовые сдвиги и сложения 64-битных регистров на ассемблере x86-64 / ARM64.\n3. Точки на кривой представлены в координатах Якоби $(X : Y : Z)$, где точка в декартовых координатах соответствует $(X/Z^2, Y/Z^3)$. Это позволяет выполнять сложение и удвоение точек без вычисления модульного обратного элемента (division-free point arithmetic), что ускоряет операции в десятки раз.",
    "pitfalls": "1. **Атака Invalid Curve Attack:** Если верификатор или точка обмена ключами принимает от внешнего клиента координаты $(x, y)$ без проверки принадлежности кривой ($y^2 \\equiv x^3 - 3x + b \\pmod p$), злоумышленник может передать точку с уязвимой кривой малого порядка и извлечь закрытый ключ. В Go функции `elliptic.Unmarshal` и `crypto/ecdsa` выполняют строгую валидацию точки.\n2. **Ненадежный генератор псевдослучайных чисел:** Слабая энтропия в `io.Reader` при вызове `ecdsa.SignASN1` приводит к повторению или предсказуемости значения $k$, что компрометирует закрытый ключ.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему для обеспечения уровня защиты 128 бит RSA требует ключ 3072 бит, тогда как ECDSA — всего 256 бит?\n**Ответ:** Лучший алгоритм факторизации составных чисел RSA (General Number Field Sieve — GNFS) работает за субэкспоненциальное время $O(\\exp(c \\cdot (\\ln N)^{1/3} (\\ln \\ln N)^{2/3}))$. По мере роста вычислительных мощностей размер ключа RSA приходится увеличивать нелинейно. Для эллиптических кривых не существует субэкспоненциальных алгоритмов решения ECDLP — лучший алгоритм $\\rho$-Полларда работает за строго экспоненциальное время $O(\\sqrt{n})$. Поэтому 256-битного ключа эллиптической кривой достаточно для обеспечения 128 бит практической криптостойкости."
  },
  {
    "num": 87,
    "title": "[X.509 Парсинг] Декодирование PEM, парсинг сертификата и валидация сроков",
    "task": "**[X.509 Парсинг]**: Считай `.pem` файл TLS-сертификата. Декодируй его из PEM в DER, затем распарси через `x509.ParseCertificate`. Выведи `Issuer`, `Subject` и срок действия (`NotAfter`).",
    "theory": "Сертификаты открытых ключей в вебе и микросервисной архитектуре стандартизированы спецификацией **ITU-T X.509** (RFC 5280).\n\nСертификат содержит:\n- Идентификатор субъекта (`Subject`): организация, Common Name (CN), страна.\n- Идентификатор издателя (`Issuer`): удостоверяющий центр (CA), подписавший сертификат.\n- Срок действия: временной интервал от `NotBefore` до `NotAfter`.\n- Открытый ключ владельца и список альтернативных доменных имен (`Subject Alternative Names`, SAN / `DNSNames`).\n- Цифровую подпись CA.\n\nФорматы хранения:\n1. **DER (Distinguished Encoding Rules):** Двоичный компактный формат представления ASN.1 структуры.\n2. **PEM (Privacy-Enhanced Mail):** Текстовая обертка над DER, закодированная в Base64 и обрамленная маркерами `-----BEGIN CERTIFICATE-----` и `-----END CERTIFICATE-----`.\n\nВ Go парсинг состоит из двух шагов:\n1. `pem.Decode(pemBytes)` — извлечение сырого DER-среза.\n2. `x509.ParseCertificate(block.Bytes)` — парсинг структуры `*x509.Certificate`.",
    "step_by_step": "1. Создайте в памяти валидный тестовый PEM-сертификат (сгенерировав его через `x509.CreateCertificate`).\n2. Распарсите PEM-блок с помощью `pem.Decode`.\n3. Убедитесь, что тип блока равен `\"CERTIFICATE\"`.\n4. Распарсите бинарный DER-блок с помощью `x509.ParseCertificate(block.Bytes)`.\n5. Извлеките и напечатайте ключевые поля: `Subject`, `Issuer`, `NotBefore`, `NotAfter`, `DNSNames`.\n6. Вычислите оставшийся срок жизни сертификата и проверьте валидность на текущий момент времени.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"encoding/pem\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"time\"\n)\n\n// generateTestCertificatePEM создает самоподписанный сертификат для тестирования\nfunc generateTestCertificatePEM() []byte {\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(2026090401),\n\t\tSubject: pkix.Name{\n\t\t\tCommonName:   \"api.company.internal\",\n\t\t\tOrganization: []string{\"HighLoad Fintech Corp\"},\n\t\t\tCountry:      []string{\"RU\"},\n\t\t},\n\t\tIssuer: pkix.Name{\n\t\t\tCommonName:   \"Fintech Internal Root CA v2\",\n\t\t\tOrganization: []string{\"Fintech Security Authority\"},\n\t\t},\n\t\tNotBefore: time.Now().Add(-1 * time.Hour),\n\t\tNotAfter:  time.Now().Add(365 * 24 * time.Hour),\n\t\tDNSNames:  []string{\"api.company.internal\", \"gateway.company.internal\"},\n\t\tKeyUsage:  x509.KeyUsageDigitalSignature | x509.KeyUsageKeyEncipherment,\n\t}\n\n\tderBytes, err := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации тестового сертификата: %v\", err)\n\t}\n\n\treturn pem.EncodeToMemory(&pem.Block{\n\t\tType:  \"CERTIFICATE\",\n\t\tBytes: derBytes,\n\t})\n}\n\nfunc main() {\n\t// 1. Получаем PEM сертификата\n\tpemData := generateTestCertificatePEM()\n\tfmt.Println(\"=== Сырой PEM блок (фрагмент) ===\")\n\tfmt.Println(string(pemData[:120]) + \"...\\n-----END CERTIFICATE-----\\n\")\n\n\t// 2. Декодирование PEM в DER\n\tblock, rest := pem.Decode(pemData)\n\tif block == nil {\n\t\tlog.Fatal(\"Не удалось декодировать PEM блок\")\n\t}\n\tif block.Type != \"CERTIFICATE\" {\n\t\tlog.Fatalf(\"Неверный тип блока: ожидался CERTIFICATE, получен %s\", block.Type)\n\t}\n\t_ = rest\n\n\t// 3. Парсинг DER через x509.ParseCertificate\n\tcert, err := x509.ParseCertificate(block.Bytes)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка парсинга X.509 сертификата: %v\", err)\n\t}\n\n\t// 4. Анализ метаданных сертификата\n\tfmt.Println(\"=== Распарсенные атрибуты X.509 ===\")\n\tfmt.Printf(\"Subject (Владелец): %s\\n\", cert.Subject)\n\tfmt.Printf(\"Issuer (Издатель):   %s\\n\", cert.Issuer)\n\tfmt.Printf(\"Common Name:         %s\\n\", cert.Subject.CommonName)\n\tfmt.Printf(\"DNS SANs:            %v\\n\", cert.DNSNames)\n\tfmt.Printf(\"Действителен с:      %s\\n\", cert.NotBefore.UTC().Format(time.RFC3339))\n\tfmt.Printf(\"Действителен до:     %s\\n\", cert.NotAfter.UTC().Format(time.RFC3339))\n\n\t// 5. Валидация срока действия\n\tnow := time.Now()\n\tif now.Before(cert.NotBefore) {\n\t\tfmt.Println(\"Статус: СЕРТИФИКАТ ЕЩЕ НЕ ВСТУПИЛ В СИЛУ\")\n\t} else if now.After(cert.NotAfter) {\n\t\tfmt.Println(\"Статус: СЕРТИФИКАТ ПРОСРОЧЕН (EXPIRED)\")\n\t} else {\n\t\tdaysLeft := int(cert.NotAfter.Sub(now).Hours() / 24)\n\t\tfmt.Printf(\"Статус: ВАЛИДЕН (осталось %d дней)\\n\", daysLeft)\n\t}\n}",
        "note": "Декодирование PEM, разбор бинарного DER в x509.Certificate и аудит сроков действия"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Сырой PEM блок (фрагмент) ===\n# -----BEGIN CERTIFICATE-----\n# MIICljCCAX6gAwIBAgIEeB7LITANBgkqhkiG9w0BAQsFADAvMS0wKwYDVQQDDCRGaW50...\n# -----END CERTIFICATE-----\n# \n# === Распарсенные атрибуты X.509 ===\n# Subject (Владелец): CN=api.company.internal,O=HighLoad Fintech Corp,C=RU\n# Issuer (Издатель):   CN=Fintech Internal Root CA v2,O=Fintech Security Authority\n# Common Name:         api.company.internal\n# DNS SANs:            [api.company.internal gateway.company.internal]\n# Действителен с:      2026-09-04T11:00:00Z\n# Действителен до:     2027-09-04T12:00:00Z\n# Статус: ВАЛИДЕН (осталось 364 дней)"
      }
    ],
    "under_the_hood": "Что делает `x509.ParseCertificate`:\n1. Под капотом вызывается ASN.1 парсер стандартной библиотеки `encoding/asn1`.\n2. Бинарный поток DER разбирается в соответствии с нотацией ITU-T X.509:\n   - `TBSCertificate` (To Be Signed): версия, серийный номер, идентификатор алгоритма подписи, имя издателя, период действия (`Validity`), имя субъекта, `SubjectPublicKeyInfo`, уникальные идентификаторы и расширения (Extensions).\n   - `SignatureAlgorithm`: идентификатор алгоритма (например, `SHA256WithRSA`).\n   - `SignatureValue`: битовая строка с самой подписью.\n3. Парсер выполняет проверку критических расширений (`Critical Extensions`). Если в сертификате присутствует расширение, помеченное флагом `critical`, но неизвестное парсеру Go, `x509.ParseCertificate` вернет ошибку `UnhandledCriticalExtension`.",
    "pitfalls": "1. **Проверка только pem.Decode без цикла:** В `.pem` файле может быть цепочка сертификатов (Fullchain = Leaf + Intermediate CA + Root CA). Если вызвать `pem.Decode` один раз, будет прочитан только первый листовой сертификат, а остальные будут проигнорированы. Для полной цепочки необходим цикл: `for len(rest) > 0 { block, rest = pem.Decode(rest) ... }`.\n2. **Игнорирование SAN (Subject Alternative Name):** Современные браузеры и TLS-стеки (включая Go 1.15+) полностью игнорируют поле `CommonName` при верификации домена и требуют наличия домена в `DNSNames` (SAN). Полагаться только на `cert.Subject.CommonName` небезопасно.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Go 1.15+ была отключена проверка доменных имен по полю `CommonName` (CN) в X.509 сертификатах, и какие последствия это несет для микросервисов?\n**Ответ:** Стандарт RFC 6125 (опубликованный еще в 2011 году) признал практику проверки хостнейма по полю `Subject.CommonName` устаревшей и небезопасной, закрепив обязательное использование расширения `Subject Alternative Name` (SAN / `DNSNames` или `IPAddresses`). Поле CN имеет ограничение длины в 64 символа, не поддерживает строгую дифференциацию типов (домен против IP) и допускает неоднозначные кодировки. Если микросервисы генерируют самоподписанные сертификаты без заполнения SAN, клиентский TLS в Go выдаст ошибку `x509: certificate relies on legacy Common Name field`."
  },
  {
    "num": 88,
    "title": "Конфигурация TLS версий и наборов шифров (Cipher Suites) в tls.Config",
    "task": "**TLS версии и cipher suites**: Настройте `tls.Config` с минимумом TLS 1.2, предпочитаемо TLS 1.3. Уберите слабые cipher suites.",
    "theory": "Неправильная конфигурация протокола Transport Layer Security (TLS) на серверах и клиентах приводит к уязвимостям класса POODLE, BEAST, SWEET32 и атакам на понижение версии (Downgrade attacks).\n\nСовременные требования Enterprise безопасности:\n1. **Запрет устаревших протоколов:** SSLv3, TLS 1.0 и TLS 1.1 должны быть безусловно запрещены. Минимальная допустимая версия — **TLS 1.2**, приоритетная — **TLS 1.3**.\n2. **Исключение небезопасных шифров:** Запрещены шифры с алгоритмами RC4, DES/3DES, неаутентифицированные режимы CBC (уязвимые к padding oracle атакам Lucky Thirteen) и статический обмен ключами RSA без Forward Secrecy.\n3. **Разрешенные шифры для TLS 1.2:** Только шифры на базе **ECDHE** (эллиптический обмен Диффи-Хеллмана) с аутентифицированным шифрованием **AEAD** (AES-GCM или ChaCha20-Poly1305).\n4. **Особенность TLS 1.3:** В протоколе TLS 1.3 наборы шифров больше не настраиваются через параметр `tls.Config.CipherSuites`! IETF удалил все небезопасные шифры из стандарта TLS 1.3, оставив исключительно AEAD-шифры с Forward Secrecy.",
    "step_by_step": "1. Создайте структуру `tls.Config`.\n2. Установите `MinVersion: tls.VersionTLS12` и `MaxVersion: tls.VersionTLS13`.\n3. Сконфигурируйте список стойких шифров для TLS 1.2 (`CipherSuites`), включив только ECDHE с AES-GCM и ChaCha20-Poly1305.\n4. Продемонстрируйте проверку согласованного протокола и шифра при установке соединения с сервером.\n5. Напишите тестовый запуск локального TLS-сервера и клиента с валидацией согласованных параметров через `tls.ConnectionState`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"time\"\n)\n\n// secureTLSConfig возвращает эталонную безопасную конфигурацию TLS для HighLoad сервисов\nfunc secureTLSConfig(cert tls.Certificate) *tls.Config {\n\treturn &tls.Config{\n\t\tCertificates: []tls.Certificate{cert},\n\t\t// 1. Запрещаем устаревшие протоколы (TLS 1.0, 1.1)\n\t\tMinVersion: tls.VersionTLS12,\n\t\tMaxVersion: tls.VersionTLS13,\n\n\t\t// 2. Безопасные шифры для TLS 1.2 (строго ECDHE + AEAD)\n\t\tCipherSuites: []uint16{\n\t\t\ttls.TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384,\n\t\t\ttls.TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,\n\t\t\ttls.TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305,\n\t\t\ttls.TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305,\n\t\t\ttls.TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256,\n\t\t\ttls.TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,\n\t\t},\n\n\t\t// 3. Отдаем приоритет эллиптическим кривым X25519 и P-256\n\t\tCurvePreferences: []tls.CurveID{\n\t\t\ttls.X25519,\n\t\t\ttls.CurveP256,\n\t\t},\n\t}\n}\n\nfunc main() {\n\t// Генерация самоподписанного сертификата на лету для демонстрации\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(1),\n\t\tSubject:      pkix.Name{CommonName: \"127.0.0.1\"},\n\t\tIPAddresses:  []net.IP{net.ParseIP(\"127.0.0.1\")},\n\t\tNotBefore:    time.Now().Add(-time.Hour),\n\t\tNotAfter:     time.Now().Add(time.Hour),\n\t}\n\tder, _ := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\tserverCert := tls.Certificate{Certificate: [][]byte{der}, PrivateKey: priv}\n\n\tserverCfg := secureTLSConfig(serverCert)\n\n\t// Запуск защищенного TLS слушателя\n\tlistener, err := tls.Listen(\"tcp\", \"127.0.0.1:0\", serverCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка запуска TLS сервера: %v\", err)\n\t}\n\tdefer listener.Close()\n\n\tserverAddr := listener.Addr().String()\n\n\t// Серверная горутина\n\tgo func() {\n\t\tconn, err := listener.Accept()\n\t\tif err != nil {\n\t\t\treturn\n\t\t}\n\t\tdefer conn.Close()\n\t\ttlsConn := conn.(*tls.Conn)\n\t\t_ = tlsConn.Handshake()\n\t\tstate := tlsConn.ConnectionState()\n\t\tfmt.Printf(\"[Сервер] Согласован TLS: %s, Шифр: %s\\n\",\n\t\t\ttls.VersionName(state.Version),\n\t\t\ttls.CipherSuiteName(state.CipherSuite))\n\t}()\n\n\t// Запуск защищенного TLS клиента\n\tclientCfg := &tls.Config{\n\t\tInsecureSkipVerify: true, // для самоподписанного IP сертификата\n\t\tMinVersion:         tls.VersionTLS12,\n\t\tMaxVersion:         tls.VersionTLS13,\n\t}\n\n\tconn, err := tls.Dial(\"tcp\", serverAddr, clientCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подключения клиента: %v\", err)\n\t}\n\tdefer conn.Close()\n\n\tstate := conn.ConnectionState()\n\tfmt.Printf(\"[Клиент] Успешное соединение!\\n\")\n\tfmt.Printf(\"[Клиент] Версия TLS: %s\\n\", tls.VersionName(state.Version))\n\tfmt.Printf(\"[Клиент] Cipher Suite: %s\\n\", tls.CipherSuiteName(state.CipherSuite))\n\tfmt.Printf(\"[Клиент] Perfect Forward Secrecy: да (согласована сессия)\\n\")\n}",
        "note": "Настройка tls.Config с ограничением версий TLS 1.2-1.3 и аудитом ConnectionState"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# [Сервер] Согласован TLS: TLS 1.3, Шифр: TLS_AES_128_GCM_SHA256\n# [Клиент] Успешное соединение!\n# [Клиент] Версия TLS: TLS 1.3\n# [Клиент] Cipher Suite: TLS_AES_128_GCM_SHA256\n# [Клиент] Perfect Forward Secrecy: да (согласована сессия)"
      }
    ],
    "under_the_hood": "Почему в TLS 1.3 поле `CipherSuites` в структуре `tls.Config` не действует?\nВ TLS 1.2 набор шифров (например, `TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384`) определял сразу 4 вещи:\n1. Алгоритм обмена ключами (ECDHE).\n2. Алгоритм аутентификации сертификата (RSA).\n3. Симметричный шифр и режим (AES-256-GCM).\n4. Хэш-функцию PRF для вычисления сессионных ключей (SHA-384).\n\nВ TLS 1.3 архитекторы IETF ортогонализовали протокол:\n- Обмен ключами вынесен в отдельное расширение `Key Share` (всегда (EC)DHE: X25519 или P-256).\n- Подпись сертификата вынесена в расширение `Signature Algorithms`.\n- Набор `CipherSuite` теперь определяет исключительно пару (AEAD-шифр + HMAC-хэш): `TLS_AES_128_GCM_SHA256`, `TLS_AES_256_GCM_SHA384`, `TLS_CHACHA20_POLY1305_SHA256`.\nПоэтому в Go стандартные шифры TLS 1.3 включены всегда и не могут быть случайно ослаблены разработчиком.",
    "pitfalls": "1. **Использование PreferServerCipherSuites:** Начиная с Go 1.17+, флаг `PreferServerCipherSuites` объявлен устаревшим и игнорируется рантаймом, так как выбор лучшего шифра теперь оптимизирован аппаратно на клиенте и сервере с учетом AES-NI.\n2. **Включение CBC шифров:** Шифры вроде `TLS_RSA_WITH_AES_128_CBC_SHA` катастрофически уязвимы к атакам перехвата сессии (Lucky Thirteen). Их наличие в `CipherSuites` немедленно проваливает аудит безопасности по стандартам PCI-DSS.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в современных распределенных сервисах запрещен шифр `TLS_RSA_WITH_AES_256_GCM_SHA384` без ECDHE (то есть чистый статический RSA key exchange)?\n**Ответ:** Статический RSA key exchange не обеспечивает **Perfect Forward Secrecy (PFS)**. Если трафик сервиса записывается спецслужбами или злоумышленником в течение нескольких лет («Store now, decrypt later»), а затем приватный RSA-ключ сервера утечет или будет скомпрометирован по ордеру суда, весь ранее записанный терабайтный трафик за все годы будет мгновенно расшифрован. В схеме ECDHE для каждой отдельной сессии генерируются одноразовые эфемерные ключи, которые уничтожаются сразу после завершения хэндшейка, делая ретроспективное расшифрование невозможным."
  },
  {
    "num": 89,
    "title": "Certificate Pinning: защита HTTP-клиента от поддельных CA и MitM",
    "task": "**Certificate pinning**: Реализуйте certificate pinning в вашем HTTP-клиенте для защиты от MitM атак через поддельные CA.",
    "theory": "Стандартная модель доверия PKI (Public Key Infrastructure) уязвима: в доверенном хранилище ОС находится более 100–150 корневых центров сертификации (Root CA). Если хотя бы один CA в мире будет скомпрометирован злоумышленниками, взломан или принужден спецслужбами выпустить поддельный сертификат (Rogue Certificate) для `api.yourbank.com`, стандартный HTTP-клиент успешно примет такое соединение.\n\n**Certificate Pinning (привязка сертификата / открытого ключа)** — это защитный механизм, при котором клиент игнорирует системное хранилище Root CA и жестко сверяет отпечаток (криптографический хэш SHA-256) сертификата сервера или его открытого ключа (SPKI Pinning).\n\nВ Go Certificate Pinning реализуется через поле `tls.Config.VerifyPeerCertificate`:\n1. Устанавливается `InsecureSkipVerify: true` (или кастомная валидация), чтобы переопределить стандартный чекер ОС.\n2. В колбэке `VerifyPeerCertificate(rawCerts [][]byte, verifiedChains [][]*x509.Certificate)` вычисляется `sha256.Sum256` от байт сертификата (или от `cert.RawSubjectPublicKeyInfo`).\n3. Отпечаток сравнивается с эталонным пином с помощью `subtle.ConstantTimeCompare`.",
    "step_by_step": "1. Определите эталонный hex-отпечаток SHA-256 ожидаемого сертификата сервера.\n2. Создайте функцию `newPinnedHTTPClient(expectedFingerprintHex string) *http.Client`.\n3. Сконфигурируйте `tls.Config` с колбэком `VerifyPeerCertificate`.\n4. В колбэке распарсите первый сертификат цепочки (`rawCerts[0]`), вычислите SHA-256 отпечаток и сравните его в постоянном времени.\n5. Запустите тестовый сервер, выполните успешный запрос при совпадении отпечатка и продемонстрируйте отказ соединения при несовпадении пина.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/sha256\"\n\t\"crypto/subtle\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"encoding/hex\"\n\t\"errors\"\n\t\"fmt\"\n\t\"io\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"time\"\n)\n\n// newPinnedClient создает http.Client с валидацией конкретного отпечатка сертификата\nfunc newPinnedClient(expectedFingerprintHex string) *http.Client {\n\texpectedBytes, err := hex.DecodeString(expectedFingerprintHex)\n\tif err != nil {\n\t\tlog.Fatalf(\"Некорректный эталонный пин: %v\", err)\n\t}\n\n\ttlsConfig := &tls.Config{\n\t\t// Отключаем стандартный чекер ОС, чтобы исключить доверие скомпрометированным Root CA\n\t\tInsecureSkipVerify: true,\n\t\tVerifyPeerCertificate: func(rawCerts [][]byte, verifiedChains [][]*x509.Certificate) error {\n\t\t\tif len(rawCerts) == 0 {\n\t\t\t\treturn errors.New(\"сервер не предоставил сертификаты\")\n\t\t\t}\n\n\t\t\t// Вычисляем SHA-256 отпечаток листового сертификата (Leaf Certificate)\n\t\t\tleafFingerprint := sha256.Sum256(rawCerts[0])\n\n\t\t\t// Сравнение в константном времени для защиты от тайминг-атак\n\t\t\tif subtle.ConstantTimeCompare(leafFingerprint[:], expectedBytes) != 1 {\n\t\t\t\treturn fmt.Errorf(\"certificate pinning mismatch: получен %x, ожидался %s\",\n\t\t\t\t\tleafFingerprint, expectedFingerprintHex)\n\t\t\t}\n\n\t\t\treturn nil\n\t\t},\n\t}\n\n\ttransport := &http.Transport{\n\t\tTLSClientConfig: tlsConfig,\n\t}\n\n\treturn &http.Client{\n\t\tTransport: transport,\n\t\tTimeout:   5 * time.Second,\n\t}\n}\n\nfunc main() {\n\t// 1. Создаем тестовый TLS сервер с самоподписанным сертификатом\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(42),\n\t\tSubject:      pkix.Name{CommonName: \"secure-vault.company.internal\"},\n\t\tIPAddresses:  []net.IP{net.ParseIP(\"127.0.0.1\")},\n\t\tNotBefore:    time.Now().Add(-time.Hour),\n\t\tNotAfter:     time.Now().Add(time.Hour),\n\t}\n\tcertDER, _ := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\tserverTLS := tls.Certificate{Certificate: [][]byte{certDER}, PrivateKey: priv}\n\n\tts := httptest.NewUnstartedServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\tw.WriteHeader(http.StatusOK)\n\t\tw.Write([]byte(\"VAULT_SECRET_PAYLOAD: 7789-ALPHA-OK\"))\n\t}))\n\tts.TLS = &tls.Config{Certificates: []tls.Certificate{serverTLS}}\n\tts.StartTLS()\n\tdefer ts.Close()\n\n\t// 2. Вычисляем легитимный отпечаток\n\tactualFingerprint := sha256.Sum256(certDER)\n\tpinHex := hex.EncodeToString(actualFingerprint[:])\n\tfmt.Printf(\"Легитимный SHA-256 отпечаток сервера: %s\\n\\n\", pinHex)\n\n\t// 3. Тест 1: Запрос с корректным PIN\n\tfmt.Println(\"=== Тест 1: Клиент с корректным отпечатком ===\")\n\tclientValid := newPinnedClient(pinHex)\n\tresp, err := clientValid.Get(ts.URL)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка запроса: %v\", err)\n\t}\n\tbody, _ := io.ReadAll(resp.Body)\n\tresp.Body.Close()\n\tfmt.Printf(\"Ответ сервера: %s\\n\\n\", string(body))\n\n\t// 4. Тест 2: Запрос с поддельным PIN (имитация MitM атаки поддельным CA)\n\tfmt.Println(\"=== Тест 2: Защита от MitM с поддельным сертификатом ===\")\n\tfakePinHex := \"00112233445566778899aabbccddeeff00112233445566778899aabbccddeeff\"\n\tclientMitM := newPinnedClient(fakePinHex)\n\t_, err = clientMitM.Get(ts.URL)\n\tif err != nil {\n\t\tfmt.Printf(\"Атака успешно заблокирована! Ошибка клиента:\\n%v\\n\", err)\n\t}\n}",
        "note": "Реализация Certificate Pinning в Go HTTP-клиенте с защитой от MitM-атак"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Легитимный SHA-256 отпечаток сервера: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855\n# \n# === Тест 1: Клиент с корректным отпечатком ===\n# Ответ сервера: VAULT_SECRET_PAYLOAD: 7789-ALPHA-OK\n# \n# === Тест 2: Защита от MitM с поддельным сертификатом ===\n# Атака успешно заблокирована! Ошибка клиента:\n# Get \"https://127.0.0.1:...\": certificate pinning mismatch: получен e3b0c4..., ожидался 001122..."
      }
    ],
    "under_the_hood": "Сравнение Certificate Pinning vs SPKI Pinning (Subject Public Key Info Pinning):\n1. **Привязка целого сертификата (Certificate Pinning):** Хэшируется весь срез `rawCerts[0]`. Минус подхода: при плановом перевыпуске сертификата (например, раз в 90 дней у Let's Encrypt) серийный номер и сроки меняются, из-за чего отпечаток меняется и клиенты теряют доступ («brick client issue»).\n2. **Привязка открытого ключа (SPKI Pinning):** Хэшируются только байты открытого ключа `cert.RawSubjectPublicKeyInfo`. При ротации сертификата разработчик выпускает новый сертификат на **том же самом закрытом ключе** (CSR генерируется из старого приватного ключа). В результате отпечаток SPKI остается прежним, и клиенты не ломаются.\n3. В BigTech мобильных приложениях и микросервисах всегда зашивают как минимум 2 пина: текущий основной пин и резервный (Backup Pin) на случай экстренной компрометации ключа.",
    "pitfalls": "1. **Использование стандартного != вместо ConstantTimeCompare:** Сравнение хэшей через обычный оператор `string == string` или `bytes.Equal` уязвимо к тайминг-атакам (побайтовое раннее завершение цикла). Всегда используйте `subtle.ConstantTimeCompare`.\n2. **Отсутствие резервного пина (Hardcoded single pin):** Если в мобильном приложении зашит только один пин, а приватный ключ сервера скомпрометирован или утерян, приложение полностью перестает работать до выпуска обновления через App Store / Google Play (что может занять несколько дней).",
    "bigtech_interview": "**Вопрос с собеседования:** В чем опасность жесткого Certificate Pinning в мобильных банковских приложениях и как архитектурно решается проблема ротации ключей?\n**Ответ:** Если зашить отпечаток полного сертификата, при его истечении или экстренной компрометации все мобильные клиенты у миллионов пользователей окажутся заблокированными («самострел»). Решение: 1) Использовать **SPKI Pinning** (привязку открытого ключа, а не сертификата), что позволяет обновлять сертификаты без смены ключа; 2) Обязательно внедрять **Backup Pin** — резервную пару ключей, хранящуюся в холодном сейфе (cold storage); 3) Включать механизм плавного fallback через подписанную динамическую конфигурацию (Remote Config) с верификацией подписи на отдельном корневом ключе вендора."
  },
  {
    "num": 90,
    "title": "Генерация самоподписанных сертификатов X.509 с SAN (Subject Alternative Names)",
    "task": "**Self-signed certificate generation**: Напишите код для генерации self-signed сертификатов с SAN (Subject Alternative Names) через `crypto/x509`.",
    "theory": "Самоподписанные сертификаты (Self-Signed Certificates) используются в локальной разработке, интеграционных тестах и закрытых контурах Kubernetes (service mesh, ингресс-контроллеры).\n\nКлючевые требования к современному X.509 v3 сертификату:\n1. **Уникальный Serial Number:** Должен быть случайным большим числом (до 160 бит) для предотвращения атак коллизий хэшей.\n2. **Обязательное наличие SAN (Subject Alternative Name):** Доменные имена указываются в `DNSNames: []string{...}`, а IP-адреса — в `IPAddresses: []net.IP{...}`. Поле `CommonName` устарело и не проверяется современными TLS-клиентами.\n3. **KeyUsage и ExtKeyUsage:** Флаги определяют назначение ключа (`KeyUsageDigitalSignature`, `KeyUsageKeyEncipherment`, `ExtKeyUsageServerAuth`, `ExtKeyUsageClientAuth`).\n4. **Basic Constraints:** Для листового сертификата `IsCA: false`, `BasicConstraintsValid: true`. Для корневого CA: `IsCA: true`.\n5. **Подписание:** Функция `x509.CreateCertificate` принимает шаблон, сертификат родителя (для self-signed родитель совпадает с шаблоном), открытый ключ и закрытый ключ.",
    "step_by_step": "1. Сгенерируйте пару ключей RSA-2048 или ECDSA P-256 с помощью `crypto/rand`.\n2. Сформируйте шаблон `x509.Certificate` с корректным серийным номером, сроком действия и SAN.\n3. Вызовите `x509.CreateCertificate` для создания бинарного DER-сертификата.\n4. Закодируйте полученный сертификат и закрытый ключ в формат PEM (`pem.EncodeToMemory`).\n5. Продемонстрируйте сборку работоспособной пары `tls.Certificate` с помощью `tls.X509KeyPair`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"encoding/pem\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"time\"\n)\n\n// GenerateSelfSignedCert создает самоподписанный сертификат с SAN в формате PEM\nfunc GenerateSelfSignedCert(hosts []string, validFor time.Duration) ([]byte, []byte, error) {\n\t// 1. Генерация приватного ключа RSA-2048\n\tpriv, err := rsa.GenerateKey(rand.Reader, 2048)\n\tif err != nil {\n\t\treturn nil, nil, fmt.Errorf(\"ошибка генерации ключа: %w\", err)\n\t}\n\n\t// 2. Генерация криптографического серийного номера (128 бит)\n\tserialLimit := new(big.Int).Lsh(big.NewInt(1), 128)\n\tserialNum, err := rand.Int(rand.Reader, serialLimit)\n\tif err != nil {\n\t\treturn nil, nil, fmt.Errorf(\"ошибка генерации серийного номера: %w\", err)\n\t}\n\n\tnotBefore := time.Now().Add(-5 * time.Minute) // защита от рассинхронизации часов\n\tnotAfter := notBefore.Add(validFor)\n\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: serialNum,\n\t\tSubject: pkix.Name{\n\t\t\tOrganization: []string{\"Microservices Internal Org\"},\n\t\t\tCommonName:   hosts[0],\n\t\t},\n\t\tNotBefore:             notBefore,\n\t\tNotAfter:              notAfter,\n\t\tKeyUsage:              x509.KeyUsageKeyEncipherment | x509.KeyUsageDigitalSignature,\n\t\tExtKeyUsage:           []x509.ExtKeyUsage{x509.ExtKeyUsageServerAuth, x509.ExtKeyUsageClientAuth},\n\t\tBasicConstraintsValid: true,\n\t\tIsCA:                  false,\n\t}\n\n\t// 3. Заполнение Subject Alternative Names (SAN)\n\tfor _, h := range hosts {\n\t\tif ip := net.ParseIP(h); ip != nil {\n\t\t\ttemplate.IPAddresses = append(template.IPAddresses, ip)\n\t\t} else {\n\t\t\ttemplate.DNSNames = append(template.DNSNames, h)\n\t\t}\n\t}\n\n\t// 4. Создание бинарного X.509 DER сертификата (self-signed: шаблон = эмитент)\n\tderBytes, err := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\tif err != nil {\n\t\treturn nil, nil, fmt.Errorf(\"ошибка создания сертификата: %w\", err)\n\t}\n\n\t// 5. Кодирование сертификата в PEM\n\tcertPEM := pem.EncodeToMemory(&pem.Block{\n\t\tType:  \"CERTIFICATE\",\n\t\tBytes: derBytes,\n\t})\n\n\t// 6. Кодирование приватного ключа в PKCS#8 PEM\n\tprivDER, err := x509.MarshalPKCS8PrivateKey(priv)\n\tif err != nil {\n\t\treturn nil, nil, fmt.Errorf(\"ошибка сериализации ключа: %w\", err)\n\t}\n\tkeyPEM := pem.EncodeToMemory(&pem.Block{\n\t\tType:  \"PRIVATE KEY\",\n\t\tBytes: privDER,\n\t})\n\n\treturn certPEM, keyPEM, nil\n}\n\nfunc main() {\n\thosts := []string{\"localhost\", \"127.0.0.1\", \"auth.internal.corp\"}\n\tcertPEM, keyPEM, err := GenerateSelfSignedCert(hosts, 365*24*time.Hour)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации: %v\", err)\n\t}\n\n\tfmt.Println(\"=== Успешно сгенерирован TLS сертификат с SAN ===\")\n\tfmt.Printf(\"Размер PEM сертификата: %d байт\\n\", len(certPEM))\n\tfmt.Printf(\"Размер PEM ключа:        %d байт\\n\", len(keyPEM))\n\n\t// Валидация созданной связки через tls.X509KeyPair\n\ttlsPair, err := tls.X509KeyPair(certPEM, keyPEM)\n\tif err != nil {\n\t\tlog.Fatalf(\"Некорректная пара ключей TLS: %v\", err)\n\t}\n\n\tparsed, _ := x509.ParseCertificate(tlsPair.Certificate[0])\n\tfmt.Printf(\"DNS SANs: %v\\n\", parsed.DNSNames)\n\tfmt.Printf(\"IP SANs:  %v\\n\", parsed.IPAddresses)\n\tfmt.Printf(\"Срок действия: до %s\\n\", parsed.NotAfter.Format(time.RFC3339))\n}",
        "note": "Генерация самоподписанного сертификата с полным заполнением SAN (DNS и IP)"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Успешно сгенерирован TLS сертификат с SAN ===\n# Размер PEM сертификата: 1123 байт\n# Размер PEM ключа:        1704 байт\n# DNS SANs: [localhost auth.internal.corp]\n# IP SANs:  [127.0.0.1]\n# Срок действия: до 2027-09-04T11:55:00Z"
      }
    ],
    "under_the_hood": "В функции `x509.CreateCertificate`:\n1. Из шаблона собирается структура `tbsCertificate` (To Be Signed Certificate).\n2. Поля SAN упаковываются в специальное расширение X.509 v3 с OID `2.5.29.17` (id-ce-subjectAltName). Каждое DNS-имя получает тег `[2] IA5String`, а IP-адрес — тег `[7] OCTET STRING` (4 байта для IPv4, 16 байт для IPv6).\n3. Затем вычисляется криптографический хэш структуры `tbsCertificate` с алгоритмом `SHA256WithRSA` или `ECDSAWithSHA256`.\n4. Хэш подписывается переданным приватным ключом.\n5. Финальная структура ASN.1 SEQUENCE (`tbsCertificate`, `signatureAlgorithm`, `signatureValue`) сериализуется в байтовый массив DER.",
    "pitfalls": "1. **Использование NotBefore: time.Now():** Если сервер клиента или эмитент имеет часы, отстающие хотя бы на 1 секунду от сервера генерации, клиент немедленно вернет ошибку `x509: certificate has expired or is not yet valid`. Всегда делайте откат `notBefore` на 5-10 минут в прошлое!\n2. **Постоянный серийный номер (например, SerialNumber = big.NewInt(1)):** Браузеры кэшируют статус отозванности сертификатов по паре (Issuer + SerialNumber). Если перевыпускать сертификаты с одинаковым серийным номером, возникают труднодиагностируемые ошибки кэширования.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему при генерации сертификатов в Go критически важно указывать `NotBefore` со сдвигом в прошлое (backdating)?\n**Ответ:** В распределенных системах и облачных кластерах (Kubernetes) часы на разных физических серверах синхронизируются через NTP с некоторой погрешностью (Clock Skew, типично от десятков миллисекунд до нескольких секунд). Если сертификат выпущен с `NotBefore = time.Now()`, а сервис-клиент на соседней ноде опережает эмитент на 500 миллисекунд, TLS-рукопожатие упадет с ошибкой `certificate is not yet valid`. Сдвиг даты начала действия на 5-15 минут назад гарантирует отсутствие сбоев при рассинхронизации часов."
  },
  {
    "num": 91,
    "title": "CSR (Certificate Signing Request) и подписание собственным CA",
    "task": "**CSR generation и подписание**: Сгенерируйте Certificate Signing Request и подпишите его через ваш собственный CA.",
    "theory": "В корпоративных Enterprise-системах приватные ключи серверов никогда не передаются по сети и не покидают узел, на котором они созданы. Для получения сертификата используется механизм **CSR (Certificate Signing Request)**, стандартизированный в RFC 2986 (PKCS#10).\n\nЖизненный цикл выпуска сертификата:\n1. **Создание ключа и CSR на сервере:** Сервер локально генерирует приватный ключ и формирует запрос на подпись (CSR), содержащий публичный ключ и запрашиваемые SAN/метаданные. CSR подписывается собственным приватным ключом сервера (Proof of Possession — доказательство владения).\n2. **Передача CSR в CA:** Запрос передается корпоративному Удостоверяющему Центру (Internal CA / Vault). Приватный ключ сервера остается на месте.\n3. **Валидация и подписание:** CA проверяет подпись CSR, создает шаблон сертификата и подписывает его **приватным ключом CA**.\n4. **Возврат сертификата:** Сервер получает готовый X.509 сертификат и может запускать TLS.",
    "step_by_step": "1. Создайте корневой удостоверяющий центр (Root CA): сгенерируйте ключ и самоподписанный CA-сертификат с флагом `IsCA: true`.\n2. На стороне клиента/сервиса сгенерируйте пару ключей и создайте запрос `x509.CertificateRequest` с SAN.\n3. Закодируйте CSR с помощью `x509.CreateCertificateRequest`.\n4. На стороне CA распарсите CSR с помощью `x509.ParseCertificateRequest` и верифицируйте подпись запроса (`csr.CheckSignature()`).\n5. Выпустите листовой сертификат с помощью `x509.CreateCertificate`, подписав его ключом CA.\n6. Проверьте цепочку доверия выпущенного сертификата против пула сертификатов CA (`cert.Verify`).",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"time\"\n)\n\n// createRootCA генерирует удостоверяющий центр (Root CA)\nfunc createRootCA() (*x509.Certificate, *rsa.PrivateKey, error) {\n\tcaPriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\tcaTemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(10001),\n\t\tSubject: pkix.Name{\n\t\t\tCommonName:   \"Internal Enterprise Root CA\",\n\t\t\tOrganization: []string{\"Fintech Infrastructure Security\"},\n\t\t},\n\t\tNotBefore:             time.Now().Add(-1 * time.Hour),\n\t\tNotAfter:              time.Now().Add(10 * 365 * 24 * time.Hour),\n\t\tIsCA:                  true,\n\t\tKeyUsage:              x509.KeyUsageCertSign | x509.KeyUsageCRLSign,\n\t\tBasicConstraintsValid: true,\n\t}\n\n\tcaDER, err := x509.CreateCertificate(rand.Reader, &caTemplate, &caTemplate, &caPriv.PublicKey, caPriv)\n\tif err != nil {\n\t\treturn nil, nil, err\n\t}\n\tcaCert, _ := x509.ParseCertificate(caDER)\n\treturn caCert, caPriv, nil\n}\n\n// generateCSR создает запрос на подпись сертификата\nfunc generateCSR(priv *rsa.PrivateKey, domain string) ([]byte, error) {\n\tcsrTemplate := x509.CertificateRequest{\n\t\tSubject:  pkix.Name{CommonName: domain},\n\t\tDNSNames: []string{domain, \"www.\" + domain},\n\t}\n\treturn x509.CreateCertificateRequest(rand.Reader, &csrTemplate, priv)\n}\n\n// signCSR выполняет подписание клиентского запроса ключом CA\nfunc signCSR(caCert *x509.Certificate, caPriv *rsa.PrivateKey, csrDER []byte) (*x509.Certificate, error) {\n\tcsr, err := x509.ParseCertificateRequest(csrDER)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка парсинга CSR: %w\", err)\n\t}\n\n\t// Обязательная проверка proof of possession\n\tif err := csr.CheckSignature(); err != nil {\n\t\treturn nil, fmt.Errorf(\"подпись CSR недействительна: %w\", err)\n\t}\n\n\tserialNum, _ := rand.Int(rand.Reader, new(big.Int).Lsh(big.NewInt(1), 128))\n\tleafTemplate := x509.Certificate{\n\t\tSerialNumber:          serialNum,\n\t\tSubject:               csr.Subject,\n\t\tDNSNames:              csr.DNSNames,\n\t\tNotBefore:             time.Now().Add(-10 * time.Minute),\n\t\tNotAfter:              time.Now().Add(90 * 24 * time.Hour),\n\t\tKeyUsage:              x509.KeyUsageDigitalSignature | x509.KeyUsageKeyEncipherment,\n\t\tExtKeyUsage:           []x509.ExtKeyUsage{x509.ExtKeyUsageServerAuth},\n\t\tBasicConstraintsValid: true,\n\t\tIsCA:                  false,\n\t}\n\n\tleafDER, err := x509.CreateCertificate(rand.Reader, &leafTemplate, caCert, csr.PublicKey, caPriv)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка подписания сертификата: %w\", err)\n\t}\n\n\treturn x509.ParseCertificate(leafDER)\n}\n\nfunc main() {\n\t// 1. Создаем CA\n\tcaCert, caPriv, err := createRootCA()\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка создания CA: %v\", err)\n\t}\n\tfmt.Printf(\"Root CA успешно инициализирован: %s\\n\", caCert.Subject.CommonName)\n\n\t// 2. Сервер генерирует свои ключи и формирует CSR\n\tserverPriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\tcsrDER, err := generateCSR(serverPriv, \"billing.corp.local\")\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка создания CSR: %v\", err)\n\t}\n\tfmt.Printf(\"CSR успешно сформирован: размер %d байт\\n\", len(csrDER))\n\n\t// 3. CA подписывает CSR\n\tsignedCert, err := signCSR(caCert, caPriv, csrDER)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подписания CSR: %v\", err)\n\t}\n\tfmt.Printf(\"Листовой сертификат успешно выпущен для: %v (эмитент: %s)\\n\",\n\t\tsignedCert.DNSNames, signedCert.Issuer.CommonName)\n\n\t// 4. Проверка цепочки доверия\n\troots := x509.NewCertPool()\n\troots.AddCert(caCert)\n\n\topts := x509.VerifyOptions{\n\t\tRoots:   roots,\n\t\tDNSName: \"billing.corp.local\",\n\t}\n\n\tchains, err := signedCert.Verify(opts)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка верификации цепочки доверия: %v\", err)\n\t}\n\tfmt.Printf(\"Цепочка доверия успешно проверена! Глубина: %d\\n\", len(chains[0]))\n}",
        "note": "Полный цикл PKI: создание Root CA, формирование CSR, верификация и подписание"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Root CA успешно инициализирован: Internal Enterprise Root CA\n# CSR успешно сформирован: размер 639 байт\n# Листовой сертификат успешно выпущен для: [billing.corp.local www.billing.corp.local] (эмитент: Internal Enterprise Root CA)\n# Цепочка доверия успешно проверена! Глубина: 2"
      }
    ],
    "under_the_hood": "В функции `csr.CheckSignature()`:\nПакет `crypto/x509` проверяет, что запрос на подпись сертификата подписан тем самым открытым ключом, который содержится внутри этого же CSR (`csr.PublicKey`). Это гарантирует:\n1. Заявитель действительно обладает соответствующим приватным ключом.\n2. Злоумышленник не может взять чужой открытый ключ и попросить CA выпустить сертификат на свое имя, если не знает закрытого ключа.\nВ функции `x509.CreateCertificate`: открытый ключ берется из `csr.PublicKey`, а не создается заново, сохраняя изоляцию закрытого ключа на узле сервиса.",
    "pitfalls": "1. **Пропуск csr.CheckSignature() на стороне CA:** Если CA слепо берет данные из CSR без верификации подписи, он уязвим к инъекциям неподтвержденных открытых ключей.\n2. **Слепое копирование Subject и расширений из CSR:** Атакующий может запросить в CSR `IsCA: true` или права на домены `*.google.com`. Надежный CA обязан переопределять шаблоны и строго фильтровать список разрешенных SAN.",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое Proof-of-Possession в PKI и почему CA обязан валидировать цифровую подпись самого CSR перед выпуском сертификата?\n**Ответ:** Proof-of-Possession (доказательство владения) подтверждает, что инициатор выпуска сертификата действительно владеет закрытым ключом, соответствующим заявленному открытому ключу. Если бы CA этого не проверял, злоумышленник мог бы перехватить чужой открытый ключ (например, платежного шлюза), отправить в CA запрос от своего имени и получить валидный сертификат с чужим ключом, что разрушило бы доверенную модель PKI и привело к подмене сущностей."
  },
  {
    "num": 92,
    "title": "OCSP Stapling: ускорение проверки статуса отзыва сертификатов",
    "task": "**OCSP Stapling**: Настройте OCSP Stapling в вашем TLS сервере для быстрой валидации сертификатов.",
    "theory": "При стандартной проверке отзыва сертификата через OCSP (Online Certificate Status Protocol, RFC 6960) каждый клиент при TLS-хэндшейке обязан отправить отдельный HTTP-запрос к стороннему серверу Удостоверяющего Центра (OCSP Responder).\n\nПроблемы традиционного OCSP:\n1. **Увеличение задержки (Latency):** Дополнительный HTTP RTT к стороннему серверу добавляет 100–500 мс к открытию страницы.\n2. **Утечка приватности (Privacy Leak):** CA видит IP-адреса всех пользователей и знает, какие сайты они посещают в реальном времени.\n3. **Отказоустойчивость:** Если OCSP Responder падает под нагрузкой, браузеры либо блокируют вход (hard-fail), либо пропускают проверку (soft-fail, обесценивая безопасность).\n\n**OCSP Stapling (TLS Certificate Status Request, RFC 6066):**\nСервер сам периодически опрашивает OCSP Responder, получает подписанный CA ответ о валидности сертификата с ограниченным сроком жизни (например, 24 часа), кэширует его и «пришпиливает» (staples) прямо в TLS Handshake (сообщение `CertificateStatus`). Клиент получает доказательство валидности без сторонних сетевых запросов!\n\nВ Go OCSP Stapling настраивается через заполнение поля `tls.Certificate.OCSPStaple []byte`.",
    "step_by_step": "1. Создайте структуру `tls.Certificate`.\n2. Заполните поле `OCSPStaple` бинарным срезом подписанного OCSP-ответа (в реальной жизни он запрашивается по таймеру фоновой горутиной).\n3. Сконфигурируйте и запустите локальный TLS-сервер.\n4. На стороне клиента подключитесь через `tls.Dial` и изучите поле `conn.ConnectionState().OCSPResponse`.\n5. Убедитесь, что клиент получает закэшированный OCSP ответ напрямую в рукопожатии без внешних обращений.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"time\"\n)\n\nfunc main() {\n\t// 1. Создание сертификата сервера\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(8890),\n\t\tSubject:      pkix.Name{CommonName: \"127.0.0.1\"},\n\t\tIPAddresses:  []net.IP{net.ParseIP(\"127.0.0.1\")},\n\t\tNotBefore:    time.Now().Add(-1 * time.Hour),\n\t\tNotAfter:     time.Now().Add(24 * time.Hour),\n\t}\n\tcertDER, _ := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\n\t// 2. Имитация подписанного OCSP-ответа от Удостоверяющего Центра\n\t// (в проде срез формируется запросом к CA и обновляется фоновой горутиной)\n\tmockOCSPResponse := []byte(\"MOCK_ASN1_SIGNED_OCSP_RESPONSE_STATUS_GOOD_EXPIRES_24H\")\n\n\tserverCert := tls.Certificate{\n\t\tCertificate: [][]byte{certDER},\n\t\tPrivateKey:  priv,\n\t\t// Ключевое поле: сервер прикрепляет OCSP-ответ к рукопожатию\n\t\tOCSPStaple:  mockOCSPResponse,\n\t}\n\n\t// 3. Запуск TLS сервера\n\tserverCfg := &tls.Config{\n\t\tCertificates: []tls.Certificate{serverCert},\n\t}\n\tlistener, err := tls.Listen(\"tcp\", \"127.0.0.1:0\", serverCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка старта сервера: %v\", err)\n\t}\n\tdefer listener.Close()\n\tserverAddr := listener.Addr().String()\n\n\tgo func() {\n\t\tconn, err := listener.Accept()\n\t\tif err != nil {\n\t\t\treturn\n\t\t}\n\t\tdefer conn.Close()\n\t\ttlsConn := conn.(*tls.Conn)\n\t\t_ = tlsConn.Handshake()\n\t}()\n\n\t// 4. Клиентское TLS рукопожатие\n\tclientCfg := &tls.Config{\n\t\tInsecureSkipVerify: true,\n\t}\n\n\tconn, err := tls.Dial(\"tcp\", serverAddr, clientCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подключения: %v\", err)\n\t}\n\tdefer conn.Close()\n\n\tstate := conn.ConnectionState()\n\n\t// 5. Проверка наличия OCSP Stapling на клиенте\n\tfmt.Println(\"=== Статус TLS Соединения ===\")\n\tfmt.Printf(\"TLS версия: %s\\n\", tls.VersionName(state.Version))\n\tif len(state.OCSPResponse) > 0 {\n\t\tfmt.Printf(\"OCSP Stapling: АКТИВЕН (получен ответ длиной %d байт)\\n\", len(state.OCSPResponse))\n\t\tfmt.Printf(\"Тело OCSP ответа: %s\\n\", string(state.OCSPResponse))\n\t} else {\n\t\tfmt.Println(\"OCSP Stapling: НЕ ПРЕДОСТАВЛЕН\")\n\t}\n}",
        "note": "Настройка tls.Certificate.OCSPStaple и клиентская валидация state.OCSPResponse"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Статус TLS Соединения ===\n# TLS версия: TLS 1.3\n# OCSP Stapling: АКТИВЕН (получен ответ длиной 54 байт)\n# Тело OCSP ответа: MOCK_ASN1_SIGNED_OCSP_RESPONSE_STATUS_GOOD_EXPIRES_24H"
      }
    ],
    "under_the_hood": "Механика протокола OCSP Stapling в TLS:\n1. Клиент в сообщении `ClientHello` передает TLS-расширение `status_request` (ExtensionType 5).\n2. Сервер, получив это расширение, проверяет срез `tls.Certificate.OCSPStaple`.\n3. Если срез не пустой, сервер отправляет клиенту TLS-сообщение `CertificateStatus` непосредственно перед `ServerHelloDone` (в TLS 1.2) или внутри зашифрованного блока расширений `EncryptedExtensions` (в TLS 1.3).\n4. Клиент локально проверяет криптографическую подпись OCSP-ответа открытым ключом CA (или назначенного OCSP Signer). Если подпись валидна и статус сертификата `good`, хэндшейк считается успешным без единого исходящего сетевого соединения от клиента к CA.",
    "pitfalls": "1. **Протухание OCSP Staple:** Подписанный OCSP-ответ имеет жесткий срок действия (обычно от нескольких часов до недели). Если фоновая горутина сервера зависнет и не обновит срез `cert.OCSPStaple`, клиенты начнут отклонять соединение из-за просроченного OCSP.\n2. **Многопоточная гонка при обновлении:** Замена среза `cert.OCSPStaple` на работающем веб-сервере должна быть защищена `sync.RWMutex` или атомарным указателем (`atomic.Pointer[tls.Certificate]`).",
    "bigtech_interview": "**Вопрос с собеседования:** Чем OCSP Stapling превосходит CRL (Certificate Revocation Lists) и обычный онлайн OCSP в высоконагруженных распределенных системах?\n**Ответ:** 1) Списки отзыва CRL могут разрастаться до десятков мегабайт, перегружая сеть и оперативную память клиентов. 2) Обычный OCSP требует от каждого клиента отдельного HTTP-запроса к CA, раскрывая посещаемые ресурсы (нарушение конфиденциальности) и добавляя 100-500 мс задержки к каждому TLS-соединению. 3) OCSP Stapling переносит нагрузку с клиента на сервер: сервер раз в сутки кэширует компактный ответ CA (~500 байт) и мгновенно отдает его клиентам прямо в теле TLS-рукопожатия с нулевой дополнительной сетевой задержкой."
  },
  {
    "num": 93,
    "title": "Certificate Transparency (CT): Signed Certificate Timestamps (SCT) и аудит открытых логов",
    "task": "**Certificate transparency (CT)**: Изучите CT логи. Все современные сертификаты должны быть в CT логах.",
    "theory": "Исторически Удостоверяющие Центры (CA) могли тайно выпустить поддельный сертификат для любого домена (например, скандал с иранским взломом DigiNotar или выпуск сертификатов `*.google.com` центром Trustwave), и владелец домена не имел возможности узнать об этом.\n\n**Certificate Transparency (CT, RFC 6962)** — это открытая экосистема мониторинга и аудита всех выпускаемых TLS-сертификатов:\n1. **Append-Only Logs:** Публичные независимые логи (Google, Cloudflare, DigiCert), построенные на базе криптографических деревьев Меркла (Merkle Tree). В лог можно только дописывать записи, изменение или удаление задним числом невозможно.\n2. **SCT (Signed Certificate Timestamp):** Доказательство того, что сертификат отправлен и включен в публичный лог.\n3. **Требование браузеров:** Google Chrome и Apple Safari жестко требуют наличия как минимум 2–3 SCT от независимых операторов логов. Если сертификат не опубликован в CT логе, браузер блокирует доступ ошибкой `NET::ERR_CERTIFICATE_TRANSPARENCY_REQUIRED`.\n\nСпособы доставки SCT:\n- **X.509 Extension:** Встраивание SCT непосредственно в тело сертификата в виде расширения с OID `1.3.6.1.4.1.11129.2.4.2`.\n- **TLS Extension:** Передача в специальном расширении рукопожатия `signed_certificate_timestamp`.\n- **OCSP Stapling:** Включение SCT внутри OCSP-ответа.",
    "step_by_step": "1. Создайте структуру парсера для извлечения SCT из расширений X.509 сертификата.\n2. Найдите расширение с OID Certificate Transparency (`1.3.6.1.4.1.11129.2.4.2`).\n3. Распарсите бинарный список SCT (Serialized SCT List).\n4. Извлеките идентификатор лога (Log ID), временную метку фиксации (Timestamp) и криптографическую подпись оператора лога.\n5. Продемонстрируйте проверку наличия CT-подтверждения у сертификата.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"encoding/binary\"\n\t\"encoding/hex\"\n\t\"fmt\"\n\t\"time\"\n)\n\n// OID расширения Certificate Transparency SCT в сертификатах X.509\nvar oidExtensionCT = []int{1, 3, 6, 1, 4, 1, 11129, 2, 4, 2}\n\n// SCTItem представляет распарсенный Signed Certificate Timestamp\ntype SCTItem struct {\n\tVersion   uint8\n\tLogID     string    // SHA-256 хэш открытого ключа CT лога\n\tTimestamp time.Time // Точное время фиксации сертификата оператором лога\n}\n\n// extractSCTs извлекает и парсит SCT из расширений X.509 сертификата\nfunc extractSCTs(cert *x509.Certificate) ([]SCTItem, error) {\n\tvar sctListRaw []byte\n\tfor _, ext := range cert.Extensions {\n\t\tif ext.Id.Equal(oidExtensionCT) {\n\t\t\tsctListRaw = ext.Value\n\t\t\tbreak\n\t\t}\n\t}\n\n\tif len(sctListRaw) == 0 {\n\t\treturn nil, fmt.Errorf(\"расширение Certificate Transparency SCT не найдено\")\n\t}\n\n\t// Структура согласно RFC 6962: 2 байта общая длина списка\n\tif len(sctListRaw) < 2 {\n\t\treturn nil, fmt.Errorf(\"поврежденный заголовок SCT списка\")\n\t}\n\n\toffset := 2\n\tvar items []SCTItem\n\n\tfor offset < len(sctListRaw) {\n\t\tif offset+2 > len(sctListRaw) {\n\t\t\tbreak\n\t\t}\n\t\titemLen := int(binary.BigEndian.Uint16(sctListRaw[offset : offset+2]))\n\t\toffset += 2\n\t\tif offset+itemLen > len(sctListRaw) {\n\t\t\tbreak\n\t\t}\n\n\t\titemData := sctListRaw[offset : offset+itemLen]\n\t\toffset += itemLen\n\n\t\tif len(itemData) < 1+32+8 {\n\t\t\tcontinue\n\t\t}\n\n\t\tversion := itemData[0]\n\t\tlogID := hex.EncodeToString(itemData[1:33])\n\t\ttsMillis := binary.BigEndian.Uint64(itemData[33:41])\n\t\tts := time.UnixMilli(int64(tsMillis))\n\n\t\titems = append(items, SCTItem{\n\t\t\tVersion:   version,\n\t\t\tLogID:     logID,\n\t\t\tTimestamp: ts,\n\t\t})\n\t}\n\n\treturn items, nil\n}\n\nfunc main() {\n\t// Создаем имитацию сертификата с расширением SCT (RFC 6962)\n\t// Формируем синтетический байтовый поток SCT\n\tsctBytes := make([]byte, 0)\n\t// Элемент SCT: [v:1b][logID:32b][timestamp:8b][sig...]\n\tsingleSCT := make([]byte, 1+32+8+10)\n\tsingleSCT[0] = 0 // SCT v1\n\tcopy(singleSCT[1:33], []byte(\"GOOGLE_ARGON_CT_LOG_OPERATOR_01_\"))\n\tbinary.BigEndian.PutUint64(singleSCT[33:41], uint64(time.Now().UnixMilli()))\n\n\t// Упаковываем в список SCT\n\tlistBytes := make([]byte, 2+2+len(singleSCT))\n\tbinary.BigEndian.PutUint16(listBytes[0:2], uint16(2+len(singleSCT)))\n\tbinary.BigEndian.PutUint16(listBytes[2:4], uint16(len(singleSCT)))\n\tcopy(listBytes[4:], singleSCT)\n\n\tmockCert := &x509.Certificate{\n\t\tExtensions: []pkix.Extension{\n\t\t\t{\n\t\t\t\tId:    oidExtensionCT,\n\t\t\t\tValue: listBytes,\n\t\t\t},\n\t\t},\n\t}\n\n\t// Извлекаем и парсим SCT\n\tscts, err := extractSCTs(mockCert)\n\tif err != nil {\n\t\tfmt.Printf(\"Ошибка: %v\\n\", err)\n\t\treturn\n\t}\n\n\tfmt.Println(\"=== Аудит Certificate Transparency (CT) ===\")\n\tfmt.Printf(\"Найдено действительных SCT записей: %d\\n\", len(scts))\n\tfor idx, s := range scts {\n\t\tfmt.Printf(\"[%d] Версия: v%d | Log ID: %s | Время фиксации: %s\\n\",\n\t\t\tidx+1, s.Version+1, s.LogID[:16]+\"...\", s.Timestamp.UTC().Format(time.RFC3339))\n\t}\n\tfmt.Println(\"Статус: Сертификат зафиксирован в открытом реестре прозрачности.\")\n}",
        "note": "Парсинг расширения Certificate Transparency (OID 1.3.6.1.4.1.11129.2.4.2) и аудит SCT"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Аудит Certificate Transparency (CT) ===\n# Найдено действительных SCT записей: 1\n# [1] Версия: v1 | Log ID: 474f4f474c455f41... | Время фиксации: 2026-09-04T12:00:00Z\n# Статус: Сертификат зафиксирован в открытом реестре прозрачности."
      }
    ],
    "under_the_hood": "Как устроена криптографическая верификация CT логов:\n1. Лог CT представляет собой дерево Меркла (Merkle Tree), основанное на хэш-функции SHA-256.\n2. Каждая листовая вершина дерева — это запись о выпуске сертификата или предсертификата (Precertificate).\n3. Оператор лога периодически выпускает подписанный корень дерева — STH (Signed Tree Head), гарантируя криптографическую целостность и неизменность истории.\n4. SCT содержит криптографическую подпись оператора лога на эллиптической кривой (ECDSA P-256). Клиент может математически проверить доказательство включения (Inclusion Proof) сертификата в дерево за $O(\\log N)$ операций хэширования без скачивания всего лога.",
    "pitfalls": "1. **Непонимание роли Precertificate:** При обычном выпуске CA сначала создает Precertificate (специальный сертификат с расширением Poison), отправляет его в CT-логи, получает SCT, вшивает SCT в финальный сертификат и лишь затем отдает клиенту. Повторная отправка финального сертификата в лог приведет к дублированию.\n2. **Отсутствие внешнего мониторинга CT:** Сам по себе CT лог лишь фиксирует выпуск. Служба безопасности компании обязана подключить CT-монитор (например, Certstream или Facebook CT Monitor), чтобы получать алерты в Slack при появлении любого нового сертификата на корпоративный домен.",
    "bigtech_interview": "**Вопрос с собеседования:** Какую угрозу безопасности решает протокол Certificate Transparency (CT) и как финтех-компании используют его для защиты от фишинга?\n**Ответ:** CT решает проблему скрытого и несанкционированного выпуска сертификатов скомпрометированными или подкупленными CA. Поскольку браузеры отклоняют сертификаты без подтверждения SCT, злоумышленники не могут выпустить скрытый сертификат для MitM. Финтех-компании слушают поток CT-логов в реальном времени: если мошенники регистрируют домен `sberbank-login-online.com` или выпускают сертификат на поддомен компании, SIEM-система фиксирует событие за секунды до запуска фишинговой кампании и немедленно отправляет жалобу регистратору."
  },
  {
    "num": 94,
    "title": "mTLS (Mutual TLS): взаимная аутентификация в архитектуре Zero Trust",
    "task": "**mTLS (Mutual TLS)**: Настройте mTLS для service-to-service аутентификации. Оба конца предъявляют сертификаты. Используется в Zero Trust architecture.",
    "theory": "В концепции **Zero Trust (нулевого доверия)** периметр локальной сети объявляется скомпрометированным по умолчанию. Простой проверки серверного сертификата клиентом недостаточно: сервер обязан жестко удостовериться в подлинности каждого вызывающего сервиса.\n\n**mTLS (Mutual TLS / Двусторонний TLS):**\n1. Сервер отправляет клиенту свой сертификат (серверная аутентификация).\n2. Сервер запрашивает сертификат клиента (`CertificateRequest`).\n3. Клиент отправляет свой сертификат и доказывает владение приватным ключом через сообщение `CertificateVerify`.\n4. Сервер проверяет клиентский сертификат по локальному пулу доверенных CA (`ClientCAs`).\n\nКонфигурация в Go `tls.Config`:\n- На сервере:\n  - `ClientAuth: tls.RequireAndVerifyClientCert` (строгое отклонение клиентов без сертификата).\n  - `ClientCAs: caCertPool` (пул доверенных центров авторизации клиентов).\n- На клиенте:\n  - `Certificates: []tls.Certificate{clientPair}` (собственная связка открытого и закрытого ключа).\n  - `RootCAs: caCertPool` (доверие серверному CA).",
    "step_by_step": "1. Создайте единый доверенный корневой удостоверяющий центр (Root CA).\n2. Выпустите серверный сертификат и клиентский сертификат, подписанные этим Root CA.\n3. Настройте сервер с `tls.ClientAuthType = tls.RequireAndVerifyClientCert`.\n4. Продемонстрируйте успешное сервисное рукопожатие, когда клиент передает валидный клиентский сертификат.\n5. Продемонстрируйте немедленный отказ соединения, когда анонимный клиент без сертификата пытается выполнить запрос.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"io\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"time\"\n)\n\n// helper: создание подписанного сертификата для mTLS\nfunc issueCert(caCert *x509.Certificate, caPriv *rsa.PrivateKey, name string, isServer bool) (tls.Certificate, error) {\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\tserial, _ := rand.Int(rand.Reader, big.NewInt(1000000))\n\n\textUsage := []x509.ExtKeyUsage{x509.ExtKeyUsageClientAuth}\n\tvar ips []net.IP\n\tif isServer {\n\t\textUsage = []x509.ExtKeyUsage{x509.ExtKeyUsageServerAuth}\n\t\tips = []net.IP{net.ParseIP(\"127.0.0.1\")}\n\t}\n\n\ttmpl := x509.Certificate{\n\t\tSerialNumber: serial,\n\t\tSubject:      pkix.Name{CommonName: name, Organization: []string{\"Mesh Network\"}},\n\t\tIPAddresses:  ips,\n\t\tNotBefore:    time.Now().Add(-time.Hour),\n\t\tNotAfter:     time.Now().Add(time.Hour),\n\t\tKeyUsage:     x509.KeyUsageDigitalSignature | x509.KeyUsageKeyEncipherment,\n\t\tExtKeyUsage:  extUsage,\n\t}\n\n\tder, err := x509.CreateCertificate(rand.Reader, &tmpl, caCert, &priv.PublicKey, caPriv)\n\tif err != nil {\n\t\treturn tls.Certificate{}, err\n\t}\n\n\treturn tls.Certificate{Certificate: [][]byte{der}, PrivateKey: priv}, nil\n}\n\nfunc main() {\n\t// 1. Инициализация Root CA\n\tcaPriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\tcaTmpl := x509.Certificate{\n\t\tSerialNumber:          big.NewInt(1),\n\t\tSubject:               pkix.Name{CommonName: \"Internal ZeroTrust CA\"},\n\t\tNotBefore:             time.Now().Add(-time.Hour),\n\t\tNotAfter:              time.Now().Add(time.Hour),\n\t\tIsCA:                  true,\n\t\tKeyUsage:              x509.KeyUsageCertSign | x509.KeyUsageCRLSign,\n\t\tBasicConstraintsValid: true,\n\t}\n\tcaDER, _ := x509.CreateCertificate(rand.Reader, &caTmpl, &caTmpl, &caPriv.PublicKey, caPriv)\n\tcaCert, _ := x509.ParseCertificate(caDER)\n\n\tcaPool := x509.NewCertPool()\n\tcaPool.AddCert(caCert)\n\n\t// 2. Выпуск сертификатов сервера и легитимного микросервиса-клиента\n\tserverCert, _ := issueCert(caCert, caPriv, \"127.0.0.1\", true)\n\tclientCert, _ := issueCert(caCert, caPriv, \"service-billing-worker\", false)\n\n\t// 3. Настройка mTLS сервера\n\tserverTLS := &tls.Config{\n\t\tCertificates: []tls.Certificate{serverCert},\n\t\tClientAuth:   tls.RequireAndVerifyClientCert, // Требуем и проверяем сертификат клиента!\n\t\tClientCAs:    caPool,\n\t}\n\n\tts := httptest.NewUnstartedServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\t// Сервер извлекает identity клиента из сертификата\n\t\tclientCN := r.TLS.PeerCertificates[0].Subject.CommonName\n\t\tfmt.Printf(\"[Сервер] Аутентифицирован клиент: %s\\n\", clientCN)\n\t\tw.WriteHeader(http.StatusOK)\n\t\tw.Write([]byte(\"ACCESS_GRANTED: SECRET_PAYMENT_DB\"))\n\t}))\n\tts.TLS = serverTLS\n\tts.StartTLS()\n\tdefer ts.Close()\n\n\t// 4. Тест 1: Запрос от авторизованного клиента с клиентским сертификатом\n\tfmt.Println(\"=== Тест 1: Авторизованный mTLS микросервис ===\")\n\tvalidClient := &http.Client{\n\t\tTransport: &http.Transport{\n\t\t\tTLSClientConfig: &tls.Config{\n\t\t\t\tRootCAs:      caPool,\n\t\t\t\tCertificates: []tls.Certificate{clientCert}, // Клиент предъявляет свой сертификат\n\t\t\t},\n\t\t},\n\t}\n\n\tresp, err := validClient.Get(ts.URL)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка mTLS клиента: %v\", err)\n\t}\n\tbody, _ := io.ReadAll(resp.Body)\n\tresp.Body.Close()\n\tfmt.Printf(\"Ответ сервера: %s\\n\\n\", string(body))\n\n\t// 5. Тест 2: Запрос от неавторизованного клиента без клиентского сертификата\n\tfmt.Println(\"=== Тест 2: Попытка доступа без сертификата ===\")\n\tunauthClient := &http.Client{\n\t\tTransport: &http.Transport{\n\t\t\tTLSClientConfig: &tls.Config{\n\t\t\t\tRootCAs: caPool, // доверяет серверу, но свой сертификат НЕ передает\n\t\t\t},\n\t\t},\n\t}\n\n\t_, err = unauthClient.Get(ts.URL)\n\tif err != nil {\n\t\tfmt.Printf(\"Сервер успешно отклонил соединение!\\nОшибка: %v\\n\", err)\n\t}\n}",
        "note": "Реализация mTLS в Go: ClientAuth = RequireAndVerifyClientCert и извлечение PeerCertificates"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Тест 1: Авторизованный mTLS микросервис ===\n# [Сервер] Аутентифицирован клиент: service-billing-worker\n# Ответ сервера: ACCESS_GRANTED: SECRET_PAYMENT_DB\n# \n# === Тест 2: Попытка доступа без сертификата ===\n# Сервер успешно отклонил соединение!\n# Ошибка: Get \"https://127.0.0.1:...\": remote error: tls: certificate required"
      }
    ],
    "under_the_hood": "Последовательность сообщений при mTLS хэндшейке:\n1. `ClientHello` (клиент отправляет список шифров и расширений).\n2. `ServerHello`, `Certificate` (сервер отправляет свой сертификат).\n3. `CertificateRequest`: сервер сообщает список поддерживаемых алгоритмов подписи и имена доверенных CA (`ClientCAs`).\n4. `ServerHelloDone`.\n5. `Certificate`: клиент отправляет цепочку своего сертификата.\n6. `ClientKeyExchange`: клиент выполняет обмен ключами (ECDHE).\n7. `CertificateVerify`: клиент вычисляет подпись поверх всех предыдущих сообщений рукопожатия своим **приватным ключом**. Сервер проверяет эту подпись открытым ключом из сертификата клиента, гарантируя, что клиент действительно владеет ключом.\n8. `Finished`: переход на симметричное шифрование сессии.",
    "pitfalls": "1. **Использование RequestClientCert вместо RequireAndVerifyClientCert:** При `RequestClientCert` сервер запрашивает сертификат, но пускает клиента даже при его отсутствии! Это часто приводит к дырам в авторизации, когда анонимный трафик попадает в защищенные хэндлеры.\n2. **Пропуск валидации атрибутов Subject/SAN:** Сам по себе валидный сертификат от CA доказывает только то, что клиент принадлежит компании. В коде хэндлера необходимо проверять, имеет ли данный `CommonName` или SAN право обращаться к конкретному эндпоинту (RBAC).",
    "bigtech_interview": "**Вопрос с собеседования:** Как устроен mTLS в Service Mesh сетях (Istio, Envoy, Linkerd) в Kubernetes и какую роль играет Envoy sidecar?\n**Ответ:** Приложения в подах общаются по чистому HTTP по локальному loopback (`localhost:8080`) с прокси-контейнером Envoy (sidecar). Envoy перехватывает трафик через iptables, автоматически запрашивает клиентский сертификат у внутреннего CA (Istiod / SPIRE) с коротким временем жизни (обычно 12–24 часа) и устанавливает зашифрованный mTLS-туннель до Envoy-sidecar целевого микросервиса. Это обеспечивает криптографическую аутентификацию и прозрачное шифрование трафика без изменения единой строчки исходного кода в Go-приложении."
  },
  {
    "num": 95,
    "title": "ACME-клиент: автоматический заказ сертификатов Let's Encrypt (RFC 8555)",
    "task": "**ACME client**: Напишите простой ACME client (как cert-manager) для получения Let's Encrypt сертификатов автоматически.",
    "theory": "Протокол **ACME (Automated Certificate Management Environment, RFC 8555)** произвел революцию в веб-безопасности, позволив полностью автоматизировать выпуск, валидацию и продление TLS-сертификатов Удостоверяющего Центра Let's Encrypt.\n\nПринцип работы протокола ACME:\n1. **Регистрация аккаунта:** Клиент генерирует пару ключей аккаунта и регистрируется в ACME-сервере (`acme.Client`).\n2. **Заказ сертификата (New Order):** Клиент запрашивает выпуск сертификата для домена (например, `api.example.com`).\n3. **Прохождение испытания (Challenge):** Сервер Let's Encrypt требует подтвердить владение доменом:\n   - **HTTP-01:** Клиент должен отдать специальный токен по пути `http://<domain>/.well-known/acme-challenge/<token>`.\n   - **TLS-ALPN-01:** Клиент должен согласовать протокол `acme-tls/1` в TLS-хэндшейке.\n   - **DNS-01:** Клиент должен создать TXT-запись `_acme-challenge.<domain>`.\n4. **Финализация и выпуск:** Клиент отправляет CSR, Let's Encrypt валидирует challenge и возвращает подписанную цепочку сертификатов.\n\nВ Go официальная библиотека `golang.org/x/crypto/acme/autocert` предоставляет готовый менеджер `autocert.Manager`, который бесшовно встраивается в `http.Server`.",
    "step_by_step": "1. Создайте экземпляр `autocert.Manager`.\n2. Настройте белый список разрешенных доменов (`autocert.HostWhitelist`).\n3. Настройте кэширование сертификатов на диске (`autocert.DirCache`), чтобы не превышать строгие лимиты (rate limits) Let's Encrypt.\n4. Сконфигурируйте `tls.Config{GetCertificate: certManager.GetCertificate}`.\n5. Напишите HTTP-хэндлер для автоматического прохождения HTTP-01 challenge на 80-м порту.\n6. Запустите безопасный HTTPS-сервер на 443 порту.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/tls\"\n\t\"fmt\"\n\t\"log\"\n\t\"net/http\"\n\t\"os\"\n\t\"path/filepath\"\n\t\"time\"\n\n\t\"golang.org/x/crypto/acme\"\n\t\"golang.org/x/crypto/acme/autocert\"\n)\n\n// setupProductionACMEServer настраивает автоматический выпуск сертификатов Let's Encrypt\nfunc setupProductionACMEServer(domain string, cacheDir string) (*http.Server, *http.Server) {\n\t// 1. Инициализация менеджера autocert\n\tcertManager := &autocert.Manager{\n\t\tPrompt:     autocert.AcceptTOS, // Согласие с условиями Let's Encrypt\n\t\tHostPolicy: autocert.HostWhitelist(domain),\n\t\tCache:      autocert.DirCache(cacheDir), // Кэширование сертификатов на диске\n\t\tClient: &acme.Client{\n\t\t\t// В реальной разработке используйте acme.LetsEncryptURL\n\t\t\t// Для тестов: \"https://acme-staging-v02.api.letsencrypt.org/directory\"\n\t\t\tDirectoryURL: acme.LetsEncryptURL,\n\t\t},\n\t}\n\n\t// 2. HTTPS Сервер (порт 443)\n\thttpsServer := &http.Server{\n\t\tAddr: \":443\",\n\t\tHandler: http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\t\tw.WriteHeader(http.StatusOK)\n\t\t\tfmt.Fprintf(w, \"Привет! Это безопасный сервис %s с сертификатом Let's Encrypt!\\n\", domain)\n\t\t}),\n\t\tTLSConfig: &tls.Config{\n\t\t\tGetCertificate: certManager.GetCertificate,\n\t\t\tMinVersion:     tls.VersionTLS12,\n\t\t},\n\t\tReadTimeout:  10 * time.Second,\n\t\tWriteTimeout: 10 * time.Second,\n\t}\n\n\t// 3. HTTP Сервер (порт 80) — обязателен для прохождения HTTP-01 challenge и редиректа на HTTPS\n\thttpServer := &http.Server{\n\t\tAddr:         \":80\",\n\t\tHandler:      certManager.HTTPHandler(nil), // Обрабатывает /.well-known/acme-challenge/\n\t\tReadTimeout:  5 * time.Second,\n\t\tWriteTimeout: 5 * time.Second,\n\t}\n\n\treturn httpsServer, httpServer\n}\n\nfunc main() {\n\ttempCache := filepath.Join(os.TempDir(), \"letsencrypt-certs-cache\")\n\tdomain := \"api.example-production.ru\"\n\n\thttpsSrv, httpSrv := setupProductionACMEServer(domain, tempCache)\n\n\tfmt.Println(\"=== Архитектура ACME клиента (Let's Encrypt) ===\")\n\tfmt.Printf(\"Домен в белом списке:    %s\\n\", domain)\n\tfmt.Printf(\"Директория кэша ключей:  %s\\n\", tempCache)\n\tfmt.Printf(\"HTTP Listener (ACME-01): %s\\n\", httpSrv.Addr)\n\tfmt.Printf(\"HTTPS Listener (TLS):    %s\\n\", httpsSrv.Addr)\n\tfmt.Println(\"Автоматическая ротация:  за 30 дней до истечения срока действия\")\n\n\t// В продакшене:\n\t// go func() { log.Fatal(httpSrv.ListenAndServe()) }()\n\t// log.Fatal(httpsSrv.ListenAndServeTLS(\"\", \"\"))\n}",
        "note": "Конфигурация автоматического ACME Let's Encrypt клиента с autocert.Manager и HTTP-01 challenge"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Архитектура ACME клиента (Let's Encrypt) ===\n# Домен в белом списке:    api.example-production.ru\n# Директория кэша ключей:  /tmp/letsencrypt-certs-cache\n# HTTP Listener (ACME-01): :80\n# HTTPS Listener (TLS):    :443\n# Автоматическая ротация:  за 30 дней до истечения срока действия"
      }
    ],
    "under_the_hood": "Что происходит внутри `certManager.GetCertificate(hello *tls.ClientHelloInfo)`:\n1. При поступлении запроса менеджер проверяет локальный кэш `autocert.DirCache`.\n2. Если сертификат найден и до конца срока действия более 30 дней, он возвращается мгновенно за 0 мс без сетевых запросов.\n3. Если сертификата нет или осталось менее 30 дней, менеджер выполняет блокирующую операцию выпуска:\n   - Генерирует в памяти ключ ECDSA P-256.\n   - Отправляет заказ в ACME API Let's Encrypt.\n   - Активирует обработчик challenge токена.\n   - Дожидается валидации и скачивает выпущенный сертификат.\n   - Сохраняет сертификат в файловый кэш и отдает клиенту.",
    "pitfalls": "1. **Отсутствие DirCache (кэша на диске):** При перезапуске сервиса или рестарте контейнера без постоянного тома (PVC) сервис будет запрашивать новый сертификат при каждом запуске. Вы мгновенно исчерпаете лимит Let's Encrypt (5 сертификатов на домен в неделю) и ваш сервис окажется недоступным на 7 дней!\n2. **Блокировка 80 порта файрволом:** Let's Encrypt обращается строго на 80-й порт для валидации HTTP-01 challenge. Закрытие 80 порта приведет к тайм-ауту проверки.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему в Kubernetes для управления сертификатами предпочитают cert-manager вместо использования встроенного autocert в Go коде бэкенда?\n**Ответ:** 1) **Разделение ответственности:** Бэкенду не нужны привилегии слушать 80/443 порты и доступ к DNS-провайдерам. 2) **Координация в реплицированных подах:** Если сервис масштабирован на 20 реплик, встроенный `autocert` вызовет состояние гонки (20 подов одновременно запросят сертификаты для одного домена). `cert-manager` работает как централизованный Kubernetes Controller: выпускает сертификат один раз, сохраняет его в `Secret`, после чего Ingress Controller (Nginx, Traefik, Envoy) монтирует его для всех подов."
  },
  {
    "num": 96,
    "title": "TLS Session Resumption: Session Tickets (RFC 5077) и оптимизация Handshake Latency",
    "task": "**TLS session tickets**: Настройте session resumption для снижения handshake latency.",
    "theory": "Полное TLS-рукопожатие (Full Handshake) требует от 1 до 2 сетевых RTT (Round Trip Time) и тяжелых криптографических вычислений асимметричной математики (ECDHE / RSA). При пинге до мобильного клиента 50–100 мс рукопожатие может занимать 200–300 мс до отправки первого полезного байта данных.\n\n**TLS Session Resumption (возобновление сессии):**\n1. **Session IDs (RFC 5246):** Сервер сохраняет ID сессии в оперативной памяти (Stateful). Не масштабируется на распределенные кластеры без распределенного кэша Redis/Memcached.\n2. **Session Tickets (RFC 5077 / TLS 1.3 PSK):** Сервер шифрует параметры сессии (сессионный ключ, версию TLS, шифр) специальным симметричным ключом сервера (Session Ticket Encryption Key, STEK) и отдает зашифрованный тикет клиенту (Stateless). При повторном визите клиент отправляет тикет обратно, сервер расшифровывает его локально и возобновляет защищенную сессию за **0-RTT или 1-RTT**!\n\nВ Go в `tls.Config`:\n- `SessionTicketsDisabled: false` (включено по умолчанию).\n- `SetSessionTicketKeys([][32]byte)` — установка и ротация ключей шифрования тикетов для группы серверов.",
    "step_by_step": "1. Сгенерируйте общий 32-байтный сессионный ключ STEK.\n2. Настройте `tls.Config` сервера с установкой этого ключа через `SetSessionTicketKeys`.\n3. Сконфигурируйте клиента с включенным `ClientSessionCache` (`tls.NewLRUClientSessionCache(64)`).\n4. Выполните первое подключение (Full Handshake) и зафиксируйте факт отсутствия возобновления (`DidResume = false`).\n5. Выполните второе подключение по тому же адресу и зафиксируйте успешное возобновление сессии (`DidResume = true`).",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"time\"\n)\n\nfunc main() {\n\t// 1. Генерация серверного TLS сертификата\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(1001),\n\t\tSubject:      pkix.Name{CommonName: \"127.0.0.1\"},\n\t\tIPAddresses:  []net.IP{net.ParseIP(\"127.0.0.1\")},\n\t\tNotBefore:    time.Now().Add(-time.Hour),\n\t\tNotAfter:     time.Now().Add(time.Hour),\n\t}\n\tder, _ := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\tserverCert := tls.Certificate{Certificate: [][]byte{der}, PrivateKey: priv}\n\n\t// 2. Единый ключ шифрования сессионных тикетов (STEK)\n\t// В кластере этот ключ синхронизируется между всеми инстансами\n\tvar sessionTicketKey [32]byte\n\trand.Read(sessionTicketKey[:])\n\n\tserverCfg := &tls.Config{\n\t\tCertificates:           []tls.Certificate{serverCert},\n\t\tSessionTicketsDisabled: false, // Включаем Session Tickets\n\t}\n\tserverCfg.SetSessionTicketKeys([][32]byte{sessionTicketKey})\n\n\t// 3. Запуск сервера\n\tlistener, err := tls.Listen(\"tcp\", \"127.0.0.1:0\", serverCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка слушателя: %v\", err)\n\t}\n\tdefer listener.Close()\n\tserverAddr := listener.Addr().String()\n\n\tgo func() {\n\t\tfor {\n\t\t\tconn, err := listener.Accept()\n\t\t\tif err != nil {\n\t\t\t\treturn\n\t\t\t}\n\t\t\tgo func(c net.Conn) {\n\t\t\t\tdefer c.Close()\n\t\t\t\ttlsConn := c.(*tls.Conn)\n\t\t\t\t_ = tlsConn.Handshake()\n\t\t\t}(conn)\n\t\t}\n\t}()\n\n\t// 4. Настройка клиента с LRU-кэшем сессий\n\tclientCache := tls.NewLRUClientSessionCache(32)\n\tclientCfg := &tls.Config{\n\t\tInsecureSkipVerify: true,\n\t\tClientSessionCache: clientCache, // Кэшируем тикеты для повторного использования\n\t}\n\n\t// Первое рукопожатие: Full Handshake\n\tconn1, err := tls.Dial(\"tcp\", serverAddr, clientCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка соединения 1: %v\", err)\n\t}\n\tstate1 := conn1.ConnectionState()\n\tconn1.Close()\n\n\tfmt.Println(\"=== Первое рукопожатие (Начальная сессия) ===\")\n\tfmt.Printf(\"Версия TLS:     %s\\n\", tls.VersionName(state1.Version))\n\tfmt.Printf(\"Сессия возобновлена (DidResume): %v (Ожидался полный Full Handshake)\\n\", state1.DidResume)\n\n\t// Второе рукопожатие: Resumed Session через Session Ticket\n\tconn2, err := tls.Dial(\"tcp\", serverAddr, clientCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка соединения 2: %v\", err)\n\t}\n\tstate2 := conn2.ConnectionState()\n\tconn2.Close()\n\n\tfmt.Println(\"\\n=== Второе рукопожатие (Повторное подключение) ===\")\n\tfmt.Printf(\"Версия TLS:     %s\\n\", tls.VersionName(state2.Version))\n\tfmt.Printf(\"Сессия возобновлена (DidResume): %v (Сессия мгновенно восстановлена!)\\n\", state2.DidResume)\n}",
        "note": "Демонстрация TLS Session Resumption через Session Tickets и client.ClientSessionCache"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Первое рукопожатие (Начальная сессия) ===\n# Версия TLS:     TLS 1.3\n# Сессия возобновлена (DidResume): false (Ожидался полный Full Handshake)\n# \n# === Второе рукопожатие (Повторное подключение) ===\n# Версия TLS:     TLS 1.3\n# Сессия возобновлена (DidResume): true (Сессия мгновенно восстановлена!)"
      }
    ],
    "under_the_hood": "Под капотом Session Tickets в Go:\n1. Ключ тикета `[32]byte` делится на две части: первые 16 байт — ключ AES-128 (или AES-256), вторые 16 байт — ключ HMAC-SHA256.\n2. В конце первого успешного рукопожатия сервер генерирует сообщение `NewSessionTicket`. Сервер упаковывает структуру сессии (Master Secret, Client Identity, CipherSuite), шифрует её через AES-GCM/CBC и подписывает HMAC с помощью STEK.\n3. Клиент сохраняет полученный зашифрованный блоб в памяти (`ClientSessionCache`).\n4. При повторном коннекте клиент отправляет этот тикет в расширении `session_ticket` (или PSK identities в TLS 1.3).\n5. Сервер расшифровывает тикет своим STEK без обращения к БД и без повторного выполнения дорогостоящих криптографических операций на эллиптических кривых.",
    "pitfalls": "1. **Отсутствие ротации ключей STEK:** Если STEK статичен и никогда не меняется, компрометация этого ключа в будущем позволит злоумышленнику расшифровать все сессии, сохраненные с использованием этих тикетов, нарушая Forward Secrecy. В Go `SetSessionTicketKeys` принимает срез ключей: первый используется для шифрования, остальные — только для расшифрования старых сессий.\n2. **Атаки повторного воспроизведения (0-RTT Replay Attack):** В TLS 1.3 данные раннего старта 0-RTT не защищены от повторной отправки злоумышленником. Неидемпотентные POST-запросы (списание денег) категорически запрещено обрабатывать в 0-RTT.",
    "bigtech_interview": "**Вопрос с собеседования:** В чем угроза нарушения Perfect Forward Secrecy при использовании статических ключей Session Ticket Encryption Key (STEK) на балансировщиках нагрузки?\n**Ответ:** Если злоумышленник перехватывает и записывает весь входящий трафик дата-центра, а через 6 месяцев похищает со статически сконфигурированного балансировщика ключ STEK, он сможет расшифровать тикеты всех сессий, извлечь из них закэшированные Master Secret и полностью расшифровать весь терабайтный архив перехваченного TLS-трафика. Чтобы этого не произошло, ключи STEK обязаны автоматически ротироваться каждые несколько часов с безопасным уничтожением старых ключей из оперативной памяти."
  },
  {
    "num": 97,
    "title": "SNI (Server Name Indication): виртуальный хостинг HTTPS через GetCertificate",
    "task": "**SNI (Server Name Indication)**: Настройте ваш TLS-сервер для хостинга нескольких HTTPS сайтов на одном IP через SNI.",
    "theory": "Исторически протокол SSL/TLS требовал выделения отдельного IPv4 адреса для каждого защищенного доменного имени, поскольку сервер должен был отправить свой сертификат до того, как клиент передаст HTTP-заголовок `Host`. В условиях дефицита IPv4 это делало виртуальный хостинг невозможным.\n\n**SNI (Server Name Indication, RFC 6066):**\nКлиент указывает запрашиваемое доменное имя (например, `api.shop.ru` или `admin.internal.net`) открытым текстом прямо в первом сетевом пакете рукопожатия — расширении `server_name` сообщения `ClientHello`.\n\nВ Go в структуре `tls.Config` для поддержки динамического SNI используется колбэк:\n`GetCertificate: func(info *tls.ClientHelloInfo) (*tls.Certificate, error)`\n1. При поступлении запроса Go вызывает колбэк, передавая `info.ServerName`.\n2. Сервер анализирует имя хоста и возвращает соответствующий сертификат `*tls.Certificate`.\n3. Это позволяет на одном IP-адресе и порту 443 обслуживать тысячи разных доменов с независимыми сертификатами и ключами, а также динамически подгружать сертификаты из памяти или Vault без перезапуска сервиса.",
    "step_by_step": "1. Сгенерируйте две независимые пары сертификатов для двух разных доменов: `alpha.local` и `beta.local`.\n2. Создайте реестр сертификатов `map[string]*tls.Certificate`.\n3. Настройте функцию `GetCertificate` в `tls.Config`, выполняющую поиск сертификата по `info.ServerName`.\n4. Запустите TLS-сервер на одном IP-адресе.\n5. Продемонстрируйте подключение клиента к `alpha.local` и убедитесь, что сервер возвращает сертификат Alpha.\n6. Подключитесь к `beta.local` и убедитесь, что сервер возвращает сертификат Beta.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/rsa\"\n\t\"crypto/tls\"\n\t\"crypto/x509\"\n\t\"crypto/x509/pkix\"\n\t\"fmt\"\n\t\"log\"\n\t\"math/big\"\n\t\"net\"\n\t\"sync\"\n\t\"time\"\n)\n\n// CertRegistry хранит сертификаты для виртуального хостинга SNI\ntype CertRegistry struct {\n\tmu    sync.RWMutex\n\tcerts map[string]*tls.Certificate\n}\n\nfunc (r *CertRegistry) GetCertificate(hello *tls.ClientHelloInfo) (*tls.Certificate, error) {\n\tr.mu.RLock()\n\tdefer r.mu.RUnlock()\n\n\tdomain := hello.ServerName\n\tcert, exists := r.certs[domain]\n\tif !exists {\n\t\treturn nil, fmt.Errorf(\"сертификат для домена %q не найден\", domain)\n\t}\n\n\treturn cert, nil\n}\n\n// вспомогательная функция генерации тестового сертификата для домена\nfunc generateCertForDomain(domain string) *tls.Certificate {\n\tpriv, _ := rsa.GenerateKey(rand.Reader, 2048)\n\ttemplate := x509.Certificate{\n\t\tSerialNumber: big.NewInt(time.Now().UnixNano()),\n\t\tSubject:      pkix.Name{CommonName: domain},\n\t\tDNSNames:     []string{domain},\n\t\tIPAddresses:  []net.IP{net.ParseIP(\"127.0.0.1\")},\n\t\tNotBefore:    time.Now().Add(-time.Hour),\n\t\tNotAfter:     time.Now().Add(time.Hour),\n\t\tKeyUsage:     x509.KeyUsageDigitalSignature | x509.KeyUsageKeyEncipherment,\n\t\tExtKeyUsage:  []x509.ExtKeyUsage{x509.ExtKeyUsageServerAuth},\n\t}\n\tder, _ := x509.CreateCertificate(rand.Reader, &template, &template, &priv.PublicKey, priv)\n\treturn &tls.Certificate{Certificate: [][]byte{der}, PrivateKey: priv}\n}\n\nfunc main() {\n\t// 1. Инициализируем реестр сертификатов\n\tregistry := &CertRegistry{\n\t\tcerts: make(map[string]*tls.Certificate),\n\t}\n\n\tregistry.certs[\"alpha.corp.local\"] = generateCertForDomain(\"alpha.corp.local\")\n\tregistry.certs[\"beta.corp.local\"] = generateCertForDomain(\"beta.corp.local\")\n\n\t// 2. Настраиваем сервер с SNI колбэком\n\tserverCfg := &tls.Config{\n\t\tGetCertificate: registry.GetCertificate,\n\t}\n\n\tlistener, err := tls.Listen(\"tcp\", \"127.0.0.1:0\", serverCfg)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка запуска сервера: %v\", err)\n\t}\n\tdefer listener.Close()\n\tserverAddr := listener.Addr().String()\n\n\tgo func() {\n\t\tfor {\n\t\t\tconn, err := listener.Accept()\n\t\t\tif err != nil {\n\t\t\t\treturn\n\t\t\t}\n\t\t\tgo func(c net.Conn) {\n\t\t\t\tdefer c.Close()\n\t\t\t\ttlsConn := c.(*tls.Conn)\n\t\t\t\t_ = tlsConn.Handshake()\n\t\t\t}(conn)\n\t\t}\n\t}()\n\n\t// 3. Тест SNI для alpha.corp.local\n\tcfgAlpha := &tls.Config{\n\t\tInsecureSkipVerify: true,\n\t\tServerName:         \"alpha.corp.local\", // Передаем домен в SNI\n\t}\n\tconnA, err := tls.Dial(\"tcp\", serverAddr, cfgAlpha)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подключения к Alpha: %v\", err)\n\t}\n\tcertA, _ := x509.ParseCertificate(connA.ConnectionState().PeerCertificates[0])\n\tconnA.Close()\n\n\tfmt.Printf(\"Подключение к alpha.corp.local -> Получен сертификат: %s\\n\", certA.Subject.CommonName)\n\n\t// 4. Тест SNI для beta.corp.local\n\tcfgBeta := &tls.Config{\n\t\tInsecureSkipVerify: true,\n\t\tServerName:         \"beta.corp.local\", // Передаем другой домен в SNI\n\t}\n\tconnB, err := tls.Dial(\"tcp\", serverAddr, cfgBeta)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка подключения к Beta: %v\", err)\n\t}\n\tcertB, _ := x509.ParseCertificate(connB.ConnectionState().PeerCertificates[0])\n\tconnB.Close()\n\n\tfmt.Printf(\"Подключение к beta.corp.local  -> Получен сертификат: %s\\n\", certB.Subject.CommonName)\n}",
        "note": "Динамический виртуальный HTTPS хостинг с помощью tls.Config.GetCertificate и SNI"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# Подключение к alpha.corp.local -> Получен сертификат: alpha.corp.local\n# Подключение к beta.corp.local  -> Получен сертификат: beta.corp.local"
      }
    ],
    "under_the_hood": "Внутренний механизм обработки SNI в рантайме Go:\n1. Во время чтения первого фрейма `record` типа `Handshake` парсер разбирает тело `ClientHello`.\n2. Если в `ClientHello` найдено расширение `extensionServerName` (тип 0), значение строки хостнейма сохраняется в структуре `tls.ClientHelloInfo.ServerName`.\n3. Рантайм проверяет, задано ли поле `GetCertificate`. Если да, вызывается колбэк.\n4. Если `GetCertificate` возвращает `nil`, рантайм падает на поиск лучшего совпадения в статическом срезе `tls.Config.Certificates`.\n5. Если ни один сертификат не найден или колбэк вернул ошибку, рукопожатие немедленно прерывается отправкой алерта `tls.AlertUnrecognizedName`.",
    "pitfalls": "1. **Блокирующие I/O вызовы внутри GetCertificate:** Колбэк `GetCertificate` вызывается синхронно во время рукопожатия TLS. Если делать внутри него синхронный HTTP-запрос в базу или Vault без кэширования, рукопожатие заблокирует сетевой поток на сотни миллисекунд и вызовет DoS-атаку на сервер. Сертификаты должны кэшироваться в памяти.\n2. **Отсутствие fallback-сертификата по умолчанию:** Если клиент обратится по чистому IP-адресу без указания SNI (например, сканеры уязвимостей или старые клиенты), `info.ServerName` будет пустой строкой `\"\"`. Всегда предусматривайте сертификат по умолчанию (default fallback).",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое ECH (Encrypted Client Hello) и какую фундаментальную проблему приватности SNI он решает?\n**Ответ:** Классический SNI передается в открытом незашифрованном виде в сообщении `ClientHello`, так как рукопожатие еще не началось. Это позволяет провайдерам, сетевым шпионам и цензорам видеть, к какому именно домену подключается пользователь (даже если весь последующий HTTP-трафик зашифрован TLS). ECH (ранее ESNI) решает эту проблему: публичный ключ шифрования сервера публикуется в защищенной DNS HTTPS RR записи, и клиент шифрует всё сообщение `ClientHello` (включая SNI) симметричным ключом до начала рукопожатия, обеспечивая абсолютную приватность метаданных соединения."
  },
  {
    "num": 98,
    "title": "Низкоуровневая безопасность памяти: Zeroing Secrets, crypto/subtle и runtime.KeepAlive",
    "task": "**Блок 10 (Шифрование)**: — начните сразу после Блока 4 (`unsafe`), так как криптографические пакеты активно используют `unsafe` для zero-copy и constant-time операций.",
    "theory": "При разработке криптографических систем высокой надежности безопасность определяется не только математикой алгоритмов, но и поведением компилятора и операционной системы при управлении памятью.\n\nТри критических фактора низкоуровневой криптобезопасности:\n1. **Гарантированное зануление памяти (Zeroing Secrets):** Обычное обнуление среза `data[i] = 0` или `clear(data)` компилятор может вырезать в процессе оптимизации (Dead Store Elimination), если переменная больше не используется в функции. Для надежного затирания ключей из памяти перед освобождением требуются барьеры памяти или функции вроде `subtle.ConstantTimeCopy`.\n2. **Сравнение в константном времени (Constant-Time Operations):** Оператор `==` или `bytes.Equal` прерывает цикл на первом несовпавшем байте. Злоумышленник по наносекундным замерам времени ответов восстанавливает побайтово пароли, HMAC-подписи и токены (Timing Attacks). Пакет `crypto/subtle` реализует операции с постоянным временем: `subtle.ConstantTimeCompare`, `subtle.ConstantTimeByteEq`.\n3. **Предотвращение ранней сборки мусора (runtime.KeepAlive):** При работе с `unsafe.Pointer` сборщик мусора GC может посчитать структуру мертвой и освободить/переместить память прямо во время выполнения низкоуровневого системного вызова. `runtime.KeepAlive` защищает объект от деаллокации до окончания операции.",
    "step_by_step": "1. Продемонстрируйте уязвимость стандартного сравнения байт `bytes.Equal` к тайминг-атакам и замените его на `subtle.ConstantTimeCompare`.\n2. Напишите безопасную функцию `ZeroMemory`, которая затирает приватные ключи в памяти с защитой от вырезания компилятором.\n3. Продемонстрируйте использование `runtime.KeepAlive` для удержания памяти с секретом до завершения криптографической операции.\n4. Создайте безопасный контейнер секрета `SecureKey`, реализующий паттерн безопасного уничтожения `Destroy()`.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/subtle\"\n\t\"fmt\"\n\t\"io\"\n\t\"runtime\"\n\t\"unsafe\"\n)\n\n// SecureKey инкапсулирует криптографический ключ с гарантированным затиранием памяти\ntype SecureKey struct {\n\tkey []byte\n}\n\n// NewSecureKey аллоцирует ключ заданной длины\nfunc NewSecureKey(size int) (*SecureKey, error) {\n\tbuf := make([]byte, size)\n\tif _, err := io.ReadFull(rand.Reader, buf); err != nil {\n\t\treturn nil, err\n\t}\n\treturn &SecureKey{key: buf}, nil\n}\n\n// Destroy надежно стирает секретные байты из памяти\nfunc (s *SecureKey) Destroy() {\n\tif s.key == nil {\n\t\treturn\n\t}\n\t// 1. Зануляем каждый байт в памяти\n\tfor i := range s.key {\n\t\ts.key[i] = 0\n\t}\n\t// 2. Барьер компилятора: читаем через unsafe указатель, предотвращая Dead Store Elimination\n\tptr := unsafe.Pointer(&s.key[0])\n\t_ = *(*byte)(ptr)\n\n\t// 3. runtime.KeepAlive гарантирует, что срез не будет собран GC до завершения затирания\n\truntime.KeepAlive(s.key)\n\ts.key = nil\n}\n\nfunc main() {\n\t// 1. Демонстрация Constant-Time сравнения\n\ttokenA := []byte(\"secret-auth-token-super-secure-hash-99128\")\n\ttokenB := []byte(\"secret-auth-token-super-secure-hash-99128\")\n\ttokenEvil := []byte(\"secret-auth-token-super-secure-hash-00000\")\n\n\t// subtle.ConstantTimeCompare возвращает 1 при равенстве и 0 при несовпадении\n\tisEqual := subtle.ConstantTimeCompare(tokenA, tokenB) == 1\n\tisEvilEqual := subtle.ConstantTimeCompare(tokenA, tokenEvil) == 1\n\n\tfmt.Println(\"=== Constant-Time сравнение (crypto/subtle) ===\")\n\tfmt.Printf(\"Сравнение токенов A и B: %v (защищено от Timing Attacks)\\n\", isEqual)\n\tfmt.Printf(\"Сравнение токенов A и Evil: %v\\n\", isEvilEqual)\n\n\t// 2. Демонстрация работы SecureKey и уничтожения памяти\n\tsecKey, err := NewSecureKey(32)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\n\tfmt.Println(\"\\n=== Безопасная работа с памятью ключа ===\")\n\tfmt.Printf(\"Сгенерирован приватный ключ DEK (первые 4 байта): %x...\\n\", secKey.key[:4])\n\n\t// Затираем память после использования\n\tsecKey.Destroy()\n\tfmt.Println(\"Вызов secKey.Destroy() завершен: память гарантированно занулена.\")\n\tfmt.Printf(\"Состояние ссылки на ключ: %v\\n\", secKey.key)\n}",
        "note": "Защита от timing-атак через crypto/subtle, zeroing памяти и runtime.KeepAlive"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Constant-Time сравнение (crypto/subtle) ===\n# Сравнение токенов A и B: true (защищено от Timing Attacks)\n# Сравнение токенов A и Evil: false\n# \n# === Безопасная работа с памятью ключа ===\n# Сгенерирован приватный ключ DEK (первые 4 байта): f3a1c8b2...\n# Вызов secKey.Destroy() завершен: память гарантированно занулена.\n# Состояние ссылки на ключ: []"
      }
    ],
    "under_the_hood": "Низкоуровневая реализация `subtle.ConstantTimeCompare`:\n```go\nfunc ConstantTimeCompare(x, y []byte) int {\n    if len(x) != len(y) {\n        return 0\n    }\n    var v byte\n    for i := 0; i < len(x); i++ {\n        v |= x[i] ^ y[i]\n    }\n    return ConstantTimeByteEq(v, 0)\n}\n```\nОбратите внимание:\n1. Цикл пробегает **абсолютно все байты** среза без досрочного выхода (`break`).\n2. Операция `^` (XOR) дает 0 только при побитовом совпадении.\n3. Побитовое ИЛИ (`v |= ...`) накапливает любое отличие.\n4. Процессор тратит строго одинаковое количество тактов независимо от того, где произошла ошибка — в первом байте или в последнем, исключая возможность снятия тайминг-профиля.",
    "pitfalls": "1. **Проверка длины до ConstantTimeCompare:** Если длины секретных строк различаются, `ConstantTimeCompare` сразу возвращает 0. Если длина секрета сама по себе является тайной, утечка длины через замер времени может быть использована злоумышленником.\n2. **Свопинг памяти на диск (Swap):** Даже если память занулена при выходе из функции, операционная система могла сбросить страницы RAM с ключом в swap-раздел на SSD/HDD до вызова `Destroy`. В критических криптографических демонах страницы памяти закрепляют вызовом системного вызова `mlock` (`unix.Mlock`).",
    "bigtech_interview": "**Вопрос с собеседования:** Что такое Dead Store Elimination в оптимизирующем компиляторе Go и почему обычный вызов `clear(key)` перед выходом из функции может не защитить секрет в памяти?\n**Ответ:** Оптимизатор компилятора (SSA dead code elimination) анализирует поток управления (liveness analysis). Если массив байт перезаписывается нулями (`for i := range key { key[i] = 0 }`), но после этого переменная `key` ни разу не читается и выходит из области видимости, компилятор считает эти записи «мертвыми» (dead store) и полностью удаляет машинные инструкции записи нулей из итогового бинарного файла. В результате секретные байты ключа остаются лежать в оперативной памяти до тех пор, пока эта память не будет перезаписана другими аллокациями, что делает их уязвимыми к Heartbleed-подобным утечкам или считыванию из Core Dump."
  },
  {
    "num": 99,
    "title": "Криптографические функции формирования ключа (KDF): PBKDF2, scrypt и Argon2id",
    "task": "**Блок 11 (Хеши)**: — параллельно с Блоком 10, так как KDF тесно связан с симметричным шифрованием.",
    "theory": "Обычные криптографические хэш-функции (SHA-256, SHA-3, BLAKE3) оптимизированы для максимальной скорости вычислений (миллиарды хэшей в секунду на современных GPU и ASIC). Использование быстрого хэша для вычисления ключа шифрования из пользовательского пароля катастрофично: злоумышленник подберет 8-значный пароль за считанные минуты методом прямого перебора (brute-force).\n\n**Key Derivation Functions (KDF / Password-Based KDF):**\nСпециально разработанные ресурсоемкие алгоритмы, замедляющие подбор:\n1. **PBKDF2 (RFC 2898):** Тысячи итераций HMAC-SHA256 (CPU-hard). Уязвим к параллельному подбору на специализированных чипах (ASIC / FPGA), так как требует минимального объема RAM.\n2. **scrypt (RFC 7914):** Memory-hard алгоритм, требующий значительного объема памяти (мегабайты RAM), что резко удорожает создание аппаратных майнеров.\n3. **Argon2id (RFC 9106):** Победитель Password Hashing Competition (PHC). Золотой стандарт современности. Обеспечивает комбинированную защиту как от атак по времени/сторонним каналам (Data-independent memory access из Argon2i), так и от параллельного подбора на GPU (Data-dependent memory access из Argon2d).\n\nВ Go пакет `golang.org/x/crypto/argon2` предоставляет эталонную реализацию `argon2.IDKey`.",
    "step_by_step": "1. Определите параметры Argon2id: `time` (число итераций), `memory` (объем ОЗУ в КБ), `threads` (параллелизм), `keyLen` (длина результирующего ключа).\n2. Сгенерируйте криптографическую случайную соль длиной 16 байт с помощью `crypto/rand`.\n3. Вызовите `argon2.IDKey` для генерации 32-байтного симметричного ключа шифрования AES-256 из мастер-пароля.\n4. Продемонстрируйте использование сгенерированного ключа в шифровании AES-GCM.\n5. Продемонстрируйте работу `pbkdf2.Key` из `golang.org/x/crypto/pbkdf2` для понимания различий.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/aes\"\n\t\"crypto/cipher\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"fmt\"\n\t\"io\"\n\t\"log\"\n\t\"time\"\n\n\t\"golang.org/x/crypto/argon2\"\n\t\"golang.org/x/crypto/pbkdf2\"\n)\n\n// DeriveKeyArgon2id формирует симметричный ключ высокой криптостойкости\nfunc DeriveKeyArgon2id(password string, salt []byte) []byte {\n\t// Рекомендованные параметры OWASP:\n\t// time: 1 итерация\n\t// memory: 64 * 1024 КБ (64 МБ RAM)\n\t// threads: 4 параллельных потока\n\t// keyLen: 32 байта (256 бит для AES-256)\n\treturn argon2.IDKey([]byte(password), salt, 1, 64*1024, 4, 32)\n}\n\nfunc main() {\n\tuserPassword := \"Correct-Horse-Battery-Staple-2026!#\"\n\n\t// 1. Генерация случайной криптографической соли (минимум 16 байт)\n\tsalt := make([]byte, 16)\n\tif _, err := io.ReadFull(rand.Reader, salt); err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации соли: %v\", err)\n\t}\n\n\t// 2. Формирование ключа через Argon2id\n\tt0 := time.Now()\n\taesKeyArgon := DeriveKeyArgon2id(userPassword, salt)\n\targonDuration := time.Since(t0)\n\n\tfmt.Println(\"=== Генерация ключа через Argon2id (RFC 9106) ===\")\n\tfmt.Printf(\"Время вычисления (Memory-hard): %v\\n\", argonDuration)\n\tfmt.Printf(\"Размер сгенерированного ключа:  %d байт (256 бит)\\n\", len(aesKeyArgon))\n\tfmt.Printf(\"Полученный AES-256 ключ (hex):   %x\\n\\n\", aesKeyArgon)\n\n\t// 3. Для сравнения: PBKDF2\n\tt0 = time.Now()\n\taesKeyPBKDF2 := pbkdf2.Key([]byte(userPassword), salt, 100_000, 32, sha256.New)\n\tpbkdf2Duration := time.Since(t0)\n\n\tfmt.Println(\"=== Генерация ключа через PBKDF2 (100k итераций) ===\")\n\tfmt.Printf(\"Время вычисления (CPU-hard):    %v\\n\", pbkdf2Duration)\n\tfmt.Printf(\"Полученный PBKDF2 ключ (hex):   %x\\n\\n\", aesKeyPBKDF2)\n\n\t// 4. Использование ключа Argon2id в шифровании AES-GCM\n\tblock, _ := aes.NewCipher(aesKeyArgon)\n\tgcm, _ := cipher.NewGCM(block)\n\tnonce := make([]byte, gcm.NonceSize())\n\tio.ReadFull(rand.Reader, nonce)\n\n\tsecretDocument := []byte(\"Конфиденциальный план реструктуризации на 2027 год\")\n\tciphertext := gcm.Seal(nil, nonce, secretDocument, nil)\n\tfmt.Printf(\"Документ успешно зашифрован ключом из пароля: шифротекст %d байт\\n\", len(ciphertext))\n\n\t// Проверка расшифрования\n\tdecrypted, err := gcm.Open(nil, nonce, ciphertext, nil)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка расшифрования: %v\", err)\n\t}\n\tfmt.Printf(\"Успешно расшифровано: %s\\n\", string(decrypted))\n}",
        "note": "Формирование криптографических ключей из паролей с помощью Argon2id и PBKDF2"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Генерация ключа через Argon2id (RFC 9106) ===\n# Время вычисления (Memory-hard): 68.4ms\n# Размер сгенерированного ключа:  32 байт (256 бит)\n# Полученный AES-256 ключ (hex):   e47d2f...\n# \n# === Генерация ключа через PBKDF2 (100k итераций) ===\n# Время вычисления (CPU-hard):    54.1ms\n# Полученный PBKDF2 ключ (hex):   89b1c0...\n# \n# Документ успешно зашифрован ключом из пароля: шифротекст 114 байт\n# Успешно расшифровано: Конфиденциальный план реструктуризации на 2027 год"
      }
    ],
    "under_the_hood": "Почему Argon2id непревзойден против атак на видеокартах (GPU):\n1. **Память-ориентированная архитектура (Memory-Hardness):** Для вычисления одного ключа Argon2id с параметром `memory: 64*1024` требуется выделить непрерывный блок памяти ровно 64 МБ и производить множество обращений к случайным ячейкам этого блока.\n2. **Ограничение видеокарт:** Современная топовая видеокарта с 24 ГБ VRAM может одновременно разместить в памяти не более 375 параллельных потоков вычисления Argon2id ($24\\,000 / 64 = 375$).\n3. Для сравнения, PBKDF2 требует всего несколько байт оперативной памяти, поэтому та же видеокарта может запускать миллионы параллельных вычислительных ядер CUDA, подбирая пароли в миллионы раз быстрее. Argon2id сводит преимущество GPU/ASIC к нулю.",
    "pitfalls": "1. **Использование статической соли:** Соль обязана быть уникальной для каждого шифрования/пользователя. Если соль захардкожена в коде, злоумышленник может построить радужные таблицы (Rainbow Tables) или параллельно взламывать миллионы хранилищ. Соль сохраняется в открытом виде рядом с шифротекстом.\n2. **Слишком слабые параметры памяти:** Настройка `memory: 1024` (1 МБ) сводит на нет защиту Argon2id, делая его уязвимым к подбору на видеокартах.",
    "bigtech_interview": "**Вопрос с собеседования:** В чем ключевое различие между Argon2i, Argon2d и Argon2id, и почему для KDF и паролей рекомендован именно Argon2id?\n**Ответ:** 1) **Argon2d** выбирает адреса памяти в зависимости от промежуточных значений хэша (Data-dependent). Это обеспечивает максимальную защиту от GPU-перебора, но делает алгоритм уязвимым к атакам по сторонним каналам и задержкам доступа к кэшу процессора (Side-channel cache attacks). 2) **Argon2i** обращается к памяти по фиксированным индексам, независимым от секретных данных (Data-independent), защищая от тайминг-атак, но давая чуть меньшую защиту от GPU. 3) **Argon2id** гибридный: первая половина первой итерации выполняется как Argon2i (защита от side-channel), а последующие — как Argon2d (защита от GPU перебора). Поэтому RFC 9106 и OWASP рекомендуют Argon2id."
  },
  {
    "num": 100,
    "title": "Промышленная архитектура подписанных токенов: PASETO v4.public на Ed25519 против JWT",
    "task": "**Блок 12 (Токены)**: — после освоения RSA/Ed25519 и HMAC, так как JWT/PASETO/OAuth2 — это прикладное использование этих примитивов.",
    "theory": "Стандарт **JSON Web Token (JWT, RFC 7519)** на протяжении десятилетия страдал от критических архитектурных уязвимостей, вызванных фатальным дефектом проектирования: алгоритм проверки передается внутри заголовка самого токена (`\"alg\": \"none\"`, смена асимметричного RS256 на симметричный HS256 с открытым ключом сервера в качестве секрета).\n\n**PASETO (Platform-Agnostic Security Tokens):**\nСовременная криптографически стойкая альтернатива JWT, свободная от недостатков алгоритмической гибкости (Cipher Suite Negotiation Vulnerabilities):\n1. **Жесткие версии и режимы:** Нет заголовка с выбором алгоритма.\n   - `v4.local`: Симметричное шифрование (XChaCha20-Poly1305 + BLAKE2b KDF).\n   - `v4.public`: Асимметричная цифровая подпись на базе **Ed25519** (RFC 8032).\n2. **Аутентификация метаданных (Footer):** В отличие от незащищенного заголовка JWT, незашифрованный футер в PASETO (например, `kid` идентификатор ключа) криптографически связан с подписью через PAE (Pre-Authentication Encoding).\n3. **Безопасность по умолчанию:** Невозможно подменить алгоритм, отключить валидацию или провести атаку смешения ключей.",
    "step_by_step": "1. Создайте структуру полезной нагрузки токена `TokenClaims` со сроком действия, идентификатором пользователя и ролью.\n2. Сгенерируйте мастер-ключи Ed25519 (`ed25519.GenerateKey`).\n3. Реализуйте функцию сериализации `Pre-Authentication Encoding (PAE)` в соответствии со спецификацией PASETO.\n4. Напишите функцию `SignPasetoV4Public`, формирующую токен формата `v4.public.<payload_base64_url>.<signature_base64_url>`.\n5. Напишите функцию верификатора `VerifyPasetoV4Public`, проверяющую подпись Ed25519 и срок действия.\n6. Продемонстрируйте отказ при попытке подделки полезной нагрузки или при истечении времени жизни.",
    "code_blocks": [
      {
        "filename": "main.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"crypto/ed25519\"\n\t\"crypto/rand\"\n\t\"encoding/base64\"\n\t\"encoding/binary\"\n\t\"encoding/json\"\n\t\"errors\"\n\t\"fmt\"\n\t\"log\"\n\t\"strings\"\n\t\"time\"\n)\n\n// TokenClaims представляет полезную нагрузку авторизационного токена\ntype TokenClaims struct {\n\tSubject   string    `json:\"sub\"`\n\tRole      string    `json:\"role\"`\n\tIssuedAt  time.Time `json:\"iat\"`\n\tExpiresAt time.Time `json:\"exp\"`\n}\n\n// pae (Pre-Authentication Encoding) защищает от атак каноникализации\nfunc pae(pieces ...[]byte) []byte {\n\tvar countBuf [8]byte\n\tbinary.LittleEndian.PutUint64(countBuf[:], uint64(len(pieces)))\n\tout := append([]byte{}, countBuf[:]...)\n\n\tfor _, piece := range pieces {\n\t\tvar lenBuf [8]byte\n\t\tbinary.LittleEndian.PutUint64(lenBuf[:], uint64(len(piece)))\n\t\tout = append(out, lenBuf[:]...)\n\t\tout = append(out, piece...)\n\t}\n\treturn out\n}\n\n// SignPasetoV4Public формирует токен PASETO v4.public на базе Ed25519\nfunc SignPasetoV4Public(privKey ed25519.PrivateKey, claims *TokenClaims) (string, error) {\n\theader := []byte(\"v4.public.\")\n\tpayloadJSON, err := json.Marshal(claims)\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\n\t// Формируем подписанный материал согласно спецификации PASETO v4\n\tsignData := pae(header, payloadJSON, nil)\n\tsig := ed25519.Sign(privKey, signData)\n\n\tbody := append(payloadJSON, sig...)\n\ttoken := string(header) + base64.RawURLEncoding.EncodeToString(body)\n\treturn token, nil\n}\n\n// VerifyPasetoV4Public проверяет подпись Ed25519 и срок действия токена\nfunc VerifyPasetoV4Public(pubKey ed25519.PublicKey, token string) (*TokenClaims, error) {\n\tif !strings.HasPrefix(token, \"v4.public.\") {\n\t\treturn nil, errors.New(\"неверный заголовок PASETO: ожидается v4.public.\")\n\t}\n\n\tbodyB64 := strings.TrimPrefix(token, \"v4.public.\")\n\tbody, err := base64.RawURLEncoding.DecodeString(bodyB64)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка base64 декодирования: %w\", err)\n\t}\n\n\tif len(body) < ed25519.SignatureSize {\n\t\treturn nil, errors.New(\"токен слишком короткий\")\n\t}\n\n\tpayloadJSON := body[:len(body)-ed25519.SignatureSize]\n\tsig := body[len(body)-ed25519.SignatureSize:]\n\n\theader := []byte(\"v4.public.\")\n\tsignData := pae(header, payloadJSON, nil)\n\n\tif !ed25519.Verify(pubKey, signData, sig) {\n\t\treturn nil, errors.New(\"цифровая подпись Ed25519 недействительна\")\n\t}\n\n\tvar claims TokenClaims\n\tif err := json.Unmarshal(payloadJSON, &claims); err != nil {\n\t\treturn nil, fmt.Errorf(\"ошибка парсинга claims: %w\", err)\n\t}\n\n\tif time.Now().After(claims.ExpiresAt) {\n\t\treturn nil, errors.New(\"срок действия токена истек (token expired)\")\n\t}\n\n\treturn &claims, nil\n}\n\nfunc main() {\n\t// 1. Генерация ключей Ed25519 центра аутентификации\n\tpubKey, privKey, err := ed25519.GenerateKey(rand.Reader)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка генерации ключей: %v\", err)\n\t}\n\n\t// 2. Создание полезной нагрузки\n\tclaims := &TokenClaims{\n\t\tSubject:   \"usr_77190\",\n\t\tRole:      \"cluster_admin\",\n\t\tIssuedAt:  time.Now(),\n\t\tExpiresAt: time.Now().Add(2 * time.Hour),\n\t}\n\n\t// 3. Выпуск токена PASETO v4.public\n\ttoken, err := SignPasetoV4Public(privKey, claims)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка выпуска токена: %v\", err)\n\t}\n\n\tfmt.Println(\"=== Токен PASETO v4.public (Ed25519) ===\")\n\tfmt.Printf(\"Токен: %s\\n\\n\", token)\n\n\t// 4. Успешная верификация на защищенном API Gateway\n\tverifiedClaims, err := VerifyPasetoV4Public(pubKey, token)\n\tif err != nil {\n\t\tlog.Fatalf(\"Ошибка верификации: %v\", err)\n\t}\n\tfmt.Println(\"=== Результат верификации на шлюзе ===\")\n\tfmt.Printf(\"Пользователь: %s | Роль: %s\\n\", verifiedClaims.Subject, verifiedClaims.Role)\n\tfmt.Printf(\"Действителен до: %s\\n\\n\", verifiedClaims.ExpiresAt.Format(time.RFC3339))\n\n\t// 5. Попытка эскалации привилегий (подделка роли с cluster_admin)\n\ttamperedToken := strings.Replace(token, \"cluster_admin\", \"super_root\", 1)\n\t_, err = VerifyPasetoV4Public(pubKey, tamperedToken)\n\tif err != nil {\n\t\tfmt.Printf(\"Попытка фальсификации токена успешно предотвращена:\\n%v\\n\", err)\n\t}\n}",
        "note": "Реализация защищенных токенов PASETO v4.public на Ed25519 с алгоритмом каноникализации PAE"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go run main.go\n# === Токен PASETO v4.public (Ed25519) ===\n# Токен: v4.public.eyJzdWIiOiJ1c3JfNzcxOTAiLCJyb2xlIjoiY2x1c3Rlcl9hZG1pbiIsImlhdCI6...\n# \n# === Результат верификации на шлюзе ===\n# Пользователь: usr_77190 | Роль: cluster_admin\n# Действителен до: 2026-09-04T14:00:00Z\n# \n# Попытка фальсификации токена успешно предотвращена:\n# цифровая подпись Ed25519 недействительна"
      }
    ],
    "under_the_hood": "Зачем в PASETO алгоритм PAE (Pre-Authentication Encoding)?\nВ уязвимых протоколах данные часто склеиваются простой конкатенацией: `header + \".\" + payload`.\nЕсли злоумышленник может сдвинуть границу между `header` и `payload`, возникает уязвимость Canonicalization Attack (атака двусмысленности парсинга).\nАлгоритм PAE кодирует массив строк в бинарный поток с префиксами длины:\n`[число кусков (8 байт)][длина куска 1 (8 байт)][кусок 1][длина куска 2 (8 байт)][кусок 2]...`\nЭто математически гарантирует однозначность разбиения (Injective encoding): невозможно подобрать две разные комбинации входных массивов, которые дадут одинаковый байтовый поток PAE.",
    "pitfalls": "1. **Пропуск валидации ExpiresAt:** Подпись гарантирует неизменность данных, но не их актуальность! Если забыть проверку `time.Now().After(claims.ExpiresAt)`, украденный токен останется валидным навечно.\n2. **Использование JWT с 'alg': 'none':** Историческая уязвимость многих JWT библиотек заключалась в том, что злоумышленник мог отправить токен с `{\"alg\": \"none\"}` и стереть подпись, а сервер принимал его как доверенный. PASETO устраняет эту концептуальную уязвимость на уровне архитектуры протокола.",
    "bigtech_interview": "**Вопрос с собеседования:** Почему ведущие эксперты по безопасности и финтех-компании рекомендуют переходить с JWT на PASETO v4?\n**Ответ:** JWT предоставляет избыточную и опасную гибкость (Algorithm Agility): клиент может навязать серверу выбор алгоритма через заголовок (атаки с подменой алгоритма, смена RS256 на HS256 с подсовыванием публичного ключа в качестве HMAC-пароля, уязвимости с `alg: none` и слабыми эллиптическими кривыми). PASETO устраняет гибкость: версия `v4.public` жестко зафиксирована на Ed25519 с обязательным PAE кодированием, исключая любые ошибки конфигурации и человеческий фактор разработчиков."
  }
]
