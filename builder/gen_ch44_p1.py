# -*- coding: utf-8 -*-
"""Part 1 of Chapter 44 (Exercises 1..32)."""

exercises = [
  {
    "num": 1,
    "title": "CQRS (Command Query Responsibility Segregation): разделение моделей записи и чтения",
    "task": "**CQRS (Command Query Responsibility Segregation)**: Разделите модели для записи (commands) и чтения (queries). Write model — нормализованная БД, Read model — денормализованные view в Redis/Elasticsearch.",
    "theory": "Архитектура CQRS (Command Query Responsibility Segregation):\n- В высоконагруженных системах (Ozon, Wildberries) соотношение чтений к записям часто превышает $100:1$ или $1000:1$.\n- Попытка использовать одну и ту же нормализованную реляционную модель (3NF с десятками JOIN) для сложных аналитических запросов приводит к деградации производительности СУБД.\n- **Разделение ответственности:**\n  1. **Write Model (Команды):** строго нормализованная транзакционная БД (PostgreSQL). Оптимизирована для быстрой записи, целостности данных и ACID-инвариантов.\n  2. **Read Model (Запросы):** денормализованные плоские документы в Redis, Elasticsearch или ClickHouse. Запрос чтения возвращает готовый DTO за 1–2 мс без единого JOIN!\n  3. **Синхронизация:** асинхронный стриминг событий (Event Bus / Kafka / Debezium CDC).",
    "step_by_step": "1. Определите команду создания заказа `CreateOrderCommand` и модель записи в реляционной БД.\n2. Реализуйте генерацию плоского денормализованного представления `OrderReadView`.\n3. Смоделируйте обновление Read-модели в кэше/поисковике.\n4. Продемонстрируйте быстрое чтение готового агрегированного DTO.",
    "code_blocks": [
      {
        "filename": "cqrs_architecture_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\n// Write Model (Нормализованная транзакционная сущность)\ntype OrderWriteModel struct {\n\tID         string\n\tCustomerID string\n\tItemIDs    []string\n\tTotalCents int64\n}\n\n// Read Model (Денормализованная плоская проекция для фронтенда)\ntype OrderReadView struct {\n\tOrderID      string   `json:\"order_id\"`\n\tCustomerName string   `json:\"customer_name\"`\n\tItemTitles   []string `json:\"item_titles\"`\n\tTotalRub     float64  `json:\"total_rub\"`\n}\n\ntype CQRSOrderRepository struct {\n\tmu        sync.RWMutex\n\twriteDB   map[string]OrderWriteModel // PostgreSQL OLTP\n\treadStore map[string]OrderReadView   // Redis / Elasticsearch Read Store\n}\n\nfunc (r *CQRSOrderRepository) ExecuteCommandCreate(ctx context.Context, cmd OrderWriteModel, customerName string, titles []string) error {\n\tr.mu.Lock()\n\tdefer r.mu.Unlock()\n\n\t// 1. Запись в транзакционную модель\n\tr.writeDB[cmd.ID] = cmd\n\n\t// 2. Асинхронная или синхронная проекция в Read Model\n\tr.readStore[cmd.ID] = OrderReadView{\n\t\tOrderID:      cmd.ID,\n\t\tCustomerName: customerName,\n\t\tItemTitles:   titles,\n\t\tTotalRub:     float64(cmd.TotalCents) / 100.0,\n\t}\n\treturn nil\n}\n\nfunc (r *CQRSOrderRepository) ExecuteQueryView(ctx context.Context, orderID string) (OrderReadView, bool) {\n\tr.mu.RLock()\n\tdefer r.mu.RUnlock()\n\tview, found := r.readStore[orderID]\n\treturn view, found\n}\n\nfunc TestCQRSArchitecture(t *testing.T) {\n\trepo := &CQRSOrderRepository{\n\t\twriteDB:   make(map[string]OrderWriteModel),\n\t\treadStore: make(map[string]OrderReadView),\n\t}\n\n\tcmd := OrderWriteModel{\n\t\tID:         \"ord-1001\",\n\t\tCustomerID: \"cust-42\",\n\t\tItemIDs:    []string{\"item-1\", \"item-2\"},\n\t\tTotalCents: 154000,\n\t}\n\n\terr := repo.ExecuteCommandCreate(context.Background(), cmd, \"Алексей Смирнов\", []string{\"MacBook M3 Pro\", \"Мышь Magic Mouse\"})\n\tif err != nil {\n\t\tt.Fatalf(\"Ошибка выполнения команды: %v\", err)\n\t}\n\n\tview, found := repo.ExecuteQueryView(context.Background(), \"ord-1001\")\n\tif !found || view.CustomerName != \"Алексей Смирнов\" || view.TotalRub != 1540.0 {\n\t\tt.Fatalf(\"Некорректная Read-проекция: %+v\", view)\n\t}\n\n\tfmt.Println(\"CQRS Architecture подтверждена:\")\n\tfmt.Printf(\"  • Write Model сохранена в OLTP (ID=%s, TotalCents=%d)\\n\", cmd.ID, cmd.TotalCents)\n\tfmt.Printf(\"  • Read Model мгновенно доступна без JOIN: %+v\\n\", view)\n}",
        "note": "Разделение моделей транзакционной записи и денормализованного чтения в архитектуре CQRS"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v cqrs_architecture_test.go\n# Вывод:\n# === RUN   TestCQRSArchitecture\n# CQRS Architecture подтверждена:\n#   • Write Model сохранена в OLTP (ID=ord-1001, TotalCents=154000)\n#   • Read Model мгновенно доступна без JOIN: {OrderID:ord-1001 CustomerName:Алексей Смирнов ItemTitles:[MacBook M3 Pro Мышь Magic Mouse] TotalRub:1540}\n# --- PASS: TestCQRSArchitecture (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В микросервисах разделение CQRS позволяет масштабировать поды чтения независимо от подов записи. Поды Queries масштабируются по HPA до 50 реплик с чтением из Read-кластеров Redis/ClickHouse, в то время как поды Commands ограничены пулом соединений к PostgreSQL.",
    "pitfalls": "Ожидать немедленной строгой согласованности (Strong Consistency) на стороне чтения: при асинхронной репликации в Read-модель возникает задержка Eventual Consistency (обычно 5–100 мс). Если пользователь сразу после нажатия «Оплатить» обновляет страницу, он может на долю секунды увидеть старый статус.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как бороться с эффектом 'Read Your Own Writes' в системах с CQRS и Eventual Consistency?»\n**Ответ:** \n1. Возвращать обновленное состояние сущности прямо в HTTP-ответе команды `POST /orders` (мутация в UI на клиенте).\n2. Читать свежие данные собственного профиля пользователя из Primary БД в течение первых 2 секунд после записи (Read-After-Write window), а для остальных пользователей отдавать реплику."
  },
  {
    "num": 2,
    "title": "Event Sourcing: сохранение изменений состояния как последовательности событий и Replay",
    "task": "**Event Sourcing**: Сохраняйте все изменения состояния как последовательность событий в Kafka/EventStoreDB. Восстанавливайте текущее состояние через replay событий.",
    "theory": "Принципы архитектуры Event Sourcing:\n- **Традиционный подход (CRUD):**\n  - Таблица `accounts` хранит только текущий баланс: `UPDATE accounts SET balance = 400 WHERE id = 1`.\n  - Вся предыстория того, как баланс стал равен 400 (было 1000, списали 600 или было 0 и начислили 400), безвозвратно утеряна!\n- **Event Sourcing:**\n  - Состояние сущности **никогда не перезаписывается**.\n  - Хранилище событий (Event Store) является Append-Only логом иммутабельных фактов:\n    `AccountCreated(id=1, initial=0)`\n    `MoneyDeposited(amount=1000)`\n    `MoneyWithdrawn(amount=600)`\n  - **Восстановление состояния (Replay):** текущий баланс вычисляется последовательным применением (fold/reduce) всех событий агрегата с момента его создания.",
    "step_by_step": "1. Создайте интерфейс доменного события `DomainEvent`.\n2. Реализуйте структуру агрегата банковского счета `BankAccountAggregate`.\n3. Напишите функцию `Apply(event)` для мутации внутреннего состояния агрегата.\n4. Продемонстрируйте восстановление состояния через повторное воспроизведение (Replay) лога событий.",
    "code_blocks": [
      {
        "filename": "event_sourcing_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype DomainEvent interface {\n\tEventType() string\n}\n\ntype AccountOpenedEvent struct {\n\tAccountID string\n\tOwner     string\n}\n\nfunc (e AccountOpenedEvent) EventType() string { return \"AccountOpened\" }\n\ntype MoneyDepositedEvent struct {\n\tAmountRub int\n}\n\nfunc (e MoneyDepositedEvent) EventType() string { return \"MoneyDeposited\" }\n\ntype MoneyWithdrawnEvent struct {\n\tAmountRub int\n}\n\nfunc (e MoneyWithdrawnEvent) EventType() string { return \"MoneyWithdrawn\" }\n\n// Агрегат, восстанавливаемый из событий\ntype BankAccountAggregate struct {\n\tID      string\n\tOwner   string\n\tBalance int\n\tVersion int\n}\n\nfunc (a *BankAccountAggregate) Apply(evt DomainEvent) {\n\tswitch e := evt.(type) {\n\tcase AccountOpenedEvent:\n\t\ta.ID = e.AccountID\n\t\ta.Owner = e.Owner\n\t\ta.Balance = 0\n\tcase MoneyDepositedEvent:\n\t\ta.Balance += e.AmountRub\n\tcase MoneyWithdrawnEvent:\n\t\ta.Balance -= e.AmountRub\n\t}\n\ta.Version++\n}\n\nfunc ReplayAggregate(history []DomainEvent) *BankAccountAggregate {\n\tacc := &BankAccountAggregate{}\n\tfor _, evt := range history {\n\t\tacc.Apply(evt)\n\t}\n\treturn acc\n}\n\nfunc TestEventSourcing(t *testing.T) {\n\teventStream := []DomainEvent{\n\t\tAccountOpenedEvent{AccountID: \"acc-99\", Owner: \"Иван Иванов\"},\n\t\tMoneyDepositedEvent{AmountRub: 50000},\n\t\tMoneyWithdrawnEvent{AmountRub: 12000},\n\t\tMoneyDepositedEvent{AmountRub: 5000},\n\t}\n\n\taccount := ReplayAggregate(eventStream)\n\n\texpectedBalance := 50000 - 12000 + 5000\n\tif account.Balance != expectedBalance || account.Version != 4 {\n\t\tt.Fatalf(\"Некорректное состояние после Replay: Баланс=%d, Версия=%d\",\n\t\t\taccount.Balance, account.Version)\n\t}\n\n\tfmt.Println(\"Паттерн Event Sourcing успешно подтвержден:\")\n\tfmt.Printf(\"  • Воспроизведено событий: %d\\n\", len(eventStream))\n\tfmt.Printf(\"  • Итоговое состояние счета: Владелец=%s, Баланс=%d руб, Версия=%d\\n\",\n\t\taccount.Owner, account.Balance, account.Version)\n\tfmt.Println(\"  • 100% аудируемость и возможность путешествия во времени (Time Travel Debugging)\")\n}",
        "note": "Восстановление состояния агрегата банковского счета через Replay цепочки доменных событий"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v event_sourcing_test.go\n# Вывод:\n# === RUN   TestEventSourcing\n# Паттерн Event Sourcing успешно подтвержден:\n#   • Воспроизведено событий: 4\n#   • Итоговое состояние счета: Владелец=Иван Иванов, Баланс=43000 руб, Версия=4\n#   • 100% аудируемость и возможность путешествия во времени (Time Travel Debugging)\n# --- PASS: TestEventSourcing (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Когда число событий агрегата превышает 1000, Replay всех событий при каждом запросе становится слишком медленным ($O(N)$). Для оптимизации используют **снапшоты (Snapshots)**: каждые 100 событий состояние агрегата сохраняется в быстрое хранилище, а при загрузке восстанавливается последний снапшот плюс дельта событий после него.",
    "pitfalls": "Использовать Kafka в качестве единственного и постоянного источника истины для Event Sourcing: Kafka оптимизирована для стриминга, а не для произвольного чтения событий конкретного агрегата по ID (`SELECT * WHERE aggregate_id = ?`). Для Event Store используют специализированные БД (EventStoreDB, PostgreSQL с версионированием).",
    "bigtech_interview": "**Вопрос с собеседования:** «Как изменять схему событий в Event Sourcing при рефакторинге (Schema Evolution)?»\n**Ответ:** Паттерн Upcasting: старые события в БД остаются иммутабельными. При вычитке из Event Store специальный Upcaster налету преобразует `v1` события в структуру `v2` (подставляя дефолтные значения новых полей), гарантируя обратную совместимость."
  },
  {
    "num": 3,
    "title": "Материализованные представления (Materialized Views) из потока событий",
    "task": "**Materialized Views**: Создавайте read-оптимизированные проекции из event stream (например, `UserOrdersView` обновляется при каждом `OrderCreated` событии).",
    "theory": "Связь Event Sourcing и проекций Materialized Views:\n- Поток доменных событий (Event Stream) содержит полную информацию обо всех операциях в системе.\n- Чтобы отдавать личный кабинет пользователя, не нужно опрашивать базу заказов, биллинга и доставки.\n- **Проектор (Event Projector):**\n  1. Фоновый воркер подписывается на топик `orders.events`.\n  2. При получении события `OrderCreated` воркер атомарно обновляет материализованное представление пользователя в Redis / PostgreSQL JSONB:\n     `UPDATE user_order_summaries SET total_spent = total_spent + ?, orders_count = orders_count + 1 WHERE user_id = ?`.\n  3. API-запрос на профиль пользователя выполняется мгновенно по первичному ключу за 0.5 мс!",
    "step_by_step": "1. Создайте модель материализованного представления `UserOrdersSummaryView`.\n2. Реализуйте проектор событий `EventStreamProjector`.\n3. Смоделируйте поступление потока событий оформления заказов.\n4. Верифицируйте корректность агрегированных показателей витрины.",
    "code_blocks": [
      {
        "filename": "materialized_views_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype OrderCreatedEvent struct {\n\tOrderID   string\n\tUserID    string\n\tAmountRub int\n}\n\ntype UserOrdersSummaryView struct {\n\tUserID     string\n\tTotalSpent int\n\tOrderCount int\n}\n\ntype EventStreamProjector struct {\n\tmu    sync.RWMutex\n\tviews map[string]UserOrdersSummaryView\n}\n\nfunc (p *EventStreamProjector) HandleOrderCreated(evt OrderCreatedEvent) {\n\tp.mu.Lock()\n\tdefer p.mu.Unlock()\n\n\tsummary := p.views[evt.UserID]\n\tsummary.UserID = evt.UserID\n\tsummary.TotalSpent += evt.AmountRub\n\tsummary.OrderCount++\n\tp.views[evt.UserID] = summary\n}\n\nfunc (p *EventStreamProjector) GetUserView(userID string) (UserOrdersSummaryView, bool) {\n\tp.mu.RLock()\n\tdefer p.mu.RUnlock()\n\tv, ok := p.views[userID]\n\treturn v, ok\n}\n\nfunc TestMaterializedViews(t *testing.T) {\n\tprojector := &EventStreamProjector{views: make(map[string]UserOrdersSummaryView)}\n\n\tevents := []OrderCreatedEvent{\n\t\t{OrderID: \"ord-1\", UserID: \"user-100\", AmountRub: 2500},\n\t\t{OrderID: \"ord-2\", UserID: \"user-100\", AmountRub: 3500},\n\t\t{OrderID: \"ord-3\", UserID: \"user-200\", AmountRub: 1200},\n\t\t{OrderID: \"ord-4\", UserID: \"user-100\", AmountRub: 4000},\n\t}\n\n\tfor _, e := range events {\n\t\tprojector.HandleOrderCreated(e)\n\t}\n\n\tviewUser100, found := projector.GetUserView(\"user-100\")\n\tif !found || viewUser100.TotalSpent != 10000 || viewUser100.OrderCount != 3 {\n\t\tt.Fatalf(\"Некорректная материализованная проекция: %+v\", viewUser100)\n\t}\n\n\tfmt.Println(\"Материализованные представления (Materialized Views) подтверждены:\")\n\tfmt.Printf(\"  • Пользователь user-100: Заказов=%d, Всего потрачено=%d руб\\n\",\n\t\tviewUser100.OrderCount, viewUser100.TotalSpent)\n\tfmt.Println(\"  • Запрос личного кабинета читает готовые агрегаты без выполнения тяжелых SUM/COUNT в БД\")\n}",
        "note": "Построение и инкрементальное обновление материализованных представлений из потока событий"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v materialized_views_test.go\n# Вывод:\n# === RUN   TestMaterializedViews\n# Материализованные представления (Materialized Views) подтверждены:\n#   • Пользователь user-100: Заказов=3, Всего потрачено=10000 руб\n#   • Запрос личного кабинета читает готовые агрегаты без выполнения тяжелых SUM/COUNT в БД\n# --- PASS: TestMaterializedViews (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В распределенных системах для построения Materialized Views часто применяют специализированные базы стриминговой аналитики, такие как Materialize или Apache Flink, которые выполняют инкрементальные SQL-запросы (Differential Dataflow) прямо по потоку Kafka.",
    "pitfalls": "Потеря идемпотентности при повторной доставке (At-Least-Once Delivery): если сообщение `OrderCreated` придет консьюмеру дважды, `total_spent` ошибочно увеличится в два раза. Проекции обязаны проверять дедупликацию по `event_id`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что делать, если в бизнес-логике изменился формат витрины и нужно пересчитать проекцию для 100 миллионов пользователей?»\n**Ответ:** Паттерн Blue-Green Projection: создается новая таблица `user_order_summaries_v2`. Запускается отдельный фоновый консьюмер, который читает архивный лог событий Kafka с самого начала (`offset = 0`). После завершения синхронизации API-трафик переключается на таблицу `v2`, а старая витрина удаляется."
  },
  {
    "num": 4,
    "title": "Репликация чтения (Read Replicas): разделение пулов соединений Primary и Replica в PostgreSQL",
    "task": "**Read Replicas**: Настройте master-slave репликацию в PostgreSQL. Write операции идут в master, read — в slaves (через separate DB connection pool).",
    "theory": "Архитектура Primary-Replica (Master-Slave) для PostgreSQL:\n- База данных Primary принимает весь трафик на запись (`INSERT`, `UPDATE`, `DELETE`).\n- Журнал предзаписи (Write-Ahead Log / WAL) непрерывно передается на несколько асинхронных реплик чтения (Read Replicas).\n- **Разделение пулов на уровне Go:**\n  - В приложении инициализируются два независимых пула соединений (`pgxpool.Pool`):\n    1. `PrimaryPool`: направлен на мастер (DNS `pg-primary:5432`).\n    2. `ReplicaPool`: направлен на балансировщик реплик (HAProxy / PgBouncer `pg-replicas:5432`).\n  - Сервис роутит команды записи в `PrimaryPool`, а запросы каталога и поиска — в `ReplicaPool`.",
    "step_by_step": "1. Создайте структуру управления разделенными пулами БД `DBClusterRouter`.\n2. Реализуйте метод `ExecWrite` с обращением к Primary.\n3. Реализуйте метод `QueryRead` с обращением к Replica.\n4. Верифицируйте изоляцию трафика записи и чтения.",
    "code_blocks": [
      {
        "filename": "read_replicas_routing_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype DBClusterRouter struct {\n\tprimaryHits int\n\treplicaHits int\n}\n\nfunc (r *DBClusterRouter) ExecWrite(ctx context.Context, sqlQuery string) error {\n\t// Все операции мутации строго на Primary!\n\tr.primaryHits++\n\treturn nil\n}\n\nfunc (r *DBClusterRouter) QueryRead(ctx context.Context, sqlQuery string) ([]string, error) {\n\t// Чтение разгружает Primary и уходит на асинхронные реплики\n\tr.replicaHits++\n\treturn []string{\"record_from_replica\"}, nil\n}\n\nfunc TestReadReplicasRouting(t *testing.T) {\n\trouter := &DBClusterRouter{}\n\n\t// 10 операций записи (создание заказов)\n\tfor i := 0; i < 10; i++ {\n\t\t_ = router.ExecWrite(context.Background(), \"INSERT INTO orders VALUES (...)\")\n\t}\n\n\t// 90 операций чтения (просмотр каталога)\n\tfor i := 0; i < 90; i++ {\n\t\t_, _ = router.QueryRead(context.Background(), \"SELECT * FROM products WHERE ...\")\n\t}\n\n\tif router.primaryHits != 10 || router.replicaHits != 90 {\n\t\tt.Fatalf(\"Сбой распределения нагрузки: Primary=%d, Replica=%d\",\n\t\t\trouter.primaryHits, router.replicaHits)\n\t}\n\n\tfmt.Println(\"Архитектура Read Replicas подтверждена:\")\n\tfmt.Printf(\"  • Запросов записи на Primary: %d (10%% трафика)\\n\", router.primaryHits)\n\tfmt.Printf(\"  • Запросов чтения на Replicas: %d (90%% трафика снято с мастера)\\n\", router.replicaHits)\n\tfmt.Println(\"  • Мастер защищен от перегрузки тяжелыми SELECT-запросами\")\n}",
        "note": "Раздельная маршрутизация операций мутации и чтения между пулами Primary и Replica"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v read_replicas_routing_test.go\n# Вывод:\n# === RUN   TestReadReplicasRouting\n# Архитектура Read Replicas подтверждена:\n#   • Запросов записи на Primary: 10 (10% трафика)\n#   • Запросов чтения на Replicas: 90 (90% трафика снято с мастера)\n#   • Мастер защищен от перегрузки тяжелыми SELECT-запросами\n# --- PASS: TestReadReplicasRouting (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В драйвере `jackc/pgx/v5` строка подключения поддерживает указание нескольких хостов: `postgres://user:pass@host1:5432,host2:5432/db?target_session_attrs=primary` автоматически подключается к мастеру, а `target_session_attrs=read-only` находит активную реплику чтения.",
    "pitfalls": "Выполнять транзакцию чтения-записи `SELECT ... FOR UPDATE` через Replica: реплики PostgreSQL находятся в режиме `read-only`, и попытка захвата строчной блокировки упадет с ошибкой СУБД `ERROR: cannot execute SELECT FOR UPDATE in a read-only transaction`.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Replication Lag (лаг репликации) и как его измерить в PostgreSQL?»\n**Ответ:** Это задержка между фиксацией транзакции в WAL на мастере и ее применением на реплике. В PostgreSQL измеряется функцией `pg_wal_lsn_diff(pg_current_wal_lsn(), pg_last_wal_replay_lsn())` в байтах или `EXTRACT(EPOCH FROM (now() - pg_last_xact_replay_timestamp()))` в секундах. Если лаг превышает порог (например 2 сек), балансировщик обязан временно вывести реплику из ротации."
  },
  {
    "num": 5,
    "title": "Горизонтальное шардирование БД (Database Sharding): маршрутизация по user_id",
    "task": "**Database Sharding**: Горизонтальное шардирование по `user_id` (например, `user_id % 16` определяет шард). Реализуйте routing logic в приложении или через Vitess/Citus.",
    "theory": "Принципы горизонтального шардирования (Database Sharding):\n- Когда размер базы данных превышает 5–10 ТБ или объем записи превышает лимиты IOPS диска одного сервера:\n  - Вертикальное масштабирование (увеличение CPU/RAM машины) упирается в потолок возможностей железа.\n- **Шардирование по ключу (Hash/Modulo Sharding):**\n  - Данные разбиваются на $N$ независимых физических баз данных (шардов).\n  - Ключ шардирования (Shard Key, например `user_id`):\n    $$\\text{ShardID} = \\text{Hash}(\\text{user\\_id}) \\pmod{N}$$\n  - Все данные одного пользователя (профиль, заказы, настройки) хранятся на одном и том же шарде, что сохраняет локальные транзакции ACID внутри пользователя!",
    "step_by_step": "1. Создайте маршрутизатор шардов `ShardRouter` с пулом виртуальных баз.\n2. Реализуйте детерминированное вычисление номера шарда по `user_id`.\n3. Смоделируйте запись и чтение данных через соответствующий шард.\n4. Верифицируйте равномерное распределение пользователей по шардам.",
    "code_blocks": [
      {
        "filename": "database_sharding_router_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"hash/fnv\"\n\t\"testing\"\n)\n\ntype ShardDatabaseInstance struct {\n\tShardID int\n\tStorage map[string]string // имитация таблиц конкретного шарда\n}\n\ntype ApplicationShardRouter struct {\n\tshardsCount int\n\tshards      []*ShardDatabaseInstance\n}\n\nfunc NewShardRouter(shardsCount int) *ApplicationShardRouter {\n\tinstances := make([]*ShardDatabaseInstance, shardsCount)\n\tfor i := 0; i < shardsCount; i++ {\n\t\tinstances[i] = &ShardDatabaseInstance{\n\t\t\tShardID: i,\n\t\t\tStorage: make(map[string]string),\n\t\t}\n\t}\n\treturn &ApplicationShardRouter{\n\t\tshardsCount: shardsCount,\n\t\tshards:      instances,\n\t}\n}\n\nfunc (r *ApplicationShardRouter) GetShardIndex(userID string) int {\n\th := fnv.New32a()\n\t_, _ = h.Write([]byte(userID))\n\treturn int(h.Sum32() % uint32(r.shardsCount))\n}\n\nfunc (r *ApplicationShardRouter) SaveUserData(userID, payload string) int {\n\tshardIdx := r.GetShardIndex(userID)\n\tr.shards[shardIdx].Storage[userID] = payload\n\treturn shardIdx\n}\n\nfunc TestDatabaseShardingRouter(t *testing.T) {\n\trouter := NewShardRouter(16) // 16 шардов\n\n\tusers := []string{\"user-alice\", \"user-bob\", \"user-charlie\", \"user-diana\"}\n\tseenShards := make(map[int]bool)\n\n\tfor _, u := range users {\n\t\tshardIdx := router.SaveUserData(u, fmt.Sprintf(\"Profile data of %s\", u))\n\t\tseenShards[shardIdx] = true\n\t}\n\n\tif len(seenShards) < 2 {\n\t\tt.Fatal(\"Данные должны распределяться по разным шардам\")\n\t}\n\n\t// Проверяем идемпотентность выбора шарда для одного и того же пользователя\n\taliceShard1 := router.GetShardIndex(\"user-alice\")\n\taliceShard2 := router.GetShardIndex(\"user-alice\")\n\tif aliceShard1 != aliceShard2 {\n\t\tt.Fatalf(\"Маршрутизация шарда обязана быть строго детерминированной: %d != %d\", aliceShard1, aliceShard2)\n\t}\n\n\tfmt.Println(\"Горизонтальное шардирование БД (Database Sharding) подтверждено:\")\n\tfor _, u := range users {\n\t\tfmt.Printf(\"  • Пользователь %-12s направлен на Shard #%d\\n\", u, router.GetShardIndex(u))\n\t}\n\tfmt.Printf(\"  • Всего активных шардов: 16 | Использовано в тесте: %d\\n\", len(seenShards))\n}",
        "note": "Детерминированная маршрутизация запросов к шардам СУБД по хэшу идентификатора пользователя"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v database_sharding_router_test.go\n# Вывод:\n# === RUN   TestDatabaseShardingRouter\n# Горизонтальное шардирование БД (Database Sharding) подтверждено:\n#   • Пользователь user-alice   направлен на Shard #7\n#   • Пользователь user-bob     направлен на Shard #0\n#   • Пользователь user-charlie направлен на Shard #13\n#   • Пользователь user-diana   направлен на Shard #5\n#   • Всего активных шардов: 16 | Использовано в тесте: 4\n# --- PASS: TestDatabaseShardingRouter (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "При шардировании `user_id % 16` добавление 17-го шарда приведет к перемещению 94% всех данных (Resharding Storm). Чтобы этого избежать, используют **Consistent Hashing (Согласованное хеширование)** с виртуальными нодами или таблицы каталогов (Directory-Based Sharding), где соответствие `user_id -> shard_id` хранится в быстром кэше.",
    "pitfalls": "Делать Cross-Shard JOIN: запрос вида «Найти всех пользователей, купивших товар X» потребует опроса всех 16 шардов (Scatter-Gather Query) и слияния результатов в памяти бэкенда, что крайне ресурсоемко. Межшардовые выборки выносят в поисковые индексы Elasticsearch или аналитический ClickHouse.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как решать проблему суперпопулярных аккаунтов (Hot Partition / Celebrity Problem) при шардировании по автору?»\n**Ответ:** Добавлять к ключу шардирования соль (Salt): вместо `sharding_key = author_id` используют составной ключ `sharding_key = hash(author_id + \"_\" + (post_id % 10))`. Это распределяет посты знаменитости равномерно по 10 разным шардам, ликвидируя перегрузку одного сервера."
  },
  {
    "num": 6,
    "title": "Архитектурные компромиссы стратегий кэширования: Cache-Aside, Read-Through, Write-Through и Write-Behind",
    "task": "Спроектируй **Caching Strategy**: `Cache-Aside` (проверь кеш → miss → БД → кеш), `Read-Through` (кеш сам ходит в БД), `Write-Through` (писать в кеш и БД синхронно), `Write-Behind` (писать в кеш, async в БД). Покажи trade-offs по consistency и latency.",
    "theory": "Сравнительный анализ фундаментальных стратегий кэширования:\n1. **Cache-Aside (Lazy Loading):**\n   - Приложение само координирует кэш и БД: сначала проверяет Redis; при промахе идет в Postgres и сохраняет ответ в Redis.\n   - *Плюсы:* отказ Redis не роняет систему (падаем в БД). *Минусы:* задержка при Cache Miss.\n2. **Read-Through:**\n   - Приложение обращается **только к кэш-провайдеру**. При промахе библиотека кэша сама прозрачно загружает данные из БД.\n3. **Write-Through:**\n   - Запись выполняется одновременно и синхронно в кэш и в БД в рамках одного вызова.\n   - *Плюсы:* 100% консистентность кэша. *Минусы:* высокая задержка записи (суммарное время двух записей).\n4. **Write-Behind (Write-Back):**\n   - Запись мгновенно фиксируется только в кэше, а фоновый воркер асинхронно сбрасывает батчи в БД.\n   - *Плюсы:* ультранизкая задержка записи (< 1 мс). *Минусы:* риск потери данных при падении сервера до сброса на диск!",
    "step_by_step": "1. Создайте архитектурную матрицу компромиссов стратегий кэширования.\n2. Смоделируйте поведение Write-Behind с буферизацией в памяти.\n3. Продемонстрируйте асинхронный сброс в хранилище.\n4. Верифицируйте соответствие критериям надежности и задержки.",
    "code_blocks": [
      {
        "filename": "caching_strategies_tradeoffs_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype CachingPattern string\n\nconst (\n\tPatternCacheAside   CachingPattern = \"Cache-Aside (Lazy Loading)\"\n\tPatternReadThrough  CachingPattern = \"Read-Through\"\n\tPatternWriteThrough CachingPattern = \"Write-Through\"\n\tPatternWriteBehind  CachingPattern = \"Write-Behind (Write-Back)\"\n)\n\ntype StrategyTradeoffs struct {\n\tPattern         CachingPattern\n\tWriteLatencyMs  float64\n\tReadMissPenalty string\n\tDataLossRisk    bool\n\tConsistency     string\n}\n\nfunc GetCachingTradeoffs() []StrategyTradeoffs {\n\treturn []StrategyTradeoffs{\n\t\t{Pattern: PatternCacheAside, WriteLatencyMs: 25.0, ReadMissPenalty: \"Высокий (Round-trip в БД)\", DataLossRisk: false, Consistency: \"Eventual\"},\n\t\t{Pattern: PatternReadThrough, WriteLatencyMs: 25.0, ReadMissPenalty: \"Скрыт внутри кэш-провайдера\", DataLossRisk: false, Consistency: \"Eventual\"},\n\t\t{Pattern: PatternWriteThrough, WriteLatencyMs: 30.0, ReadMissPenalty: \"Минимальный (кэш всегда свеж)\", DataLossRisk: false, Consistency: \"Strong\"},\n\t\t{Pattern: PatternWriteBehind, WriteLatencyMs: 0.8, ReadMissPenalty: \"Нулевой\", DataLossRisk: true, Consistency: \"Eventual (Асинхронная задержка)\"},\n\t}\n}\n\nfunc TestCachingStrategiesTradeoffs(t *testing.T) {\n\tmatrix := GetCachingTradeoffs()\n\n\tif len(matrix) != 4 {\n\t\tt.Fatal(\"Ожидалось 4 канонических паттерна кэширования\")\n\t}\n\n\tfmt.Println(\"Матрица архитектурных компромиссов стратегий кэширования:\")\n\tfor _, m := range matrix {\n\t\tfmt.Printf(\"  • %-26s | Запись: %4.1f мс | Риск потери данных: %-5v | Консистентность: %s\\n\",\n\t\t\tm.Pattern, m.WriteLatencyMs, m.DataLossRisk, m.Consistency)\n\t}\n\tfmt.Println(\"  • Выбор стратегии зависит от баланса между задержкой ответа и допустимостью потерь данных\")\n}",
        "note": "Сравнительная матрица задержек, надежности и консистентности основных стратегий кэширования"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v caching_strategies_tradeoffs_test.go\n# Вывод:\n# === RUN   TestCachingStrategiesTradeoffs\n# Матрица архитектурных компромиссов стратегий кэширования:\n#   • Cache-Aside (Lazy Loading) | Запись: 25.0 мс | Риск потери данных: false | Консистентность: Eventual\n#   • Read-Through               | Запись: 25.0 мс | Риск потери данных: false | Консистентность: Eventual\n#   • Write-Through              | Запись: 30.0 мс | Риск потери данных: false | Консистентность: Strong\n#   • Write-Behind (Write-Back)  | Запись:  0.8 мс | Риск потери данных: true  | Консистентность: Eventual (Асинхронная задержка)\n#   • Выбор стратегии зависит от баланса между задержкой ответа и допустимостью потерь данных\n# --- PASS: TestCachingStrategiesTradeoffs (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Паттерн Write-Behind идеально подходит для задач с высокой интенсивностью обновлений (например счетчики просмотров постов, игровой прогресс, геопозиция курьеров). Сброс в БД выполняется сгруппированными батчами (`INSERT INTO ... VALUES (...) ON CONFLICT DO UPDATE`), что снижает нагрузку на диск на 95%.",
    "pitfalls": "Использовать Write-Behind для финансовых операций: если нода с кэшем перезагрузится по аварии питания до того, как сбросит проводку в Postgres, транзакция исчезнет без следа. Для финансов применяют строго Write-Through или Transactional Outbox.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в микросервисной архитектуре BigTech чаще всего выбирают именно Cache-Aside?»\n**Ответ:** Потому что Cache-Aside обеспечивает максимальную отказоустойчивость: если кластер Redis полностью выйдет из строя, сервис продолжит работать напрямую с базой данных (хоть и с более высокой задержкой), в то время как сбой в жестко связанном Read-Through/Write-Through адаптере заблокирует всё приложение."
  },
  {
    "num": 7,
    "title": "Реализация стратегий кэширования: Cache-Aside, Write-Through и асинхронный Write-Behind",
    "task": "**Caching Strategies**: Реализуйте Cache-Aside (lazy loading), Write-Through, Write-Behind (async write). Используйте Redis для hot data, CDN для static assets.",
    "theory": "Практическая реализация трех стратегий кэширования в Go:\n- **Cache-Aside:**\n  ```go\n  val, err := cache.Get(key)\n  if err != nil { val = db.Query(); cache.Set(key, val, ttl) }\n  ```\n- **Write-Through:**\n  ```go\n  db.Write(key, val)\n  cache.Set(key, val, ttl)\n  ```\n- **Write-Behind:**\n  ```go\n  cache.Set(key, val, ttl)\n  asyncWriteQueue <- Item{Key: key, Val: val} // фоновый воркер сбрасывает в БД\n  ```",
    "step_by_step": "1. Создайте интерфейс хранилища кэша и основной базы данных.\n2. Реализуйте три метода записи и чтения по соответствующим стратегиям.\n3. Продемонстрируйте работу фонового сброса Write-Behind очереди.\n4. Верифицируйте корректность сохранения данных.",
    "code_blocks": [
      {
        "filename": "caching_strategies_impl_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype StorageEngine struct {\n\tmu         sync.Mutex\n\tcacheStore map[string]string\n\tdbStore    map[string]string\n\twriteQueue chan struct{ key, val string }\n}\n\nfunc NewStorageEngine() *StorageEngine {\n\tengine := &StorageEngine{\n\t\tcacheStore: make(map[string]string),\n\t\tdbStore:    make(map[string]string),\n\t\twriteQueue: make(chan struct{ key, val string }, 100),\n\t}\n\t// Фоновый воркер для Write-Behind\n\tgo func() {\n\t\tfor item := range engine.writeQueue {\n\t\t\tengine.mu.Lock()\n\t\t\tengine.dbStore[item.key] = item.val\n\t\t\tengine.mu.Unlock()\n\t\t}\n\t}()\n\treturn engine\n}\n\n// 1. Cache-Aside Read\nfunc (e *StorageEngine) GetCacheAside(key string) (string, string) {\n\te.mu.Lock()\n\tdefer e.mu.Unlock()\n\n\tif val, ok := e.cacheStore[key]; ok {\n\t\treturn val, \"CACHE_HIT\"\n\t}\n\n\tval, ok := e.dbStore[key]\n\tif !ok {\n\t\treturn \"\", \"NOT_FOUND\"\n\t}\n\te.cacheStore[key] = val // Прогрев кэша\n\treturn val, \"CACHE_MISS_LOADED_FROM_DB\"\n}\n\n// 2. Write-Through\nfunc (e *StorageEngine) WriteThrough(key, val string) {\n\te.mu.Lock()\n\tdefer e.mu.Unlock()\n\te.dbStore[key] = val\n\te.cacheStore[key] = val\n}\n\n// 3. Write-Behind (Мгновенный возврат клиенту)\nfunc (e *StorageEngine) WriteBehind(key, val string) {\n\te.mu.Lock()\n\te.cacheStore[key] = val\n\te.mu.Unlock()\n\n\te.writeQueue <- struct{ key, val string }{key, val}\n}\n\nfunc TestCachingStrategiesImpl(t *testing.T) {\n\tengine := NewStorageEngine()\n\n\t// 1. Проверяем Write-Through\n\tengine.WriteThrough(\"product:10\", \"Laptop Pro\")\n\tval1, source1 := engine.GetCacheAside(\"product:10\")\n\tif source1 != \"CACHE_HIT\" || val1 != \"Laptop Pro\" {\n\t\tt.Fatalf(\"Write-Through сбой: %s, %s\", source1, val1)\n\t}\n\n\t// 2. Проверяем Write-Behind\n\tengine.WriteBehind(\"counter:likes\", \"1450\")\n\tval2, source2 := engine.GetCacheAside(\"counter:likes\")\n\tif source2 != \"CACHE_HIT\" || val2 != \"1450\" {\n\t\tt.Fatalf(\"Write-Behind сбой: %s, %s\", source2, val2)\n\t}\n\n\tfmt.Println(\"Реализация стратегий кэширования подтверждена:\")\n\tfmt.Printf(\"  • Write-Through чтение: %s (Источник: %s)\\n\", val1, source1)\n\tfmt.Printf(\"  • Write-Behind чтение:  %s (Источник: %s, Запись в фоне)\\n\", val2, source2)\n}",
        "note": "Практическая реализация Cache-Aside, синхронного Write-Through и асинхронного Write-Behind"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v caching_strategies_impl_test.go\n# Вывод:\n# === RUN   TestCachingStrategiesImpl\n# Реализация стратегий кэширования подтверждена:\n#   • Write-Through чтение: Laptop Pro (Источник: CACHE_HIT)\n#   • Write-Behind чтение:  1450 (Источник: CACHE_HIT, Запись в фоне)\n# --- PASS: TestCachingStrategiesImpl (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В распределенных средах статичный контент (изображения, стили, бандлы JS) кэшируется на периферии сети с помощью CDN (Cloudflare, Yandex Cloud CDN). Заголовки `Cache-Control: public, max-age=31536000, immutable` позволяют отсечь до 98% трафика до того, как запрос дойдет до бэкенда Go.",
    "pitfalls": "Не ограничивать размер очереди `writeQueue` в Write-Behind: при аварии базы данных очередь переполнит оперативную память и приведет к падению контейнера по OOM Killer. Очередь должна иметь фиксированную емкость и стратегию сброса нагрузки.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что лучше обновлять в Cache-Aside при изменении данных: перезаписывать значение в кэше (`SET`) или удалять его (`DEL`)?»\n**Ответ:** Лучше **удалять ключ (`DEL` / Invalidate)**. Если одновременно два параллельных запроса попытаются обновить кэш (`SET`), из-за гонки сетевых пакетов более старая версия может перезаписать более свежую. Удаление гарантирует, что следующий запрос надежно вычитает актуальные закоммиченные данные из БД."
  },
  {
    "num": 8,
    "title": "Многоуровневый кэш (Multi-Level Cache): L1 в памяти, L2 в Redis и L3 в PostgreSQL с метриками Hit Rate",
    "task": "Реализуй **Multi-Level Cache**: L1 (in-process, `sync.Map`, 1ms), L2 (Redis, 5ms), L3 (PostgreSQL, 50ms). `GetUser`: L1 → L2 → L3. `Invalidate`: publish event, все уровни обновляются. Покажи hit rate per level.",
    "theory": "Иерархия кэширования L1 $\\to$ L2 $\\to$ L3:\n- **L1 (In-Process RAM):** локальная память Go (`sync.Map`, Ristretto).\n  - Задержка: **< 100 наносекунд** (в 50 000 раз быстрее Redis!).\n- **L2 (Distributed In-Memory):** общий сетевой кластер Redis.\n  - Задержка: **1–3 миллисекунды**.\n- **L3 (Persistent Database):** реляционная СУБД PostgreSQL.\n  - Задержка: **10–50 миллисекунд**.\n- **Алгоритм поиска:**\n  1. Проверяем L1 $\\to$ если HIT, возвращаем результат мгновенно.\n  2. Если MISS в L1 $\\to$ проверяем L2 (Redis). При успехе прогреваем L1 и возвращаем.\n  3. Если MISS в L2 $\\to$ читаем L3 (PostgreSQL), прогреваем L2 и L1.",
    "step_by_step": "1. Создайте структуру трехуровневого кэша с подсчетом счетчиков попаданий.\n2. Реализуйте метод `GetUser` с каскадным переходом по уровням.\n3. Смоделируйте прогрев и инвалидацию кэша.\n4. Рассчитайте метрику Hit Rate для каждого уровня.",
    "code_blocks": [
      {
        "filename": "multilevel_cache_hitrate_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"sync/atomic\"\n\t\"testing\"\n)\n\ntype MultiLevelCacheEngine struct {\n\tl1Memory sync.Map          // L1: In-process RAM\n\tl2Redis  map[string]string // L2: Redis cluster\n\tl3DB     map[string]string // L3: PostgreSQL\n\n\tl1Hits int64\n\tl2Hits int64\n\tl3Hits int64\n}\n\nfunc (c *MultiLevelCacheEngine) GetUser(userID string) (string, string) {\n\t// 1. Проверяем L1 (In-process memory)\n\tif val, ok := c.l1Memory.Load(userID); ok {\n\t\tatomic.AddInt64(&c.l1Hits, 1)\n\t\treturn val.(string), \"L1_MEMORY\"\n\t}\n\n\t// 2. Проверяем L2 (Redis)\n\tif val, ok := c.l2Redis[userID]; ok {\n\t\tatomic.AddInt64(&c.l2Hits, 1)\n\t\tc.l1Memory.Store(userID, val) // Прогрев L1\n\t\treturn val, \"L2_REDIS\"\n\t}\n\n\t// 3. Проверяем L3 (PostgreSQL)\n\tif val, ok := c.l3DB[userID]; ok {\n\t\tatomic.AddInt64(&c.l3Hits, 1)\n\t\tc.l2Redis[userID] = val        // Прогрев L2\n\t\tc.l1Memory.Store(userID, val) // Прогрев L1\n\t\treturn val, \"L3_DATABASE\"\n\t}\n\n\treturn \"\", \"NOT_FOUND\"\n}\n\nfunc (c *MultiLevelCacheEngine) Invalidate(userID string) {\n\tc.l1Memory.Delete(userID)\n\tdelete(c.l2Redis, userID)\n}\n\nfunc TestMultiLevelCacheHitRate(t *testing.T) {\n\tengine := &MultiLevelCacheEngine{\n\t\tl2Redis: make(map[string]string),\n\t\tl3DB:    map[string]string{\"user-1\": \"Анна Каренина\"},\n\t}\n\n\t// Запрос 1: L3 (холодный старт)\n\t_, src1 := engine.GetUser(\"user-1\")\n\t// Запрос 2: L1 (уже прогрет в памяти)\n\t_, src2 := engine.GetUser(\"user-1\")\n\t// Запрос 3: L1\n\t_, src3 := engine.GetUser(\"user-1\")\n\n\tif src1 != \"L3_DATABASE\" || src2 != \"L1_MEMORY\" || src3 != \"L1_MEMORY\" {\n\t\tt.Fatalf(\"Сбой каскада уровней: %s, %s, %s\", src1, src2, src3)\n\t}\n\n\ttotal := float64(engine.l1Hits + engine.l2Hits + engine.l3Hits)\n\tl1Rate := (float64(engine.l1Hits) / total) * 100.0\n\n\tfmt.Println(\"Многоуровневый кэш (Multi-Level Cache L1/L2/L3) подтвержден:\")\n\tfmt.Printf(\"  • Попадания L1 (RAM):   %d (%.1f%% Hit Rate)\\n\", engine.l1Hits, l1Rate)\n\tfmt.Printf(\"  • Попадания L2 (Redis): %d\\n\", engine.l2Hits)\n\tfmt.Printf(\"  • Попадания L3 (DB):    %d\\n\", engine.l3Hits)\n\tfmt.Println(\"  • Подавляющее большинство запросов обслуживается из памяти без сетевых накладных расходов\")\n}",
        "note": "Каскадная обработка промахов кэша на уровнях L1 (память), L2 (Redis) и L3 (СУБД)"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v multilevel_cache_hitrate_test.go\n# Вывод:\n# === RUN   TestMultiLevelCacheHitRate\n# Многоуровневый кэш (Multi-Level Cache L1/L2/L3) подтвержден:\n#   • Попадания L1 (RAM):   2 (66.7% Hit Rate)\n#   • Попадания L2 (Redis): 0\n#   • Попадания L3 (DB):    1\n#   • Подавляющее большинство запросов обслуживается из памяти без сетевых накладных расходов\n# --- PASS: TestMultiLevelCacheHitRate (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В микросервисах с сотнями подов инвалидация локального L1 кэша координируется через Redis Pub/Sub: сервис, изменивший данные, отправляет `PUBLISH invalidate user-1`. Все 100 подов слушают этот канал и моментально вызывают `l1Memory.Delete(\"user-1\")`.",
    "pitfalls": "Использовать в L1 кэше `sync.Map` без механизма вытеснения (LRU / TinyLFU): при миллионах уникальных пользователей память пода переполнится. Для боевого L1 используют библиотеки с жестким ограничением размера памяти (например `dgraph-io/ristretto`).",
    "bigtech_interview": "**Вопрос с собеседования:** «Что делать, если сообщение инвалидации L1 через Redis Pub/Sub потеряется из-за сетевого сбоя?»\n**Ответ:** Устанавливать на ключи в L1 жесткий короткий TTL (например 15–30 секунд). Даже при потере широковещательного пакета инвалидации под вернется к согласованному состоянию максимум через 30 секунд."
  },
  {
    "num": 9,
    "title": "Прогрев кэша (Cache Warming): предотвращение перегрузки БД при деплое новой версии",
    "task": "Реализуй **Cache Warming**: при деплое новой версии — preload hot data into cache. `CacheWarmer` читает `SELECT * FROM products WHERE active = true ORDER BY views DESC LIMIT 10000`, пишет в Redis. Покажи, что cold cache = DB overload.",
    "theory": "Проблема холодного старта (Cold Cache Problem):\n- При перезапуске сервиса или очистке кэша после масштабного релиза:\n  - Первый же наплыв трафика в 20 000 RPS обрушивается на абсолютно пустой кэш.\n  - 20 000 параллельных запросов одновременно идут в PostgreSQL.\n  - Пул соединений СУБД исчерпывается, CPU базы подскакивает до 100%, сайт падает (Cascading Failure).\n- **Решение через Cache Warming (Прогрев кэша):**\n  - До переключения сетевого трафика (Readiness Probe в K8s):\n  - Фоновый воркер `CacheWarmer` выбирает топ-10 000 самых популярных активных товаров (`ORDER BY views DESC LIMIT 10000`).\n  - Батчами прогревает их в Redis.\n  - Только после 100% готовности кэша сервис открывает порт для пользовательского трафика!",
    "step_by_step": "1. Создайте структуру воркера предварительного прогрева `CacheWarmer`.\n2. Реализуйте выборку горячих товаров из БД и запись в кэш.\n3. Продемонстрируйте прохождение проверки готовности сервиса (Readiness).\n4. Верифицируйте защиту базы данных от холодного шторма запросов.",
    "code_blocks": [
      {
        "filename": "cache_warming_readiness_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype ProductEntity struct {\n\tID    string\n\tTitle string\n\tViews int\n}\n\ntype CacheWarmer struct {\n\tisWarmed bool\n\tcache    map[string]ProductEntity\n}\n\nfunc (w *CacheWarmer) WarmUpHotProducts(dbProducts []ProductEntity, limit int) int {\n\tw.cache = make(map[string]ProductEntity)\n\n\t// Имитация предзагрузки ТОП-N популярных товаров\n\tloaded := 0\n\tfor _, p := range dbProducts {\n\t\tif loaded >= limit {\n\t\t\tbreak\n\t\t}\n\t\tw.cache[p.ID] = p\n\t\tloaded++\n\t}\n\n\tw.isWarmed = true\n\treturn loaded\n}\n\nfunc (w *CacheWarmer) IsReadyForTraffic() bool {\n\treturn w.isWarmed && len(w.cache) > 0\n}\n\nfunc TestCacheWarmingReadiness(t *testing.T) {\n\twarmer := &CacheWarmer{}\n\n\t// До прогрева под не должен принимать боевой трафик!\n\tif warmer.IsReadyForTraffic() {\n\t\tt.Fatal(\"Сервис не имеет права принимать трафик с холодным кэшем\")\n\t}\n\n\thotProducts := []ProductEntity{\n\t\t{ID: \"prod-1\", Title: \"Смартфон Флагман 2026\", Views: 95000},\n\t\t{ID: \"prod-2\", Title: \"Беспроводные наушники\", Views: 74000},\n\t\t{ID: \"prod-3\", Title: \"Игровая консоль\", Views: 62000},\n\t}\n\n\tloadedCount := warmer.WarmUpHotProducts(hotProducts, 10000)\n\n\tif !warmer.IsReadyForTraffic() || loadedCount != 3 {\n\t\tt.Fatalf(\"Сбой прогрева: warmed=%v, count=%d\", warmer.IsReadyForTraffic(), loadedCount)\n\t}\n\n\tfmt.Println(\"Прогрев кэша (Cache Warming) успешно подтвержден:\")\n\tfmt.Printf(\"  • Прогрето горячих позиций в Redis: %d\\n\", loadedCount)\n\tfmt.Printf(\"  • Статус готовности к трафику (K8s Readiness Probe): %v (Готов)\\n\", warmer.IsReadyForTraffic())\n\tfmt.Println(\"  • База данных защищена от шторма запросов при холодном старте подов\")\n}",
        "note": "Предварительный прогрев горячих данных перед открытием K8s Readiness Probe для трафика"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v cache_warming_readiness_test.go\n# Вывод:\n# === RUN   TestCacheWarmingReadiness\n# Прогрев кэша (Cache Warming) успешно подтвержден:\n#   • Прогрето горячих позиций в Redis: 3\n#   • Статус готовности к трафику (K8s Readiness Probe): true (Готов)\n#   • База данных защищена от шторма запросов при холодном старте подов\n# --- PASS: TestCacheWarmingReadiness (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Kubernetes прогрев кэша настраивают в рамках `startupProbe`: пока скрипт или эндпоинт `/health/warmup` не вернет `200 OK`, Kubelet не направляет трафик с балансировщика Ingress на новый под, обеспечивая 100% прогретый старт.",
    "pitfalls": "Прогревать весь миллионный каталог товаров: прогрев займет 40 минут, заблокировав CI/CD пайплайн деплоя. Прогревать нужно **строго верхний квантиль (Top 5-10% горячих ключей)** по правилу Парето (80/20).",
    "bigtech_interview": "**Вопрос с собеседования:** «Как прогревать кэш без выполнения тяжелого аналитического SQL-запроса `ORDER BY views` на боевом мастере?»\n**Ответ:** Выгружать список горячих ключей из аналитической реплики ClickHouse или использовать логирование популярных URL с балансировщика Nginx: скрипт считывает топ-10 000 URL за прошедший час и прогоняет их синтетическими `curl` запросами через сервис до релиза."
  },
  {
    "num": 10,
    "title": "Инвалидация кэша через Redis Pub/Sub при изменении данных в базе",
    "task": "**Cache Invalidation**: Реализуйте инвалидацию кэша через pub/sub (Redis Pub/Sub) при изменении данных в БД.",
    "theory": "Синхронизация распределенных кэшей через шину событий (Pub/Sub Cache Invalidation):\n- В микросервисной архитектуре развернуты десятки инстансов приложения, каждый из которых имеет локальный буфер в оперативной памяти.\n- Если экземпляр №1 обрабатывает запрос на обновление цены товара:\n  1. Экземпляр №1 выполняет `UPDATE products SET price = ? WHERE id = 42` в СУБД.\n  2. Экземпляр №1 публикует в Redis канал `cache:invalidation`:\n     ```json\n     {\"action\": \"INVALIDATE\", \"key\": \"product:42\"}\n     ```\n  3. Все остальные 50 инстансов слушают этот канал. Получив событие, они моментально сбрасывают ключ `product:42` из своих локальных кэшей.\n  4. Рассинхронизация данных устраняется за доли миллисекунды!",
    "step_by_step": "1. Создайте структуру шины сообщений инвалидации `CacheInvalidationBus`.\n2. Реализуйте подписку слушателей (инстансов приложения).\n3. Смоделируйте публикацию события сброса кэша при обновлении записи в БД.\n4. Верифицируйте очистку устаревших значений на всех узлах.",
    "code_blocks": [
      {
        "filename": "cache_invalidation_pubsub_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype CacheInvalidationBus struct {\n\tmu          sync.Mutex\n\tsubscribers []chan string\n}\n\nfunc (b *CacheInvalidationBus) Subscribe() chan string {\n\tb.mu.Lock()\n\tdefer b.mu.Unlock()\n\tch := make(chan string, 10)\n\tb.subscribers = append(b.subscribers, ch)\n\treturn ch\n}\n\nfunc (b *CacheInvalidationBus) PublishInvalidate(key string) {\n\tb.mu.Lock()\n\tdefer b.mu.Unlock()\n\tfor _, sub := range b.subscribers {\n\t\tselect {\n\t\tcase sub <- key:\n\t\tdefault:\n\t\t}\n\t}\n}\n\ntype NodeLocalCache struct {\n\tdata map[string]string\n}\n\nfunc TestCacheInvalidationPubSub(t *testing.T) {\n\tbus := &CacheInvalidationBus{}\n\n\t// Создаем два независимых пода приложения\n\tnodeA := &NodeLocalCache{data: map[string]string{\"item-5\": \"Старая цена 1000\"}}\n\tnodeB := &NodeLocalCache{data: map[string]string{\"item-5\": \"Старая цена 1000\"}}\n\n\tsubA := bus.Subscribe()\n\tsubB := bus.Subscribe()\n\n\t// Нода А обновила товар в БД и публикует инвалидацию\n\tbus.PublishInvalidate(\"item-5\")\n\n\t// Обе ноды вычитывают сигнал инвалидации\n\tkeyA := <-subA\n\tdelete(nodeA.data, keyA)\n\n\tkeyB := <-subB\n\tdelete(nodeB.data, keyB)\n\n\tif len(nodeA.data) != 0 || len(nodeB.data) != 0 {\n\t\tt.Fatal(\"Кэш на обеих нодах должен быть сброшен\")\n\t}\n\n\tfmt.Println(\"Инвалидация кэша через Pub/Sub успешно подтверждена:\")\n\tfmt.Printf(\"  • Получен сигнал сброса ключа: %s на всех нодах\\n\", keyA)\n\tfmt.Println(\"  • Состояние кэша синхронизировано между распределенными экземплярами сервиса\")\n}",
        "note": "Синхронная инвалидация распределенного кэша через брокер сообщений Pub/Sub"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v cache_invalidation_pubsub_test.go\n# Вывод:\n# === RUN   TestCacheInvalidationPubSub\n# Инвалидация кэша через Pub/Sub успешно подтверждена:\n#   • Получен сигнал сброса ключа: item-5 на всех нодах\n#   • Состояние кэша синхронизировано между распределенными экземплярами сервиса\n# --- PASS: TestCacheInvalidationPubSub (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Redis 6+ внедрен встроенный протокол **RESP3 Client Tracking**: сервер Redis сам отслеживает, какие клиенты читали определенный ключ, и при его изменении автоматически шлет сокет-пакет `INVALIDATE` в сторону клиента Go без ручного создания Pub/Sub каналов.",
    "pitfalls": "Использовать Redis Pub/Sub для инвалидации критичных данных без гарантии доставки (At-Most-Once): стандартный Pub/Sub не буферизирует сообщения. Если нода была в кратковременном сетевом флапе, она пропустит событие. Для критичных витрин ключам всегда задают максимальный TTL.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие Redis Pub/Sub от Redis Streams в задачах инвалидации кэша?»\n**Ответ:** Redis Pub/Sub работает по модели Fire-and-Forget (сообщения не сохраняются в памяти и теряются, если подписчик оффлайн). Redis Streams сохраняет историю событий в Append-Only лог, позволяя отставшему поду дочитать пропущенные инвалидации после восстановления связи."
  },
  {
    "num": 11,
    "title": "Распределенное ограничение частоты (Rate Limiting): алгоритм Token Bucket с общим состоянием в Redis",
    "task": "Реализуй **Rate Limiting (Token Bucket)**: `user_id` — 100 requests/minute. Redis: `INCR user:123:rate` + `EXPIRE 60`. Или `redis-cell` модуль. Distributed: все instances share Redis state. Покажи fairness.",
    "theory": "Принцип работы алгоритма Token Bucket (Корзина токенов):\n- Виртуальная корзина имеет фиксированную емкость $B$ (Burst Capacity, например 10 токенов).\n- Корзина непрерывно пополняется токенами со скоростью $R$ токенов в секунду.\n- Каждый входящий HTTP-запрос пытается забрать 1 токен:\n  - Если токен есть $\\implies$ запрос пропускается к обработчику (`HTTP 200`).\n  - Если корзина пуста $\\implies$ запрос отклоняется со статусом `HTTP 429 Too Many Requests` и заголовком `Retry-After`.\n- **Преимущество перед Fixed Window:**\n  - Token Bucket сглаживает скачки нагрузки и предотвращает всплески трафика на границе минутных окон.",
    "step_by_step": "1. Создайте структуру корзины токенов `DistributedTokenBucket`.\n2. Реализуйте математику добавления токенов с учетом времени, прошедшего с прошлого вызова.\n3. Смоделируйте исчерпание токенов при превышении лимита.\n4. Верифицируйте отклонение лишних запросов со статусом 429.",
    "code_blocks": [
      {
        "filename": "token_bucket_limiter_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"math\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype DistributedTokenBucket struct {\n\tmu           sync.Mutex\n\tcapacity     float64\n\ttokens       float64\n\trefillRate   float64 // токенов в секунду\n\tlastRefillAt time.Time\n}\n\nfunc NewTokenBucket(capacity float64, refillRate float64) *DistributedTokenBucket {\n\treturn &DistributedTokenBucket{\n\t\tcapacity:     capacity,\n\t\ttokens:       capacity,\n\t\trefillRate:   refillRate,\n\t\tlastRefillAt: time.Now(),\n\t}\n}\n\nfunc (tb *DistributedTokenBucket) AllowRequest() (bool, time.Duration) {\n\ttb.mu.Lock()\n\tdefer tb.mu.Unlock()\n\n\tnow := time.Now()\n\telapsed := now.Sub(tb.lastRefillAt).Seconds()\n\ttb.lastRefillAt = now\n\n\t// Добавляем токены за прошедшее время\n\ttb.tokens = math.Min(tb.capacity, tb.tokens+elapsed*tb.refillRate)\n\n\tif tb.tokens >= 1.0 {\n\t\ttb.tokens -= 1.0\n\t\treturn true, 0\n\t}\n\n\t// Время ожидания до появления следующего токена\n\tmissingTokens := 1.0 - tb.tokens\n\tretryAfter := time.Duration((missingTokens / tb.refillRate) * float64(time.Second))\n\treturn false, retryAfter\n}\n\nfunc TestTokenBucketLimiter(t *testing.T) {\n\t// Емкость 3 токена, восполнение 1 токен/сек\n\tlimiter := NewTokenBucket(3.0, 1.0)\n\n\t// Первые 3 запроса должны пройти моментально (Burst)\n\tfor i := 1; i <= 3; i++ {\n\t\tallowed, _ := limiter.AllowRequest()\n\t\tif !allowed {\n\t\t\tt.Fatalf(\"Запрос #%d обязан был пройти из burst capacity\", i)\n\t\t}\n\t}\n\n\t// 4-й запрос превышает емкость корзины!\n\tallowed4, retryAfter := limiter.AllowRequest()\n\tif allowed4 {\n\t\tt.Fatal(\"4-й запрос обязан быть отклонен (429 Too Many Requests)\")\n\t}\n\n\tfmt.Println(\"Алгоритм Token Bucket Rate Limiting подтвержден:\")\n\tfmt.Printf(\"  • Первые 3 запроса успешно пропущены (Burst Capacity)\\n\")\n\tfmt.Printf(\"  • 4-й запрос заблокирован: 429 Too Many Requests (Retry-After: %v)\\n\", retryAfter)\n\tfmt.Println(\"  • Все инстансы приложения справедливо делят квоту пользователя\")\n}",
        "note": "Математическая модель алгоритма Token Bucket с плавным восполнением токенов и расчетом Retry-After"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v token_bucket_limiter_test.go\n# Вывод:\n# === RUN   TestTokenBucketLimiter\n# Алгоритм Token Bucket Rate Limiting подтвержден:\n#   • Первые 3 запроса успешно пропущены (Burst Capacity)\n#   • 4-й запрос заблокирован: 429 Too Many Requests (Retry-After: ...)\n#   • Все инстансы приложения справедливо делят квоту пользователя\n# --- PASS: TestTokenBucketLimiter (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В высоконагруженном Redis для реализации Token Bucket используют модуль `redis-cell` с единственной атомарной командой `CL.THROTTLE user:123 100 100 60 1`, которая за один системный вызов проверяет токены, списывает 1 токен и возвращает оставшееся время до восстановления.",
    "pitfalls": "Реализовывать Rate Limiter двумя раздельными командами `INCR` и `EXPIRE` без транзакции: если процесс упадет между вызовами, ключ останется в Redis навсегда без TTL, заблокировав пользователя на вечность. Операции объединяют в **атомарный Lua скрипт**.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие алгоритма Leaky Bucket от Token Bucket?»\n**Ответ:** \n- **Token Bucket:** допускает кратковременные всплески нагрузки (Burst) в пределах емкости корзины.\n- **Leaky Bucket:** сглаживает трафик до строго постоянной скорости вытекания (Traffic Shaping), отбрасывая любые всплески сверх жесткого буфера."
  },
  {
    "num": 12,
    "title": "Распределенный Rate Limiter на базе атомарных Lua-скриптов в Redis",
    "task": "**Rate Limiting**: Реализуйте distributed rate limiter через Redis Lua scripts (token bucket или sliding window) для защиты от DoS и fair usage.",
    "theory": "Атомарность Redis Lua скриптов в распределенных системах:\n- В распределенной системе десятки подов одновременно обращаются к Redis для проверки лимита одного пользователя.\n- Несколько сетевых вызовов (`GET` $\\to$ вычисление в Go $\\to$ `SET`) создают состояние гонки (Race Condition), позволяя атакующему обойти лимит в 10 раз.\n- **Атомарный Lua Script:**\n  - Скрипт исполняется на стороне Redis в однопоточном ядре атомарно.\n  - Никакая другая операция не может вклиниться между проверкой времени и списанием токена.\n  - Все промежуточные состояния изолированы, обеспечивая 100% точность лимитирования при любых нагрузках!",
    "step_by_step": "1. Создайте спецификацию Lua-скрипта для алгоритма Token Bucket.\n2. Смоделируйте движок выполнения скрипта в изолированной памяти.\n3. Продемонстрируйте атомарное списание токена и расчет остатка.\n4. Верифицируйте защиту от параллельных гонок.",
    "code_blocks": [
      {
        "filename": "redis_lua_rate_limiter_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\n// Эталонный Lua скрипт для Redis Token Bucket\nconst RedisTokenBucketLuaScript = `\nlocal key = KEYS[1]\nlocal limit = tonumber(ARGV[1])\nlocal current = tonumber(redis.call('get', key) or \"0\")\n\nif current + 1 > limit then\n    return 0 -- Блокировка (Лимит исчерпан)\nelse\n    redis.call('incrby', key, 1)\n    if current == 0 then\n        redis.call('expire', key, tonumber(ARGV[2]))\n    end\n    return 1 -- Разрешено\nend\n`\n\ntype MockRedisServer struct {\n\tstorage map[string]int\n}\n\nfunc (m *MockRedisServer) EvalTokenBucketLua(key string, limit int, windowSec int) int {\n\tcurrent := m.storage[key]\n\tif current+1 > limit {\n\t\treturn 0 // 429 Too Many Requests\n\t}\n\tm.storage[key] = current + 1\n\treturn 1 // OK\n}\n\nfunc TestRedisLuaRateLimiter(t *testing.T) {\n\tmockRedis := &MockRedisServer{storage: make(map[string]int)}\n\n\t// Лимит 2 запроса в окно\n\tres1 := mockRedis.EvalTokenBucketLua(\"ratelimit:user-42\", 2, 60)\n\tres2 := mockRedis.EvalTokenBucketLua(\"ratelimit:user-42\", 2, 60)\n\tres3 := mockRedis.EvalTokenBucketLua(\"ratelimit:user-42\", 2, 60)\n\n\tif res1 != 1 || res2 != 1 {\n\t\tt.Fatal(\"Первые два запроса обязаны быть разрешены\")\n\t}\n\n\tif res3 != 0 {\n\t\tt.Fatal(\"Третий запрос обязан быть заблокирован Lua-скриптом\")\n\t}\n\n\tfmt.Println(\"Distributed Rate Limiting через Redis Lua подтвержден:\")\n\tfmt.Printf(\"  • Lua Script:\\n%s\\n\", RedisTokenBucketLuaScript)\n\tfmt.Printf(\"  • Результаты вызовов: Req1=%d, Req2=%d, Req3=%d (Заблокирован)\\n\", res1, res2, res3)\n\tfmt.Println(\"  • Атомарность исполнения гарантирует защиту от Race Condition при 100 000 RPS\")\n}",
        "note": "Атомарный Lua-скрипт для Redis с гарантией отсутствия состояния гонки при проверке квот"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v redis_lua_rate_limiter_test.go\n# Вывод:\n# === RUN   TestRedisLuaRateLimiter\n# Distributed Rate Limiting через Redis Lua подтвержден:\n#   • Lua Script:\n# ...\n#   • Результаты вызовов: Req1=1, Req2=1, Req3=0 (Заблокирован)\n#   • Атомарность исполнения гарантирует защиту от Race Condition при 100 000 RPS\n# --- PASS: TestRedisLuaRateLimiter (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Для максимальной производительности в драйвере `go-redis` скрипты не передаются текстом при каждом запросе. Используется команда `SCRIPT LOAD`, возвращающая SHA1-хэш скрипта, после чего вызовы идут через быструю команду `EVALSHA <sha1>`, экономя сетевой трафик.",
    "pitfalls": "Писать в Lua-скрипте тяжелые неблокируемые циклы: пока Lua-скрипт выполняется, сервер Redis **полностью заблокирован** и не отвечает на запросы других клиентов. Скрипт должен выполняться за несколько микросекунд.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в Redis Cluster Lua-скрипты требуют, чтобы все ключи `KEYS` находились на одном шарде?»\n**Ответ:** Redis Cluster шардирует данные по хэш-слотам (`CRC16(key) % 16384`). Redis не может обеспечить транзакционность и атомарность операций, затрагивающих разные физические узлы кластера. Чтобы ключи гарантированно попали на один шард, используют фигурные скобки хэш-тегов: `{user:123}:limiter` и `{user:123}:metadata`."
  },
  {
    "num": 13,
    "title": "Скользящее окно (Sliding Window Counter): точный Rate Limiting на Redis Sorted Sets (ZADD)",
    "task": "Реализуй **Rate Limiting (Sliding Window Counter)**: Redis `ZADD` timestamp'ов, `ZREMRANGEBYSCORE` старых, `ZCARD` для подсчёта. Точнее, но дороже. Покажи precision vs performance trade-off.",
    "theory": "Алгоритм скользящего окна на базе Redis Sorted Set (ZSET):\n- **Проблема фиксированного окна (Fixed Window):**\n  - Пользователь имеет лимит 100 запросов в минуту.\n  - Он отправляет 100 запросов в 12:00:59 и еще 100 запросов в 12:01:01.\n  - Формально в каждой календарной минуте лимит не превышен, но за 2 секунды сервер получил 200 запросов!\n- **Sliding Window Log (ZSET):**\n  1. Ключ: `ratelimit:user_id`.\n  2. Добавляем текущую миллисекунду: `ZADD key now now`.\n  3. Удаляем все записи старше 60 секунд: `ZREMRANGEBYSCORE key 0 (now - 60s)`.\n  4. Считаем число элементов в окне: `ZCARD key`.\n  5. Если `count > limit` $\\implies$ отклоняем запрос!",
    "step_by_step": "1. Создайте структуру скользящего лога времени `SlidingWindowLogLimiter`.\n2. Реализуйте очистку устаревших временных меток.\n3. Продемонстрируйте блокировку всплеска на стыке минут.\n4. Верифицируйте точность соблюдения лимита.",
    "code_blocks": [
      {
        "filename": "sliding_window_zset_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype SlidingWindowLogLimiter struct {\n\twindowDuration time.Duration\n\tlimit          int\n\ttimestamps     []time.Time\n}\n\nfunc NewSlidingWindowLimiter(window time.Duration, limit int) *SlidingWindowLogLimiter {\n\treturn &SlidingWindowLogLimiter{\n\t\twindowDuration: window,\n\t\tlimit:          limit,\n\t\ttimestamps:     make([]time.Time, 0),\n\t}\n}\n\nfunc (l *SlidingWindowLogLimiter) Allow(now time.Time) bool {\n\t// 1. ZREMRANGEBYSCORE: удаляем метки за пределами скользящего окна\n\tthreshold := now.Add(-l.windowDuration)\n\tvalidIdx := 0\n\tfor i, t := range l.timestamps {\n\t\tif t.After(threshold) {\n\t\t\tvalidIdx = i\n\t\t\tbreak\n\t\t}\n\t\tif i == len(l.timestamps)-1 {\n\t\t\tvalidIdx = len(l.timestamps)\n\t\t}\n\t}\n\tl.timestamps = l.timestamps[validIdx:]\n\n\t// 2. ZCARD: проверяем количество запросов в текущем плавающем окне\n\tif len(l.timestamps) >= l.limit {\n\t\treturn false\n\t}\n\n\t// 3. ZADD: добавляем текущую отметку времени\n\tl.timestamps = append(l.timestamps, now)\n\treturn true\n}\n\nfunc TestSlidingWindowZSet(t *testing.T) {\n\tlimiter := NewSlidingWindowLimiter(1*time.Minute, 2)\n\tbaseTime := time.Date(2026, 9, 4, 12, 0, 0, 0, time.UTC)\n\n\t// Запрос 1: 12:00:10 -> разрешен\n\tok1 := limiter.Allow(baseTime.Add(10 * time.Second))\n\t// Запрос 2: 12:00:40 -> разрешен\n\tok2 := limiter.Allow(baseTime.Add(40 * time.Second))\n\t// Запрос 3: 12:00:50 -> превышение (2 запроса за последние 60 сек!)\n\tok3 := limiter.Allow(baseTime.Add(50 * time.Second))\n\n\tif !ok1 || !ok2 || ok3 {\n\t\tt.Fatalf(\"Сбой скользящего окна: ok1=%v, ok2=%v, ok3=%v\", ok1, ok2, ok3)\n\t}\n\n\t// Запрос 4: 12:01:15 (прошло 65 сек с первого запроса -> окно сдвинулось, 1-й запрос удален)\n\tok4 := limiter.Allow(baseTime.Add(75 * time.Second))\n\tif !ok4 {\n\t\tt.Fatal(\"Запрос после сдвига скользящего окна обязан быть разрешен\")\n\t}\n\n\tfmt.Println(\"Sliding Window Rate Limiting (Redis ZSET) подтвержден:\")\n\tfmt.Printf(\"  • Окно 60 секунд: Запрос 1 (10s)=%v, Запрос 2 (40s)=%v, Запрос 3 (50s)=%v (Отклонен)\\n\", ok1, ok2, ok3)\n\tfmt.Printf(\"  • Запрос 4 (75s)=%v: Успешно пропущен после естественного сдвига окна\\n\", ok4)\n\tfmt.Println(\"  • Абсолютная защита от всплесков нагрузки на границе минут\")\n}",
        "note": "Точное скользящее окно запросов с удалением устаревших таймстемпов"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v sliding_window_zset_test.go\n# Вывод:\n# === RUN   TestSlidingWindowZSet\n# Sliding Window Rate Limiting (Redis ZSET) подтвержден:\n#   • Окно 60 секунд: Запрос 1 (10s)=true, Запрос 2 (40s)=true, Запрос 3 (50s)=false (Отклонен)\n#   • Запрос 4 (75s)=true: Успешно пропущен после естественного сдвига окна\n#   • Абсолютная защита от всплесков нагрузки на границе минут\n# --- PASS: TestSlidingWindowZSet (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Хранение временных меток в Redis ZSET требует памяти $O(N)$ от числа запросов. При 100 000 RPS память Redis быстро заполнится. На сверхвысоких нагрузках применяют гибридный **Sliding Window Counter**: взвешенная сумма счетчиков текущей и предыдущей минуты, требующая всего два целочисленных ключа.",
    "pitfalls": "Забывать вызывать `EXPIRE` для ZSET ключа: если пользователь перестал слать запросы, ZSET останется в оперативной памяти Redis. Ключу всегда задают `EXPIRE key window_seconds * 2`.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем компромисс между Token Bucket и Sliding Window Log?»\n**Ответ:** \n- **Token Bucket:** чрезвычайно дешев по памяти ($O(1)$ — 2 числа в Redis), но допускает кратковременный Burst.\n- **Sliding Window Log:** математически точен и полностью исключает всплески, но дорог по памяти ($O(N)$) и требует $O(\\log M)$ на вставку и удаление из Sorted Set."
  },
  {
    "num": 14,
    "title": "Сброс нагрузки (Load Shedding): отсечение второстепенных запросов и отдача кэшированных данных",
    "task": "**Load Shedding**: При перегрузке отбрасывайте \"дешёвые\" запросы (health checks, неважные метрики) или возвращайте cached/stale data.",
    "theory": "Философия Load Shedding (Управляемый сброс нагрузки):\n- Когда сервер перегружен (очередь входящих запросов растет быстрее, чем CPU успевает их обрабатывать), задержка (Latency) стремится к бесконечности.\n- Попытка обработать **все** запросы приводит к краху сервера (Server Crash) и 100% отказу для всех пользователей.\n- **Load Shedding:**\n  1. Приоритезация трафика:\n     - `Tier 1 (Critical):` оформление заказа, оплата, авторизация.\n     - `Tier 2 (Standard):` добавление в избранное, отзывы.\n     - `Tier 3 (Low):` внутренние метрики, фоновые хелсчеки, рекомендации.\n  2. При загрузке CPU > 85% шлюз моментально отбрасывает запросы Tier 3 (HTTP 503 Service Unavailable).\n  3. Для Tier 2 отдаются устаревшие данные из кэша (Stale Data).\n  4. Tier 1 продолжает работать штатно за 5 мс!",
    "step_by_step": "1. Создайте модель классификации приоритетов входящих запросов.\n2. Реализуйте шлюз сброса нагрузки `LoadSheddingGateway`.\n3. Смоделируйте состояние перегрузки сервера (CPU 92%).\n4. Верифицируйте пропуск критических транзакций и отсечение второстепенных запросов.",
    "code_blocks": [
      {
        "filename": "load_shedding_gateway_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype RequestPriority int\n\nconst (\n\tPriorityCritical RequestPriority = iota // Оплата, Чекаут\n\tPriorityStandard                        // Карточка товара\n\tPriorityLow                             // Фоновые метрики, рекомендации\n)\n\ntype SystemHealthMetrics struct {\n\tCPUUsagePercent float64\n}\n\ntype LoadSheddingGateway struct {\n\thealth SystemHealthMetrics\n}\n\nfunc (gw *LoadSheddingGateway) DispatchRequest(priority RequestPriority, payload string) (string, int) {\n\t// Если система в критической перегрузке (CPU > 85%)\n\tif gw.health.CPUUsagePercent > 85.0 {\n\t\tif priority == PriorityLow {\n\t\t\treturn \"LOAD_SHED: dropped to protect system health\", 503\n\t\t}\n\t\tif priority == PriorityStandard {\n\t\t\treturn \"DEGRADED: served from stale fallback cache\", 200\n\t\t}\n\t}\n\t// Критические запросы обслуживаются всегда!\n\treturn fmt.Sprintf(\"SUCCESS: processed %s\", payload), 200\n}\n\nfunc TestLoadSheddingGateway(t *testing.T) {\n\t// Сервер перегружен: CPU на 92%\n\tgw := &LoadSheddingGateway{health: SystemHealthMetrics{CPUUsagePercent: 92.0}}\n\n\t// 1. Фоновая рекомендация -> должна быть сброшена\n\tresLow, codeLow := gw.DispatchRequest(PriorityLow, \"GetRecommendations\")\n\tif codeLow != 503 {\n\t\tt.Fatalf(\"Низкоприоритетный запрос обязан быть сброшен: %d\", codeLow)\n\t}\n\n\t// 2. Просмотр товара -> отдача из кэша\n\tresStd, codeStd := gw.DispatchRequest(PriorityStandard, \"GetProductDetails\")\n\tif codeStd != 200 || resStd != \"DEGRADED: served from stale fallback cache\" {\n\t\tt.Fatalf(\"Стандартный запрос должен деградировать: %s\", resStd)\n\t}\n\n\t// 3. Платеж -> должен быть выполнен в приоритете!\n\tresCrit, codeCrit := gw.DispatchRequest(PriorityCritical, \"ProcessPayment\")\n\tif codeCrit != 200 || resCrit != \"SUCCESS: processed ProcessPayment\" {\n\t\tt.Fatalf(\"Критический платеж обязан пройти: %s\", resCrit)\n\t}\n\n\tfmt.Println(\"Сброс нагрузки (Load Shedding Gateway) успешно подтвержден:\")\n\tfmt.Printf(\"  • Low Priority:      %d %s\\n\", codeLow, resLow)\n\tfmt.Printf(\"  • Standard Priority: %d %s\\n\", codeStd, resStd)\n\tfmt.Printf(\"  • Critical Priority: %d %s\\n\", codeCrit, resCrit)\n\tfmt.Println(\"  • Жизнеспособность ядра системы сохранена во время пикового шторма нагрузки\")\n}",
        "note": "Управляемый сброс низкоприоритетных запросов для сохранения критических транзакций при перегрузке"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v load_shedding_gateway_test.go\n# Вывод:\n# === RUN   TestLoadSheddingGateway\n# Сброс нагрузки (Load Shedding Gateway) успешно подтвержден:\n#   • Low Priority:      503 LOAD_SHED: dropped to protect system health\n#   • Standard Priority: 200 DEGRADED: served from stale fallback cache\n#   • Critical Priority: 200 SUCCESS: processed ProcessPayment\n#   • Жизнеспособность ядра системы сохранена во время пикового шторма нагрузки\n# --- PASS: TestLoadSheddingGateway (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Netflix и Uber алгоритм Load Shedding реализуется на основе алгоритма **CoDel (Controlled Delay)**: если время нахождения запроса в очереди ожидания горутин превышает 20 мс, шлюз начинает проактивно сбрасывать входящие запросы до тех пор, пока длина очереди не вернется в норму.",
    "pitfalls": "Отбрасывать запросы дорого: если для проверки приоритета сервер сначала парсит 10-мегабайтный JSON в куче, сам процесс Load Shedding сожжет процессор. Приоритет запроса проверяют **на уровне заголовков HTTP** (`X-Priority: critical`) за 10 наносекунд.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему возврат HTTP 503 при Load Shedding должен обязательно сопровождаться случайным заголовком Retry-After с джиттером?»\n**Ответ:** Без заголовка `Retry-After` все 100 000 клиентов, получив ошибку, синхронно повторят запрос ровно через 1 секунду, вызвав повторный разрушительный шторм (Thundering Herd / Retry Storm). Джиттер (Jitter) распределяет повторные попытки равномерно во времени."
  },
  {
    "num": 15,
    "title": "Балансировка нагрузки уровня L4 (Transport Layer): TCP passthrough, Round-Robin и IP-Hash",
    "task": "Реализуй **Load Balancing (Layer 4)**: HAProxy/Nginx TCP passthrough. Round-robin, least-connections, IP-hash (sticky sessions). Покажи, что L4 не видит HTTP, работает с любым TCP.",
    "theory": "Разница между балансировкой L4 и L7:\n- **L7 Балансировка (Application Layer — Envoy, Nginx HTTP):**\n  - Разбирает HTTP-заголовки, куки, путь URL `/api/v1/orders`.\n  - Требует терминации TLS (расшифровки сертификата) и парсинга протокола.\n  - Высокий расход CPU, пропускная способность ограничена ~50k RPS на ядро.\n- **L4 Балансировка (Transport Layer — HAProxy, Linux IPVS, AWS NLB):**\n  - Работает строго на уровне TCP/UDP пакетов (IP-адрес и порт назначения).\n  - Не заглядывает внутрь пакетов (TCP Passthrough). Не тратит CPU на расшифровку TLS.\n  - Способна перекладывать миллионы пакетов в секунду на скорости сетевой карты (Wire Speed)!\n  - **Алгоритмы:** Round-Robin, Least Connections, IP-Hash (сессии пользователя привязаны к конкретному серверу).",
    "step_by_step": "1. Создайте структуру балансировщика L4 `L4TCPBalancer`.\n2. Реализуйте алгоритмы Round-Robin и IP-Hash.\n3. Докажите, что балансировщик передает сырые бинарные TCP-пакеты без знания протокола приложения.\n4. Верифицируйте привязку клиента по IP-Hash.",
    "code_blocks": [
      {
        "filename": "l4_load_balancer_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"hash/fnv\"\n\t\"sync/atomic\"\n\t\"testing\"\n)\n\ntype BackendNode struct {\n\tAddress string\n}\n\ntype L4TCPBalancer struct {\n\tbackends []BackendNode\n\tcounter  uint64\n}\n\n// 1. Алгоритм Round-Robin\nfunc (b *L4TCPBalancer) RouteRoundRobin() BackendNode {\n\tidx := atomic.AddUint64(&b.counter, 1) - 1\n\treturn b.backends[idx%uint64(len(b.backends))]\n}\n\n// 2. Алгоритм IP-Hash (Sticky Sessions на уровне IP)\nfunc (b *L4TCPBalancer) RouteIPHash(clientIP string) BackendNode {\n\th := fnv.New32a()\n\t_, _ = h.Write([]byte(clientIP))\n\tidx := int(h.Sum32()) % len(b.backends)\n\tif idx < 0 {\n\t\tidx = -idx\n\t}\n\treturn b.backends[idx]\n}\n\nfunc TestL4LoadBalancer(t *testing.T) {\n\tnodes := []BackendNode{\n\t\t{Address: \"10.0.1.10:9000\"},\n\t\t{Address: \"10.0.1.11:9000\"},\n\t\t{Address: \"10.0.1.12:9000\"},\n\t}\n\n\tbalancer := &L4TCPBalancer{backends: nodes}\n\n\t// Проверка Round-Robin\n\tn1 := balancer.RouteRoundRobin()\n\tn2 := balancer.RouteRoundRobin()\n\tn3 := balancer.RouteRoundRobin()\n\n\tif n1.Address != \"10.0.1.10:9000\" || n2.Address != \"10.0.1.11:9000\" || n3.Address != \"10.0.1.12:9000\" {\n\t\tt.Fatalf(\"Сбой Round-Robin: %s, %s, %s\", n1.Address, n2.Address, n3.Address)\n\t}\n\n\t// Проверка IP-Hash (Sticky)\n\tipAlice := \"192.168.1.50\"\n\tstickyNode1 := balancer.RouteIPHash(ipAlice)\n\tstickyNode2 := balancer.RouteIPHash(ipAlice)\n\n\tif stickyNode1.Address != stickyNode2.Address {\n\t\tt.Fatalf(\"IP-Hash обязан всегда направлять один IP на один узел: %s != %s\",\n\t\t\tstickyNode1.Address, stickyNode2.Address)\n\t}\n\n\tfmt.Println(\"Балансировка нагрузки L4 (TCP Passthrough) подтверждена:\")\n\tfmt.Printf(\"  • Round-Robin последовательность: %s -> %s -> %s\\n\", n1.Address, n2.Address, n3.Address)\n\tfmt.Printf(\"  • IP-Hash фиксация: Клиент %s -> Сервер %s (Детерминированно)\\n\", ipAlice, stickyNode1.Address)\n\tfmt.Println(\"  • L4 оперирует сырыми TCP сокетами с околонулевыми накладными расходами CPU\")\n}",
        "note": "Реализация L4 балансировщика TCP-пакетов с алгоритмами Round-Robin и детерминированным IP-Hash"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v l4_load_balancer_test.go\n# Вывод:\n# === RUN   TestL4LoadBalancer\n# Балансировка нагрузки L4 (TCP Passthrough) подтверждена:\n#   • Round-Robin последовательность: 10.0.1.10:9000 -> 10.0.1.11:9000 -> 10.0.1.12:9000\n#   • IP-Hash фиксация: Клиент 192.168.1.50 -> Сервер ... (Детерминированно)\n#   • L4 оперирует сырыми TCP сокетами с околонулевыми накладными расходами CPU\n# --- PASS: TestL4LoadBalancer (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "На уровне ядра Linux L4 балансировка реализуется через технологию **IPVS (IP Virtual Server)** и eBPF/XDP: пакеты маршрутизируются на уровне сетевого драйвера без копирования данных в пространство пользователя (Zero-Copy Packet Forwarding).",
    "pitfalls": "Терять реальный IP-адрес клиента при L4 TCP Passthrough: бэкенд видит только IP балансировщика. Для сохранения исходного адреса клиента на L4 включают протокол **HAProxy PROXY Protocol v2**, который вставляет 16-байтный бинарный заголовок с IP клиента в начало TCP потока.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Direct Server Return (DSR) в L4 балансировке?»\n**Ответ:** Это архитектурная оптимизация, при которой входящие запросы клиента идут через балансировщик L4, а исходящие тяжелые ответы серверов (видео, картинки) отправляются клиенту **напрямую в обход балансировщика**. Это снимает с балансировщика исходящий трафик и позволяет одной ноде балансировать терабитные потоки данных."
  },
  {
    "num": 16,
    "title": "Управление обратным давлением (Backpressure): адаптивная регулировка скорости консьюмера Kafka",
    "task": "**Backpressure**: Если consumer не успевает обрабатывать сообщения из Kafka, уменьшайте fetch size или приостанавливайте producer.",
    "theory": "Принцип обратного давления (Backpressure) в потоковой обработке:\n- Если продюсер генерирует 10 000 сообщений в секунду, а тяжелый консьюмер с записью в БД успевает обработать только 2 000 сообщений:\n  - Без механизма Backpressure консьюмер вычитает миллион сообщений в память, исчерпает RAM и упадет по OOM.\n- **Механизмы Backpressure:**\n  1. **Dynamic Fetch Size:** при росте очереди локальной обработки размер батча `fetch.max.bytes` динамически уменьшается.\n  2. **Pause / Resume партиций:** консьюмер вызывает `consumer.Pause(partitions)`, перестает запрашивать новые данные из брокера, спокойно разгребает накопленный буфер, после чего вызывает `consumer.Resume(partitions)`.\n  3. Буфер Kafka надежно сохраняет сообщения на диске брокера без риска потери!",
    "step_by_step": "1. Создайте структуру потокового процессора с контролем обратного давления `BackpressureConsumer`.\n2. Реализуйте мониторинг заполнения локальной очереди задач.\n3. Смоделируйте автоматическую приостановку (Pause) при заполнении буфера на 80%.\n4. Верифицируйте возобновление вычитки (Resume) после освобождения очереди.",
    "code_blocks": [
      {
        "filename": "kafka_backpressure_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype BackpressureConsumer struct {\n\tmu           sync.Mutex\n\tlocalQueue   chan string\n\tisPaused     bool\n\tqueueCapacity int\n}\n\nfunc NewBackpressureConsumer(capacity int) *BackpressureConsumer {\n\treturn &BackpressureConsumer{\n\t\tlocalQueue:    make(chan string, capacity),\n\t\tqueueCapacity: capacity,\n\t}\n}\n\nfunc (c *BackpressureConsumer) IngestMessage(msg string) bool {\n\tc.mu.Lock()\n\tdefer c.mu.Unlock()\n\n\t// Если очередь заполнена более чем на 80% -> включаем Backpressure (Pause)\n\tif len(c.localQueue) >= int(float64(c.queueCapacity)*0.8) {\n\t\tc.isPaused = true\n\t\treturn false // Сигнал брокеру: приостановить чтение!\n\t}\n\n\tc.localQueue <- msg\n\treturn true\n}\n\nfunc (c *BackpressureConsumer) DrainProcessed(count int) {\n\tc.mu.Lock()\n\tdefer c.mu.Unlock()\n\n\tfor i := 0; i < count && len(c.localQueue) > 0; i++ {\n\t\t<-c.localQueue\n\t}\n\n\t// Если очередь разгрузилась ниже 30% -> возобновляем вычитку (Resume)\n\tif len(c.localQueue) <= int(float64(c.queueCapacity)*0.3) {\n\t\tc.isPaused = false\n\t}\n}\n\nfunc TestKafkaBackpressure(t *testing.T) {\n\tconsumer := NewBackpressureConsumer(10)\n\n\t// Заполняем 8 сообщений (80% емкости)\n\tfor i := 1; i <= 8; i++ {\n\t\tok := consumer.IngestMessage(fmt.Sprintf(\"msg-%d\", i))\n\t\tif !ok {\n\t\t\tt.Fatalf(\"Сообщение #%d должно было поместиться\", i)\n\t\t}\n\t}\n\n\t// 9-е сообщение должно вызвать срабатывание Backpressure Pause!\n\tok9 := consumer.IngestMessage(\"msg-9\")\n\tif ok9 || !consumer.isPaused {\n\t\tt.Fatal(\"Backpressure обязан был приостановить прием сообщений\")\n\t}\n\n\t// Воркеры обрабатывают 6 сообщений\n\tconsumer.DrainProcessed(6)\n\n\tif consumer.isPaused {\n\t\tt.Fatal(\"После разгрузки очереди консьюмер обязан возобновить прием (Resume)\")\n\t}\n\n\tfmt.Println(\"Управление обратным давлением (Backpressure Control) подтверждено:\")\n\tfmt.Println(\"  • Буфер достиг 80% -> Сработала пауза вычитки (Pause Partitions)\")\n\tfmt.Println(\"  • Очередь обработана воркерами -> Поток автоматически возобновлен (Resume)\")\n\tfmt.Println(\"  • Консьюмер гарантированно защищен от переполнения памяти OOM\")\n}",
        "note": "Автоматическое регулирование потока вычитки сообщений на основе порога заполнения локального буфера"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v kafka_backpressure_test.go\n# Вывод:\n# === RUN   TestKafkaBackpressure\n# Управление обратным давлением (Backpressure Control) подтверждено:\n#   • Буфер достиг 80% -> Сработала пауза вычитки (Pause Partitions)\n#   • Очередь обработана воркерами -> Поток автоматически возобновлен (Resume)\n#   • Консьюмер гарантированно защищен от переполнения памяти OOM\n# --- PASS: TestKafkaBackpressure (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В официальном клиенте `confluent-kafka-go` вызов `consumer.Pause([]kafka.TopicPartition{...})` временно исключает партицию из итератора poll. Брокер Kafka перестает слать сокет-фреймы, а TCP-буфер сокета на стороне ОС естественным образом сигнализирует брокеру о заполнении (TCP Zero Window).",
    "pitfalls": "Останавливать консьюмер дольше, чем `max.poll.interval.ms` (по дефолту 5 минут): если консьюмер заблокируется на обработке и не вызовет `Poll()`, Kafka решит, что под умер, и запустит тяжелый процесс ребалансировки группы (Group Rebalance), парализовав всех остальных потребителей.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как реализовать сквозной Backpressure в каналах Go?»\n**Ответ:** Использовать строго небуферизированные (`make(chan T)`) или каналы с жестко ограниченным буфером (`make(chan T, 100)`). Операция `ch <- item` естественным образом блокирует горутину продюсера, если консьюмер не успевает вычитывать данные, передавая сигнал замедления вверх по стеку вызовов."
  },
  {
    "num": 17,
    "title": "Балансировка нагрузки уровня L7 (Application Layer): Path, Header и Cookie-based маршрутизация",
    "task": "Реализуй **Load Balancing (Layer 7)**: Nginx/Envoy HTTP routing. Path-based: `/api/v1/users/*` → `user-service-v1`, `/api/v2/users/*` → `user-service-v2`. Header-based: `Canary: true` → `user-service-canary`. Cookie-based: `version=v2` → `user-service-v2`.",
    "theory": "Принципы маршрутизации L7 (Envoy, Traefik, Nginx):\n- В отличие от L4, L7-прокси полностью разбирает HTTP-запрос (URL, заголовки, куки, метод).\n- **Виды L7-маршрутизации:**\n  1. **Path-Based:** префикс пути `/api/v1` или `/api/v2` определяет целевую группу подов.\n  2. **Header-Based (Canary / Dark Launch):** при наличии заголовка `X-Canary: true` запрос направляется на канареечный под с новой версией.\n  3. **Cookie-Based (A/B тесты):** кука `beta_tester=1` переключает пользователя на альтернативный бэкенд.\n  4. Возможность инъекции заголовков трассировки (`X-Request-ID`, W3C Trace Context).",
    "step_by_step": "1. Создайте структуру L7 HTTP-маршрутизатора `L7HttpRouter`.\n2. Реализуйте проверку заголовков (Canary), куки и путей URL.\n3. Продемонстрируйте корректный выбор целевого сервиса для различных комбинаций запросов.\n4. Верифицируйте правила приоритизации канареечного трафика.",
    "code_blocks": [
      {
        "filename": "l7_http_routing_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"strings\"\n\t\"testing\"\n)\n\ntype L7HttpRouter struct{}\n\nfunc (r *L7HttpRouter) RouteRequest(req *http.Request) string {\n\t// 1. Header-based routing: Canary проверка\n\tif req.Header.Get(\"X-Canary\") == \"true\" {\n\t\treturn \"user-service-canary\"\n\t}\n\n\t// 2. Cookie-based routing: A/B тестирование\n\tif cookie, err := req.Cookie(\"version\"); err == nil && cookie.Value == \"v2\" {\n\t\treturn \"user-service-v2\"\n\t}\n\n\t// 3. Path-based routing: версионирование API\n\tif strings.HasPrefix(req.URL.Path, \"/api/v2/users\") {\n\t\treturn \"user-service-v2\"\n\t}\n\tif strings.HasPrefix(req.URL.Path, \"/api/v1/users\") {\n\t\treturn \"user-service-v1\"\n\t}\n\n\treturn \"default-gateway\"\n}\n\nfunc TestL7HttpRouting(t *testing.T) {\n\trouter := &L7HttpRouter{}\n\n\t// Запрос 1: Canary заголовок\n\treqCanary := httptest.NewRequest(http.MethodGet, \"/api/v1/users/42\", nil)\n\treqCanary.Header.Set(\"X-Canary\", \"true\")\n\ttarget1 := router.RouteRequest(reqCanary)\n\tif target1 != \"user-service-canary\" {\n\t\tt.Fatalf(\"Ожидался канареечный маршрут: %s\", target1)\n\t}\n\n\t// Запрос 2: Path v2\n\treqV2 := httptest.NewRequest(http.MethodGet, \"/api/v2/users/100\", nil)\n\ttarget2 := router.RouteRequest(reqV2)\n\tif target2 != \"user-service-v2\" {\n\t\tt.Fatalf(\"Ожидался user-service-v2: %s\", target2)\n\t}\n\n\t// Запрос 3: Обычный v1\n\treqV1 := httptest.NewRequest(http.MethodGet, \"/api/v1/users/100\", nil)\n\ttarget3 := router.RouteRequest(reqV1)\n\tif target3 != \"user-service-v1\" {\n\t\tt.Fatalf(\"Ожидался user-service-v1: %s\", target3)\n\t}\n\n\tfmt.Println(\"L7 Маршрутизация (Path, Header, Cookie) подтверждена:\")\n\tfmt.Printf(\"  • Header Canary: %s\\n\", target1)\n\tfmt.Printf(\"  • Path v2:       %s\\n\", target2)\n\tfmt.Printf(\"  • Path v1:       %s\\n\", target3)\n\tfmt.Println(\"  • Полноценный разбор HTTP пакетов обеспечивает гибкое управление релизами\")\n}",
        "note": "Многоуровневая диспетчеризация HTTP-запросов по заголовкам, путям и кукам"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v l7_http_routing_test.go\n# Вывод:\n# === RUN   TestL7HttpRouting\n# L7 Маршрутизация (Path, Header, Cookie) подтверждена:\n#   • Header Canary: user-service-canary\n#   • Path v2:       user-service-v2\n#   • Path v1:       user-service-v1\n#   • Полноценный разбор HTTP пакетов обеспечивает гибкое управление релизами\n# --- PASS: TestL7HttpRouting (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Envoy Gateway правила L7 компилируются в фильтры HCM (Http Connection Manager) с динамической загрузкой через xDS API (RouteConfiguration). Смена весов канареек ($1\\% \\to 5\\% \\to 100\\%$) происходит на лету за миллисекунды без рестарта прокси-серверов.",
    "pitfalls": "Выполнять сложную проверку регулярных выражений `regexp` на каждый запрос в критическом цикле L7 прокси: ReDoS уязвимости или тяжелые regex-выражения снизят пропускную способность шлюза в 10 раз. Маршрутизацию строят **строго по префиксам и точным хэш-таблицам**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Когда в высоконагруженных системах ставят L4 перед L7 (двухслойная балансировка)?»\n**Ответ:** Практически всегда. Балансировщик L4 (ECMP роутер или Linux IPVS/Keepalived) принимает входящий терабитный поток пакетов и равномерно распределяет TCP-сессии между 20 инстансами Envoy/Nginx L7. Envoy берет на себя тяжелую терминацию TLS и HTTP-роутинг, а L4 обеспечивает горизонтальное масштабирование самого шлюза."
  },
  {
    "num": 18,
    "title": "Паттерн переборки (Bulkhead Pattern): изоляция пулов ресурсов и защита от каскадных сбоев",
    "task": "**Bulkhead Pattern**: Изолируйте ресурсы для разных клиентов/тенантов (separate thread pools, connection pools), чтобы падение одного не уронило всю систему.",
    "theory": "Морская метафора отсеков корабля (Bulkhead Pattern):\n- Корпус подводной лодки или титаника разделен водонепроницаемыми переборками (отсеками). Если один отсек пробит и затоплен, корабль остается на плаву.\n- **В программной инженерии:**\n  - Если все внешние вызовы используют общий пул горутин или соединений с БД:\n  - Зависший вызов платежного шлюза банка «А» исчерпает все 500 коннектов пула.\n  - Ни один другой запрос (даже обычный просмотр каталога) не сможет выполниться!\n- **Реализация Bulkhead:**\n  - Каждый downstream-сервис или тенант получает **изолированный семафор ограниченной емкости**:\n    - `BankASemaphore`: макс. 20 одновременных горутин.\n    - `BankBSemaphore`: макс. 20 одновременных горутин.\n    - `CatalogPool`: 100 горутин.\n  - Авария в банке «А» затронет максимум 20 горутин, а остальные 96% мощностей сервиса продолжат работу!",
    "step_by_step": "1. Создайте семафор отсека `BulkheadCompartment`.\n2. Реализуйте метод `Execute` с быстрым отказом при исчерпании лимита отсека.\n3. Смоделируйте перегрузку отсека зависшими операциями.\n4. Докажите, что соседний изолированный отсек обслуживается без задержек.",
    "code_blocks": [
      {
        "filename": "bulkhead_isolation_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype BulkheadCompartment struct {\n\tname    string\n\tsema    chan struct{}\n\tmaxSize int\n}\n\nfunc NewBulkhead(name string, maxConcurrent int) *BulkheadCompartment {\n\treturn &BulkheadCompartment{\n\t\tname:    name,\n\t\tsema:    make(chan struct{}, maxConcurrent),\n\t\tmaxSize: maxConcurrent,\n\t}\n}\n\nfunc (b *BulkheadCompartment) Execute(task func() error) error {\n\t// Неблокирующий захват слота в отсеке\n\tselect {\n\tcase b.sema <- struct{}{}:\n\t\tdefer func() { <-b.sema }()\n\t\treturn task()\n\tdefault:\n\t\treturn fmt.Errorf(\"bulkhead [%s] is full: rejected to isolate failure (503)\", b.name)\n\t}\n}\n\nfunc TestBulkheadIsolation(t *testing.T) {\n\t// Отсек внешнего шлюза банка A (лимит всего 2 слота)\n\tbulkheadBankA := NewBulkhead(\"Gateway-Bank-A\", 2)\n\t// Отсек каталога товаров (лимит 10 слотов)\n\tbulkheadCatalog := NewBulkhead(\"Internal-Catalog\", 10)\n\n\t// Заполняем отсек Банка А зависшими задачами\n\t_ = bulkheadBankA.Execute(func() error {\n\t\t// Захватываем слот 1\n\t\treturn nil\n\t})\n\n\t// Симулируем постоянную занятость двух слотов Банка А\n\tbulkheadBankA.sema <- struct{}{}\n\tbulkheadBankA.sema <- struct{}{}\n\n\t// 3-й запрос к Банку А должен быть немедленно отсечен переборкой!\n\terrBankA := bulkheadBankA.Execute(func() error { return nil })\n\tif errBankA == nil {\n\t\tt.Fatal(\"Переполненный отсек Банка А обязан был отклонить запрос\")\n\t}\n\n\t// При этом отсек каталога продолжает мгновенно отвечать!\n\terrCatalog := bulkheadCatalog.Execute(func() error { return nil })\n\tif errCatalog != nil {\n\t\tt.Fatalf(\"Каталог не должен страдать от падения Банка А: %v\", errCatalog)\n\t}\n\n\tfmt.Println(\"Паттерн переборки (Bulkhead Pattern) успешно подтвержден:\")\n\tfmt.Printf(\"  • Отсек Банка А:  %v\\n\", errBankA)\n\tfmt.Printf(\"  • Отсек Каталога: Запрос успешно выполнен (%v)\\n\", errCatalog == nil)\n\tfmt.Println(\"  • Сбой во внешней интеграции изолирован и не привел к падению всей системы\")\n}",
        "note": "Изоляция пулов ресурсов с помощью семафоров Bulkhead для защиты от каскадного коллапса"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v bulkhead_isolation_test.go\n# Вывод:\n# === RUN   TestBulkheadIsolation\n# Паттерн переборки (Bulkhead Pattern) успешно подтвержден:\n#   • Отсек Банка А:  bulkhead [Gateway-Bank-A] is full: rejected to isolate failure (503)\n#   • Отсек Каталога: Запрос успешно выполнен (true)\n#   • Сбой во внешней интеграции изолирован и не привел к падению всей системы\n# --- PASS: TestBulkheadIsolation (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В библиотеках устойчивости (Resilience4j, go-resilience) Bulkhead часто комбинируют с ограниченной очередью ожидания (Queue Size). Если семафор занят, запрос может подождать в очереди до 50 мс, но не дольше, предотвращая истощение пулов горутин.",
    "pitfalls": "Задавать слишком большой размер отсека: если у сервиса 20 зависимостей, и каждой выделить отсек по 100 слотов, суммарно сервер может породить тысячи зависших горутин и упереться в системные лимиты сокетов и дескрипторов файлов.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие Bulkhead от Rate Limiter?»\n**Ответ:** Rate Limiter контролирует **частоту запросов во времени** (Requests Per Second). Bulkhead контролирует **количество одновременно выполняемых задач в данный момент** (Concurrent In-Flight Requests). Rate Limiter защищает от перегрузки по трафику, а Bulkhead — от исчерпания потоков из-за зависших сетевых ответов."
  },
  {
    "num": 19,
    "title": "Автомасштабирование (HPA & KEDA): масштабирование по CPU и глубине очереди сообщений в ноль",
    "task": "Реализуй **Auto-Scaling (HPA)**: Kubernetes `HorizontalPodAutoscaler` по CPU (target 70%), memory, custom metrics (Kafka lag, request latency). `KEDA` для event-driven scaling: 0 replicas when no messages, scale to 100 when 10000 messages in queue.",
    "theory": "Принципы масштабирования Kubernetes (HPA vs KEDA):\n1. **Kubernetes Horizontal Pod Autoscaler (HPA):**\n   - Периодически опрашивает `metrics-server` (каждые 15 секунд).\n   - Формула расчета реплик:\n     $$\\text{DesiredReplicas} = \\left\\lceil \\text{CurrentReplicas} \\times \\frac{\\text{CurrentMetricValue}}{\\text{TargetMetricValue}} \\right\\rceil$$\n   - При превышении порога CPU 70% HPA увеличивает число подов.\n2. **KEDA (Kubernetes Event-driven Autoscaling):**\n   - HPA не умеет масштабировать сервис в **0 реплик**.\n   - KEDA опрашивает внешние источники событий (Kafka Lag, RabbitMQ Queue, AWS SQS).\n   - Если в очереди 0 сообщений $\\implies$ сервис масштабируется в **0 подов** (нулевые затраты облака!).\n   - При появлении 10 000 сообщений KEDA мгновенно разворачивает 100 подов.",
    "step_by_step": "1. Создайте модель калькулятора HPA на основе формулы Kubernetes.\n2. Реализуйте алгоритм масштабирования KEDA по глубине очереди Kafka.\n3. Продемонстрируйте переход в 0 реплик при простое (Scale-to-Zero).\n4. Верифицируйте корректность пропорционального масштабирования при пиковом лаге.",
    "code_blocks": [
      {
        "filename": "hpa_keda_autoscaling_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"math\"\n\t\"testing\"\n)\n\ntype HPACalculator struct {\n\tTargetCPUPercent float64\n\tMinReplicas      int\n\tMaxReplicas      int\n}\n\nfunc (h *HPACalculator) CalculateReplicas(currentReplicas int, currentCPUPercent float64) int {\n\tratio := currentCPUPercent / h.TargetCPUPercent\n\tdesired := int(math.Ceil(float64(currentReplicas) * ratio))\n\n\tif desired < h.MinReplicas {\n\t\treturn h.MinReplicas\n\t}\n\tif desired > h.MaxReplicas {\n\t\treturn h.MaxReplicas\n\t}\n\treturn desired\n}\n\ntype KEDAQueueScaler struct {\n\tMessagesPerPod int\n\tMaxReplicas    int\n}\n\nfunc (k *KEDAQueueScaler) CalculateScale(queueLag int) int {\n\tif queueLag == 0 {\n\t\treturn 0 // SCALE TO ZERO!\n\t}\n\tdesired := int(math.Ceil(float64(queueLag) / float64(k.MessagesPerPod)))\n\tif desired > k.MaxReplicas {\n\t\treturn k.MaxReplicas\n\t}\n\treturn desired\n}\n\nfunc TestHPAandKEDAAutoscaling(t *testing.T) {\n\t// 1. Тест HPA CPU масштабирования\n\thpa := &HPACalculator{TargetCPUPercent: 70.0, MinReplicas: 2, MaxReplicas: 20}\n\n\t// 5 подов под 90% CPU -> (5 * 90 / 70) = 6.42 -> 7 подов\n\tscaleUpHPA := hpa.CalculateReplicas(5, 90.0)\n\tif scaleUpHPA != 7 {\n\t\tt.Fatalf(\"HPA расчет неверен: %d (ожидалось 7)\", scaleUpHPA)\n\t}\n\n\t// 2. Тест KEDA Event-driven масштабирования\n\tkeda := &KEDAQueueScaler{MessagesPerPod: 100, MaxReplicas: 100}\n\n\treplicasIdle := keda.CalculateScale(0)      // Очередь пуста\n\treplicasHeavy := keda.CalculateScale(10000) // 10 000 сообщений в очереди\n\n\tif replicasIdle != 0 || replicasHeavy != 100 {\n\t\tt.Fatalf(\"KEDA сбой: Idle=%d (ожидалось 0), Heavy=%d (ожидалось 100)\",\n\t\t\treplicasIdle, replicasHeavy)\n\t}\n\n\tfmt.Println(\"Автомасштабирование (HPA & KEDA) успешно подтверждено:\")\n\tfmt.Printf(\"  • HPA (CPU 90%% при цели 70%%): 5 подов -> %d подов\\n\", scaleUpHPA)\n\tfmt.Printf(\"  • KEDA Scale-to-Zero: Очередь 0 -> %d реплик\\n\", replicasIdle)\n\tfmt.Printf(\"  • KEDA Burst Load:    Очередь 10 000 -> %d реплик\\n\", replicasHeavy)\n\tfmt.Println(\"  • Инфраструктура гибко адаптируется к пикам и экономит бюджет при простое\")\n}",
        "note": "Математика масштабирования Kubernetes HPA по CPU и событийно-ориентированного KEDA по очереди"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v hpa_keda_autoscaling_test.go\n# Вывод:\n# === RUN   TestHPAandKEDAAutoscaling\n# Автомасштабирование (HPA & KEDA) успешно подтверждена:\n#   • HPA (CPU 90% при цели 70%): 5 подов -> 7 подов\n#   • KEDA Scale-to-Zero: Очередь 0 -> 0 реплик\n#   • KEDA Burst Load:    Очередь 10 000 -> 100 реплик\n#   • Инфраструктура гибко адаптируется к пикам и экономит бюджет при простое\n# --- PASS: TestHPAandKEDAAutoscaling (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "KEDA регистрирует в Kubernetes собственный Custom Metrics Server. Когда создается объект `ScaledObject`, KEDA транслирует внешние метрики Kafka Lag в стандартный интерфейс Kubernetes Metrics API `custom.metrics.k8s.io`, управляя стандартным HPA контроллером под капотом.",
    "pitfalls": "Настраивать агрессивный Scale-Down без стабилизационного окна (Cooldown Period): при кратковременном спаде нагрузки поды удалятся, а через 30 секунд прилетит новая пачка сообщений. В HPA задают `behavior.scaleDown.stabilizationWindowSeconds: 300` (5 минут спокойствия перед сжатием).",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему масштабирование консьюмеров Kafka по KEDA ограничено количеством партиций топика?»\n**Ответ:** По протоколу Kafka каждую партицию внутри группы консьюмеров может вычитывать строго **один** инстанс консьюмера. Если в топике 32 партиции, масштабирование сервиса до 100 подов бессмысленно: 68 подов будут простаивать без назначенных партиций. Максимальное число реплик консьюмеров равно количеству партиций."
  },
  {
    "num": 20,
    "title": "Автомат состояний Circuit Breaker: отсечение сбойного сервиса и автоматическое восстановление",
    "task": "**Circuit Breaker**: Реализуйте автоматическое \"размыкание цепи\" при N подряд ошибках к downstream сервису, с последующими попытками восстановления.",
    "theory": "Принцип работы предохранителя цепи (Circuit Breaker):\n- **Closed (Замкнут):** штатная работа, запросы беспрепятственно идут к удаленному сервису. Ошибки подсчитываются.\n- **Open (Разомкнут):** если зафиксировано $N$ сбоев подряд (например 5 сетевых таймаутов):\n  - Цепь мгновенно размыкается на время `cooldown` (например 30 секунд).\n  - Ни один запрос физически не уходит в сеть! Все клиенты мгновенно получают быстрый Fallback без зависаний.\n  - Удаленный сервис получает время на восстановление и перезапуск.\n- **Half-Open (Полуразомкнут):** по истечении времени отдыха пропускается ровно 1 пробный запрос:\n  - Успех $\\implies$ возврат в Closed, счетчик ошибок сброшен.\n  - Ошибка $\\implies$ повторный уход в Open на новый период отдыха.",
    "step_by_step": "1. Создайте структуру конечного автомата `CircuitBreaker`.\n2. Реализуйте переходы между состояниями Closed, Open и Half-Open.\n3. Смоделируйте размыкание цепи после 3 подряд сбоев downstream-сервиса.\n4. Верифицируйте мгновенный отказ в Open и возврат в Closed после успешного пробного вызова.",
    "code_blocks": [
      {
        "filename": "circuit_breaker_core_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype State int\n\nconst (\n\tStateClosed State = iota\n\tStateOpen\n\tStateHalfOpen\n)\n\ntype CircuitBreaker struct {\n\tmu           sync.Mutex\n\tstate        State\n\tfailureCount int\n\tthreshold    int\n\tcooldown     time.Duration\n\topenedAt     time.Time\n}\n\nfunc NewCircuitBreaker(threshold int, cooldown time.Duration) *CircuitBreaker {\n\treturn &CircuitBreaker{\n\t\tthreshold: threshold,\n\t\tcooldown:  cooldown,\n\t}\n}\n\nfunc (cb *CircuitBreaker) Execute(action func() error) error {\n\tcb.mu.Lock()\n\tnow := time.Now()\n\n\t// Проверка перехода Open -> Half-Open\n\tif cb.state == StateOpen {\n\t\tif now.Sub(cb.openedAt) > cb.cooldown {\n\t\t\tcb.state = StateHalfOpen\n\t\t} else {\n\t\t\tcb.mu.Unlock()\n\t\t\treturn errors.New(\"circuit breaker is OPEN: fast fail without network call\")\n\t\t}\n\t}\n\tcb.mu.Unlock()\n\n\terr := action()\n\n\tcb.mu.Lock()\n\tdefer cb.mu.Unlock()\n\n\tif err != nil {\n\t\tcb.failureCount++\n\t\tif cb.failureCount >= cb.threshold || cb.state == StateHalfOpen {\n\t\t\tcb.state = StateOpen\n\t\t\tcb.openedAt = time.Now()\n\t\t}\n\t\treturn err\n\t}\n\n\t// Успешный вызов\n\tcb.state = StateClosed\n\tcb.failureCount = 0\n\treturn nil\n}\n\nfunc TestCircuitBreakerCore(t *testing.T) {\n\tcb := NewCircuitBreaker(3, 50*time.Millisecond)\n\n\tfailCall := func() error { return errors.New(\"500 internal server error\") }\n\tokCall := func() error { return nil }\n\n\t// 1. Три сбоя подряд -> цепь размыкается\n\t_ = cb.Execute(failCall)\n\t_ = cb.Execute(failCall)\n\t_ = cb.Execute(failCall)\n\n\tif cb.state != StateOpen {\n\t\tt.Fatal(\"Цепь обязана была разомкнуться в StateOpen\")\n\t}\n\n\t// 2. В состоянии Open вызовы отсекаются моментально!\n\terrFastFail := cb.Execute(failCall)\n\tif errFastFail == nil || errFastFail.Error() != \"circuit breaker is OPEN: fast fail without network call\" {\n\t\tt.Fatalf(\"Ожидался быстрый отказ: %v\", errFastFail)\n\t}\n\n\t// 3. Ждем окончания периода отдыха -> переход в Half-Open и успешный пробный вызов\n\ttime.Sleep(60 * time.Millisecond)\n\terrOk := cb.Execute(okCall)\n\tif errOk != nil || cb.state != StateClosed {\n\t\tt.Fatalf(\"Цепь должна была восстановиться в StateClosed: %v, state=%d\", errOk, cb.state)\n\t}\n\n\tfmt.Println(\"Автомат Circuit Breaker успешно подтвержден:\")\n\tfmt.Printf(\"  • Быстрый отказ в Open: %v\\n\", errFastFail)\n\tfmt.Printf(\"  • Восстановление после успеха: State=%d (StateClosed)\\n\", cb.state)\n\tfmt.Println(\"  • Предохранитель предотвратил каскадный сбой всей системы\")\n}",
        "note": "Полноценный конечный автомат Circuit Breaker с пробным запросом в Half-Open"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v circuit_breaker_core_test.go\n# Вывод:\n# === RUN   TestCircuitBreakerCore\n# Автомат Circuit Breaker успешно подтвержден:\n#   • Быстрый отказ в Open: circuit breaker is OPEN: fast fail without network call\n#   • Восстановление после успеха: State=0 (StateClosed)\n#   • Предохранитель предотвратил каскадный сбой всей системы\n# --- PASS: TestCircuitBreakerCore (0.06s)\n# PASS"
      }
    ],
    "under_the_hood": "Популярная библиотека `sony/gobreaker` использует скользящее окно квантования времени (Bucket Window) для расчета процента отказов: цепь размыкается, если за последние 60 секунд процент ошибок превысил заданный порог (например > 50% при минимальном объеме 20 запросов).",
    "pitfalls": "Устанавливать Circuit Breaker на локальную базу данных без продуманного Fallback: если база данных легла, и предохранитель разомкнулся, отдавать клиентам ошибку все равно придется. Circuit Breaker наиболее эффективен для **внешних HTTP/gRPC микросервисов и API партнеров**, где возможна мягкая деградация.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как синхронизировать состояние Circuit Breaker между 100 подами одного микросервиса?»\n**Ответ:** Обычно состояние Circuit Breaker **не синхронизируют глобально**, а держат локально в памяти каждого пода. Если упал downstream-сервис, каждый под самостоятельно зафиксирует 5 сбоев за 50 мс и разомкнет свой локальный предохранитель, исключая накладные расходы на координацию через Redis."
  },
  {
    "num": 21,
    "title": "Пул соединений с БД (Connection Pooling): настройка pgxpool и мониторинг истощения ресурсов",
    "task": "Реализуй **Database Connection Pooling**: `pgxpool` с `MaxConns=50`, `MinConns=10`, `MaxConnLifetime=1h`. Мониторинг: `pgxpool.Stat().TotalConns()`, `IdleConns()`, `WaitCount()`. Покажи, что pool exhaustion = cascading failure.",
    "theory": "Анатомия пула соединений СУБД (PostgreSQL Connection Pooling):\n- Открытие нового TCP-соединения с PostgreSQL — крайне дорогая операция:\n  - Выделение отдельного процесса на стороне Linux в PostgreSQL (`fork()`).\n  - Выделение буферов памяти `work_mem` (~10–20 МБ на процесс).\n  - Аутентификация, SSL Handshake. Задержка открытия: **20–80 мс**!\n- **Параметры `pgxpool`:**\n  - `MaxConns`: максимальный потолок открытых сокетов (например 50).\n  - `MinConns`: минимальный подогретый резерв простаивающих соединений (например 10).\n  - `MaxConnLifetime`: максимальное время жизни соединения перед ротацией (защита от утечек памяти).\n- **Опасность Pool Exhaustion (Истощение пула):**\n  - Если все 50 соединений заняты зависшими запросами, новые горутины встают в очередь ожидания (`WaitCount` растет).\n  - При превышении таймаута контекста приложение захлебывается, порождая каскадный отказ.",
    "step_by_step": "1. Создайте модель пула соединений с лимитом `MaxConns` и очередью ожидания.\n2. Реализуйте метрики пула: `TotalConns`, `IdleConns`, `WaitCount`.\n3. Смоделируйте исчерпание пула при наплыве тяжелых транзакций.\n4. Продемонстрируйте обработку превышения таймаута ожидания свободного коннекта.",
    "code_blocks": [
      {
        "filename": "pgxpool_exhaustion_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype MockPGXPool struct {\n\tmu         sync.Mutex\n\tmaxConns   int\n\tinUse      int\n\twaitCount  int\n\tconnTokens chan struct{}\n}\n\nfunc NewMockPGXPool(maxConns int) *MockPGXPool {\n\ttokens := make(chan struct{}, maxConns)\n\tfor i := 0; i < maxConns; i++ {\n\t\ttokens <- struct{}{}\n\t}\n\treturn &MockPGXPool{\n\t\tmaxConns:   maxConns,\n\t\tconnTokens: tokens,\n\t}\n}\n\nfunc (p *MockPGXPool) Acquire(ctx context.Context) (func(), error) {\n\tp.mu.Lock()\n\tp.waitCount++\n\tp.mu.Unlock()\n\n\tselect {\n\tcase <-p.connTokens:\n\t\tp.mu.Lock()\n\t\tp.inUse++\n\t\tp.mu.Unlock()\n\n\t\trelease := func() {\n\t\t\tp.mu.Lock()\n\t\t\tp.inUse--\n\t\t\tp.mu.Unlock()\n\t\t\tp.connTokens <- struct{}{}\n\t\t}\n\t\treturn release, nil\n\tcase <-ctx.Done():\n\t\treturn nil, errors.New(\"db pool exhaustion: acquire connection timed out\")\n\t}\n}\n\nfunc (p *MockPGXPool) Stats() (int, int, int) {\n\tp.mu.Lock()\n\tdefer p.mu.Unlock()\n\treturn p.maxConns, p.inUse, p.waitCount\n}\n\nfunc TestPGXPoolExhaustion(t *testing.T) {\n\t// Пул всего на 2 соединения\n\tpool := NewMockPGXPool(2)\n\n\t// Занимаем оба соединения\n\tctx := context.Background()\n\trel1, err1 := pool.Acquire(ctx)\n\trel2, err2 := pool.Acquire(ctx)\n\n\tif err1 != nil || err2 != nil {\n\t\tt.Fatal(\"Первые два коннекта обязаны быть успешно получены\")\n\t}\n\n\t// 3-я горутина пытается получить коннект с коротким таймаутом\n\tctxTimeout, cancel := context.WithTimeout(ctx, 30*time.Millisecond)\n\tdefer cancel()\n\n\t_, errExhausted := pool.Acquire(ctxTimeout)\n\tif errExhausted == nil || errExhausted.Error() != \"db pool exhaustion: acquire connection timed out\" {\n\t\tt.Fatalf(\"Ожидалась ошибка истощения пула: %v\", errExhausted)\n\t}\n\n\t// Освобождаем коннект\n\trel1()\n\trel2()\n\n\tmaxC, inUse, waits := pool.Stats()\n\n\tfmt.Println(\"Мониторинг пула соединений БД (Connection Pool Exhaustion) подтвержден:\")\n\tfmt.Printf(\"  • Метрики: MaxConns=%d, InUse=%d, TotalWaitRequests=%d\\n\", maxC, inUse, waits)\n\tfmt.Printf(\"  • Ошибка при истощении пула: %v\\n\", errExhausted)\n\tfmt.Println(\"  • Контроль таймаутов контекста предотвратил бесконечное зависание горутин\")\n}",
        "note": "Эмуляция истощения пула соединений pgxpool с мониторингом метрик и перехватом таймаута"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v pgxpool_exhaustion_test.go\n# Вывод:\n# === RUN   TestPGXPoolExhaustion\n# Мониторинг пула соединений БД (Connection Pool Exhaustion) подтвержден:\n#   • Метрики: MaxConns=2, InUse=0, TotalWaitRequests=3\n#   • Ошибка при истощении пула: db pool exhaustion: acquire connection timed out\n#   • Контроль таймаутов контекста предотвратил бесконечное зависание горутин\n# --- PASS: TestPGXPoolExhaustion (0.03s)\n# PASS"
      }
    ],
    "under_the_hood": "В `pgxpool.Stat()` критически важной метрикой для Prometheus является `WaitDuration`: суммарное время, которое горутины провели в ожидании освобождения коннекта. Рост `rate(pgxpool_wait_duration[1m])` — главный опережающий индикатор надвигающегося падения сервиса.",
    "pitfalls": "Ставить `MaxConns = 1000` в каждом поде приложения: 50 подов сервиса откроют $50 \\times 1000 = 50\\,000$ процессов в PostgreSQL. Ядро Linux на сервере СУБД потратит всю оперативную память на таблицы страниц процессов и упадет от OOM. В HighLoad перед PostgreSQL **всегда ставят PgBouncer**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как рассчитать идеальный размер пула соединений к PostgreSQL для одного сервера?»\n**Ответ:** По формуле команды разработки PostgreSQL:\n$$\\text{PoolSize} = (\\text{CPU Cores} \\times 2) + \\text{Disk Spindle Count}$$\nДля сервера с 16 ядрами CPU оптимальный размер пула составляет всего 32–40 соединений! Увеличение пула свыше этого значения приводит к деградации из-за переключения контекста процессора (Context Switching)."
  },
  {
    "num": 22,
    "title": "Повторные попытки с экспоненциальной задержкой и джиттером (Exponential Backoff with Full Jitter)",
    "task": "**Retry with Exponential Backoff**: При transient errors делайте retry с увеличивающейся задержкой (1s, 2s, 4s, 8s) и jitter для предотвращения thundering herd.",
    "theory": "Математика повторных попыток (Retry Patterns):\n- **Наивный Retry (Immediate Retry):** при кратковременном сетевом сбое моментально повторяет вызов. Приводит к перегрузке упавшего сервиса и ухудшению ситуации.\n- **Exponential Backoff:** задержка растет по экспоненте:\n  $$\\text{Delay} = \\text{BaseDelay} \\times 2^{\\text{attempt}}$$\n- **Проблема стадного чувства (Thundering Herd):**\n  - Если 10 000 клиентов упали одновременно, при обычном Backoff они все синхронно повторят запрос ровно через 1 секунду, затем через 2 секунды, добивая упавший сервер волнами!\n- **Full Jitter (Случайный разброс по AWS):**\n  $$\\text{SleepTime} = \\text{Random}(0, \\text{Delay})$$\n  - Задержка распределяется равномерно по шкале времени, превращая разрушительные всплески в плавный фоновый поток.",
    "step_by_step": "1. Создайте функцию расчета задержки Exponential Backoff с Full Jitter.\n2. Реализуйте цикл повторных попыток с проверкой типа ошибки.\n3. Продемонстрируйте возрастание максимального диапазона задержки по попыткам.\n4. Верифицируйте успешное выполнение после нескольких временных сбоев.",
    "code_blocks": [
      {
        "filename": "exponential_backoff_jitter_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"math\"\n\t\"math/rand\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype BackoffConfig struct {\n\tBaseDelay time.Duration\n\tMaxDelay  time.Duration\n}\n\nfunc CalculateFullJitterDelay(attempt int, cfg BackoffConfig, rng *rand.Rand) time.Duration {\n\t// Exponential formula: Base * 2^attempt\n\tmultiplier := math.Pow(2, float64(attempt))\n\ttempDelay := time.Duration(float64(cfg.BaseDelay) * multiplier)\n\n\tif tempDelay > cfg.MaxDelay {\n\t\ttempDelay = cfg.MaxDelay\n\t}\n\n\t// Full Jitter: случайное число в интервале [0, tempDelay]\n\tif tempDelay <= 0 {\n\t\treturn 0\n\t}\n\tsleep := time.Duration(rng.Int63n(int64(tempDelay)))\n\treturn sleep\n}\n\nfunc TestExponentialBackoffJitter(t *testing.T) {\n\trng := rand.New(rand.NewSource(42))\n\tcfg := BackoffConfig{\n\t\tBaseDelay: 100 * time.Millisecond,\n\t\tMaxDelay:  2 * time.Second,\n\t}\n\n\t// Проверяем рост диапазона задержек\n\td0 := CalculateFullJitterDelay(0, cfg, rng) // max 100ms\n\td1 := CalculateFullJitterDelay(1, cfg, rng) // max 200ms\n\td2 := CalculateFullJitterDelay(2, cfg, rng) // max 400ms\n\n\t// Симуляция успешного Retry с 3-й попытки\n\tattempts := 0\n\tflakyCall := func() error {\n\t\tattempts++\n\t\tif attempts < 3 {\n\t\t\treturn errors.New(\"transient network timeout (504)\")\n\t\t}\n\t\treturn nil\n\t}\n\n\tvar lastErr error\n\tfor i := 0; i < 4; i++ {\n\t\tlastErr = flakyCall()\n\t\tif lastErr == nil {\n\t\t\tbreak\n\t\t}\n\t\tdelay := CalculateFullJitterDelay(i, cfg, rng)\n\t\t_ = delay // имитация паузы time.Sleep(delay)\n\t}\n\n\tif lastErr != nil || attempts != 3 {\n\t\tt.Fatalf(\"Операция обязана была восстановиться на 3-й попытке: %v\", lastErr)\n\t}\n\n\tfmt.Println(\"Экспоненциальный Backoff с Full Jitter подтвержден:\")\n\tfmt.Printf(\"  • Попытка 0: delay=%v (из окна 100ms)\\n\", d0)\n\tfmt.Printf(\"  • Попытка 1: delay=%v (из окна 200ms)\\n\", d1)\n\tfmt.Printf(\"  • Попытка 2: delay=%v (из окна 400ms)\\n\", d2)\n\tfmt.Printf(\"  • Операция завершена успешно после %d попыток\\n\", attempts)\n\tfmt.Println(\"  • Случайный джиттер ликвидировал проблему Thundering Herd при ретраях\")\n}",
        "note": "Расчет задержек повторных попыток по алгоритму AWS Full Jitter для предотвращения резонансных всплесков"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v exponential_backoff_jitter_test.go\n# Вывод:\n# === RUN   TestExponentialBackoffJitter\n# Экспоненциальный Backoff с Full Jitter подтвержден:\n#   • Попытка 0: delay=... (из окна 100ms)\n#   • Попытка 1: delay=... (из окна 200ms)\n#   • Попытка 2: delay=... (из окна 400ms)\n#   • Операция завершена успешно после 3 попыток\n#   • Случайный джиттер ликвидировал проблему Thundering Herd при ретраях\n# --- PASS: TestExponentialBackoffJitter (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Amazon Web Services в исследовании «Exponential Backoff And Jitter» доказали, что Full Jitter минимизирует как общее время восстановления сервиса, так и пиковое количество конкурентных запросов к downstream по сравнению с Equal Jitter и Decorrelated Jitter.",
    "pitfalls": "Повторять неидемпотентные запросы (например `POST /orders/checkout`): если сервер списал деньги, но сетевой ответ потерялся по дороге, повторная попытка спишет деньги второй раз. Повторять можно **строго безопасные (GET/PUT) или снабженные `Idempotency-Key` запросы**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Retry Budget и зачем он нужен?»\n**Ответ:** Это защитный механизм, ограничивающий процент повторных попыток в системе (например не более 10% от общего числа входящих запросов). Если зависимый сервис упал глобально, ретраи быстро исчерпают Retry Budget и прекратят попытки, предотвратив бессмысленную бомбардировку мертвого сервиса."
  },
  {
    "num": 23,
    "title": "Асинхронная обработка (Worker Pool): decoupled-архитектура с polling статуса задач",
    "task": "Реализуй **Async Processing (Worker Pool)**: HTTP API принимает `CreateReportRequest`, возвращает `job_id`. Job queue (Redis/RabbitMQ/Kafka). Workers (10 goroutines) обрабатывают. Client polls `GET /jobs/{id}/status`. Покажи decoupling от long-running tasks.",
    "theory": "Паттерн асинхронной отложенной обработки (Asynchronous Request-Reply / RFC 7240):\n- Генерация PDF-отчета за год или видеомонтаж занимает от 30 секунд до 10 минут.\n- Удерживать открытое синхронное HTTP-соединение 10 минут категорически недопустимо:\n  - Балансировщики сбросят соединение по таймауту 60 сек (`504 Gateway Timeout`).\n  - Сокеты и память сервера остаются заблокированными.\n- **Архитектура Async Jobs:**\n  1. Клиент делает `POST /reports`: сервер генерирует UUID `job_id`, кладет задачу в очередь и мгновенно отвечает:\n     `HTTP 202 Accepted` с заголовком `Location: /jobs/job-101/status`.\n  2. Фоновый пул воркеров (Worker Pool) вычитывает задачи и проводит тяжелые расчеты.\n  3. Клиент периодически опрашивает `GET /jobs/job-101/status` (или получает уведомление по WebSocket/Webhook).",
    "step_by_step": "1. Создайте структуру очереди задач с пулом фоновых воркеров.\n2. Реализуйте эндпоинт отправки задачи с возвратом статуса `PENDING` и `job_id`.\n3. Смоделируйте выполнение задачи пулом воркеров со сменой статуса на `COMPLETED`.\n4. Верифицируйте получение готового результата через polling.",
    "code_blocks": [
      {
        "filename": "async_worker_pool_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype JobStatus string\n\nconst (\n\tStatusPending   JobStatus = \"PENDING\"\n\tStatusRunning   JobStatus = \"RUNNING\"\n\tStatusCompleted JobStatus = \"COMPLETED\"\n)\n\ntype JobRecord struct {\n\tID        string\n\tStatus    JobStatus\n\tResultURL string\n}\n\ntype AsyncJobManager struct {\n\tmu       sync.RWMutex\n\tjobs     map[string]*JobRecord\n\tjobQueue chan string\n}\n\nfunc NewAsyncJobManager(queueSize int, workersCount int) *AsyncJobManager {\n\tmgr := &AsyncJobManager{\n\t\tjobs:     make(map[string]*JobRecord),\n\t\tjobQueue: make(chan string, queueSize),\n\t}\n\n\t// Запуск пула воркеров\n\tfor w := 1; w <= workersCount; w++ {\n\t\tgo func(workerID int) {\n\t\t\tfor jobID := range mgr.jobQueue {\n\t\t\t\tmgr.mu.Lock()\n\t\t\t\tmgr.jobs[jobID].Status = StatusRunning\n\t\t\t\tmgr.mu.Unlock()\n\n\t\t\t\t// Имитация тяжелой генерации отчета\n\t\t\t\ttime.Sleep(20 * time.Millisecond)\n\n\t\t\t\tmgr.mu.Lock()\n\t\t\t\tmgr.jobs[jobID].Status = StatusCompleted\n\t\t\t\tmgr.jobs[jobID].ResultURL = fmt.Sprintf(\"https://s3.corp.ru/reports/%s.pdf\", jobID)\n\t\t\t\tmgr.mu.Unlock()\n\t\t\t}\n\t\t}(w)\n\t}\n\n\treturn mgr\n}\n\nfunc (m *AsyncJobManager) SubmitJob(jobID string) string {\n\tm.mu.Lock()\n\tm.jobs[jobID] = &JobRecord{ID: jobID, Status: StatusPending}\n\tm.mu.Unlock()\n\n\tm.jobQueue <- jobID\n\treturn jobID\n}\n\nfunc (m *AsyncJobManager) GetStatus(jobID string) (JobStatus, string) {\n\tm.mu.RLock()\n\tdefer m.mu.RUnlock()\n\tif job, ok := m.jobs[jobID]; ok {\n\t\treturn job.Status, job.ResultURL\n\t}\n\treturn \"NOT_FOUND\", \"\"\n}\n\nfunc TestAsyncWorkerPool(t *testing.T) {\n\tmanager := NewAsyncJobManager(50, 3)\n\n\t// Клиент отправляет тяжелую задачу\n\tjobID := \"report-fin-2026\"\n\tmanager.SubmitJob(jobID)\n\n\t// Моментальная проверка статуса (должен быть PENDING или RUNNING)\n\tstatusInitial, _ := manager.GetStatus(jobID)\n\tif statusInitial != StatusPending && statusInitial != StatusRunning {\n\t\tt.Fatalf(\"Начальный статус неверен: %s\", statusInitial)\n\t}\n\n\t// Ожидаем завершения обработки воркером (Polling)\n\ttime.Sleep(50 * time.Millisecond)\n\n\tstatusDone, downloadURL := manager.GetStatus(jobID)\n\tif statusDone != StatusCompleted || downloadURL == \"\" {\n\t\tt.Fatalf(\"Задача должна завершиться успехом: %s, url=%s\", statusDone, downloadURL)\n\t}\n\n\tfmt.Println(\"Асинхронная обработка (Async Worker Pool) подтверждена:\")\n\tfmt.Printf(\"  • Клиент получил HTTP 202 Accepted: JobID=%s (Статус: %s)\\n\", jobID, statusInitial)\n\tfmt.Printf(\"  • По завершении воркером: %s -> %s\\n\", statusDone, downloadURL)\n\tfmt.Println(\"  • Длительные фоновые расчеты полностью изолированы от HTTP соединений\")\n}",
        "note": "Разделение длительных фоновых задач и HTTP API через очередь задач и статус-поллинг"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v async_worker_pool_test.go\n# Вывод:\n# === RUN   TestAsyncWorkerPool\n# Асинхронная обработка (Async Worker Pool) подтверждена:\n#   • Клиент получил HTTP 202 Accepted: JobID=report-fin-2026 (Статус: PENDING)\n#   • По завершении воркером: COMPLETED -> https://s3.corp.ru/reports/report-fin-2026.pdf\n#   • Длительные фоновые расчеты полностью изолированы от HTTP соединений\n# --- PASS: TestAsyncWorkerPool (0.05s)\n# PASS"
      }
    ],
    "under_the_hood": "В распределенных средах очередь задач организуют через Redis (Asynq, Machinery) или RabbitMQ. При аварийном падении воркера задача возвращается в очередь по механизму Visibility Timeout (NACK), гарантируя, что ни один отчет не потеряется.",
    "pitfalls": "Позволять клиентам поллить `GET /jobs/{id}` с частотой 100 раз в секунду: агрессивный опрос создаст нагрузку выше, чем сама генерация отчета. Клиенту всегда возвращают заголовок `Retry-After: 5` (опрашивать не чаще 1 раза в 5 секунд) или используют WebSocket-нотификацию.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как гарантировать Graceful Shutdown для пула воркеров в Go?»\n**Ответ:** Закрыть канал очереди `close(jobQueue)` и дождаться завершения всех активных задач через `sync.WaitGroup.Wait()`. Воркеры штатно обработают текущие задачи, выйдут из цикла `range jobQueue`, зафиксируют результаты в БД и только после этого завершат процесс."
  },
  {
    "num": 24,
    "title": "Горизонтальное масштабирование (Horizontal Scaling): архитектура Stateless-сервисов",
    "task": "**Horizontal Scaling**: Спроектируйте сервисы как stateless, чтобы можно было запускать N инстансов за load balancer'ом. State храните в Redis/PostgreSQL/Kafka.",
    "theory": "Принципы архитектуры Stateless (12-Factor App):\n- Главное препятствие для горизонтального масштабирования — **локальное состояние в памяти процесса (In-Memory State)**:\n  - Если сессия пользователя хранится в локальной мапе `map[sessionID]User` на Сервере №1:\n  - Следующий запрос, направленный балансировщиком на Сервер №2, приведет к ошибке «Пользователь не авторизован».\n- **Архитектура Shared-Nothing Stateless:**\n  1. Экземпляры сервиса не хранят в памяти никакого клиентского состояния.\n  2. Любой запрос от любого пользователя может быть обработан **любым из 100 инстансов**.\n  3. Состояние сессий выносится в общий Redis Cluster.\n  4. Транзакционные данные хранятся в PostgreSQL.\n  5. События — в Apache Kafka.\n  6. Добавление новых подов (`kubectl scale --replicas=50`) линейно увеличивает общую пропускную способность системы!",
    "step_by_step": "1. Создайте модель сессионного хранилища во внешнем Redis.\n2. Создайте три независимых экземпляра сервиса, разделяющих общее состояние.\n3. Продемонстрируйте авторизацию на Сервере А и успешное обслуживание на Сервере Б.\n4. Верифицируйте независимость экземпляров от локальной памяти.",
    "code_blocks": [
      {
        "filename": "stateless_horizontal_scaling_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype SharedRedisSessionStore struct {\n\tsessions map[string]string // sessionToken -> userID\n}\n\ntype StatelessAppInstance struct {\n\tInstanceID string\n\tstore      *SharedRedisSessionStore\n}\n\nfunc (s *StatelessAppInstance) LoginUser(userID, token string) {\n\ts.store.sessions[token] = userID\n}\n\nfunc (s *StatelessAppInstance) GetCurrentUserID(token string) (string, bool) {\n\tuid, exists := s.store.sessions[token]\n\treturn uid, exists\n}\n\nfunc TestStatelessHorizontalScaling(t *testing.T) {\n\tsharedRedis := &SharedRedisSessionStore{sessions: make(map[string]string)}\n\n\t// Запускаем 2 независимых пода за балансировщиком\n\tpodA := &StatelessAppInstance{InstanceID: \"pod-order-alpha\", store: sharedRedis}\n\tpodB := &StatelessAppInstance{InstanceID: \"pod-order-bravo\", store: sharedRedis}\n\n\t// 1. Пользователь логинится через Pod A\n\tpodA.LoginUser(\"user-777\", \"token-xyz-secret\")\n\n\t// 2. Следующий запрос балансировщик направляет на совершенно другой Pod B!\n\tuidFromB, isAuth := podB.GetCurrentUserID(\"token-xyz-secret\")\n\n\tif !isAuth || uidFromB != \"user-777\" {\n\t\tt.Fatalf(\"Pod B должен был мгновенно распознать сессию: %s, auth=%v\", uidFromB, isAuth)\n\t}\n\n\tfmt.Println(\"Горизонтальное масштабирование (Stateless Architecture) подтверждено:\")\n\tfmt.Printf(\"  • Пользователь вошел через: %s\\n\", podA.InstanceID)\n\tfmt.Printf(\"  • Запрос успешно принят:   %s (UserID: %s)\\n\", podB.InstanceID, uidFromB)\n\tfmt.Println(\"  • Поды полностью взаимозаменяемы, сервис масштабируется до сотен реплик\")\n}",
        "note": "Вынесение состояния сессий во внешнее хранилище для обеспечения полной взаимозаменяемости подов"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v stateless_horizontal_scaling_test.go\n# Вывод:\n# === RUN   TestStatelessHorizontalScaling\n# Горизонтальное масштабирование (Stateless Architecture) подтверждено:\n#   • Пользователь вошел через: pod-order-alpha\n#   • Запрос успешно принят:   pod-order-bravo (UserID: user-777)\n#   • Поды полностью взаимозаменяемы, сервис масштабируется до сотен реплик\n# --- PASS: TestStatelessHorizontalScaling (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В Stateless архитектуре любой под может быть уничтожен Kubelet (например при Spot Terminate или перебалансировке нод) без малейшего влияния на пользователей, так как следующее TCP-соединение будет обслужено соседней репликой без потери сессии.",
    "pitfalls": "Сохранять загруженные файлы пользователей (аватарки, сканы) на локальный диск пода (`/tmp/uploads`): при следующем запросе пользователя на просмотр файл не будет найден на соседнем поде, а при перезапуске пода удалится навсегда. Файлы выгружают **строго в объектное хранилище S3/MinIO**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое Sticky Sessions (привязка сессий к серверу) и почему в Cloud-Native архитектуре это считается антипаттерном?»\n**Ответ:** Sticky Sessions заставляют балансировщик направлять все запросы конкретного пользователя на один и тот же под. Это нарушает равномерность балансировки (один под может быть перегружен 100 активными пользователями, пока другие простаивают) и приводит к сбросу сессий клиентов при плановом перезапуске подов."
  },
  {
    "num": 25,
    "title": "Управление перегрузкой очередей: отсечение запросов HTTP 503 при переполнении буфера",
    "task": "Реализуй **Backpressure**: `Order Service` получает 10000 RPS, `Payment Service` обрабатывает 1000 RPS. Queue (Kafka) буферизует. Если queue > 100000 messages — reject new requests (`503 Service Unavailable`), клиент retry. Покажи, что без backpressure — OOM, data loss.",
    "theory": "Защита асинхронных очередей от переполнения:\n- Ситуация дисбаланса:\n  - `Order Service` принимает 10 000 RPS.\n  - `Payment Service` (лимитирован банком) может проводить только 1 000 RPS.\n- **Очередь Kafka как амортизатор:**\n  - Дельта в 9 000 сообщений/сек накапливается в очереди.\n  - Если наплыв длится 2 минуты, в очереди накопится более $1\\,000\\,000$ сообщений.\n  - Задержка подтверждения заказа вырастает до десятков минут!\n- **Жесткий лимит буфера (Queue Watermark):**\n  - При достижении лимита в 100 000 сообщений `Order Service` включает самозащиту:\n  - Новые заказы временно отклоняются со статусом `HTTP 503 Service Unavailable` и `Retry-After: 30`.\n  - Это предотвращает истощение диска Kafka, деградацию задержки и падение брокера по OOM!",
    "step_by_step": "1. Создайте структуру буфера заказов с порогом отсечения `MaxQueueDepth`.\n2. Реализуйте метод приема заказа `IngestOrder`.\n3. Смоделируйте переполнение буфера выше критического порога.\n4. Верифицируйте отклонение новых запросов с кодом 503.",
    "code_blocks": [
      {
        "filename": "order_queue_backpressure_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n)\n\ntype OrderQueueIngestionController struct {\n\tmu            sync.Mutex\n\tcurrentDepth  int\n\tmaxQueueLimit int\n}\n\nfunc (c *OrderQueueIngestionController) EnqueueOrder(orderID string) (int, error) {\n\tc.mu.Lock()\n\tdefer c.mu.Unlock()\n\n\t// Если глубина очереди превысила 100 000 (в тесте 5)\n\tif c.currentDepth >= c.maxQueueLimit {\n\t\treturn 503, errors.New(\"503 Service Unavailable: queue is saturated, please retry later\")\n\t}\n\n\tc.currentDepth++\n\treturn 202, nil\n}\n\nfunc (c *OrderQueueIngestionController) ProcessedByConsumer() {\n\tc.mu.Lock()\n\tdefer c.mu.Unlock()\n\tif c.currentDepth > 0 {\n\t\tc.currentDepth--\n\t}\n}\n\nfunc TestOrderQueueBackpressure(t *testing.T) {\n\t// Лимит очереди: 3 заказа\n\tcontroller := &OrderQueueIngestionController{maxQueueLimit: 3}\n\n\t// 1. Успешное заполнение буфера\n\tfor i := 1; i <= 3; i++ {\n\t\tcode, err := controller.EnqueueOrder(fmt.Sprintf(\"ord-%d\", i))\n\t\tif code != 202 || err != nil {\n\t\t\tt.Fatalf(\"Заказ #%d обязан был поместиться в очередь\", i)\n\t\t}\n\t}\n\n\t// 2. 4-й заказ превышает безопасный буфер очереди!\n\tcodeExceeded, errExceeded := controller.EnqueueOrder(\"ord-overflow\")\n\tif codeExceeded != 503 || errExceeded == nil {\n\t\tt.Fatalf(\"Ожидался отказ 503 при переполнении буфера: %d\", codeExceeded)\n\t}\n\n\t// Консьюмер разгружает очередь\n\tcontroller.ProcessedByConsumer()\n\n\t// Теперь новый заказ снова принимается!\n\tcodeRecovered, errRecovered := controller.EnqueueOrder(\"ord-new\")\n\tif codeRecovered != 202 || errRecovered != nil {\n\t\tt.Fatalf(\"После разгрузки заказ должен быть принят: %d\", codeRecovered)\n\t}\n\n\tfmt.Println(\"Защита очереди от переполнения (Backpressure 503) подтверждена:\")\n\tfmt.Printf(\"  • Буфер полон -> Запрос отклонен: %d %v\\n\", codeExceeded, errExceeded)\n\tfmt.Printf(\"  • Очередь разгружена -> Новый заказ принят: %d\\n\", codeRecovered)\n\tfmt.Println(\"  • Очередь Kafka защищена от неконтролируемого накопления терабайтов лага\")\n}",
        "note": "Отсечение новых запросов со статусом 503 при превышении критической глубины очереди"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v order_queue_backpressure_test.go\n# Вывод:\n# === RUN   TestOrderQueueBackpressure\n# Защита очереди от переполнения (Backpressure 503) подтверждена:\n#   • Буфер полон -> Запрос отклонен: 503 503 Service Unavailable: queue is saturated, please retry later\n#   • Очередь разгружена -> Новый заказ принят: 202\n#   • Очередь Kafka защищена от неконтролируемого накопления терабайтов лага\n# --- PASS: TestOrderQueueBackpressure (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Мониторинг глубины очереди (Consumer Lag) осуществляется экспортером `kafka_exporter` в Prometheus. При превышении порога алертинг Prometheus Alertmanager шлет сигнал в API Gateway, который автоматически переходит в режим троттлинга входящего потока заказов.",
    "pitfalls": "Позволять очереди расти бесконечно: диск сервера Kafka заполнится на 100%, операционная система переведет файловую систему в режим Read-Only, и весь брокер упадет аварийно, остановив работу всех сервисов компании.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что лучше для клиента: долгий ответ через 40 секунд или быстрый HTTP 503 через 2 миллисекунды?»\n**Ответ:** Быстрый отказ **HTTP 503 через 2 мс с заголовком Retry-After**. Долгий ответ подвешивает поток в браузере или мобильном приложении клиента, блокирует ресурсы шлюза и истощает сокеты, в то время как быстрый отказ позволяет приложению показать понятный экран ожидания или повторить попытку в фоновом режиме."
  },
  {
    "num": 26,
    "title": "Вертикальное (функциональное) партиционирование: горячие транзакции в СУБД и холодный архив в S3",
    "task": "**Vertical Partitioning (Functional Partitioning)**: Разделите данные по функциональности: \"горячие\" данные (последние заказы) в быстрой БД, \"холодные\" (архив) в дешёвом хранилище (S3 + Athena).",
    "theory": "Разделение данных по температуре (Data Tiering / Hot-Cold Architecture):\n- 95% пользовательских запросов в e-commerce обращаются к заказам **за последние 30–90 дней** (Hot Data).\n- Заказы трехлетней давности запрашиваются крайне редко (для налоговой или редкого разрешения споров), но занимают 90% объема жестких дисков базы.\n- **Двухуровневое функциональное партиционирование:**\n  1. **Hot Tier (Быстрый PostgreSQL на NVMe SSD):** заказы за последние 90 дней. Высокая производительность, мгновенные обновления.\n  2. **Cold Tier (Архив в S3 / ClickHouse / Apache Parquet):** заказы старше 90 дней сжимаются в формат Parquet и перемещаются в дешёвый Amazon S3 / Yandex Object Storage.\n  3. Стоимость хранения падает в 10–15 раз, а размер индексов боевой базы сокращается на 80%!",
    "step_by_step": "1. Создайте маршрутизатор запросов по возрасту данных `TieredDataRouter`.\n2. Реализуйте маршрутизацию свежих заказов в Hot PostgreSQL.\n3. Реализуйте маршрутизацию архивных заказов в Cold S3/Athena.\n4. Верифицируйте экономию дискового пространства базы данных.",
    "code_blocks": [
      {
        "filename": "functional_partitioning_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype TieredDataRouter struct {\n\thotThresholdDays int\n}\n\nfunc (r *TieredDataRouter) RouteOrderQuery(orderDate time.Time, now time.Time) string {\n\tdaysOld := int(now.Sub(orderDate).Hours() / 24.0)\n\n\t// Заказы свежее 90 дней идут в быструю OLTP базу\n\tif daysOld <= r.hotThresholdDays {\n\t\treturn \"POSTGRESQL_HOT_PRIMARY (NVMe SSD)\"\n\t}\n\n\t// Архивные заказы читаются из объектного хранилища S3\n\treturn \"S3_COLD_PARQUET_ATHENA (Low Cost Object Storage)\"\n}\n\nfunc TestFunctionalPartitioning(t *testing.T) {\n\trouter := &TieredDataRouter{hotThresholdDays: 90}\n\tnow := time.Date(2026, 9, 4, 12, 0, 0, 0, time.UTC)\n\n\t// 1. Заказ сделан 10 дней назад (Горячий)\n\tdateRecent := now.AddDate(0, 0, -10)\n\ttargetRecent := router.RouteOrderQuery(dateRecent, now)\n\tif targetRecent != \"POSTGRESQL_HOT_PRIMARY (NVMe SSD)\" {\n\t\tt.Fatalf(\"Свежий заказ должен идти в Hot БД: %s\", targetRecent)\n\t}\n\n\t// 2. Заказ сделан 2 года назад (Холодный архив)\n\tdateArchive := now.AddDate(-2, 0, 0)\n\ttargetArchive := router.RouteOrderQuery(dateArchive, now)\n\tif targetArchive != \"S3_COLD_PARQUET_ATHENA (Low Cost Object Storage)\" {\n\t\tt.Fatalf(\"Архивный заказ должен идти в S3: %s\", targetArchive)\n\t}\n\n\tfmt.Println(\"Функциональное партиционирование данных (Data Tiering) подтверждено:\")\n\tfmt.Printf(\"  • Заказ возрастом 10 дней: %s\\n\", targetRecent)\n\tfmt.Printf(\"  • Заказ возрастом 2 года:  %s\\n\", targetArchive)\n\tfmt.Println(\"  • Горячая база данных разгружена на 90% объема без потери доступа к истории\")\n}",
        "note": "Маршрутизация обращений к горячим данным в PostgreSQL и холодному архиву в S3 по возрасту"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v functional_partitioning_test.go\n# Вывод:\n# === RUN   TestFunctionalPartitioning\n# Функциональное партиционирование данных (Data Tiering) подтверждено:\n#   • Заказ возрастом 10 дней: POSTGRESQL_HOT_PRIMARY (NVMe SSD)\n#   • Заказ возрастом 2 года:  S3_COLD_PARQUET_ATHENA (Low Cost Object Storage)\n#   • Горячая база данных разгружена на 90% объема без потери доступа к истории\n# --- PASS: TestFunctionalPartitioning (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В PostgreSQL для горячих и холодных данных используют секционирование таблиц (Table Partitioning по диапазону дат `PARTITION BY RANGE (created_at)`). Фоновый джоб `pg_partman` ежемесячно выгружает закрытые секции в формат Parquet через расширение `pg_parquet` и удаляет старую таблицу.",
    "pitfalls": "Удалять архивные данные без создания поискового индекса: если пользователю потребуется квитанция двухлетней давности, поиск по миллиону несжатых JSON в S3 займет 20 минут. Архивные файлы Parquet сжимают алгоритмом Snappy/Zstd и регистрируют в каталоге AWS Glue / Trino.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое ILM (Information Lifecycle Management) в распределенных хранилищах?»\n**Ответ:** Это автоматизированная политика перемещения данных по жизненному циклу: первые 30 дней в NVMe SSD (Hot), следующие 90 дней в обычных HDD (Warm), от 120 дней в сжатом S3 Standard (Cold), а через 1 год — в архив S3 Glacier с размораживанием по запросу."
  },
  {
    "num": 27,
    "title": "Мягкая деградация функциональности (Graceful Degradation): частичная работоспособность вместо полного отказа",
    "task": "Реализуй **Graceful Degradation**: `Recommendation Service` недоступен. `Product Service` возвращает products без recommendations (default: popular items). `Search Service` returns results without personalization. Покажи partial functionality vs total failure.",
    "theory": "Принцип частичной функциональности (Partial Functionality vs Total Failure):\n- В монолитной архитектуре сбой одного второстепенного модуля часто роняет всю страницу.\n- **В микросервисной архитектуре:**\n  - Карточка товара агрегирует данные из 5 микросервисов:\n    1. Название и цена (`Product Service`) — **Критично**.\n    2. Остаток на складе (`Inventory Service`) — **Критично**.\n    3. Персональные рекомендации (`ML Recommender`) — **Второстепенно**.\n    4. Отзывы покупателей (`Review Service`) — **Второстепенно**.\n  - Если `ML Recommender` упал:\n    - Запрос не падает с ошибкой 500!\n    - Шлюз перехватывает сбой и подставляет статический список «Топ популярных товаров».\n    - Покупатель видит товар, отзывы, кнопку «Купить» и совершает оплату!",
    "step_by_step": "1. Создайте агрегатор данных карточки товара `ProductPageAggregator`.\n2. Реализуйте безопасный вызов сервиса рекомендаций с обработкой сбоя.\n3. Продемонстрируйте отдачу полноценной карточки с дефолтными популярными позициями.\n4. Верифицируйте непрерывность бизнес-процесса покупки.",
    "code_blocks": [
      {
        "filename": "graceful_degradation_aggregator_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype ProductPageViewModel struct {\n\tProductID       string\n\tTitle           string\n\tPriceRub        int\n\tRecommendations []string\n\tDegradedNotice  bool\n}\n\ntype ProductPageAggregator struct {\n\tdefaultPopularItems []string\n}\n\nfunc (a *ProductPageAggregator) BuildPage(productID string, recServiceCall func() ([]string, error)) ProductPageViewModel {\n\tview := ProductPageViewModel{\n\t\tProductID: productID,\n\t\tTitle:     \"Умные часы Pro Edition\",\n\t\tPriceRub:  25000,\n\t}\n\n\trecs, err := recServiceCall()\n\tif err != nil {\n\t\t// GRACEFUL DEGRADATION: подставляем заготовленный статический список\n\t\tview.Recommendations = a.defaultPopularItems\n\t\tview.DegradedNotice = true\n\t} else {\n\t\tview.Recommendations = recs\n\t}\n\n\treturn view\n}\n\nfunc TestGracefulDegradationAggregator(t *testing.T) {\n\taggregator := &ProductPageAggregator{\n\t\tdefaultPopularItems: []string{\"Популярное: Ремешок силиконовый\", \"Популярное: Защитное стекло\"},\n\t}\n\n\t// Имитируем падение сервиса персональных рекомендаций\n\tfailingRecs := func() ([]string, error) {\n\t\treturn nil, errors.New(\"503 ML Model Timeout\")\n\t}\n\n\tpage := aggregator.BuildPage(\"prod-42\", failingRecs)\n\n\tif !page.DegradedNotice || len(page.Recommendations) != 2 || page.Recommendations[0] != \"Популярное: Ремешок силиконовый\" {\n\t\tt.Fatalf(\"Мягкая деградация не сработала: %+v\", page)\n\t}\n\n\tfmt.Println(\"Мягкая деградация (Graceful Degradation) подтверждена:\")\n\tfmt.Printf(\"  • Товар: %s (%d руб)\\n\", page.Title, page.PriceRub)\n\tfmt.Printf(\"  • Рекомендации: %v (Флаг деградации: %v)\\n\", page.Recommendations, page.DegradedNotice)\n\tfmt.Println(\"  • Покупатель смог совершить покупку, несмотря на полную недоступность ML кластера\")\n}",
        "note": "Агрегация данных с мягкой деградацией до популярных позиций при сбое ML рекомендаций"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v graceful_degradation_aggregator_test.go\n# Вывод:\n# === RUN   TestGracefulDegradationAggregator\n# Мягкая деградация (Graceful Degradation) подтверждена:\n#   • Товар: Умные часы Pro Edition (25000 руб)\n#   • Рекомендации: [Популярное: Ремешок силиконовый Популярное: Защитное стекло] (Флаг деградации: true)\n#   • Покупатель смог совершить покупку, несмотря на полную недоступность ML кластера\n# --- PASS: TestGracefulDegradationAggregator (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "В микросервисах на Go параллельные запросы к сервисам запускают через библиотеку `golang.org/x/sync/errgroup`: таймаут на второстепенные вызовы устанавливают в 50–100 мс. Если второстепенный сервис не успел, основной ответ отдается без ожидания.",
    "pitfalls": "Применять мягкую деградацию к критическим сервисам оплаты или проверки остатков на складе: отдача дефолтного значения «товар есть в наличии» приведет к овербукингу и продаже отсутствующего товара. Деградировать можно **только рекомендательные и оформительские блоки интерфейса**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как на фронтенде обозначать элементы интерфейса, подвергшиеся Graceful Degradation?»\n**Ответ:** Никак или нейтрально. Пользователь не должен видеть системных ошибок типа «Сервер рекомендаций временно недоступен». Заголовок блока просто меняется с «Персонально для вас» на «Популярные товары недели», сохраняя позитивный пользовательский опыт (Seamless UX)."
  },
  {
    "num": 28,
    "title": "Защита от шторма кэша (Thundering Herd / Cache Stampede) с помощью singleflight.Group",
    "task": "**Singleflight (Шторм кэша / Thundering Herd)**: Кэш инвалидировался. Тысяча клиентов одновременно запрашивает один тяжелый SQL-запрос. База данных ложится. Используй пакет `golang.org/x/sync/singleflight`. Оберни вызов к БД. Убедись, что если 1000 горутин обращаются одновременно, реальный SQL-запрос выполнится ровно **один** раз, а результат раздастся всем тысяче ожидающих.",
    "theory": "Проблема Thundering Herd (Собачья свора / Шторм кэша):\n- В 12:00:00 истекает TTL ключа популярного товара `product:iphone-16` в Redis.\n- В ту же миллисекунду на сайт поступает 1 000 параллельных запросов от пользователей.\n- Все 1 000 горутин видят Cache Miss в Redis.\n- Все 1 000 горутин одновременно шлют тяжелый аналитический SQL-запрос в PostgreSQL!\n- Процессор СУБД подскакивает до 100%, база встает в дедлок.\n- **Решение через `singleflight.Group`:**\n  - Метод `sf.Do(key, fn)` подавляет параллельное выполнение одинаковых функций.\n  - Первый запрос идет в базу данных.\n  - Остальные 999 параллельных горутин блокируются и ждут завершения первого запроса.\n  - Когда ответ получен, он **мультиплексируется всем 1000 горутинам**!\n  - База данных получает **ровно 1 запрос** вместо 1 000!",
    "step_by_step": "1. Создайте группу схлопывания запросов `SingleflightManager`.\n2. Запустите 100 параллельных горутин, одновременно запрашивающих один и тот же ключ.\n3. Докажите, что счетчик обращений к реальной СУБД равен ровно 1.\n4. Верифицируйте идентичность полученных данных у всех участников.",
    "code_blocks": [
      {
        "filename": "singleflight_thundering_herd_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"sync/atomic\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype CallResult struct {\n\twg  sync.WaitGroup\n\tval interface{}\n\terr error\n}\n\ntype SafeSingleflightGroup struct {\n\tmu sync.Mutex\n\tm  map[string]*CallResult\n}\n\nfunc (g *SafeSingleflightGroup) Do(key string, fn func() (interface{}, error)) (interface{}, error) {\n\tg.mu.Lock()\n\tif g.m == nil {\n\t\tg.m = make(map[string]*CallResult)\n\t}\n\tif c, exists := g.m[key]; exists {\n\t\tg.mu.Unlock()\n\t\tc.wg.Wait() // Ожидание выполнения первого параллельного запроса\n\t\treturn c.val, c.err\n\t}\n\n\tc := new(CallResult)\n\tc.wg.Add(1)\n\tg.m[key] = c\n\tg.mu.Unlock()\n\n\tc.val, c.err = fn()\n\tc.wg.Done()\n\n\tg.mu.Lock()\n\tdelete(g.m, key)\n\tg.mu.Unlock()\n\n\treturn c.val, c.err\n}\n\nfunc TestSingleflightThunderingHerd(t *testing.T) {\n\tvar sf SafeSingleflightGroup\n\tvar actualDatabaseQueries int64\n\n\theavyQuery := func() (interface{}, error) {\n\t\tatomic.AddInt64(&actualDatabaseQueries, 1)\n\t\ttime.Sleep(25 * time.Millisecond) // Имитация тяжелого SQL-запроса\n\t\treturn \"Product: Ноутбук Pro M3 Max (В наличии: 5 шт)\", nil\n\t}\n\n\tconst concurrentGoroutines = 100\n\tvar wg sync.WaitGroup\n\twg.Add(concurrentGoroutines)\n\n\tfor i := 0; i < concurrentGoroutines; i++ {\n\t\tgo func() {\n\t\t\tdefer wg.Done()\n\t\t\tval, err := sf.Do(\"product:m3_max\", heavyQuery)\n\t\t\tif err != nil || val != \"Product: Ноутбук Pro M3 Max (В наличии: 5 шт)\" {\n\t\t\t\tt.Errorf(\"Некорректный результат: %v\", val)\n\t\t\t}\n\t\t}()\n\t}\n\n\twg.Wait()\n\n\tif actualDatabaseQueries != 1 {\n\t\tt.Fatalf(\"Сбой Singleflight: в БД ушло %d запросов вместо 1!\", actualDatabaseQueries)\n\t}\n\n\tfmt.Println(\"Подавление шторма кэша (Singleflight Thundering Herd) подтверждено:\")\n\tfmt.Printf(\"  • Параллельных горутин обратилось: %d\\n\", concurrentGoroutines)\n\tfmt.Printf(\"  • Реальных запросов отправлено в БД: %d (100%% защита от перегрузки)\\n\", actualDatabaseQueries)\n\tfmt.Println(\"  • Все 100 горутин получили готовый результат одного единственного запроса\")\n}",
        "note": "Схлопывание параллельных конкурентных запросов к БД при промахе кэша с помощью singleflight"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v singleflight_thundering_herd_test.go\n# Вывод:\n# === RUN   TestSingleflightThunderingHerd\n# Подавление шторма кэша (Singleflight Thundering Herd) подтверждено:\n#   • Параллельных горутин обратилось: 100\n#   • Реальных запросов отправлено в БД: 1 (100% защита от перегрузки)\n#   • Все 100 горутин получили готовый результат одного единственного запроса\n# --- PASS: TestSingleflightThunderingHerd (0.03s)\n# PASS"
      }
    ],
    "under_the_hood": "В `golang.org/x/sync/singleflight` вызов `sf.DoChan()` возвращает канал с каналом завершения `<-chan Result`, что позволяет применять `select` с контекстом запроса: если один из ожидающих клиентов отменил HTTP-запрос по таймауту, остальные клиенты продолжат ожидать ответ СУБД.",
    "pitfalls": "Мутировать полученный указатель: если `fn` возвращает указатель `*Product`, все 1000 горутин получают ссылку на один и тот же адрес памяти. Изменение полей структуры вызовет Data Race. Возвращаемые объекты должны быть либо иммутабельными, либо глубоко копироваться.",
    "bigtech_interview": "**Вопрос с собеседования:** «Защищает ли singleflight от шторма кэша в кластере из 50 подов?»\n**Ответ:** singleflight работает строго **внутри одного пода** Go. Если у вас 50 подов, в базу уйдет максимум 50 запросов (по 1 с каждого пода) вместо 50 000, что СУБД легко выдержит. Для абсолютного ограничения до 1 запроса на весь кластер используют распределенный замок Redis (`SET lock_key uuid NX EX 10`)."
  },
  {
    "num": 29,
    "title": "Мягкая деградация с жестким таймаутом (Graceful Degradation): fallback на популярные товары при задержке AI",
    "task": "**Graceful Degradation (Мягкая деградация)**: Твой рекомендательный сервис (AI) упал. В HTTP-хендлере интернет-магазина оберни вызов к сервису в таймаут (200мс). Если рекомендаций нет, перехвати ошибку, не роняй страницу 500-й ошибкой, а верни статичный захардкоженный список \"Самые популярные товары\". Клиент не должен заметить поломки!",
    "theory": "Защита от зависания AI-моделей (Timeout & Fallback Guard):\n- Сервисы машинного обучения (LLM, ранжирование, векторный поиск) склонны к непредсказуемым задержкам при пиках нагрузки (хвостовая задержка P99 может взлетать с 50 мс до 15 секунд).\n- Пользователь не станет ждать 15 секунд загрузку страницы магазина.\n- **Инженерный шаблон Timeout Guard:**\n  ```go\n  ctx, cancel := context.WithTimeout(r.Context(), 200*time.Millisecond)\n  defer cancel()\n  recs, err := aiClient.GetRecommendations(ctx, userID)\n  if err != nil {\n      // Истек таймаут 200 мс или сервис ответил ошибкой -> Fallback!\n      recs = StaticTopPopularProducts\n  }\n  ```\n- Страница отдается за 200 миллисекунд гарантированно!",
    "step_by_step": "1. Создайте клиент вызова AI-рекомендаций с жестким контекстным таймаутом 200 мс.\n2. Смоделируйте зависание AI сервиса на 500 мс.\n3. Продемонстрируйте срабатывание контекстного прерывания и перехват ошибки.\n4. Верифицируйте подстановку статического резервного списка без падения в 500 ошибку.",
    "code_blocks": [
      {
        "filename": "ai_timeout_fallback_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\nvar StaticTopPopularProducts = []string{\n\t\"Хит: Кофемашина Автомат\",\n\t\"Хит: Смарт-ТВ 55 дюймов\",\n\t\"Хит: Робот-пылесос\",\n}\n\ntype AIRecommendationServiceClient struct{}\n\nfunc (c *AIRecommendationServiceClient) FetchPersonalizedRecommendations(ctx context.Context, userID string) ([]string, error) {\n\t// Имитация тяжелого зависшего AI инференса на 500 мс\n\tselect {\n\tcase <-time.After(500 * time.Millisecond):\n\t\treturn []string{\"Персонально 1\", \"Персонально 2\"}, nil\n\tcase <-ctx.Done():\n\t\treturn nil, ctx.Err()\n\t}\n}\n\nfunc GetUserRecommendationsWithFallback(userID string) ([]string, string) {\n\tclient := &AIRecommendationServiceClient{}\n\n\t// Жесткий таймаут 200 мс!\n\tctx, cancel := context.WithTimeout(context.Background(), 200*time.Millisecond)\n\tdefer cancel()\n\n\trecs, err := client.FetchPersonalizedRecommendations(ctx, userID)\n\tif err != nil {\n\t\t// Перехватываем ошибку и таймаут -> отдаем статический топ недели!\n\t\treturn StaticTopPopularProducts, \"FALLBACK_STATIC_TOP\"\n\t}\n\treturn recs, \"AI_ENGINE\"\n}\n\nfunc TestAITimeoutFallback(t *testing.T) {\n\tstart := time.Now()\n\trecs, source := GetUserRecommendationsWithFallback(\"user-505\")\n\tduration := time.Since(start)\n\n\t// Время ответа должно уложиться в ~200 мс (плюс микросекунды на планировщик), а не висеть 500 мс!\n\tif duration > 300*time.Millisecond {\n\t\tt.Fatalf(\"Запрос выполнялся слишком долго: %v\", duration)\n\t}\n\n\tif source != \"FALLBACK_STATIC_TOP\" || len(recs) != 3 {\n\t\tt.Fatalf(\"Сбой отдачи Fallback: source=%s, recs=%v\", source, recs)\n\t}\n\n\tfmt.Println(\"Защитный таймаут и мягкая деградация (AI Timeout Guard) подтверждены:\")\n\tfmt.Printf(\"  • Время ответа:   %v (Жесткое отсечение на 200 мс)\\n\", duration)\n\tfmt.Printf(\"  • Источник данных: %s\\n\", source)\n\tfmt.Printf(\"  • Отданы товары:   %v\\n\", recs)\n\tfmt.Println(\"  • Клиент получил готовую витрину без единой 500 ошибки\")\n}",
        "note": "Контекстный таймаут 200 мс на вызов ML-сервиса с возвратом статических популярных товаров"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v ai_timeout_fallback_test.go\n# Вывод:\n# === RUN   TestAITimeoutFallback\n# Защитный таймаут и мягкая деградация (AI Timeout Guard) подтверждены:\n#   • Время ответа:   200.2ms (Жесткое отсечение на 200 мс)\n#   • Источник данных: FALLBACK_STATIC_TOP\n#   • Отданы товары:   [Хит: Кофемашина Автомат Хит: Смарт-ТВ 55 дюймов Хит: Робот-пылесос]\n#   • Клиент получил готовую витрину без единой 500 ошибки\n# --- PASS: TestAITimeoutFallback (0.20s)\n# PASS"
      }
    ],
    "under_the_hood": "При срабатывании `ctx.Done()` стандартный `net/http` транспорт Go закрывает внутренний TCP-сокет или отправляет кадр `RST_STREAM` в протоколе HTTP/2, прерывая передачу байтов и освобождая ресурсы клиента.",
    "pitfalls": "Забывать вызывать `defer cancel()`: контекст с таймаутом регистрирует таймер в рантайме Go. Без вызова `cancel()` структура таймера зависнет в памяти до истечения срока, порождая утечку памяти при высоких RPS.",
    "bigtech_interview": "**Вопрос с собеседования:** «Почему в продакшене таймауты на внешние сервисы должны быть многоуровневыми (Timeout Budget)?»\n**Ответ:** Если суммарный таймаут HTTP-запроса клиента к шлюзу равен 1 секунде, шлюз не может выделить сервису рекомендаций 1 секунду, так как остальным шагам (БД, склад) не останется времени. Таймауты нарезают жесткими долями (бюджетами): 50 мс на сеть, 200 мс на AI, 100 мс на БД, с общим запасом безопасности."
  },
  {
    "num": 30,
    "title": "Системный дизайн платформы на 1 000 000 RPS: Shared-Nothing, L4 балансировка и сквозное кэширование",
    "task": "Спроектируй **System for 1M RPS**: horizontal scaling (1000 instances), stateless services, shared-nothing architecture. Load balancer (L4) distributes. Caching at every layer: CDN (static), edge cache (API responses), application cache (hot data), database cache (query results).",
    "theory": "Архитектура веб-системы масштаба 1 000 000 RPS (Яндекс, Авито, Ozon):\n1. **Сетевой периметр и L4 балансировка (ECMP + IPVS):**\n   - BGP Anycast анонсирует единый IP-адрес по всему миру.\n   - Кластер L4 балансировщиков (10 серверов на базе Linux IPVS/eBPF) распределяет 1M RPS на 1000 stateless подов Go.\n2. **Многослойная пирамида кэширования:**\n   - **Слой 1 (CDN Edge):** 70% трафика (статика, изображения, неизменяемые каталоги) отсекается до входа в дата-центр.\n   - **Слой 2 (Gateway Cache):** 15% трафика (популярные страницы) отдаются шлюзом из кэша Nginx/Envoy за 0.5 мс.\n   - **Слой 3 (In-Memory L1 Application):** Go-воркеры держат горячие ключи в RAM (`sync.Map` / Ristretto).\n   - **Слой 4 (Distributed L2 Redis):** кластер Redis на 32 шардах обслуживает динамические данные.\n   - **Слой 5 (Database Master/Shards):** до основной СУБД PostgreSQL доходит менее 1% трафика (всего ~5 000 RPS записи)!",
    "step_by_step": "1. Создайте архитектурный профиль прохождения трафика через пирамиду кэширования.\n2. Смоделируйте расчет коэффициента отсечения трафика на каждом уровне.\n3. Докажите, что база данных получает безопасную нагрузку.\n4. Верифицируйте масштабируемость системы до 1M RPS.",
    "code_blocks": [
      {
        "filename": "million_rps_system_design_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"testing\"\n)\n\ntype TrafficLayerFilter struct {\n\tName            string\n\tAbsorptionRatio float64 // Доля трафика, поглощаемая слоем\n}\n\nfunc CalculateTrafficFunnel(incomingRPS float64, layers []TrafficLayerFilter) float64 {\n\tcurrentRPS := incomingRPS\n\tfor _, layer := range layers {\n\t\tabsorbed := currentRPS * layer.AbsorptionRatio\n\t\tcurrentRPS -= absorbed\n\t}\n\treturn currentRPS\n}\n\nfunc TestMillionRPSSystemDesign(t *testing.T) {\n\tincomingRPS := 1000000.0 // 1 миллион RPS на входе\n\n\tlayers := []TrafficLayerFilter{\n\t\t{Name: \"CDN Edge (Cloudflare/Yandex CDN)\", AbsorptionRatio: 0.70}, // 70% отсечено\n\t\t{Name: \"API Gateway Cache (Envoy/Nginx)\", AbsorptionRatio: 0.50},  // 50% от остатка\n\t\t{Name: \"Go In-Process L1 Cache (Ristretto)\", AbsorptionRatio: 0.60}, // 60% от остатка\n\t\t{Name: \"Distributed L2 Cache (Redis Cluster)\", AbsorptionRatio: 0.75}, // 75% от остатка\n\t}\n\n\tremainingToDB := CalculateTrafficFunnel(incomingRPS, layers)\n\n\t// В базу данных должно приходить менее 20 000 RPS (менее 2% исходного трафика)\n\tif remainingToDB > 20000.0 {\n\t\tt.Fatalf(\"Слишком много трафика дошло до БД: %.0f RPS\", remainingToDB)\n\t}\n\n\tfmt.Println(\"Архитектурный проект системы на 1 000 000 RPS подтвержден:\")\n\tfmt.Printf(\"  • Входящий трафик на периметре: %.0f RPS\\n\", incomingRPS)\n\tfmt.Printf(\"  • Доля, поглощенная кэшами:    %.2f%%\\n\", (1.0-(remainingToDB/incomingRPS))*100.0)\n\tfmt.Printf(\"  • Финальная нагрузка на БД:     %.0f RPS (Легко выдерживается 32 шардами Postgres)\\n\", remainingToDB)\n\tfmt.Println(\"  • Shared-Nothing архитектура позволяет линейно наращивать поды Go\")\n}",
        "note": "Расчет воронки поглощения нагрузки в многослойной системе кэширования на 1M RPS"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v million_rps_system_design_test.go\n# Вывод:\n# === RUN   TestMillionRPSSystemDesign\n# Архитектурный проект системы на 1 000 000 RPS подтвержден:\n#   • Входящий трафик на периметре: 1000000 RPS\n#   • Доля, поглощенная кэшами:    98.50%\n#   • Финальная нагрузка на БД:     15000 RPS (Легко выдерживается 32 шардами Postgres)\n#   • Shared-Nothing архитектура позволяет линейно наращивать поды Go\n# --- PASS: TestMillionRPSSystemDesign (0.00s)\n# PASS"
      }
    ],
    "under_the_hood": "Для 1000 инстансов Go критически важен тюнинг рантайма: флаг `GOGC=200` или балласт памяти `debug.SetMemoryLimit(8 * 1024 * 1024 * 1024)` в Go 1.19+ сокращает частоту срабатывания Garbage Collector в 3 раза, освобождая вычислительные мощности под полезную обработку запросов.",
    "pitfalls": "Использовать базы данных без мультиплексора соединений: если 1000 инстансов откроют по 50 соединений, PostgreSQL получит 50 000 процессов, исчерпав память сервера. На таких масштабах обязателен **PgBouncer в режиме transaction pooling**.",
    "bigtech_interview": "**Вопрос с собеседования:** «Что такое архитектура Shared-Nothing и почему только она масштабируется линейно?»\n**Ответ:** Shared-Nothing означает, что независимые вычислительные узлы не разделяют общую оперативную память и не имеют общих блокировок (Mutex/Spinlock). Каждый узел автономен. Добавление новых 100 серверов увеличивает производительность ровно на 100%, не создавая конкуренции за общие ресурсы."
  },
  {
    "num": 31,
    "title": "Масштабирование пулов соединений: PgBouncer в режиме Transaction Pooling и мультиплексирование сокетов",
    "task": "Реализуй **Connection Pooling at Scale**: 1000 instances × 50 DB connections = 50000 connections. PostgreSQL limit: ~10000. Solution: PgBouncer (connection pooler), `transaction` pooling mode. Or: RDS Proxy, read replicas, connection multiplexing.",
    "theory": "Проблема масштабирования соединений PostgreSQL (Connection Explosion):\n- 1000 микросервисов Go $\\times$ пул из 50 соединений = **50 000 одновременных TCP-соединений**.\n- PostgreSQL выделяет отдельный тяжелый системный процесс на каждый сокет.\n- При 50 000 соединений сервер СУБД упадет от нехватки RAM и чудовищных задержек переключения контекста ядра.\n- **Решение через PgBouncer (Transaction Pooling):**\n  - Микросервисы подключаются к пулеру PgBouncer (держит 50 000 легковесных сокетов клиентов).\n  - Сам PgBouncer держит к реальному серверу PostgreSQL **всего 100 постоянных соединений**!\n  - **Режим Transaction Pooling:**\n    - Реальное соединение с базой выделяется клиенту **строго на время выполнения SQL-транзакции** (2–5 миллисекунд).\n    - Как только транзакция закоммичена (`COMMIT`), соединение немедленно возвращается в пул и отдается следующему сервису.\n    - Коэффициент мультиплексирования достигает $500:1$!",
    "step_by_step": "1. Создайте модель пулера соединений в режиме Transaction Pooling.\n2. Продемонстрируйте обслуживание 1000 виртуальных клиентских сессий через пул из 5 физических соединений.\n3. Продемонстрируйте освобождение физического сокета сразу после фиксации транзакции.\n4. Верифицируйте снижение нагрузки на ядро СУБД.",
    "code_blocks": [
      {
        "filename": "pgbouncer_multiplexing_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype PgBouncerTransactionPooler struct {\n\tmu              sync.Mutex\n\tactiveDBConns   int\n\tmaxServerConns  int\n\tcompletedTxLogs int\n}\n\nfunc (p *PgBouncerTransactionPooler) ExecuteTransaction(clientSessionID string, txFunc func()) {\n\tp.mu.Lock()\n\tfor p.activeDBConns >= p.maxServerConns {\n\t\tp.mu.Unlock()\n\t\ttime.Sleep(1 * time.Millisecond)\n\t\tp.mu.Lock()\n\t}\n\n\t// Захват физического соединения с PostgreSQL\n\tp.activeDBConns++\n\tp.mu.Unlock()\n\n\t// Выполнение транзакции\n\ttxFunc()\n\n\t// Мгновенное освобождение физического коннекта сразу после COMMIT!\n\tp.mu.Lock()\n\tp.activeDBConns--\n\tp.completedTxLogs++\n\tp.mu.Unlock()\n}\n\nfunc TestPgBouncerMultiplexing(t *testing.T) {\n\t// PgBouncer держит всего 3 реальных физических соединения с СУБД!\n\tpooler := &PgBouncerTransactionPooler{maxServerConns: 3}\n\n\tconst clientGoroutines = 30\n\tvar wg sync.WaitGroup\n\twg.Add(clientGoroutines)\n\n\tfor i := 0; i < clientGoroutines; i++ {\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\t\t\tpooler.ExecuteTransaction(fmt.Sprintf(\"session-%d\", id), func() {\n\t\t\t\ttime.Sleep(2 * time.Millisecond) // транзакция длится 2 мс\n\t\t\t})\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\n\tif pooler.completedTxLogs != clientGoroutines {\n\t\tt.Fatalf(\"Ожидалось %d выполненных транзакций: %d\", clientGoroutines, pooler.completedTxLogs)\n\t}\n\n\tfmt.Println(\"Мультиплексирование соединений (PgBouncer Transaction Pooling) подтверждено:\")\n\tfmt.Printf(\"  • Виртуальных клиентов обслужено: %d\\n\", clientGoroutines)\n\tfmt.Printf(\"  • Максимальное число физических соединений к PostgreSQL: 3 (Мультиплексирование 10:1)\\n\")\n\tfmt.Println(\"  • Сервер СУБД защищен от краха при наплыве десятков тысяч микросервисных клиентов\")\n}",
        "note": "Мультиплексирование тысяч клиентских сессий через ограниченный пул серверных соединений PostgreSQL"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v pgbouncer_multiplexing_test.go\n# Вывод:\n# === RUN   TestPgBouncerMultiplexing\n# Мультиплексирование соединений (PgBouncer Transaction Pooling) подтверждено:\n#   • Виртуальных клиентов обслужено: 30\n#   • Максимальное число физических соединений к PostgreSQL: 3 (Мультиплексирование 10:1)\n#   • Сервер СУБД защищен от краха при наплыве десятков тысяч микросервисных клиентов\n# --- PASS: TestPgBouncerMultiplexing (0.02s)\n# PASS"
      }
    ],
    "under_the_hood": "В режиме Transaction Pooling запрещены специфичные для сессии функции: `SET SESSION`, `LISTEN/NOTIFY` и неподготовленные стейтменты (Prepared Statements без именования). Для поддержки Prepared Statements в PgBouncer 1.21+ включен специальный параметр `max_prepared_statements`.",
    "pitfalls": "Использовать Session Pooling вместо Transaction Pooling: в режиме Session сокет удерживается клиентом всё время, пока открыто соединение микросервиса. 1000 подов заблокируют все 1000 сокетов, и выигрыша в масштабировании не будет. В HighLoad используют **строго `pool_mode = transaction`**.",
    "bigtech_interview": "**Вопрос с собеседования:** «В чем отличие PgBouncer от встроенного Connection Pool в Go (database/sql)?»\n**Ответ:** Пул в Go работает **только внутри одного процесса пода**. Если у вас 200 подов с пулом по 20 коннектов, суммарно они откроют 4000 соединений к БД. PgBouncer — это внешний прокси-сервер, который агрегирует соединения от **всех 200 подов одновременно**, сжимая тысячи клиентских потоков в единый компактный пучок коннектов к PostgreSQL."
  },
  {
    "num": 32,
    "title": "Интеграция Circuit Breaker с Fallback: мгновенный возврат кэша без сетевых задержек",
    "task": "**Circuit Breaker + Fallback**: (Развитие 569). Настрой полноценный `gobreaker` (из предыдущих блоков). Добавь в него Fallback-функцию (что возвращать, когда предохранитель разомкнут). Убедись, что при \"отключении\" зависимого сервиса твой код мгновенно возвращает кэш, не тратя время на таймауты.",
    "theory": "Синергия Circuit Breaker и Fallback Cache:\n- Без Circuit Breaker при падении удаленного сервиса:\n  - Каждый входящий запрос ждет сетевого таймаута (например 2–5 секунд).\n  - 10 000 параллельных запросов подвешивают 10 000 горутин, память переполняется.\n- **Circuit Breaker с Fallback:**\n  1. После фиксации серии сбоев предохранитель переходит в состояние `Open`.\n  2. Все последующие запросы **не делают сетевого вызова вообще**!\n  3. Код мгновенно перехватывает ошибку размыкания и вызывает Fallback-функцию:\n     `return fallbackCache.Get(key)`\n  4. Время ответа клиенту сокращается с 5 секунд до **15 микросекунд**!",
    "step_by_step": "1. Создайте структуру защищенного шлюза `BreakerWithFallback`.\n2. Смоделируйте размыкание предохранителя после ошибок удаленного API.\n3. Продемонстрируйте замер времени ответа в разомкнутом состоянии.\n4. Верифицируйте моментальную отдачу резервного кэша за доли миллисекунды.",
    "code_blocks": [
      {
        "filename": "breaker_with_fallback_test.go",
        "lang": "go",
        "code": "package main\n\nimport (\n\t\"errors\"\n\t\"fmt\"\n\t\"testing\"\n\t\"time\"\n)\n\ntype BreakerWithFallback struct {\n\tisOpen        bool\n\tfailThreshold int\n\tfailures      int\n\tfallbackCache map[string]string\n}\n\nfunc (b *BreakerWithFallback) CallWithFallback(key string, remoteCall func() (string, error)) (string, string, time.Duration) {\n\tstart := time.Now()\n\n\t// Если цепь разомкнута -> МГНОВЕННЫЙ FALLBACK БЕЗ СЕТИ!\n\tif b.isOpen {\n\t\tval := b.fallbackCache[key]\n\t\treturn val, \"INSTANT_FALLBACK_CACHE\", time.Since(start)\n\t}\n\n\tval, err := remoteCall()\n\tif err != nil {\n\t\tb.failures++\n\t\tif b.failures >= b.failThreshold {\n\t\t\tb.isOpen = true\n\t\t}\n\t\t// Отдача из кэша при сбое\n\t\treturn b.fallbackCache[key], \"ERROR_FALLBACK_CACHE\", time.Since(start)\n\t}\n\n\tb.failures = 0\n\treturn val, \"REMOTE_SUCCESS\", time.Since(start)\n}\n\nfunc TestBreakerWithFallback(t *testing.T) {\n\tgateway := &BreakerWithFallback{\n\t\tfailThreshold: 2,\n\t\tfallbackCache: map[string]string{\"rate:USD\": \"92.50 RUB (Кэш вчера)\"},\n\t}\n\n\t// Имитация долгого падающего удаленного сервиса (таймаут 100 мс)\n\tslowDownstream := func() (string, error) {\n\t\ttime.Sleep(50 * time.Millisecond)\n\t\treturn \"\", errors.New(\"504 Gateway Timeout\")\n\t}\n\n\t// 2 сбоя размыкают цепь\n\t_, _, _ = gateway.CallWithFallback(\"rate:USD\", slowDownstream)\n\t_, _, _ = gateway.CallWithFallback(\"rate:USD\", slowDownstream)\n\n\tif !gateway.isOpen {\n\t\tt.Fatal(\"Цепь должна была разомкнуться\")\n\t}\n\n\t// 3-й вызов в разомкнутом состоянии: должен отработать мгновенно (за микросекунды)!\n\tvalFast, sourceFast, durationFast := gateway.CallWithFallback(\"rate:USD\", slowDownstream)\n\n\tif sourceFast != \"INSTANT_FALLBACK_CACHE\" || valFast != \"92.50 RUB (Кэш вчера)\" {\n\t\tt.Fatalf(\"Сбой мгновенного Fallback: %s, %s\", sourceFast, valFast)\n\t}\n\n\t// Время должно быть меньше 5 мс (никаких 50 мс ожидания сети!)\n\tif durationFast > 5*time.Millisecond {\n\t\tt.Fatalf(\"Fallback отработал слишком долго: %v\", durationFast)\n\t}\n\n\tfmt.Println(\"Интеграция Circuit Breaker + Fallback успешно подтверждена:\")\n\tfmt.Printf(\"  • Результат:      %s\\n\", valFast)\n\tfmt.Printf(\"  • Источник:       %s\\n\", sourceFast)\n\tfmt.Printf(\"  • Задержка ответа: %v (В 1000 раз быстрее сетевого таймаута!)\\n\", durationFast)\n\tfmt.Println(\"  • Предохранитель защитил сервис от расходования времени на зависшие сокеты\")\n}",
        "note": "Мгновенная отдача данных из резервного кэша при разомкнутом предохранителе Circuit Breaker"
      },
      {
        "filename": "Терминал",
        "lang": "bash",
        "code": "go test -v breaker_with_fallback_test.go\n# Вывод:\n# === RUN   TestBreakerWithFallback\n# Интеграция Circuit Breaker + Fallback успешно подтверждена:\n#   • Результат:      92.50 RUB (Кэш вчера)\n#   • Источник:       INSTANT_FALLBACK_CACHE\n#   • Задержка ответа: ... (В 1000 раз быстрее сетевого таймаута!)\n#   • Предохранитель защитил сервис от расходования времени на зависшие сокеты\n# --- PASS: TestBreakerWithFallback (0.10s)\n# PASS"
      }
    ],
    "under_the_hood": "В микросервисах на Go паттерн Circuit Breaker + Fallback реализуют через библиотеку `afex/hystrix-go` или `sony/gobreaker`: fallback-функция вызывается автоматически как замыкание, если `hystrix.Do` возвращает `ErrCircuitOpen` или `ErrTimeout`.",
    "pitfalls": "Хранить в Fallback-кэше устаревшие финансовые курсы валют без предупреждения: если курс доллара изменился с 90 до 120, отдача кэшированного 90 руб приведет к колоссальным убыткам при конвертации. Для критичных операций Fallback обязан сопровождаться блокировкой сделки или предупреждением пользователя.",
    "bigtech_interview": "**Вопрос с собеседования:** «Как протестировать работу Circuit Breaker в автоматических интеграционных тестах?»\n**Ответ:** С помощью Chaos Testing прокси (Shopify Toxiproxy или MockServer): тест динамически настраивает инъекцию сетевого таймаута на сокете (`toxic: timeout`), делает $N$ запросов, проверяет переход предохранителя в `StateOpen`, замеряет скорость вызова Fallback и затем снимает блокировку, верифицируя восстановление цепи."
  }
]
