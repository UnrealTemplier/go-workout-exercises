# Мокирование и интеграционное тестирование

В этом файле собраны уникальные упражнения по теме **"Мокирование и интеграционное тестирование"**, отсортированные по возрастанию сложности.

---

1. Объяви интерфейс `UserRepository` с методами `GetByID(id int) (*User, error)`, `Save(user *User) error`. Напиши **ручной мок** `MockUserRepository` с полями `GetByIDFunc`, `SaveFunc` (функции). Используй в тесте сервиса.

2. Создайте интерфейс `UserRepository` с методами `FindByID(id int) (*User, error)` и `Save(user *User) error`. Напишите сервис `UserService`, зависящий от этого интерфейса.

3. Напиши **spy/mock с вызовами**: `MockUserRepository` хранит `Calls []string` (`"GetByID:123"`, `"Save:Alice"`). Напиши тест, проверяющий не только результат, но и **порядок/количество вызовов**.

4. Напиши **stub** (заглушку): `FakeUserRepository` с предзаполненной мапой `users map[int]*User`. Методы возвращают данные из мапы без обращения к реальной БД. Используй для тестирования бизнес-логики.

5. Напиши **mock с состоянием**: `MockEmailService` считает, сколько писем отправлено (`SentCount int`). Напиши тест: после регистрации пользователя `SentCount` должен быть 1.

6. Реализуйте ручной мок (стаб): структура `MockUserRepository`, которая возвращает предопределённые данные. Протестируйте `UserService`, внедряя мок.

7. Напиши **mock, возвращающий ошибку по условию**: `MockPaymentGateway{FailAfterNCalls: 3}`. Первые 2 вызова — успех, 3-й — ошибка. Используй для тестирования retry-логики.

---

8. Напиши `MockEmailSender` (просто структуру, которая записывает аргументы в свои поля и возвращает `nil`). Используй его в тесте для `UserService`.

9. Установи `go install github.com/golang/mock/mockgen@latest`. Создай интерфейс `Cache` с методами `Get(key string) (string, error)`, `Set(key, value string, ttl time.Duration) error`. Сгенерируй мок: `mockgen -source=cache.go -destination=mock_cache.go -package=mocks`.

10. Напиши тест с `gomock.NewController(t)`. Создай `mockCache := mocks.NewMockCache(ctrl)`. Настрой ожидания: `mockCache.EXPECT().Get("user:123").Return("Alice", nil)`. Вызови тестируемую функцию. Проверь `ctrl.Finish()` (автоматический с Go 1.14+ через `t.Cleanup`).

11. Используйте `github.com/stretchr/testify/mock` для создания мока: проверьте, что метод `FindByID` был вызван с ожидаемым аргументом и вернул заданные значения.

12. Используй `github.com/stretchr/testify/mock`. Создай мок для `EmailSender`. Настрой ожидание: `mockObj.On("Send", "test@test.com", "Hello").Return(nil)`. Вызови метод и проверь `mockObj.AssertExpectations(t)`.

13. Напиши тест с **strict expectations**: `mockCache.EXPECT().Get(gomock.Any()).Times(2)`. Покажи, что если метод вызван 1 или 3 раза — тест падает. Используй `gomock.Any()`, `gomock.Eq()`, `gomock.Not()`.

14. Напиши тест с **mock, возвращающим динамическое значение**: `mockCache.EXPECT().Get(gomock.Any()).DoAndReturn(func(key string) (string, error) { return "prefix:" + key, nil })`. Используй для сложной логики мока.

---

15. **[Каверзный кейс]**: Тестирование зависящее от времени. Напиши функцию, которая ведет себя по-разному днем и ночью. Выдели `time.Now` в переменную или интерфейс. В тесте подменивай время на фиксированное.

16. Проверьте с помощью мока, что при ошибке репозитория сервис возвращает обёрнутую ошибку.

17. Напиши тест для handler'а с query-параметрами: `req := httptest.NewRequest("GET", "/search?q=test", nil)`. Проверь, что handler корректно парсит `r.URL.Query()`.

18. Напиши тест для POST handler'а с JSON: `body := strings.NewReader(`{"name":"Alice"}`)`, `req := httptest.NewRequest("POST", "/users", body)`. Установи `req.Header.Set("Content-Type", "application/json")`. Проверь статус и ответ.

19. Используй пакет `gomonkey` (или аналогичный) для monkey-patching (подмена функций во время выполнения, например `gomonkey.ApplyFunc(time.Now, func() time.Time { return fixedTime })`). *Примечание: работает только на Linux/macOS, на Windows требует выключения CFG.*

20. Напиши HTTP-обработчик `HealthHandler(w http.ResponseWriter, r *http.Request)`. В тесте используй `httptest.NewRequest("GET", "/health", nil)` и `httptest.NewRecorder()`. Вызови обработчик и проверь статус-код 200.

21. Напиши тест для middleware: создай `next := http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) { w.WriteHeader(200) })`. Оборачивай в `LoggingMiddleware(next)`. Проверь, что middleware вызывает `next` и логирует.

22. Напишите тест для HTTP-обработчика с использованием `httptest.NewRecorder` и `httptest.NewRequest`. Проверьте статус и тело ответа.

23. Напиши **end-to-end HTTP тест**: `httptest.NewServer(handler)`. Сделай реальный `http.Get(server.URL + "/path")`. Проверь статус и тело. Покажи, что сервер слушает на `127.0.0.1:random-port`.

24. Напиши тест для HTTP клиента с **таймаутом**: сервер спит 10 секунд, клиент имеет таймаут 100ms. Проверь `context.DeadlineExceeded` или кастомную ошибку таймаута.

25. Протестируйте обработчик с middleware (логирование, аутентификация): проверьте, что middleware добавляет заголовок или блокирует запрос без токена.

26. В тесте из предыдущего задания проверь тело ответа с помощью `recorder.Body.String()`.

27. Напиши тест для **reverse proxy**: `httptest.NewServer(httputil.NewSingleHostReverseProxy(targetURL))`. Проверь, что запрос проксируется на target и ответ возвращается.

---

28. **[Тестирование мидлвара]**: Напиши middleware для логирования. Оберни им `HealthHandler` в тесте и убедись, что запрос проходит через цепочку.

29. Создайте тестовый HTTP-сервер с `httptest.NewServer`, который возвращает фиксированный JSON. Напишите клиента и протестируйте его работу, направляя запросы на тестовый сервер.

30. **[Высокая сложность]**: Создай фейковый сервер с помощью `httptest.NewServer`. Напиши HTTP-клиент, который делает запрос к этому серверу. В тесте используй фейковый сервер вместо реального внешнего API.

31. Проверьте поведение клиента при ошибках сети: используйте `httptest.NewServer` с обработчиком, который закрывает соединение или возвращает 500.

32. **[Каверзный кейс]**: Напиши тест для обработчика, который внутри делает запрос к стороннему API (например, к GitHub API). Используй `httptest.NewServer` для создания мока этого API, чтобы тесты не зависели от интернета и не падали при достижении лимитов.

33. Напишите интеграционный тест для репозитория PostgreSQL. Используйте тестовую базу или `testcontainers-go` для поднятия контейнера с PostgreSQL. Выполните миграции и протестируйте `Save` и `FindByID`.

34. Установи `github.com/DATA-DOG/go-sqlmock`. Создай мок базы данных `sqlmock.New()`.

35. **Ручные моки (Dependency Injection)**: Напиши интерфейс `EmailSender` (метод `Send(to, body string) error`). Напиши структуру `UserService`, которая принимает этот интерфейс. В тестах создай `MockSender`, который просто сохраняет переданные email-ы в локальный срез (slice) и возвращает `nil`. Убедись, что сервис "отправил" письмо через мок.

36. Напиши **интеграционный тест** для HTTP API: запусти сервер на `httptest.NewServer` или реальном порту. Сделай `http.Post`, проверь статус, распарси JSON-ответ. Проверь side effects (данные в БД).

37. Используйте транзакции для изоляции тестов: каждый тест запускается в своей транзакции и откатывается, чтобы не влиять на другие.

38. Настрой мок так, чтобы база вернула ошибку (например, симуляция потери соединения). Проверь, что твоя функция корректно обрабатывает эту ошибку.

39. Протестируйте слой кэширования в Redis: проверьте, что при промахе данные загружаются из мока БД и сохраняются в Redis, а при попадании возвращаются из кэша.

40. Создайте `http.NewRequest` с нужным методом, URL и телом, передайте его в хендлер вместе с `Recorder`, и проверьте статус-код, заголовки и тело ответа.

41. Используйте `miniredis` (in-memory Redis) для unit-тестирования логики, зависящей от Redis, без реального сервера.

42. Используйте `httptest.NewServer(handler)` для создания реального тестового HTTP-сервера на случайном порту. Тестируйте через него ваш HTTP-клиент.

43. Используйте `httptest.NewTLSServer` для тестирования HTTPS-клиента с самоподписанным сертификатом.

44. Напишите table-driven HTTP-тесты, перебирая разные сценарии (успех, 400, 401, 404, 500).

45. Проверьте, что хендлер устанавливает правильные Cookie через `resp.Result().Cookies()`.

46. Используй `github.com/DATA-DOG/go-sqlmock`. Создай `db, mock, err := sqlmock.New()`. Настрой ожидание: `mock.ExpectQuery("SELECT (.+) FROM users").WithArgs(1).WillReturnRows(sqlmock.NewRows(...).AddRow(...))`. Вызови функцию, проверь `mock.ExpectationsWereMet()`.

47. Протестируйте middleware: оберните тестовый хендлер в middleware и проверьте, что заголовки/логи появляются корректно.

48. В интеграционном тесте из предыдущего задания создай таблицу, вставь запись, прочитай её и проверь результат.

49. Используйте `httptest.Server` для мокирования внешних API при тестировании вашего клиента.

50. Спроектируйте код так, чтобы все внешние зависимости (БД, API, файловая система) были скрыты за интерфейсами. Это фундамент тестируемости в Go.

51. Напиши тест с **sqlmock для transaction**: `mock.ExpectBegin()`, `mock.ExpectExec(...)`, `mock.ExpectCommit()`. Проверь, что функция начинает и коммитит транзакцию.

52. Напиши **интеграционный тест** для PostgreSQL: запусти PostgreSQL в Docker (`testcontainers-go` или ручной `docker run`). Подключись, создай таблицу, вставь данные, проверь SELECT. Очисти после теста (`DROP TABLE` или отдельная база).

53. Создайте in-memory реализацию интерфейса `UserRepository` для юнит-тестов сервиса, без реальной БД.

54. **Ручное мокирование интерфейсов**: Создайте интерфейс `SMSClient` с методом `Send(phone, msg string) error`. Напишите структуру `NotificationService`, которая использует этот клиент. Для тестов `NotificationService` напишите ручной мок `MockSMSClient`, который не отправляет реальные SMS, а просто сохраняет отправленные сообщения в срез внутри структуры мока для последующей проверки.

55. Используйте `gomock` (или `mockery`) для автоматической генерации моков из интерфейсов.

56. Напишите тест, который проверяет, что сервис вызывает репозиторий с правильными аргументами (behavior verification).

57. Напиши **интеграционный тест** с **test fixtures**: SQL-файл `testdata/setup.sql` создаёт таблицы и начальные данные. `testdata/teardown.sql` очищает. Выполняй перед/после теста.

58. Создайте mock HTTP-клиента, подменяя `http.RoundTripper`. Возвращайте разные ответы (успех, таймаут, 500) для разных тестов.

59. Напиши **интеграционный тест** с **transaction rollback**: начни транзакцию в `TestMain` или `SetupTest`, выполни тесты внутри транзакции, сделай `ROLLBACK` в `Cleanup`. Данные не сохраняются в БД.

60. Используйте `monkey patching` (пакет `bouk/monkey`) для замены функций во время теста (например, `time.Now`). Изучите, почему это считается антипаттерном.

61. Напиши **интеграционный тест** для **message queue** (Redis Stream или NATS): публикуй сообщение, подписывайся, проверь доставку. Используй таймаут для ожидания сообщения.

---

62. **Мокирование с помощью `testify/mock`**: Перепишите задачу с моком, используя встроенный пакет моков из библиотеки testify [349]. Настройте ожидания вызовов методов с помощью `.On("Send", "12345", "Привет").Return(nil)` [349] и проверьте их выполнение в конце теста с помощью метода `.AssertExpectations(t)`.

63. Реализуйте fake time через `clockwork` или собственный интерфейс `Clock` с методами `Now()` и `Sleep()`, чтобы тестировать таймауты без реального ожидания.

64. Напиши тест с **sqlmock для INSERT**: `mock.ExpectExec("INSERT INTO users").WithArgs("Alice", "alice@example.com").WillReturnResult(sqlmock.NewResult(1, 1))`. Проверь, что функция корректно обрабатывает `sql.Result`.

65. Протестируйте функцию, которая работает с файловой системой, используя in-memory FS (`fstest.MapFS` из `testing/fstest`).

66. Настройте `testcontainers-go` для поднятия Docker-контейнера с PostgreSQL перед интеграционными тестами.

67. **Интеграционное тестирование в Docker (`testcontainers`)**: Напишите интеграционный тест с использованием библиотеки `testcontainers-go` [353]. Настройте тест так, чтобы перед запуском проверок он автоматически скачивал и поднимал реальный Docker-контейнер с PostgreSQL, накатывал миграции, выполнял реальные SQL-запросы вашей программы и тушил контейнер после завершения тестов [353].

68. Примените миграции к testcontainer-БД через `golang-migrate` в `SetupSuite`.

69. **Тестирование HTTP-обработчиков (`httptest`)**: Напишите HTTP-обработчик `/ping`, который возвращает статус `200 OK` и строку `"pong"`. Напишите для него unit-тест без реального запуска веб-сервера. Используйте пакет `net/http/httptest` [350]: создайте виртуальный запрос с помощью `httptest.NewRequest` [350] и запишите ответ в `httptest.NewRecorder` [350]. Сделайте проверку статус-кода и тела ответа.

70. Напишите тест, который создает пользователя через API, затем читает его через репозиторий и проверяет консистентность.

71. Используй `testcontainers-go` для PostgreSQL: `postgresContainer, err := postgres.Run(ctx, "postgres:15-alpine")`. Получи `connString`, подключись, выполни миграции, тестируй. Останови контейнер в `t.Cleanup`.

72. Используйте Testcontainers для запуска Redis, Kafka, MinIO (S3) и тестируйте взаимодействие с ними.

73. Настройте переиспользование контейнеров между тестами (reuse) для ускорения CI-пайплайна.

74. Создайте хелпер-функцию, которая поднимает контейнер, возвращает `*sql.DB` и автоматически очищает всё через `t.Cleanup`.

75. **Тестирование HTTP-клиента с заглушкой сервера**: Напишите функцию `GetWeather(city string) (float64, error)`, которая делает GET-запрос к внешнему платному API погоды. Чтобы протестировать эту функцию без реальных сетевых запросов к платному API, запустите локальный тестовый HTTP-сервер в кодовой базе теста с помощью `httptest.NewServer` [351]. Настройте этот сервер так, чтобы он возвращал фиксированный JSON-ответ с температурой, и передайте его локальный URL в тестируемый HTTP-клиент [351].

76. **Симуляция ошибок (Mocking errors)**: В ручном моке из упр. 429 добавь поле `ReturnError error`. Настрой мок так, чтобы он возвращал ошибку (симуляция отвала почтового сервера), и протестируй, как твой `UserService` корректно обрабатывает это падение.

77. Протестируйте сценарий отказа БД: остановите контейнер во время выполнения запроса и проверьте, что приложение корректно обрабатывает ошибку.

78. **Mockgen / Gomock (Генерация)**: Установи `go.uber.org/mock/mockgen`. Сгенерируй мок для интерфейса `EmailSender` из консоли.

79. **Мокирование SQL-запросов к БД (`sqlmock`)**: Установите библиотеку `github.com/DATA-DOG/go-sqlmock`. Напишите тест для функции, которая сохраняет пользователя в PostgreSQL с помощью `db.Exec`. Используйте `sqlmock` для имитации успешного подключения к БД, настройки ожидаемого INSERT-запроса с конкретными аргументами и проверки корректности выполнения логики вашей программы без реального запуска PostgreSQL [352].

80. **Использование Gomock**: В тесте инициализируй сгенерированный мок: `ctrl := gomock.NewController(t)`. Настрой ожидание: `mockSender.EXPECT().Send("test@ya.ru", gomock.Any()).Return(nil).Times(1)`. Если сервис не вызовет метод (или вызовет не с теми аргументами) — тест автоматически упадет.

81. Напиши **"golden file" тест**: функция генерирует сложный вывод (например, SQL-дамп или JSON). Сохрани ожидаемый вывод в `testdata/expected.json`. Тест сравнивает `assert.Equal(t, expected, actual)`. При изменении — обнови golden file через флаг `-update`.

---

82. **Заморозка времени (Time Mocking)**: Твоя функция использует `time.Now()`. Создай интерфейс `Clock` с методом `Now() time.Time`. В проде передавай реализацию с реальным `time.Now`, а в тестах — мок, который всегда возвращает жестко заданную дату. (Так тесты не будут падать по ночам или в високосные годы).

83. **Тестирование переменных окружения**: Напишите функцию, которая считывает порт сервера из переменной окружения `PORT`. Напишите тест для этой функции, который временно устанавливает переменную окружения с помощью встроенного метода `t.Setenv("PORT", "9090")` [354] и проверяет корректность парсинга значения. Убедитесь, что после завершения теста значение переменной окружения автоматически сбрасывается в исходное состояние.

84. Напиши полный **"hexagonal architecture" тест**:
- Domain: `UserService` с интерфейсом `UserRepository`
- Реализация: `PostgresUserRepository` 
- Тест: `InMemoryUserRepository` (stub) для unit-тестов бизнес-логики
- Интеграционный тест: `PostgresUserRepository` с testcontainers

85. **Мокирование внешнего API (`httptest.Server`)**: Твой код делает запросы к стороннему API (например, к Telegram). В тесте создай `ts := httptest.NewServer(http.HandlerFunc(func(w, r) { w.Write([]byte(`{"status": "ok"}`)) }))`. Переопредели URL в твоем клиенте на `ts.URL`. Теперь код делает реальные HTTP-запросы, но обращается к твоему локальному тестовому серверу.

---

86. Напиши **"contract test"**: интерфейс `Cache` с методами `Get`, `Set`, `Delete`. Напиши тест, параметризованный реализацией (`InMemoryCache`, `RedisCache`). Один и тот же тест проходит для обеих реализаций. Покажи, что контракт соблюдается.

87. **Проверка утечек горутин (`goleak`)**: Установите библиотеку `go.uber.org/goleak`. Напишите функцию, которая запускает горутину и по ошибке оставляет её висеть в заблокированном состоянии навсегда (утечка) [355]. Напишите unit-тест для этой функции, вызвав проверку `defer goleak.VerifyNone(t)` [355], и убедитесь, что библиотека ловит утечку горутины и валит тест.

88. Напиши **"load test"** через `testing.B` + горутины: эмулируй 100 конкурентных пользователей, каждый делает 1000 запросов к `UserService`. Измерь latency (p50, p95, p99) через `github.com/montanaflynn/stats` или ручной расчёт.

89. **Golden Files**: У тебя есть функция, генерирующая сложный HTML-отчет (или PDF). Запиши ожидаемый идеальный результат в файл `testdata/report.golden`. В тесте считывай этот файл и сравнивай с результатом функции. Добавь флаг `go test -update`, при передаче которого тест сам перезапишет `.golden` файл новыми данными (удобно при намеренных изменениях формата).

90. Напиши **"chaos test"** (упрощённый): функция `UnreliableDB` с вероятностью ошибки 30%. Напиши тест для retry-логики: при 3 попытках с backoff вероятность общего failure = 0.3³ = 2.7%. Запусти 1000 раз, проверь статистику.

91. Напиши **"approval test"** (ручной snapshot): функция генерирует отчёт. Тест сохраняет в `testdata/approval/received.txt`. Если `approved.txt` не существует — тест падает с сообщением "approve by copying received to approved". Разработчик вручную проверяет и копирует.

92. **Testcontainers (База)**: Установи `github.com/testcontainers/testcontainers-go`. Напиши тест, который программно скачивает и запускает Docker-контейнер с PostgreSQL перед выполнением теста и убивает его в `t.Cleanup`. (Убедись, что Docker запущен локально).

93. **Тестирование файловой системы через виртуальный FS**: Напишите парсер файлов конфигурации. Используйте библиотеку `github.com/spf13/afero` для работы с файловой системой [356]. Напишите тест, заменив реальную файловую систему ОС на виртуальную файловую систему в оперативной памяти (`afero.NewMemMapFs()`) [356], чтобы избежать создания реальных файлов на жестком диске во время прохождения тестов.

94. Напиши **"mutation testing"** (упрощённый): вручную "мутируй" код (например, замени `>` на `>=`, `&&` на `||`). Проверь, что существующие тесты **ловят** мутацию (падают). Если не ловят — тесты недостаточны.

95. Напиши **"table-driven test with generics"**: `func TestGenericMax[T constraints.Ordered](t *testing.T) { ... }`. Не работает напрямую — обойди через `TestMaxInt`, `TestMaxFloat64`, `TestMaxString` с общей логикой в helper'е.

96. Напиши **"flaky test detector"**: тест, который иногда падает из-за race condition. Запусти `go test -count=100`. Покажи, что `-count` помогает обнаружить нестабильные тесты.

97. **Миграции в интеграционных тестах**: Подняв контейнер с Postgres (упр. 437), получи случайный порт контейнера, собери строку подключения `postgres://...`, подключись к БД и накати миграции из папки (используя пакет миграций).

98. Напиши **"test for idempotency"**: функция `CreateUser` должна быть идемпотентной при повторном вызове с теми же данными. Проверь, что второй вызов не создаёт дубликат (или возвращает тот же ID).

99. **Черный ящик против Белого ящика (Black-box vs White-box)**: Объясните разницу между тестированием приватных методов и тестированием только публичного интерфейса.
    * Напишите тест "белого ящика" в файле `service_test.go` с пакетом `package main` (чтобы иметь доступ к неэкспортируемым полям структуры).
    * Напишите тест "черного ящика" в файле `service_blackbox_test.go` с пакетом `package main_test` [357]. Импортируйте ваш пакет и протестируйте только публичные функции.

100. Напиши **"test for graceful degradation"**: функция зависит от внешнего API. Если API недоступен — функция должна вернуть stale-данные из кеша, а не ошибку. Замокай API на ошибку, проверь fallback.

101. Напиши **"test for resource cleanup"**: функция открывает файл, создаёт временную директорию, запускает горутину. Напиши тест, проверяющий, что после функции (даже при ошибке) все ресурсы освобождены. Используй `/proc/self/fd` или `runtime.NumGoroutine()`.

102. **Полноценный тест БД**: Используя поднятую БД из упр. 438, инициализируй свой реальный `UserRepository` (из прошлых блоков). Вызови `Create(user)`, затем `GetByID(user.ID)`. Убедись, что данные физически записались в реальную базу и корректно прочитались обратно.

103. Напиши **"test for deterministic concurrency"**: функция с `sync.Mutex` должна давать одинаковый результат при любом порядке планирования. Запусти 10000 раз с `-count=10000`, проверь детерминизм.

104. Напиши **"test for backward compatibility"**: сохрани старый JSON-ответ в `testdata/v1_response.json`. Новая версия API должна парсить его без ошибок (дополнительные поля — `omitempty`). Проверь совместимость.

105. **Testcontainers Redis**: Аналогично подними контейнер с Redis. Протестируй свой паттерн Cache-Aside (из блока 29), убедившись, что данные берутся из PostgreSQL, помещаются в реальный Redis (проверь ключ напрямую через Redis-клиент в тесте), и при повторном запросе достаются из кэша.

---
**Совет к этому этапу:** Отличай юнит-тесты от интеграционных. Юнит-тесты должны выполняться за миллисекунды — для этого мы изолируем БД и сеть с помощью интерфейсов и моков (Блок 32). А вот интеграционные тесты (Блок 33) гарантируют, что твои SQL-запросы реально работают — но их запуск может занимать 10-30 секунд из-за поднятия контейнеров. Хорошая практика: разделять их (например, скрывать интеграционные за build tags `//go:build integration`).

106. **Тестирование CLI-приложений**: Напишите функцию для CLI-утилиты: `RunCLI(args []string, stdout io.Writer, stderr io.Writer) int` [358]. Напишите тест, который передает в качестве аргументов неверные флаги, а в качестве `stdout`/`stderr` передает буферы `bytes.Buffer` [358]. Проверьте правильность вывода сообщений об ошибках в буферы и возвращаемый утилитой код ошибки [358].

107. Напиши **"test suite"** через `testify/suite`: `type UserServiceTestSuite struct { suite.Suite; repo UserRepository }`. Методы `SetupTest`, `TearDownTest`. Тесты как методы на suite. Покажи организацию сложных тестов.

---
