# Protocol Buffers и gRPC

В этом файле собраны уникальные упражнения по теме **"Protocol Buffers и gRPC"**, отсортированные по возрастанию сложности.

---

1. **Описание первой схемы Protobuf**: Создайте файл `user.proto`. Напишите схему protobuf (версии `syntax = "proto3"`). Объявите сообщение `UserRequest` с полем `user_id` (тип `string`, тег `1`) и сообщение `UserResponse` с полями `id`, `name`, `email` и списком ролей `repeated string roles`. Опишите gRPC-сервис `UserService` с методом `GetUser`, который принимает `UserRequest` и возвращает `UserResponse`.

2. **Настройка окружения**: Установи компилятор `protoc` (с официального сайта) и плагины для Go: `go install google.golang.org/protobuf/cmd/protoc-gen-go@latest` и `go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest`.

3. Добави `enum Status { STATUS_UNSPECIFIED = 0; STATUS_ACTIVE = 1; STATUS_INACTIVE = 2; }` в `user.proto`. Сгенерируй код. Покажи, что в Go это тип `Status` с константами `Status_STATUS_UNSPECIFIED` и т.д. Объясни, почему `0` — `UNSPECIFIED` (best practice).

4. Сгенерируйте Go-код из `.proto` файла командой `protoc --go_out=. --go-grpc_out=. hello.proto`.

5. Создай файл `user.proto`. Опиши сообщение `User` с полями `id` (int32), `name` (string), `age` (int32).

6. Добавьте в сообщение вложенные поля, `enum` и поле с `repeated`. Сгенерируйте Go-код.

7. Реализуйте серверную часть: создайте структуру, которая встраивает `UnimplementedGreeterServer` и переопределяет метод `SayHello`.

8. Добави **nested message**: `message User { message Address { string city = 1; string street = 2; } Address address = 5; }`. Сгенерируй код. Покажи доступ через `user.Address.City`.

9. Установи protoc и Go-плагины: `protoc`, `protoc-gen-go`, `protoc-gen-go-grpc`. Создай файл `user.proto` с `syntax = "proto3"; package user;`. Определи сообщение `User { int64 id = 1; string name = 2; string email = 3; }`. Сгенерируй Go-код: `protoc --go_out=. --go_opt=paths=source_relative user.proto`.

10. Запустите gRPC-сервер на порту 50051 с помощью `grpc.NewServer()` и `RegisterGreeterServer`.

11. **Генерация Go-кода (protoc и buf)**: Установите компилятор `protoc` и плагины `protoc-gen-go` и `protoc-gen-go-grpc`. Сгенерируйте Go-код на основе вашего `user.proto` с помощью терминала. Дополнительно изучите современный инструмент сборки `buf` (создайте файлы конфигурации `buf.yaml` и `buf.gen.yaml`) и выполните генерацию через команду `buf generate`.

12. Добави **oneof**: `oneof contact { string phone = 8; string telegram = 9; }`. Сгенерируй код. Покажи `GetContact()` — возвращает `interface{}`. Покажи type switch для определения, что выбрано.

13. **Marshal vs JSON**: В Go-коде создай экземпляр сгенерированной структуры `User`. Сериализуй её в байты через `proto.Marshal()`. Для сравнения сериализуй те же данные в JSON. Выведи размеры обоих срезов байт в консоль и оцени разницу.

14. Используйте `google.protobuf.Timestamp` и `google.protobuf.Duration` в своих сообщениях. Научитесь преобразовывать их в `time.Time` и `time.Duration`.

15. Скомпилируй `.proto` файл в Go-код. Изучи сгенерированные файлы `user.pb.go` (структуры) и `user_grpc.pb.go` (интерфейсы клиента и сервера).

16. Напишите gRPC-клиент, который подключается к серверу через `grpc.Dial` (используйте `grpc.WithInsecureCredentials()` для начала).

17. Добави **map field**: `map<string, string> metadata = 10;`. Сгенерируй код. Покажи `map[string]string` в Go. Добави, удали, итерируй.

18. Добави **optional field** (proto3): `optional string nickname = 11;`. Сгенерируй код (требует `protoc` 3.15+). Покажи `HasNickname()` и `GetNickname()` — разница между отсутствием и пустой строкой.

19. Создайте `.proto` с полем `repeated string tags` (аналог слайса). Сгенерируйте код и посмотрите, как он мапится на Go.

20. **Сложные типы**: Расширь `user.proto`. Добавь перечисление (`enum Role { ADMIN = 0; USER = 1; }`), массив строк (`repeated string tags = 4;`) и мапу (`map<string, string> attributes = 5;`). Перегенерируй код и заполни эти поля в Go.

21. Определите сервис с потоковыми RPC: client streaming, server streaming, bidirectional streaming. Сгенерируйте код и изучите интерфейсы.

22. Добавь в `.proto` файл enum `Role` (UNKNOWN, ADMIN, USER). Включи его в сообщение `User`. Перекомпилируй.

23. Создай **JSON marshaling/unmarshaling** для protobuf: `protojson.Marshal(user)` и `protojson.Unmarshal(data, &user)`. Покажи, что имена полей в JSON — camelCase по умолчанию. Настрой через `json_name` опцию.

24. Используйте `oneof` в `.proto` для создания union-типов (аналог `interface{}` с конкретными вариантами). Сгенерируйте код и изучите, как работать с ним в Go.

25. **Простой Unary RPC (Запрос-Ответ)**: * Создайте gRPC-сервер. Реализуйте интерфейс `UserServiceServer`, сгенерированный в предыдущем шаге. Напишите метод `GetUser`, который ищет пользователя в локальной мапе и возвращает данные. Запустите gRPC-сервер на TCP-порту `50051`.
    * Создайте gRPC-клиент. Подключитесь к серверу с помощью `grpc.Dial` (или `grpc.NewClient` в современных версиях Go-пакета gRPC), вызовите метод `GetUser` и выведите полученный результат в консоль.

26. Добави **custom options** в `.proto`: `import "google/protobuf/descriptor.proto";`. Создай `extend google.protobuf.MessageOptions { string table_name = 50001; }`. Используй в `User` как `option (table_name) = "users";`. Прочитай в Go через `proto.GetExtension`.

27. Создайте вложенные сообщения (nested messages) и `enum` типы в `.proto`.

28. Создай **proto package с версионированием**: `package user.v1;` и `package user.v2;`. Покажи, как импортировать обе версии в одном Go-проекте (разные import paths). Обсуди стратегию обновления API.

---

29. **[Каверзный кейс]**: В Protobuf поля могут быть помечены как `optional`. Сделай поле `age` опциональным. Сгенерируй код и посмотри, как в Go это превращается в указатель (`*int32`).

30. Реализуйте сервер `Greeter`: метод `SayHello` возвращает приветствие с переданным именем. Запустите сервер на порту 50051.

31. Используйте `google.protobuf.Timestamp` и `google.protobuf.Empty` из `google/protobuf/` пакета для стандартных типов.

32. Добави в `user.proto` поле `google.protobuf.Timestamp created_at = 4;` (`import "google/protobuf/timestamp.proto";`). Сгенерируй код. Покажи, как работать с `timestamppb.Timestamp` в Go (конвертация `time.Time` ↔ `*timestamppb.Timestamp`).

33. Изучите `well-known types`: `Struct`, `Value`, `ListValue`, `Any`, `Duration`, `FieldMask`.

34. Реализуй **gRPC сервер**: `type server struct { pb.UnimplementedUserServiceServer }`. Реализуй `GetUser(ctx context.Context, req *pb.GetUserRequest) (*pb.User, error)`. Зарегистрируй: `pb.RegisterUserServiceServer(grpcServer, &server{})`. Слушай на `:50051`.

35. Создайте `.proto` с `map<string, User> users` и посмотрите, как это превращается в Go-мапу.

36. **Полиморфизм (`oneof`)**: Опиши в Protobuf поле `oneof contact { string email = 7; string phone = 8; }`. Сгенерируй код и посмотри, как элегантно Go использует интерфейсы-обертки для реализации `oneof` (в структуре может быть только одно из этих полей).

---

37. Добавь сообщение `GetUserRequest` с полем `id`. Опиши сервис `UserService` с методом `GetUser`, который принимает запрос и возвращает `User`. Скомпилируй.

38. Напишите консольный клиент, который вызывает `SayHello` и выводит ответ. Добавьте таймаут через `context.WithTimeout`.

39. Используйте `import` в `.proto` для переиспользования общих сообщений из другого файла.

40. **Потоковое вещание сервера (Server Streaming RPC)**: Добавьте в схему `.proto` метод `GetUsersList(Empty) returns (stream UserResponse)`. На сервере реализуйте отправку нескольких пользователей клиенту по частям с задержкой в 500 миллисекунд (используя метод `stream.Send`). На клиенте прочитайте поток в цикле с помощью `stream.Recv` до тех пор, пока сервер не завершит отправку.

41. Настройте `buf` (buf.build) — современный инструмент для работы с protobuf (линтинг, генерация, версионирование). Создайте `buf.yaml` и `buf.gen.yaml`.

42. **Определение сервиса**: В `.proto` файле добавь `service UserService { rpc GetUser (UserRequest) returns (UserResponse); }`. Сгенерируй код с поддержкой gRPC (`--go-grpc_out=.`). Посмотри на сгенерированные интерфейсы сервера и клиента.

43. Добавьте серверу метод `GetUser(id int32) returns User`. Реализуйте его на сервере, возвращая тестовые данные. Клиент вызывает его и выводит поля.

44. Используйте `buf lint` для проверки `.proto` файлов на соответствие best practices.

45. Добави **deadline/timeout**: в клиенте `ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)`. В сервере проверяй `ctx.Err() == context.DeadlineExceeded` и верни `codes.DeadlineExceeded`.

46. **Server Streaming**: Создайте метод `StreamNumbers(req *Request, stream ServerStream_ServerNumbers)`, который отправляет клиенту последовательность чисел. Клиент читает через `for { resp, err := stream.Recv() }`.

47. Реализуй **gRPC клиент**: `conn, err := grpc.Dial("localhost:50051", grpc.WithTransportCredentials(insecure.NewCredentials()))`. Создай `client := pb.NewUserServiceClient(conn)`. Вызови `client.GetUser(ctx, &pb.GetUserRequest{Id: 1})`.

48. Добави **interceptor (unary)**: `func loggingInterceptor(ctx context.Context, req any, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (any, error)`. Логируй метод, длительность, ошибку. Зарегистрируй через `grpc.UnaryInterceptor(loggingInterceptor)`.

49. **Server Streaming**: Добавь в контракт метод `GetUsersByRole`, который принимает `Role` и возвращает `stream User`. Реализуй на сервере отправку нескольких пользователей через `stream.Send()`.

50. **gRPC Сервер**: Создай структуру, реализующую сгенерированный интерфейс `UserServiceServer`. В `main` открой TCP-порт (`net.Listen`), создай сервер `grpc.NewServer()`, зарегистрируй свой сервис и запусти `Serve()`.

51. Server streaming: реализуйте метод `ListUsers(stream UserService_ListUsersServer)`, который отправляет нескольких пользователей по одному. Клиент читает поток до EOF.

52. **Потоковое вещание клиента (Client Streaming RPC)**: Добавьте в схему `.proto` метод `UploadProfilePictures(stream Chunk) returns (UploadStatus)`. Метод должен позволять клиенту отправлять файл изображения по частям (чанками байт). Напишите код клиента, считывающего локальный файл кусками и отправляющего их на сервер. На сервере соберите файл воедино и верните общий статус загрузки.

53. Добави **auth interceptor**: проверяй `authorization` metadata. Извлеки Bearer token, валидируй (заглушка). Если невалиден — верни `codes.Unauthenticated`. Примени `grpc.UnaryInterceptor(authInterceptor)` к серверу.

54. **Bidirectional Streaming**: Реализуйте чат `Chat(stream ChatStream_ChatServer)`, где клиент и сервер одновременно отправляют сообщения друг другу.

55. Добави **chain of interceptors**: `grpc.ChainUnaryInterceptor(loggingInterceptor, authInterceptor, recoveryInterceptor)`. Покажи порядок выполнения (внешний → внутренний → handler → обратно).

56. Используйте `context.Context` в streaming-методах для корректной отмены потока при разрыве соединения.

57. Добави **client interceptor**: `grpc.WithUnaryInterceptor(clientMetricsInterceptor)`. Считай количество запросов, latency. Отправь в Prometheus или логируй.

58. Client streaming: метод `CreateUsers(stream UserService_CreateUsersServer)` — клиент отправляет несколько пользователей, сервер собирает их и возвращает количество созданных.

59. Напиши клиент для Server Streaming: вызови метод и читай поток в цикле `for { user, err := stream.Recv(); if err == io.EOF { break } ... }`.

60. Реализуйте streaming-метод, который отправляет данные по таймеру (например, тикеры времени каждую секунду). Клиент должен отписаться через отмену контекста.

61. Реализуй **health check**: импортируй `grpc.health.v1`. Реализуй `HealthServer`. Клиент проверяет `healthClient.Check(ctx, &healthpb.HealthCheckRequest{Service: "user.UserService"})`. Верни `SERVING` или `NOT_SERVING`.

---

62. Изучите проблему "backpressure" в streaming: что будет, если сервер генерирует данные быстрее, чем клиент успевает их читать? (TCP-буферы переполнятся, соединение разорвется).

63. Реализуйте метод, который принимает поток файлов (chunk'ами) от клиента, сохраняет их на диск и возвращает метаданные.

64. **Двунаправленный стриминг (Bidirectional Streaming RPC)**: Опишите и реализуйте метод `Chat(stream ChatMessage) returns (stream ChatMessage)`. Напишите полноценный real-time чат, в котором несколько клиентов могут одновременно отправлять сообщения на сервер и мгновенно получать сообщения, отправленные другими пользователями в этот же стрим.

---

65. **Дедлайны (Context)**: На стороне сервера в методе `GetUser` добавь `time.Sleep(2 * time.Second)`. На стороне клиента вызови метод, передав контекст с таймаутом в 1 секунду (`context.WithTimeout`). Убедись, что клиент отваливается с ошибкой `DeadlineExceeded`.

66. Bidirectional streaming: чат-сервис `Chat(stream ChatService_ChatServer)`, где клиенты и сервер обмениваются сообщениями в реальном времени. Реализуйте эхо-сервер и клиента, отправляющего несколько сообщений.

67. **Client Streaming**: Добавь метод `UploadUsers`, принимающий `stream User` и возвращающий ответ с количеством загруженных. Реализуй на сервере чтение потока через `stream.Recv()`.

68. Определи **client streaming RPC**: `rpc CreateUsers(stream User) returns (CreateUsersResponse);`. Реализуй на сервере: `func (s *server) CreateUsers(stream pb.UserService_CreateUsersServer) error`. Читай `for { user, err := stream.Recv(); ... }`. Считай количество, верни итог.

69. Добави **metadata** (headers): в клиенте `md := metadata.Pairs("x-request-id", "123-abc")`, `ctx := metadata.NewOutgoingContext(ctx, md)`. В сервере читай `md, ok := metadata.FromIncomingContext(ctx)`. Верни metadata в ответе через `grpc.SetHeader`/`SetTrailer`.

70. Создайте streaming-метод, который читает из канала Go и отправляет данные клиенту. Это паттерн "bridge channel to gRPC stream".

71. Изучите разницу между `google.golang.org/grpc` и `google.golang.org/grpc/credentials/insecure`. Почему `insecure` — это только для dev-среды?

72. Используйте `grpc.MaxRecvMsgSize` и `grpc.MaxSendMsgSize` для настройки лимитов размера сообщений (по умолчанию 4MB).

73. Реализуй **client для client streaming**: `stream, err := client.CreateUsers(ctx)`. Отправляй `stream.Send(user)` несколько раз. Закрой `resp, err := stream.CloseAndRecv()`.

74. Верните из серверного метода `codes.NotFound` с детальным сообщением, если пользователь не найден. Клиент проверяет статус-код с помощью `status.FromError`.

75. Напиши клиент для Client Streaming: отправь 5 пользователей через `stream.Send()` в цикле, затем вызови `stream.CloseAndRecv()` для получения ответа.

76. Создайте **Unary Server Interceptor**, который логирует все входящие RPC-вызовы: метод, время начала, длительность, код ответа.

77. Определи **bidirectional streaming**: `rpc Chat(stream ChatMessage) returns (stream ChatMessage);`. Реализуй на сервере: читай `Recv()` в одной горутине, отправляй `Send()` в другой. Обе горутины завершаются при `io.EOF` или ошибке.

78. **Обработка ошибок (gRPC Codes)**: Измени логику сервера: если юзер не найден, возвращай ошибку. Используй пакет `status` из gRPC: `return nil, status.Errorf(codes.NotFound, "юзер не найден")`. На клиенте проверь ошибку через `s, ok := status.FromError(err); if ok && s.Code() == codes.NotFound { ... }`.

79. Добави **rich error** через `google.golang.org/genproto/googleapis/rpc/errdetails`: `st := status.New(codes.InvalidArgument, "invalid email")`, `ds, _ := st.WithDetails(&errdetails.BadRequest{...})`. Верни `ds.Err()`. В клиенте извлеки детали через `status.FromError` + `details := s.Details()`.

80. Реализуй **bidirectional streaming клиент**: аналогично серверу — `Send()` и `Recv()` в разных горутинах. Синхронизируй завершение через `sync.WaitGroup` или `errgroup`.

81. Создайте **Stream Server Interceptor** для логирования streaming-вызовов.

82. **Серверный интерцептор (Unary Server Interceptor)**: Напишите Middleware для вашего gRPC-сервера с помощью `grpc.UnaryServerInterceptor`. Интерцептор должен перехватывать каждый входящий запрос, замерять время его выполнения, логировать название вызываемого метода и выводить информацию в консоль.

83. Добави **flow control** в streaming: сервер отправляет 10000 сообщений. Клиент медленно читает. Покажи, что gRPC **backpressure** автоматически замедляет сервер (через HTTP/2 flow control). Замерь память сервера — она не растёт бесконечно.

84. **Rich Errors (Детальные ошибки)**: gRPC позволяет прикреплять к ошибкам целые структуры. Используй `status.New(codes.InvalidArgument, "ошибка валидации")`. Прикрепи к нему детали (например, `errdetails.BadRequest`) через метод `WithDetails()`. На клиенте извлеки эти детали и выведи, какое конкретно поле не прошло валидацию.

85. **Bidirectional Streaming**: Добавь метод `Chat`, принимающий и возвращающий `stream Message`. Реализуй echo-сервер: горутина читает из `stream.Recv()` и тут же отправляет обратно через `stream.Send()`.

86. Используйте богатые детали ошибки (`google.rpc.Status`): при ошибке валидации прикрепите к статусу `BadRequest` с информацией о поле, вызвавшем ошибку.

87. Добави **cancellation в streaming**: клиент отменяет `ctx` посреди стрима. Покажи, что сервер получает `ctx.Done()` и корректно завершает отправку. Проверь через логи.

88. Создайте interceptor для rate limiting, используя `golang.org/x/time/rate`. Возвращайте `codes.ResourceExhausted` при превышении лимита.

89. Установите `protoc` и плагины `protoc-gen-go` и `protoc-gen-go-grpc`. Создайте простой `.proto`-файл с сервисом `Greeter` и методом `SayHello`.

90. Реализуй **"heartbeat" в bidirectional stream**: клиент и сервер отправляют пинг каждые 5 секунд. Если пинг не получен за 15 секунд — закройте соединение. Используй `time.Ticker` + `select` с `ctx.Done()`.

---

91. Реализуйте interceptor для recovery (аналог middleware Recovery в HTTP): перехватывайте `panic` в хендлерах и возвращайте `codes.Internal`.

92. Настрой **TLS** для gRPC: сгенерируй сертификаты (self-signed или через mkcert). Сервер: `credentials.NewServerTLSFromFile("server.crt", "server.key")`. Клиент: `credentials.NewClientTLSFromFile("ca.crt", "")`. Подключись по TLS.

93. **[Каверзный кейс]**: В Bidirectional Streaming клиент должен отправлять и получать сообщения одновременно. Напиши клиент, который в одной горутине читает входящие сообщения, а в другой — отправляет (например, читая текст из консоли `bufio.Scanner`).

94. Обработайте `status.Code` на клиенте и реализуйте повторные попытки при `Unavailable` (с exponential backoff).

95. Настрой **mTLS** для gRPC: клиент предоставляет сертификат. Сервер проверяет `ClientAuth: tls.RequireAndVerifyClientCert`. Клиент загружает `client.crt` + `client.key` в `tls.Config.Certificates`.

96. **Передача метаданных (gRPC Metadata)**: gRPC не использует традиционные HTTP-заголовки напрямую, вместо этого применяются Metadata.
    * На клиенте создайте контекст с метаданными `metadata.AppendToOutgoingContext` и положите туда токен авторизации `"authorization": "Bearer secret_token"`.
    * На сервере внутри интерцептора извлеките метаданные из входящего контекста с помощью `metadata.FromIncomingContext` и проверьте токен. Если токена нет — верните ошибку.

97. Напишите interceptor для автоматического retry при определенных ошибках (например, `codes.Unavailable`).

98. Настрой **load balancing** в клиенте: `grpc.WithDefaultServiceConfig(`{"loadBalancingPolicy":"round_robin"}`)`. Используй **DNS resolver** с несколькими A-записями. Покажи распределение запросов.

99. Используйте `grpc.ChainUnaryInterceptor` для применения нескольких interceptor'ов в цепочке.

100. **Server Streaming**: Опиши в `.proto`: `rpc DownloadLogs (Request) returns (stream LogChunk);`. Сервер должен в цикле `for` отправлять сообщения клиенту через `stream.Send()`. Клиент должен читать их через `stream.Recv()` до получения ошибки `io.EOF`.

101. Unary interceptor для логирования каждого запроса: метод, длительность, статус. Подключите его к серверу.

102. Настрой **service mesh** (упрощённо): клиент коннектится к `localhost:50051` (sidecar proxy, например Envoy). Proxy маршрутизирует на реальные сервисы. Покажи, что клиент не знает о реальных адресах.

103. Создайте interceptor для метрик: инкрементируйте счетчики Prometheus для каждого RPC-метода и статуса.

104. Реализуй **reflection**: импортируй `google.golang.org/grpc/reflection`. Зарегистрируй `reflection.Register(grpcServer)`. Используй `grpcurl` или `grpcui` для интроспекции API без `.proto` файлов.

105. Реализуйте interceptor для аутентификации: проверяйте заголовок `authorization` в метаданных и возвращайте `codes.Unauthenticated`, если токен невалиден.

106. Создайте **Unary Client Interceptor**, который добавляет request ID в метаданные каждого исходящего запроса.

107. Реализуй **graceful shutdown**: `grpcServer.GracefulStop()` — перестаёт принимать новые соединения, дожидается завершения активных RPC. Используй с `signal.NotifyContext`. Сравни с `grpcServer.Stop()` (принудительно).

108. Реализуйте interceptor для distributed tracing: извлекайте trace ID из метаданных и пробрасывайте его в `context`.

109. **Client Streaming**: Опиши: `rpc UploadFile (stream FileChunk) returns (UploadStatus);`. Напиши клиента, который читает большой файл с диска по 64КБ и отправляет куски серверу. Сервер собирает файл и в конце возвращает финальный статус через `stream.SendAndClose()`.

110. Stream interceptor для трекинга потоковых сообщений (логирование начала и конца потока).

111. Настрой **keepalive**: сервер `grpc.KeepaliveParams(keepalive.ServerParameters{MaxConnectionIdle: 15 * time.Minute})`. Клиент `grpc.WithKeepaliveParams(keepalive.ClientParameters{Time: 10 * time.Second, Timeout: 3 * time.Second})`. Покажи поддержание соединения.

112. Изучите `grpc.StatsHandler` для сбора детальной статистики по соединениям и RPC (альтернатива interceptor'ам для низкоуровневой телеметрии).

113. **Специфическая обработка ошибок в gRPC**: В gRPC ошибки возвращаются в виде статус-кодов. Напишите код: если запрашиваемый пользователь не найден, сервер должен вернуть ошибку со статус-кодом `codes.NotFound` с помощью пакета `google.golang.org/grpc/status`. На клиенте проверьте ошибку: извлеките статус-код с помощью `status.FromError` и выведите понятное текстовое сообщение.

114. Настрой **max message size**: `grpc.MaxRecvMsgSize(10 * 1024 * 1024)` (10MB). Отправь сообщение больше дефолта (4MB). Покажи ошибку `ResourceExhausted` без настройки.

115. Отправьте метаданные (аналог HTTP-заголовков) из клиента: `metadata.AppendToOutgoingContext(ctx, "authorization", "Bearer token")`.

116. Настрой **compression**: `grpc.UseCompressor(gzip.Name)` на клиенте. Покажи снижение трафика. Проверь, что сервер поддерживает тот же compressor (автоматически, если зарегистрирован).

117. Прочитайте метаданные на сервере: `md, ok := metadata.FromIncomingContext(ctx)`.

118. **Bidirectional (Двунаправленный) Streaming**: Опиши: `rpc Chat (stream ChatMessage) returns (stream ChatMessage);`. Запусти на клиенте горутину для чтения сообщений (`stream.Recv()`), а в основной горутине читай ввод с клавиатуры и отправляй серверу (`stream.Send()`). Сервер должен работать как эхо-чат.

119. Реализуй **retry policy** в клиенте: `grpc.WithDefaultServiceConfig(`{"methodConfig":[{"name":[{"service":"user.UserService"}],"retryPolicy":{"maxAttempts":3,"initialBackoff":"0.1s","maxBackoff":"1s","backoffMultiplier":2,"retryableStatusCodes":["UNAVAILABLE"]}}]}`)`. Покажи автоматический retry.

120. Реализуйте **JWT-аутентификацию**: клиент отправляет JWT токен в метаданных, сервер валидирует его (используйте `github.com/golang-jwt/jwt/v5`) и извлекает `user_id`.

121. Реализуй **circuit breaker** в клиенте (ручной или через interceptor): при 5 ошибок подряд — возвращай `codes.Unavailable` быстро, не делая реальный запрос. Через 30s — пробный запрос. Используй `sync/atomic` для состояния.

---

122. Создайте interceptor, который проверяет роль пользователя (`admin`, `user`) из JWT и разрешает/запрещает доступ к определенным RPC-методам.

123. **Разрыв соединения (Stream Context)**: В серверном стриме (упр. 454) добавь бесконечный цикл отправки данных раз в секунду. Жестко прерви работу клиента (Ctrl+C). Убедись, что сервер корректно останавливает работу, проверяя `stream.Context().Done()`.

124. На стороне клиента извлеки детали из ошибки: `st, _ := status.FromError(err); details := st.Details()`. Приведи `details[0]` к типу `*errdetails.BadRequest` и выведи информацию о нарушенных полях.

125. **Плавное завершение gRPC-сервера (Graceful Shutdown)**: Напишите код остановки gRPC-сервера. При получении сигнала завершения процесса от ОС (Ctrl+C), вызовите метод `server.GracefulStop()`. Напишите комментарий, как этот метод позволяет серверу завершить текущие активные стримы и запросы, не обрывая соединения грубо, и сравните его с `server.Stop()`.

126. Используйте `grpc.WithTransportCredentials(credentials.NewTLS(tlsConfig))` для настройки TLS на клиенте.

127. **Ограничение размера сообщения**: По умолчанию gRPC запрещает принимать сообщения больше 4 МБ. Попробуй передать кусок файла на 5 МБ — получишь ошибку. Настрой клиента и сервер, передав опцию `grpc.MaxCallRecvMsgSize()` при инициализации.

---

128. **Unary Server Interceptor**: Напиши функцию-логгер, которая выводит метод запроса и время его выполнения. Зарегистрируй её через `grpc.UnaryInterceptor()`.

129. **[Высокая сложность]**: Используй `google.golang.org/genproto/googleapis/rpc/errdetails`. Создай ошибку `BadRequest` с деталями о том, какие именно поля некорректны (FieldViolation).

130. Реализуйте API key аутентификацию через метаданные (заголовок `x-api-key`).

131. Создайте middleware, который логирует IP-адрес клиента (извлекайте из `peer.Peer` в контексте).

132. **Тестирование gRPC на In-Memory сокетах (`bufconn`)**: Написание unit-тестов для gRPC не должно зависеть от открытия портов в операционной системе. Напишите тест для вашего сервера с использованием пакета `google.golang.org/grpc/test/bufconn`. Создайте in-memory слушатель сокетов, подключите к нему клиент и выполните тестовый вызов метода.

133. **Unary Server Interceptor (Логирование)**: Напиши функцию, соответствующую сигнатуре `grpc.UnaryServerInterceptor`. Засеки время старта, вызови `handler(ctx, req)`, логируй имя метода (из `info.FullMethod`), потраченное время и ошибку. Передай интерцептор при создании сервера.

134. **Auth Interceptor**: Напиши перехватчик, который достает из метаданных запроса (`metadata.FromIncomingContext`) токен. Если токен не равен "secret", прерываем запрос через `handler` не вызывается, возвращаем `codes.Unauthenticated`.

135. Изучите проблему "metadata spoofing": как защититься от подделки заголовков, если между клиентом и сервером стоит proxy (например, Envoy)? Используйте trusted headers.

136. Верните стандартную gRPC-ошибку: `status.Errorf(codes.NotFound, "user %s not found", id)`.

137. **Client Interceptor (Автоинжект токена)**: Напиши клиентский интерцептор, который автоматически перед каждым RPC-вызовом добавляет токен авторизации в метаданные контекста (чтобы не писать это руками перед каждым вызовом, как в упр. 453).

138. **Recovery Interceptor**: Напиши перехватчик с `defer func() { if r := recover(); r != nil { ... } }()`. Сымитируй панику в обработчике. Убедись, что сервер не падает, а клиент получает `codes.Internal`.

139. Изучите все коды ошибок gRPC (`codes.InvalidArgument`, `codes.PermissionDenied`, `codes.DeadlineExceeded` и т.д.) и когда их использовать.

140. Добавьте детали к ошибке через `status.WithDetails` (например, список полей, которые не прошли валидацию).

141. **gRPC-Gateway (REST поверх gRPC)**: Добавьте в файл `user.proto` аннотации для HTTP (используя пакеты `google.api.http`). Скомпилируйте прокси-сервер gRPC-Gateway. Запустите HTTP-сервер, который будет принимать обычные REST-запросы в формате JSON на порту `8080` (например, `GET /api/v1/users/123`), автоматически транслировать их в gRPC-запросы на порт `50051` и возвращать JSON-ответ клиенту.

---

142. На клиенте извлеките детали ошибки: `st := status.Convert(err); details := st.Details()`.

143. **Recovery Interceptor**: В одном из обработчиков сервера сделай `panic("boom")`. Весь gRPC сервер упадет. Напиши (или возьми готовую из `go-grpc-middleware`) Recovery Interceptor, который делает `recover()` и возвращает клиенту красивую ошибку с кодом `codes.Internal`.

144. Зарегистрируй цепочку из трех интерсепторов (Recover -> Logging -> Auth) с помощью `grpc.ChainUnaryInterceptor()`.

145. Реализуйте валидацию входных данных через `github.com/go-playground/validator/v10` в interceptor'е. Возвращайте `codes.InvalidArgument` с деталями.

146. **[Каверзный кейс]**: Stream Interceptor: Напиши перехватчик для стриминга. Оберни входящий `grpc.ServerStream` в свою структуру, которая логирует каждый вызов `SendMsg` и `RecvMsg`.

147. **Stream Interceptor**: Напиши логгер для стримовых вызовов (это сложнее, так как требуется обернуть интерфейс `grpc.ServerStream` и переопределить методы `SendMsg` и `RecvMsg`).

148. Используйте `buf.build/gen/go/bufbuild/protovalidate` — современную валидацию на уровне `.proto` файлов (через annotations).

149. Создайте кастомный error code через metadata (если стандартных `codes` не хватает для вашей бизнес-логики).

150. Реализуйте паттерн "circuit breaker" в client interceptor'е: при получении N подряд `codes.Unavailable` перестаньте делать запросы на M секунд.

151. **Unary Client Interceptor**: Напиши перехватчик на стороне клиента, который автоматически добавляет заголовок `Authorization: Bearer secret` в метаданные каждого запроса.

152. Изучите разницу между `codes.Canceled` (клиент отменил) и `codes.DeadlineExceeded` (истек таймаут).

153. **Health Check**: В микросервисной архитектуре балансировщики нагрузки (Kubernetes, Envoy) должны знать, жив ли сервис. Установи пакет `google.golang.org/grpc/health`, зарегистрируй стандартный сервис `health.v1` и научись менять статус своего сервиса с `SERVING` на `NOT_SERVING`.

154. **Мультиплексирование (cmux)**: Часто микросервис должен отдавать gRPC-данные по порту 50051 и HTTP-метрики для Prometheus (или /livez роуты) по тому же порту. Установи библиотеку `github.com/soheilhy/cmux`. Настрой её так, чтобы она слушала один порт, но перенаправляла HTTP-запросы в стандартный HTTP-сервер, а gRPC — в gRPC-сервер.

155. Добавьте `google.api.http` annotations в `.proto` файл, чтобы замапить gRPC-методы на REST endpoints: `option (google.api.http) = { get: "/v1/users/{id}" };`.

156. Реализуйте **mTLS (Mutual TLS)**: настройте сервер и клиент для обязательной проверки сертификатов друг друга. Сгенерируйте CA, серверный и клиентский сертификаты.

157. Сгенерируйте reverse-proxy сервер, который принимает HTTP/JSON запросы и транслирует их в gRPC-вызовы.

158. Запустите gRPC-Gateway как отдельный процесс, который проксирует запросы на gRPC-сервер.

159. **Мягкая остановка (Graceful Stop)**: Настрой перехват сигнала `SIGINT` (Ctrl+C). При его получении вызови `grpcServer.GracefulStop()`. Запусти долгий RPC (на пару секунд) и нажми Ctrl+C — убедись, что сервер дождался ответа клиенту перед завершением.

---

160. Подключи Server Reflection (`reflection.Register(server)`). Используй утилиту `grpcurl` (поставь через brew/go install), чтобы вызвать метод твоего сервиса без написания Go-кода клиента.

161. Сгенерируйте OpenAPI/Swagger документацию из `.proto` файла с помощью `protoc-gen-openapiv2`.

162. Настройте CORS в gRPC-Gateway для работы с фронтенд-приложениями.

163. **[Высокая сложность — gRPC Gateway]**: Сгенерируй reverse-proxy с помощью `protoc-gen-grpc-gateway`. Настрой аннотации `google.api.http` в `.proto` файле (например, `get: "/v1/users/{id}"`). Подними HTTP-сервер, который переводит REST-запросы в gRPC-вызовы. Сделай запрос через `curl`.

164. Используйте `runtime.WithIncomingHeaderMatcher` для проброса определенных HTTP-заголовков в gRPC metadata.

165. Реализуйте кастомную обработку ошибок: конвертируйте gRPC-ошибки в HTTP статус-коды с JSON-телом.

166. Изучите ограничения gRPC-Gateway: streaming RPC не поддерживаются (используйте WebSocket или SSE для них).

167. **[Тестирование через bufconn]**: Используй `google.golang.org/grpc/test/bufconn` для запуска gRPC-сервера в памяти (без занятия реального TCP-порта). Напиши клиент, который коннектится к этому in-memory серверу. Запусти юнит-тест.

168. Реализуй **полный микросервис "User Service"**:
- gRPC API: `CreateUser`, `GetUser`, `UpdateUser`, `DeleteUser`, `ListUsers` (streaming)
- PostgreSQL для persistence
- Redis для кеша
- Outbox pattern для событий (`UserCreated`, `UserUpdated`)
- gRPC interceptors: logging, auth, metrics, tracing
- Health checks, graceful shutdown
- Dockerfile, docker-compose

169. Реализуй **"Order Service"**, зависящий от User Service:
- gRPC вызов `user.GetUser` при создании заказа
- Circuit breaker + retry + timeout для вызова User Service
- Saga: `CreateOrder` → `ReserveInventory` → `ProcessPayment` → `ShipOrder`
- Компенсация при ошибке
- Distributed tracing: trace проходит через Order → User → Payment

170. Реализуй **"Notification Service"**:
- Подписка на events из Kafka/NATS (Outbox от User/Order сервисов)
- Отправка email/SMS/push (заглушки)
- DLQ для необработанных событий
- gRPC API для управления подписками

171. Реализуйте асинхронную обработку: клиент отправляет запрос, сервер возвращает `operation_id`, клиент опрашивает статус операции другим RPC.

172. Реализуй **API Gateway**:
- HTTP REST на входе, gRPC на выходе к сервисам
- Auth: JWT validation, rate limiting per client
- Request/response transformation: REST JSON ↔ gRPC protobuf
- Aggregation: `GET /dashboard` вызывает 3 сервиса параллельно, мержит результаты

173. Реализуй **"Integration Test Suite"**:
- testcontainers: PostgreSQL, Redis, Kafka
- Запуск всех 3 сервисов в памяти (или в Docker)
- gRPC клиенты, HTTP клиенты
- Проверка end-to-end сценариев: регистрация → создание заказа → уведомление
- Очистка данных между тестами

174. Реализуй **"Load Test"**:
- `ghz` или `k6` для gRPC/HTTP нагрузки
- 1000 RPS на User Service
- Измерь latency p50/p95/p99
- Найди bottleneck через pprof
- Оптимизируй и повтори

175. Используйте серверную рефлексию (`grpc.reflection.v1alpha.ServerReflection`) для удобной отладки с `grpcurl`.

176. Реализуй **"Blue-Green Deployment"**:
- 2 версии сервиса (blue=v1, green=v2)
- Ingress переключает трафик 100% blue → 100% green
- Без downtime, с rollback возможностью
- Проверь через integration tests на green перед переключением

177. Реализуй **"Multi-region deployment"** (концептуально):
- Active-Active: 2 региона, каждый обрабатывает свой shard пользователей
- Global load balancer (GeoDNS) маршрутизирует
- Replication PostgreSQL (read replicas), Redis Cluster
- Failover: при падении региона — traffic shift в другой

178. Реализуй **"Event-driven architecture"**:
- Сервисы не вызывают друг друга напрямую
- Все коммуникации через event bus (Kafka/NATS)
- Event schema registry (Avro/protobuf)
- Backward/forward compatibility проверка в CI

179. Создайте gRPC-gateway: сгенерируйте REST-прокси для вашего сервиса и предоставьте доступ через HTTP/JSON параллельно с gRPC.

180. Реализуй **"Schema Evolution"**:
- v1: `User { id, name }`
- v2: `User { id, name, email }` (новое обязательное поле?)
- v3: `User { id, name, email, phone }` (optional)
- Покажи backward compatibility: v2 клиент читает v3 сервер (unknown fields ignored)
- Forward compatibility: v3 клиент читает v2 сервер (default values)

181. Реализуй **"gRPC-Web"**:
- Envoy proxy транслирует HTTP/1.1 + WebSocket → gRPC
- Браузерный клиент на TypeScript/JavaScript
- Unary и server streaming через gRPC-Web
- Сравни с REST API: типизация, streaming, производительность

182. Реализуйте переключение между REST и gRPC в одном приложении с помощью `grpc-gateway` и `ServeMux`.

183. Реализуй **"gRPC vs REST benchmark"**:
- Одинаковый функционал: GET/POST user
- gRPC (protobuf binary) vs REST (JSON)
- Замерь: payload size, serialization time, deserialization time, total latency
- Документируй результаты: когда gRPC выигрывает, когда REST проще

184. Реализуй **"Security hardening"**:
- mTLS между всеми сервисами
- OAuth2/OIDC для пользовательской аутентификации
- RBAC: роли (admin, user, guest) в JWT, проверка в interceptor
- Input validation: protobuf constraints (`validate.rules`)
- Rate limiting, DDoS protection
- Security headers, CORS policy

185. Соберите несколько сервисов в docker-compose: `users`, `orders`, `gateway`. Настройте их взаимодействие по gRPC с healthcheck и зависимостями.

186. Реализуй **"Performance optimization"**:
- Профилирование: CPU profile → найди hotspot
- Memory profile → найди аллокации
- goroutine profile → найди утечки
- Оптимизация: pooling, zero-allocation, batching
- Повтори benchmark, документируй improvement

187. Реализуй **"Disaster Recovery"**:
- RPO (Recovery Point Objective): 5 минут потерь данных допустимо
- RTO (Recovery Time Objective): 15 минут на восстановление
- Backup: PostgreSQL WAL archiving, Redis RDB+AOF
- Restore procedure: документирован, автоматизирован, тестируется регулярно
- Multi-AZ, multi-region failover

188. Напишите интеграционный тест, который поднимает все сервисы (можно с testcontainers), выполняет полный пользовательский сценарий и проверяет корректность данных.

---

Общий счёт теперь достиг **430 упражнений**. Этот раздел даёт полный цикл разработки современных микросервисных систем на Go — от контракта protobuf до эксплуатационных практик. Успешной интенсивной тренировки!

189. Реализуй **"Compliance and Audit"**:
- Все mutation-запросы логируются: кто, когда, что изменил, до/после
- Immutable audit log (append-only, подписанный)
- GDPR: право на забвение — soft delete + anonymization
- Data retention: автоматическое удаление старых данных
- Access log: кто запрашивал какие данные

---
