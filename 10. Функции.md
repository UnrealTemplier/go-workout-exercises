# Функции (все виды и вариации)

В этом файле собраны уникальные упражнения по теме **"Функции (все виды и вариации)"**, отсортированные по возрастанию сложности.

---

1. Напишите функцию без параметров и возврата, печатающую приветствие.

2. Примените именованные возвращаемые значения: функция, возвращающая площадь и периметр прямоугольника, использует `return` без аргументов.

3. Создайте вариативную функцию (variadic function), принимающую произвольное количество чисел и возвращающую их среднее арифметическое.

4. Сделайте вариативную функцию `max(numbers ...int) int`, передайте в неё срез через `slice...`.

5. Используйте оператор `...` для передачи слайса в вариативную функцию.

6. Напишите функцию, которая принимает строку и возвращает её реверс (учитывая Unicode-символы, а не просто байты).

7. Создайте замыкание — функцию, возвращающую счётчик вызовов.

8. Создайте функцию, которая принимает возраст и возвращает две булевы переменные: `isAdult` и `isSenior`.

9. Напишите функцию, которая принимает два числа и возвращает их сумму, разность, произведение и частное (множественный возврат).

10. Через замыкание реализуйте генератор чисел Фибоначчи (функция возвращает очередное число).

11. Создайте функцию, которая генерирует последовательность Фибоначчи до N-го элемента и возвращает её в виде слайса.

12. Реализуйте рекурсивное вычисление факториала.

13. Реализуйте «каррированную» функцию: `adder(a int) func(int) int`, возвращающую функцию, прибавляющую `a`.

14. Создайте функцию с именованными возвращаемыми значениями (naked return) и изучите, как это влияет на читаемость кода.

15. Реализуйте функцию, которая принимает массив (не слайс!) и изменяет его элементы. Убедитесь, что массив передается по значению.

16. **Множественный возврат**: Напишите функцию `Divide(a, b float64) (float64, error)`, которая возвращает результат деления и ошибку, если делитель равен нулю.

17. Покажите, как `defer` может изменить именованное возвращаемое значение (ловушка с `defer func() { result = 42 }()`).

18. Объявите анонимную функцию и немедленно её вызовите (IIFE).

19. Организуйте `panic` в одной функции и перехватите его через `recover` в отложенной функции, вернув ошибку.

20. В одной функции напишите три отложенных вызова `defer` и определите порядок их исполнения.

21. Создайте функцию, принимающую указатель на структуру `Person` и изменяющую возраст.

22. Напишите функцию-генератор последовательности четных чисел с использованием замыкания.

23. Определите метод `Inc` для типа `type Counter int` с получателем-указателем; продемонстрируйте вызов.

24. **Функция как переменная**: Присвой анонимную функцию переменной и вызови её несколько раз через эту переменную.

25. **Простая функция**: Напишите функцию `SayHello()`, которая просто печатает приветствие в консоль.

26. Напиши функцию `Add(a, b int) int`.

27. Используйте ключевое слово `defer` для отложенного вывода сообщения "Программа завершена" в конце функции `main`.

28. Реализуйте метод для структуры `Rectangle`, изменяющий её размеры (получатель-указатель).

29. Объявите интерфейс `Stringer` с методом `String() string` и реализуйте его для вашей структуры; используйте в `fmt.Println`.

30. **Сокращенный синтаксис параметров**: Напишите функцию `SumThree(a, b, c int) int`, демонстрирующую группировку однотипных аргументов.

31. Напишите функцию, которая открывает файл (эмулируя), и используйте `defer` для гарантированного закрытия файла.

32. Напишите функцию, принимающую пустой интерфейс `interface{}`, и примените утверждение типа `.(type)`.

33. **Функция с возвратом**: Напиши функцию для вычисления площади прямоугольника.

34. Напиши функцию, которая возвращает другую функцию (замыкание/closure). Возвращаемая функция должна прибавлять к своему аргументу число, захваченное из внешней функции.

35. Реализуйте переключатель типов `type switch`, печатающий разную информацию в зависимости от типа переданного значения.

36. Используйте `panic` для прерывания выполнения программы при возникновении критической ошибки (например, деление на ноль).

37. **Именованный возврат**: Напишите функцию `RectangleStats(width, height float64) (area, perimeter float64)`, которая вычисляет площадь и периметр прямоугольника, используя именованные возвращаемые значения.

38. Вызови вариативную функцию, передав ей срез `[]int{1, 2, 3}` с использованием оператора `...`.

39. **Именованные возвращаемые значения (Naked return)**: Перепиши функцию из упр. 53, назвав возвращаемые переменные в сигнатуре. Сделай возврат через `return` без аргументов.

40. **"Голый" возврат (Naked return)**: Перепишите предыдущую функцию `RectangleStats` так, чтобы она использовала "голый" оператор `return`. Напишите комментарий, почему в больших функциях этого стоит избегать.

41. Поймайте классическую ошибку замыкания в цикле: создайте слайс функций, каждая должна выводить свой индекс. Сравните неправильный вариант и правильный (с копией переменной).

42. **Вариативные параметры**: Напиши функцию `Sum(nums ...int) int`, которая принимает любое количество чисел и возвращает их сумму.

43. Передайте функцию как аргумент в другую функцию (функция высшего порядка), например, для применения математической операции к числу.

44. Напиши функцию `SafeDivide(a, b int) (result int, err error)`, которая использует `defer` + `recover` для перехвата деления на ноль (через `panic` при `b == 0`). Обсуди, хорошая ли это практика.

45. Напиши функцию `Filter(nums []int, predicate func(int) bool) []int`, которая фильтрует слайс по предикату. Реализуй предикаты: чётные, положительные, больше 10.

46. **Опасность Naked return**: Напиши функцию с именованным возвратом `result int`. Внутри функции открой блок `if` и объяви там новую `result := 10` (через `:=`). Сделай naked return. Пойми ошибку затенения в возвращаемых значениях.

47. **Вариативные параметры**: Напишите функцию `Concat(sep string, words ...string) string`, которая соединяет переданные строки через разделитель.

48. Напишите вариативную функцию-обёртку для `fmt.Println`, принимающую `...interface{}`.

49. **Анонимная функция (IIFE)**: Напишите и сразу же вызовите анонимную функцию, которая выводит текущее время.

50. **Распаковка среза**: Создайте срез строк `[]string{"Go", "is", "awesome"}`. Передайте его в созданную ранее функцию `Concat` с использованием оператора `...`.

51. Напиши функцию `ApplyOperation(a, b int, op func(int, int) int) int`. Передай ей анонимную функцию для умножения.

52. Используйте `sort.Slice` с функцией-замыканием для сортировки среза структур по разным полям.

53. Напиши функцию `Greet(name string)`, которая выводит приветствие. Вызови её из `main`.

54. Реализуйте калькулятор с операциями, хранящимися в `map[string]func(float64, float64) float64`.

55. **Распаковка слайса**: Создай обычный срез (slice) `[]int`. Передай его в функцию `Sum` из упр. 56 с помощью оператора `...` (распаковка).

56. Напиши функцию `Divide(a, b float64) (float64, error)`. При `b == 0` верни ошибку (`errors.New`). Обработай ошибку в вызывающем коде.

57. Создайте метод `String() string` для сложной структуры (например, дерева), обеспечивающий её красивое рекурсивное представление.

---

58. **Функция как переменная**: Создайте переменную `operation` типа "функция, принимающая два `int` и возвращающая `int`". Присвойте ей анонимную функцию сложения, а затем замените её на функцию умножения.

59. **Анонимные функции**: Объяви функцию прямо внутри `main()` без имени и тут же вызови её.

60. Напиши функцию с отложенным вызовом `defer`. Внутри функции выведи "Start", в `defer` выведи "End". Вызови функцию.

61. **Функции высшего порядка (Аргумент)**: Напиши функцию `Filter`, которая принимает срез чисел и функцию-условие (`func(int) bool`). Она должна вернуть только те числа, для которых условие вернуло `true`.

62. Создай цикл с тремя вызовами `defer`. Посмотри, в каком порядке они выполнятся (LIFO).

63. **Замыкание (Closure)**: Создайте генератор идентификаторов `NewIDGenerator() func() int`. Каждый вызов возвращенной функции должен возвращать число на 1 больше предыдущего (начиная с 1).

64. **[Каверзный кейс]**: Напиши цикл `for i := 0; i < 3; i++` с `defer fmt.Println(i)` внутри. Объясни, почему выведутся двойки (и как это исправить, передавая аргумент в функцию).

65. **Замыкания (Closures) 1**: Напиши функцию `Counter() func() int`, которая при каждом вызове возвращает число, на 1 большее предыдущего. (Сохранение состояния в замыкании).

66. **Замыкание в цикле (Исторический баг)**: Создайте срез функций `funcs := make([]func(), 3)`. В цикле `for i := 0; i < 3; i++` заполните этот срез анонимными функциями, каждая из которых выводит `i`. Запустите эти функции после цикла. Какое поведение вы увидите? (Учтите особенности версий Go до 1.22 и современных).

67. Напиши функцию `Add(a, b int) int` с именованными параметрами: `func Add(a, b int) (sum int) { sum = a + b; return }`. Объясни, зачем именованные возвращаемые значения.

68. **[Высокая сложность]**: Напиши функцию-обертку (decorator), которая принимает любую функцию (через `interface{}` или `reflect`) и выводит "Before" до её вызова и "After" после.

69. **Базовый defer**: Напишите функцию, которая открывает гипотетический ресурс (выводит "ресурс открыт"), затем производит вычисления, а закрытие ресурса ("ресурс закрыт") гарантирует с помощью `defer`.

70. **Замыкания 2**: Создай несколько независимых счетчиков из упр. 62. Убедись, что каждый ведет свой счет независимо.

71. **Порядок выполнения defer (LIFO)**: Напишите функцию, в которой друг за другом вызываются три оператора `defer` с выводом чисел 1, 2 и 3. Убедитесь в порядке их выполнения (стек).

72. **Каверзный случай с циклами (Go до 1.22)**: Напиши цикл `for i := 0; i < 3; i++`, внутри которого в слайс добавляются анонимные функции, возвращающие `i`. Вызови функции после цикла. Изучи проблему захвата переменной цикла (с версии 1.22 поведение изменилось, поэкспериментируй!).

73. Напиши функцию с множественным возвратом: `MinMax(nums ...int) (min, max int)`. Найди минимум и максимум в слайсе.

74. **Изменение именованного возврата в defer**: Напишите функцию `DoubleValue(x int) (result int)`. В теле функции присвойте `result = x`. Внутри `defer` удвойте значение `result`. Проверьте, какое значение вернет функция при вызове.

75. Напиши функцию с variadic-параметром: `Sum(nums ...int) int`. Сложи все переданные числа. Вызови с разным количеством аргументов и со слайсом (через `...`).

76. **Базовый defer**: Поставь `defer fmt.Println("Конец")` в начале `main`. Убедись, что вывод происходит перед самым выходом из функции.

77. Определи тип функции: `type MathFunc func(int, int) int`. Используй этот тип в качестве аргумента для другой функции.

78. **Рекурсия**: Напишите рекурсивную функцию для нахождения n-го числа Фибоначчи.

---

79. Напиши функцию `PrintAll(format string, args ...interface{})`, которая форматирует и выводит аргументы. Используй `fmt.Sprintf` внутри.

80. **Замер времени выполнения**: Напишите функцию `SlowWorker()`, которая засыпает на 100 миллисекунд (`time.Sleep`). С помощью связки `defer` и функции `time.Now()` / `time.Since()` сделайте автоматический замер и вывод времени её работы при завершении.

81. **Стек defer**: Напиши цикл с `defer fmt.Println(i)`. Убедись, что `defer` вызываются в порядке LIFO (Последним пришел — первым ушел).

82. Создай функцию `MakeMultiplier(factor int) func(int) int` (функция, возвращающая функцию). Верни замыкание, которое умножает аргумент на `factor`. Создай несколько множителей и проверь их работу.

83. **Panic и Recover**: Напишите функцию, которая провоцирует панику (`panic("критическая ошибка")`). Оберните вызов в функцию с `recover()` внутри `defer`, чтобы программа не завершалась аварийно, а корректно обрабатывала инцидент.

84. **Defer и возвращаемые значения**: Напиши функцию с именованным возвращаемым значением. Используй `defer`, чтобы изменить это значение прямо перед выходом из функции (используется для подмены возвращаемого результата).

85. Напиши функцию `Reduce(nums []int, fn func(int, int) int, initial int) int` — аналог `fold`/`reduce`. Реализуй сумму, произведение, максимум через `Reduce`.

---

86. **Рекурсия 2**: Реализуй вычисление n-го числа Фибоначчи через рекурсию.

87. **Panic**: Напиши функцию, которая вызывает `panic("что-то пошло не так")` при определенном условии. Убедись, что программа экстренно завершается.

88. **Recover (перехват паники)**: Добавь `defer` с функцией `recover()` в упр. 70, чтобы "спасти" программу от падения и корректно обработать ошибку.

89. **Panic на уровне вызовов**: Помести `panic` в функцию C, которая вызывается из функции B, которая вызывается из функции A (где стоит `recover`). Посмотри, как разматывается стек вызовов.

90. **Методы (база)**: Объяви свой тип `type Counter int`. Напиши для него метод `func (c Counter) Print()`, который можно будет вызвать как `myCounter.Print()`.

91. Напиши функцию `PanicRecover()`. Вызови `panic("something went wrong")`. В другой функции отложи `recover()` через `defer`. Покажи, как восстановиться после паники.

92. **Пустой интерфейс (анонс)**: Напиши функцию `PrintAnything(v interface{})` (или `any`), которая принимает значение любого типа и использует `%v` для его вывода.

---

93. Напиши функцию высшего порядка: `ApplyToEach(nums []int, fn func(int) int) []int`, которая применяет функцию к каждому элементу слайса.

94. Напиши рекурсивную функцию `Factorial(n int) int`. Добавь проверку на отрицательные числа и переполнение.

95. Напиши рекурсивную функцию `Fibonacci(n int) int`. Замерь время для `n = 40`. Затем оптимизируй через мемоизацию (map или слайс). Сравни производительность.

96. Напиши функцию `DeferStack()`. Используй `defer` с анонимной функцией, которая захватывает переменную. Покажи, что defer видит значение на момент выполнения (не объявления), если переменная передаётся по указателю или замыканию.

97. Напиши функцию `DeferWithArgs()`. Вызови `defer fmt.Println(i)` в цикле. Покажи, что все defer'ы выводят одно и то же значение (потому что `i` захватывается по ссылке в замыкание). Исправь, передавая `i` как аргумент анонимной функции.

98. Напиши функцию `Timer(name string) func()`, которая замеряет время выполнения. Используй замыкание: при вызове `defer Timer("operation")()` замерь время между входом и выходом.

99. Напиши generic-функцию `Filter[T any](s []T, predicate func(T) bool) []T`. Создай ограничение (constraint), чтобы `T` был `comparable` или `constraints.Ordered`.

100. Напиши generic-функцию `Max[T constraints.Ordered](a, b T) T`. Протестируй с `int`, `float64`, `string`. Объясни, что такое `constraints.Ordered`.

---
