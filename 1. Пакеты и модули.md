# Пакеты и модули

В этом файле собраны уникальные упражнения по теме **"Пакеты и модули"**, отсортированные по возрастанию сложности.

---

1. **Hello, World!**: Инициализируй модуль (`go mod init`), создай `main.go`, напиши вывод строки и запусти через `go run`.

2. Установите Go, настройте окружение (`GOROOT`, `GOPATH`, `PATH`), проверьте командой `go version`.

3. Измени модуль: добавь второй файл `.go` в тот же пакет `main`. Раздели программу между двумя файлами (например, `main.go` и `greet.go`). Убедись, что `go run` работает с обоими файлами.

4. Напишите файл `main.go` с `package main` и `func main()`, выводящий `Hello, World!`. Запустите через `go run main.go`.

5. **Первый запуск**: Создайте файл `main.go` в корне вашего модуля. Напишите базовую функцию `main`, которая выводит строку `"Hello, Go!"`. Запустите программу напрямую без компиляции с помощью команды `go run main.go`.

6. Инициализируйте новый модуль в пустой папке с помощью `go mod init example.com/myproject`.

7. Создай внутри модуля поддиректорию `utils/` с пакетом `utils`. Вынеси туда функцию приветствия. Импортируй и используй её из `main`. Запусти программу.

8. **Инициализация модуля**: Создайте пустую директорию `hello_mod`. Инициализируйте в ней Go-модуль с помощью команды `go mod init hello_mod`. Убедитесь, что файл `go.mod` успешно создался, и изучите его содержимое.

9. Создай ещё один подпакет `utils/strings` (вложенный пакет). Напиши функцию `Reverse(s string) string`. Импортируй оба пакета `utils` и `utils/strings` в `main`. Обрати внимание на пути импорта.

10. **Разделение файлов**: В папке с `main.go` создай файл `helper.go` (с тем же пакетом `main`). Напиши там простую функцию и вызови её в `main()`. Запусти через `go run .`.

11. Используйте флаг `-o` при компиляции, чтобы задать кастомное имя для исполняемого файла.

12. Создай пакет `mathops` с функциями `Add`, `Sub`, `Mul`, `Div`. Сделай так, чтобы `Div` возвращала `(float64, error)` при делении на ноль. Протестируй все функции из `main`.

13. Создай пакет `geometry` с подпакетами `2d` и `3d`. В `2d` — функции площади круга и прямоугольника. В `3d` — объём сферы и куба. Импортируй оба подпакета в `main`. Обрати внимание на то, что имена пакетов не должны начинаться с цифры — придумай правильные имена.

14. **Свой пакет**: Создай директорию `mathutils`. В ней создай пакет `mathutils` с экспортируемой функцией `Add` (начинается с большой буквы). Импортируй её в `main.go` и используй.

15. Создайте второй файл в той же папке с тем же `package main` и разнесите логику между двумя файлами. Скомпилируйте проект.

16. Добавьте внешнюю зависимость, например `github.com/fatih/color`, выполните `go get` и используйте её для цветного вывода строки.

17. Создай пакет `shapes` с экспортируемыми и неэкспортируемыми функциями. Попробуй вызвать неэкспортируемую функцию из `main` — убедись, что получаешь ошибку компиляции. Объясни, почему.

18. **Создание подпакета**: Внутри модуля создайте новую папку `calculator`. Внутри нее создайте файл `calc.go` с пакетом `package calculator`. Напишите там простую экспортируемую функцию `Add(a, b int) int`. Импортируйте этот пакет в свой `main.go` и вызовите функцию.

19. Проверьте версию Go, установленную в системе, используя команду `go version`.

20. Создайте в проекте подкаталог `mypkg` с файлом `helper.go` (пакет `mypkg`). Экспортируйте из него функцию и вызовите её из `main`.

21. Создай пакет `data` с глобальной экспортируемой переменной `GlobalCounter` (тип `int`). Увеличивай её из `main` и из другого пакета. Проследи, как изменяется значение. Объясни поведение.

22. Создай новый модуль. Импортируй `github.com/fatih/color`. Напиши программу, которая выводит цветной текст в консоль (красный, зелёный, жёлтый). Запусти `go mod tidy` и изучи изменения в `go.mod` и `go.sum`.

23. **Разделение одного пакета**: Создайте файл `helper.go` в той же директории, где находится `main.go` (оба файла должны объявлять `package main`). Перенесите часть логики вывода в `helper.go`. Запустите проект с помощью `go run main.go helper.go` или `go run .`.

24. **Стандартная библиотека**: Импортируй стандартный пакет `math/rand`. Сгенерируй и выведи случайное число.

25. Выведите информацию о переменных окружения Go с помощью `go env` и найдите пути к `GOPATH` и `GOROOT`.

26. Создай пакет `config` с функцией `init()`. Внутри `init()` установи глобальную переменную. Добавь `init()` в `main`. Проследи порядок выполнения `init()`-функций (используй `fmt.Println` с метками). Создай несколько файлов в одном пакете с `init()` — в каком порядке они выполняются?

27. Выполните `go fmt` и `go vet` для своего кода, исправьте все замечания.

28. **Экспорт и видимость**: В пакете `calculator` создайте две функции: одну с заглавной буквы (`Sub`), другую со строчной (`multiply`). Попробуйте вызвать обе из пакета `main`. Зафиксируйте ошибку компиляции для неэкспортируемой функции.

29. Создай пакет `logger` с несколькими файлами, каждый со своей `init()`. Проверь порядок инициализации. Затем создай зависимость: пакет `app` импортирует `logger`, `main` импортирует `app`. Проследи полный порядок `init()`.

30. **Алиасы импорта**: Импортируй пакет `fmt` с псевдонимом `f` (например, `import f "fmt"`). Выведи текст, используя `f.Println`.

31. Используйте команду `go fmt` на файле с намеренно плохим форматированием кода и проанализируйте результат.

32. Создай модуль `calculator` с пакетом `calc`. В другой директории создай модуль `app`, который импортирует `calculator` по локальному пути (`replace` в `go.mod`). Настрой `replace` директиву.

33. **Функция инициализации пакета**: Напишите функцию `init()` в пакете `calculator`, которая выводит текст `"Calculator init"`. Напишите аналогичную функцию `init()` в пакете `main`. Запустите программу и посмотрите на порядок вывода сообщений.

34. Напишите программу, читающую аргументы командной строки через `os.Args` и выводящую их количество и значения.

---

35. **Внешние зависимости**: Найди библиотеку `github.com/google/uuid`. Скачай её через `go get`, импортируй и выведи сгенерированный UUID.

36. Создай модуль с несколькими версиями (используй git-теги v1.0.0, v2.0.0). В `app` импортируй разные версии одного модуля. Объясни, как Go обрабатывает semver.

37. В пакете `mathops` создай переменную `secretCode` (с маленькой буквы) и функцию `GetSecretCode` (с большой). Попробуй обратиться к `secretCode` из `main` (поймай ошибку) и получи доступ через функцию.

38. **Исследование go.mod**: Открой файл `go.mod` и `go.sum`. Изучи, как записалась зависимость `uuid`. Попробуй вручную удалить её из кода и запусти `go mod tidy` — посмотри, как обновится `go.mod`.

39. Запустите статический анализатор кода с помощью `go vet` на вашем проекте и исправьте найденные предупреждения.

40. Создай модуль `mymod` с пакетом `api`. Сделай `go build` для получения исполняемого файла. Затем сделай `go install`. Найди, куда установился бинарник (`GOBIN`, `GOPATH`). Объясни разницу между `go build`, `go install`, `go run`.

41. **Подключение внешней зависимости**: Инициализируйте новый модуль. Найдите на GitHub или pkg.go.dev популярную библиотеку (например, `github.com/google/uuid`). Скачайте её с помощью `go get github.com/google/uuid`. Убедитесь, что в `go.mod` появилась запись, а также создался файл `go.sum`.

42. **Несколько init-функций**: Напишите две разные функции `init()` в одном файле `main.go`. Напишите еще одну в файле `helper.go` (тоже `package main`). Посмотрите, выполняются ли они все и в каком порядке.

43. Создай модуль, скомпилируй его в бинарник для текущей ОС. Затем скомпилируй кросс-платформенно: для Windows (GOOS=windows GOARCH=amd64), для Linux ARM (GOOS=linux GOARCH=arm64). Проверь бинарники командой `file`.

44. **Функция init() в main**: Напиши функцию `func init()` в `main.go`. Выведи в ней сообщение. Убедись, что она вызывается до `main()`.

45. **Использование стороннего пакета**: Внедрите импорт `github.com/google/uuid` в ваш код. Сгенерируйте UUID версии 4 и выведите его в консоль.

46. Создай модуль `calculator`. Внутри создай пакет `mathops`. Напиши там функции `Add` и `Subtract` (с большой буквы!).

47. Создай Makefile (или скрипт) с целями: `build`, `run`, `test`, `clean`, `install`. Автоматизируй сборку и запуск.

---

48. В пакете `main` импортируй свой пакет `mathops` и используй его функции.

49. **Очистка зависимостей**: Удалите из кода `main.go` использование библиотеки `uuid`, но оставьте её в импортах. Выполните команду `go mod tidy`. Посмотрите, как изменились файлы `go.mod` и `go.sum`.

50. **Порядок инициализации пакетов**: Добавь `init()` в свой пакет `mathutils`. Импортируй его в `main`. Проследи, чей `init()` выполняется раньше (пакета или `main`).

51. Используя `github.com/olekukonko/tablewriter`, создай красивую таблицу с данными (имя, возраст, город) минимум 5 строк.

52. **Псевдонимы (алиасы) пакетов**: Импортируйте ваш пакет `calculator` под другим именем (например, `calc`). Используйте новый алиас для вызова функций пакета: `calc.Add(...)`.

53. **Анонимный импорт (blank import)**: Импортируй пакет `mathutils` через `_ "путь/к/mathutils"`. Убедись, что вызывается только его `init()`, а компилятор не ругается на неиспользуемый пакет.

54. Импортируй `github.com/spf13/cobra` и создай CLI-приложение с двумя командами: `hello` (приветствие) и `time` (текущее время). Добавь флаг `--name` для команды `hello`.

55. Создай пакеты `models/user` и `models/order`. Используй их в `main`. Обрати внимание на то, как Go трактует имя последней папки как имя пакета.

56. **Импорт ради побочных эффектов**: Импортируйте пакет `calculator` с использованием пустого идентификатора `_ "hello_mod/calculator"`. Убедитесь, что функция `init()` из него вызывается, даже если вы не используете функции пакета напрямую.

---

57. Используя `github.com/sirupsen/logrus`, настрой логгер с разными уровнями (Debug, Info, Warn, Error). Запиши логи в файл и в консоль одновременно (multi-writer).

58. Импортируй `golang.org/x/exp/constraints` и создай generic-функцию `Min` и `Max` для числовых типов. Объясни, почему этот пакет в `x/exp`, а не в стандартной библиотеке.

59. Открой файл `go.mod` и изучи его содержимое. Найди версию Go и requirement.

60. Создай модуль, добавь зависимость `github.com/stretchr/testify` (только для тестов). Убедись, что она попадает в `go.mod` с директивой `// indirect`. Затем напиши тест с `assert.Equal` и запусти `go test`.

61. Выполни команду `go mod tidy` после удаления импорта `color` из кода. Проверь, что зависимость исчезла из `go.mod`.

62. Изучи `go.mod` после добавления зависимостей. Объясни назначение каждой секции: `module`, `go`, `require`, `replace`, `exclude`. Попробуй использовать `exclude` для блокировки конкретной версии пакета.

63. Просмотри все скачанные зависимости в кэше с помощью `go env GOPATH` и перехода в папку `pkg/mod`.

64. Выполни `go mod graph` и проанализируй граф зависимостей. Найди транзитивные зависимости. Выполни `go mod why` для одной из зависимостей.

65. Создай локальный модуль `myutils`. В основном модуле `mainapp` добавь зависимость через `replace` в `go.mod`, указав путь к `myutils` на файловой системе.

66. Создай модуль с уязвимой зависимостью. Запусти `govulncheck` (или `go list -m -json all | nancy sleuth`). Проанализируй результат.

67. Скачай исходники любого стороннего пакета (например, `github.com/stretchr/testify`) с помощью `go mod download`.

68. Создай приватный модуль (локально или на приватном репозитории). Настрой `GOPRIVATE` и `GONOSUMDB`. Импортируй его в другой проект.

---

69. Проверь, какие версии стороннего пакета доступны, используя `go list -m -versions github.com/fatih/color`.

70. Создай пакет `internal/secret`. Попробуй импортировать его из другого модуля (должна быть ошибка), а затем из того же модуля (должно работать).

71. Создай программу, которая читает переменные окружения (`os.Getenv`, `os.LookupEnv`). Реализуй конфигурацию через `.env` файл (можно использовать `github.com/joho/godotenv` или парсить самостоятельно).

72. Найдите популярный сторонний пакет (например, `github.com/fatih/color` или `github.com/sirupsen/logrus`), добавьте его в ваш проект с помощью `go get`.

73. Обновите версию стороннего пакета до последней минорной версии с помощью `go get -u` и проверьте, не сломался ли ваш код.

74. Создайте модуль с двумя пакетами: `main` и `greeter`. Импортируйте `greeter` и вызовите его публичную функцию.

75. Создайте пакет с внутренней реализацией в каталоге `internal` и убедитесь, что код извне модуля не может его импортировать.

76. Продемонстрируйте функцию `init()`: пакет автоматически регистрирует себя при импорте (выводит сообщение).

77. Импортируйте пакет с алиасом (`import f "fmt"`) и только ради побочных эффектов (`import _ "net/http/pprof"`).

78. Создайте несколько экспортируемых и неэкспортируемых функций, объясните правило заглавной буквы.

79. Напишите комментарии для пакета и экспортируемой функции в формате godoc, запустите локальный godoc.

80. Добавьте зависимость через `go get package@v1.2.3`, изучите `go.mod` и `go.sum`.

81. Обновите все зависимости до последних минорных версий (`go get -u`), затем выполните `go mod tidy`.

82. Создайте простое CLI-приложение с использованием стандартного пакета `flag` (разбор опций).

83. Установите сторонний пакет `github.com/spf13/pflag` (или `cobra`) и реализуйте тот же CLI с ним.

84. Напишите модульный тест для одной из функций (файл `_test.go`), запустите `go test`.

85. Используйте добавленный пакет для вывода цветного текста в консоль или для логирования событий.

86. Напишите бенчмарк для функции, запустите `go test -bench=.`.

87. Изучите файл `go.sum`, поймите его назначение и почему его обязательно нужно коммитить в систему контроля версий.

88. Соберите программу, внедрив версию и время сборки через `-ldflags "-X main.version=..."`.

89. Воспользуйтесь `go mod vendor` и соберите проект с вендорингом зависимостей.

90. Создайте собственный локальный модуль (отдельная папка с `go.mod`) и подключите его в ваш основной проект с помощью директивы `replace` в `go.mod`.

---
Выполняя эти упражнения последовательно, вы пройдете путь от написания простых консольных скриптов до понимания тонкостей работы памяти, слайсов, интерфейсов и управления зависимостями в Go. Если в процессе понадобится разобрать решение или теорию по любому из этих пунктов — обращайтесь!

91. Постройте проект с многоуровневой структурой пакетов (`cmd/server`, `pkg/database`, `internal/config`), обеспечив корректные пути импорта.

---

Всего 125 упражнений, охватывающих запрошенные темы от нуля до продвинутых нюансов. Рекомендуется выполнять их последовательно, писать код самостоятельно и проверять результаты. Успешной интенсивной тренировки!
