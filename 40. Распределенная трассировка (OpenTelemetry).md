# Распределенная трассировка (OpenTelemetry)

В этом файле собраны уникальные упражнения по теме **"Распределенная трассировка (OpenTelemetry)"**, отсортированные по возрастанию сложности.

---

1. Установи OpenTelemetry SDK: `go.opentelemetry.io/otel`, `go.opentelemetry.io/otel/sdk`, `go.opentelemetry.io/otel/exporters/stdout/stdouttrace`. Создай `TracerProvider` с `BatchSpanProcessor` и `stdout` exporter. Получи tracer: `tp.Tracer("my-service")`. Создай span: `ctx, span := tracer.Start(ctx, "operation-name")`. Заверши `defer span.End()`.

2. Добави **status и error recording**: при ошибке `span.SetStatus(codes.Error, "database connection failed")`, `span.RecordError(err, trace.WithAttributes(attribute.String("db.instance", "postgres-primary")))`. Покажи разницу между status (semantic) и error event (detailed).

3. Создай **nested spans**: родительский span `processOrder`, дочерние `validateInventory`, `chargePayment`, `sendNotification`. Передавай `ctx` через `trace.ContextWithSpan`. Покажи иерархию в stdout выводе.

4. Создай **span links**: при обработке batch сообщения из Kafka — создай span для каждого сообщения, но link'ай к span'у producer'а через `span.AddLink(trace.Link{SpanContext: producerSpanCtx})`. Покажи, что link — не parent-child, а causal connection.

5. Настрой **W3C Trace Context propagation**: `prop := propagation.NewCompositeTextMapPropagator(propagation.TraceContext{}, propagation.Baggage{})`. Inject в HTTP headers: `prop.Inject(ctx, propagation.HeaderCarrier(req.Header))`. Extract из входящего запроса: `ctx := prop.Extract(r.Context(), propagation.HeaderCarrier(r.Header))`. Покажи сквозную трассировку между сервисами.

6. Настрой **Baggage**: `ctx = baggage.ContextWithBaggage(ctx, baggage.MustNewMember("tenant.id", "acme-corp"))`. Читай: `tenantID := baggage.FromContext(ctx).Member("tenant.id").Value()`. Покажи, как baggage передаёт бизнес-контекст через все span'ы.

7. Настрой **OTLP exporter** (OpenTelemetry Protocol): `otlptracegrpc.New(ctx, otlptracegrpc.WithEndpoint("localhost:4317"), otlptracegrpc.WithInsecure())`. Отправляй spans в **Jaeger** или **Tempo**. Проверь в UI.

8. Настрой **Resource** для идентификации сервиса: `resource.NewWithAttributes(semconv.SchemaURL, semconv.ServiceNameKey.String("order-service"), semconv.ServiceVersionKey.String("v2.1.0"), semconv.DeploymentEnvironmentKey.String("production"), attribute.String("k8s.pod.name", os.Getenv("HOSTNAME")))`. Покажи, как resource атрибуты группируют spans в Jaeger.

---

9. Инструментируй **HTTP server** автоматически: `otelhttp.NewHandler(next, "http-server", otelhttp.WithPropagators(prop), otelhttp.WithSpanNameFormatter(func(operation string, r *http.Request) string { return r.Method + " " + r.URL.Path }))`. Каждый request — span с атрибутами `http.method`, `http.url`, `http.status_code`.

10. Инструментируй **HTTP client** автоматически: `client := http.Client{Transport: otelhttp.NewTransport(http.DefaultTransport)}`. Каждый outgoing request — span, propagation заголовков автоматический.

11. Инструментируй **gRPC server**: `grpc.NewServer(grpc.UnaryInterceptor(otelgrpc.UnaryServerInterceptor()), grpc.StreamInterceptor(otelgrpc.StreamServerInterceptor()))`. Покажи spans для каждого RPC с `rpc.method`, `rpc.service`, `rpc.system`.

12. Инструментируй **gRPC client**: `grpc.Dial(..., grpc.WithUnaryInterceptor(otelgrpc.UnaryClientInterceptor()))`. Покажи distributed trace: client span → server span → database span.

13. Добави **attributes** в span: `span.SetAttributes(attribute.String("user.id", "123"), attribute.Int("http.status_code", 200))`. Покажи, как structured metadata обогащает трассировку.

14. Инструментируй **database/sql**: `github.com/XSAM/otelsql`. `db, _ := otelsql.Open("postgres", dsn, otelsql.WithAttributes(semconv.DBSystemPostgreSQL))`. Каждый query — span с `db.statement`, `db.operation`, `db.sql.table`.

15. Добави **events** в span: `span.AddEvent("cache-miss", trace.WithAttributes(attribute.String("key", "user:123")))`. Используй для отметки значимых моментов внутри операции.

16. Инструментируй **Redis/go-redis**: `github.com/redis/go-redis/extra/redisotel/v9`. `rdb.AddHook(redisotel.NewTracingHook())`. Каждая команда — span с `db.system = redis`, `db.statement = GET user:123`.

17. Инструментируй **Kafka producer/consumer**: `github.com/open-telemetry/opentelemetry-go-contrib/instrumentation/github.com/segmentio/kafka-go/otelkafka`. Producer: inject span context в message headers. Consumer: extract и создай child span.

18. Инструментируй **NATS**: вручную inject/extract через message headers. Producer: `msg.Header.Set("traceparent", carrier.Get("traceparent"))`. Consumer: extract, создай span с `trace.WithLinks()`.

19. Создай **custom instrumentation** для бизнес-логики: `func ProcessOrder(ctx context.Context, orderID string) error { ctx, span := tracer.Start(ctx, "ProcessOrder", trace.WithAttributes(attribute.String("order.id", orderID))); defer span.End(); ... }`. Добави sub-spans для каждого шага.

20. Создай **span metrics bridge**: `otelcol` (OpenTelemetry Collector) конфигурация `spanmetrics` processor. Генерируй RED metrics (Rate, Errors, Duration) из spans автоматически. Покажи, что trace → metrics без кода.

---

21. Установите OpenTelemetry SDK: `go.opentelemetry.io/otel`, `go.opentelemetry.io/otel/sdk/trace`, `go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc`.

22. Создайте первый **span**: `tracer.Start(ctx, "processOrder")`. Убедитесь, что он отображается в Jaeger UI.

23. Подключите Jaeger (или Grafana Tempo), настройте OpenTelemetry TracerProvider, экспортирующий трейсы. Создайте корневой спан в main.

24. Используйте **context propagation**: передавайте `ctx` через все функции, чтобы дочерние span'ы автоматически привязывались к родительским.

25. Инициализируй глобальный TracerProvider, который отправляет трейсы в OTLP collector (подними Jaeger в Docker: `jaegertracing/all-in-one`).

26. Автоматически инструментируйте HTTP-клиент и сервер с помощью `otelhttp`. Проверьте, что входящие и исходящие запросы отображаются в трейсе.

27. Установите **status** span'а: `span.SetStatus(codes.Error, "database timeout")` при ошибке.

28. Используйте **OTEL HTTP middleware**: `otelhttp.NewHandler(handler, "server")` для автоматического создания span'ов на каждый HTTP-запрос.

29. Используйте **OTEL HTTP client transport**: `otelhttp.NewTransport(http.DefaultTransport)` для автоматической инъекции trace context в исходящие HTTP-запросы.

30. Создай span для HTTP-обработчика: `tracer.Start(ctx, "handleRequest")`. Обязательно вызови `span.End()` через `defer`.

31. Инструментируйте gRPC с `otelgrpc.UnaryServerInterceptor()` и `otelgrpc.UnaryClientInterceptor()`.

32. Оберните драйвер базы данных (`database/sql`) с `otelsql`, чтобы видеть все SQL-запросы как спаны в трейсе.

33. Инструментируйте database/sql с `github.com/uptrace/opentelemetry-go-extra/otelsql` для автоматического трейсинга SQL-запросов.

34. Инструментируйте Redis с `github.com/redis/go-redis/extra/redisotel` для трейсинга Redis-команд.

35. Добавь атрибуты (теги) к спану: `span.SetAttributes(attribute.String("user.id", "123"))`.

36. Реализуйте **W3C Trace Context propagation** через HTTP-заголовки (`traceparent`, `tracestate`) для распределенной трассировки между микросервисами.

37. Создайте **custom span** для бизнес-логики: `validateOrder`, `reserveInventory`, `processPayment` — каждый как отдельный span внутри `createOrder`.

38. Используйте **span links** для связи трейсов, которые не имеют отношения parent-child (например, batch processing, где один batch связан с множеством независимых запросов).

39. Настройте **sampling strategy**: `TraceIDRatioBased(0.1)` для трейсинга 10% запросов в продакшене (снижение нагрузки на Jaeger/Tempo).

40. **[Контекст-propagation (Внутрипроцессный)]**: В обработчике создай родительский спан. Вызови функцию `SaveToDB(ctx)`. Внутри `SaveToDB` создай дочерний спан, передав `ctx`. Убедись, что в Jaeger дочерний спан вложен в родительский.

41. Реализуйте **tail-based sampling** через OpenTelemetry Collector: сохраняйте 100% трейсов с ошибками и только 1% успешных.

42. Передайте контекст трейсинга между сервисами через заголовки W3C Trace Context. Убедитесь, что цепочка вызовов видна в Jaeger.

43. Используйте **baggage** (`go.opentelemetry.io/otel/baggage`) для проброса бизнес-данных (tenant_id, feature_flag) через всю цепочку сервисов.

44. **[Контекст-propagation (Межсервисный)]**: Напиши два сервиса. Сервис A делает HTTP-запрос к сервису B. Используй `otelhttp.NewTransport(http.DefaultTransport)` на клиенте и `otelhttp.NewHandler` на сервере. Убедись, что трейс склеивается между сервисами (TraceID одинаковый).

45. **[Трассировка БД]**: Используй `go.opentelemetry.io/contrib/instrumentation/github.com/jackc/pgx/v5/pgxotel`, чтобы автоматически создавать спаны для SQL-запросов.

46. **Инициализация OTel SDK**: Установи пакеты `go.opentelemetry.io/otel`. Напиши функцию инициализации `TracerProvider`. Настрой экспорт трейсов в консоль (`stdouttrace`) для начала, чтобы видеть структуру данных глазами.

47. **Инициализация OpenTelemetry Tracing SDK**: Напишите функцию инициализации трейсера `InitTracer()`. Настройте отправку трейсов на локальный коллектор (например, Jaeger или Tempo) по протоколу OTLP (gRPC или HTTP). Обеспечьте корректный сброс буферов трейсера при выходе из приложения через `tracerProvider.Shutdown()`.

48. **[Baggage]**: Передай данные через Baggage (например, `synthetic=true` для запросов от бота) от клиента серверу. Извлеки baggage в сервере и пропиши в логи.

49. **Ручные Спаны (Spans)**: В функции напиши `ctx, span := tracer.Start(ctx, "calculate_tax")`. Сделай `defer span.End()`. Вызови внутри другую функцию, передав ей этот `ctx`, и там начни новый спан (`db_query`). Убедись, что второй спан автоматически стал "ребенком" первого.

50. **[Каверзный кейс]**: Создай горутину внутри обработчика (`go processBackground()`). Передай в неё `context.Background()` вместо родительского `ctx`. Посмотри, как трейс обрывается. Исправь, передав `ctx`, чтобы сохранить связь.

51. **Создание ручных спанов (Spans)**: Напишите функцию `ProcessOrder()`, которая создает именованный спан с помощью `otel.Tracer("order-service").Start(ctx, "ProcessOrder")` [428]. Внутри функции установите кастомные атрибуты спана (например, `order.id = 42` [428]) и запишите ключевые события процесса в виде логов-эвентов с помощью `span.AddEvent("validation_started")` [428]. Не забудьте завершить спан через `defer span.End()`.

52. **Экспорт в Jaeger**: Замени консольный экспортер на OTLP HTTP/gRPC экспортер (`go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp`). Запусти код, сделай запрос, открой UI Jaeger (localhost:16686) и найди свой трейс визуально.

53. **Фиксация ошибок**: В функции произошла ошибка. Вместо простого возврата, сделай `span.RecordError(err)` и `span.SetStatus(codes.Error, "failed to get user")`. Это подсветит спан в Jaeger ярко-красным цветом.

54. **Атрибуты и События**: Внутри спана вызови `span.SetAttributes(attribute.Int("user_id", 42))`. Затем добавь событие `span.AddEvent("cache_miss")`. Посмотри в Jaeger, как это выглядит (События — это как логи, но привязанные конкретно к этому куску трейса).

55. **Обработка паник и ошибок в трейсах**: Если внутри спана происходит ошибка или паника, трейс должен визуализировать это красным цветом. Напишите код: оберните выполнение тяжелой функции в блок с `recover()`. При возникновении ошибки запишите её в спан с помощью `span.RecordError(err)` [429] и явно установите статус спана в `codes.Error` с описанием причины падения [429].

56. Настройте **OpenTelemetry Collector** как агент (sidecar) и как gateway (centralized). Изучите разницу.

57. **Auto-Instrumentation (HTTP/gRPC)**: Не пиши спаны для HTTP вручную! Оберни свой HTTP роутер в мидлварь `otelhttp.NewHandler()`. Аналогично, если есть gRPC, добавь `otelgrpc.UnaryServerInterceptor()`. Убедись, что OTel сам создает спаны с правильным URL, статусом и методом.

58. Настройте pipeline: `receivers → processors → exporters` для traces, metrics и logs (единый пайплайн для всех сигналов).

59. Используйте **batch processor** для агрегации данных перед отправкой (снижение нагрузки на backend).

60. Используйте **memory_limiter processor** для защиты Collector от OOM при пиковых нагрузках.

61. **Сквозная трассировка по HTTP (Context Propagation)**: * Создайте HTTP-клиент, который с помощью `otelhttp` или ручного внедрения (Inject) упаковывает `TraceID` текущего спана в HTTP-заголовки запроса по стандарту W3C Trace Context [430].
    * Создайте HTTP-сервер, который с помощью экстрактора (Extract) извлекает этот `TraceID` из заголовков входящего запроса и создает дочерний спан [430].
    Проверьте, что в системе визуализации (Jaeger) оба действия склеились в один общий трейс.

62. **Запросы во внешний мир (http.Client)**: Твой сервис ходит в стороннее API. Оберни транспорт клиента: `client := http.Client{Transport: otelhttp.NewTransport(http.DefaultTransport)}`. Сделай запрос с передачей `ctx`. Убедись, что создался клиентский спан, и в HTTP заголовки ушел `traceparent`.

63. Настройте **filter processor** для удаления "шумных" span'ов (например, health checks).

64. Используйте **attributes processor** для обогащения телеметрии: добавление `service.version`, `deployment.environment`, `host.name`.

65. Настройте экспорт трейсов в **Jaeger** (для ad-hoc отладки) и метрик в **Prometheus** (через remote_write или Prometheus receiver).

66. **Трейсинг Базы Данных**: Установи библиотеку `github.com/uptrace/opentelemetry-go-extra/otelsql`. Оберни свой драйвер БД. Сделай SQL запрос (передав `Context`). Убедись, что в Jaeger появился спан, внутри которого лежит сам текст SQL-запроса!

67. Настройте экспорт логов в **Loki** через OpenTelemetry Collector для корреляции логов с трейсами.

68. Используйте **correlation**: в Grafana настройте data source links, чтобы из трейса в Tempo можно было перейти к логам в Loki по `trace_id`.

69. **Автоматическая трассировка gRPC-запросов**: Интегрируйте готовые интерцепторы OpenTelemetry для gRPC на стороне клиента и сервера (пакет `go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc`) [431]. Убедитесь, что все метаданные трассировки передаются между микросервисами автоматически без ручного написания кода внедрения заголовков.

---

70. Настройте **service graph** в Grafana: визуализация зависимостей между микросервисами на основе span'ов.

71. **[Exemplar (Связь метрик и трейсов)]**: Prometheus поддерживает Exemplars. Когда записываешь Histogram метрику длительности запроса, прикрепляй к ней `TraceID`. (В Grafana при наведении на график метрики можно будет провалиться прямо в трейс).

72. **[Сломанная зависимость]**: Сервис A вызывает сервис B. Сервис B падает с паникой. Сервис A должен: получить 500 ошибку, записать Error-спан в трейс, инкрементировать метрику `http_client_errors_total`, записать Error-лог с TraceID.

73. **Baggage (Багаж)**: Трейсы передают только TraceID. Но тебе нужно прокинуть `tenant_id` через 4 микросервиса (даже туда, где он явно не нужен в API). Положи его в `baggage.NewContext()`. Убедись, что OTel автоматически пробросил этот "багаж" через HTTP/gRPC заголовки до самого последнего сервиса, где ты смог его прочитать.

---

74. **[Semconv (Семантические конвенции)]**: Используй стандартные атрибуты OTel для HTTP: `http.method`, `http.status_code`, `http.route`. Не выдумывай свои имена тегов.

75. **[Sampling (Сэмплирование)]**: Настрой `ParentBased(TraceIDRatioBased(0.1))` сэмплировщик. Убедись, что в Jaeger улетает только 10% трейсов (чтобы не перегружать хранилище), но ошибочные трейсы (с паникой) всегда попадают (используй кастомный сэмплер или логику в коде).

76. **Связка Metrics + Traces (Exemplars)**: *Для продвинутых.* Настрой Prometheus Histogram так, чтобы при записи долгого ответа (например, 2 секунды) она сохраняла `Exemplar` (пример) с текущим `TraceID`. Это позволяет в Grafana кликнуть на пик графика и сразу перейти в Jaeger на этот конкретный медленный запрос.

77. Реализуй **Distributed Trace Analysis**:
- Trace: `POST /orders` → `OrderService.CreateOrder` → `ValidateInventory` (50ms) → `ReserveInventory` (200ms, DB lock contention!) → `ProcessPayment` (300ms, external API) → `SendNotification` (100ms, async)
- Identify bottleneck: `ReserveInventory` — optimize DB query, add index
- Identify optimization: `SendNotification` — make async, don't block response
- Measure improvement: before/after trace comparison

78. **Утечка трейсов при рестарте**: Если твой сервис выключается (Ctrl+C), последние отправленные трейсы могут потеряться в буфере OTel. Обязательно добавь вызов `defer tracerProvider.Shutdown(ctx)` в свой Graceful Shutdown блок.

79. **[Slow Query Alert]**: Напиши middleware для БД, которое замеряет время выполнения SQL-запроса. Если запрос выполняется дольше 500ms, оно пишет Warning-лог, создает span с тегом "slow_query=true" и инкрементирует метрику `db_slow_queries_total`.
