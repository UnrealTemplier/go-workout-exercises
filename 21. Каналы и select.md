# Каналы и select (Channels, buffering, multiplexing)

В этом файле собраны уникальные упражнения по теме **"Каналы и select (Channels, buffering, multiplexing)"**, отсортированные по возрастанию сложности.

---

1. **Небуферизованный канал (Блокировка)**: Создайте небуферизованный канал `ch := make(chan int)`. Попробуйте записать туда число `ch <- 42` прямо в функции `main` без запуска других горутин. Объясните причину возникновения дедлока (`all goroutines are asleep - deadlock!`). Исправьте код, запустив фоновую горутину, которая считывает данные из этого канала.

2. **Буферизованный канал**: Создайте буферизованный канал емкостью 3 элемента `make(chan int, 3)`. Запишите туда три числа подряд в функции `main`. Заблокируется ли программа? Попробуйте записать четвертое число. Объясните разницу между поведением буферизованных и небуферизованных каналов.

3. **Закрытие канала и Comma-ok**: Создайте канал, запустите горутину, которая пишет туда числа 1, 2, 3 и закрывает канал с помощью `close(ch)`. В основной горутине считывайте данные в бесконечном цикле, используя синтаксис `val, ok := <-ch`. Выведите значение `ok`. Что возвращает канал после закрытия и как понять, что передача данных завершена?

4. Создайте небуферизированный канал `chan string`. Основная горутина отправляет сообщение, другая принимает и печатает.

5. **Бесконечное чтение из закрытого канала (Баг с CPU 100%)**: Напишите программу, которая закрывает канал, а затем в бесконечном цикле `for` читает из него значения без проверки `ok`. Убедитесь, что цикл никогда не завершается, потребляя 100% процессора и возвращая нулевые значения (zero values). Напишите комментарий о том, как избегать этого бага.

6. Два небуферизированных канала: одна горутина пишет в `ch1`, другая читает из `ch1` и пишет в `ch2`, main читает из `ch2` — цепочка передачи.

7. Создай **unbuffered канал** `ch := make(chan int)`. Запусти горутину, которая пишет в канал. В `main` прочитай. Покажи, что запись блокируется, пока нет читателя (синхронизация "рука об руку").

8. **Однонаправленные каналы**: Напишите две функции:
    * `produce(ch chan<- int)` — только записывает данные в канал.
    * `consume(ch <-chan int)` — только читает данные из канала.
    Попробуйте внутри `produce` прочитать из канала или внутри `consume` записать в него. Убедитесь, что компилятор предотвращает такие действия на этапе сборки.

9. Буферизированный канал ёмкостью 3: отправьте 5 значений в горутине, читайте в main с задержкой. Проверьте блокировку отправителя при заполнении.

10. Покажи, что **чтение из закрытого канала** возвращает zero value и `ok=false`: `v, ok := <-ch`. Напиши функцию `Drain(ch <-chan int) []int`, которая забирает все оставшиеся значения после закрытия.

11. Напиши программу с **"deadlock"** через канал: `main` пишет в unbuffered канал без горутины-читателя. Получи `fatal error: all goroutines are asleep - deadlock!`. Объясни, почему Go обнаруживает это.

12. **Небуферизированный канал**: Создай канал `ch := make(chan int)`. В `main` отправь в него число: `ch <- 42`. Запусти и поймай `fatal error: all goroutines are asleep - deadlock!`. (Запись в такой канал блокирует выполнение, пока кто-то не прочитает из него).

13. Используйте `for range` для чтения из канала, пока он не закрыт. Отправитель закрывает канал после нескольких сообщений.

14. **Писатель и Читатель**: Реши проблему из 264: запусти горутину, которая пишет в канал, а чтение `<-ch` сделай в `main`. Программа должна успешно завершиться.

15. Напиши функцию `Square(in <-chan int) <-chan int`, читающую из входного канала, возводящую в квадрат и пишущую в выходной. Закрой выходной канал при закрытии входного. Это **pipeline stage**.

16. Соедини генератор и `Square` в pipeline: `for v := range Square(Generate(2, 3, 4, 5)) { fmt.Println(v) }`. Покажи композицию стадий.

17. **Мультиплексирование с `select`**: Создайте два канала, в которые фоновые горутины отправляют сообщения с разной задержкой (`time.Sleep`). С помощью конструкции `select` ожидайте сообщения из обоих каналов в цикле `for` и выводите то сообщение, которое пришло быстрее.

18. Напиши функцию `Generate(nums ...int) <-chan int`, возвращающую канал. Горутина внутри пишет числа и закрывает канал. Покажи **"generator pattern"**.

19. Паттерн «сигнал завершения»: горутина-воркер читает из канала, пока не получит специальное значение `"stop"`.

20. **Буферизированный канал**: Создай канал `ch := make(chan int, 2)`. Отправь в него два числа подряд прямо в `main` (без горутин). Считай их. Убедись, что дедлока нет (канал с буфером не блокирует запись, пока буфер не заполнится).

21. Напиши функцию `Merge(chs ...<-chan int) <-chan int`, которая мержит несколько входных каналов в один выходной. Используй `sync.WaitGroup` + горутину на каждый входной канал. Закрой выходной, когда все входные закрыты.

22. Покажи **"panic на закрытии закрытого канала"**: закрой канал дважды. Получи `panic: close of closed channel`. Напиши `SafeClose(ch chan int)` с `recover` или `sync.Once`.

23. **Переполнение буфера**: Добавь в упр. 266 отправку третьего числа (при буфере 2). Поймай дедлок.

24. Напиши программу с **"nil channel"**: `var ch chan int` (nil). Покажи, что запись в nil канал **блокирует навечно** (deadlock, если нет других горутин). Чтение из nil канала тоже блокирует навечно.

25. Попробуй отправить 4-е число в канал из предыдущего задания (без читателя). Дождись дедлока.

26. Используй `close(ch)` для сигнализации о завершении. Запусти горутину-продюсер, которая пишет 10 чисел и закрывает канал. В `main` читай через `for v := range ch`. Покажи, что цикл завершается при закрытии.

27. **Неблокирующее чтение (`default` в `select`)**: Напишите программу, которая пытается прочитать сообщение из канала. Если сообщения в канале прямо сейчас нет, программа не должна блокироваться и ждать — она должна мгновенно вывести сообщение `"Данных нет"` и продолжить работу. Реализуйте это с помощью ветки `default` внутри `select`.

---

28. **Закрытие канала (`close`)**: Запиши в канал 3 числа. Вызови `close(ch)`. Считай из него 4 числа. Что вернет 4-е чтение? (Спойлер: zero value, т.е. `0`).

29. Напиши **"done channel" pattern**: функция `LongRunning(done <-chan struct{})` читает из `done` в `select`. При получении сигнала — выходит. Покажи graceful cancellation через закрытие done-канала.

30. Напиши **"quit channel"** с обратной связью: горутина получает сигнал на остановку, делает cleanup и пишет в `quit` канал подтверждение. `main` ждёт подтверждения через `select`.

31. **Идиома `comma ok` в каналах**: Считай данные как `val, ok := <-ch`. Выведи `ok` для открытого и для закрытого канала, из которого уже всё прочитали.

32. Реализуйте `filter(in <-chan int, fn func(int) bool) <-chan int`, которая пропускает только удовлетворяющие условию числа.

33. Реализуй **bounded parallelism** через канал-семафор: `semaphore := make(chan struct{}, 3)`. Каждая горутина захватывает слот (`semaphore <- struct{}{}`), выполняет работу, освобождает (`<-semaphore`). Ограничь одновременные горутины.

34. **Итерация по каналу через `range`**: Создайте канал, запишите туда несколько строк в фоновой горутине, а затем закройте канал. В основной горутине обойдите канал с помощью цикла `for val := range ch`. Убедитесь, что цикл автоматически завершается, когда канал закрыт. Что произойдет, если забыть закрыть канал в фоновой горутине?

35. Напиши программу, демонстрирующую **"channel direction"**: функция принимает `send chan<- int` (только для записи) и `receive <-chan int` (только для чтения). Попробуй записать в `receive` — получи ошибку компиляции.

---

36. Прочитай данные из закрытого канала. Убедись, что данные читаются до конца, а затем канал возвращает нулевое значение.

37. Закройте канал с помощью `close(ch)`. Попытайтесь отправить данные в закрытый канал (поймайте `panic: send on closed channel`).

38. Напиши программу с `select` между двумя каналами: `ch1` и `ch2`. Одна горутина пишет в `ch1` через 100ms, другая в `ch2` через 200ms. В `main` читай в цикле `for` с `select` и печатай, откуда пришло значение. Покажи, что `select` выбирает **готовый** канал.

39. Создайте буферизованный канал (`make(chan int, 3)`). Отправьте туда 3 значения без блокировки. Что будет при попытке отправить 4-е значение?

40. Попробуйте отправить значение в канал в `main` без запуска горутины для чтения. Убедитесь, что это вызывает моментальный Deadlock.

41. Покажи **случайный выбор** в `select`: запусти 10 горутин, каждая пишет в свой канал. В `select` с 10 case'ами — покажи, что при одновременной готовности выбор **равновероятен** (random uniform).

42. Используй идиому `val, ok := <-ch` для определения, закрыт ли канал (`ok` будет `false`).

43. **Двойное закрытие**: Попробуй закрыть уже закрытый канал. Снова паника.

44. Напиши **"non-blocking select"** с `default`: проверь, есть ли данные в канале, не блокируясь. Если нет — сделай что-то другое (например, `fmt.Println("no data")`).

45. Добавьте `time.After` в `select`, чтобы выйти из цикла, если ничего не пришло за 2 секунды.

46. Используй цикл `for val := range ch` для чтения данных из канала до его закрытия.

47. Напиши **"heartbeat"** pattern: горутина каждые 500ms пишет `"ping"` в канал. В `main` читай с `select` и таймаутом 1s. Если нет ping'a в течение 1s — считай, что горутина зависла.

48. **Каверзный случай: Забыл закрыть**: Убери `close(ch)` из упр. 272. Посмотри, как `for range` ловит дедлок, ожидая новых данных вечно.

49. Напиши программу с `select` в цикле, где один case — чтение из `done` канала (завершение), другой — основная работа. При закрытии `done` — корректно выйди из цикла.

50. Прочитайте данные из закрытого канала. Какое значение вернется? Как отличить "канал закрыт" от "в канале лежит zero-value"?

51. Неблокирующая отправка/чтение с `default`: попробуйте отправить в канал, если он заполнен — выведите «channel full» и пропустите.

52. **Направленные каналы**: Напиши функцию `Producer(ch chan<- int)` (только на запись) и `Consumer(ch <-chan int)` (только на чтение). Передай им обычный двунаправленный канал из `main`.

53. Напиши **"priority select"** через `for` с `select` и `default`: сначала проверяй высокоприоритетный канал (с `case`), если он не готов — проверяй низкоприоритетный через `default` и вложенный `select`. Обсуди, почему в Go нет встроенного приоритета в `select`.

54. Создай два канала. Две горутины отправляют в них данные с разной задержкой (`time.Sleep`). Используй `select` для чтения из того канала, который пришел первым (выполняется несколько раз).

55. Используйте идиому `val, ok := <-ch` для безопасного чтения из канала с проверкой его статуса.

56. **[Каверзный кейс]**: Попробуй прочитать данные из `nil`-канала (`var ch chan int; <-ch`). Поймай дедлок. Затем попробуй записать в `nil`-канал. Изучи это поведение.

57. Напиши **"fan-in"** через `select` в одной горутине: два входных канала мержатся в один выходной. Используй `select` с двумя case'ами в цикле. Закрой выходной, когда оба входных закрыты (используй флаги или `sync.WaitGroup`).

58. **Nil-каналы**: Объяви `var ch chan int` без `make`. Попробуй написать в него. Затем попробуй прочитать из него. Узнай, что операции с nil-каналом блокируются **навсегда**.

59. Утечка горутин: горутина блокируется на отправке в канал, который никто не читает. Исправьте с помощью `select` с таймаутом или буфера.

60. Напиши программу с `select` и **закрытым каналом**: один из case'ов читает из закрытого канала. Покажи, что закрытый канал **всегда готов** для чтения (возвращает zero value). Это может привести к "busy loop" если не обрабатывать `ok`.

61. Напиши функцию-генератор, которая возвращает канал и горутину, записывающую в него N чисел и закрывающую канал.

62. Напиши **"rate limiter"** через `select` + `time.Ticker`: ограничь обработку до 5 запросов в секунду. Запросы поступают в канал, `select` ждёт либо запрос, либо тика таймера.

63. **Сигнальный канал**: Создай `done := make(chan struct{})`. Горутина-воркер делает долгую работу, а по завершении делает `done <- struct{}{}`. В `main` жди этого сигнала `<-done`. Пустая структура не занимает памяти!

64. Напиши **"timeout pattern"** с `select`: читай из канала `ch`, но если данных нет в течение 2 секунд — выведи `"timeout"` и выйди. Используй `time.After(2 * time.Second)`.

65. Создайте направленные каналы (send-only `<-chan int` и receive-only `chan<- int`) и попробуйте использовать их не по назначению. Изучите ошибки компилятора.

66. Напиши **"debounce"** через `select` + `time.After`: при поступлении события запускай таймер. Если за время таймера пришло новое событие — сбрасывай старый таймер и запускай новый. Обработай только последнее событие в серии.

67. Мультиплексор: функция `fanIn(ch1, ch2 <-chan int) <-chan int`, сливающая два канала в один. Используйте `select` в цикле.

68. Передай канал в функцию и закрой его *получателе* (плохая практика, но важно понимать, почему так делать не стоит — поймай панику, если отправитель попытается отправить после).

69. Напиши **"throttle"**: пропускай события не чаще, чем раз в N миллисекунд. Если событие пришло раньше — игнорируй (или буферизуй одно). Используй `select` с `time.After`.

70. Напишите функцию, которая принимает канал для записи, генерирует числа и закрывает канал по завершении.

71. Напиши **"graceful shutdown"**: `select` между рабочим каналом и `os.Signal` каналом (`signal.Notify`). При получении `os.Interrupt` — закрой рабочий канал, дождись `sync.WaitGroup`, выведи `"shutting down gracefully"`.

72. Передайте канал в качестве аргумента функции для возврата результата (паттерн "Futures" / "Promises").

73. Напиши программу с `select` и **nil каналом**: динамически "выключай" case, присваивая каналу `nil`. Покажи, что case с nil каналом **никогда не выбирается** (это способ временно отключить case).

74. Тикер: каждые 500 мс отправлять в канал `time.Tick`, основная горутина читает и выводит «tick», пока не истечёт таймаут.

75. **`select` в цикле**: Помести `select` в бесконечный `for { select { ... } }`, чтобы читать из нескольких каналов непрерывно.

76. Напиши **"round-robin"** через `select`: три канала-источника. Используй `select` в цикле, но после чтения из канала A — временно "отключай" его (nil) на одну итерацию, чтобы дать возможность B и C. Обсуди, почему это сложно сделать чисто через select.

---

77. Сравните скорость передачи данных через канал и через глобальную переменную с `sync.Mutex`.

78. Используй `default` в `select`, чтобы сделать неблокирующее чтение из канала (если данных нет, select сразу переходит к `default`).

79. **Non-blocking чтение**: В `select` добавь ветку `default`. Убедись, что если ни один канал не готов, мгновенно срабатывает `default`, и программа не блокируется.

80. Используйте `select` для ожидания данных из двух разных каналов одновременно.

81. **Таймаут через `select`**: Добавь в `select` case: `case <-time.After(2 * time.Second):`. Убедись, что если данные не приходят 2 секунды, срабатывает этот таймаут.

82. Добавьте `default` в `select`, чтобы реализовать неблокирующее чтение/запись (non-blocking channel operations).

83. **Каверзный `time.After` в цикле**: Напиши `for` с `select`, внутри которого есть `case <-time.After(...)`. Изучи (в теории или профилировщиком), почему это вызывает **утечку памяти** (на каждой итерации создается новый таймер, который висит в памяти до срабатывания). Вынеси `time.NewTimer` перед циклом.

---

84. **[Каверзный кейс]**: Создай бесконечный цикл с `select`, который читает из двух каналов: `data` и `quit`. При получении сигнала в `quit`, выходи из цикла (используй `return` или `break` с меткой).

85. Реализуйте таймаут операции: используйте `select` с каналом данных и каналом из `time.After()`.

86. Используй `time.Tick` для создания канала, который отправляет время каждую секунду. Выводи "tick" в консоль. Останови через 5 секунд.

87. Создайте "тикущий" канал с помощью `time.Tick` и выведите текущее время каждые 500мс, прервав цикл через `time.After`.

88. **Каверзный случай**: Что произойдет, если в `select` указать несколько кейсов, которые готовы к выполнению одновременно? (Ответ: выбор случайный). Проверьте на практике.

89. Напиши функцию, которая эмулирует долгий запрос к БД (sleep 2s) и возвращает результат через канал. В `main` используй `select`, чтобы дождаться либо результата, либо таймаута в 1 секунду (выведи "Request timed out").

90. **Каверзный случай**: Что произойдет, если использовать `nil`-канал (неинициализированный `var ch chan int`) в `select`? (Он блокируется навсегда). Используйте это знание, чтобы отключить ветку `select` динамически (обнулив канал).

91. **[Каверзный кейс]**: Если в `select` готовы сразу несколько `case`, какой выберет Go? Напиши программу, которая доказывает, что выбор случаен (random).

92. **Каверзный случай**: Что произойдет, если попытаться закрыть `nil`-канал? (Паника).

93. Используй `select` для отправки данных: если канал забит (буфер заполнен), срабатывает `default` (отбрасывание данных / drop pattern).

94. Напишите функцию `or(done <-chan interface{}, channels ...<-chan interface{}) <-chan interface{}`, которая объединяет несколько каналов "сигналов завершения" в один (закрытие любого из них закрывает результирующий).

95. Реализуйте паттерн "Done channel" (`done := make(chan struct{}); close(done)`), чтобы сигнализировать горутинам о необходимости завершения работы (Cancellation).
