# Реляционные базы данных (SQL и PostgreSQL)

В этом файле собраны уникальные упражнения по теме **"Реляционные базы данных (SQL и PostgreSQL)"**, отсортированные по возрастанию сложности.

---

1. **Подключение к PostgreSQL**: Инициализируйте новый Go-модуль. Подключите драйвер PostgreSQL (например, `github.com/jackc/pgx/v5/stdlib` для интеграции с `database/sql` или традиционный `github.com/lib/pq`). Напишите код подключения к БД через `sql.Open` и выполните проверку связи с помощью `db.Ping()`. Обеспечьте закрытие пула соединений через `defer db.Close()`.

2. **Подключение (Ping)**: Установи драйвер `github.com/lib/pq`. Напиши код, который подключается к локальной базе PostgreSQL через `sql.Open("postgres", "postgres://user:pass@localhost:5432/db?sslmode=disable")`. Обязательно вызови `db.Ping()`, чтобы убедиться, что соединение реально установлено (сам `Open` не стучится в базу!).

3. Установи PostgreSQL (локально или через Docker). Создай базу `testdb`, таблицу `users (id SERIAL PRIMARY KEY, name TEXT, email TEXT, created_at TIMESTAMP)`. Подключись через `sql.Open("postgres", dsn)` (`github.com/lib/pq`) и проверь соединение `db.Ping()`.

4. Установи драйвер `github.com/jackc/pgx/v5` (и пакет `github.com/jackc/pgx/v5/stdlib` для работы через стандартный `database/sql`). Создай строку подключения (DSN - Data Source Name) и установи соединение с базой `testdb`.

5. Установите драйвер `pgx` (или `lib/pq`), подключитесь к локальному PostgreSQL с использованием `database/sql`. Проверьте соединение `db.Ping()`.

6. Напиши функцию `CreateUser(name, email string) error`, использующую `db.Exec("INSERT INTO users(name, email) VALUES($1, $2)", name, email)`. Проверь `sql.Result` — `LastInsertId()` (не работает в PostgreSQL!) и `RowsAffected()`.

7. Используйте `db.PingContext(ctx)`, чтобы проверить реальную доступность базы данных.

8. Выполни `db.PingContext(ctx)`, чтобы проверить, что соединение с БД действительно установлено. Обработай ошибку.

9. Изучите `sql.DB` как пул соединений. Настройте `SetMaxOpenConns`, `SetMaxIdleConns` и `SetConnMaxLifetime`. Объясните, почему `ConnMaxLifetime` критически важен для балансировщиков нагрузки и корректного сброса состояния сессий.

10. **Создание схемы данных**: Напишите функцию, которая при старте приложения создает таблицу `users` в PostgreSQL с помощью `db.Exec`. Таблица должна содержать поля: `id` (SERIAL PRIMARY KEY), `name` (VARCHAR), `email` (VARCHAR UNIQUE), `age` (INT), `bio` (TEXT) и `created_at` (TIMESTAMP).

11. Настройте DSN (Data Source Name) или `pgx.ConnConfig` с использованием переменных окружения (не хардкодьте пароли в коде!).

12. Покажи **утечку соединений**, если забыть `rows.Close()`. Запусти 10000 запросов в цикле без `defer rows.Close()` — получи `"sorry, too many clients already"` от PostgreSQL. Исправь.

13. Вставьте одного пользователя с параметризованным запросом (`$1`, `$2`), избегая SQL-инъекций.

14. **Защита от SQL-инъекций (База)**: Попробуй вставить данные, конкатенируя строку (например, `INSERT INTO users (name) VALUES ('" + name + "')`). Пойми, насколько это опасно. Перепиши запрос с использованием плейсхолдеров: `INSERT INTO users ... VALUES ($1, $2)`.

15. Напишите функцию, которая выполняет простейший `SELECT 1` и возвращает результат, чтобы убедиться в работоспособности коннекта.

16. Напиши функцию `UpdateUserEmail(id int, email string) error`. Используй `db.Exec`. Проверь `RowsAffected()` — если 0, верни `sql.ErrNoRows` или кастомную ошибку `"user not found"`.

17. Создайте таблицу `users` (id, email, created_at) с помощью `db.ExecContext`. Изучите разницу между `Exec` (для INSERT/UPDATE/DELETE) и `Query` (для SELECT).

18. Напишите функцию `ListUsers`, которая использует `QueryContext` и итерируется по `rows.Next()`. **Важно:** всегда вызывайте `rows.Close()` (лучше через `defer`) и проверяйте `rows.Err()` после цикла!

19. Напиши функцию `GetUserByID(id int) (*User, error)`, использующую `db.QueryRow("SELECT id, name, email, created_at FROM users WHERE id = $1", id).Scan(&user.ID, &user.Name, &user.Email, &user.CreatedAt)`. Обработай `sql.ErrNoRows`.

20. Вставь новую запись в таблицу `users` используя `db.ExecContext`. Обрати внимание, как экранировать переменные (плейсхолдеры `$1, $2` в PostgreSQL, а не `?`).

21. Вставьте несколько пользователей транзакционно: если email дублируется, откатите всю группу.

22. **Возврат ID (RETURNING)**: Вставь нового пользователя. Поскольку `id` генерируется базой, используй `db.QueryRow("INSERT ... RETURNING id").Scan(&id)`, чтобы сразу получить ID созданной записи.

23. **Безопасная вставка (INSERT) с плейсхолдерами**: Напишите функцию добавления нового пользователя в базу данных. Передайте имя, email и возраст в качестве аргументов. Используйте плейсхолдеры `$1`, `$2`, `$3` вместо конкатенации строк, чтобы предотвратить угрозу SQL-инъекций [308].

24. Напиши функцию `CreateUsersBulk(users []User) error`, использующую **подготовленный запрос** (`db.Prepare`) и цикл `stmt.Exec(...)`. Покажи, что prepare один раз + exec много раз быстрее, чем `db.Exec` в цикле.

25. Перепиши bulk insert через **один запрос с множественными VALUES**: `INSERT INTO users(name, email) VALUES ($1, $2), ($3, $4), ...`. Сгенерируй SQL динамически. Сравни производительность с prepare + цикл.

26. Напишите функцию `GetUserByID`, которая использует `QueryRowContext` и сканирует результат в структуру. Обработайте ошибку `sql.ErrNoRows`, чтобы возвращать кастомную `ErrUserNotFound`.

27. **[Каверзный кейс]**: Извлеки `id` только что созданной записи. Вместо `ExecContext` используй `db.QueryRowContext` с запросом `INSERT INTO ... RETURNING id`. Отскань результат в переменную.

28. Извлеките всех пользователей в слайс структур, используя `rows.Scan`. Выведите их в консоль.

29. **Получение автоинкрементного ID (`RETURNING`)**: Измените ваш INSERT-запрос так, чтобы PostgreSQL автоматически возвращал ID только что созданной записи с помощью конструкции `RETURNING id`. Считайте этот ID в переменную с помощью метода `db.QueryRow().Scan(&insertedID)`.

30. **Чтение одной записи**: Запроси пользователя по ID через `db.QueryRow`. Считай данные в переменные через `Scan(&id, &name, &age)`.

31. Напиши функцию `SearchUsers(namePattern string) ([]User, error)` с `ILIKE` (case-insensitive like): `WHERE name ILIKE $1`. Передай `"%" + namePattern + "%"`. Обработай SQL-инъекцию — покажи, что placeholder'ы (`$1`) защищают.

32. Попробуй **SQL-инъекцию** намеренно: сконструируй запрос через `fmt.Sprintf("SELECT * FROM users WHERE name = '%s'", userInput)` с входом `"' OR '1'='1"`. Покажи, что это ломает безопасность. Исправь на placeholder'ы.

33. Выбери одного пользователя по `id` с помощью `QueryRowContext`. Используй метод `Scan` для чтения колонок в переменные.

34. Напиши функцию `GetUsersPaginated(page, pageSize int) ([]User, int, error)`. Используй `LIMIT $1 OFFSET $2` для пагинации. Верни общее количество через `SELECT COUNT(*)`. Обработай `pageSize > 100` — ограничь максимум.

35. **Каверзный случай: sql.ErrNoRows**: Запроси из упр. 370 несуществующий ID. Убедись, что `Scan` возвращает ошибку. Сравни её строго: `if errors.Is(err, sql.ErrNoRows) { fmt.Println("Юзер не найден") }`.

36. Реализуйте `DELETE` (soft delete через флаг `is_deleted` и hard delete).

37. Напиши функцию `GetUsersSorted(sortBy, order string) ([]User, error)`. Валидируй `sortBy` против whitelist (`"name"`, `"email"`, `"created_at"`), иначе верни ошибку. Не подставляй `sortBy` напрямую в SQL без валидации!

38. **SQL Injection**: Намеренно составьте запрос через конкатенацию строк (`"SELECT * FROM users WHERE id = " + id`). "Взломайте" свою БД, передав `1 OR 1=1`. Затем перепишите запрос с использованием плейсхолдеров (`$1`, `$2`), чтобы сделать его безопасным.

39. Реализуйте `UPDATE` и проверьте количество затронутых строк через `Result.RowsAffected()`. Что вернет `RowsAffected`, если запись не была найдена?

40. Обновите email пользователя по ID. Если пользователь не найден, верните ошибку.

41. Напиши функцию `GetUserStats() (map[string]int, error)`, использующую `COALESCE` и `GROUP BY`: `SELECT DATE(created_at), COUNT(*) FROM users GROUP BY DATE(created_at)`. Сканируй в `map[time.Time]int`.

---

42. **Чтение множества записей**: Используй `db.Query("SELECT ...")` для получения всех юзеров. В цикле `for rows.Next() { ... }` считай их в срез структур `[]User`. Обязательно добавь `defer rows.Close()` сразу после `Query`.

43. Используйте `db.BeginTx` для начала транзакции. Выполните перевод средств между двумя счетами (debit/credit). Сделайте `Commit` при успехе и `Rollback` (через `defer`) при ошибке.

44. **Получение списка записей (`Query`)**: Напишите функцию получения всех пользователей, чей возраст превышает заданный лимит.
    * Используйте метод `db.Query`.
    * Напишите цикл `for rows.Next() { ... }` для обхода результатов.
    * Сделайте `rows.Scan` для распаковки данных в структуру `User`.
    * Обработайте возможную ошибку после цикла с помощью `rows.Err()`.
    * Обязательно вызовите `rows.Close()` через `defer` сразу после открытия запроса.

45. Напиши функцию `TransferMoney(fromID, toID, amount int) error`. Используй `db.Begin()` для начала транзакции. Внутри: `UPDATE accounts SET balance = balance - $1 WHERE id = $2`, затем `UPDATE ... + $1`. `tx.Commit()` при успехе, `tx.Rollback()` при ошибке.

46. Изучите уровни изоляции транзакций в PostgreSQL (`ReadCommitted`, `RepeatableRead`, `Serializable`). Настройте `sql.TxOptions` и воспроизведите аномалию "Phantom Read".

47. Покажи **"lost update"**: два параллельных `TransferMoney` без транзакции/блокировки. Баланс "теряется". Запусти 100 параллельных переводов — итоговый баланс ≠ начальному. Исправь через транзакцию + `SELECT FOR UPDATE`.

48. **[Каверзный кейс]**: Напиши `QueryContext`, но забудь вызвать `rows.Close()`. Запусти код в цикле 1000 раз. Используя `runtime.NumGoroutine()`, покажи, как утекает память и горутины (исчерпание пула соединений). Исправь код.

49. **Скрытая ловушка `rows.Err()`**: После цикла `rows.Next()` добавь проверку `if err := rows.Err(); err != nil`. Узнай из документации, почему цикл мог прерваться досрочно (например, из-за обрыва сети), и почему эта проверка **обязательна**.

50. Модифицируй `TransferMoney`: добавь `SELECT balance FROM accounts WHERE id = $1 FOR UPDATE` в начале транзакции. Покажи, что "lost update" исчез. Объясни уровень изоляции `READ COMMITTED` + row-level locks.

51. Используйте Savepoints внутри транзакции, чтобы откатить только часть операций при ошибке, не прерывая всю транзакцию.

52. Напиши функцию `CreateOrderWithItems(order Order, items []OrderItem) error`. Используй транзакцию: сначала `INSERT INTO orders`, получи `order_id` через `RETURNING id` + `Scan`, затем `INSERT INTO order_items` для каждого item. Всё или ничего.

53. Обработай ошибку `sql.ErrNoRows` при поиске пользователя, которого нет в базе. Верни кастомную ошибку `ErrUserNotFound`.

54. Используйте `pgxpool` для управления пулом соединений, настройте максимальное количество соединений.

55. **Bulk Insert**: Вставьте 10 000 строк. Сравните скорость выполнения 10 000 отдельных `INSERT`-ов и одного множественного `INSERT INTO ... VALUES (...), (...), ...`.

56. **Обработка значений NULL**: В базе данных поле `bio` у некоторых пользователей может быть не заполнено (`NULL`). Попробуйте считать такую запись в обычную строковую переменную `var bio string`. Посмотрите на ошибку сканирования. Решите проблему двумя способами:
    * Использованием типа `sql.NullString` в структуре.
    * Считыванием значения в указатель на строку `*string`.

57. **NULL значения**: Добавь в базу юзера, у которого `age = NULL`. Попытайся считать его в обычный `int` и получи панику/ошибку. Исправь это, считывая значение в специальный тип `sql.NullInt64` (или в указатель `*int`).

58. Напиши **"savepoint"** в транзакции: `tx.Exec("SAVEPOINT sp1")`. Выполни рискованную операцию. При ошибке — `tx.Exec("ROLLBACK TO SAVEPOINT sp1")` вместо полного `Rollback()`. Продолжи транзакцию.

59. Используйте `db.PrepareContext` для создания Prepared Statements. Поймите, как это снижает нагрузку на парсер БД при частом выполнении одного и того же запроса.

60. Напиши функцию `WithTransaction(ctx context.Context, db *sql.DB, fn func(*sql.Tx) error) error`. Оборачивай логику в транзакцию: `Begin`, `defer` с `Rollback` (если не committed), `Commit`. Покажи паттерн "unit of work".

61. Настрой пул соединений: установи `db.SetMaxOpenConns(10)`, `db.SetMaxIdleConns(5)`, `db.SetConnMaxLifetime(time.Hour)`. Объясни, почему дефолтные значения (бесконечные коннекты) могут быть вредны для БД.

62. Выполните batch-вставку 1000 пользователей с помощью `pgx.Batch` и COPY-протокола.

63. Напишите сложный запрос с использованием CTE (Common Table Expressions, `WITH ... AS`) и Window Functions (`ROW_NUMBER() OVER(PARTITION BY ...)`).

64. **Транзакции (ACID)**: Начни транзакцию через `tx, err := db.Begin()`. Выполни два запроса: спиши деньги у одного юзера и добавь другому. Если на любом этапе ошибка — `tx.Rollback()`. Если всё ок — `tx.Commit()`.

65. Покажи **"transaction rollback on panic"**: внутри `WithTransaction` используй `recover()` в `defer` для `Rollback()` при панике. Логируй панику и перевыбрасывай.

66. Реализуйте постраничное чтение огромной таблицы (миллионы строк) с помощью серверных курсоров PostgreSQL (`DECLARE cursor`, `FETCH`), чтобы не загружать всю выборку в память Go-приложения.

67. **Утечка соединений (Connection Leak)**: Напишите функцию, которая делает запрос `db.Query`, но намеренно не закрывает `rows` через `Close()`. Запустите эту функцию в цикле 100 раз подряд. Обратите внимание, как после нескольких итераций программа зависает, так как пул соединений исчерпан. Напишите комментарий, как этого избегать.

68. Напиши **"optimistic locking"**: таблица `products (id, name, price, version INTEGER DEFAULT 1)`. При обновлении: `UPDATE products SET price = $1, version = version + 1 WHERE id = $2 AND version = $3`. Если `RowsAffected() == 0` — верни ошибку `"concurrent modification detected"`.

---

69. Удали пользователя по `id` (`DELETE FROM users WHERE id = $1`). Проверь, сколько строк было удалено, используя метод `result.RowsAffected()`.

70. Реализуйте постраничную пагинацию (LIMIT/OFFSET) и курсорную пагинацию (по ID или created_at).

71. **UUID**: Используйте тип `uuid` (через `github.com/google/uuid`) в качестве первичного ключа вместо `serial`.

72. Настрой пул соединений: `db.SetMaxOpenConns(25)`, `db.SetMaxIdleConns(25)`, `db.SetConnMaxLifetime(5 * time.Minute)`. Объясни каждый параметр. Покажи, что без настройки пула — дефолт `MaxOpenConns = 0` (unlimited) может исчерпать ресурсы PostgreSQL.

73. **Транзакции и Defer**: Улучши код из упр. 375. Добавь `defer tx.Rollback()` сразу после `Begin`. (Если транзакция уже была закоммичена, Rollback безопасно проигнорируется, а вот в случае паники — спасет базу от зависших блокировок).

74. Напиши программу, которая запускает 1000 горутин, каждая делает `db.QueryRow("SELECT pg_sleep(0.1)")`. С `MaxOpenConns = 10` — покажи, что запросы выполняются пачками по 10. С `MaxOpenConns = 0` — PostgreSQL может отказать.

75. **JSONB**: Создайте колонку типа `jsonb`. Сохраните туда сложную JSON-структуру. Напишите SQL-запрос, который ищет пользователей по вложенному полю (`metadata->>'role' = 'admin'`).

76. Добавьте полнотекстовый поиск: создайте столбец `tsv TSVECTOR`, триггер для автообновления и выполните поиск с `ts_query`.

77. Начни транзакцию с помощью `db.BeginTx(ctx, nil)`. Внутри транзакции выполни два `INSERT`. Сделай `tx.Commit()` в конце.

78. Напиши **"connection health check"**: `db.PingContext(ctx)` с таймаутом. Используй в readiness probe HTTP endpoint'а. Покажи, что `Ping` проверяет не только TCP, но и способность выполнять запросы.

79. **Arrays**: Сохраните слайс строк (теги) в колонку типа `text[]`. Используйте пакет `github.com/lib/pq` и функцию `pq.Array()` для корректного сканирования массива из БД в Go-слайс.

80. **Таймаут запроса (Контекст)**: Используй `db.QueryContext(ctx, "SELECT pg_sleep(2)")`. Передай контекст с таймаутом в 1 секунду. Убедись, что запрос прерывается на стороне Go и возвращает ошибку таймаута.

81. **Настройка пула соединений (Connection Pool Configuration)**: Настройте параметры пула соединений вашего объекта `db`:
    * Максимальное количество открытых соединений (`SetMaxOpenConns`).
    * Максимальное количество простаивающих соединений (`SetMaxIdleConns`).
    * Максимальное время жизни соединения (`SetConnMaxLifetime`).
    Опишите в комментариях, как эти настройки помогают балансировать нагрузку на СУБД.

82. Напиши программу с **"connection leak simulation"**: открываешь `sql.Rows`, не закрываешь, итерируешь не до конца (`break` после первой строки). Покажи, что соединение остаётся занятым. Исправь: `defer rows.Close()` + дочитывай до конца или явно закрой.

---

83. **TimestampTZ**: Работайте с типом `timestamptz`. Убедитесь, что Go-структура использует `time.Time`, и поймите, как Postgres и Go обрабатывают часовые пояса (всегда сохраняйте в UTC!).

84. **[Каверзный кейс]**: Внутри транзакции из предыдущего задания сымитируй ошибку (например, деление на ноль или неверный SQL) после первого `INSERT`. Используй `defer tx.Rollback()` сразу после `BeginTx`. Убедись, что если произошла ошибка, транзакция откатывается, а если успех — `Rollback` возвращает `sql.ErrTxDone` (которую можно проигнорировать).

85. Используйте `sqlx` для упрощённого сканирования структур: `Select`, `Get`, `NamedExec`.

86. Перепиши подключение на `github.com/jackc/pgx/v5/pgxpool`. Создай `pgxpool.New(ctx, connString)`. Покажи, что pgxpool управляет пулом соединений автоматически.

87. **NULL значения**: Создайте колонку, которая может быть `NULL`. Попробуйте просканировать её в обычный `string` и получите панику. Используйте `sql.NullString` или `sql.NullInt64`.

88. **Пул соединений (Connection Pool)**: Настрой пул "под капотом" стандартной библиотеки: задай `db.SetMaxOpenConns(20)` (макс. соединений), `db.SetMaxIdleConns(5)` (кол-во спящих) и `db.SetConnMaxLifetime(time.Minute * 5)`.

89. Используй `pgx` для **batch запросов**: `batch := &pgx.Batch{}`, `batch.Queue("INSERT INTO users(name) VALUES($1)", "Alice")`, `batch.Queue(...)`. `br := pool.SendBatch(ctx, batch)`, затем `br.Exec()`. Покажи, что batch — один round-trip.

90. **Кастомные Null-типы**: Реализуйте собственные типы (например, `NullTime`), которые реализуют интерфейсы `sql.Scanner` и `driver.Valuer` для более удобной работы с `null` в JSON-сериализации (чтобы `null` отдавался как `null`, а не как `{"Time": "...", "Valid": true}`).

91. **Транзакции (Transactions)**: Напишите симуляцию банковского перевода. У вас есть таблица `accounts` с балансами. Начните транзакцию с помощью `db.BeginTx(ctx, nil)`. Сделайте списание с одного счета и начисление на другой. Если при каком-то из действий возникла ошибка, вызовите `tx.Rollback()`. Если всё прошло успешно, примените изменения через `tx.Commit()`.

92. Используй `pgx` для **copy from**: `copyCount, err := pool.CopyFrom(ctx, pgx.Identifier{"users"}, []string{"name", "email"}, pgx.CopyFromSlice(len(users), func(i int) ([]any, error) { return []any{users[i].Name, users[i].Email}, nil }))`. Это самый быстрый способ bulk insert.

93. Реализуй перевод денег между двумя счетами (таблица `accounts` с полем `balance`). В одной транзакции уменьши баланс одного счета и увеличь баланс другого. Если на первом счете не хватает денег — откатывай транзакцию (`Rollback`).

94. Обработайте NULL-поля: добавьте поле `bio TEXT` с возможным NULL, используйте `sql.NullString` или указатели.

95. **Ускорение с sqlx**: Установи `github.com/jmoiron/sqlx`. Перепиши упр. 372 (чтение множества записей), используя `db.Select(&users, "SELECT ...")`. Насладись тем, как `sqlx` сам матчит колонки БД с тегами `db:"..."` в твоей структуре без ручного `Scan`.

96. **Enums**: Создайте кастомный тип ENUM в Postgres и мапьте его на строки или `iota`-константы в Go.

97. Используй `pgx` для **LISTEN/NOTIFY**: `conn.Exec(ctx, "LISTEN channel_name")`. В горутине читай `conn.WaitForNotification(ctx)` — получишь push-уведомления от PostgreSQL. Напиши простой pub/sub через БД.

98. **pgx напрямую**: Откажитесь от `database/sql` и используйте `github.com/jackc/pgx/v5` напрямую. Используйте `pgxpool` для управления пулом. Оцените прирост производительности.

99. Используй `pgx` с **кодеками для кастомных типов**: создай тип `Status` (enum в PostgreSQL: `'active', 'inactive', 'banned'`). Реализуй `EncodeText`/`DecodeText` для `pgx`. Сканируй напрямую в `Status`.

100. Создай Prepared Statement (`db.PrepareContext`) для частого запроса `SELECT name FROM users WHERE id = $1`. Вызови его несколько раз в цикле с разными `id`.

101. Установите `golang-migrate`, создайте первую миграцию, добавляющую таблицу `posts`.

102. **Миграции**: Установи CLI-утилиту `golang-migrate/migrate`. Создай файлы `up.sql` (создание таблицы) и `down.sql` (дроп таблицы). Напиши скрипт или команду для накатки и отката миграций. Никогда не создавай таблицы через `db.Exec` в продакшен-коде!

---

103. Используй `pgx` с **array типами**: PostgreSQL `TEXT[]`. Сканируй в `[]string` через `pgtype.Array[string]`. Или вставляй `[]string` напрямую — pgx конвертирует автоматически.

104. **pgx.CopyFrom**: Реализуйте массовую загрузку данных (миллион строк) с помощью протокола `COPY`. Это самый быстрый способ вставки данных в Postgres из Go.

105. **Подготовленные запросы (Prepared Statements)**: Напишите код, который выполняет 1000 вставок записей в цикле. Вместо вызова `db.Exec` на каждой итерации, подготовьте шаблон запроса один раз с помощью `db.Prepare()`, а затем в цикле вызывайте метод `stmt.Exec()`. Сравните время выполнения запросов с подготовкой и без неё.

106. Используй `pgx` с **JSONB**: колонка `data JSONB`. Сканируй в `map[string]any` или структуру с `json` тегами. Вставляй структуру — pgx сериализует в JSON.

107. **sqlx**: Подключите `github.com/jmoiron/sqlx`. Используйте теги ``db:"column_name"`` в структурах. Замените ручной `rows.Scan` на `sqlx.Select` и `sqlx.Get`.

108. **[Каверзный кейс]**: Используй `pgx.Conn` напрямую (без `database/sql`), чтобы выполнить батч-вставку (Batch). Вставь 1000 записей за один сетевой обмен с БД вместо 1000 отдельных `INSERT`. Оцени разницу в скорости.

109. Напишите миграцию, добавляющую столбец `updated_at` с автоматическим обновлением.

110. Напиши **"upsert"** через `ON CONFLICT`: `INSERT INTO users(email, name) VALUES($1, $2) ON CONFLICT(email) DO UPDATE SET name = EXCLUDED.name`. Верни `RowsAffected` и определи: insert или update произошёл.

---

111. **Нативный pgx**: Установи `github.com/jackc/pgx/v5`. Подключись к базе через `pgxpool.New(ctx, connString)`. Обрати внимание, что `pgx` использует контекст везде, с самого первого вызова.

112. **sqlx.In**: Решите проблему динамического `IN (...)` запроса, используя `sqlx.In` для автоматического расширения плейсхолдеров.

113. Используй `github.com/Masterminds/squirrel` для построения SQL: `sq.Select("id", "name").From("users").Where(sq.Eq{"id": 1}).ToSql()`. Покажи, что builder предотвращает SQL-инъекции и делает код читаемым.

114. **[Высокая сложность]**: Реализуй уровень изоляции транзакций. Создай транзакцию с `sql.LevelSerializable`. Запусти две горутины, которые одновременно пытаются изменить баланс одного счета. Поймай ошибку сериализации (serialization failure) и реализуй логику повторной попытки (retry).

115. Откатите миграцию, проверьте, что изменения исчезли, затем снова накатите.

116. **sqlx.NamedExec**: Используйте именованные плейсхолдеры (`:name`, `:email`) вместо `$1`, `$2` для улучшения читаемости больших `INSERT`-ов.

117. **Хранение сложных объектов (JSONB)**: Создайте таблицу `orders` с полем `metadata` типа `JSONB`. Напишите структуру `OrderMetadata` в Go (с полями `Tags []string`, `DiscountCode string`). Напишите код записи этой структуры в БД (сериализовав её в срез байт JSON) и обратного считывания из базы напрямую в структуру.

118. Напиши динамический WHERE через squirrel: `q := sq.Select("*").From("users")`. Если `name != ""` — `q = q.Where(sq.ILike{"name": "%" + name + "%"}`). Если `minAge > 0` — `q = q.Where(sq.GtOrEq{"age": minAge})`. Собери запрос.

119. **Работа с JSONB**: Создай в базе колонку типа `JSONB`. В Go создай вложенную структуру. При вставке передай её напрямую (pgx сам сериализует её в JSON, если правильно настроить, или используй ручной Marshal). При чтении распарси JSONB обратно в структуру.

120. **sqlc (Game Changer)**: Установите `sqlc`. Напишите `.sql` файлы с вашими запросами и схемой БД. Сгенерируйте типобезопасный Go-код. Поймите, почему `sqlc` считается современным стандартом (вы получаете производительность сырого SQL и типобезопасность ORM).

121. Используй **sqlc** (`github.com/sqlc-dev/sqlc`): создай `query.sql` с SQL-запросами, `sqlc generate` сгенерирует типобезопасный Go-код. Напиши запросы: `GetUser`, `ListUsers`, `CreateUser`. Сравни с ручным написанием.

122. Добавь в таблицу `users` колонку `bio TEXT` (которая может быть `NULL`). Попробуй отсканировать её в обычную `string` (поймай ошибку сканирования `NULL` в string). Используй тип `sql.NullString` для решения проблемы.

123. Переведите деньги между двумя счетами в одной транзакции: списание с одного и зачисление на другой.

124. Сравните подход `sqlc` (код-генерация) и `GORM` (рефлексия во время выполнения). Напишите один и тот же запрос обоими способами и сравните читаемость и безопасность.

125. Настрой `sqlc` с **pgx** драйвером. Сгенерируй код с `pgxpool.Pool`. Покажи, что sqlc генерирует структуры, интерфейсы и реализацию — остаётся только вызвать.

126. **Массивы в БД**: Создай в базе колонку `tags text[]`. Передай из Go обычный срез `[]string{"go", "backend", "sql"}`. `pgx` нативно поддерживает массивы Postgres, в отличие от `lib/pq`.

127. Изучите концепцию версионирования схемы БД. Выберите инструмент: `golang-migrate/migrate` или `pressly/goose`.

128. Напиши **"repository pattern"** вручную: интерфейс `UserRepository` с методами `Create`, `GetByID`, `List`, `Update`, `Delete`. Реализация `PostgresUserRepository` через `*sql.DB`. Покажи, что бизнес-логика не зависит от БД.

129. **Ознакомление с ORM (GORM)**: Установите библиотеку GORM (`github.com/go-gorm/gorm`). Напишите код инициализации подключения через GORM, объявите модель пользователя, выполните автомиграцию схемы данных (`db.AutoMigrate`), создайте запись и сделайте выборку. Напишите комментарий со сравнением подходов `database/sql` vs GORM (производительность, сложность отладки, удобство).

130. Используй указатели (`*string`, `*int`) для сканирования `NULL`-значений вместо `sql.NullString`. Объясни плюсы и минусы обоих подходов.

131. Продемонстрируйте уровень изоляции `SERIALIZABLE`: попробуйте конкурентно обновить одну запись и получите ошибку сериализации.

132. Напиши **"in-memory repository"** `MemoryUserRepository`, реализующий тот же `UserRepository`. Используй `map[int]User` + `sync.RWMutex`. Покажи, что тесты бизнес-логики проходят без реальной БД.

---

133. Напишите пару миграций: `000001_create_users.up.sql` и `000001_create_users.down.sql`.

134. **Пакетная вставка (Batching)**: Тебе нужно вставить 1000 записей. Вместо 1000 отдельных `INSERT` запросов, создай `batch := &pgx.Batch{}`. В цикле добавь запросы через `batch.Queue()`. Выполни их одним махом через `pool.SendBatch()`. Замерь разницу в скорости!

135. Используй **golang-migrate** (`github.com/golang-migrate/migrate`). Создай миграцию `001_create_users.up.sql` и `001_create_users.down.sql`. Примени: `migrate -path migrations -database postgres://... up`. Откати: `down`.

136. Напишите Go-скрипт, который читает миграции из папки и применяет их к БД при старте приложения.

137. Реализуйте пессимистичную блокировку строки с `SELECT ... FOR UPDATE`, чтобы предотвратить гонку при бронировании билетов.

138. Создай таблицу `events` с колонкой `data JSONB`. Вставь в неё JSON-объект, используя структуру Go и `[]byte`.

139. Напиши **"zero-downtime migration"**: добавь колонку `email_verified BOOLEAN DEFAULT false`. Заполни существующие строки через `UPDATE`. Добавь `NOT NULL` через `ALTER ... SET NOT NULL` после заполнения. Обсуди, почему DEFAULT + ALTER лучше, чем NOT NULL сразу.

140. Реализуйте откат миграций (downgrade) до определенной версии через CLI-утилиту.

141. **Обработка специфических ошибок БД**: Напишите обработчик ошибки дублирования уникального значения (например, при попытке зарегистрировать пользователя с уже существующим email). Используйте проверку типа ошибки драйвера (например, приведя ошибку к типу `*pgconn.PgError` при использовании драйвера `pgx`), чтобы извлечь код ошибки PostgreSQL `23505` (unique_violation) и вернуть клиенту понятное сообщение.

---

142. Напиши **seed-данные**: миграция `002_seed_data.up.sql` вставляет тестовых пользователей. Или отдельный Go-скрипт, использующий repository для создания данных. Обсуди разницу: миграции vs seed.

---

143. **Сверхбыстрая вставка (CopyFrom)**: Используй метод `pool.CopyFrom()`. Загрузи в базу 100 000 записей за доли секунды, используя бинарный протокол копирования Postgres (идеально для импорта CSV/логов).

144. Добавьте миграции в Dockerfile вашего приложения, чтобы они применялись автоматически перед запуском бинарника (через `ENTRYPOINT` скрипт).

145. Прочитай `JSONB` данные из БД и десериализуй их обратно в структуру Go.

146. Напишите скрипт на Go, который использует `os/exec` для вызова `pg_dump` и создания резервной копии БД, а затем загружает её в S3.

147. **Listen / Notify (Очереди на минималках)**: Открой одно соединение и запусти `conn.WaitForNotification(ctx)` в горутине — она будет заблокирована в ожидании. В другой горутине (или из консоли psql) сделай `NOTIFY my_channel, 'hello_from_go'`. Убедись, что первая горутина получила сообщение.

148. Используй библиотеку `github.com/jmoiron/sqlx`. Выведи данные из таблицы `users` сразу в слайс структур `[]User` с помощью `db.SelectContext(&users, query)`. Оцени, насколько меньше стало boilerplate-кода по сравнению с ручным `Scan`.

149. **Паттерн Repository (Архитектура)**: Создай интерфейс `UserRepository` с методами `Create`, `GetByID`. Напиши структуру `PostgresRepo`, которая принимает `*pgxpool.Pool` и реализует этот интерфейс. Это основа тестируемого кода.

---

150. **[Каверзный кейс]**: Используй `sqlx.NamedExec` для вставки данных, когда плейсхолдеры называются по именам полей структуры (`INSERT INTO users (name, age) VALUES (:name, :age)`).

151. **Индексы**: Создайте медленный запрос (Full Table Scan). Проанализируйте его через `EXPLAIN ANALYZE`. Создайте B-Tree индекс, а затем GIN-индекс для JSONB-колонки. Сравните планы выполнения.

152. **Проблема N+1**: Напишите код, который получает список постов, а затем в цикле делает `GetUserByID` для каждого автора. "Вылечите" это, написав один `JOIN` запрос или используя `sqlc` с батчингом.

153. **Testcontainers (Postgres)**: Используйте `github.com/testcontainers/testcontainers-go`, чтобы в начале интеграционного теста автоматически поднимать Docker-контейнер с реальным Postgres, применять миграции и тестировать репозиторий.

154. **Мокание (sqlmock)**: Используйте `github.com/DATA-DOG/go-sqlmock` для юнит-тестирования сервисного слоя без поднятия реальной БД. Настройте ожидания (`mock.ExpectQuery`) и проверьте, что сервис корректно обрабатывает ошибки БД.

155. **Транзакционные тесты**: Реализуйте паттерн, где каждый тест стартует транзакцию, выполняет действия, а в `defer` делает `ROLLBACK`. Это гарантирует, что каждый тест начинается с абсолютно чистой БД без затрат на очистку таблиц.

156. Напиши **"User Service"** с PostgreSQL + Redis:
- `Register(name, email, password)` — хеш пароля (bcrypt), INSERT в PostgreSQL, инвалидация кеша
- `GetByID(id)` — cache-aside
- `GetByEmail(email)` — cache-aside по email
- `UpdateProfile(id, name)` — UPDATE в PostgreSQL, `CacheDelete` по id и email
- `Delete(id)` — soft delete (флаг `deleted_at`), инвалидация

157. **Безопасность**: Настройте подключение к Postgres с обязательным SSL (`sslmode=verify-full`). Настройте ролевую модель (отдельный пользователь для миграций с правами DDL, отдельный для приложения только с правами DML).

158. **Полнотекстовый поиск**: Реализуйте поиск по статьям, используя `tsvector` и `tsquery` в Postgres. Добавьте ранжирование (`ts_rank`) и подсветку найденных слов (`ts_headline`).

159. Напиши **"Analytics Service"**:
- `TrackEvent(userID int, event string, metadata map[string]any)` — пиши в Redis Stream (`XAdd`)
- `GetEvents(userID int, from, to time.Time) ([]Event, error)` — читай из PostgreSQL (batch insert из Redis каждые 5 минут через воркер)
- `GetRealtimeStats()` — `PFCount` по HyperLogLog уникальных пользователей за сегодня

160. **Секционирование (Partitioning)**: Создайте таблицу логов, которая автоматически партицируется по месяцам (Declarative Partitioning). Напишите Go-скрипт, который создает новые партиции на будущий месяц и удаляет (detaches) старые.

161. Напиши **"URL Shortener"**:
- `Shorten(longURL string) (string, error)` — генерация short code (base62 от atomic counter или hash), `SET` в Redis с TTL, `INSERT` в PostgreSQL для persistency
- `Resolve(shortCode string) (string, error)` — `GET` из Redis, fallback `SELECT` из PostgreSQL, `SET` в Redis при miss
- `GetStats(shortCode)` — `HGETALL` clicks по странам/времени из Redis, агрегация из PostgreSQL

162. **Финальный Босс (Highload API Gateway):**
    Создайте микросервис "Сборщик аналитики":
    * Принимает тысячи событий в секунду по HTTP.
    * Валидирует их и складывает в **Redis Streams** (очередь) для сглаживания пиковых нагрузок (Backpressure).
    * Фоновые воркеры (Worker Pool) читают из Streams, агрегируют данные (например, счетчики за минуту) и делают **Bulk Insert** (через `pgx.CopyFrom`) в **PostgreSQL** (таблица с временными рядами).
    * Использует **Redis Sorted Sets** для поддержания real-time топа "самых активных пользователей" за последние 24 часа.
    * Использует **Redis Distributed Locks**, чтобы гарантировать, что агрегация за конкретный час происходит только одним воркером (идемпотентность).
    * Оборачивает все операции в `context` с таймаутами и логирует метрики (latency БД, размер очереди) через `slog`.
    * Покрыт интеграционными тестами через `testcontainers`.

---

163. Напиши **"Task Queue"** через PostgreSQL + Redis:
- PostgreSQL: таблица `tasks (id, payload JSONB, status, scheduled_at, attempts, max_attempts)`
- Producer: `INSERT` задачу со `status = 'pending'`, `scheduled_at = NOW()`
- Worker: `SELECT ... FOR UPDATE SKIP LOCKED WHERE status = 'pending' AND scheduled_at <= NOW() LIMIT 1`, обнови `status = 'processing'`, выполни, `UPDATE status = 'completed'` или `'failed'` + `retry_at`
- Redis: `BLPop` для мгновенного уведомления воркеров о новых задачах (Pub/Sub или List)

---
