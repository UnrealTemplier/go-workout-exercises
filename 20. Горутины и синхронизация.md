# Горутины и примитивы синхронизации (WaitGroup, Mutex, Atomic)

В этом файле собраны уникальные упражнения по теме **"Горутины и примитивы синхронизации (WaitGroup, Mutex, Atomic)"**, отсортированные по возрастанию сложности.

---

1. **Запуск первой горутины**: Напишите программу, которая запускает горутину, выводящую текст `"Привет из горутины!"`. В основной функции `main` напишите обычный вывод. Запустите программу несколько раз. Почему вывод из горутины то появляется, то исчезает? (Изучите поведение планировщика: `main` не ждет завершения фоновых горутин).

2. Запусти обычную функцию как горутину (через ключевое слово `go`). Выведи "main" и "goroutine". Убедись, что программа завершается до вывода горутины (используй `time.Sleep` для временного фикса).

3. Запустите простейшую горутину (`go func() { ... }()`), которая выводит сообщение. Запустите программу и объясните, почему сообщение не появилось в консоли (программа завершилась раньше).

4. Запусти функцию `sayHello()` как горутину: `go sayHello()`. Покажи, что `main` завершается раньше, чем горутина успевает выполниться. Добавь `time.Sleep` как временное решение.

5. Запустите горутину, которая печатает «Hello from goroutine», а main продолжает выполняться и завершается через `time.Sleep`.

6. **Сон для ожидания (Плохая практика)**: Добавь `time.Sleep(time.Second)` в конце `main` из упр. 253. Убедись, что теперь вывод есть. Осознай правило: *если завершается main, убиваются все горутины*.

7. Создай 5 горутин, каждая из которых выводит свой номер. Используй `time.Sleep` в `main`, чтобы дождаться их. Осознай, что это плохой подход.

8. Напиши функцию `spawnWorkers(n int, wg *sync.WaitGroup)`. Создай `n` горутин, каждая спит `time.Duration(id)*100ms` и печатает `"worker X done"`. Дождись всех через `wg.Wait()`.

9. Объясните, почему без синхронизации горутина может не успеть выполниться. Исправьте с помощью `sync.WaitGroup`: запустите 5 горутин, каждая печатает свой индекс, дождитесь всех.

10. Используй `sync.WaitGroup` для ожидания завершения 3 горутин. Каждая горутина печатает свой номер и вызывает `wg.Done()`. В `main` — `wg.Wait()`.

11. **Замыкание переменной цикла в горутине**: Напишите цикл `for i := 0; i < 10; i++`, внутри которого запускается горутина `go func() { fmt.Println(i) }()`. Запустите код на версии Go ниже 1.22 (или смоделируйте старое поведение, передавая значение не через аргумент). Почему выводится много одинаковых чисел (например, девяток или десяток)? Исправьте это, явно передавая `i` в качестве аргумента анонимной функции: `go func(val int) { ... }(i)`.

12. Покажи, что **передача WaitGroup по значению** в горутину ломает синхронизацию (копия счётчика). Исправь: передавай по указателю или используй замыкание с внешней `wg`.

13. **Базовый `sync.WaitGroup`**: Убери `time.Sleep`. Создай `var wg sync.WaitGroup`. Вызови `wg.Add(1)` перед запуском горутины. Внутри горутины вызови `wg.Done()`. В конце `main` вызови `wg.Wait()`. Это правильный способ ожидания.

14. Изучите методы `Add()`, `Done()` и `Wait()`. Что произойдет, если вызвать `Wait()` до `Add()`?

15. Исправь data race из 14.5 через `sync.Mutex` (пока просто познакомься, детали в следующей секции). Убедись, что `-race` больше не ругается.

16. Запустите 3 горутины, каждая считает и выводит числа от 1 до 5 с задержкой. Сравните выводы: порядок не детерминирован.

17. **Много горутин**: Запусти в цикле от 1 до 10 анонимные горутины, выводящие номер итерации. Используй `WaitGroup` для ожидания всех десяти. Обрати внимание на порядок вывода (он случайный).

18. **[Каверзный кейс]**: Передай счетчик цикла `i` в горутину напрямую (замыкание). Запусти 10 горутин. Вывод может быть непредсказуемым (все выведут 10). Объясни почему (захват переменной по ссылке).

19. Напиши программу, которая вычисляет сумму чисел от 1 до N **параллельно**: разбивает диапазон на 4 части, каждая горутина считает свою часть, результат отправляет в канал, `main` суммирует.

20. Передайте `WaitGroup` в функцию по значению и изучите ошибку компилятора (`go vet` ругается на копирование `sync.WaitGroup`). Всегда передавайте `WaitGroup` по указателю!

21. **Каверзный случай: Захват переменной цикла (До Go 1.22)**: Если в упр. 256 ты просто выводил `i` внутри анонимной горутины, запусти это на старой версии Go (или почитай об этом). Объясни, почему все 10 горутин могли вывести число `11`.

22. Создай горутину с **бесконечным циклом** и попробуй остановить её извне через `bool` флаг (читаемый в цикле). Покажи, что без `sync/atomic` или mutex это **data race**. Исправь через `atomic.Bool` (или `atomic.Value`).

23. Покажите проблему гонки данных: две горутины инкрементируют общую переменную без синхронизации. Запустите с `go run -race` и увидьте предупреждение.

24. Напиши функцию `ParallelMap(inputs []int, fn func(int) int) []int`, запускающую обработку каждого элемента в отдельной горутине. Собери результаты в правильном порядке (используй слайс с индексами).

25. **Правильный захват**: Передай переменную `i` как аргумент в анонимную горутину: `go func(id int) { ... }(i)`. Убедись, что вывод корректен.

26. Покажи **goroutine leak**: запусти горутину, которая пишет в канал, но никто не читает. Горутина зависнет навечно. Используй `runtime.NumGoroutine()` до и после, чтобы показать утечку.

27. Напишите программу, где горутины работают бесконечно, но `main` завершается. Убедитесь, что при выходе из `main` все горутины принудительно убиваются (в Go нет способа "убить" горутину извне).

28. **[Каверзный кейс]**: Используй `WaitGroup` внутри цикла. Попробуй вызвать `wg.Add(1)` внутри горутины. Поймай состояние гонки (или дедлок), когда `main` вызовет `wg.Wait()` до того, как горутины успеют сделать `Add`.

29. Напиши программу с `GOMAXPROCS`. Выведи `runtime.GOMAXPROCS(0)` (по умолчанию = число CPU). Установи в 1 и запусти CPU-bound задачу в нескольких горутинах. Замерь время до и после.

30. Исправьте гонку с помощью `sync.Mutex`: общая структура со счётчиком и метод `Inc`.

31. **Каверзный случай: Передача WaitGroup по значению**: Напиши функцию `Worker(wg sync.WaitGroup)`. Передай туда `wg` без указателя. Внутри сделай `wg.Done()`. Запусти. Поймай *deadlock* (потому что функция получила копию, а оригинальный WaitGroup в `main` так и остался ждать).

32. Используйте `runtime.NumGoroutine()`, чтобы вывести текущее количество активных горутин в программе.

33. Напиши программу, которая демонстрирует: горутины — ** cooperatively scheduled**. Создай CPU-bound горутину (бесконечный цикл без вызовов функций/каналов) и попробуй запустить вторую. Покажи, что в Go 1.14+ preemption работает даже для чистых циклов, но раньше была проблема.

34. Вынеси логику горутины в именованную функцию. Передавай `*sync.WaitGroup` в неё и вызывай `wg.Done()` через `defer`.

35. Создай горутину с **recover**. Покажи, что паника в горутине **не убивает** программу, если есть `defer recover()`. Напиши `SafeGo(fn func())`, которая запускает функцию в горутине с защитой от panic.

36. **Правильная передача WG**: Перепиши упр. 259, передавая указатель `*sync.WaitGroup`.

37. Используйте `runtime.GOMAXPROCS(0)`, чтобы узнать, сколько ядер процессора Go использует для выполнения горутин по умолчанию.

38. Напиши программу с `sync.WaitGroup`, где количество горутин определяется динамически (например, список URL). Обработай каждый URL в горутине, собери статус-коды. Используй `wg.Add(1)` перед запуском, а не `wg.Add(n)` заранее.

39. Изучи `runtime.GOMAXPROCS(0)` (вывод количества логических процессоров). Установи `runtime.GOMAXPROCS(1)` и запусти несколько горутин. Посмотри, как они чередуются (планировщик).

40. Создайте программу, которая падает с `fatal error: all goroutines are asleep - deadlock!`. Разберитесь, почему планировщик Go обнаружил дедлок.

41. **Паника отрицательного счетчика**: Вызови `wg.Done()` больше раз, чем было вызвано `wg.Add()`. Изучи сообщение о панике.

42. Напиши программу с **"потерянной" горутиной**: `go func() { for { select {} } }()`. Покажи, что она никогда не завершится. Объясни, как это обнаружить через `pprof` или `runtime.NumGoroutine()`.

---

43. Исправьте упражнение на конкурентный инкремент счетчика, защитив критическую секцию с помощью `sync.Mutex` (`Lock()` и `Unlock()`).

44. Напиши программу, которая запускает горутину, которая в свою очередь запускает другие горутины (иерархия). Дождись всех с помощью одной `WaitGroup`.

45. Изучите `sync.RWMutex`. Создайте структуру кэша, где много горутин читают данные (`RLock()`) и редко пишут (`Lock()`). Сравните производительность с обычным `Mutex`.

46. **Управление ядрами**: Импортируй `runtime`. Выведи `runtime.NumCPU()`. С помощью `runtime.GOMAXPROCS(1)` заставь программу работать только на одном логическом ядре, запусти 100 горутин и посмотри, изменилось ли поведение.

---

47. Используй `runtime.Goexit()` внутри горутины для её немедленной остановки. Добавь `defer` до вызова `Goexit` и убедись, что он выполняется.

48. Что произойдет, если вызвать `Unlock()` на мьютексе, который не был заблокирован? Что будет при двойном `Lock()` в одной горутине? (Поймайте Deadlock).

49. Используйте `defer mu.Unlock()` сразу после `mu.Lock()`, чтобы гарантировать разблокировку даже при панике или раннем `return`.

50. **Состояние гонки (Race Condition)**: Создайте глобальную переменную `counter := 0`. Запустите 1000 горутин, каждая из которых инкрементирует счетчик: `counter++`. Дождитесь завершения всех горутин через `WaitGroup` и выведите `counter`. Почему результат почти никогда не равен 1000? Запустите компиляцию программы с флагом обнаружения гонок: `go run -race main.go` и изучите отчет.

51. Переиспользуйте упражнение на конкурентный инкремент счетчика, но защитите счетчик с помощью атомарных операций `atomic.AddInt64(&counter, 1)`. Сравните читаемость и производительность с Mutex.

52. Реализуйте потокобезопасный счетчик (структуру `Counter` с методами `Inc()`, `Dec()`, `Val()`), используя внутри `atomic`.

53. Используйте `sync.Once` для инициализации глобальной конфигурации. Запустите 100 горутин, вызывающих функцию инициализации, и убедитесь, что она выполнилась строго один раз.

54. Изучите `sync.Pool`. Создайте пул объектов (например, `bytes.Buffer`), чтобы переиспользовать их в горутинах и снизить нагрузку на Garbage Collector.

55. **Использование `sync.WaitGroup`**: Перепишите задачу с горутиной. Вместо использования костылей в виде `time.Sleep` для ожидания горутины, примените `sync.WaitGroup`. Объясните правила вызова методов `Add()`, `Done()` и `Wait()`.

56. **Ошибка копирования `WaitGroup`**: Создайте функцию `worker(wg sync.WaitGroup)`, которая выполняет работу и вызывает `wg.Done()`. Запустите её в горутине, передав `wg` по значению. Объясните, почему программа зависает в бесконечном ожидании дедлока. Исправьте ошибку, передавая `WaitGroup` по указателю `*sync.WaitGroup`.

57. **Защита данных через `sync.Mutex`**: Исправьте предыдущую задачу на инкремент счетчика. Добавьте `sync.Mutex` и защитите операцию инкремента критической секцией (`mutex.Lock()` / `mutex.Unlock()`). Убедитесь, что теперь результат всегда равен 1000, а детектор гонок больше не выдает предупреждений.

58. **Анонимные функции и defer**: Используй `defer wg.Done()` внутри горутины (сразу после её начала) — это стандартный паттерн, гарантирующий, что счетчик уменьшится даже при панике внутри горутины.

59. **Безопасная разблокировка с `defer`**: Напишите функцию, которая блокирует мьютекс, делает сложные вычисления, которые могут вернуть ошибку (`return err`), и разблокирует мьютекс. Используйте `defer mutex.Unlock()` сразу после блокировки. Объясните, почему ручной вызов `Unlock()` перед каждым `return` считается плохой практикой.

60. **[Каверзный кейс]**: Создай глобальную переменную `counter = 0`. Запусти 1000 горутин, каждая делает `counter++`. Выведи результат (он будет меньше 1000). Запусти программу с флагом `-race`, чтобы увидеть ошибку.

61. **Оптимизация чтения с `sync.RWMutex`**: Создайте структуру "База данных в памяти" (под капотом обычная мапа). Напишите методы записи и чтения. Предполагая, что чтение происходит в 99% случаев, а запись — в 1%, используйте `sync.RWMutex`. Продемонстрируйте параллельный вызов методов чтения (`RLock` / `RUnlock`), не блокирующих друг друга.

62. **Однократное выполнение (`sync.Once`)**: Напишите потокобезопасный синглтон инициализации подключения к базе данных. Используйте `sync.Once`, чтобы гарантировать, что тяжелая функция инициализации выполнится ровно один раз, даже если её параллельно вызывают 100 горутин.

63. Реализуй `SafeCounter` с `sync.Mutex`. Методы: `Inc()`, `Dec()`, `Value() int`. Запусти 1000 горутин, каждая инкрементирует 1000 раз. Проверь через `-race`, что гонок нет.

64. Покажи **deadlock с Mutex**: рекурсивный вызов `Lock()` внутри метода с `sync.Mutex` (не `RWMutex`). Получи deadlock. Объясни, почему `sync.Mutex` не reentrant. Исправь через вынесение логики или `sync.RWMutex` (если чтение).

65. Реализуй `SafeMap` с `sync.RWMutex`. Методы: `Get(key string) (int, bool)`, `Set(key string, value int)`, `Delete(key string)`. Запусти читателей и писателей. Покажи, что `RLock` позволяет параллельное чтение.

66. **Условные переменные (`sync.Cond`)**: Напишите программу, где несколько горутин-воркеров ждут сигнала о готовности данных. Используйте `sync.Cond` с методом `Wait()`. Главная горутина подготавливает данные и будит все воркеры сразу с помощью метода `Broadcast()`.

67. Сравни производительность: `sync.Mutex` vs `sync.RWMutex` при 90% чтений и 10% записей. Замерь `time.Now()` до/после 1M операций. Покажи, что `RWMutex` выигрывает при преобладании чтений.

68. `sync.RWMutex`: `SafeMap` с методами `Get`, `Set`, `Delete`. Много читателей могут читать одновременно, писатель эксклюзивно.

69. Используйте `atomic.LoadInt64` и `atomic.StoreInt64` для потокобезопасного чтения и записи флагов состояния (например, `isRunning`).

70. Используй `sync.Map` для хранения `map[string]int`. Запусти конкурентные чтения/записи. Покажи, что `sync.Map` безопасен без ручного mutex. Напиши `Range` с `func(key, value any) bool`.

71. Сравни `sync.Map` и `map + sync.RWMutex` по производительности. Напиши бенчмарк (`testing.B`). Покажи, что `sync.Map` оптимизирован под **read-mostly** сценарии с редкими записами, а для write-heavy — обычная map + mutex быстрее.

72. `sync.Once`: инициализация дорогого ресурса (например, чтение конфигурации из файла) только один раз при конкурентных вызовах.

73. **Потокобезопасная мапа (`sync.Map`)**: Напишите программу, где 50 горутин параллельно пишут и читают данные из одной мапы. Обычная мапа упадет с паникой `concurrent map writes`. Решите эту проблему с помощью встроенной структуры `sync.Map` и её методов `Store`, `Load` и `Delete`.

74. Используй `sync.Once` для **lazy initialization**: `var once sync.Once; var instance *Singleton`. Функция `GetInstance()` вызывает `once.Do(func() { instance = &Singleton{} })`. Покажи, что инициализация происходит ровно один раз, даже при конкурентных вызовах.

75. **Детектор гонок**: Запусти код из 282 с флагом `go run -race`. Изучи красный пугающий лог "WARNING: DATA RACE".

76. Покажи, что `sync.Once` **не сбрасывается**: если `Do` паникует, `Once` считает выполненным и повторно не вызовет функцию. Напиши программу, демонстрирующую это (используй `recover` внутри `Do`).

77. Используй `sync.Pool` для переиспользования `bytes.Buffer`. Напиши функцию, которая берёт буфер из пула, пишет в него, возвращает строку, и **обязательно** возвращает буфер в пул через `defer`. Покажи снижение аллокаций.

78. `sync.Cond`: реализуйте очередь задач с ожиданием: горутины ждут `Cond.Wait()`, пока не появятся задачи. Поставщик вызывает `Signal()`.

79. **Исправление через `sync.Mutex`**: Добавь мьютекс. Делай `mu.Lock()` перед инкрементом и `mu.Unlock()` после. Убедись, что счетчик равен миллиону и `-race` молчит.

80. **Пул объектов (`sync.Pool`)**: Создайте структуру `Buffer` большого размера. Напишите код, который часто создает и уничтожает эти буферы, перегружая сборщик мусора (GC). Оптимизируйте программу, используя `sync.Pool` для переиспользования выделенной памяти под буферы.

81. Напиши программу с `sync.Pool`, где забываешь `Put` — покажи, что это не утечка памяти (GC соберёт), но теряется преимущество пула. Замерь аллокации через `runtime.ReadMemStats`.

82. Используй `sync.Cond` (условная переменная): создай `sync.Cond` с `sync.Mutex`. Одна горутина ждёт `cond.Wait()`, пока другая не вызовет `cond.Signal()` или `cond.Broadcast()`. Реализуй "очередь ожидания" или "barrier".

83. **Забытый Unlock (Deadlock)**: В упр. 284 напиши условие `if counter == 5000 { return }` внутри горутины, до `Unlock()`. Увидь, как все остальные горутины зависают навсегда.

84. Используйте `errgroup.Group` (из `golang.org/x/sync/errgroup`) для параллельных HTTP-запросов с обработкой первой же ошибки.

85. Реализуй **barrier** на `sync.WaitGroup`: N горутин должны достичь точки, прежде чем все продолжат. Используй `sync.WaitGroup` дважды: первый — достижение барьера, второй — завершение.

86. **Атомарные операции (`sync/atomic`)**: Перепишите задачу на инкремент счетчика 1000 горутинами без использования мьютексов. Примените функцию `atomic.AddInt64` (или `atomic.Int64` в новых версиях Go). Сравните производительность атомиков и мьютексов.

87. **Правильный `defer`**: Замени ручной вызов на `defer mu.Unlock()` сразу после `mu.Lock()`.

88. Реализуй **"try lock"** через `sync.Mutex` + канал: `TryLock()` захватывает мьютекс с `select` и `default` (или используй `atomic` для флага). Объясни, почему в `sync.Mutex` нет встроенного `TryLock` (до Go 1.18; с 1.18 есть `TryLock()` — покажи его).

89. Напиши программу с **"priority inversion"** (симуляция): высокоприоритетная горутина ждёт мьютекс, захваченный низкоприоритетной. Покажи, что в Go планировщик не гарантирует приоритеты, но мьютекс fair (очередь FIFO с Go 1.19+).

90. **RW Mutex (`sync.RWMutex`)**: Создай мапу-кэш. Запусти 100 горутин на чтение (`mu.RLock()`) и 10 на запись (`mu.Lock()`). Пойми, что `RLock` позволяет параллельное чтение, но блокирует запись.

91. **Классический дедлок (Deadlock)**: Создайте две горутины и два мьютекса `A` и `B`. Напишите код так, чтобы первая горутина блокировала мьютекс `A` и пыталась заблокировать `B`, а вторая горутина в это же время блокировала мьютекс `B` и пыталась заблокировать `A`. Запустите код и проанализируйте панику планировщика.

92. Реализуй счётчик через `atomic.Int64` (Go 1.19+): `var counter atomic.Int64`. Методы: `counter.Add(1)`, `counter.Load()`, `counter.Store(0)`. Запусти конкурентные инкременты.

93. Исправь предыдущее задание с помощью `sync.Mutex` (`Lock()` перед инкрементом, `Unlock()` после).

94. **Исправление через `sync/atomic`**: Убери мьютексы. Замени `counter` на `var counter int64`. Используй `atomic.AddInt64(&counter, 1)` в горутинах. Сравни скорость (в уме: атомики сильно быстрее мьютексов для простых счетчиков).

95. Реализуй **lock-free стек** через `atomic.Pointer` (Go 1.19+): `type Node[T any] struct { value T; next atomic.Pointer[Node[T]] }`. Методы `Push` и `Pop` через `atomic.CompareAndSwapPointer` (или `CompareAndSwap` в `atomic.Pointer`).

96. **Утечка горутин (Goroutine Leak)**: Напишите функцию, которая запускает горутину, ожидающую запись в небуферизованный канал. Однако функция завершается раньше, и никто никогда в этот канал ничего не запишет. Объясните, почему запущенная горутина останется в памяти навсегда и как такие утечки могут исчерпать ресурсы сервера.

---

97. Напиши программу с `atomic.Value`. Сохрани в неё `config struct{ Host string; Port int }`. Покажи, что `atomic.Value` позволяет **атомарно** заменить конфиг целиком. Читатели получают либо старую, либо новую версию, но не "смешанную".

98. **Сложный атомик (Atomic Pointer)**: В Go 1.19+ используй `atomic.Pointer[T]` для безопасной замены целой структуры конфигурации на лету без блокировок.

99. Напиши **spin-lock** через `atomic.Bool`: `for !lock.CompareAndSwap(false, true) { runtime.Gosched() }`. Объясни, когда spin-lock уместен (короткие критические секции), а когда лучше `sync.Mutex`.

100. Используй `sync/atomic` для lock-free счётчика: `atomic.AddInt64(&counter, 1)`. Запусти 1M инкрементов из 100 горутин. Сравни производительность с `sync.Mutex`.

---

101. **Синглтон через `sync.Once`**: Напиши функцию `InitConfig()`. Создай `var once sync.Once`. В цикле запусти 100 горутин, каждая вызывает `once.Do(InitConfig)`. Убедись, что инициализация отработала ровно **один** раз.

102. **[Каверзный кейс]**: Попробуй заблокировать `Mutex`, который уже заблокирован этой же горутиной (двойной Lock). Поймай дедлок. (В Go нет реентерабельных мьютексов).

103. Напиши программу с `atomic.AddUint64` и **memory ordering**: покажи, что атомарные операции гарантируют **happens-before** отношения между горутинами. Без `atomic` (или `sync`) компилятор может переупорядочить инструкции.

104. Реализуй **lock-free max** через `atomic.Int64`: `UpdateMax(newVal int64)` атомарно обновляет значение, только если `newVal > current`. Используй цикл `for` с `Load` и `CompareAndSwap`.

105. **Базовый `sync.Map`**: Когда нужна конкурентная мапа (много горутин читают и пишут), используй `sync.Map`. Попробуй методы `Store`, `Load`, `LoadOrStore` и `Delete`.

106. Создай структуру-счетчик с полем `count` и встроенным `sync.Mutex`. Напиши методы `Increment()` и `Value()`.

107. Напиши программу с `atomic.Pointer` и **ABA-problem**: покажи, что `CompareAndSwap` может "пропустить" изменение, если указатель вернулся к тому же значению. Обсуди, почему в Go managed memory это менее критично, чем в C++.

108. Напиши **"one-shot"** сигнал через `atomic.Bool`: `var ready atomic.Bool`. Горутина-продюсер делает `ready.Store(true)` после инициализации. Потребители ждут в цикле `for !ready.Load() { runtime.Gosched() }`. Покажи, что это работает без mutex.

109. **Обход `sync.Map`**: Используй метод `Range` для итерации по `sync.Map`. Обрати внимание, что он принимает callback-функцию.

110. Используй `sync.RWMutex`. Создай мапу. Несколько горутин читают из мапы (`RLock`), и одна периодически пишет (`Lock`). Убедись, что чтение не блокирует само себя.

111. **Повторное использование с `sync.Pool`**: Создай пул тяжелых структур `pool := &sync.Pool{ New: func() any { return new(HeavyObject) } }`. Забирай объекты `pool.Get()`, работай с ними и возвращай `pool.Put()`.

112. Напиши программу с **"false sharing"**: два `atomic.Int64` на соседних полях одной структуры. Запусти две горутины, каждая инкрементирует свой счётчик. Покажи, что из-за кэш-линии (64 байта) они конкурируют за одну линию. Исправь через **padding** (`_ [56]byte` между полями).

---

113. **[Каверзный кейс]**: Скопируй структуру, содержащую `sync.Mutex`, в другую переменную. Передай копию в горутину и вызови `Lock()`. Запусти `go vet` и пойми, почему копировать мьютексы нельзя (и как исправить — передавать по указателю).

114. **Условные переменные (`sync.Cond`)**: Создай `cond := sync.NewCond(&sync.Mutex{})`. 10 воркеров вызывают `cond.Wait()` (ожидая старта). В `main` вызови `cond.Broadcast()`, чтобы мгновенно разбудить их всех.

115. Используй `sync/atomic` пакет: замени `Mutex` на `atomic.AddInt64` для счетчика. Оцени разницу в синтаксисе.

116. **Каверзный случай: Копирование Mutex**: Передай структуру, содержащую `sync.Mutex`, в функцию по значению. Используй `go vet` — он покажет ошибку "locks by value". Никогда не копируй мьютексы!

---

117. Создай объект-одиночку (Singleton) с отложенной инициализацией (lazy loading) с использованием `sync.Once`.

118. **[Каверзный кейс]**: Внутри `sync.Once.Do(func())` вызови панику. Вызови `Do` еще раз с другой функцией. Убедись, что `Once` запомнил вызов, и вторая функция не выполнится, несмотря на панику.

119. Используйте пакет `golang.org/x/sync/errgroup`. Запустите группу горутин, которые могут вернуть ошибку.

120. Настройте `errgroup` так, чтобы при возникновении *первой* ошибки все остальные горутины в группе автоматически отменялись (через контекст).

121. Ограничьте максимальное количество параллельных задач в `errgroup` (с помощью `SetLimit`, доступно в свежих версиях пакета).

122. Используйте `sync.Map` для создания кэша, который оптимизирован для сценариев, где ключи записываются один раз, но читаются множеством горутин (Read-heavy).

123. Сравните производительность `sync.Map` и обычной `map + sync.RWMutex` в разных сценариях.

124. Используйте `sync.Cond` для реализации паттерна "Producer-Consumer", где Consumer ждет (Wait), пока Producer не сообщит (Signal/Broadcast) о появлении новых данных.
