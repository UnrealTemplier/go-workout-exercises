# Базовые типы, переменные и константы

В этом файле собраны уникальные упражнения по теме **"Базовые типы, переменные и константы"**, отсортированные по возрастанию сложности.

---

1. Объявите переменные всех базовых типов с помощью `var` (с указанием типа и без), выведите их значения и типы (`%T`).

2. Используйте короткое объявление `:=` внутри функции и посмотрите, что происходит при повторном объявлении (частичное переприсваивание).

3. Продемонстрируйте zero-значения для `int`, `float64`, `string`, `bool`, `complex128`.

4. Объявите переменные всех базовых целочисленных типов (`int8`, `int16`, `int32`, `int64`, `uint8` и т.д.) и выведите их размер в байтах с помощью `unsafe.Sizeof`.

5. Создайте константу `Pi` и используйте её для вычисления площади круга заданного радиуса.

6. Создайте константы чисел, строк, а также перечисление дней недели с помощью `iota`.

7. Используйте нетипизированную константу в выражениях с разными типами (`int` и `float64`) — убедитесь в её гибкости.

8. Попробуйте изменить значение константы во время выполнения программы и изучите ошибку компилятора.

9. Преобразуйте `int` в `float64` и обратно, `string` в `[]byte` и `[]rune`, `int` в `string` (через `strconv.Itoa` и через `string(rune)`).

10. Покажите перекрытие переменной (shadowing): объявите переменную на уровне пакета и внутри функции с тем же именем.

11. Объявите неименованную константу (blank identifier `_`) в блоке `const`.

12. Поработайте со строкой: длина в байтах, итерация по байтам и по рунам, срезы по индексам (с учётом UTF-8).

13. Создайте блок `const` с `iota` для перечисления дней недели, месяцев или уровней логирования.

14. Сделайте арифметические операции с `int8` и `uint8`, проверьте переполнение (например, `var a uint8 = 255; a++`).

15. Создайте псевдоним типа `type Celsius float64` и используйте его для переменных, демонстрируя строгую типизацию.

16. Изучите переполнение целочисленных типов (integer overflow) на примере `uint8`, добавив единицу к максимальному значению.

17. Объявите массив из 5 чисел, инициализируйте, передайте в функцию и убедитесь, что внутри функции изменения не влияют на оригинал (копирование).

18. Объявите переменные с использованием короткого объявления (`:=`) и изучите, в каких областях видимости оно работает.

19. Создайте срез, добавьте элементы `append`, исследуйте `len` и `cap`. Создайте подсрез и измените элемент — проверьте эффект на исходный срез.

20. Создайте переменную типа `rune` (алиас для `int32`), присвойте ей кириллический символ и выведите его как символ и как число.

21. Создайте map `string->int`, добавьте, удалите, проверьте наличие ключа (запятая-ok), проитерируйтесь в цикле.

22. Объявите переменную типа `complex128`, выполните базовые арифметические операции и выведите мнимую и действительную части.

23. Объявите структуру `Person` с полями `Name`, `Age`. Инициализируйте разными способами (именованные поля, без имён), вложите структуру `Address`.

24. **Нулевые значения (Zero values)**: Объявите переменные типов `int`, `float64`, `string`, `bool`, `*int`, `[]int`, `map[string]int` без явной инициализации. Выведите их значения с помощью `%#v`, чтобы увидеть их дефолтные значения.

25. **Краткое объявление**: Создайте несколько переменных с помощью оператора `:=`. Попробуйте изменить их тип после объявления (например, присвоить строке число). Зафиксируйте поведение компилятора.

26. Создайте строку с эмодзи и выведите её длину в байтах (`len`) и количество символов (через конвертацию в `[]rune`).

27. **Zero values (нулевые значения)**: Объяви переменные базовых типов, не присваивая им значений. Выведи их и запомни, какие значения Go подставляет по умолчанию.

28. Используйте `strconv.Atoi`, `strconv.ParseFloat`, `strconv.FormatBool` для преобразования строк и обратно.

29. Используйте побитовые операторы (`&`, `|`, `^`, `<<`, `>>`) над переменными типа `uint8`.

30. **Блочное объявление**: Объявите группу переменных разных типов, используя ключевое слово `var` и круглые скобки `var (...)`.

31. Строковые операции из пакета `strings`: `Contains`, `HasPrefix`, `Split`, `Join`, `Replace`, `ToLower`.

32. **Короткое объявление**: Используй оператор `:=` внутри функции `main` для создания новых переменных.

33. Создайте переменную-указатель, не инициализируйте её и выведите её значение по умолчанию (`nil`).

34. Получите текущее время, отформатируйте его в нескольких популярных форматах (пакет `time`).

35. **Блочное объявление**: Объяви 5 переменных через конструкцию `var ( ... )`.

36. Покажите, что преобразование `string` ↔ `[]byte` создаёт копию (изменение слайса байт не меняет строку).

37. **Затенение в блоках (Shadowing)**: Напишите блок `if true { ... }`. Внутри блока объявите переменную с именем, которое уже используется во внешней функции. Измените её значение внутри блока. Выведите значение переменной до блока, внутри блока и после блока.

38. **Мульти-объявление**: Объявите три переменные разных типов в одной строке (например, `a, b, c := 1, 2.5, "hello"`). Выведите их значения.

39. Исследуйте разницу между одинарными и двойными кавычками при объявлении строк и символов (`string` vs `rune`).

40. Объявите несколько строковых констант и проведите их конкатенацию во время компиляции (без рантайм-затрат).

---

41. **Приведение типов**: Создайте переменную `int32` и `float64`. Попробуйте сложить их напрямую. Исправьте ошибку компиляции с помощью явного приведения типов.

42. Объяви переменную типа `int` с помощью `var`, выведи её значение (должен быть 0).

43. **Отбрасывание значений (Blank Identifier)**: Вызови функцию, возвращающую два значения (например, из упр. 21 `fmt.Scan`), но проигнорируй ошибку с помощью `_`.

44. **Переполнение типов (Overflow)**: Объявите переменную типа `uint8` со значением `255`. Прибавьте к ней `1`. Выведите результат. Объясните поведение.

45. Напишите программу, которая меняет местами значения двух переменных без использования третьей (через множественное присваивание `a, b = b, a`).

46. **Явное приведение типов**: Создай `int` и `float64`. Попытайся сложить их напрямую (увидишь ошибку). Затем приведи к одному типу и сложи.

47. Объяви несколько переменных одного типа в одной строке.

48. **Работа с рунами**: Объявите переменную типа `rune` со значением `'Ш'`. Выведите её числовое представление (код Unicode), а затем преобразуйте в строку и выведите как символ.

49. **Конвертация строки в число**: Преобразуй строку "100" в число типа `int` с помощью пакета `strconv` (`Atoi`). Обработай возможную ошибку, если в строке будут буквы.

50. **Преобразование строк в байты**: Создайте строку `"Go язык"`. Преобразуйте её в срез байт `[]byte`. Выведите длину полученного среза. Почему длина среза байт больше, чем количество символов в строке?

51. Объяви переменные всех базовых типов Go: `bool`, `string`, `int`, `int8`, `int16`, `int32`, `int64`, `uint`, `uint8`, `uint16`, `uint32`, `uint64`, `uintptr`, `float32`, `float64`, `complex64`, `complex128`, `byte`, `rune`. Выведи их нулевые значения. Объясни разницу между `byte` и `rune`.

52. **Конвертация числа в строку**: Преобразуй число `42` в строку (`Itoa`) и конкатенируй с другой строкой.

53. Создай переменную с явно указанным типом, но без начального значения. Запиши в неё значение. Выведи тип переменной (`%T`).

54. **Простые константы**: Объявите типизированную константу `const Limit int = 100` и нетипизированную константу `const Speed = 300000`. Попробуйте изменить значение константы в коде.

55. **Теневое копирование (Shadowing)**: Объяви переменную `x := 10` в `main`. Открой блок `{ ... }` (просто фигурные скобки), внутри объяви `x := 20` и выведи её. Выведи `x` после блока. Пойми суть "затенения".

56. Создай переменные `a int32 = 100` и `b int64 = 100`. Попробуй сложить их без преобразования типа. Получи ошибку компиляции. Исправь, сделав явное преобразование. Объясни, почему Go требует явное преобразование.

57. Используй преобразование типов: приведи `float64` к `int` и посмотри, что произойдет с дробной частью.

58. **Групповые константы**: Объявите блок констант, описывающих математические величины (Пи, Экспонента) через `const (...)`.

59. **Глобальные vs Локальные**: Объявите переменную на уровне пакета (вне функций). Объявите переменную с таким же именем внутри функции `main`. Выведите значение внутри функции. Какая переменная приоритетнее? Попробуйте использовать оператор `:=` на уровне пакета и объясните ошибку компиляции.

60. Напиши функцию `SafeAdd(a, b int) (int, error)`, которая проверяет переполнение при сложении. Верни ошибку, если результат выходит за пределы `int`.

61. **Строки и байты**: Измерь длину строки "Hello" и "Привет" с помощью встроенной функции `len()`. Изучи, почему результат для "Привет" больше 6.

62. Создай переменную-указатель через `new()`: `p := new(int)`. Измени значение через указатель. Сравни с `var p *int` (что внутри `p`?).

63. Преобразуй `int` в `string`. Обрати внимание, что получится (число превратится в Unicode символ). Используй `strconv.Itoa` для правильного преобразования.

64. **Нетипизированные константы в действии**: Создайте нетипизированную константу `const X = 5`. Объявите переменные типов `int`, `float64`, `complex128`. Попробуйте умножить каждую из переменных на `X` без явного приведения типов.

65. Работа с `complex64`/`complex128`: создай два комплексных числа, сложи, умножь, найди модуль (`cmplx.Abs`). Выведи действительную и мнимую части.

66. **Руны**: Используй `utf8.RuneCountInString` для получения реального количества символов в строке "Привет".

67. **Простой iota-enum**: С помощью генератора констант `iota` создайте перечисление дней недели (от `Sunday` до `Saturday`), где каждому дню соответствует число от 0 до 6.

68. Напиши программу, которая демонстрирует разницу между `float32` и `float64` в точности: сложи 0.1 + 0.2, сравни результаты. Покажи проблему точности с плавающей точкой.

69. Объяви константу `Pi = 3.14`. Попробуй изменить её значение (поймай ошибку).

70. **Базовые константы**: Объяви типизированную (`const a int = 5`) и нетипизированную (`const b = 5`) константы. Попробуй сложить `b` с `float64`.

71. Используй `math.MaxInt`, `math.MaxUint`, `math.MaxFloat64` и другие константы из пакета `math`. Напиши функцию, которая возвращает максимальное значение для переданного типа (используй `switch type` или рефлексию).

72. **Битовые сдвиги с iota**: Используя `iota` и оператор битового сдвига `<<`, создайте константы для размеров информации: `KB`, `MB`, `GB`, `TB` (1024, 1024^2, ...).

73. Объяви группу констант в скобках `const ( ... )`.

74. **Блоки констант**: Сгруппируй 3 математические константы в блок `const ( ... )`.

75. Создай псевдоним типа: `type Celsius float64`, `type Fahrenheit float64`. Напиши функции конвертации. Попробуй сложить `Celsius` и `Fahrenheit` — что произойдёт? Объясни, почему Go не позволяет (или позволяет?) это сделать.

76. Напиши программу, которая демонстрирует переполнение `uint8`: прибавь 1 к `255`. Что произойдёт? Сделай то же с `int8` (127 + 1). Объясни результат.

77. **Пропуск значений в iota**: Создайте перечисление, в котором значения 0 и 1 пропускаются с помощью пустого идентификатора `_`, а константы начинаются со значения 2.

78. Создай определённый тип `type UserID int`. Напиши функцию `NewUserID(id int) UserID`. Попробуй передать `UserID` туда, где ожидается `int` — получи ошибку. Объясни концепцию определённых типов.

79. Используй `iota` для создания битовых флагов (например, `Read = 1 << iota`, `Write`, `Execute`).

80. **Неизменяемость**: Попытайся переопределить константу в процессе выполнения программы и изучи ошибку компиляции.

81. **Создание своих типов**: Создай тип-псевдоним `type MyInt int`. Создай переменную этого типа и попробуй сложить её с обычным `int`. Пойми, зачем нужно явное приведение типов даже для псевдонимов.

82. **Смещение в iota**: Создайте перечисление месяцев года, используя `iota`, но так, чтобы `January` имел значение 1, а не 0.

83. Напиши программу, которая определяет размер в памяти разных типов с помощью `unsafe.Sizeof()`. Сравни: `bool`, `int32`, `int64`, `string`, `slice`, `map`, `struct` с разными полями. Объясни padding/alignment в структурах.

---

84. **Перечисления через iota (база)**: Создай блок констант, представляющих дни недели (Понедельник=0, Вторник=1, ...), используя `iota`.

85. Изучи неявное преобразование нетипизированных констант: умножь целочисленную константу на `float64` переменную без ошибок.

86. **Сравнение вещественных чисел**: Создайте две переменные `float64` со значениями `0.1 + 0.2` и `0.3`. Сравните их через `==`. Напишите правильную функцию сравнения вещественных чисел с использованием небольшой дельты (эпсилон).

87. Объяви переменные всеми возможными способами:
- `var x int`
- `var x = 10`
- `var x int = 10`
- `x := 10` (краткое объявление)
- `var x, y, z = 1, 2, 3`
- `var ( x = 1; y = 2 )` (групповое объявление)
Выведи все переменные.

88. Создай переменную типа `byte` и присвой ей символ `'A'`. Выведи его числовое значение.

89. **Магия iota**: Задай статусы заказов (Pending=1, Processing=2, Completed=4, Failed=8), используя `iota` и оператор побитового сдвига `<<`.

90. Напиши программу, которая демонстрирует разницу между `var` и `:=` внутри функции. Покажи случай, когда `:=` создаёт новую переменную, а когда переиспользует существующую (shadowing).

91. **Комплексные числа**: Создайте комплексное число `3 + 4i`. Извлеките его действительную и мнимую части программным путем.

92. Создай ситуацию shadowing: объяви `x` на уровне пакета, затем в функции используй `:=` для создания локальной `x`. Покажи, что внутри функции видна локальная `x`, а глобальная скрыта.

93. **Поиск лимитов**: Используя пакет `math` (например, `math.MaxInt8`, `math.MinFloat32`), выведите максимальные и минимальные значения для типов `int8`, `uint16`, `float32`.

---

94. **[Каверзный кейс]**: Объяви переменную `x` в `main()`, затем объяви вложенный блок `{ }` и создай там переменную `x` (затенение/shadowing). Измени внутренний `x` и выведи оба значения после выхода из блока.

95. Напиши программу с множественным присваиванием: `a, b = b, a` (обмен значениями без временной переменной). Реализуй циклический сдвиг трёх переменных: `a, b, c = b, c, a`.

96. Используй "blank identifier" (`_`): прочитай три значения из функции, но используй только одно. Напиши функцию `getCoords() (x, y, z int)` и вызови её, игнорируя `y` и `z`.

97. **Compile-time вычисления**: Объяви константу, равную результату арифметической операции других констант. Затем попробуй присвоить константе результат вызова функции (например, `math.Sqrt(4)`). Пойми разницу между compile-time и runtime.

---

98. Напиши программу, которая демонстрирует нулевые значения переменных разных типов. Создай структуру с полями разных типов и выведи её без инициализации — все поля должны быть нулевыми.

99. Используй `make()` для создания `slice`, `map`, `channel`. Объясни, почему `make()` нужен именно для этих типов, а для других — нет.

100. Напиши программу с `defer`: отложи несколько вызовов функций. Покажи, что они выполняются в порядке LIFO (последний defer — первым).

101. Создай "self-referential" структуру: `type Node struct { Value int; Next *Node }`. Создай связный список из 3 узлов вручную. Обойди список и выведи значения.

---

102. Объяви константы всеми способами:
- `const Pi = 3.14`
- `const Pi float64 = 3.14`
- `const ( Monday = iota; Tuesday; Wednesday )`
- `const ( _ = iota; KB = 1 << (10 * iota); MB; GB; TB )`
Выведи все значения.

103. Напиши программу с константами-перечислениями для дней недели (`Sunday = iota` ... `Saturday`). Напиши функцию `DayName(day int) string`, возвращающую название дня.

104. Создай константы с `iota` для битовых флагов: `FlagRead = 1 << iota`, `FlagWrite`, `FlagExecute`. Напиши функции `HasFlag(flags, flag int) bool`, `AddFlag`, `RemoveFlag`.

105. Напиши программу, которая демонстрирует "untyped constants": `const Big = 1 << 100`. Присвой это значение `float64` и `big.Int`. Объясни, почему это работает.

106. Создай константу строку с многострочным значением (raw string literal с backticks). И константу с интерпретируемыми escape-последовательностями (двойные кавычки). Покажи разницу.

107. Напиши программу, которая пытается изменить константу (получи ошибку компиляции). Затем создай "константный" эффект через `const` vs `var` с неизменяемым типом.

108. Создай константный массив (или слайс-литерал) через `const` — получи ошибку. Объясни, почему в Go нельзя создать константу-слайс или константу-мапу. Найди обходной путь (функция, возвращающая копию).

109. Используй `iota` в разных константных группах. Покажи, что `iota` сбрасывается в 0 в каждой новой группе `const ()`.

110. Создай константы для математических операций с `iota` и оператором: `OpAdd = iota`, `OpSub` и т.д. Напиши функцию `Calculate(op int, a, b float64) float64`, использующую `switch`.

111. Напиши программу с "typed" и "untyped" константами. Покажи, что untyped константу можно присвоить любому совместимому типу, а typed — только своему типу.

---
