Продолжаем нашу масштабную тренировку! Темы, которые вы запросили сейчас — это **"сердце" повседневной разработки на Go**. Именно вокруг массивов, слайсов, мап, структур, интерфейсов и грамотной обработки ошибок строится архитектура любого Go-приложения. 

Ниже представлены **70 новых качественных упражнений (с 151 по 220)**. Они охватывают как классические подходы, так и современные возможности языка (включая пакеты `slices`, `maps` и `slog`, появившиеся в Go 1.21+).

### Тема 17: Массивы (Arrays)
*Массивы в Go используются редко, но понимание их природы критически важно для понимания слайсов.*
151. Объявите массив из 5 целых чисел, не инициализируя его. Выведите значения по умолчанию (zero values).
152. Создайте двумерный массив (матрицу) 3x3 и заполните его числами от 1 до 9 с помощью вложенных циклов.
153. Напишите функцию, принимающую массив `[3]int`. Попробуйте передать ей массив `[4]int` и изучите ошибку компилятора (размер массива — часть его типа).
154. Сравните два массива одинакового размера с помощью оператора `==`. Выясните, какие типы массивов нельзя сравнивать (например, содержащие слайсы или мапы).
155. Напишите программу, которая находит сумму элементов массива, используя классический цикл `for` с индексом, без использования `range`.
156. Передайте массив в функцию по значению и измените его элемент внутри. Убедитесь, что оригинальный массив в `main` остался прежним.
157. Используйте `unsafe.Sizeof` и `unsafe.Alignof`, чтобы изучить, сколько памяти занимает массив из 100 элементов `int64` и как он располагается в памяти.

### Тема 18: Слайсы (Slices): Основы и механика
158. Создайте слайс с помощью `make([]int, 5, 10)`. Выведите его длину (`len`) и емкость (`cap`).
159. Создайте слайс из массива (`arr[1:4]`). Измените элемент в слайсе и посмотрите, как изменился исходный массив (общий базовый массив).
160. Используйте `append` для добавления элемента в слайс, емкость которого уже исчерпана. Отследите, как изменится `cap` (реаллокация памяти).
161. Создайте два слайса и скопируйте элементы из одного в другой с помощью встроенной функции `copy`. Что вернет `copy`, если целевой слайс короче исходного?
162. Реализуйте функцию, которая удаляет элемент из слайса по индексу с сохранением порядка остальных элементов (используя `append`).
163. Реализуйте функцию *быстрого* удаления элемента из слайса (без сохранения порядка: просто переместите последний элемент на место удаляемого).
164. Исследуйте разницу между `var s []int` (nil-слайс) и `s := []int{}` (пустой слайс). Попробуйте сделать `append` в оба.
165. Срежьте слайс до нулевой длины (`s = s[:0]`). Выведите его `len` и `cap`. Объясните, почему этот паттерн полезен для переиспользования памяти без новых аллокаций.
166. Используйте современный пакет `slices` (Go 1.21+) для сортировки, поиска (`Contains`, `Index`) и удаления дубликатов (`Compact`).
167. Напишите функцию, которая принимает слайс и "переворачивает" его (reverse) in-place (без создания нового слайса).

### Тема 19: Мапы (Maps)
168. Создайте мапу `map[string]int`, добавьте в нее 3 пары ключ-значение и выведите их.
169. Попробуйте обратиться к несуществующему ключу в мапе. Какое значение "по умолчанию" вернется?
170. Используйте идиому `value, ok := myMap[key]` для безопасной проверки наличия ключа перед его использованием.
171. Удалите элемент из мапы с помощью `delete`. Что произойдет (упадет ли программа), если попытаться удалить несуществующий ключ?
172. Создайте мапу, где значениями являются слайсы (`map[string][]string`). Реализуйте добавление новой строки в слайс по определенному ключу.
173. Попробуйте использовать слайс или мапу в качестве ключа другой мапы и изучите ошибку компиляции. Почему ключами могут быть только сравниваемые (comparable) типы?
174. Переберите все элементы мапы и выведите их. Запустите программу 5 раз, чтобы убедиться в псевдослучайном порядке итерации.
175. Напишите функцию, которая принимает строку и возвращает мапу с частотой встречаемости каждого символа (rune) в ней.
176. Реализуйте инвертирование мапы (поменять местами ключи и значения). Подумайте, что делать, если значения не уникальны.
177. Попробуйте записать значение в неинициализированную (`nil`) мапу. Поймайте `panic: assignment to entry in nil map` и исправьте это с помощью `make`.
178. Используйте пакет `maps` (Go 1.21+) для клонирования мап (`Clone`), копирования (`Copy`) и сравнения (`Equal`).

### Тема 20: Структуры (Structs)
179. Объявите структуру `Book` с полями `Title`, `Author` и `Year`. Создайте экземпляр с помощью литерала структуры.
180. Создайте анонимную структуру (без предварительного объявления типа `type`) для временного хранения конфигурации.
181. Напишите функцию-конструктор `NewBook(...)`, которая инициализирует поля и возвращает указатель на новую структуру `Book`.
182. Добавьте к структуре метод `PrintInfo()` с получателем по значению (value receiver) и метод `UpdateYear()` с получателем по указателю (pointer receiver). Объясните, почему для изменения состояния нужен указатель.
183. Создайте структуру `Car`, которая содержит *встроенную* (embedded) структуру `Engine`. Вызовите методы `Engine` напрямую через объект `Car`.
184. Попробуйте сравнить две структуры с помощью `==`. Добавьте в структуру поле типа `[]string` (слайс) и попробуйте снова. Изучите ошибку компилятора.
185. Добавьте к структуре теги полей (например, ``json:"title,omitempty"``) и сериализуйте структуру в JSON с помощью `json.Marshal`.
186. Десериализуйте JSON-строку обратно в структуру (`json.Unmarshal`), обработав случай, когда JSON содержит лишние поля.
187. Реализуйте структуру `Vector2D` (X, Y float64) с методами `Add`, `Subtract` и `Scale`, возвращающими новые векторы.
188. Создайте структуру `Node` для односвязного списка (`Value int`, `Next *Node`) и напишите функцию, которая обходит список и выводит значения.

### Тема 21: Интерфейсы (Interfaces)
189. Создайте интерфейс `Animal` с методом `Speak() string`. Реализуйте его для структур `Dog` и `Cat` (неявная реализация).
190. Напишите функцию, принимающую `Animal` и вызывающую `Speak()`. Передайте туда `Dog` и `Cat`, чтобы увидеть полиморфизм в действии.
191. Реализуйте интерфейс `fmt.Stringer` (метод `String() string`) для вашей структуры `Book`, чтобы `fmt.Println(book)` выводил красивый отформатированный текст.
192. Используйте пустой интерфейс `any` (или `interface{}`), чтобы создать слайс, содержащий данные абсолютно разных типов (int, string, struct).
193. Примените утверждение типа (type assertion) `val, ok := myVar.(string)`, чтобы безопасно извлечь строку из переменной типа `any` без паники.
194. Напишите `type switch`, который принимает `any` и выполняет разные действия в зависимости от динамического типа переменной.
195. Сделайте композицию интерфейсов: создайте интерфейс `ReadWriter`, объединив встроенные `io.Reader` и `io.Writer`.
196. **Каверзный случай:** Изучите "nil interface vs nil pointer in interface". Создайте указатель на структуру (равный `nil`), присвойте его переменной интерфейса и проверьте, равно ли `myInterface == nil`. (Спойлер: интерфейс не будет `nil`, так как хранит информацию о типе!).
197. Реализуйте интерфейс `io.Writer` для кастомной структуры, которая просто подсчитывает количество записанных в нее байт.
198. Напишите функцию, которая принимает слайс интерфейсов `fmt.Stringer` и выводит их строковое представление.

### Тема 22: Обработка ошибок (Error Handling)
199. Напишите функцию деления, которая возвращает `(float64, error)`. При делении на ноль возвращайте `errors.New("division by zero")`.
200. Используйте `fmt.Errorf("failed to process user %d: %w", id, err)` для оборачивания (wrap) ошибки с сохранением контекста и цепочки вызовов.
201. Создайте Sentinel-ошибку `var ErrNotFound = errors.New("not found")`. Используйте `errors.Is(err, ErrNotFound)` для проверки, даже если ошибка была обернута несколько раз.
202. Создайте кастомный тип ошибки `type ValidationError struct { Field string }`, реализуйте для него метод `Error() string`.
203. Используйте `errors.As(err, &validationErr)` для безопасного извлечения вашей кастомной ошибки из глубины обернутых ошибок.
204. Напишите функцию, которая возвращает несколько разных типов ошибок, и корректно обработайте их в `main` с помощью `switch` и `errors.As`.
205. Реализуйте паттерн повторных попыток (Retry): если функция вернула определенную ошибку (например, "timeout"), вызовите ее снова до 3 раз с экспоненциальной задержкой.
206. Изучите функцию `errors.Join` (Go 1.20+), чтобы собрать несколько ошибок, возникших при валидации разных полей формы, в одну агрегированную ошибку.
207. Напишите функцию, которая вызывает `panic` при критическом сбое, и используйте `defer` + `recover` на верхнем уровне (`main` или HTTP-хендлере), чтобы перехватить панику и превратить ее в обычный лог или HTTP 500.
208. Сравните подход "возвращать ошибки" (как в Go) и "бросать исключения" (как в Java/Python). Попробуйте написать функцию без `if err != nil` и осознайте, почему в Go это считается плохой практикой.

### Тема 23: Логирование (Logging)
209. Используйте стандартный пакет `log` для вывода информационного сообщения с автоматическим добавлением даты и времени.
210. Настройте префикс и флаги логгера (`log.SetPrefix`, `log.SetFlags`), чтобы каждый лог содержал имя файла и номер строки, где он был вызван (`log.Lshortfile`).
211. Перенаправьте вывод стандартного `log` в файл вместо консоли с помощью `os.OpenFile` и `log.SetOutput`.
212. Используйте `io.MultiWriter`, чтобы логировать сообщения **одновременно** в консоль (`os.Stdout`) и в файл.
213. Изучите современный пакет `log/slog` (стандарт с Go 1.21). Создайте структурированный лог: `slog.Info("user logged in", "user_id", 123, "ip", "127.0.0.1")`.
214. Настройте `slog` для вывода в формате JSON (через `slog.NewJSONHandler`), что является стандартом для облачных приложений и сбора логов (ELK, Loki).
215. Создайте дочерний логгер в `slog` с помощью `logger.With("module", "database")`, чтобы все последующие логи из этого модуля автоматически содержали это поле.
216. Напишите HTTP-мидлварь (middleware), которая логирует каждый входящий запрос: время, метод, URL, статус-код и время выполнения (latency).
217. Реализуйте кастомный `slog.Handler`, который скрывает (маскирует) чувствительные данные (например, слово "password") перед записью лога.
218. Напишите функцию, которая принимает `*slog.Logger` в качестве аргумента. Это позволит вам легко передавать `nil` (что вызовет панику, проверьте `slog.Default()`) или мок-логгер при юнит-тестировании.
219. Перехватите необработанную панику в программе, залогировав её полный стек вызовов (stack trace) с помощью `debug.Stack()` перед аварийным завершением (`os.Exit(1)`).
220. Свяжите всё вместе: создайте структуру `UserService` с методом `CreateUser`. Он должен валидировать данные (возвращая кастомную ошибку), записывать пользователя в мапу (эмуляция БД) и логировать успех или провал через `slog`.

---
**Как работать с этим блоком:**
* **Слайсы и Мапы:** Обязательно рисуйте на бумаге схему памяти (где указатель на массив, где `len`, где `cap`), когда делаете упражнения на `append` и срезку. Это снимет 90% вопросов о том, "почему мой слайс изменился сам по себе".
* **Интерфейсы:** В Go интерфейсы используются для описания *поведения*, а не иерархии наследования. Старайтесь создавать интерфейсы только тогда, когда они реально нужны (например, для мокирования при тестах или для абстрагирования над разными реализациями). Правило Go: "Accept interfaces, return structs".
* **Ошибки и Логи:** В Go не логируют ошибки на каждом уровне стека вызовов. Либо обработайте ошибку (и залогирьте), либо оберните (`%w`) и пробросьте наверх. Двойное логирование одной и той же ошибки захламляет логи и затрудняет отладку.

На данный момент у вас в арсенале **220 глубоких упражнений**. Этого более чем достаточно, чтобы перейти от новичка к уверенному Middle Go разработчику в рамках синтаксиса и стандартной библиотеки! Если будете готовы к следующему этапу (Горутины, Каналы, Контекст, Тестирование и HTTP) — дайте знать!
