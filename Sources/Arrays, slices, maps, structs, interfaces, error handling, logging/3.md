Отличный выбор тем! Это фундамент Go. Именно здесь кроется 80% повседневной работы разработчика. Слайсы, мапы и интерфейсы имеют множество подводных камней, а правильная обработка ошибок отличает джуниора от мидла.

Добавляю **еще 72 упражнения** (с 171 по 242). Теперь твой общий список состоит из 242 задач, что делает этот план тренировок поистине исчерпывающим.

---

### Блок 11: Массивы (Arrays)
*Цель: Понять, что массивы в Go — это значения с фиксированной длиной, а не ссылки.*

171. Объяви массив из 5 целых чисел. Выведи его. Выведи его длину с помощью `len()`.
172. Объяви массив строк `[3]string{"Go", "Rust", "C++"}`. Обрати внимание на вывод типа.
173. Используй синтаксис `[...]int{10, 20, 30}`. Выведи длину массива.
174. Объяви двумерный массив матрицы 3x3, заполненный нулями. Выведи его.
175. Напиши функцию, которая принимает массив `[5]int` и изменяет его первый элемент. Выведи массив после вызова функции (убедись, что оригинал не изменился — передача по значению).
176. Напиши функцию, которая принимает указатель на массив `*[5]int` и изменяет элемент. Убедись, что оригинал изменился.
177. Используй `for range` для итерации по массиву. Выведи индекс и значение.

### Блок 12: Слайсы (Slices) — *Самая важная структура данных в Go*
*Цель: Глубоко понять устройство слайса (заголовок: указатель, len, cap), дозапись (append), утечки памяти.*

178. Создай слайс из строк `[]string{"a", "b", "c"}`. Выведи длину и вместимость (`cap()`).
179. Создай пустой слайс с помощью `make([]int, 3)`. Выведи значения (они должны быть нулями).
180. Создай слайс с помощью `make([]int, 3, 5)`. Выведи `len` и `cap`. Объясни разницу.
181. Используй `append()` для добавления элементов в слайс, пока его длина не превысит изначальную вместимость. Выводи `len` и `cap` после каждого `append` (наблюдай, как растёт capacity: 1, 2, 4, 8...).
182. Создай слайс `s := []int{1, 2, 3, 4, 5}`. Сделай срез `s2 := s[1:3]`. Выведи `s2`.
183. **[Каверзный кейс]** В слайсе `s` из предыдущего задания измени `s2[0]` на `99`. Выведи `s`. Объясни, почему изменился оригинал (слайсы делят один базовый массив).
184. Используй `copy()` для создания независимой копии слайса. Измени копию и убедись, что оригинал не изменился.
185. **[Каверзный кейс]** Сделай срез `s3 := s[1:3]` и сделай `append(s3, 10)`. Посмотри, изменится ли оригинальный массив `s` (зависит от capacity).
186. Напиши функцию, которая удаляет элемент из слайса по индексу `i` (используй `append(s[:i], s[i+1:]...)`).
187. Реализуй стек (LIFO) на основе слайса: функции `Push` (через append) и `Pop` (возврат последнего элемента и уменьшение длины).
188. **[Высокая сложность]** Напиши функцию `Insert(s []int, index int, val int) []int`, которая вставляет элемент в середину слайса без потери данных.
189. **[Каверзный кейс]** Утечка памяти: создай большой слайс (1 млн элементов), затем сделай маленький срез `small := large[0:2]`. Объясни, почему весь большой массив останется в памяти. Исправь это с помощью `copy()`.
190. Создай слайс слайсов `[][]int`. Заполни его несколькими внутренними слайсами разной длины (зубчатый массив).
191. Напиши функцию `Filter(nums []int, predicate func(int) bool) []int`, которая возвращает новый слайс только с теми элементами, для которых `predicate` вернул `true`.

### Блок 13: Мапы (Maps)
*Цель: Освоить хеш-таблицы, их создание, чтение, удаление и особенности работы с `nil`-мапами.*

192. Создай мапу, хранящую частоту слов в строке: `map[string]int`. Посчитай слова и выведи результат.
193. Создай мапу с помощью `make(map[string]string)`. Добавь пару ключ-значение.
194. **[Каверзный кейс]** Объяви `var m map[string]int`. Попробуй сделать `m["a"] = 1` (поймай панику записи в nil-мапу). Исправь с помощью `make`.
195. Попробуй прочитать значение по несуществующему ключу. Выведи результат (должен быть нулевым значением типа).
196. Используй идиому `val, ok := m["key"]` для проверки существования ключа.
197. Удали элемент из мапы с помощью `delete(m, "key")`.
198. Используй `for range` для итерации по мапе. Запусти несколько раз и убедись, что порядок случайный.
199. **[Каверзный кейс]** Создай мапу, где значениями являются слайсы `map[string][]int`. Добавь значения в слайс по ключу `m["a"] = append(m["a"], 1)`.
200. **[Каверзный кейс]** Попробуй взять адрес элемента мапы `&m["key"]` (поймай ошибку компиляции — элементы мапы не адресуемы из-за возможного перехеширования).
201. Напиши функцию, которая принимает строку и возвращает `map[rune]int` с количеством каждой буквы.
202. Используй `clear(m)` (добавлено в Go 1.21) для полной очистки мапы. Проверь её длину после.
203. Создай вложенную мапу `map[string]map[string]int` (например, города и районы с населением). Реализуй добавление и чтение данных из неё.

### Блок 14: Структуры (Structs)
*Цель: Научиться создавать собственные типы данных, встраивать их и использовать методы.*

204. Создай структуру `User` с полями `ID int`, `Name string`, `Email string`. Объяви экземпляр структуры и выведи его.
205. Инициализируй структуру по именам полей `User{ID: 1, Name: "Alice"}`.
206. Создай анонимную структуру прямо внутри функции `main`, запиши в неё данные и выведи.
207. **[Каверзный кейс]** Создай структуру `Point` с полями `X, Y int`. В цикле создай срез указателей на структуры `[]*Point`. Изменяй поля через указатели.
208. Создай структуру `Employee`, которая анонимно встраивает (embedding) `User`. Обрати внимание, что поля `User` доступны напрямую (например, `emp.Name`).
209. Добавь метод `Greet()` для структуры `User` (value receiver).
210. Добавь метод `UpdateEmail(newEmail string)` (pointer receiver). Объясни, почему для изменения данных нужен pointer receiver.
211. **[Каверзный кейс]** Напиши функцию, которая принимает структуру по значению и пытается изменить её поле. Убедись, что оригинал не изменился. Напиши другую функцию, принимающую указатель.
212. Выведи структуру с помощью `fmt.Printf("%+v\n", user)` (с именами полей) и `%#v` (синтаксис Go).
213. Создай конструктор (идиома Go — функция `NewUser(id int, name string) *User`), которая возвращает указатель на созданную структуру.
214. Создай структуру с тегами (struct tags) `type User struct { Name string `json:"name"` }`.
215. **[Высокая сложность]** Создай структуру `TreeNode` для бинарного дерева (поля `Value int`, `Left *TreeNode`, `Right *TreeNode`). Напиши метод для обхода дерева (in-order traversal).

### Блок 15: Интерфейсы (Interfaces)
*Цель: Понять неявное внедрение интерфейсов, полиморфизм, пустой интерфейс и type assertions.*

216. Создай интерфейс `Shape` с методом `Area() float64`.
217. Создай структуры `Circle` и `Rectangle`. Реализуй для них метод `Area()`. (Теперь они неявно удовлетворяют интерфейсу `Shape`).
218. Напиши функцию `PrintArea(s Shape)`, которая принимает интерфейс. Передай в неё круг и прямоугольник.
219. Используй пустой интерфейс `interface{}` (или алиас `any`) для создания функции `PrintAnything(v any)`. Передай в неё число, строку и структуру.
220. Используй type assertion для извлечения конкретного типа из `any`: `str, ok := v.(string)`.
221. Используй `switch v := x.(type)` (type switch) для обработки различных типов внутри `any` (int, string, bool, default).
222. **[Каверзный кейс]** Реализуй интерфейс с pointer receiver. Попробуй передать *значение* (а не указатель) структуры в функцию, принимающую интерфейс. Поймай ошибку компиляции.
223. Внедри один интерфейс в другой: `type ReadWriter interface { Reader; Writer }`.
224. Реализуй интерфейс `fmt.Stringer` для своей структуры (метод `String() string`). Выведи структуру через `fmt.Println` и убедись, что вызывается твой метод.
225. **[Каверзный кейс]** "Nil interface": объяви переменную типа интерфейс `var s Shape`. Сравни её с `nil` (должно быть true). Присвой ей указатель на структуру, который равен `nil`. Сравни снова (теперь `false`! Интерфейс внутри содержит тип, хотя указатель nil). Обработай эту ситуацию.
226. Используй `sort.Slice` для сортировки среза структур по определенному полю (передав замыкание, реализующее логику `less(i, j int) bool`).
227. **[Высокая сложность]** Создай интерфейс `Validator` с методом `Validate() error`. Создай 3 разные структуры, реализующие этот интерфейс, и функцию, которая валидирует слайс любых валидаторов.

### Блок 16: Обработка ошибок (Error Handling)
*Цель: Изучить идиоматический подход Go к ошибкам (errors are values), оборачивание и проверка типов ошибок.*

228. Напиши функцию `Divide(a, b float64) (float64, error)`. Возвращай `errors.New("division by zero")` при `b == 0`.
229. Вызови функцию `Divide` и проверь ошибку стандартным `if err != nil`.
230. Используй `fmt.Errorf` для создания ошибки с форматированием: `fmt.Errorf("user %s not found", name)`.
231. Создай свои "сентинель" (sentinel) ошибки: `var ErrNotFound = errors.New("not found")`. Верни её из функции и проверь через `if errors.Is(err, ErrNotFound)`.
232. **[Каверзный кейс]** Оберни ошибку: `fmt.Errorf("db query failed: %w", err)`. Проверь исходную ошибку с помощью `errors.Is` (она "пробьется" через обертку `%w`).
233. Создай кастомный тип ошибки (структуру, реализующую метод `Error() string`).
234. Верни кастомную ошибку из функции, обернув её через `%w`. Извлеки оригинальный тип ошибки с помощью `errors.As`.
235. Используй `defer` и `recover()` внутри функции для перехвата паники. Верни перехваченную панику как обычную `error` из функции.
236. **[Каверзный кейс]** Напиши функцию, которая возвращает несколько ошибок одновременно. (Можно использовать `errors.Join` из Go 1.20+). Проверь наличие конкретной ошибки в объединенной ошибке через `errors.Is`.
237. Напиши функцию `CloseResource(c io.Closer)`, которая вызывает `c.Close()` и паникует, если закрытие вернуло ошибку (используй `log.Panic` или `panic(err)`).
238. **[Высокая сложность]** Напиши функцию, которая считывает число из строки, делит его на другое число и записывает результат в файл. Оберни ошибки на каждом этапе (`strconv`, `math`, `os`), чтобы в логе было видно всю цепочку вызовов.

### Блок 17: Логирование (Logging)
*Цель: Научиться писать логи в консоль и файлы, использовать структурированное логирование (log/slog).*

239. Используй стандартный `log.Println` и `log.Printf` для вывода информации.
240. Сравни вывод `log.Println` и `fmt.Println` (обрати внимание на автоматическую простановку даты и времени в `log`).
241. Вызови `log.Fatalf("Fatal error: %v", err)`. Убедись, что программа завершается с кодом 1 после записи лога.
242. Измени флаги логгера: `log.SetFlags(log.LstdFlags | log.Lshortfile)`. Убедись, что в лог добавилось имя файла и номер строки.
243. Создай кастомный логгер с помощью `log.New()`. Направь его вывод в `os.Stderr`.
244. Направь вывод кастомного логгера в файл (открой файл через `os.OpenFile` с флагами `os.O_APPEND|os.O_CREATE|os.O_WRONLY`).
245. **[Высокая сложность]** Используй пакет `log/slog` (стандарт с Go 1.21). Создай JSON логгер: `slog.New(slog.NewJSONHandler(os.Stdout, nil))`. Выведи лог с контекстом: `slog.Info("user logged in", "id", 42, "name", "Alice")`.
246. Используй `slog` с уровнями логирования: `slog.Debug`, `slog.Info`, `slog.Warn`, `slog.Error`.
247. Создай дочерний логгер `slog` с контекстом, используя `logger.With("module", "db")`, чтобы все последующие логи автоматически содержали это поле.
248. **[Практический кейс]** Напиши мини-программу, которая пытается открыть несуществующий файл. Если файла нет, пишет `Warn` в лог и создает его. Если есть другая ошибка (например, нет прав), пишет `Error` и завершает работу через `os.Exit(1)`.

---

Теперь у тебя есть **248 упражнений**. Если ты дойдешь до конца этого списка и выполнишь все каверзные кейсы, ты будешь уверенно чувствовать себя на позициях Go-разработчика уровня Junior+ / Middle-. Удачи в кодинге! Если по какому-то заданию понадобится теория или разбор — обращайся!
