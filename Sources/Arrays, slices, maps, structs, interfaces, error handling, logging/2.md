Отлично! Добавляю большой блок из 50 качественных упражнений, который ставится **после раздела 3 (Условные конструкции и циклы)**. Он глубоко охватывает массивы, слайсы, карты, структуры, интерфейсы, обработку ошибок и логирование. Все последующие номера в исходном плане сдвигаются на 50 (итого получается 200 упражнений).

---

### 4. Массивы, слайсы, карты, структуры, интерфейсы, ошибки и логирование (добавленный раздел)

#### 4.1 Массивы
46. Создайте массив из 7 чисел, найдите сумму и произведение всех элементов.
47. Проверьте, является ли массив палиндромом (читается одинаково слева направо и справа налево).
48. Передайте массив в функцию для модификации (удвоить каждый элемент). Убедитесь, что исходный массив не изменился.
49. Создайте двумерный массив 3×3, заполните его «змейкой» и выведите матрицу.
50. Отсортируйте массив строк по длине, используя ручную реализацию пузырьковой сортировки.
51. Используйте массив как ключ в map: создайте `map[[3]int]string`, где ключ — RGB-тройка, а значение — название цвета.

#### 4.2 Слайсы (углублённо)
52. Создайте срез целых чисел, добавьте к нему 10 элементов в цикле, выводя `len` и `cap` на каждом шаге.
53. Реализуйте функцию `Filter(s []int, fn func(int) bool) []int` без использования внешних пакетов.
54. Сделайте «reverse in-place»: разверните слайс без выделения дополнительной памяти.
55. Удалите i-й элемент из слайса с сохранением порядка (два способа).
56. Вставьте элемент в середину слайса (по индексу) с помощью `append` и копирования.
57. Проверьте, является ли слайс отсортированным (напишите универсальную функцию с comparable).
58. Объедините два слайса без дубликатов (сохранив порядок первого вхождения).
59. Создайте слайс слайсов (двумерный) разной длины, заполните случайными числами и выведите красиво.
60. Напишите функцию, которая принимает слайс и возвращает слайс указателей на его элементы (осторожно с захватом переменной цикла). Покажите правильный способ.

#### 4.3 Карты (map)
61. Постройте частотный словарь: на вход строка, на выход `map[rune]int`.
62. Инвертируйте map `[string]int` в `[int]string`, проверьте на коллизии.
63. Отсортируйте ключи map и выведите значения в отсортированном порядке.
64. Используйте map как множество: реализуйте добавление, удаление, проверку наличия элемента.
65. Безопасно удалите все элементы в цикле `for k := range m { delete(m, k) }`, не вызывая паники.
66. Создайте map с функциями как значениями: `map[string]func(int, int) int` и вызывайте операцию по имени.
67. Сравните два map на равенство (напишите свою функцию).
68. Используйте map для кеширования результатов дорогой функции (мемоизация чисел Фибоначчи).

#### 4.4 Структуры (продвинутое использование)
69. Объявите структуру `Book` с полями `Title`, `Author`, `Year`. Создайте слайс книг и отсортируйте по году с помощью `sort.Slice`.
70. Вложенные структуры: `Address` внутри `Employee`. Выведите вложенное поле напрямую (`emp.Address.City`).
71. Анонимные поля: структура `Car` с анонимным полем `Engine`. Покажите продвижение методов.
72. Теги структуры: добавьте теги `json` и `db`, выведите их с помощью `reflect`.
73. Создайте конструктор `NewPerson(name string, age int) *Person`, проверяющий корректность данных.
74. Сравнение структур: покажите, что две структуры равны, если все поля сравнимы. Что будет, если поле — слайс?
75. Методы с получателем-значением и получателем-указателем: у структуры `Circle` метод `Area()` (значение) и `Scale(factor)` (указатель). Продемонстрируйте вызовы и разницу.
76. Реализуйте связный список (односвязный) на структурах: добавление в конец, печать, поиск.

#### 4.5 Интерфейсы
77. Определите интерфейс `Shape` с методами `Area() float64` и `Perimeter() float64`. Реализуйте его для `Rectangle` и `Circle`.
78. Напишите функцию, принимающую срез `Shape` и выводящую суммарную площадь и периметр.
79. Пустой интерфейс: функция `PrintAll(v ...interface{})`, которая печатает тип и значение каждого элемента.
80. Утверждение типа: проверьте, реализует ли переданный `interface{}` интерфейс `fmt.Stringer`, и если да, вызовите его.
81. Переключатель типов: обработайте `int`, `string`, `[]int` и `default` в одной функции.
82. Композиция интерфейсов: `type ReadWriter interface { io.Reader; io.Writer }`. Создайте тип, удовлетворяющий этому интерфейсу (например, буфер в памяти).
83. Интерфейс `error`: создайте пользовательский тип ошибки `SyntaxError` с полями `Line` и `Message`, реализуйте метод `Error() string`.
84. Сравнение интерфейсов с nil: покажите разницу между nil-интерфейсом и интерфейсом, хранящим nil-указатель на конкретный тип.
85. Напишите функцию `Describe(i interface{})`, которая печатает динамический тип и значение, а также, если возможно, использует reflect для вывода полей структуры.
86. Создайте и используйте интерфейс `Sorter` с методом `Sort(data []int)`, реализуйте его с разными алгоритмами (пузырёк, выбор) и выберите стратегию во время выполнения.

#### 4.6 Обработка ошибок
87. Напишите функцию деления, возвращающую `error` с подробным сообщением при делении на ноль. Вызовите и обработайте её.
88. Создайте цепочку функций: `readFile -> parseJSON -> validate`. Каждая возвращает ошибку. Пробросьте ошибку вверх, добавив контекст с помощью `fmt.Errorf("...: %w", err)`.
89. Используйте `errors.Is` для проверки конкретной ошибки в цепочке обёрнутых ошибок.
90. Используйте `errors.As` для извлечения конкретного типа ошибки из цепочки.
91. Реализуйте функцию, которая может вернуть несколько ошибок (слайс ошибок). Используйте `errors.Join` (Go 1.20+) для объединения.
92. Создайте sentinel-ошибки (пакетные переменные) `ErrNotFound`, `ErrPermission`. Возвращайте их и проверяйте снаружи.
93. Напишите recover в defer, который превращает панику в обычную ошибку и возвращает её из функции.
94. Реализуйте функцию, которая в случае ошибки заносит её в лог, но продолжает выполнение цикла (graceful degradation).

#### 4.7 Логирование
95. Настройте стандартный логгер (`log.Default`) для вывода в файл с флагами даты, времени и имени файла.
96. Используйте разные уровни логирования: напишите обёртку с методами `Debug`, `Info`, `Warn`, `Error` на основе стандартного `log`.
97. Логируйте в несколько адресатов одновременно: в консоль и в файл (используя `io.MultiWriter`).
98. Создайте кастомный логгер для каждого модуля приложения с префиксами `[DB]`, `[API]`.
99. Логируйте все фатальные ошибки перед вызовом `os.Exit` или `log.Fatal`.
100. Структурированное логирование: напишите функцию, которая выводит JSON-строку с полями `"level"`, `"msg"`, `"time"` (без внешних библиотек).

---

Эти 55 упражнений (с 46 по 100) дают всестороннюю проработку структур данных, интерфейсов, обработки ошибок и логирования. Вместе с предыдущими 45 пунктами (основы + условия/циклы) общий счёт достиг 100, а после добавления всех остальных разделов (функции, указатели, файлы, модули) итоговое количество составит 200. Успешной тренировки!
