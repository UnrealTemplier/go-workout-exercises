Вот обширный блок из **85 упражнений** по Message Queues в Go — Kafka, RabbitMQ и NATS с упором на реальные production-задачи. Вставляй его **после микросервисов и WebSockets** — очереди являются фундаментом event-driven архитектуры.

---

## Часть XX. Message Queues (Kafka, RabbitMQ, NATS)

### 94. NATS — основы и core patterns

**Упражнение 20.1.** Установи NATS Server (`docker run -p 4222:4222 nats`). Подключись через `github.com/nats-io/nats.go`: `nc, _ := nats.Connect(nats.DefaultURL)`. Опубликуй сообщение: `nc.Publish("hello.world", []byte("Hello NATS!"))`. Подпишись: `nc.Subscribe("hello.world", func(m *nats.Msg) { fmt.Println(string(m.Data)) })`.

**Упражнение 20.2.** Покажи разницу между **Publish/Subscribe** (one-to-many, все подписчики получают) и **Queue Groups** (one-to-one, round-robin внутри группы). Создай 3 подписчика на `"orders"` без queue group — все получат. Создай 3 подписчика с `nats.QueueSubscribe("orders", "workers", ...)` — только один получит.

**Упражнение 20.3.** Реализуй **Request/Reply pattern**: клиент `nc.Request("help", []byte("help me"), 2*time.Second)` — ждёт ответа с timeout. Сервер подписан на `"help"` и отвечает `m.Respond([]byte("I can help!"))`. Покажи синхронный RPC поверх асинхронного NATS.

**Упражнение 20.4.** Реализуй **Subject hierarchies и wildcards**: публикуй на `"orders.us.electronics"`, `"orders.eu.books"`. Подпишись на `"orders.>"` (получит всё), `"orders.us.>"` (только US), `"orders.*.electronics"` (electronics в любом регионе). Покажи гибкость routing'а.

**Упражнение 20.5.** Напиши **connection resilience**: `nats.Connect(url, nats.MaxReconnects(10), nats.ReconnectWait(time.Second), nats.DisconnectErrHandler(func(nc *nats.Conn, err error) { log.Println("disconnected:", err) }), nats.ReconnectHandler(func(nc *nats.Conn) { log.Println("reconnected") }))`. Симулируй рестарт NATS — покажи автоматический reconnect.

**Упражнение 20.6.** Напиши **graceful shutdown**: `defer nc.Drain()`. `Drain()` — публикует все pending сообщения, обрабатывает все pending callbacks, затем закрывает. Сравни с `nc.Close()` (немедленно, может потерять сообщения).

**Упражнение 20.7.** Реализуй **message headers**: `msg := nats.NewMsg("events"); msg.Header.Set("X-Event-Type", "user.created"); msg.Header.Set("X-Correlation-ID", "abc-123"); nc.PublishMsg(msg)`. В subscriber'е читай `m.Header.Get("X-Event-Type")`. Покажи передачу метаданных без payload parsing.

**Упражнение 20.8.** Напиши **typed messages через JSON**: определи `type Event struct { Type string json:"type"; Payload any json:"payload"; Timestamp time.Time json:"timestamp" }`. Сериализуй `json.Marshal(event)`, публикуй. В subscriber'е `json.Unmarshal` в `Event` + type switch по `Type`. Обработай `unknown event type`.

---

### 95. NATS JetStream — persistence и streams

**Упражнение 20.9.** Включи JetStream: `docker run -p 4222:4222 nats -js`. Создай Stream: `js, _ := nc.JetStream(); js.AddStream(&nats.StreamConfig{Name: "ORDERS", Subjects: []string{"orders.>"}, Retention: nats.WorkQueuePolicy, MaxMsgs: 100000, MaxAge: 24 * time.Hour})`. Покажи, что JetStream — persistence layer поверх NATS Core.

**Упражнение 20.10.** Напиши **Producer для JetStream**: `js.Publish("orders.us", []byte("order-123"))`. Покажи, что сообщение сохраняется даже если consumer offline. Проверь через `nats stream info ORDERS`.

**Упражнение 20.11.** Напиши **Push Consumer**: `sub, _ := js.Subscribe("orders.>", func(m *nats.Msg) { ... }, nats.Durable("processor-1"), nats.ManualAck())`. Обработай сообщение, затем `m.Ack()`. Покажи, что без `Ack` сообщение redeliver'ится.

**Упражнение 20.12.** Напиши **Pull Consumer** (предпочтительно для high-throughput): `sub, _ := js.PullSubscribe("orders.>", "processor-2", nats.PullMaxWaiting(100))`. В цикле: `msgs, _ := sub.Fetch(10, nats.MaxWait(500*time.Millisecond))`. Обработай batch, ack каждое. Покажи контроль над rate.

**Упражнение 20.13.** Реализуй **exactly-once processing**: `js.Publish("orders", data, nats.MsgId("order-123"))` — дедупликация по `MsgId` в течение `DuplicateWindow` (default 2 мин). Consumer: `m.InProgress()` для "я работаю", `m.Ack()` для успеха, `m.Nak()` для "переотправь сейчас", `m.Term()` для "отбрось навсегда".

**Упражнение 20.14.** Напиши **Dead Letter Queue (DLQ)**: в Stream Config: `MaxDeliver: 3`. Consumer с `nats.MaxDeliver(3)`. При 3 неудачных попытках — сообщение идёт в `DeadLetterPolicy{Policy: nats.DiscardOld, MaxSamples: 100}`. Или ручной: `m.Nak()` с `m.Header.Set("Nats-Last-Consumer", "...")`, после 3 `Nak` — `m.Term()` + публикуй в `"orders.dlq"`.

**Упражнение 20.15.** Реализуй **Consumer Groups с scaling**: 5 инстансов consumer'а, один `Durable` name `"order-processors"`, `QueueSubscribe` на JetStream pull. JetStream распределяет сообщения round-robin. Добавь/убери инстансы — покажи rebalancing.

**Упражнение 20.16.** Напиши **Stream mirroring / sourcing**: Stream `ORDERS_ARCHIVE` с `Mirror: &nats.StreamSource{Name: "ORDERS"}`. Или `Sources: []*nats.StreamSource{{Name: "PAYMENTS"}, {Name: "SHIPPING"}}`. Покажи агрегацию нескольких streams.

**Упражнение 20.17.** Реализуй **Message replay**: `js.Subscribe("orders.>", ..., nats.DeliverAll())` — все сообщения с начала. `nats.DeliverLast()` — только последнее. `nats.StartSequence(1000)` — с sequence 1000. `nats.StartTime(time.Now().Add(-24*time.Hour))` — за последние 24 часа. Покажи использование для recovery / new consumer.

**Упражнение 20.18.** Напиши **Key-Value Store через JetStream**: `kv, _ := js.CreateKeyValue(&nats.KeyValueConfig{Bucket: "user-sessions", TTL: time.Hour})`. `kv.Put("user:123", []byte("active"))`, `kv.Get("user:123")`. Покажи CRUD, `Watch` для изменений, `History` для аудита.

**Упражнение 20.19.** Напиши **Object Store через JetStream**: `os, _ := js.CreateObjectStore(&nats.ObjectStoreConfig{Bucket: "files", TTL: time.Hour*24})`. `os.Put("report.pdf", reader)`. `os.Get("report.pdf")`. Покажи хранение больших файлов (>1MB) в NATS.

**Упражнение 20.20.** Реализуй **Distributed locking через JetStream KV**: `kv.Create("lock:resource-1", []byte("owner-123"))` — атомарный, fail если ключ существует. `kv.Update("lock:resource-1", []byte("owner-123"), revision)` — CAS (compare-and-swap). `kv.Delete("lock:resource-1")` — release. Покажи `context.WithTimeout` для lock TTL.

---

### 96. NATS — production patterns

**Упражнение 20.21.** Напиши **Event Sourcing с NATS JetStream**: Stream `EVENTS` с `Subjects: []string{"events.>"}`. Каждое событие — `Event{AggregateID, Type, Version, Payload, Timestamp}`. Consumer проецирует в PostgreSQL/Elasticsearch. Покажи replay: новый consumer читает все события, строит state.

**Упражнение 20.22.** Реализуй **Outbox pattern с NATS**: PostgreSQL транзакция: `INSERT INTO orders ...` + `INSERT INTO outbox (topic, payload, headers) ...`. Отдельный воркер: `SELECT * FROM outbox ORDER BY id LIMIT 100`, `js.Publish`, `DELETE FROM outbox WHERE id = ...`. Покажи exactly-once: транзакция БД + at-least-once NATS + идемпотентность consumer'а.

**Упражнение 20.23.** Напиши **Saga Orchestrator через NATS**: Saga `CreateOrder` = `ReserveInventory` → `ProcessPayment` → `ShipOrder`. Каждый шаг — публикация в `saga.{sagaID}.execute`. Каждый сервис отвечает в `saga.{sagaID}.result`. Orchestrator читает результаты, решает: next step или compensate. Состояние saga в JetStream KV.

**Упражнение 20.24.** Реализуй **CQRS Event Bus через NATS**: Command side пишет события в `events.{aggregate}.{id}`. Query side (3 read models: Elasticsearch, Redis, ClickHouse) — отдельные consumer groups. Покажи, что каждый read model обрабатывает в своём темпе.

**Упражнение 20.25.** Напиши **Multi-tenancy через NATS**: tenant isolation через subject prefix: `{tenant}.orders.>` или через **Accounts** (NATS 2.0). Создай Account `tenant-a`, `tenant-b` с разными JWT. Покажи, что `tenant-a` не видит `tenant-b` subjects.

---

### 97. RabbitMQ — основы и AMQP

**Упражнение 20.26.** Установи RabbitMQ (`docker run -p 5672:5672 -p 15672:15672 rabbitmq:3-management`). Подключись через `github.com/rabbitmq/amqp091-go`: `conn, _ := amqp.Dial("amqp://guest:guest@localhost:5672/")`. Создай channel: `ch, _ := conn.Channel()`.

**Упражнение 20.27.** Напиши **Direct exchange**: `ch.ExchangeDeclare("logs.direct", "direct", true, false, false, false, nil)`. Публикуй с routing key `"error"`: `ch.Publish("logs.direct", "error", false, false, amqp.Publishing{Body: []byte("critical!")})`. Queue `q1` binds to `"error"` — получит. `q2` binds to `"info"` — не получит.

**Упражнение 20.28.** Напиши **Fanout exchange** (broadcast): `ch.ExchangeDeclare("notifications", "fanout", ...)`. Все bound queues получают копию. Покажи: 3 queues bound → 3 копии сообщения.

**Упражнение 20.29.** Напиши **Topic exchange**: `ch.ExchangeDeclare("orders.topic", "topic", ...)`. Routing keys: `"orders.us.electronics"`, `"orders.eu.books"`. Bind queue с pattern `"orders.us.#"` (всё из US) или `"orders.*.electronics"` (electronics в любом регионе). Покажи гибкость.

**Упражнение 20.30.** Напиши **Headers exchange**: `ch.ExchangeDeclare("logs.headers", "headers", ...)`. Bind с `amqp.Table{"x-match": "all", "format": "json", "type": "log"}`. Публикация с `Headers: amqp.Table{"format": "json", "type": "log"}` — match. С `"format": "xml"` — no match. Покажи сложный routing по metadata.

**Упражнение 20.31.** Реализуй **Work Queue (competing consumers)**: Queue без exchange (default exchange, routing key = queue name). 3 consumers на одной queue. RabbitMQ round-robins сообщения. Покажи load balancing.

**Упражнение 20.32.** Напиши **Message acknowledgment**: `autoAck := false` в `ch.Consume`. Обработай сообщение, затем `d.Ack(false)` (ack только это сообщение). Если consumer упал до `Ack` — сообщение redeliver'ится другому consumer'у. Покажи reliability.

**Упражнение 20.33.** Напиши **Negative acknowledgment + requeue**: `d.Nack(false, true)` — requeue this message (возвращает в очередь). `d.Nack(false, false)` — discard (или в DLX). Покажи retry logic: `Nack` + requeue → повторная обработка.

**Упражнение 20.34.** Напиши **Prefetch (QoS)**: `ch.Qos(10, 0, false)` — не более 10 unacknowledged сообщений на consumer. Покажи, что без QoS RabbitMQ отправит все сообщения сразу, перегружая медленный consumer.

**Упражнение 20.35.** Реализуй **Dead Letter Exchange (DLX)**: Queue args: `amqp.Table{"x-dead-letter-exchange": "orders.dlx", "x-dead-letter-routing-key": "failed"}`. При `Nack(false, false)` или message TTL expired — сообщение идёт в DLX. Consumer DLX обрабатывает/логирует/alert'ит.

**Упражнение 20.36.** Напиши **Message TTL**: Queue args `{"x-message-ttl": 60000}` — сообщение удаляется через 60s если не обработано. Или per-message: `amqp.Publishing{Expiration: "60000"}`. Покажи timeout для time-sensitive задач.

**Упражнение 20.37.** Напиши **Queue TTL**: `{"x-expires": 1800000}` — queue удаляется через 30 мин без consumers. Покажи auto-cleanup временных queues.

**Упражнение 20.38.** Реализуй **Priority Queue**: Queue args `{"x-max-priority": 10}`. Публикация: `amqp.Publishing{Priority: 5}`. Покажи, что сообщения с higher priority обрабатываются раньше (при наличии consumers).

**Упражнение 20.39.** Напиши **Connection recovery**: `conn.NotifyClose(make(chan *amqp.Error))` — слушай disconnect. При close — reconnect с exponential backoff. Recreate channel, redeclare exchanges/queues (idempotent). Покажи resilience.

**Упражнение 20.40.** Реализуй **Publisher Confirms**: `ch.Confirm(false)`, `confirms := ch.NotifyPublish(make(chan amqp.Confirmation, 1))`. После `Publish` жди `<-confirms`, проверь `ack.DeliveryTag`. Покажи exactly-once publishing (с idempotent consumer).

---

### 98. RabbitMQ — production patterns

**Упражнение 20.41.** Напиши **RPC over RabbitMQ**: Client создаёт exclusive callback queue. Публикует в `"rpc_queue"` с `ReplyTo: callbackQueue` и `CorrelationId: uuid`. Server обрабатывает, публикует ответ в `ReplyTo`. Client ждёт сообщение с matching `CorrelationId`. Покажи request-reply без HTTP.

**Упражнение 20.42.** Реализуй **Transaction Outbox с RabbitMQ**: PostgreSQL транзакция: `INSERT INTO outbox ...`. Poller: `SELECT * FROM outbox`, `ch.Publish`, `DELETE FROM outbox`. Покажи, что outbox гарантирует at-least-once (если publish fail — retry, если publish success + delete fail — duplicate, consumer идемпотентен).

**Упражнение 20.43.** Напиши **Event-driven microservices с RabbitMQ**: `UserService` публикует `UserCreated` в `"events.user.created"`. `NotificationService` и `AnalyticsService` — отдельные queues, bound to fanout exchange `"events"`. Покажи decoupling: добавь `AuditService` без изменения `UserService`.

**Упражнение 20.44.** Реализуй **Saga Choreography через RabbitMQ**: каждый сервис слушает события, выполняет шаг, публикует следующее событие. `OrderCreated` → `InventoryService` резервирует → `InventoryReserved` → `PaymentService` списывает → `PaymentProcessed` → `ShippingService` отправляет. Compensation: `PaymentFailed` → `InventoryService` освобождает.

**Упражнение 20.45.** Напиши **Circuit Breaker для RabbitMQ consumer'а**: если обработка 5 сообщений подряд падает — пауза 30 секунд (не consume). Потом пробный consume. Если успех — resume. Используй `ch.Cancel` + `time.Timer` + `ch.Consume`.

---

### 99. Apache Kafka — основы

**Упражнение 20.46.** Установи Kafka (`docker-compose` с ZooKeeper/KRaft или `docker run` Redpanda для development). Подключись через `github.com/segmentio/kafka-go`: `conn, _ := kafka.DialLeader(context.Background(), "tcp", "localhost:9092", "topic-1", 0)`. Напиши сообщение: `conn.WriteMessages(kafka.Message{Value: []byte("hello")})`.

**Упражнение 20.47.** Напиши **Producer** через `kafka.Writer`: `w := &kafka.Writer{Addr: kafka.TCP("localhost:9092"), Topic: "orders", Balancer: &kafka.LeastBytes{}, RequiredAcks: kafka.RequireAll, Async: false}`. `w.WriteMessages(ctx, kafka.Message{Key: []byte("order-123"), Value: data})`. Покажи настройки durability vs performance.

**Упражнение 20.48.** Напиши **Consumer** через `kafka.Reader`: `r := kafka.NewReader(kafka.ReaderConfig{Brokers: []string{"localhost:9092"}, Topic: "orders", GroupID: "order-processors", MinBytes: 10e3, MaxBytes: 10e6, MaxWait: time.Second})`. Читай `r.ReadMessage(ctx)`, обрабатывай, `r.CommitMessages(ctx, m)` (manual commit).

**Упражнение 20.49.** Покажи **Consumer Groups**: запусти 3 инстанса с одинаковым `GroupID`. Kafka распределяет partitions между consumers (1 partition = 1 consumer max). Добавь 4-й инстанс — он будет idle (если partitions < consumers). Убери 1-й — rebalance, partitions перераспределяются.

**Упражнение 20.50.** Напиши **Partitioning strategy**: `Balancer: &kafka.Hash{}` — сообщения с одинаковым Key идут в один partition (ordering guarantee). `Balancer: &kafka.RoundRobin{}` — равномерное распределение. Покажи, когда нужен Key (ordering) vs RoundRobin (load balancing).

**Упражнение 20.51.** Реализуй **Exactly-once semantics (EOS)**: Producer ` transactional.id = "order-producer-1"`, `InitTransactions`, `BeginTransaction`, `Send`, `SendOffsetsToTransaction`, `CommitTransaction`. Consumer `isolation.level = read_committed` — не видит uncommitted сообщений. Покажи atomic publish + consume.

**Упражнение 20.52.** Напиши **Consumer lag monitoring**: `r.Stats()` — `Lag` per partition. Алерт если `Lag > 10000` (consumer не справляется). Scale consumers или оптимизируй обработку. Покажи метрики в Prometheus.

**Упражнение 20.53.** Реализуй **Dead Letter Topic**: Consumer пытается обработать 3 раза (с `Nack` + sleep). После 3 попыток — `w.WriteMessages(ctx, kafka.Message{Topic: "orders.dlq", Key: m.Key, Value: m.Value, Headers: append(m.Headers, kafka.Header{Key: "x-fail-reason", Value: []byte(err.Error())})})`. DLQ consumer — alert + manual investigation.

**Упражнение 20.54.** Напиши **Topic compaction**: `kafka.Conn.CreateTopics(kafka.TopicConfig{Topic: "user-sessions", NumPartitions: 1, ReplicationFactor: 1, ConfigEntries: []kafka.ConfigEntry{{ConfigName: "cleanup.policy", ConfigValue: "compact"}}})`. Покажи, что compaction хранит только последнее сообщение per Key (для stateful topics).

**Упражнение 20.55.** Реализуй **Kafka Streams-like processing** (вручную): читай из `orders` topic, агрегируй (sum по `user_id` за 5 минутное окно), пиши в `orders-aggregated`. Используй `kafka.Reader` + `kafka.Writer` + `time.Ticker` для windowing.

---

### 100. Kafka — Connect, Schema Registry и продакшн

**Упражнение 20.56.** Настрой **Schema Registry** (Confluent): `docker run -p 8081:8081 confluentinc/cp-schema-registry`. Зарегистрируй Avro schema для `Order`: `POST /subjects/orders-value/versions`. Producer сериализует через `github.com/riferrei/srclient`. Consumer десериализует с проверкой schema compatibility.

**Упражнение 20.57.** Покажи **Schema Evolution**: v1 `Order { id: long, total: double }`. v2 добавляет `status: string` (optional, default `"pending"`). Compatibility `BACKWARD`: новый consumer читает старые сообщения (default value). `FORWARD`: старый consumer читает новые (ignores unknown field). `FULL`: оба направления.

**Упражнение 20.58.** Настрой **Kafka Connect**: Source Connector (Debezium) читает CDC из PostgreSQL (`wal2json`), публикует в `dbserver1.public.orders`. Sink Connector пишет из Kafka в Elasticsearch. Покажи zero-code data pipeline.

**Упражнение 20.59.** Напиши **Kafka Admin operations**: `admin := kafka.NewClient(kafka.ClientConfig{...})`. `CreateTopics`, `DeleteTopics`, `ListTopics`, `DescribeGroups`. Покажи programmatic management.

**Упражнение 20.60.** Реализуй **Multi-region Kafka**: MirrorMaker 2 или `kafka-reassign-partitions`. Topic `orders` replicated в 2 датацентра. Producer пишет в local DC. Consumer читает из local DC (follower fetch). Failover: при падении DC1 — DC2 продолжает.

**Упражнение 20.61.** Напиши **Kafka security**: SASL/SCRAM аутентификация (`sasl.Mechanism = "SCRAM-SHA-256"`), TLS (`Dialer.TLS = &tls.Config{...}`). ACL: `User:alice` может писать в `orders`, `User:bob` может читать из `orders`. Покажи production security.

**Упражнение 20.62.** Реализуй **Kafka monitoring**: JMX metrics → Prometheus (kafka_exporter). Dashboard: `kafka_server_brokertopicmetrics_messagesin_total`, `kafka_consumer_group_lag`, `kafka_network_request_total`. Алерты на lag, offline partitions, under-replicated partitions.

---

### 101. Сравнение и выбор MQ

**Упражнение 20.63.** Напиши **бенчмарк**: одинаковый сценарий (publish 1M messages, 1KB each, consume all). Сравни NATS Core, NATS JetStream, RabbitMQ, Kafka. Метрики: throughput (msg/s), latency p50/p99, CPU/memory usage, disk usage.

**Упражнение 20.64.** Напиши **decision matrix**: когда какой MQ. NATS Core — simple pub/sub, low latency, no persistence. NATS JetStream — persistence, streams, KV, simple ops. RabbitMQ — complex routing, AMQP, enterprise. Kafka — high throughput, log compaction, stream processing, big data.

**Упражнение 20.65.** Реализуй **Multi-protocol bridge**: HTTP API принимает заказ, публикует в NATS JetStream. NATS consumer читает, публикует в RabbitMQ (для legacy сервиса). RabbitMQ consumer читает, публикует в Kafka (для analytics). Покажи interoperability.

---

### 102. Комплексные production-задачи

**Упражнение 20.66.** Реализуй **Event-driven Order Processing** (NATS JetStream):
- `OrderService`: `CreateOrder` → публикует `OrderCreated` в `orders.events`
- `InventoryService`: consumer `orders.events`, резервирует stock → `InventoryReserved` или `InventoryInsufficient`
- `PaymentService`: consumer `InventoryReserved`, списывает средства → `PaymentProcessed` или `PaymentFailed`
- `ShippingService`: consumer `PaymentProcessed`, создаёт shipment
- `NotificationService`: consumer всех events, отправляет email/SMS
- DLQ для каждого сервиса, retry 3x, then human intervention

**Упражнение 20.67.** Реализуй **CQRS с Kafka**:
- Command side: HTTP/gRPC → `CreateOrder` → PostgreSQL + Debezium CDC → Kafka `orders.events`
- 3 Read Models:
  - Elasticsearch: `OrderSearchView` (full-text search)
  - Redis: `OrderCache` (by ID, TTL 1 hour)
  - ClickHouse: `OrderAnalytics` (aggregations, time-series)
- Each — separate consumer group, own pace, replay capability

**Упражнение 20.68.** Реализуй **Saga Orchestrator с RabbitMQ**:
- Orchestrator service: state machine (pending → reserved → paid → shipped)
- Commands: `ReserveInventory`, `ProcessPayment`, `CreateShipment`
- Events: `InventoryReserved`, `PaymentProcessed`, `ShipmentCreated`
- Compensation: `ReleaseInventory`, `RefundPayment`, `CancelShipment`
- State в PostgreSQL, outbox pattern для commands

**Упражнение 20.69.** Реализуй **Real-time Analytics Pipeline** (Kafka):
- `WebsiteEvents` → Kafka (clickstream, 100K events/sec)
- `Kafka Streams` (или ручной consumer): windowed aggregation (5 min), unique users (HyperLogLog), page views
- Output: `analytics.5min` topic → ClickHouse/Redis for dashboard
- Backfill: replay from `analytics.5min` for any time range

**Упражнение 20.70.** Реализуй **Multi-tenant Message Queue** (NATS):
- Account per tenant, JWT-based auth
- Subject isolation: `tenant-a.orders.>`, `tenant-b.orders.>`
- Shared stream `ORDERS` с subject mapping: `orders.>` captures all, `orders.{tenant}.>` per tenant
- Resource quotas: max messages/sec, max storage per tenant

**Упражнение 20.71.** Реализуй **Message Queue as a Service** (внутренний):
- HTTP API: `POST /queues/{name}/messages` — publish
- `GET /queues/{name}/messages` — consume (long-polling)
- WebSocket: real-time subscribe
- Backend: NATS JetStream (persistence), Redis (caching), PostgreSQL (audit)
- Multi-protocol: HTTP, gRPC, WebSocket, AMQP

**Упражнение 20.72.** Реализуй **Disaster Recovery для MQ**:
- Kafka: MirrorMaker 2, replication factor 3, min.insync.replicas 2
- NATS: Stream replication (clustered JetStream), snapshot + restore
- RabbitMQ: federation, shovel, mirrored queues (quorum queues)
- RPO: 0 (synchronous replication) vs 5 min (asynchronous)
- RTO: automatic failover vs manual switchover

**Упражнение 20.73.** Реализуй **GDPR-compliant Event Log**:
- Encryption at rest: AES-256 для сообщений в Kafka
- Encryption in transit: TLS 1.3
- Right to be forgotten: `crypto-shredding` (удаление ключа шифрования для пользователя) или `compaction` + tombstone
- Audit log: кто читал/писал какие сообщения
- Data retention: 30 days default, then delete or anonymize

**Упражнение 20.74.** Реализуй **Chaos Engineering для MQ**:
- `pumba`/`chaos-mesh`: network partition between Kafka brokers
- Kill leader broker → election → no data loss (if min.insync.replicas met)
- Kill consumer → rebalance → continue from last committed offset
- Inject latency → consumer lag grows → auto-scale consumers (HPA)

**Упражнение 20.75.** Реализуй **Observability for MQ**:
- Distributed tracing: OpenTelemetry, each produce/consume = span, trace across services
- Metrics: Prometheus, `mq_messages_produced_total`, `mq_messages_consumed_total`, `mq_consumer_lag`, `mq_publish_latency_seconds`
- Logging: structured JSON, correlation ID, message metadata
- Alerting: lag > threshold, dead letter queue growth, broker disk > 80%

**Упражнение 20.76.** Реализуй **Message Queue Testing**:
- Unit: mock `kafka.Writer`/`kafka.Reader` через интерфейсы
- Integration: testcontainers Kafka/RabbitMQ/NATS, real produce/consume
- Contract: Pact или ручной — producer test публикует, consumer test проверяет schema
- Load: `kafka-producer-perf-test`, `kafka-consumer-perf-test`, custom k6 script
- Chaos: randomly kill brokers/consumers, verify no data loss

**Упражнение 20.77.** Реализуй **Migration from RabbitMQ to Kafka**:
- Dual-write: пиши в обе системы (RabbitMQ для legacy, Kafka для new)
- Change data capture: RabbitMQ → Kafka Connect source
- Gradual cutover: consumers мигрируют с RabbitMQ на Kafka
- Verification: сравни сообщения в обеих системах
- Rollback plan: если Kafka fail → вернуться на RabbitMQ

**Упражнение 20.78.** Реализуй **Serverless Event Processing**:
- AWS Lambda / Google Cloud Functions triggered by Kafka/NATS events
- Function: `func HandleEvent(ctx context.Context, event kafka.Message) error`
- Cold start optimization: keep-alive, provisioned concurrency
- Idempotency: deduplication by event ID, store processed IDs in DynamoDB/Redis
- Error handling: DLQ for failed invocations, retry with exponential backoff

**Упражнение 20.79.** Реализуй **IoT Message Processing** (NATS):
- 1M устройств, каждое публикует telemetry каждые 10 секунд
- NATS Core для real-time (low latency, no persistence)
- NATS JetStream для analytics (persistence, replay)
- Subject design: `telemetry.{device_type}.{device_id}.{sensor_type}`
- Aggregation: windowed average per device type, alert on anomaly

**Упражнение 20.80.** Реализуй **Financial Transaction Processing** (Kafka):
- Exactly-once semantics: EOS producer, transactional consumer
- Ordering: `account_id` as partition key → all transactions for account in order
- Idempotency: `transaction_id` deduplication in consumer
- Audit: immutable log, all transactions forever, compacted by account
- Compliance: SOX, PCI-DSS, encryption, access control

**Упражнение 20.81.** Реализуй **Real-time ML Inference Pipeline** (Kafka):
- `RawEvents` → `FeatureEngineering` (Kafka Streams) → `Features` topic
- `ModelInference` consumer: reads features, calls ML model (TensorFlow Serving, ONNX Runtime), publishes `Predictions`
- `DecisionEngine` consumer: reads predictions, makes business decision, publishes `Actions`
- Latency budget: end-to-end < 100ms p99

**Упражнение 20.82.** Реализуй **Blockchain Event Indexing** (Kafka/NATS):
- Ethereum node emits events → Web3.js → publish to `eth.events.{contract_address}`
- Consumer: decode event logs, index in Elasticsearch
- Real-time: NFT transfer notification, DeFi liquidation alert
- Replay: re-index from block 0 for new feature

**Упражнение 20.83.** Реализуй **Gaming Event Pipeline** (NATS):
- Game server publishes: `player.move`, `player.shoot`, `match.end`
- Real-time: matchmaking service subscribes to `player.online`, updates Redis
- Analytics: daily active users, retention, monetization (Kafka → ClickHouse)
- Replay: spectator mode, replay match from events

**Упражнение 20.84.** Реализуй **Healthcare HL7/FHIR Messaging** (RabbitMQ):
- Hospital systems emit HL7 messages → RabbitMQ
- Router: `headers` exchange by message type (`ADT^A01`, `ORU^R01`)
- Consumers: EHR system, lab system, billing system
- Compliance: HIPAA, audit log, encryption, access control
- Guaranteed delivery: persistent messages, publisher confirms, consumer ack

**Упражнение 20.85.** Реализуй **Cross-cloud Message Bridge**:
- AWS SNS/SQS ↔ NATS ↔ GCP Pub/Sub ↔ Azure Event Grid
- Universal message format: CloudEvents spec
- Transformation: schema conversion, protocol adaptation
- Reliability: at-least-once, deduplication, ordering guarantees per channel

---

## Итоговая сводка полного курса

| Часть | Тема | Количество |
|-------|------|-----------|
| I | Пакеты и модули | 35 |
| II | Ввод/вывод (fmt) | 15 |
| III | Типы, переменные, константы | 30 |
| III-A | Условные конструкции и циклы | 40 |
| VI | Массивы и слайсы | 30 |
| VII | Мапы | 20 |
| VIII | Структуры | 25 |
| IX | Интерфейсы | 30 |
| X | Обработка ошибок | 20 |
| XI | Логирование | 15 |
| XII | ООП в Go | 45 |
| XIII | Дженерики | 55 |
| XIV | Конкурентность | 125 |
| XV | Работа с сетью | 90 |
| XVI | Работа с БД (PostgreSQL + Redis) | 85 |
| XVII | Тестирование | 110 |
| XVIII | gRPC и микросервисы | 95 |
| XIX | GraphQL и WebSockets | 40 |
| **XX** | **Message Queues (Kafka, RabbitMQ, NATS)** | **85** |
| IV | Функции и указатели | 40 |
| V | Работа с файлами | 25 |
| **ИТОГО** | | **1075** |

---

### Ключевые моменты для освоения Message Queues:

1. **Выбор MQ — trade-offs**: NATS Core — простота и скорость, но no persistence. JetStream — persistence + streams, но сложнее. RabbitMQ — маршрутизация и AMQP, но throughput ниже Kafka. Kafka — throughput и log compaction, но latency выше, ops сложнее.

2. **At-least-once vs exactly-once**: exactly-once требует idempotent consumer + transactional producer. В production чаще at-least-once + deduplication.

3. **Ordering**: Kafka — ordering в partition (key-based). NATS JetStream — ordering в stream. RabbitMQ — ordering в queue (но не между queues). Проектируй subject/topic/partition key с учётом ordering requirements.

4. **Backpressure**: consumer не справляется → lag растёт. Решения: scale consumers (но max = partitions в Kafka), optimize processing, drop old messages (TTL), or circuit breaker.

5. **Observability**: lag — главная метрика. Алерт на lag, DLQ growth, broker disk. Distributed tracing через сообщения — must have для debug.

6. **Schema evolution**: используй Schema Registry (Avro/Protobuf/JSON Schema). BACKWARD compatibility — новый consumer читает старые данные. FORWARD — старый consumer читает новые. FULL — оба направления.

7. **Disaster Recovery**: replication factor ≥ 3, min.insync.replicas ≥ 2. MirrorMaker / federation для multi-region. Тестируй failover регулярно.

8. **Security**: TLS для transit, SASL/SCRAM или mTLS для auth. ACL на topic/subject level. Encryption at rest для sensitive data.

Если нужно — следующий этап: **serverless (AWS Lambda, Cloud Functions), WebAssembly (WASI, TinyGo), embedded systems, blockchain/Web3 (go-ethereum, Cosmos SDK), ML/AI (Go + ONNX, TensorFlow), game dev (Ebitengine, Raylib), CLI tools (Cobra, Viper, Bubble Tea TUI)**. 🚀
