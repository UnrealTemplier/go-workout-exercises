Переход к микросервисной архитектуре и gRPC — это логичный следующий шаг для Go-разработчика. Go создан для таких задач, а gRPC является стандартом де-факто для взаимодействия между сервисами.

Здесь **37 упражнений** (с 471 по 507), которые проведут тебя от описания Protocol Buffers до построения отказоустойчивых микросервисов с перехватчиками (interceptors), балансировкой нагрузки и тестированием.

---

### Блок 46: Protocol Buffers (Protobuf) — Контракты данных
*Цель: Освоить язык описания интерфейсов (IDL), сгенерировать Go-код из `.proto` файлов.*

471. Установи компилятор `protoc` и плагины для Go: `protoc-gen-go` и `protoc-gen-go-grpc`.
472. Создай файл `user.proto`. Опиши сообщение `User` с полями `id` (int32), `name` (string), `age` (int32).
473. Скомпилируй `.proto` файл в Go-код. Изучи сгенерированные файлы `user.pb.go` (структуры) и `user_grpc.pb.go` (интерфейсы клиента и сервера).
474. Добавь в `.proto` файл enum `Role` (UNKNOWN, ADMIN, USER). Включи его в сообщение `User`. Перекомпилируй.
475. **[Каверзный кейс]** В Protobuf поля могут быть помечены как `optional`. Сделай поле `age` опциональным. Сгенерируй код и посмотри, как в Go это превращается в указатель (`*int32`).
476. Добавь сообщение `GetUserRequest` с полем `id`. Опиши сервис `UserService` с методом `GetUser`, который принимает запрос и возвращает `User`. Скомпилируй.
477. Используй тип `google.protobuf.Timestamp` для поля `created_at` в сообщении `User`. Изучи, как работать с этим типом в Go (конвертация `time.Time` в `timestamppb.Timestamp`).

### Блок 47: gRPC — Unary RPC (Базовый клиент и сервер)
*Цель: Поднять gRPC-сервер, реализовать простые методы (запрос-ответ) и написать клиента.*

478. Реализуй интерфейс `UserServiceServer` в Go-структуре. Напиши логику для метода `GetUser` (просто возвращай захардкоженного пользователя).
479. Создай gRPC-сервер: `grpc.NewServer()`, зарегистрируй свой сервис (`RegisterUserServiceServer`), запусти прослушивание TCP-порта (`net.Listen`).
480. Напиши gRPC-клиента. Создай соединение `grpc.Dial` (или `grpc.NewClient` в новых версиях). Создай клиента `NewUserServiceClient`. Вызови `GetUser` и выведи результат.
481. **[Каверзный кейс — Контексты]** Передай в клиентский вызов `context.WithTimeout(ctx, 1*time.Second)`. На стороне сервера добавь `time.Sleep(2*time.Second)`. Убедись, что клиент отваливается по таймауту с ошибкой `DeadlineExceeded`. Обработай эту ошибку на клиенте (используй `status.Code(err)`).

### Блок 48: gRPC — Стриминг (Streaming RPC)
*Цель: Освоить передачу потоков данных между сервисами.*

482. **Server Streaming:** Добавь в контракт метод `GetUsersByRole`, который принимает `Role` и возвращает `stream User`. Реализуй на сервере отправку нескольких пользователей через `stream.Send()`.
483. Напиши клиент для Server Streaming: вызови метод и читай поток в цикле `for { user, err := stream.Recv(); if err == io.EOF { break } ... }`.
484. **Client Streaming:** Добавь метод `UploadUsers`, принимающий `stream User` и возвращающий ответ с количеством загруженных. Реализуй на сервере чтение потока через `stream.Recv()`.
485. Напиши клиент для Client Streaming: отправь 5 пользователей через `stream.Send()` в цикле, затем вызови `stream.CloseAndRecv()` для получения ответа.
486. **Bidirectional Streaming:** Добавь метод `Chat`, принимающий и возвращающий `stream Message`. Реализуй echo-сервер: горутина читает из `stream.Recv()` и тут же отправляет обратно через `stream.Send()`.
487. **[Каверзный кейс]** В Bidirectional Streaming клиент должен отправлять и получать сообщения одновременно. Напиши клиент, который в одной горутине читает входящие сообщения, а в другой — отправляет (например, читая текст из консоли `bufio.Scanner`).

### Блок 49: Обработка ошибок (Rich Error Model)
*Цель: Возвращать из gRPC не просто коды ошибок, а структурированные детали.*

488. Верни из метода сервера стандартную ошибку: `status.Errorf(codes.NotFound, "user %d not found", id)`. На клиенте проверь, что `status.Code(err)` равен `codes.NotFound`.
489. **[Высокая сложность]** Используй `google.golang.org/genproto/googleapis/rpc/errdetails`. Создай ошибку `BadRequest` с деталями о том, какие именно поля некорректны (FieldViolation).
490. На стороне сервера упакуй детали в ошибку через `status.New(codes.InvalidArgument, "...").WithDetails(&errdetails.BadRequest{...})`.
491. На стороне клиента извлеки детали из ошибки: `st, _ := status.FromError(err); details := st.Details()`. Приведи `details[0]` к типу `*errdetails.BadRequest` и выведи информацию о нарушенных полях.

### Блок 50: Перехватчики (Interceptors) — Middleware для gRPC
*Цель: Логировать запросы, проверять авторизацию и восстанавливаться после паник.*

492. **Unary Server Interceptor:** Напиши функцию-логгер, которая выводит метод запроса и время его выполнения. Зарегистрируй её через `grpc.UnaryInterceptor()`.
493. **Auth Interceptor:** Напиши перехватчик, который достает из метаданных запроса (`metadata.FromIncomingContext`) токен. Если токен не равен "secret", прерываем запрос через `handler` не вызывается, возвращаем `codes.Unauthenticated`.
494. **Recovery Interceptor:** Напиши перехватчик с `defer func() { if r := recover(); r != nil { ... } }()`. Сымитируй панику в обработчике. Убедись, что сервер не падает, а клиент получает `codes.Internal`.
495. Зарегистрируй цепочку из трех интерсепторов (Recover -> Logging -> Auth) с помощью `grpc.ChainUnaryInterceptor()`.
496. **[Каверзный кейс]** Stream Interceptor: Напиши перехватчик для стриминга. Оберни входящий `grpc.ServerStream` в свою структуру, которая логирует каждый вызов `SendMsg` и `RecvMsg`.
497. **Unary Client Interceptor:** Напиши перехватчик на стороне клиента, который автоматически добавляет заголовок `Authorization: Bearer secret` в метаданные каждого запроса.

### Блок 51: Экосистема микросервисов (Health, Reflection, Gateway)
*Цель: Сделать сервис готовым к продакшену (k8s, отладка, REST API).*

498. Подключи `google.golang.org/grpc/health` и `google.golang.org/grpc/health/grpc_health_v1`. Зарегистрируй Health-сервис. Установи статус сервиса в `SERVING`. Проверь здоровье через `grpcurl`.
499. Подключи Server Reflection (`reflection.Register(server)`). Используй утилиту `grpcurl` (поставь через brew/go install), чтобы вызвать метод твоего сервиса без написания Go-кода клиента.
500. **[Высокая сложность — gRPC Gateway]** Сгенерируй reverse-proxy с помощью `protoc-gen-grpc-gateway`. Настрой аннотации `google.api.http` в `.proto` файле (например, `get: "/v1/users/{id}"`). Подними HTTP-сервер, который переводит REST-запросы в gRPC-вызовы. Сделай запрос через `curl`.
501. **[Тестирование через bufconn]** Используй `google.golang.org/grpc/test/bufconn` для запуска gRPC-сервера в памяти (без занятия реального TCP-порта). Напиши клиент, который коннектится к этому in-memory серверу. Запусти юнит-тест.

### Блок 52: Взаимодействие микросервисов и Отказоустойчивость
*Цель: Научиться связывать сервисы и обрабатывать падения соседей.*

502. **Service-to-Service:** Создай два gRPC-сервера: `OrderService` и `InventoryService`. `OrderService` при создании заказа делает gRPC-вызов в `InventoryService` для резервации товара.
503. **[Каверзный кейс — Graceful Shutdown]** Настрой gRPC-сервер на корректное завершение: при получении `os.Interrupt` вызови `server.GracefulStop()` (он дождется завершения активных стримов и запросов) в отдельной горутине, а в `main` подожди этого завершения через `sync.WaitGroup` или канал.
504. **[Load Balancing]** Запусти 3 экземпляра `InventoryService` на разных портах. На стороне клиента `OrderService` используй `resolver` и `grpc.WithDefaultServiceConfig` для балансировки по алгоритму `round_robin`. Убедись, что запросы распределяются между тремя инстансами (добавь логирование ID сервера).
505. **[Circuit Breaker]** Интегрируй библиотеку `github.com/sony/gobreaker` в gRPC-клиент (через Client Interceptor). Сымитируй падение `InventoryService`. Убедись, что после N неудачных попыток `OrderService` перестает слать запросы (Circuit переходит в состояние Open) и мгновенно возвращает ошибку, не ожидая таймаута сети.
506. **[Deadline Propagation]** Входящий запрос в `OrderService` имеет `context.Context` с дедлайном (timeout 2s). `OrderService` делает вызов в `InventoryService`, передавая *тот же самый* контекст. Убедись, что если `InventoryService` подвиснет, дедлайн сработает на обоих уровнях одновременно, а не будет удваиваться.
507. **[Финальное испытание — Mini-Microservices]**
    Собери систему из двух сервисов:
    1. `AuthService` (gRPC + gRPC Gateway): имеет метод `Login`, который проверяет пароль в PostgreSQL и возвращает JWT-токен.
    2. `TaskService` (gRPC): имеет метод `CreateTask`.
    - Напиши Client Interceptor для `TaskService`, который прикрепляет JWT токен.
    - Напиши Server Interceptor (Auth) для `TaskService`, который парсит JWT (через `github.com/golang-jwt/jwt`) и кладет userID в контекст.
    - Покрой `TaskService` Health-чеками и Server Reflection.
    - Настрой Graceful Shutdown для обоих сервисов.
    - Напиши юнит-тест для `TaskService` с использованием `bufconn` и замоканным токеном в контексте.

---

Твой список теперь состоит из **507 упражнений**.
Пройдя этот путь, ты пройдешь эволюцию от "Hello World" до архитектора распределенных систем на Go. Ты освоишь сырую конкурентность, сети, БД, тестирование и современный микросервисный стек (gRPC, Protobuf, Interceptors, Resiliency). Это максимум, что можно дать в рамках практического intensive-курса. Удачи в покорении архитектуры!
