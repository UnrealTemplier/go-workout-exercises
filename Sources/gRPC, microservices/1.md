Вот обширный блок из **95 упражнений** по gRPC и микросервисной архитектуре в Go. Вставляй его **после сети, БД и тестирования** — gRPC активно использует контекст, protobuf, интерфейсы и конкурентность.

---

## Часть XVIII. gRPC и микросервисы

### 79. Protocol Buffers — основы

**Упражнение 18.1.** Установи protoc и Go-плагины: `protoc`, `protoc-gen-go`, `protoc-gen-go-grpc`. Создай файл `user.proto` с `syntax = "proto3"; package user;`. Определи сообщение `User { int64 id = 1; string name = 2; string email = 3; }`. Сгенерируй Go-код: `protoc --go_out=. --go_opt=paths=source_relative user.proto`.

**Упражнение 18.2.** Добави в `user.proto` поле `google.protobuf.Timestamp created_at = 4;` (`import "google/protobuf/timestamp.proto";`). Сгенерируй код. Покажи, как работать с `timestamppb.Timestamp` в Go (конвертация `time.Time` ↔ `*timestamppb.Timestamp`).

**Упражнение 18.3.** Добави `enum Status { STATUS_UNSPECIFIED = 0; STATUS_ACTIVE = 1; STATUS_INACTIVE = 2; }` в `user.proto`. Сгенерируй код. Покажи, что в Go это тип `Status` с константами `Status_STATUS_UNSPECIFIED` и т.д. Объясни, почему `0` — `UNSPECIFIED` (best practice).

**Упражнение 18.4.** Добави **nested message**: `message User { message Address { string city = 1; string street = 2; } Address address = 5; }`. Сгенерируй код. Покажи доступ через `user.Address.City`.

**Упражнение 18.5.** Добави **repeated field**: `repeated string tags = 6;` и `repeated Address previous_addresses = 7;`. Сгенерируй код. Покажи, что в Go это слайс `[]string` и `[]*User_Address`.

**Упражнение 18.6.** Добави **oneof**: `oneof contact { string phone = 8; string telegram = 9; }`. Сгенерируй код. Покажи `GetContact()` — возвращает `interface{}`. Покажи type switch для определения, что выбрано.

**Упражнение 18.7.** Добави **map field**: `map<string, string> metadata = 10;`. Сгенерируй код. Покажи `map[string]string` в Go. Добави, удали, итерируй.

**Упражнение 18.8.** Добави **optional field** (proto3): `optional string nickname = 11;`. Сгенерируй код (требует `protoc` 3.15+). Покажи `HasNickname()` и `GetNickname()` — разница между отсутствием и пустой строкой.

**Упражнение 18.9.** Создай **JSON marshaling/unmarshaling** для protobuf: `protojson.Marshal(user)` и `protojson.Unmarshal(data, &user)`. Покажи, что имена полей в JSON — camelCase по умолчанию. Настрой через `json_name` опцию.

**Упражнение 18.10.** Создай **binary marshaling**: `proto.Marshal(user)` и `proto.Unmarshal(data, &user)`. Сравни размер binary vs JSON. Покажи, что binary — компактнее и быстрее.

**Упражнение 18.11.** Добави **custom options** в `.proto`: `import "google/protobuf/descriptor.proto";`. Создай `extend google.protobuf.MessageOptions { string table_name = 50001; }`. Используй в `User` как `option (table_name) = "users";`. Прочитай в Go через `proto.GetExtension`.

**Упражнение 18.12.** Создай **proto package с версионированием**: `package user.v1;` и `package user.v2;`. Покажи, как импортировать обе версии в одном Go-проекте (разные import paths). Обсуди стратегию обновления API.

---

### 80. gRPC — Unary RPC

**Упражнение 18.13.** Определи **gRPC service** в `.proto`: `service UserService { rpc GetUser(GetUserRequest) returns (User); }`. Сгенерируй gRPC код: `protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative user.proto`.

**Упражнение 18.14.** Реализуй **gRPC сервер**: `type server struct { pb.UnimplementedUserServiceServer }`. Реализуй `GetUser(ctx context.Context, req *pb.GetUserRequest) (*pb.User, error)`. Зарегистрируй: `pb.RegisterUserServiceServer(grpcServer, &server{})`. Слушай на `:50051`.

**Упражнение 18.15.** Реализуй **gRPC клиент**: `conn, err := grpc.Dial("localhost:50051", grpc.WithTransportCredentials(insecure.NewCredentials()))`. Создай `client := pb.NewUserServiceClient(conn)`. Вызови `client.GetUser(ctx, &pb.GetUserRequest{Id: 1})`.

**Упражнение 18.16.** Добави **error handling** в сервер: если пользователь не найден — верни `status.Error(codes.NotFound, "user not found")`. В клиенте проверь `status.Code(err) == codes.NotFound`. Используй `status.FromError(err)` для извлечения деталей.

**Упражнение 18.17.** Добави **rich error** через `google.golang.org/genproto/googleapis/rpc/errdetails`: `st := status.New(codes.InvalidArgument, "invalid email")`, `ds, _ := st.WithDetails(&errdetails.BadRequest{...})`. Верни `ds.Err()`. В клиенте извлеки детали через `status.FromError` + `details := s.Details()`.

**Упражнение 18.18.** Добави **deadline/timeout**: в клиенте `ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)`. В сервере проверяй `ctx.Err() == context.DeadlineExceeded` и верни `codes.DeadlineExceeded`.

**Упражнение 18.19.** Добави **metadata** (headers): в клиенте `md := metadata.Pairs("x-request-id", "123-abc")`, `ctx := metadata.NewOutgoingContext(ctx, md)`. В сервере читай `md, ok := metadata.FromIncomingContext(ctx)`. Верни metadata в ответе через `grpc.SetHeader`/`SetTrailer`.

**Упражнение 18.20.** Добави **interceptor (unary)**: `func loggingInterceptor(ctx context.Context, req any, info *grpc.UnaryServerInfo, handler grpc.UnaryHandler) (any, error)`. Логируй метод, длительность, ошибку. Зарегистрируй через `grpc.UnaryInterceptor(loggingInterceptor)`.

**Упражнение 18.21.** Добави **auth interceptor**: проверяй `authorization` metadata. Извлеки Bearer token, валидируй (заглушка). Если невалиден — верни `codes.Unauthenticated`. Примени `grpc.UnaryInterceptor(authInterceptor)` к серверу.

**Упражнение 18.22.** Добави **chain of interceptors**: `grpc.ChainUnaryInterceptor(loggingInterceptor, authInterceptor, recoveryInterceptor)`. Покажи порядок выполнения (внешний → внутренний → handler → обратно).

**Упражнение 18.23.** Добави **client interceptor**: `grpc.WithUnaryInterceptor(clientMetricsInterceptor)`. Считай количество запросов, latency. Отправь в Prometheus или логируй.

**Упражнение 18.24.** Реализуй **health check**: импортируй `grpc.health.v1`. Реализуй `HealthServer`. Клиент проверяет `healthClient.Check(ctx, &healthpb.HealthCheckRequest{Service: "user.UserService"})`. Верни `SERVING` или `NOT_SERVING`.

---

### 81. gRPC — Streaming

**Упражнение 18.25.** Определи **server streaming RPC**: `rpc ListUsers(ListUsersRequest) returns (stream User);`. Реализуй на сервере: `func (s *server) ListUsers(req *pb.ListUsersRequest, stream pb.UserService_ListUsersServer) error`. Отправляй `stream.Send(user)` в цикле.

**Упражнение 18.26.** Реализуй **client для server streaming**: `stream, err := client.ListUsers(ctx, &pb.ListUsersRequest{})`. Читай `for { user, err := stream.Recv(); if err == io.EOF { break } }`. Обработай ошибки.

**Упражнение 18.27.** Определи **client streaming RPC**: `rpc CreateUsers(stream User) returns (CreateUsersResponse);`. Реализуй на сервере: `func (s *server) CreateUsers(stream pb.UserService_CreateUsersServer) error`. Читай `for { user, err := stream.Recv(); ... }`. Считай количество, верни итог.

**Упражнение 18.28.** Реализуй **client для client streaming**: `stream, err := client.CreateUsers(ctx)`. Отправляй `stream.Send(user)` несколько раз. Закрой `resp, err := stream.CloseAndRecv()`.

**Упражнение 18.29.** Определи **bidirectional streaming**: `rpc Chat(stream ChatMessage) returns (stream ChatMessage);`. Реализуй на сервере: читай `Recv()` в одной горутине, отправляй `Send()` в другой. Обе горутины завершаются при `io.EOF` или ошибке.

**Упражнение 18.30.** Реализуй **bidirectional streaming клиент**: аналогично серверу — `Send()` и `Recv()` в разных горутинах. Синхронизируй завершение через `sync.WaitGroup` или `errgroup`.

**Упражнение 18.31.** Добави **flow control** в streaming: сервер отправляет 10000 сообщений. Клиент медленно читает. Покажи, что gRPC **backpressure** автоматически замедляет сервер (через HTTP/2 flow control). Замерь память сервера — она не растёт бесконечно.

**Упражнение 18.32.** Добави **cancellation в streaming**: клиент отменяет `ctx` посреди стрима. Покажи, что сервер получает `ctx.Done()` и корректно завершает отправку. Проверь через логи.

**Упражнение 18.33.** Реализуй **"heartbeat" в bidirectional stream**: клиент и сервер отправляют пинг каждые 5 секунд. Если пинг не получен за 15 секунд — закройте соединение. Используй `time.Ticker` + `select` с `ctx.Done()`.

---

### 82. gRPC — продвинутые темы

**Упражнение 18.34.** Настрой **TLS** для gRPC: сгенерируй сертификаты (self-signed или через mkcert). Сервер: `credentials.NewServerTLSFromFile("server.crt", "server.key")`. Клиент: `credentials.NewClientTLSFromFile("ca.crt", "")`. Подключись по TLS.

**Упражнение 18.35.** Настрой **mTLS** для gRPC: клиент предоставляет сертификат. Сервер проверяет `ClientAuth: tls.RequireAndVerifyClientCert`. Клиент загружает `client.crt` + `client.key` в `tls.Config.Certificates`.

**Упражнение 18.36.** Настрой **load balancing** в клиенте: `grpc.WithDefaultServiceConfig(`{"loadBalancingPolicy":"round_robin"}`)`. Используй **DNS resolver** с несколькими A-записями. Покажи распределение запросов.

**Упражнение 18.37.** Настрой **service mesh** (упрощённо): клиент коннектится к `localhost:50051` (sidecar proxy, например Envoy). Proxy маршрутизирует на реальные сервисы. Покажи, что клиент не знает о реальных адресах.

**Упражнение 18.38.** Реализуй **reflection**: импортируй `google.golang.org/grpc/reflection`. Зарегистрируй `reflection.Register(grpcServer)`. Используй `grpcurl` или `grpcui` для интроспекции API без `.proto` файлов.

**Упражнение 18.39.** Реализуй **graceful shutdown**: `grpcServer.GracefulStop()` — перестаёт принимать новые соединения, дожидается завершения активных RPC. Используй с `signal.NotifyContext`. Сравни с `grpcServer.Stop()` (принудительно).

**Упражнение 18.40.** Настрой **keepalive**: сервер `grpc.KeepaliveParams(keepalive.ServerParameters{MaxConnectionIdle: 15 * time.Minute})`. Клиент `grpc.WithKeepaliveParams(keepalive.ClientParameters{Time: 10 * time.Second, Timeout: 3 * time.Second})`. Покажи поддержание соединения.

**Упражнение 18.41.** Настрой **max message size**: `grpc.MaxRecvMsgSize(10 * 1024 * 1024)` (10MB). Отправь сообщение больше дефолта (4MB). Покажи ошибку `ResourceExhausted` без настройки.

**Упражнение 18.42.** Настрой **compression**: `grpc.UseCompressor(gzip.Name)` на клиенте. Покажи снижение трафика. Проверь, что сервер поддерживает тот же compressor (автоматически, если зарегистрирован).

**Упражнение 18.43.** Реализуй **retry policy** в клиенте: `grpc.WithDefaultServiceConfig(`{"methodConfig":[{"name":[{"service":"user.UserService"}],"retryPolicy":{"maxAttempts":3,"initialBackoff":"0.1s","maxBackoff":"1s","backoffMultiplier":2,"retryableStatusCodes":["UNAVAILABLE"]}}]}`)`. Покажи автоматический retry.

**Упражнение 18.44.** Реализуй **circuit breaker** в клиенте (ручной или через interceptor): при 5 ошибок подряд — возвращай `codes.Unavailable` быстро, не делая реальный запрос. Через 30s — пробный запрос. Используй `sync/atomic` для состояния.

---

### 83. Микросервисы — архитектура и паттерны

**Упражнение 18.45.** Раздели монолит на 3 сервиса: `user-service`, `order-service`, `notification-service`. Каждый — отдельный Go-модуль с собственным `go.mod`. Определи `.proto` для межсервисного общения.

**Упражнение 18.46.** Реализуй **API Gateway**: HTTP-сервер на `:8080`, маршрутизирует `/users/*` → `user-service:50051`, `/orders/*` → `order-service:50052`. Используй `grpc-gateway` или ручной `httputil.ReverseProxy` + gRPC клиент.

**Упражнение 18.47.** Настрой **service discovery** (упрощённо): Consul или etcd. Сервис регистрирует себя при старте (`Register` с TTL). Клиент резолвит адрес через `Lookup`. Реализуй health check для регистрации.

**Упражнение 18.48.** Реализуй **centralized configuration**: сервисы читают конфиг из etcd/Consul при старте. Подписка на изменения: при обновлении конфига — hot reload без перезапуска. Используй `watch` + канал.

**Упражнение 18.49.** Реализуй **distributed tracing** (OpenTelemetry): инструментируй gRPC interceptors. Каждый RPC создаёт span. Передавай `traceparent` через metadata. Собирай в Jaeger или Zipkin.

**Упражнение 18.50.** Реализуй **metrics** (Prometheus): `grpc_prometheus` для автоматических метрик RPC. Добавь кастомные: `order_created_total`, `user_login_duration_seconds`. Экспонируй `/metrics` HTTP endpoint.

**Упражнение 18.51.** Реализуй **structured logging** в микросервисах: каждый сервис логирует в JSON с `trace_id`, `span_id`, `service_name`. Используй `grpc_ctxtags` + `zap`/`slog`. Коррелируй логи между сервисами по `trace_id`.

**Упражнение 18.52.** Реализуй **Saga pattern** (упрощённый): `CreateOrder` вызывает `ReserveInventory` → `ProcessPayment` → `ShipOrder`. Если шаг падает — компенсируй предыдущие (`ReleaseInventory`, `RefundPayment`). Используй gRPC + координатор.

**Упражнение 18.53.** Реализуй **Outbox pattern**: сервис пишет событие в PostgreSQL (`outbox` таблица) в одной транзакции с бизнес-данными. Отдельный воркер читает outbox, отправляет в Kafka/NATS, удаляет/помечает как отправленное.

**Упражнение 18.54.** Реализуй **CQRS** в микросервисе: `Command` (Write) — gRPC, изменяет состояние. `Query` (Read) — HTTP/GraphQL/gRPC, читает из read model (Redis/Elasticsearch). Синхронизация через events.

**Упражнение 18.55.** Реализуй **Event Sourcing** (упрощённый): храни события `UserCreated`, `UserEmailChanged` в PostgreSQL (append-only). Текущее состояние — проекция событий. Воспроизведи состояние с любой точки.

---

### 84. Микросервисы — resilience паттерны

**Упражнение 18.56.** Реализуй **Retry** с exponential backoff + jitter для gRPC вызовов. Настрой через interceptor или вручную. Jitter предотвращает thundering herd при восстановлении сервиса.

**Упражнение 18.57.** Реализуй **Timeout** per call: `context.WithTimeout` для каждого gRPC вызова. Если сервис не ответил за 2s — fail fast. Не жди дефолтный таймаут TCP (минуты).

**Упражнение 18.58.** Реализуй **Circuit Breaker** для gRPC клиента: 3 состояния (Closed, Open, HalfOpen). При 5 ошибок подряд — Open. Через 10s — HalfOpen (1 пробный запрос). Используй `github.com/sony/gobreaker` или ручной.

**Упражнение 18.59.** Реализуй **Bulkhead** (ограничение ресурсов): сервис A вызывает B и C. Отдельные пулы горутин/каналов для B и C. Если B упал — C продолжает работать. Не исчерпай все ресурсы на одном зависимом сервисе.

**Упражнение 18.60.** Реализуй **Fallback**: если `GetUserFromService` падает — верни stale-данные из кеша. Если кеша нет — верни default/empty response. Не проваливай весь запрос из-за одного сервиса.

**Упражнение 18.61.** Реализуй **Rate Limiter** per client: `x-ratelimit-*` headers в gRPC metadata. Сервер отслеживает запросы по `client_id`. При превышении — `codes.ResourceExhausted`. Клиент адаптирует скорость.

**Упражнение 18.62.** Реализуй **Idempotency Key**: клиент генерирует `Idempotency-Key: uuid` для mutation-запросов. Сервер хранит обработанные ключи (Redis, TTL=24h). При повторе с тем же ключом — верни кешированный ответ, не выполняй повторно.

**Упражнение 18.63.** Реализуй **Dead Letter Queue (DLQ)**: воркер обрабатывает события из Kafka/NATS. При 3 неудачных попытках — отправляет в DLQ. Отдельный сервис анализирует DLQ, алертит, позволяет retry вручную.

---

### 85. Микросервисы — observability

**Упражнение 18.64.** Настрой **distributed logging**: каждый сервис пишет JSON-логи с `trace_id`, `span_id`, `service`, `timestamp`. Используй `context` для передачи trace_id между сервисами. Агрегируй в ELK/Loki.

**Упражнение 18.65.** Настрой **distributed tracing** (OpenTelemetry + Jaeger): инструментируй gRPC client/server interceptors. Каждый RPC — span. Смотри trace в Jaeger UI: timeline, зависимости, latency каждого hop.

**Упражнение 18.66.** Настрой **metrics** (Prometheus + Grafana): `grpc_server_handled_total`, `grpc_server_handling_seconds`, `grpc_client_handled_total`. Создай dashboard: RPS, latency p50/p95/p99, error rate.

**Упражнение 18.67.** Настрой **health checks**: Kubernetes liveness probe (`/healthz` — HTTP 200, сервис жив), readiness probe (`/ready` — сервис готов принимать трафик, например БД подключена). Разные endpoint'ы!

**Упражнение 18.68.** Настрой **profiling**: `net/http/pprof` endpoint'ы (`/debug/pprof/heap`, `/debug/pprof/goroutine`, `/debug/pprof/cpu`). Собирай профили в production (с осторожностью). Анализируй через `go tool pprof`.

**Упражнение 18.69.** Настрой **alerting**: Prometheus rules на `grpc_server_handled_total{grpc_code!="OK"} / grpc_server_handled_total > 0.05` (error rate > 5%). Alertmanager → Slack/PagerDuty.

---

### 86. Микросервисы — deployment и DevOps

**Упражнение 18.70.** Создай **Dockerfile** для gRPC-сервиса: multi-stage build (builder stage с `golang:1.24-alpine`, final stage `scratch` или `distroless`). Статическая линковка: `CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo`.

**Упражнение 18.71.** Создай **docker-compose.yml** для 3 сервисов + PostgreSQL + Redis + Jaeger. Настрой сети, volumes, health checks. Запусти `docker-compose up` и проверь взаимодействие.

**Упражнение 18.72.** Создай **Kubernetes manifests**: Deployment (3 реплики), Service (ClusterIP), Ingress (gRPC через nginx-ingress или Istio). Настрой HPA (Horizontal Pod Autoscaler) по CPU/memory.

**Упражнение 18.73.** Настрой **Istio service mesh**: sidecar proxy для каждого pod. mTLS между сервисами автоматически. Traffic splitting (canary deployment: 90% v1, 10% v2). Circuit breaker на уровне mesh.

**Упражнение 18.74.** Настрой **CI/CD pipeline** (GitHub Actions/GitLab CI): lint (`golangci-lint`), test (`go test -race -cover`), build, push Docker image, deploy to staging. Gate: coverage > 80%, no critical vulnerabilities.

**Упражнение 18.75.** Настрой **GitOps** (ArgoCD/Flux): Git-репозиторий с Kubernetes manifests — единственный источник правды. ArgoCD синхронизирует кластер с Git. Rollback через `git revert`.

---

### 87. Комплексные задачи

**Упражнение 18.76.** Реализуй **полный микросервис "User Service"**:
- gRPC API: `CreateUser`, `GetUser`, `UpdateUser`, `DeleteUser`, `ListUsers` (streaming)
- PostgreSQL для persistence
- Redis для кеша
- Outbox pattern для событий (`UserCreated`, `UserUpdated`)
- gRPC interceptors: logging, auth, metrics, tracing
- Health checks, graceful shutdown
- Dockerfile, docker-compose

**Упражнение 18.77.** Реализуй **"Order Service"**, зависящий от User Service:
- gRPC вызов `user.GetUser` при создании заказа
- Circuit breaker + retry + timeout для вызова User Service
- Saga: `CreateOrder` → `ReserveInventory` → `ProcessPayment` → `ShipOrder`
- Компенсация при ошибке
- Distributed tracing: trace проходит через Order → User → Payment

**Упражнение 18.78.** Реализуй **"Notification Service"**:
- Подписка на events из Kafka/NATS (Outbox от User/Order сервисов)
- Отправка email/SMS/push (заглушки)
- DLQ для необработанных событий
- gRPC API для управления подписками

**Упражнение 18.79.** Реализуй **API Gateway**:
- HTTP REST на входе, gRPC на выходе к сервисам
- Auth: JWT validation, rate limiting per client
- Request/response transformation: REST JSON ↔ gRPC protobuf
- Aggregation: `GET /dashboard` вызывает 3 сервиса параллельно, мержит результаты

**Упражнение 18.80.** Реализуй **"Integration Test Suite"**:
- testcontainers: PostgreSQL, Redis, Kafka
- Запуск всех 3 сервисов в памяти (или в Docker)
- gRPC клиенты, HTTP клиенты
- Проверка end-to-end сценариев: регистрация → создание заказа → уведомление
- Очистка данных между тестами

**Упражнение 18.81.** Реализуй **"Load Test"**:
- `ghz` или `k6` для gRPC/HTTP нагрузки
- 1000 RPS на User Service
- Измерь latency p50/p95/p99
- Найди bottleneck через pprof
- Оптимизируй и повтори

**Упражнение 18.82.** Реализуй **"Chaos Engineering"** (упрощённый):
- `pumba` или `chaos-mesh` для network latency, packet loss, pod kill
- Запусти нагрузку, внедри chaos
- Проверь, что circuit breaker срабатывает, fallback работает, система восстанавливается

**Упражнение 18.83.** Реализуй **"Blue-Green Deployment"**:
- 2 версии сервиса (blue=v1, green=v2)
- Ingress переключает трафик 100% blue → 100% green
- Без downtime, с rollback возможностью
- Проверь через integration tests на green перед переключением

**Упражнение 18.84.** Реализуй **"Canary Deployment"**:
- Istio/Flagger: 5% трафика на v2, 95% на v1
- Мониторинг error rate, latency на v2
- Автоматическое увеличение до 50%, затем 100% при успехе
- Автоматический rollback при degradation

**Упражнение 18.85.** Реализуй **"Multi-region deployment"** (концептуально):
- Active-Active: 2 региона, каждый обрабатывает свой shard пользователей
- Global load balancer (GeoDNS) маршрутизирует
- Replication PostgreSQL (read replicas), Redis Cluster
- Failover: при падении региона — traffic shift в другой

**Упражнение 18.86.** Реализуй **"Event-driven architecture"**:
- Сервисы не вызывают друг друга напрямую
- Все коммуникации через event bus (Kafka/NATS)
- Event schema registry (Avro/protobuf)
- Backward/forward compatibility проверка в CI

**Упражнение 18.87.** Реализуй **"CQRS with Materialized Views"**:
- Command side: PostgreSQL, normalized schema
- Event sourcing: все изменения — события
- Read side: Elasticsearch/ClickHouse, denormalized views
- Projection service: читает события, обновляет read model
- Консистентность: eventual, мониторинг lag

**Упражнение 18.88.** Реализуй **"Schema Evolution"**:
- v1: `User { id, name }`
- v2: `User { id, name, email }` (новое обязательное поле?)
- v3: `User { id, name, email, phone }` (optional)
- Покажи backward compatibility: v2 клиент читает v3 сервер (unknown fields ignored)
- Forward compatibility: v3 клиент читает v2 сервер (default values)

**Упражнение 18.89.** Реализуй **"gRPC-Web"**:
- Envoy proxy транслирует HTTP/1.1 + WebSocket → gRPC
- Браузерный клиент на TypeScript/JavaScript
- Unary и server streaming через gRPC-Web
- Сравни с REST API: типизация, streaming, производительность

**Упражнение 18.90.** Реализуй **"gRPC vs REST benchmark"**:
- Одинаковый функционал: GET/POST user
- gRPC (protobuf binary) vs REST (JSON)
- Замерь: payload size, serialization time, deserialization time, total latency
- Документируй результаты: когда gRPC выигрывает, когда REST проще

**Упражнение 18.91.** Реализуй **"Security hardening"**:
- mTLS между всеми сервисами
- OAuth2/OIDC для пользовательской аутентификации
- RBAC: роли (admin, user, guest) в JWT, проверка в interceptor
- Input validation: protobuf constraints (`validate.rules`)
- Rate limiting, DDoS protection
- Security headers, CORS policy

**Упражнение 18.92.** Реализуй **"Data migration between microservices"**:
- Монолит → микросервисы: strangler fig pattern
- Постепенный перенос: read from new service, write to both
- Dual write consistency: saga/compensation
- Verification: сравнение данных в старой и новой системе
- Cutover: переключение трафика, rollback plan

**Упражнение 18.93.** Реализуй **"Performance optimization"**:
- Профилирование: CPU profile → найди hotspot
- Memory profile → найди аллокации
- goroutine profile → найди утечки
- Оптимизация: pooling, zero-allocation, batching
- Повтори benchmark, документируй improvement

**Упражнение 18.94.** Реализуй **"Disaster Recovery"**:
- RPO (Recovery Point Objective): 5 минут потерь данных допустимо
- RTO (Recovery Time Objective): 15 минут на восстановление
- Backup: PostgreSQL WAL archiving, Redis RDB+AOF
- Restore procedure: документирован, автоматизирован, тестируется регулярно
- Multi-AZ, multi-region failover

**Упражнение 18.95.** Реализуй **"Compliance and Audit"**:
- Все mutation-запросы логируются: кто, когда, что изменил, до/после
- Immutable audit log (append-only, подписанный)
- GDPR: право на забвение — soft delete + anonymization
- Data retention: автоматическое удаление старых данных
- Access log: кто запрашивал какие данные

---

## Итоговая сводка полного курса

| Часть | Тема | Количество |
|-------|------|-----------|
| I | Пакеты и модули | 35 |
| II | Ввод/вывод (fmt) | 15 |
| III | Типы, переменные, константы | 30 |
| III-A | Условные конструкции и циклы | 40 |
| VI | Массивы и слайсы | 30 |
| VII | Мапы | 20 |
| VIII | Структуры | 25 |
| IX | Интерфейсы | 30 |
| X | Обработка ошибок | 20 |
| XI | Логирование | 15 |
| XII | ООП в Go | 45 |
| XIII | Дженерики | 55 |
| XIV | Конкурентность | 125 |
| XV | Работа с сетью | 90 |
| XVI | Работа с БД (PostgreSQL + Redis) | 85 |
| XVII | Тестирование | 110 |
| **XVIII** | **gRPC и микросервисы** | **95** |
| IV | Функции и указатели | 40 |
| V | Работа с файлами | 25 |
| **ИТОГО** | | **950** |

---

### Ключевые моменты для освоения gRPC и микросервисов:

1. **Protobuf — contract first**: начинай с `.proto`, генерируй код. Никогда не редактируй сгенерированный код вручную.
2. **Context propagation**: `trace_id`, `deadline`, `cancellation` передаются через gRPC metadata и context. Всегда пробрасывай контекст.
3. **Idempotency**: mutation-запросы должны быть идемпотентны. Используй `Idempotency-Key` для дедупликации.
4. **Circuit Breaker + Retry + Timeout**: три кита resilience. Не полагайся на дефолты — настраивай под свой SLA.
5. **Observability**: tracing, metrics, logging — must have. Без этого отладка распределённой системы невозможна.
6. **Graceful degradation**: если сервис B недоступен — сервис A должен работать (fallback, cached data, degraded mode).
7. **Event-driven > synchronous**: по возможности используй async коммуникацию (events, message queue) вместо sync RPC. Уменьшает coupling и повышает resilience.
8. **Schema evolution**: всегда думай о backward/forward compatibility. `optional`, `reserved`, не меняй номера полей.
9. **Test in production**: chaos engineering, feature flags, canary deployments. Невозможно протестировать всё в staging.
10. **Security by default**: mTLS, authn/authz, input validation, rate limiting. Не добавляй security "потом".

Если нужно — следующий этап: **GraphQL, WebSocket в production, message queues (Kafka, RabbitMQ, NATS), serverless (AWS Lambda, Cloud Functions), WebAssembly, embedded systems, blockchain/Web3, machine learning (Go + TensorFlow), game development (Ebitengine, Raylib)**. 🚀
