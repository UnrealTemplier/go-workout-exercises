# Оркестрация в Kubernetes

В этом файле собраны уникальные упражнения по теме **"Оркестрация в Kubernetes"**, отсортированные по возрастанию сложности.

---

1. Закрепите версии зависимостей в `go.sum`, используйте кэширование слоёв (сначала копируйте `go.mod` и `go.sum`, затем `go mod download`, потом исходники).

2. Напишите `Makefile` с командами `build`, `test`, `lint`, `docker-build`, `docker-run`.

3. Настройте GitHub Actions (или GitLab CI): пайплайн запускает линтер (`golangci-lint`), тесты с race detector, сборку образа и пуш в Docker Hub при пуше в main.

4. **Deployment и Service манифесты**: Напишите K8s-манифест `deployment.yaml` для запуска вашего Go-приложения, указав количество реплик равным 3. Напишите манифест `service.yaml` типа `ClusterIP` (или `NodePort`), который будет балансировать входящий трафик между вашими тремя подами (Pods).

5. **Конфигурация через ConfigMap и Secrets**: Не храните пароли и адреса БД в кодовой базе. Напишите манифесты `ConfigMap` (для хранения адреса СУБД) и `Secret` (для хранения пароля к базе в зашифрованном Base64 виде). Настройте манифест `deployment.yaml` так, чтобы эти данные автоматически пробрасывались в контейнер с Go в виде переменных окружения.

6. Автоматизируйте версионирование: при пуше тега `v*` запускайте goreleaser для сборки бинарников под разные платформы и создания GitHub Release.

7. Напиши **Deployment manifest**: `apiVersion: apps/v1`, `kind: Deployment`. `replicas: 3`, `selector: matchLabels: app: myapp`. `template: spec: containers: - name: app, image: myapp:v1, ports: - containerPort: 8080`. Примени: `kubectl apply -f deployment.yml`.

8. Напиши **Service manifest**: `kind: Service`, `type: ClusterIP`, `selector: app: myapp`, `ports: - port: 80, targetPort: 8080`. Покажи internal load balancing между pods.

9. Напиши **ConfigMap**: `kind: ConfigMap`, `data: DATABASE_URL: postgres://...`, `LOG_LEVEL: info`. Подключи в pod: `envFrom: - configMapRef: name: app-config`. Покажи externalized configuration.

10. Создайте минимальный **Pod** манифест для вашего Go-приложения. Запустите через `kubectl apply`.

11. Напиши **Secret**: `kind: Secret`, `type: Opaque`, `stringData: DB_PASSWORD: secret123`. Base64 encoding автоматический. Подключи: `env: - name: DB_PASSWORD, valueFrom: secretKeyRef: name: app-secrets, key: DB_PASSWORD`. Покажи secrets management (но не для production — use external secrets operator).

12. Создайте **Deployment** с 3 репликами. Изучите, как K8s поддерживает желаемое состояние.

13. Создайте Kubernetes манифесты: `Deployment`, `Service`, `ConfigMap` для вашего приложения. Настройте количество реплик и ресурсы (requests/limits).

14. **Настройка Liveness и Readiness Probes**: Интегрируйте в веб-сервер Go два диагностических эндпоинта:
    * `/live` (Liveness) — возвращает статус `200 OK`, если приложение просто запущено и не зависло мертвой блокировкой.
    * `/ready` (Readiness) — возвращает `200 OK` только тогда, когда приложение успешно установило соединение с БД и готово обрабатывать трафик пользователей.
    Пропишите опрос этих эндпоинтов в K8s-манифесте пода в секциях `livenessProbe` и `readinessProbe`.

15. Добавьте **Resource requests и limits** для CPU и memory. Что произойдёт при превышении limits? (OOMKilled для memory, throttling для CPU).

16. Напиши **Job**: `kind: Job`, `template: spec: restartPolicy: OnFailure`, `containers: - command: ["./migrate", "up"]`. Запускается один раз, до completion. Покажи database migrations as K8s Job.

17. Настройте **Liveness probe** через HTTP GET `/healthz`. K8s будет перезапускать поды, которые не отвечают.

18. Напиши `Deployment.yaml` для своего Go-приложения (replicas: 3). Примени `kubectl apply -f`. Проверь статус подов.

19. Напиши **CronJob**: `kind: CronJob`, `schedule: "0 2 * * *"` (daily at 2 AM). `jobTemplate` для backup, cleanup, report generation. Покажи scheduled tasks in Kubernetes.

20. Настройте **Readiness probe** через `/readyz`. K8s перестанет отправлять трафик на поды, которые не готовы (например, при недоступности БД).

21. Напиши **Liveness и Readiness пробы**: `livenessProbe: httpGet: path: /healthz, port: 8080, initialDelaySeconds: 10, periodSeconds: 5`. `readinessProbe: httpGet: path: /ready, port: 8080`. Покажи разницу: liveness = restart pod, readiness = remove from service endpoints.

22. Используйте **Startup probe** для медленно стартующих приложений (долгие миграции, прогрев кэша).

23. Добавьте `readinessProbe` и `livenessProbe` в Deployment: проверяйте HTTP-эндпоинты `/health/ready` и `/health/live`.

24. Напиши `Service.yaml` (ClusterIP), чтобы暴露 (expose) деплоймент внутри кластера.

25. Напиши **Startup probe**: `startupProbe: httpGet: path: /healthz, port: 8080, failureThreshold: 30, periodSeconds: 10` (5 минут на старт). Отключает liveness/readiness до success. Покажи для slow-starting applications.

26. Создайте **Service** типа ClusterIP для внутреннего доступа к вашему Deployment.

27. **Конфликт планировщика Go с лимитами CPU (GOMAXPROCS)**: * Объясните в комментариях проблему: если в K8s выставить лимит по процессору `resources.limits.cpu: "2"`, рантайм Go по-прежнему будет видеть общее количество ядер физического сервера (например, 64 ядра) и установит `GOMAXPROCS` равным 64. Это приведет к сильному троттлингу (замедлению) контейнера планировщиком CFS ядра Linux.
    * Решите эту проблему: импортируйте популярную библиотеку `go.uber.org/automaxprocs` в ваш файл `main.go` [465]. Запустите контейнер в K8s и убедитесь по логам, что Go автоматически скорректировал количество потоков под реальные лимиты контейнера [465].

28. Настрой **resource requests/limits**: `resources: requests: memory: "256Mi", cpu: "250m"`, `limits: memory: "512Mi", cpu: "500m"`. Покажи QoS classes: `Guaranteed` (requests=limits), `Burstable` (requests<limits), `BestEffort` (none).

---

29. Настрой **HorizontalPodAutoscaler (HPA)**: `apiVersion: autoscaling/v2`, `metrics: - type: Resource, resource: name: cpu, target: type: Utilization, averageUtilization: 70`. Scale 3 → 10 pods. Покажи autoscaling.

30. Создайте **Service** типа LoadBalancer для production (в облаке создаст внешний LB).

31. **[ConfigMap & Secret]**: Вынеси конфигурацию (порт, лог-уровень) в `ConfigMap`. Чувствительные данные (DSN БД) помести в `Secret`. Подключи их в Pod через `envFrom` или `valueFrom`.

32. Настрой **VerticalPodAutoscaler (VPA)**: `updateMode: "Auto"` или `"Off"` (recommendation only). Автоматически adjusts requests/limits based on actual usage. Покажи difference с HPA (scale up pod vs scale out pods).

33. Настройте `HorizontalPodAutoscaler` по CPU и памяти, проверьте масштабирование нагрузочным тестом.

34. Настрой **StatefulSet**: `kind: StatefulSet`, `serviceName: postgres`, `replicas: 3`, `volumeClaimTemplates: - metadata: name: data, spec: accessModes: [ReadWriteOnce], resources: requests: storage: 10Gi`. Stable network identity: `postgres-0`, `postgres-1`, `postgres-2`. Покажи для databases.

35. Настрой **PodDisruptionBudget (PDB)**: `minAvailable: 2` или `maxUnavailable: 1`. Защита от voluntary disruptions: node drain, cluster upgrade. Покажи, что ensures availability during maintenance.

36. Создайте **ConfigMap** для конфигурации приложения. Смонтируйте его как env vars и как файлы.

37. **Подготовка (Pod)**: Напиши файл `pod.yaml`. Опиши в нем сущность `Pod`, укажи свой Docker-образ (из упр. 591). Запусти через `kubectl apply -f pod.yaml`. Проверь статус через `kubectl get pods`. Посмотри логи через `kubectl logs my-pod`.

38. **[Ingress]**: Настрой `Ingress` (с использованием NGINX Ingress Controller), чтобы маршрутизировать HTTP-трафик с локального хоста на твой `Service`.

39. Настрой **Affinity/Anti-affinity**: `podAntiAffinity: preferredDuringSchedulingIgnoredDuringExecution` — распредели pods across nodes. `podAffinity` — co-locate related pods. `nodeAffinity` — run on specific nodes (GPU, SSD). Покажи topology spread.

40. Создайте **Secret** для sensitive данных (DB password, API keys). Используйте `stringData` для удобного задания в plain text.

41. Настрой **Taints и Tolerations**: `node taint: dedicated=gpu:NoSchedule`. Pod с `toleration: key: dedicated, operator: Equal, value: gpu, effect: NoSchedule` может запланироваться на GPU node. Покажи dedicated node pools.

42. Изучите разницу между env vars и mounted volumes для ConfigMap/Secret (первый не обновляется без restart, второй — обновляется).

43. **Плавное завершение (Graceful Shutdown) в Kubernetes**: При удалении пода K8s отправляет сигнал `SIGTERM` и одновременно удаляет его адрес из Service. Однако эти процессы происходят асинхронно! Напишите код в Go: при получении сигнала `SIGTERM` программа должна подождать 5–10 секунд (продолжая обрабатывать входящие запросы, пока K8s обновляет сетевые маршруты) и только после этого закрыть активные соединения и завершить процесс. Это позволит избежать ошибок 502 (Bad Gateway) у клиентов при деплое новых версий.

44. Настрой **NetworkPolicy**: `kind: NetworkPolicy`, `podSelector: matchLabels: app: database`, `policyTypes: [Ingress]`, `ingress: - from: - podSelector: matchLabels: app: backend`, `ports: - protocol: TCP, port: 5432`. Покажи zero-trust network segmentation.

45. Настройте **ServiceAccount** с минимальными правами для вашего приложения (principle of least privilege).

46. **[Каверзный кейс — GOMEMLIMIT]**: Установи в контейнере лимит памяти `resources.limits.memory: 128Mi`. В коде Go сделай аллокацию большого слайса. Поймай `OOMKilled` (Out of Memory). Установи переменную окружения `GOMEMLIMIT=120MiB`, чтобы GC Go понимал лимиты cgroup и работал эффективнее.

47. **Развертывание (Deployment)**: Поды смертны. Если под удалят, он не воскреснет. Удали под. Напиши `deployment.yaml`. Опиши `Deployment`, который управляет 3 репликами (копиями) твоего приложения. Примени его. `kubectl get pods` должен показать 3 работающих пода.

48. Настрой **ServiceAccount + RBAC**: `kind: ServiceAccount`, `kind: Role` (permissions in namespace), `kind: RoleBinding` (bind SA to Role). Покажи principle of least privilege for pods.

49. Настрой **Headless Service**: `clusterIP: None`. DNS: `postgres-0.postgres.default.svc.cluster.local`. Используй для StatefulSet peer discovery. Покажи, как pods находят друг друга.

50. Создайте **Ingress** с Nginx Ingress Controller для роутинга HTTP-трафика на ваш сервис по доменному имени.

51. Настрой **Ingress**: `kind: Ingress`, `spec: rules: - host: api.example.com, http: paths: - path: /, pathType: Prefix, backend: service: name: myapp, port: number: 80`. TLS: `spec: tls: - hosts: [api.example.com], secretName: api-tls`. Покажи external access.

52. **Самовосстановление (Self-healing)**: Намеренно удали один из подов Deployment'а (`kubectl delete pod <имя>`). Быстро сделай `kubectl get pods`. Увидь, как Kubernetes мгновенно создает новый под на замену убитому, чтобы поддержать требуемое число реплик = 3.

53. **[Init Containers]**: Напиши Pod с `initContainer`, который ждет, пока PostgreSQL поднимется (использует `pg_isready` или `nc -z`). Только после успеха должен стартовать основной контейнер с твоим приложением.

54. Настрой **Ingress Controller** (nginx/traefik): Helm install. `LoadBalancer` service (cloud) or `NodePort` (bare metal). Cert-manager for automatic TLS (Let's Encrypt). Покажи complete ingress stack.

55. Используйте **Ingress annotations** для rate limiting, CORS, rewrite target.

56. Настрой **Custom Resource Definition (CRD)**: `kind: CustomResourceDefinition`, `spec: group: example.com, versions: - name: v1, served: true, storage: true, schema: ...`. Напиши **Operator** на Go (`controller-runtime`): watches CR, reconciles state. Покажи Kubernetes-native automation.

---

57. Создайте **Horizontal Pod Autoscaler (HPA)** на основе CPU utilization.

58. **Спецификация Twelve-Factor App по логированию**: Объясните, почему в контейнеризированных средах и Kubernetes приложение на Go должно писать логи исключительно в стандартные потоки вывода `os.Stdout` и `os.Stderr`, а не в файлы внутри контейнера. Настройте ваш структурированный логгер `slog` на вывод в `Stdout` и объясните, как внешние сборщики логов (например, FluentBit или Loki) собирают эти данные из K8s.

59. Разверните приложение в локальном Kubernetes (minikube/kind) и проверьте доступ через `kubectl port-forward`.

60. Настройте HPA на основе **custom metrics** (например, длина Kafka очереди через Prometheus Adapter).

61. **[Probes (Liveness, Readiness)]**: Настрой в Deployment `livenessProbe` (HTTP GET `/healthz`) и `readinessProbe` (HTTP GET `/readyz`). Сымитируй зависание приложения — K8s должен перезапустить под (Liveness). Сымитируй потерю соединения с БД — K8s должен убрать под из балансировщика (Readiness).

62. **Доступ внутри кластера (Service)**: Поды имеют динамические IP, к ним нельзя обращаться напрямую. Создай `service.yaml` (тип `ClusterIP`). Он создаст стабильный внутренний IP и DNS-имя, балансируя нагрузку между твоими тремя подами.

63. Используйте **Vertical Pod Autoscaler (VPA)** для автоматического подбора requests/limits.

64. Настрой **DaemonSet**: `kind: DaemonSet`. Запускает по одному pod на каждом node. Используй для: log collectors (Fluent Bit), monitoring agents (node-exporter), CNI plugins. Покажи node-level services.

65. Создайте **Pod Disruption Budget (PDB)** для гарантии минимального количества доступных реплик во время maintenance.

66. **Тест Service (Port-Forward)**: Используй команду `kubectl port-forward svc/my-service 8080:80`. Открой в браузере `localhost:8080`. Запросы пойдут с ноутбука в кластер K8s прямо на твой сервис, а оттуда на один из подов с Go-приложением.

67. **[Startup Probe]**: Если твое приложение долго стартует (например, мигрирует БД 30 секунд), Liveness-проба может убить его до старта. Добавь `startupProbe` с `failureThreshold: 30`.

68. Настрой **Init Containers**: `initContainers: - name: wait-for-db, image: busybox, command: ['sh', '-c', 'until nc -z postgres 5432; do sleep 2; done']`. Запускается перед основным контейнером, должен завершиться успешно. Покажи dependency management.

69. Настройте **Network Policies** для изоляции: поды вашего сервиса могут общаться только с БД и другими разрешёнными сервисами.

70. Управляйте секретами: создайте Kubernetes Secret для пароля БД и монтируйте его как переменную окружения в поде.

71. Настрой **Sidecar Pattern**: основной контейнер `app` + sidecar `nginx` (reverse proxy) или `envoy` (service mesh). Shared `emptyDir` volume для Unix socket communication. Покажи separation of concerns.

72. Используйте **Init Containers** для запуска миграций БД перед стартом основного приложения.

73. **Resource Limits и Memory OOMKilled**: Настройте в манифесте пода лимиты по памяти: `resources.limits.memory: "128Mi"`. Напишите тест: запустите внутри Go-приложения код, который аллоцирует в цикле слайс объемом 200 МБ. Запустите деплой в K8s, посмотрите статус пода через `kubectl get pods` и зафиксируйте ошибку завершения процесса по нехватке памяти `OOMKilled` (Exit Code 137).

74. Настрой **Ambassador Pattern**: sidecar проксирует внешние сервисы. `app` обращается к `localhost:5432`, sidecar маршрутизирует на `postgres.external:5432` с TLS, retry, circuit breaker. Покажи transparent proxying.

75. Настройте **Sidecar containers** для логирования (fluent-bit), проксирования (envoy) или service mesh.

76. **Конфигурация (ConfigMap)**: Зашивать настройки в Docker-образ — антипаттерн. Создай `configmap.yaml` со словарем (ключ `SERVER_PORT: 8080`). Прокинь это значение внутрь пода как переменную окружения (Env Var). Сделай так, чтобы твое Go-приложение читало порт из `os.Getenv()`.

77. **[Jobs / CronJobs]**: Напиши `CronJob`, которая запускается каждый день в 2:00 ночи и выполняет Go-скрипт очистки старых записей из БД.

78. Настрой **Adapter Pattern**: sidecar преобразует интерфейс. `app` пишет metrics в файл, sidecar `prometheus-exporter` читает файл, exposes `/metrics`. Покажи interface adaptation.

---

79. Создайте **StatefulSet** для stateful приложений (например, Redis Cluster, Kafka brokers).

80. Настройте TLS-termination на ingress с использованием `cert-manager` для автоматического получения Let's Encrypt сертификатов.

81. Используйте **PersistentVolume** и **PersistentVolumeClaim** для stateful workloads.

82. **[Каверзный кейс — Graceful Shutdown в K8s]**: Настрой в K8s `terminationGracePeriodSeconds: 30`. Когда ты удаляешь Pod, K8s отправляет `SIGTERM`. Убедись, что твое Go-приложение ловит сигнал, прекращает принимать новые запросы (K8s через `preStop` hook удаляет Pod из Endpoints/Service), дорабатывает текущие запросы и завершается, не дожидаясь `SIGKILL`.

83. Напиши **Helm Chart с dependencies**: `dependencies: - name: postgresql, version: 12.x.x, repository: https://charts.bitnami.com/bitnami`. `helm dependency update`. Покажи composite applications.

84. **Секреты (Secrets)**: Создай `secret.yaml` для хранения пароля от БД (значения в yaml должны быть в base64). Прокинь его в под аналогично ConfigMap. Никогда не коммить секреты в Git в открытом виде!

85. Напишите Helm-чарт для вашего приложения с параметризацией образа, реплик, ресурсов и ingress.

86. Настройте **StorageClass** с dynamic provisioning (например, AWS EBS, GCE PD).

87. Напиши **Helm Chart с hooks**: `annotations: "helm.sh/hook": pre-install, "helm.sh/hook-weight": "1"`. Job для database migrations перед установкой. `post-upgrade` для cache warming. Покажи lifecycle hooks.

88. Используйте **CronJob** для периодических задач (cleanup, backups, batch processing).

89. **Использование Distroless-образов**: Напишите `Dockerfile`, финальный этап которого построен на базе минимального образа безопасности от Google: `gcr.io/distroless/static-debian12` [469]. Напишите комментарий со сравнением этого подхода с Alpine: почему отсутствие командной строки `sh`, утилит `curl`, `wget` и пакетных менеджеров в дистролесс-образах резко снижает площадь атаки (attack surface) вашей системы [469].

---

90. Напиши **Kustomize overlay**: `base/` (common resources), `overlays/dev/` (development patches), `overlays/prod/` (production patches: replicas, resources, TLS). `kustomization.yml` с `resources`, `patchesStrategicMerge`, `configMapGenerator`. Покажи `kubectl apply -k overlays/prod`.

91. Настройте **Job** для one-off задач (например, миграции БД при деплое).

92. **Монтирование файлов (Volumes)**: Твоему приложению нужен файл `config.yaml`. Помести содержимое этого файла в ConfigMap. Настрой Deployment так, чтобы он примонтировал (Volume Mount) этот ConfigMap внутрь пода как реальный физический файл (например, в путь `/etc/config/config.yaml`).

93. **[Helm Basics]**: Создай Helm-чарт для своего приложения (`helm create myapp`). Изучи структуру (`templates/`, `values.yaml`). Задеплой через `helm install`.

94. Сравни **Helm vs Kustomize**: Helm — templating (Go templates), package management, releases. Kustomize — patching, no templating, native in kubectl. Покажи, когда что использовать (Helm for packaged apps, Kustomize for own manifests).

---

95. Изучите **DaemonSet** для агентов observability (Prometheus node exporter, Fluentd).

96. Установи **Istio**: `istioctl install`. Enable sidecar injection: `kubectl label namespace default istio-injection=enabled`. Покажи automatic Envoy sidecar injection.

97. Установите ingress-контроллер (nginx) и настройте Ingress-ресурс для маршрутизации внешнего трафика к вашему сервису.

98. Напиши **Helm Chart**: `helm create myapp`. Структура: `Chart.yaml`, `values.yaml`, `templates/`. Параметризуй: `replicaCount`, `image.tag`, `resources`, `ingress.enabled`. Установи: `helm install myapp ./myapp -f values-production.yaml`.

99. Настрой **Istio Traffic Management**: `VirtualService` для routing (weight-based: 90% v1, 10% v2). `DestinationRule` для subsets (version labels). `Gateway` for external access. Покажи canary deployment with Istio.

100. Используйте **Go-templates** в Helm для генерации манифестов: `{{ .Values.replicaCount }}`, `{{ include "myapp.fullname" . }}`.

101. **Обновление версии (Rollout)**: Внеси изменение в код (например, поменяй текст ответа HTTP сервера). Собери и запушь образ с новым тегом (`v1.1.0`). Измени тег в `deployment.yaml` и сделай `kubectl apply`. K8s сделает *Rolling Update*: будет аккуратно гасить старые поды и поднимать новые без даунтайма (Zero Downtime).

102. **[Templating]**: Параметризуй количество реплик, образ и переменные окружения в `deployment.yaml` с помощью Go-шаблонов (`.Values.replicaCount`).

103. Настрой **Istio mTLS**: `PeerAuthentication` with `mtls: mode: STRICT`. All service-to-service communication encrypted and authenticated. `DestinationRule` with `trafficPolicy: tls: mode: ISTIO_MUTUAL`. Покажи zero-trust network.

104. Параметризуйте chart через `values.yaml`: image tag, replica count, resources, env vars.

105. Настрой **Istio Authorization**: `AuthorizationPolicy` — allow/deny based on source (service account), operation (GET /api/users), conditions (JWT claims). Покажи L7 authorization.

106. Используйте `helm template` для рендера манифестов локально без установки в кластер.

107. **Откат (Rollback)**: Ты понял, что версия `v1.1.0` с багом. Выполни команду `kubectl rollout undo deployment/my-app`. Кластер автоматически вернет старую рабочую версию приложения.

---

108. **[Helm Dependencies]**: В `Chart.yaml` добавь зависимость от PostgreSQL (Bitnami chart). Установи чарт так, чтобы вместе с приложением поднялась база данных.

109. Настрой **Istio Observability**: Kiali (topology), Grafana (metrics), Jaeger (tracing). `Telemetry` resource for custom metrics. Покажи unified observability without application changes.

110. Установи **Cert-manager**: `ClusterIssuer` for Let's Encrypt (production/staging). `Certificate` resource automatically requests and renews TLS certificates. `Ingress` or `Gateway` references secret. Покажи automated TLS.

111. Используйте `helm upgrade` для обновления и `helm rollback` для отката.

112. Настрой **External Secrets Operator**: `ExternalSecret` syncs secrets from AWS Secrets Manager / Azure Key Vault / HashiCorp Vault to Kubernetes Secrets. Автоматическая ротация. Покажи no secrets in Git.

113. Опубликуйте chart в Helm repository (ChartMuseum или GitHub Pages).

114. **Пробы жизни (Liveness Probe)**: Что если Go-приложение поймало deadlock (зависло), но процесс не упал? K8s этого не поймет. Напиши в Go хендлер `/health`. В манифесте добавь `livenessProbe`, которая делает HTTP-запрос на этот роут каждые 10 секунд. Если Go ответит 500 (или таймаут) 3 раза подряд — K8s сам убьет и перезапустит контейнер.

115. **[Kustomize]**: Перепиши деплой с Helm на Kustomize. Создай `base/` с манифестами и два оверлея: `overlays/dev` (1 replica, ConfigMap для dev) и `overlays/prod` (3 replicas, ингресс, prod secrets). Запусти `kubectl apply -k overlays/prod`.

116. Настрой **OPA Gatekeeper**: `ConstraintTemplate` (Rego policy), `Constraint` (apply to resources). Пример: `K8sRequiredLabels` — все namespaces must have `cost-center`. `K8sPSP` — no privileged containers. Покажи policy-as-code.

---

117. Используйте **Helm subcharts** (dependencies) для упаковки PostgreSQL, Redis вместе с приложением.

118. Настрой **Pod Security Standards**: `restricted` profile (enforced), `baseline` (warn), `privileged` (audit). `PodSecurity` admission controller. Покажи security hardening without OPA.

119. **Kustomize alternative**: Создайте `kustomization.yaml` с `bases`, `patches`, `overlays` для управления окружениями без шаблонов.

120. **[ArgoCD / Flux (GitOps)]**: Установи ArgoCD в локальный K8s. Подключи свой Git-репозиторий с манифестами. Настрой App-of-Apps паттерн: когда ты делаешь пуш в репозиторий с манифестами (меняешь тег образа), ArgoCD автоматически синхронизирует это в кластере (деплоит новую версию).

121. **Пробы готовности (Readiness Probe)**: Твое приложение стартует 5 секунд (подключается к БД, греет кэш). В это время ему нельзя слать трафик. Добавь `readinessProbe`. K8s не пустит пользовательский трафик в под (он не добавится в endpoints Service), пока эта проба не вернет 200 OK.

122. Настрой **Falco**: runtime security. Rules: `Unexpected outbound connection`, `Write to /etc`, `Privilege escalation`. Alerts to Slack/PagerDuty. Покажи threat detection.

123. Используйте `kustomize edit add configmap` для генерации ConfigMap из файлов.

124. Настрой **Velero**: backup and restore. `Backup` resource: cluster state + PV snapshots. `Schedule` for automated backups. Disaster recovery: restore to new cluster. Покажи DR testing.

125. Настройте overlays: `base/`, `overlays/dev/`, `overlays/staging/`, `overlays/prod/` с разными values.

126. Настрой **Cluster Autoscaler**: scales node groups based on pending pods. `minSize: 3`, `maxSize: 100`. Integration with cloud provider (AWS ASG, GKE node pools). Покажи cost optimization.

127. Сравните Helm и Kustomize: когда что использовать? (Helm — для переиспользуемых charts, Kustomize — для environment-specific customizations).

128. **Graceful Shutdown в K8s**: Когда K8s решает убить под, он сначала шлет сигнал `SIGTERM`, ждет (по умолчанию 30с - `terminationGracePeriodSeconds`), а затем делает `SIGKILL`. Убедись, что твое приложение (из упр. 353) правильно ловит `SIGTERM`, завершает работу `http.Server` и закрывает коннекты к БД. *Проверь по логам кластера, что приложение завершается без ошибок.*

129. **[Metrics Server & HPA]**: Установи Metrics Server в K8s. Настрой `HorizontalPodAutoscaler` для твоего Deployment: таргет CPU utilization = 70%. Сгенерируй нагрузку (busybox в K8s) и посмотри, как K8s автоскейлит поды с 1 до 5.

130. Настрой **Karpenter** (alternative to Cluster Autoscaler): provisioner per workload type. `NodePool` with `requirements` (instance types, architecture, capacity type: spot/on-demand). Faster scaling, better cost optimization. Покажи modern autoscaling.

131. Установите **ArgoCD** в кластер через Helm или official manifests.

132. Настрой **Cost Monitoring**: Kubecost / OpenCost. Per-namespace, per-deployment, per-pod cost allocation. Showback / chargeback. Recommendations: right-sizing, spot instances. Покажи FinOps for Kubernetes.

133. Создайте Git репозиторий с Kubernetes манифестами (или Helm chart) вашего приложения.

134. **Защита от OOM (Resources & GOMEMLIMIT)**: Укажи в манифесте `resources.requests` (гарантированные ресурсы: 100m CPU, 64Mi RAM) и `resources.limits` (потолок: 200m CPU, 128Mi RAM). В Go 1.19+ появилась переменная `GOMEMLIMIT`. Если под потребляет память и упирается в 128Mi, Linux убьет его (OOMKilled). Передай `GOMEMLIMIT=100MiB` в под. Теперь сборщик мусора (GC) в Go будет работать агрессивнее при подходе к этому порогу, спасая приложение от смерти в кластере!

135. **[Prometheus Stack]**: Установи `kube-prometheus-stack` через Helm. Настрой `ServiceMonitor`, чтобы Prometheus начал собирать метрики с твоего Go-приложения (`/metrics`). Проверь метрики в Grafana.

136. Настрой **GitOps at Scale**: `ApplicationSet` (ArgoCD) — generates `Application` per environment, per cluster, per tenant. Matrix generator: `clusters × environments`. Покажи managing 100+ clusters.

137. Настройте ArgoCD **Application** resource, который синхронизирует состояние кластера с Git.

138. Настрой **Multi-Cluster**: `kind: Cluster` (CAPI — Cluster API). Management cluster provisions workload clusters. Federated services, global load balancing, disaster recovery. Покажи fleet management.

139. Изучите концепцию **desired state** vs **actual state** и как ArgoCD устраняет drift.

140. Настрой **Edge/K3s**: lightweight Kubernetes for IoT/edge. `k3s` single binary, SQLite instead of etcd, Traefik ingress. Deploy to Raspberry Pi cluster. Покажи Kubernetes everywhere.

141. Настройте **auto-sync** с **prune** (удаление ресурсов, которых нет в Git) и **self-heal** (восстановление при ручных изменениях).

142. **Публичный доступ (Ingress)**: Service ClusterIP работает только внутри. Чтобы пользователи могли зайти снаружи по красивому URL, установи Ingress Controller (например, NGINX Ingress для Kind). Создай `ingress.yaml` с правилом: трафик для `myapp.local` направить в твой Service. Настрой `/etc/hosts` на ноутбуке и проверь работу.

143. **[Loki for Logs]**: Установи Grafana Loki. Настрой логирование приложения в stdout. Настрой Promtail (агент) для сбора логов подов и отправки в Loki. Найди логи своего пода в Grafana, отфильтруй по `namespace` и `app`.

144. Реализуйте сбор метрик и логов из подов: подключите Prometheus для сбора метрик и Grafana Loki для логов. Настройте дашборд в Grafana, отображающий состояние деплоя.

145. Настрой **Local Development**: `kind` (Kubernetes in Docker) or `k3d` (k3s in Docker). `tilt` or `devspace` for live reload. `telepresence` for remote cluster development. Покажи inner loop optimization.

146. Используйте **sync waves** и **hooks** для контроля порядка деплоя (например, БД → миграции → приложение).

147. Настрой **E2E Testing in Kubernetes**: ` kuttl` or `helm test`. Spin up test namespace, deploy app + dependencies, run tests, cleanup. CI integration: `kind` cluster in GitHub Actions. Покажи testing in production-like environment.

148. Настройте **ApplicationSet** для автоматического создания Applications для множества микросервисов или окружений.

149. **Rolling Update**: Настройте `strategy.rollingUpdate.maxSurge: 25%` и `maxUnavailable: 0` для zero-downtime деплоя.

150. **Автомасштабирование (HPA - Horizontal Pod Autoscaler)**: Если нагрузка выросла, K8s может сам увеличивать число подов. Создай HPA-ресурс, который следит за CPU. Укажи правило: если среднее использование CPU > 70%, масштабировать до 10 реплик. Запусти утилиту нагрузочного тестирования (например, `hey`) на свой Ingress и смотри (`kubectl get hpa`), как кластер сам поднимает новые инстансы твоего приложения!

151. **[OpenTelemetry Collector]**: Установи OTel Collector в K8s. Настрой Go-приложение на отправку трейсов на адрес `otel-collector:4317`. Collector должен пересылать трейсы в Jaeger.

152. Спроектируй **полный production pipeline**:
- Developer pushes code → GitHub Actions: lint, test, build, scan
- Merge to `main` → build Docker image, push to registry, update Helm chart
- ArgoCD detects change → sync to staging cluster
- Automated tests in staging → promote to production
- Argo Rollouts: canary 1% → 10% → 50% → 100%
- Monitoring: SLO verification, automatic rollback on degradation
- Incident response: PagerDuty, runbook, post-mortem

---

153. Используйте **Kustomize + ArgoCD** для управления multiple clusters (dev, staging, prod) из одного Git repo.

154. Интегрируйте ArgoCD с CI: CI билдит и пушит Docker image, обновляет tag в Git репозитории, ArgoCD автоматически деплоит.

155. Настройте алерты в Prometheus AlertManager: если количество подов падает ниже заданного или растёт процент 5xx.

156. **GitOps**: Git = single source of truth. Всё declarative, всё versioned. ArgoCD/Flux обеспечивают convergence (cluster state = desired state). Self-healing, audit trail, easy rollback.

157. Настройте **ArgoCD Image Updater** для автоматического обновления image tags при появлении новых версий в registry.

158. **Пакетный менеджер Helm**: YAML-файлов стало слишком много, и они не переиспользуемые. Установи `Helm`. Сделай `helm create my-go-app`. Перенеси свои манифесты в шаблоны (Templates). Теперь ты можешь деплоить приложение целиком одной командой `helm install ...`.

159. **[Network Policies]**: Напиши `NetworkPolicy`, которая запрещает ВСЕ входящие подключения к твоему поду, кроме трафика от Pod'а с Ingress-контроллером и от Prometheus.

160. Используйте **Argo Rollouts** для canary и blue-green deployments с автоматическим анализом метрик (Prometheus) для принятия решения о продвижении.

161. **Observability in Production**: если нельзя наблюдать — нельзя управлять. Metrics, logs, traces, alerts, runbooks — часть deployment, не после.

162. Настройте **notifications** в Slack/Teams при успешных/неуспешных sync и деплоях.

163. **[Каверзный кейс — Ephemeral Containers]**: Собери образ `FROM scratch`. В поде упало приложение, но там нет ни `sh`, ни `ls`, ни `cat` для дебага. Используй `kubectl debug -it <pod> --image=busybox --target=<container>` чтобы прицепить контейнер с инструментами к тому же неймспейсу процессов (PID namespace) и изучить файловую систему упавшего приложения.

164. **Helm Values (Мульти-среда)**: Используй файл `values.yaml` в Helm-чарте. Настрой шаблоны так, чтобы при деплое на `staging` создавалась 1 реплика пода, а при деплое на `production` — 5 реплик, используя один и тот же чарт, но разные `values-*.yaml` файлы.

165. **Progressive Delivery**: canary, blue-green, feature flags. Не deploy'и на 100% сразу. Автоматический rollback по SLO degradation. Reduce blast radius.

166. Подготовьте приложение к запуску в облаке (AWS ECS или GCP Cloud Run): создайте соответствующий конфигурационный файл (task definition / service.yaml).

167. **Recreate**: Используйте для приложений, которые не могут работать в нескольких версиях одновременно (например, с несовместимыми миграциями БД).

168. **Cost Awareness**: right-sizing, spot instances, autoscaling, FinOps. Every resource has owner and cost. Optimize continuously.

169. **Blue-Green**: Создайте два идентичных окружения, переключайте трафик через Service selector. Мгновенный rollback.

170. **[Multi-tier App in K8s]**: Разверни систему из трех микросервисов (API Gateway, User Service, Order Service).
    - API Gateway доступен извне через Ingress.
    - Сервисы общаются между собой по gRPC через K8s Service Discovery (DNS имена вроде `user-service.default.svc.cluster.local`).
    - Настрои Liveness/Readiness пробы.
    - Настрой HPA для каждого сервиса.
    - Разверни PostgreSQL (StatefulSet + PersistentVolume) и Redis.
    - Настрой Network Policies (API Gateway может говорить с User/Order, но User/Order не могут принимать трафик извне кластера).
    - Все логи и метрики собираются в Grafana/Prometheus/Loki.

171. **Безопасность (Network Policies)**: Напиши `NetworkPolicy`, которая запрещает ЛЮБЫЕ входящие сетевые соединения к поду с твоей базой данных, КРОМЕ подов твоего Go-приложения (фильтрация по меткам/labels).

172. **Disaster Recovery**: regular DR drills, backup testing, documented runbooks. RPO/RTO defined and tested. Multi-region, multi-cloud if needed.

173. **Canary**: Направляйте 5% трафика на новую версию, мониторьте метрики, постепенно увеличивайте до 100%.

174. Настройте **preStop hook** с `sleep 15` для graceful termination: K8s отправляет SIGTERM, приложение завершает текущие запросы, но load balancer ещё некоторое время отправляет трафик.

175. **Developer Experience**: local development (kind, tilt), fast feedback loop (< 5 min from push to deploy in dev), good documentation, paved paths. Happy developers = productive developers.

176. **Финальный босс (Production-Ready Go Deployment Pipeline):**
    Создайте полный пайплайн деплоя Go-микросервиса:
    
    **Local Development:**
    * Docker Compose с Go приложением, PostgreSQL, Redis, Kafka, NATS, Jaeger, Prometheus, Grafana.
    * Air для hot-reload, Makefile для стандартных команд (`make dev`, `make test`, `make build`).
    
    **CI (GitHub Actions):**
    * Linting через golangci-lint.
    * Unit tests с coverage >80%.
    * Integration tests через testcontainers.
    * Security scan через trivy, gosec.
    * Build multi-arch Docker image с distroless base.
    * Подпись образа через cosign (Sigstore).
    * Генерация SBOM через syft.
    * Push в GHCR с semantic versioning.
    
    **GitOps (ArgoCD):**
    * Отдельный Git repo с Kustomize манифестами.
    * Overlays для dev, staging, prod.
    * Автоматическое обновление image tags через ArgoCD Image Updater.
    * Sync waves: ConfigMap → Secrets → Database migrations → Application.
    * Health checks и auto-sync с self-heal.
    
    **Deployment Strategies:**
    * Dev: Rolling update.
    * Staging: Canary с автоматическим анализом Prometheus метрик (Argo Rollouts).
    * Prod: Blue-green с ручным approval через Slack.
    
    **Kubernetes Resources:**
    * Deployment с PDB (minAvailable: 50%).
    * HPA на основе CPU и custom metrics (request rate).
    * Network Policies для изоляции.
    * ConfigMap и Secrets с rotation через External Secrets Operator.
    * ServiceAccount с минимальными RBAC правами.
    * Ingress с TLS через cert-manager, rate limiting через annotations.
    
    **Observability:**
    * OpenTelemetry Collector как sidecar.
    * Prometheus ServiceMonitor для автоматического discovery.
    * Grafana dashboards через ConfigMap provisioning.
    * Loki для логов через Promtail.
    
    **Security:**
    * Pod Security Standards (Restricted profile).
    * Kyverno policies для enforcement (no root, read-only rootfs, no privileged).
    * Network policies для east-west traffic control.
    * Vault integration через CSI driver для dynamic secrets.
    
    **Disaster Recovery:**
    * Velero для backups кластера и PV.
    * Cross-region replication для критичных данных.
    * Automated failover testing через chaos engineering.

---

177. **[Zero-Downtime Deployment & Chaos]**
    - Настрой CI/CD пайплайн, который собирает образ и обновляет Helm-чарт.
    - ArgoCD разворачивает новую версию с использованием `RollingUpdate` (`maxSurge: 1`, `maxUnavailable: 0`).
    - Во время деплоя запусти скрипт, который постоянно делает HTTP-запросы к API Gateway.
    - **Критерий успеха:** В процессе обновления подов не должно быть ни одной 5xx ошибки или таймаута (благодаря Graceful Shutdown, readiness probes и preStop hooks).
    - **[Chaos Engineering]:** Используй `litmuschaos` или просто удаляй случайные поды (`kubectl delete pod -l app=user-service`). Убедись, что система остается доступной (показывает resilient behavior).
178. **Основы GitOps (Взгляд в будущее)**: Ручной `kubectl apply` или деплой прямо из GitHub Actions считается устаревающим подходом. Установи в кластер (локально) **ArgoCD**. Скажи ему следить за твоим Git-репозиторием с YAML-манифестами. Измени количество реплик прямо в Гитхабе, закоммить. Увидь, как ArgoCD заметит изменение в Git и сам синхронизирует состояние твоего кластера, не требуя никаких секретов кластера в самом GitHub!
179. Реализуйте GitOps-подход: храните Kubernetes манифесты в отдельном репозитории, настройте ArgoCD или Flux для автоматической синхронизации состояния кластера с репозиторием.

---

🎯 Теперь план насчитывает **555 упражнений** — полный путь от основ синтаксиса Go до эксплуатации высоконагруженных систем в Kubernetes. Успешной интенсивной тренировки!

180. **Continuous Improvement**: post-mortems without blame, blameless culture, learning from incidents. SRE practices: error budgets, SLO-driven development, chaos engineering.
