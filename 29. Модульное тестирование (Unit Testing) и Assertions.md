# Модульное тестирование (Unit Testing) и Assertions

В этом файле собраны уникальные упражнения по теме **"Модульное тестирование (Unit Testing) и Assertions"**, отсортированные по возрастанию сложности.

---

1. **Первый модульный тест**: Создайте файл `calc.go` и напишите простую функцию `Add(a, b int) int`. В той же директории создайте файл `calc_test.go` с пакетом `package main`. Напишите тест `TestAdd(t *testing.T)`, который проверяет правильность сложения. Запустите его из терминала с помощью команды `go test`.

2. **Первый тест**: Напиши простую функцию `Add(a, b int) int` в файле `math.go`. Создай файл `math_test.go` (правило именования!). Напиши функцию `TestAdd(t *testing.T)`. Вызови `Add(2, 2)` и сравни с `4`. Если не равно — вызови `t.Errorf("Ожидалось 4, получили %d", result)`. Запусти `go test`.

3. Создайте простейший тест `TestAdd` в файле `main_test.go` рядом с функцией `Add`. Запустите его командой `go test`.

4. Напиши **табличный тест (table-driven test)** для `Add`: слайс структур `{a, b, want int}`. Пройди циклом, вызывай `t.Run(fmt.Sprintf("%d+%d", tc.a, tc.b), func(t *testing.T) { ... })`. Покажи, что `t.Run` создаёт подтесты с именами.

5. Напиши тест с **несколькими `t.Run` в цикле**, где один case падает. Покажи, что остальные case'и **продолжают выполняться** (Go 1.7+). До этого — `t.Fatalf` останавливал весь тест.

6. Запусти тесты с помощью `go test`. Запусти с флагом `-v` (verbose), чтобы видеть детальный вывод.

7. **Error vs Fatal**: В тесте добавь вызов `t.Error()`, а затем `t.Log("Продолжение")`. Убедись, что после `Error` тест продолжается. Замени `Error` на `t.Fatal()`. Убедись, что `Fatal` немедленно прерывает выполнение текущего теста.

8. Напиши функцию `Divide(a, b float64) (float64, error)`. Напиши тесты на: нормальное деление, деление на ноль (проверь `err != nil`), отрицательные числа.

9. Протестируйте функцию деления: при `b == 0` возвращается ошибка. Напишите тест на успешный случай и на ошибку.

10. Запустите тесты конкретной функции с помощью `-run TestAdd`. Используйте регулярные выражения (`-run "TestAdd|TestSubtract"`) для запуска нескольких тестов.

11. **Табличные тесты (Table-Driven Tests)**: Напишите функцию проверки палиндромов `IsPalindrome(s string) bool` [332]. Напишите для неё табличный тест — идиоматичный паттерн для Go [332]. Создайте анонимную структуру со сценариями (поля: название теста, входные данные, ожидаемый результат) и обойдите их в цикле `for`, проверяя корректность работы функции для каждого случая [332].

12. Создайте файл `main_test.go` и попробуйте написать функцию `TestMain`, которая не является тестом, а настраивает окружение (setup) и выполняет cleanup (teardown) до и после всех тестов в пакете.

13. Используй `t.Fatalf` вместо `t.Errorf` в месте, где дальнейшее выполнение теста не имеет смысла (например, если функция вернула ошибку, а мы пытаемся прочитать результат).

14. Напиши тест с **`t.Skip`**: если `runtime.GOOS == "windows"` — пропусти тест с сообщением `"unix-specific test"`. Покажи, что `go test` отмечает его как `SKIP`.

15. Используйте `t.Setenv` для временного изменения переменных окружения в рамках теста. Убедитесь, что они автоматически восстанавливаются после теста.

16. **Table-Driven Tests (Золотой стандарт Go)**: Перепиши `TestAdd`. Создай срез анонимных структур: `tests := []struct{ a, b, expected int }{ ... }`. Заполни его 5-ю разными кейсами (включая отрицательные числа и нули). В цикле обойди срез и проверь каждый кейс.

17. Напиши тест с **`t.Parallel()`**: `func TestFoo(t *testing.T) { t.Parallel(); ... }`. Запусти `go test -parallel 4`. Покажи, что тесты выполняются параллельно (порядок вывода перемешан).

18. Табличные тесты: создайте таблицу тестовых случаев для функции `IsPalindrome(s string) bool`. Проверьте граничные случаи (пустая строка, один символ, пробелы).

19. Используйте `t.TempDir()` для создания временной директории, которая будет автоматически удалена после завершения теста.

20. Напиши **несколько параллельных тестов**, которые модифицируют **глобальную переменную** без синхронизации. Запусти с `-race`. Покажи, что race detector ловит гонку даже в тестах.

21. **Табличный тест (Table-driven test)**: Создай структуру `testCase` с полями `name`, `inputA`, `inputB`, `expected`. Создай слайс кейсов. Пройдись по ним в цикле и вызови `t.Run(tc.name, func(t *testing.T) { ... })`.

22. Протестируйте функцию, которая паникует, используя `defer` и `recover` внутри теста (без использования сторонних библиотек).

23. **Fatal против Error**: Продемонстрируйте разницу между методами логирования ошибок в тестах. Напишите тест, который проверяет два независимых условия. В первом случае при ошибке вызовите `t.Errorf()`, а во втором — `t.Fatalf()`. Запустите тест и убедитесь, что `t.Error` позволяет тесту продолжить выполнение и проверить следующие условия, в то время как `t.Fatal` мгновенно останавливает текущий тест [333].

24. **Подтесты (`t.Run`)**: Доработай упр. 403. Оберни проверку каждого кейса в таблице в `t.Run(name, func(t *testing.T) { ... })`. Запусти `go test -v` и посмотри, как красиво и структурированно выводится результат каждого кейса.

25. Напиши тест с **`t.Fatal` vs `t.Errorf`**: `t.Fatal` немедленно останавливает текущий тест (горутину), `t.Errorf` продолжает. Покажи разницу на примере: после `Fatal` — код не выполняется.

26. Используйте `t.Skip()` для пропуска теста по определенному условию (например, если отсутствует переменная окружения `RUN_INTEGRATION_TESTS=1`).

27. Напиши тест с **`t.Helper()`**: создай вспомогательную функцию `assertEqual(t *testing.T, got, want int)`, вызывай `t.Helper()` в начале. Покажи, что при ошибке stack trace указывает на строку вызова `assertEqual`, а не на `t.Errorf` внутри неё.

28. Используйте подтесты с `t.Run` для табличного теста: каждому случаю дайте имя и запустите параллельно с `t.Parallel`.

29. Используйте `t.Parallel()`, чтобы запускать тесты параллельно. Убедитесь, что ваши тесты действительно независимы друг от друга.

30. Напиши тест с **`t.TempDir()`**: создай файл в `t.TempDir()`. Покажи, что директория автоматически удаляется после теста. Сравни с ручным `os.MkdirTemp` + `defer os.RemoveAll`.

31. Изучи флаг `-run`. Напиши 3 теста: `TestAdd`, `TestSub`, `TestMul`. Запусти `go test -run TestAdd -v`, чтобы выполнился только один.

32. Используйте `t.Cleanup(func() { ... })` для регистрации функций очистки, которые будут вызваны после завершения теста (более удобная альтернатива `defer` для сложных сценариев).

33. Напиши тест с **`t.Chdir()`** (Go 1.24+) или ручным `os.Chdir` + `defer` для возврата. Покажи, что тест может менять рабочую директорию, не влияя на другие тесты.

---

34. Используйте `t.Errorf` для провала теста и `t.Logf` для отладочной информации. Изучите разницу: `Errorf` помечает тест как failed, но продолжает выполнение, `Fatalf` немедленно прерывает тест.

35. Перепишите серию из 5 похожих тестов в один **Table-Driven Test**: слайс структур с полями `name`, `input`, `expected` и циклом `for _, tc := range tests`.

36. **Субтесты (`t.Run`)**: Перепишите ваши табличные тесты из табличных тестов с использованием метода `t.Run(name, func(t *testing.T) { ... })` [334]. Запустите тесты с флагом подробного вывода `go test -v` и посмотрите, как красиво группируются результаты для каждого тест-кейса [334]. Попробуйте запустить только один конкретный субтест с помощью флага фильтрации, например: `go test -run=TestIsPalindrome/empty_string`.

37. **[Каверзный кейс]**: Используй `t.Skip("Skipping because feature is not ready")` внутри теста. Запусти `go test -v` и убедись, что тест помечен как SKIP, а не FAIL.

38. Напишите тест с `t.Helper()`: вспомогательная функция, которая проверяет, что ошибка равна ожидаемой, и вызывает `t.Fatal` при несовпадении. В выводе ошибки должна указываться строка вызова теста, а не хелпера.

39. Используйте `t.Run(tc.name, func(t *testing.T) { ... })` для создания подтестов. Это позволяет запускать конкретный кейс отдельно и видеть понятное имя в выводе.

40. **Покрытие кода (Coverage)**: Напиши функцию с ветвлением (например, валидацию возраста: <0 ошибка, 0-18 ребенок, 18+ взрослый). Напиши тест только для одного условия. Запусти `go test -cover`. Посмотри процент покрытия.

41. Используй `assert.Contains(t, slice, element)`, `assert.Len(t, slice, 3)`, `assert.NotNil(t, ptr)`. Напиши тесты для функций, работающих со слайсами и мапами.

42. Перепиши тесты из 17.1–17.4 с `github.com/stretchr/testify/assert`. Используй `assert.Equal(t, want, got)`, `assert.NoError(t, err)`, `assert.Error(t, err)`. Сравни читаемость.

43. Запустите тесты с флагом `-cover`, чтобы увидеть процент покрытия кода тестами.

44. Изучи флаг `-count`. Запусти тест 10 раз с помощью `go test -count=10` (полезно для поиска плавающих багов / flaky tests).

45. Используй `assert.Panics(t, func() { ... })` и `assert.NotPanics(t, func() { ... })`. Тестируй функцию, которая должна паниковать при неверных аргументах.

46. Используй `assert.Eventually(t, condition, timeout, tick)` для тестирования асинхронного кода. Например: запусти горутину, которая через 100ms меняет флаг. Проверь, что флаг стал true в течение 1 секунды.

47. **Параллельное выполнение тестов**: Сделайте ваши субтесты параллельными, добавив вызов `t.Parallel()` внутри `t.Run` [335]. Объясните в комментариях, почему при запуске параллельных тестов в цикле `for` критически важно правильно передавать переменную итератора цикла в анонимную функцию (или переопределять её внутри цикла локально) [335].

48. Изучите разницу между `cover` и `-covermode=atomic`. Почему `atomic` обязателен при использовании флага `-race`?

49. **Визуализация покрытия (HTML)**: Выполни `go test -coverprofile=c.out`. Затем вызови `go tool cover -html=c.out`. Браузер откроет твой код: зеленым подсвечены протестированные строки, красным — то, куда тесты не зашли. Допиши тесты до 100%.

50. Напишите тест, который покрывает все ветки `if/else` в функции, включая edge cases (nil, пустые значения, границы).

51. Используйте `testing.Short()` для пропуска долгих тестов при быстром прогоне (`go test -short`).

52. Используй `assert.Implements(t, (*io.Reader)(nil), obj)` для проверки, что тип реализует интерфейс. Покажи, что это runtime-проверка, а не compile-time.

---

53. Используйте `require` вместо `assert` там, где при ошибке нет смысла продолжать тест (например, если не получен объект, а дальше идёт обращение к полям).

54. **Пропуск тестов (`t.Skip`)**: Напиши тест, который проверяет что-то тяжелое (или требует интернета). В начале теста добавь `if testing.Short() { t.Skip("Пропуск долгого теста") }`. Запусти обычный `go test -v` (тест пройдет), а затем `go test -v -short` (тест будет пропущен).

55. Протестируйте функцию, которая возвращает ошибку. Убедитесь, что ошибка не только `!= nil`, но и имеет ожидаемый тип/сообщение.

56. **Покрытие кода тестами (Code Coverage)**: Напишите функцию со сложной логикой разветвления (`if-else` цепочки). Напишите для неё несколько тестов, покрывающих лишь часть логики. Запустите тесты с генерацией отчета о покрытии: `go test -coverprofile=coverage.out` [337]. Откройте интерактивный HTML-отчет в браузере с помощью команды `go tool cover -html=coverage.out` [337] и посмотрите, какие строки кода подсвечены красным (не покрыты тестами).

57. **[Каверзный кейс]**: Используй `t.Cleanup(func() { ... })` внутри теста. Убедись, что функции очистки вызываются в обратном порядке (LIFO), даже если тест упал с `t.Fatal`.

58. Напиши функцию, возвращающую sentinel error `var ErrNotFound = errors.New("not found")`. Напиши тест, проверяющий `errors.Is(err, ErrNotFound)` через `assert.ErrorIs`.

59. **Очистка ресурсов через `t.Cleanup`**: Напишите тест, который создает временный файл на диске с помощью `os.CreateTemp`, записывает в него данные, выполняет проверку их чтения, а очистку (удаление файла) регистрирует через `t.Cleanup(func() { ... })` [336]. Напишите комментарий, чем `t.Cleanup` удобнее и безопаснее, чем обычный `defer` внутри теста [336].

60. Создайте отдельный пакет `testdata/` с тестовыми данными (JSON-файлы, CSV). Используйте `os.ReadFile("testdata/sample.json")` — Go игнорирует эту директорию при сборке бинарника.

61. Напиши тест с **`t.Cleanup(func())`**: создай временный файл в тесте, зарегистрируй `t.Cleanup(func() { os.Remove(file) })`. Покажи, что cleanup выполняется даже при `t.Fatal` или панике.

62. Напиши кастомный тип ошибки `ValidationError`. Напиши тест, проверяющий `errors.As(err, &ve)` через `assert.ErrorAs`. Убедись, что `ve` заполнена корректно.

63. Используй `assert.ElementsMatch(t, expected, actual)` для сравнения слайсов без учёта порядка. Покажи разницу с `assert.Equal`, который требует точного порядка.

64. Напиши тест на **обёрнутую ошибку**: `fmt.Errorf("context: %w", ErrNotFound)`. Проверь `errors.Is` — должно работать через цепочку обёрток. Проверь `assert.ErrorIs`.

65. Напиши тест, который проверяет, что функция возвращает ожидаемую ошибку. Используй `errors.Is` или сравнение с сентинель-ошибкой.

66. **Функции-помощники (`t.Helper`)**: Напиши отдельную функцию `assertMatch(t *testing.T, expected, actual string)`, чтобы не дублировать код в тестах. Внутри нее вызови `t.Helper()`. Сделай так, чтобы тест упал. Убедись, что ошибка в логах указывает на строку в самом тесте, а не внутри твоей функции-помощника.

67. Проверьте, что функция паникует: используйте `assert.Panics` / `require.Panics` для функции, которая вызывает `panic` при неверных аргументах.

68. Напиши функцию, которая паникует при `nil` аргументе. Напиши тест с `defer func() { if r := recover(); r == nil { t.Error("expected panic") } }()`. Покажи ручной способ тестирования panic.

69. Используйте `assert.NoError`, `assert.Nil`, `assert.Empty`, `assert.Contains` для разных типов проверок.

70. Перепиши тест на panic с `assert.PanicsWithValue(t, "nil argument", func() { ... })`. Проверь точное сообщение паники.

---

71. **Черный ящик vs Белый ящик**: Твой код лежит в пакете `calculator`. Напиши тесты в пакете `calculator_test` (в том же файле добавь `_test` к названию пакета). Попробуй протестировать приватную функцию (с маленькой буквы) и убедись, что теперь она недоступна. (Тестирование публичного контракта пакета).

72. Используйте `assert.JSONEq` для сравнения JSON-строк (игнорирует пробелы и порядок ключей).

73. Используйте пакет `suite` из testify для создания тестовых наборов в стиле xUnit с методами `SetupSuite`, `SetupTest`, `TearDownTest`.

74. **Параллельные тесты (`t.Parallel`)**: Создай 3 долгих теста (поставь `time.Sleep(1 * time.Second)` в каждый). Запусти `go test`. Они выполнятся за 3 секунды. Добавь `t.Parallel()` в начало каждого. Запусти снова — они должны выполниться за 1 секунду.

75. **Глобальный Setup и Teardown (`TestMain`)**: Создай функцию `func TestMain(m *testing.M)`. Внутри напиши код инициализации (например, логов), затем вызови `code := m.Run()`, и после него — код очистки ресурсов. Вызови `os.Exit(code)`.

---

76. **Тестирование недетерминированного кода**: Напишите функцию `Greet() string`, которая возвращает `"Доброе утро"`, `"Добрый день"` или `"Добрый вечер"` в зависимости от текущего системного времени (`time.Now()`). Сделайте эту функцию тестируемой, передав интерфейс-зависимость `Clock` (с методом `Now() time.Time`) в качестве параметра. Напишите тест, передав туда фейковые (mock) часы, возвращающие фиксированное время.

77. Создайте mock-объект с помощью `mock.Mock` из testify. Реализуйте интерфейс, логируйте вызовы и проверяйте ожидания через `AssertExpectations`.

78. Используйте `mock.On("MethodName", args...).Return(result)` для настройки поведения мока.

79. Проверьте, что метод был вызван ровно N раз с помощью `mock.AssertNumberOfCalls`.

80. **Каверзная ловушка параллельных табличных тестов**: Используй таблицу из упр. 403 и вызови `t.Parallel()` внутри `t.Run`. Если у тебя Go ниже 1.22, тесты могут упасть или проверить только последний кейс (из-за захвата переменной цикла). Осознай, зачем внутри цикла раньше писали `tc := tc` (shadowing).

81. **Использование библиотеки `testify`**: Установите популярный пакет утверждений `github.com/stretchr/testify`. Перепишите ваши базовые проверки в тестах с использованием пакетов `assert` и `require` (например, `assert.Equal(t, expected, actual)`). Объясните в комментариях разницу между поведением `assert` и `require` при падении теста [339].

82. **Библиотека Testify (Assert)**: Установи `github.com/stretchr/testify`. Перепиши тест с `if expected != result { t.Errorf(...) }` на короткий и выразительный `assert.Equal(t, expected, result)`.

83. **Управление запуском пакета тестов (`TestMain`)**: Напишите функцию `TestMain(m *testing.M)` для вашего пакета [340]. Настройте её так, чтобы перед запуском тестов создавалась временная директория для логов, затем выполнялись все тесты с помощью `m.Run()`, а после завершения тестов временная директория гарантированно удалялась [340].

---

84. **Настройка окружения (Setenv)**: Напиши функцию, читающую конфигурацию из переменной окружения (например `PORT`). В тесте используй `t.Setenv("PORT", "8080")`. (Go сам вернет старое значение после завершения теста).

85. Напишите `ExampleAdd()` функцию — это одновременно и документация для `go doc`, и выполняемый тест.

86. Добавьте в Example комментарий `// Output:` с ожидаемым выводом. Go автоматически проверит, что реальный `fmt.Print` совпадает.

87. Создайте Example для конкретной структуры или метода: `ExampleUser_GetFullName()`.

88. Сгенерируйте документацию через `go doc -all ./...` и убедитесь, что примеры отображаются корректно.

89. Используйте `testable examples` как форму документации, которая никогда не устаревает (т.к. ломается при изменениях API).

90. **Assert vs Require**: В `testify` есть пакет `require`. Напиши тест, где сначала парсится JSON (`json.Unmarshal`), а потом проверяются его поля. Ошибку парсинга проверь через `require.NoError(t, err)`. Пойми, что `require` (в отличие от `assert`) делает `t.Fatal` и останавливает тест (чтобы не словить панику при проверке полей nil-объекта).

91. **Сравнение JSON**: Тебе нужно протестировать функцию, генерирующую JSON. Используй `assert.JSONEq(t, expectedJSON, actualJSON)`. Обрати внимание, что этот метод успешно сравнит JSON, даже если ключи идут в другом порядке!

92. **Тестирование HTTP API изнутри (`httptest.ResponseRecorder`)**: Напиши HTTP-хендлер. В тесте создай фейковый запрос `req := httptest.NewRequest(...)` и фейковый писатель `rec := httptest.NewRecorder()`. Вызови `handler(rec, req)`. Проверь статускод через `rec.Code` и тело ответа через `rec.Body.String()`.

93. Сгенерируйте отчёт о покрытии: `go test -coverprofile=coverage.out`, затем `go tool cover -html=coverage.out`. Найдите непокрытые участки и добавьте тесты.

94. Настройте CI (например, GitHub Actions) для автоматического запуска тестов и проверки покрытия.

95. Используйте `testing.T.Cleanup` для освобождения ресурсов (удаление временных файлов, закрытие соединений) после каждого теста.

96. Напишите тест, который временно переопределяет глобальную переменную (например, URL внешнего API) и восстанавливает её после завершения через `defer` или `t.Cleanup`.

---

Теперь полный план насчитывает **395 упражнений**. Этот блок превращает вас в разработчика, который не только пишет код, но и гарантирует его качество через всестороннее тестирование. Успешной тренировки!
